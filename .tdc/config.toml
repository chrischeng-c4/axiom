# tdc workspace config
# Lite tech-design CLI for SDD.
# See: https://github.com/anthropics/cclab (or your fork) for the source spec list.

[platform]
# auto-detected from issue URL on `tdc init`; pin manually if you only use one host.
# default = "github"   # or "gitlab"

[paths]
tech_designs = "tech_designs"
