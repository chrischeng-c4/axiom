# Lumen status

The Standalone GKE live acceptance is controller-run, manual, and paid. It is
separate from Managed/operator GCP acceptance. Candidate CI has no GKE or
`gcloud` claim. Uniform durable-2xx and crash-injection roadmap outcomes are
not complete.

## Scope

This document describes the support contract in the current repository source.
It does not claim that every listed gate ran in this working session. A release
can contain an older contract.

Use the [README](README.md) for the product workflow and capabilities. Use the
[roadmap](ROADMAP.md) for future outcomes and explicit non-goals.

## State definitions

| State | Meaning |
|---|---|
| Supported | The current source has a public contract, an implementation, and a named executable gate for the stated scope. |
| Limited | The current source supports the stated scope, but the Limits cell names a material boundary. |
| Not supported | The behavior is not part of the current product contract. The Evidence cell points to a future outcome or a non-goal. |

## Support matrix

| Surface | ID | State | Supported scope | Limits | Evidence |
|---|---|---|---|---|---|
| Standalone runtime | `standalone-runtime` | Supported | One bare process or container runs `lumen serve`. CLI arguments and environment variables configure it. | The bare binary is loopback and in-memory by default. The shipped image defaults to segment persistence at `/var/lib/lumen/data` and declares that VOLUME; deliberate replacement retention needs a named volume or a caller-managed bind mount. | `cargo test -p lumen --test spec_cli --test cli_convention --test standalone_container_contract` `bash apps/lumen/scripts/standalone-container-smoke.sh bind` |
| Standalone GKE renderer | `standalone-gke-render` | Supported | `lumen standalone gke init` and `lumen standalone gke render` use the shared `StatefulInstancePlan` boundary to produce one StatefulSet plus a separately owned PVC instance. | Render support is separate from the controller-run manual live GKE release gate. That gate checks Standalone `LUMEN_AUTH=in-cluster`, inherited Managed `LUMEN_AUTH=required` continuity, cleanup, and a sanitized receipt. It has not passed until the controller records the receipt. | `cargo test -p lumen --test standalone_gke_render` |
| Standalone authentication default | `standalone-authentication-default` | Supported | `lumen serve` defaults to auth off and makes no Kubernetes review call. The Standalone GKE renderer selects `in-cluster`, which verifies the mounted token for the calling Pod's configured KSA through TokenReview and checks that KSA against `allowedServiceAccounts`; SubjectAccessReview authorizes each operation. | Auth-off is safe only while the listener and network boundary are trusted. | `cargo test -p lumen --test auth_e2e --test authz_matrix_e2e --test standalone_gke_render` |
| Direct kustomize compatibility | `direct-kustomize-compatibility` | Supported | The checked-in overlays run one Standalone Deployment for cluster-local development and compatibility testing. | The Deployment is single-process and in-memory. It is not the Managed path and provides no HA or data-retention promise. | `cargo test -p lumen --test capacity_retire_hpa` |
| GKE zonal acceptance | `gke-zonal-acceptance` | Limited | The current GCP acceptance path provisions and checks Lumen on a zonal GKE Standard environment. | This evidence does not prove a regional, three-zone, quorum-safe production profile. See [GKE regional production profile](ROADMAP.md#gke-regional-production-profile). | `acceptance/gcp/scripts/run.sh` |
| GKE regional production profile | `gke-regional-production-profile` | Not supported | The current acceptance path does not certify GKE Standard Regional as a production environment. | Regional topology, disruption, rollout, security, backup, restore, and failure drills remain target work. | [GKE regional production profile](ROADMAP.md#gke-regional-production-profile) |
| Index and query API | `index-query-api` | Supported | Callers declare collections, write indexed values, and use the query methods declared by current OpenAPI and `lumen spec --shapes`. | Lumen returns IDs and scores. The caller owns and loads source records. | `cargo test -p lumen --test api_e2e --test coverage_gaps_e2e` |
| Raw vector and hash search | `raw-vector-and-hash-search` | Supported | Callers can index raw vectors and perceptual hashes, then use exact or HNSW vector search, RRF, and Hamming-distance search. | The caller or its ingest pipeline must produce the vector or hash value. | `cargo test -p lumen --test vector_e2e --test hash_hamming --test hybrid_rrf` |
| Embedding-model execution | `embedding-model-execution` | Not supported | Lumen does not load or execute an embedding model. | Callers supply raw vectors through the indexing contract. | [Embedding-model execution](ROADMAP.md#embedding-model-execution) |
| Current index write API | `current-index-write-api` | Supported | `/index` merges fields, `docs:replace` replaces complete indexed documents, `docs:unindex` removes a bounded caller-selected ID list, and `docs:truncate` clears one collection's indexed documents while keeping its schema. | Unindex and truncate are atomic per physical shard. Routed callers can observe a mixed cross-shard window and must treat an ambiguous failure as unsafe to retry while writes continue. This row does not promise the Search v2 idempotency, item-atomic, or shadow-generation contract. | `cargo test -p lumen --test api_e2e --test truncate_docs_e2e --test batch_unindex_docs_e2e` |
| Sealed Keyword update persistence | `sealed-keyword-update-persistence` | Supported | Updating the same external ID after a Keyword segment is sealed remains visible after another checkpoint and a cold reopen. The stale value stays absent, and a true deletion stays deleted. | This correction changes only Keyword lookup across sealed and live layers. It does not change Number, Set, or LWW behavior. | `cargo test -p lumen --lib` `cargo test -p lumen --test indexing_durable_oracle` |
| Current external-version writes | `current-external-version-writes` | Supported | Merge items accept field external versions, and document replacements accept document external versions. Stale versions do not replace a newer accepted value. | The current mechanisms do not define collection-wide ownership, uniform versioning, or delete tombstones. | `cargo test -p lumen --test api_e2e` |
| Current request-ID de-duplication | `current-request-id-deduplication` | Limited | Current index requests can use `request_id` for a five-minute process-local de-duplication window. | The key is not payload-bound, does not replay the original response, and does not survive a restart or another process. See [Idempotent write replay](ROADMAP.md#idempotent-write-replay). | `cargo test -p lumen --test api_e2e` |
| Uniform durable write acknowledgement | `uniform-durable-write-ack` | Limited | Current persistent paths provide WAL, fsync, apply, and replicated-commit mechanisms according to their selected backend and policy. | One 2xx response does not yet have the same durable commit-and-apply meaning in every mode. In-memory mode remains ephemeral. See [Durable write contract](ROADMAP.md#durable-write-contract). | `cargo test -p lumen --test api_e2e --test wal_nats_e2e` |
| Item-atomic batch writes | `item-atomic-batch-writes` | Not supported | The current batch APIs do not expose the complete per-item commit, failure, and partial-success contract. | A caller must not infer the Search v2 item-atomic model from current responses. | [Item-atomic batch writes](ROADMAP.md#item-atomic-batch-writes) |
| Versioned delete tombstones | `versioned-delete-tombstones` | Not supported | Current external-version writes do not persist a versioned delete tombstone. | An older delayed write can only be blocked by mechanisms outside the target tombstone contract. | [Versioned deletes and tombstones](ROADMAP.md#versioned-deletes-and-tombstones) |
| Shadow rebuild generations | `shadow-rebuild-generations` | Not supported | Current stream reindex writes into the active collection. | There is no shadow generation, dual-write validation, seal, ETag activation, or rollback. | [Shadow rebuild generations](ROADMAP.md#shadow-rebuild-generations) |
| Unified scoring and filter model | `unified-scoring-filter-model` | Not supported | The current request uses one combined query tree. | It does not have separate typed `ScoringQuery` and `FilterExpr` inputs or their Search v2 score rules. | [Unified search contract](ROADMAP.md#unified-search-contract) |
| Strict search schema types | `strict-search-schema-types` | Not supported | The current schema uses `text`, `keyword`, `number`, `set`, `vector`, and `hash`. | It does not provide the Search v2 strict scalar types or orthogonal `multi` and `facetable` options. | [Strict search schema types](ROADMAP.md#strict-search-schema-types) |
| Search result contract v2 | `search-result-contract-v2` | Not supported | Current ordering, totals, cursor, and collapse shapes remain the 0.4 contract. | The stable external-ID tie-break, typed cursor errors, decimal-string totals, PIT, and source-ID-preserving collapse are not implemented. | [Unified search contract](ROADMAP.md#unified-search-contract) |
| Exact search facets and metrics | `exact-search-facets-metrics` | Not supported | The current search API has no Search v2 terms facets, caller-defined range facets, or top-level metrics. | The exact scope, precision, and wire contract in the querying guide is future work. | [Exact search facets and metrics](ROADMAP.md#exact-search-facets-metrics) |
| Facet resource governance | `facet-resource-governance` | Not supported | The current runtime has no facet-definition, bucket, memory, admission-credit, or exact-or-fail budget contract. | Static complexity and temporary-capacity failures do not yet follow the planned 400 and 429 rules. | [Facet resource governance](ROADMAP.md#facet-resource-governance) |
| Routed facet convergence | `routed-facet-convergence` | Not supported | Shards and coordinators do not produce and merge the planned exact facet state. | Cross-shard totals, precision, resource cleanup, and shard-failure behavior have no facet contract. | [Distributed facet convergence](ROADMAP.md#distributed-facet-convergence) |
| Generated-client search v2 parity | `generated-client-search-v2-parity` | Not supported | Generated clients model only the current committed OpenAPI search surface. | They have no typed Search v2 scoring, filter, facet, metric, total, collapse, or cursor API. | [Generated-client search v2 parity](ROADMAP.md#generated-client-search-v2-parity) |
| Managed search capability activation | `managed-search-capability-activation` | Not supported | Current `/version` and Managed reconciliation do not separate binary, active, and effective compatibility capabilities. | Mixed-version activation and `search_facets_v1` convergence are not implemented. | [Search capability activation](ROADMAP.md#search-capability-activation) |
| OpenAPI declared contract | `openapi-declared-contract` | Supported | `lumen spec`, `GET /openapi.json`, and the committed snapshot expose the methods, paths, operation IDs, schemas, status codes, media types, and security that Lumen currently declares. | This scope covers declared facts only. Cross-request behavior and known missing declarations remain separate surfaces. | `cargo test -p lumen --test spec_cli --test spec_route_parity` |
| Protocol contract completeness | `protocol-contract-completeness` | Limited | OpenAPI owns declared wire shapes. The protocol guide maps cross-request facts to current focused topics and maintained guides. | OpenAPI does not yet declare `X-Read-Consistency`, the complete shared `401`, `413`, `429`, and `500` responses, or the complete NDJSON streaming response. See [Protocol contract completeness](ROADMAP.md#protocol-contract-completeness). | `cargo test -p lumen --test spec_cli --test spec_route_parity --test protocol_transport_e2e` |
| Generated-client source generation | `generated-client-source-generation` | Supported | `lumen spec gen` emits TypeScript, Python, and Rust source for the pinned targets from the current OpenAPI document. | Generated source is created on demand. This row does not promise package publication, streaming parity, typed errors, or that every local language toolchain executed. | `cargo test -p lumen --test spec_gen_e2e` `cargo test -p openapi-codegen` |
| Generated-client runtime parity | `generated-client-runtime-parity` | Limited | One test attempts the same create, index, search, stats, delete, and drop journey with generated TypeScript, Python, and Rust clients. | Each language branch returns without failure when its interpreter, dependency, compiler, Node runtime, or Cargo tool is missing. A green run can therefore execute fewer than three clients. See [Strict generated-client gates](ROADMAP.md#strict-generated-client-gates). | `cargo test -p lumen --test generated_clients_crud_e2e` |
| Generated-client streaming and errors | `generated-client-streaming-and-errors` | Not supported | Generated clients cover ordinary JSON operations. | They do not model the NDJSON stream-reindex request and response or decode Lumen's structured error envelope into one typed API error. | [Generated-client protocol parity](ROADMAP.md#generated-client-protocol-parity) |
| Generated-client request resilience | `generated-client-request-resilience` | Not supported | Generated clients do not apply the planned operation-aware deadline, cancellation, backoff, `Retry-After`, or idempotency-safe retry contract. | Application code must currently make these decisions. | [Generated-client request resilience](ROADMAP.md#generated-client-request-resilience) |
| Generated-client source-integration helpers | `generated-client-source-integration-helpers` | Not supported | Generated clients do not provide a bulk-fetch callback helper that restores Lumen hit order. | Callers currently implement their own ID-list hydration and ordering. | [Generated-client source-integration helpers](ROADMAP.md#generated-client-source-integration-helpers) |
| Versioned client workload template | `versioned-client-workload-template` | Not supported | Lumen does not ship a Managed client-workload Kustomize template. | The existing `k8s/overlays/template` renders a Standalone runtime and is not a client template. | [Versioned client workload template](ROADMAP.md#versioned-client-workload-template) |
| Protocol compatibility policy | `protocol-compatibility-policy` | Not supported | The protocol guide records the current permanent POST twins for dedicated QUERY operations. | No general versioning, breaking-change, deprecation-window, or release-note policy covers the rest of the HTTP contract. | [Protocol compatibility policy](ROADMAP.md#protocol-compatibility-policy) |
| Published generated SDK packages | `published-generated-sdk-packages` | Not supported | Consumers generate source with `lumen spec gen` and can vendor it. | Lumen does not publish or maintain npm, PyPI, or crates.io SDK packages. | [Published generated SDK packages](ROADMAP.md#published-generated-sdk-packages) |
| Direct Managed instance | `direct-managed-instance` | Supported | One namespaced `Lumen` resource renders the stateful workload, Services, configuration, status, and disruption policy. | This is an advanced entry point. The namespace, current capacity catalog, and any referenced Secrets must already exist. | `cargo test -p lumen --features operator --test operator_render --test operator_backup_kubernetes_wiring` |
| Managed embedded data durability | `managed-embedded-data-durability` | Supported | A one-replica Managed runtime mounts its embedded index and AOF at the exact `/var/lib/lumen/data` child path on the retained `raft` PVC while preserving the parent mount. | Replicated Raft mode does not use the embedded child mount. This does not recover data lost before the corrected topology. | `cargo test -p lumen --features operator --test report-4018-app` |
| Managed delegated KSA auth | `managed-delegated-ksa-auth` | Limited | A Managed `Lumen` defaults to required auth. It accepts an audience-bound ServiceAccount token, uses TokenReview for identity, and uses SubjectAccessReview for current per-collection and instance-admin permissions. | The CRD still accepts `auth: disabled`. Current RBAC is external to Fleet, and more routes are anonymous than the target contract allows. See [Managed auth unification](ROADMAP.md#managed-auth-unification). | `cargo test -p lumen --test auth_e2e --test authz_matrix_e2e` |
| Human TokenRequest access | `human-tokenrequest-access` | Supported | `lumen connect` can use kubeconfig identity to mint and refresh a short-lived token for a named ServiceAccount and present it through a loopback proxy. | Kubernetes RBAC must allow the human identity to create a token for that ServiceAccount. Fleet does not grant this issuer permission. | `cargo test -p lumen --test behavior_lumen_claim_cli_connect_query_workflow` |
| Fleet-managed whole-runtime access | `fleet-managed-whole-runtime-access` | Not supported | `LumenFleet` and `Lumen` have no typed `access.allowedServiceAccounts` field, and Fleet does not reconcile client access Roles or RoleBindings. | Current access uses manual or externally managed RBAC and current per-collection resources. | [Managed runtime KSA access](ROADMAP.md#managed-runtime-ksa-access) |
| Generated-client automatic KSA auth | `generated-client-automatic-ksa-auth` | Limited | `lumen spec gen` opts all three generated clients into a generic file-bearer provider for the mounted token of the Pod's configured KSA on exact in-cluster Service DNS HTTP or HTTPS URLs. | This covers generated source only. ManagedKsa private-audience auth, published SDK packages, Fleet adoption, TLS setup, HA, and release publication remain separate. See [Projected KSA client auth](ROADMAP.md#projected-ksa-client-auth). | `cargo test -p openapi-codegen --locked` |
| Operator-managed runtime certificates | `operator-managed-runtime-certificates` | Not supported | Managed reconciliation consumes pre-created serving and peer TLS Secrets. | The operator does not request, rotate, or own leaf certificates. | [Managed runtime certificates](ROADMAP.md#managed-runtime-certificates) |
| Operator-managed client trust | `operator-managed-client-trust` | Not supported | The operator does not publish a runtime CA ConfigMap into allowed client namespaces. | Client trust material is currently supplied outside Lumen reconciliation. | [Managed client trust](ROADMAP.md#managed-client-trust) |
| Fleet instance materialization | `fleet-instance-materialization` | Limited | One cluster-scoped `LumenFleet` merges defaults with each RFC 7386 instance patch and creates or applies one child `Lumen` in each accepted namespace. | `Created` and `Applied` mean that the child resource was materialized. They do not mean that its runtime is searchable. See [Fleet production convergence](ROADMAP.md#fleet-production-convergence). | `cargo test -p lumen --features operator --lib` |
| Fleet LumenSpec overrides | `fleet-lumen-spec-overrides` | Limited | `instances[].spec` can override fields that exist in `LumenSpec`. The controller rejects unknown or invalid merged fields per entry. | The patch is free-form at admission time and cannot configure serving options that have no `LumenSpec` field. See [Runtime configuration parity](ROADMAP.md#runtime-configuration-parity). | `cargo test -p lumen --features operator --lib` |
| Fleet extra environment variables | `fleet-extra-environment` | Not supported | No general per-instance environment-variable surface exists. | Operator-owned and user-owned variables are not yet classified in one runtime contract. | [Runtime configuration parity](ROADMAP.md#runtime-configuration-parity) |
| Fleet extra process arguments | `fleet-extra-arguments` | Not supported | No general per-instance process-argument surface exists. | The operator renders its own command and runtime settings. | [Runtime configuration parity](ROADMAP.md#runtime-configuration-parity) |
| Fleet safe rollout | `fleet-safe-rollout` | Not supported | The current 30-second Fleet pass continues through every declared instance without child-generation or child-Ready gating. | No pause or concurrency policy protects a multi-instance update. | [Fleet safe rollout](ROADMAP.md#fleet-safe-rollout) |
| Fleet runtime readiness aggregation | `fleet-runtime-readiness-aggregation` | Not supported | Fleet status reports materialization results only. | It does not watch child readiness or publish ready and degraded counts. | [Fleet production convergence](ROADMAP.md#fleet-production-convergence) |
| Capacity catalog placement | `capacity-catalog-placement` | Limited | Legacy Managed reconciliation reads `lumen-system/lumen-capacity-catalog` and maps a direct GCE machine type to selector and toleration data. | Empty selectors, tolerations-only placement, and non-default machine types still require the legacy catalog. See [Kubernetes-native placement](ROADMAP.md#kubernetes-native-placement). | `cargo test -p lumen --features operator --test capacity_catalog_client --test capacity_catalog_contract` |
| Kubernetes-native placement | `kubernetes-native-placement` | Limited | A bounded compatibility path accepts a non-empty `nodeSelector` with the default machine type, skips the catalog, and preserves the selector and tolerations. | The full resource, StorageClass, selector, toleration, and topology-intent roadmap contract is not implemented. See [Kubernetes-native placement](ROADMAP.md#kubernetes-native-placement). | `cargo test -p lumen --features operator --test capacity_catalog_client --test operator_render` |
| Fleet child pruning | `fleet-child-pruning` | Supported | `prunePolicy: Retain` leaves an undeclared child running. `prunePolicy: Delete` deletes only the child `Lumen` object. | The default is Retain. Neither value is a PVC-retention promise. | `cargo test -p lumen --features operator --lib` |
| Managed PVC deletion | `managed-pvc-deletion` | Not supported | No `Lumen` data-retention policy or finalizer deletes the instance PVCs. | StatefulSet PVCs can remain after a child is deleted. PV lifecycle follows the StorageClass reclaim policy. | [Managed data retention](ROADMAP.md#managed-data-retention) |
| Runtime readiness dimensions | `runtime-readiness-dimensions` | Limited | `/readyz` and the `Ready` condition report searchable serving availability and selected control-plane blockers. | Searchable, writable, replication, configuration, capacity, and degraded state are not exposed as independent complete dimensions. Storage-full read-only mode can stay searchable. See [Fleet production convergence](ROADMAP.md#fleet-production-convergence). | `cargo test -p lumen --features operator --lib` |
| Fixed shard and replica topology | `fixed-topology` | Supported | `shardCount`, `replicasPerShard`, and `voterCount` define a fixed serving topology. | Replica count changes are not automatic. Replicated peers require a pre-existing peer TLS Secret. | `cargo test -p lumen --features operator --test operator_render` |
| Deterministic consensus conformance | `deterministic-consensus-conformance` | Supported | The deterministic Raft host controls ticks and message delivery, replays a fixed adversarial corpus, minimizes failures, and rejects the declared unsafe mutants. | This harness is deterministic evidence. It is not production network testing. | `cargo test -p raft-runtime --test adversarial_recovery` `cargo test -p lumen --features raft-wal --test legacy-3073-app` |
| Bounded Raft shutdown and failover | `bounded-raft-shutdown-and-failover` | Supported | A Raft-backed Lumen process stops proposal admission, syncs its local AOF, gives Raft one four-phase shutdown deadline, reports the terminal result, and then closes or aborts the peer listener. An acknowledged write survives leader shutdown and failover. | This does not add Standalone high availability. It does not certify GKE. | `cargo test -p lumen --features raft-wal --test raft_shutdown_failover` `cargo test -p raft-runtime` |
| Per-shard failure-domain placement | `per-shard-failure-domain-placement` | Not supported | Current scheduling does not enforce three-zone placement for the three voters of each shard. | Current whole-runtime spread and anti-affinity do not form the target per-shard HA guarantee. | [Per-shard failure-domain placement](ROADMAP.md#per-shard-failure-domain-placement) |
| Quorum-safe runtime rollout | `quorum-safe-runtime-rollout` | Not supported | Current StatefulSet rollout does not use a Lumen-owned member-at-a-time quorum and replication gate. | Fleet ordering across runtimes cannot replace an in-runtime Raft member rollout. | [Quorum-safe runtime rollout](ROADMAP.md#quorum-safe-runtime-rollout) |
| Automatic shard splitting | `automatic-shard-splitting` | Limited | The operator can start the published shard-split workflow when `maxShardBytes` is set. | The autonomous driver refuses `replicasPerShard` greater than one. See [High-availability shard expansion](ROADMAP.md#high-availability-shard-expansion). | `cargo test -p lumen --features operator --test reshard_driver_e2e` |
| Membership-aware replica autoscaling | `membership-aware-replica-autoscaling` | Not supported | No controller changes the Raft replica layer from load signals. | A reusable planner exists, but Lumen has no safe membership-transition actuator. | [Membership-aware replica autoscaling](ROADMAP.md#membership-aware-replica-autoscaling) |
| Generic HorizontalPodAutoscaler | `generic-horizontal-pod-autoscaling` | Not supported | Kubernetes HPA does not control Lumen serving pods. | HPA cannot perform the required stateful membership transition. | [Generic horizontal pod autoscaling](ROADMAP.md#generic-horizontal-pod-autoscaling) |

## Evidence policy

The commands above are the required gates for each supported scope. This file
names the gates but does not record a test run. Test output, CI results, and
release evidence stay in their execution systems.

Update a row in the same change that alters its public contract, implementation
boundary, or gate. Promote a future outcome into the README and STATUS only
after the implementation and its executable gate exist.
