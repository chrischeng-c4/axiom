[project]
name = "lumen-external-contracts"
version = "0.0.0"
requires-python = ">=3.11"

[tool.aw.python-artifact]
protocol = "aw.python-artifact.v1"
entrypoint = "src/runner.py"
source_roots = ["src"]
dependency_files = ["pyproject.toml", "uv.lock"]
evidence_dir = "evidence"

[tool.aw.python-ec]
protocol = "aw.python-ec.v1"
author = "019fb951-e713-7600-b4a2-bf3949cb7a48"
efficiency_policy = "optional"

[[tool.aw.python-ec.cases]]
id = "topology-contract-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/topology-contract-behavior.py"
promise = "The #2937 topology design admits the canonical 1x1 default, admits one or three voters, carries a user-set shard minimum and a non-voting read-replica count into the admitted topology, separates user policy from current topology, target topology, and generations, and refuses to call an uncommitted render converged."
oracle = "The verifier imports the design and drives its real admission, status, and availability entry points with EC-owned literal specs. Every expected value is transcribed from issue #2937 R1/R3/R5/R7; no expectation is read back out of the design, and no boolean pass flag is accepted in place of an observed value."
target = "rust"
command = "topology-contract-behavior"
evidence_paths = ["evidence/topology-contract-behavior.json"]

[[tool.aw.python-ec.cases]]
id = "topology-contract-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/topology-contract-security.py"
promise = "The #2937 topology design fails closed on zero shard minimums, zero voters, even voter counts, unsupported odd voter counts, the ambiguous legacy replica field, negative read-replica counts, HPA/VPA knobs it does not own, voter-membership mutations with no safe controller, and two Lumen instances placing data members on one node; and it never describes a one-voter shard as surviving unexpected node loss."
oracle = "Each refusal is driven with an EC-owned literal spec and checked against the exact rejection reason named in issue #2937 R2/R4/R5/R6, not merely against 'some rejection happened'. A closing row holds every refusal in the case to a non-empty field path, so an admission that refuses without naming the offending field fails."
target = "rust"
command = "topology-contract-security"
evidence_paths = ["evidence/topology-contract-security.json"]

[[tool.aw.python-ec.cases]]
id = "gke-ksa-rbac-td-behavior"
artifact_id = "artifact:lumen/ksa-rbac-auth"
capability_id = "security-hardening"
use_case_id = "kubernetes-native-request-identity-and-authorization"
dimension = "behavior"
applicability = "td"
test_path = "src/ec-2879.py"
promise = "The #2879 TD source declares the complete run, auth-verification, cleanup, and clean-verification public boundary: Google issuers stop at kube-apiserver, a named client KSA token with audience lumen.axiom.dev reaches Lumen, and SubjectAccessReview is authoritative."
oracle = "The verifier AST-reads the protocol-located TD artifact under a controller-bound source-root digest and checks EC-owned hop, issuer, audience, KSA, authorization-resource, complete four-target public-boundary, and raw-evidence literals. Environment-selected TD roots, producer mirrors, and pass flags are not accepted."
target = "rust"
command = "gke-ksa-rbac-td-behavior"
evidence_paths = ["evidence/gke-ksa-rbac-td-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The current #2879 TD exposes only one producer target and has no source-literal external-contract declaration for the complete orchestration/finalization boundary."
expected = "The TD behavior verifier fails closed on the incomplete public producer target set until the TD stage publishes all four boundary targets and the EC-owned literals."

[[tool.aw.python-ec.cases]]
id = "gke-ksa-rbac-td-security"
artifact_id = "artifact:lumen/ksa-rbac-auth"
capability_id = "security-hardening"
use_case_id = "kubernetes-native-request-identity-and-authorization"
dimension = "security"
applicability = "td"
test_path = "src/ec-2879.py"
promise = "The #2879 TD source fails closed for direct Google or legacy bearer credentials, wildcard TokenRequest grants, unbounded issuer/SAR revocation, non-controller-committed redaction, retained credentials, and non-exact raw cleanup residue queries."
oracle = "The actual TD declaration is checked against EC-owned forbidden credential classes, one-named-KSA scope, 2700-second token limit, 120-second issuer and 360-second SAR limits, controller-committed live/terminal redaction, and exact raw cleanup-query requirements; booleans alone cannot satisfy it."
target = "rust"
command = "gke-ksa-rbac-td-security"
evidence_paths = ["evidence/gke-ksa-rbac-td-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The current #2879 TD does not yet declare the exact named-KSA, bounded-lifetime, raw-revocation, and raw-cleanup security contract."
expected = "The TD security verifier fails closed on the missing literal #2879 external-contract declaration until the TD stage publishes the EC-owned security literals."

[[tool.aw.python-ec.cases]]
id = "gke-ksa-rbac-cb-behavior"
artifact_id = "artifact:lumen/ksa-rbac-auth"
capability_id = "security-hardening"
use_case_id = "kubernetes-native-request-identity-and-authorization"
dimension = "behavior"
applicability = "cb"
test_path = "src/ec-2879.py"
promise = "A fresh, clean-commit GKE auth-only run proves controller-created archive bytes with exact SHA-256 plus immutable GCS MD5/CRC32C and cryptographically verified Cloud Build DSSE provenance/image/deployed-lumen.dev/v1alpha1-CR chain, exact rendered issuer and Lumen RBAC objects, then exactly two controller-challenged raw observed Google issuers mint only bounded named-KSA tokens and Lumen authorizes only their literal least-privilege grants."
oracle = "Only retained raw evidence is read: a controller commitment over clean commit, GCS bucket/object/generation/checksums, and exact archive content digest verified against retained archive bytes; DSSE PAE signature verification against a controller-supplied trusted Cloud Build Ed25519 public key plus SLSA builder identity; controller-challenged raw WhoAmI commands for distinct expected Google principals; the closed rendered-RBAC object set; and closed-world exact TokenRequest, HTTP, TokenReview, SubjectAccessReview, and Lumen-audit rows. The runner never reads Rust or harness source and never launches a cloud command."
target = "rust"
command = "gke-ksa-rbac-cb-behavior"
evidence_paths = ["evidence/gke-ksa-rbac-cb-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The current retained GKE producer does not yet emit controller-bound archive hashes plus Cloud Build attestation, controller-challenged raw issuer acquisitions, closed rendered issuer/Lumen RBAC objects, or exact raw TokenRequest/HTTP/TokenReview/SAR correlation rows."
expected = "The CB behavior verifier fails closed for missing or contradictory provenance, unknown/overbroad rendered RBAC, stale/raw-unbound issuers, or incomplete closed-world correlation observations until the CB/harness stage retains them."

[[tool.aw.python-ec.cases]]
id = "gke-ksa-rbac-cb-security"
artifact_id = "artifact:lumen/ksa-rbac-auth"
capability_id = "security-hardening"
use_case_id = "kubernetes-native-request-identity-and-authorization"
dimension = "security"
applicability = "cb"
test_path = "src/ec-2879.py"
promise = "The same fresh GKE bundle proves exact-schema direct Google/wrong-audience/anonymous rejection without an authenticated TokenReview/SAR/audit subject, public-CLI sibling-mint failure from controller-extracted image bytes with a controller-signed execution transcript, overbroad-grant denial, raw UID-bound unbound grant plus issuer/SAR revocation, controller-committed exact-credential destructive redaction within five seconds, terminal corpus integrity, and cleanup-to-exactly-one-per-class per-tool raw-output query-to-summary chronology."
oracle = "EC-owned literal HTTP/status/bound assertions validate raw RoleBinding delete requests/responses/audit UIDs and strict rendered/pre-allow/deletion/first-denial order; direct Google rows reject any TokenReview/SAR/authenticated audit subject; exact real per-class kubectl/gcloud argv with parsed machine-readable stdout empty lists or stderr NotFound, not producer response summaries; controller-Ed25519-authenticated CLI execution transcript; and a controller-committed complete credential path/class/issuer/audience/observation byte scan that destroys the directory within five seconds followed by a terminal manifest scan across raw, URL-safe encoded, hex, JSON, and credential-field forms. Missing, stale, appended, recreated, mismatched, arbitrary, summarized-only, or foreign-source evidence fails closed."
target = "rust"
command = "gke-ksa-rbac-cb-security"
evidence_paths = ["evidence/gke-ksa-rbac-cb-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The current retained GKE producer does not yet emit source-bound public-CLI failure, raw RoleBinding deletion/poll/audit UID rows, exact every-class verify-clean identities and raw empty responses, or controller-committed live destruction plus terminal full-corpus redaction records."
expected = "The CB security verifier fails closed for uncorrelated direct-Google evidence, summary-only revocation/cleanup, arbitrary/no-op query identities, forged canaries, missing credential destruction, appended terminal files, or nonterminal redaction proof until the CB/harness stage retains the required raw records."
[[tool.aw.python-ec.cases]]
id = "durability-contract-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/durability-contract-behavior.py"
promise = "The #2938 durability design selects one named Raft group for every new production shard including one-voter shards, routes index/delete/schema/admin mutations through durable Raft commit-and-apply, admits Raft in production, preserves group identity and durable format through learner or voter extension, and maps typed persistence failure to durability-unavailable degraded status. The closest existing topology capability and use case are reused because this durability path is the production shard topology's persistence dimension."
oracle = "The verifier imports and drives the pure durability design with EC-owned literal profile, shard, group, membership, mutation, and persistence-failure inputs. Every expected value is transcribed from #2938 R1/R2/R3/R5/R7; no expected value is read from the design, and no design-computed validity flag is accepted in place of the observed plan, route, identity, format, result, or status value."
target = "rust"
command = "durability-contract-behavior"
evidence_paths = ["evidence/durability-contract-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2938 pure durability design modules have not landed, so the EC import fails closed."
expected = "The behavior verifier remains a known failure until the design exposes the frozen specification, admitted durability verdicts, and pure admission, routing, membership, and failure-mapping entry points."

[[tool.aw.python-ec.cases]]
id = "durability-contract-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/durability-contract-security.py"
promise = "The #2938 durability design fails closed when production selects embedded durability, keeps local/test/detected-legacy exceptions explicit, refuses non-empty legacy embedded state without Raft as migration-required without a bootstrap plan, and never overclaims a typed persistence failure as a successful or ready write. The closest existing topology capability and use case are reused because this durability path is the production shard topology's persistence dimension."
oracle = "The verifier drives pure durability-profile, startup-state, and typed-persistence-failure deciders with EC-owned literal inputs. It checks exact refusal vocabulary, the named offending field, neighbouring admitted profiles, the absence of a bootstrap plan, and structured write/readiness values; expected values are never read from the design and a design-computed boolean cannot satisfy a row."
target = "rust"
command = "durability-contract-security"
evidence_paths = ["evidence/durability-contract-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2938 pure durability design modules have not landed, so the EC import fails closed."
expected = "The security verifier remains a known failure until the design exposes fail-closed profile and startup verdicts plus structured persistence-failure status and write results."

[[tool.aw.python-ec.cases]]
id = "capability-2324-behavior"
artifact_id = "artifact:lumen/capability-contracts-and-shared-ownership"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/capability-2324-behavior.py"
promise = "The #2324 pure ownership design declares the ordered reusable Lumen gates and wrapper-before-pass policy, one canonical shared owner or Lumen-domain owner for every checked concern, its capability and integration-seam linkage, a total mixed-failure partition, and the exact passed or bounded app-domain tracked_skip terminal values. The closest existing capability and use case are reused because this issue verifies the service platform and ownership boundary that includes Lumen's Kubernetes deployment topology."
oracle = "The verifier imports and drives the pure capability-ownership design with EC-owned literal gate, concern, owner, capability, seam, failure-owner, and issue inputs. Every expected value is transcribed from #2324 R1/R2/R3/R4/AC2/AC3; no expected value is read from the design, and no design-computed boolean is accepted in place of the observed plan, inventory, partition, action, or terminal value."
target = "rust"
command = "capability-2324-behavior"
evidence_paths = ["evidence/capability-2324-behavior.json"]


[[tool.aw.python-ec.cases]]
id = "capability-2324-security"
artifact_id = "artifact:lumen/capability-contracts-and-shared-ownership"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/capability-2324-security.py"
promise = "The #2324 pure ownership design fails closed for mutable, ownerless, multiply owned, shared-and-domain, or unlinked ownership declarations; refuses unknown failure owners; refuses any shared/non-domain tracked skip; and refuses an app-domain skip without a bounded issue. The closest existing capability and use case are reused because this issue verifies the service platform and ownership boundary that includes Lumen's Kubernetes deployment topology."
oracle = "The verifier drives explicit hostile pure-model inputs at ownership validation, failure partitioning, and terminal-result entry points. It checks EC-owned literal refusal vocabulary and field paths alongside neighbouring admitted inputs in the behavior case; expected values are never read from the design, and no design-computed boolean can satisfy a row."
target = "rust"
command = "capability-2324-security"
evidence_paths = ["evidence/capability-2324-security.json"]

[[tool.aw.python-ec.cases]]
id = "verification-2328-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/verification-2328-behavior.py"
promise = "The #2328 pure verification design passes only a complete reusable-gate record, classifies shared, non-domain, and app-domain failures without collapsing them, and splits a mixed failure into rerun-required shared work plus an issue-backed app-domain tracked skip. The closest existing topology capability and use case are reused because this local-runtime/domain verification contract governs the production topology test journey."
oracle = "The verifier imports and drives the pure verification record validator, ownership classifier, mixed-failure splitter, and terminal decider with EC-owned literal records and failures. Every expected value is transcribed from #2328 R1/R3/R4/AC1/AC3; no expected value is read from the design, and no design-computed boolean is accepted in place of the observed terminal, ownership, slice, disposition, or issue reference."
target = "rust"
command = "verification-2328-behavior"
evidence_paths = ["evidence/verification-2328-behavior.json"]

[[tool.aw.python-ec.cases]]
id = "verification-2328-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/verification-2328-security.py"
promise = "The #2328 pure verification design fails closed for non-zero or zero-work gates and missing required reproducibility evidence; it refuses tracked_skip for shared or non-domain failures and for app-domain failures lacking a bounded issue, and it accepts no unknown ownership class. The closest existing topology capability and use case are reused because this local-runtime/domain verification contract governs the production topology test journey."
oracle = "The verifier drives the pure terminal decider and ownership classifier with EC-owned literal incomplete records and forbidden failure classifications. It checks exact refusal vocabulary, named offending fields, and the neighbouring admitted complete/app-domain-with-issue cases; expected values are never read from the design and a design-computed boolean cannot satisfy a row."
target = "rust"
command = "verification-2328-security"
evidence_paths = ["evidence/verification-2328-security.json"]

[[tool.aw.python-ec.cases]]
id = "cli-spec-codegen-2334-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/cli-spec-codegen-2334-behavior.py"
promise = "The #2334 CLI-spec-codegen design admits only the exact reusable Lumen CLI/spec/codegen gate with successful non-zero work and complete evidence, admits complete two-path cleanup receipts, and permits terminal Passed or issue-backed app-domain-only TrackedSkip results while retaining their observable terminal data. The closest existing topology capability and use case are reused because the repository inventory has no dedicated verification-gate capability/use case."
oracle = "The verifier imports and drives the pure CLI-spec-codegen admission, cleanup, and terminal decision entry points with EC-owned literal records. It checks the required Cargo command, named gate observations, evidence fields, cleanup receipt categories, and terminal result values; no expected value is read from the design and no design-computed boolean is accepted as a row observation."
target = "rust"
command = "cli-spec-codegen-2334-behavior"
evidence_paths = ["evidence/cli-spec-codegen-2334-behavior.json"]

[[tool.aw.python-ec.cases]]
id = "cli-spec-codegen-2334-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/cli-spec-codegen-2334-security.py"
promise = "The #2334 CLI-spec-codegen design fails closed for a changed reusable command, nonzero or zero-work gate observations, missing provenance, absent grammar observation, TODO or invalid-shell scaffolds, incomplete cleanup, and shared/non-domain/mixed terminal failures. The closest existing topology capability and use case are reused because the repository inventory has no dedicated verification-gate capability/use case."
oracle = "The verifier supplies explicit invalid values to each pure decision entry point and checks the exact rejection vocabulary, named offending field, neighbouring admitted command, and open terminal type. Expected values are EC-owned literals; no value is read back from the design and no validity boolean can satisfy a row."
target = "rust"
command = "cli-spec-codegen-2334-security"
evidence_paths = ["evidence/cli-spec-codegen-2334-security.json"]

[[tool.aw.python-ec.cases]]
id = "verification-2337-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/verification-2337-behavior.py"
promise = "The #2337 pure verification design classifies failures into closed shared-repair or app-domain-trackable dispositions, splits mixed input without loss or duplication, passes only completed reruns with no unresolved failures, and permits tracked_skip(#issue) only for the app-domain-only path. The closest existing topology capability and use case are reused because this verification journey is the cross-service topology-and-security evidence dimension."
oracle = "The verifier imports and drives the pure failure classifier, mixed-failure splitter, and terminal-result decider with EC-owned literal failures, classifications, issue references, and rerun inputs. Every expected value is transcribed from #2337 R3/R4/AC3; no expected value is read from the design and no design-computed boolean can satisfy a row."
target = "rust"
command = "verification-2337-behavior"
evidence_paths = ["evidence/verification-2337-behavior.json"]

[[tool.aw.python-ec.cases]]
id = "verification-2337-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/verification-2337-security.py"
promise = "The #2337 pure verification design fails closed for unknown failure ownership, shared or mixed failures claimed as tracked_skip, incomplete reruns, and multiple issue references; it names the invalid owner, classifications, or rerun gate rather than overclaiming a terminal result. The closest existing topology capability and use case are reused because this verification journey is the cross-service topology-and-security evidence dimension."
oracle = "The verifier drives the pure classifier, splitter, and terminal decider with EC-owned forbidden owners, shared and mixed classifications, rerun state, and issue-reference counts. It checks exact refusal vocabulary, the named offending input, and a neighbouring app-domain admission; expected values are never read from the design and a design-computed boolean cannot satisfy a row."
target = "rust"
command = "verification-2337-security"
evidence_paths = ["evidence/verification-2337-security.json"]

[[tool.aw.python-ec.cases]]
id = "container-kustomize-2343-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/container-kustomize-2343-behavior.py"
promise = "The #2343 container-and-Kustomize decision design classifies shared and non-domain failures as shared repair work, permits an issue-backed tracked skip only for app-domain failure, splits mixed failures into repair-and-rerun plus separately tracked domain work, and allows terminal pass only after a successful final gate. The closest existing topology capability and use case are reused because this is the Kubernetes deployment verification and ownership-classification dimension."
oracle = "The verifier imports and drives the pure #2343 ownership and terminal-result deciders with EC-owned literal owners, bounded issue numbers, and final-gate states. Every expected action, result, and issue number is transcribed from R3/R4/AC3; no expected value is read from the design and no design-computed boolean is accepted in place of the observed decision values."
target = "rust"
command = "container-kustomize-2343-behavior"
evidence_paths = ["evidence/container-kustomize-2343-behavior.json"]

[[tool.aw.python-ec.cases]]
id = "container-kustomize-2343-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/container-kustomize-2343-security.py"
promise = "The #2343 container-and-Kustomize decision design fails closed when an app-domain skip lacks a bounded issue, names the invalid issue field at every classification and terminal-result entry point, never drops a mixed shared repair-and-rerun slice, and keeps an unrepaired or unrerrun shared failure open. The closest existing topology capability and use case are reused because this is the Kubernetes deployment verification and ownership-classification dimension."
oracle = "The verifier drives every pure #2343 entry point with EC-owned invalid zero and valid bounded issue identities plus shared and domain ownership inputs. It checks exact refusal vocabulary, named fields, neighbouring admitted inputs, mixed shared-action preservation, and open terminal values; expected values are never read from the design and a design-computed boolean cannot satisfy a row."
target = "rust"
command = "container-kustomize-2343-security"
evidence_paths = ["evidence/container-kustomize-2343-security.json"]

[[tool.aw.python-ec.cases]]
id = "kind-verification-2348-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/kind-verification-2348-behavior.py"
promise = "The #2348 pure Kind-verification design totally partitions failures into shared/non-domain and app-domain-only ownership, preserves both slices of a mixed failure, returns passed for a failure-free record, and returns tracked_skip only for an issue-backed app-domain-only record. The closest existing topology capability and use case are reused because this is the Kind deployment verification decision dimension."
oracle = "The verifier imports and drives the pure Kind-verification classification and terminal-admission entry points with EC-owned literal failures and records. Every expected value is transcribed from #2348 R3/R4/AC3; no expectation is read back out of the design, and no design-computed validity flag is accepted in place of a partition, terminal result, refusal reason, or refusal field."
target = "rust"
command = "kind-verification-2348-behavior"
evidence_paths = ["evidence/kind-verification-2348-behavior.json"]

[[tool.aw.python-ec.cases]]
id = "kind-verification-2348-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/kind-verification-2348-security.py"
promise = "The #2348 pure Kind-verification design fails closed when shared/non-domain work or a mixed failure is presented for terminal skipping, requires a non-empty validated issue reference for an app-domain-only tracked skip, and exposes only passed or tracked_skip terminal results. The closest existing topology capability and use case are reused because this is the Kind deployment verification decision dimension."
oracle = "The verifier drives the terminal-admission entry point with EC-owned literal shared, app-domain-only, mixed, and missing-issue records and checks exact terminal vocabulary, refusal reasons, and named offending fields. Every expected value is transcribed from #2348 R3/R4/AC3; no expected value is read from the design and no boolean validity flag substitutes for the observed result or typed refusal."
target = "rust"
command = "kind-verification-2348-security"
evidence_paths = ["evidence/kind-verification-2348-security.json"]

[[tool.aw.python-ec.cases]]
id = "matrix-2349-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/matrix-2349-behavior.py"
promise = "The #2349 pure topology design admits the 1x1, Nx1, 1xR, and NxR matrix with separately recorded shards, voters, and non-voting read replicas; admits unique data-member placement; permits read-replica and machine-type scale-down; and distinguishes the one-voter and three-voter unexpected-node-loss promises. The closest existing topology capability and use case are reused because this is the public GKE topology journey's pure policy dimension."
oracle = "The verifier imports and drives the pure matrix, admission, mutation, placement, and availability design with EC-owned literal topology and placement inputs. Every expected value is transcribed from #2349 R2/R4/R6/AC3; no expectation is read from the design, and no design-computed validity flag is accepted in place of an observed topology value, refusal vocabulary, named field, or availability promise."
target = "rust"
command = "matrix-2349-behavior"
evidence_paths = ["evidence/matrix-2349-behavior.json"]

[[tool.aw.python-ec.cases]]
id = "matrix-2349-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/matrix-2349-security.py"
promise = "The #2349 pure topology design fails closed when data members share a node or an automatic mutation contracts shard count, voter count, or voter/shard-PVC capacity, while retaining explicitly admitted read-replica and machine-type scale-down neighbours. The closest existing topology capability and use case are reused because this is the public GKE topology journey's pure fail-closed policy dimension."
oracle = "The verifier drives the pure placement and topology-mutation deciders with EC-owned literal conflicting placements and transitions. It checks exact refusal vocabulary, the named offending field, and the adjoining permitted scale-down inputs; expected values are never read from the design and a design-computed boolean cannot satisfy a row."
target = "rust"
command = "matrix-2349-security"
evidence_paths = ["evidence/matrix-2349-security.json"]

[[tool.aw.python-ec.cases]]
id = "capacity-2360-behavior"
artifact_id = "artifact:lumen/capacity-retained-gke-journey"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/capacity-2360-behavior.py"
promise = "The #2360 capacity design resumes exactly one interrupted mutation, preserves automatic ownership on unchanged reapply, admits only a feasible stable downgrade, carries committed storage to new members, classifies the one- and three-voter availability promises, assigns shared pools by machine type with distinct nodes, and resumes the blocked generation after correction. The closest existing capability and use-case identifiers are reused because the manifest has no capacity-specific pair."
oracle = "The verifier imports the pure capacity design and drives every listed decision entry point with EC-owned literal inputs. Each expected value is declared in the matrix from #2360 R2-R8; no expected value is read from the design and no design-produced pass boolean is accepted."
target = "rust"
command = "capacity-2360-behavior"
evidence_paths = ["evidence/capacity-2360-behavior.json"]


[[tool.aw.python-ec.cases]]
id = "capacity-2360-security"
artifact_id = "artifact:lumen/capacity-retained-gke-journey"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/capacity-2360-security.py"
promise = "The #2360 capacity design fails closed when another mutation is active, each stable-window/headroom/pool-maximum/cooldown downgrade gate fails, a non-reconstructible PVC would be reclaimed, a namespace-derived pool or same-node shared-pool placement is requested, or capacity is blocked; it retains the healthy old member and requested generation. The closest existing capability and use-case identifiers are reused because the manifest has no capacity-specific pair."
oracle = "The verifier sends explicit forbidden inputs to the pure capacity decision entry points and compares their observed reasons, named pool keys, member identity, and generation with EC-owned literals from #2360 R2/R4/R5/R7/R8. It never accepts a design-produced validity boolean."
target = "rust"
command = "capacity-2360-security"
evidence_paths = ["evidence/capacity-2360-security.json"]

[[tool.aw.python-ec.cases]]
id = "capacity-2362-behavior"
artifact_id = "artifact:lumen/capacity-arbitration"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/capacity-2362-behavior.py"
promise = "The #2362 pure capacity design chooses one deterministic safe PVC-growth, read-replica, vertical-machine, highmem-machine, shard-split, machine-downgrade, or hold outcome from generation-bound technical signals, preserves expansion priority, and carries one generation across recommendation, action, and status. The closest existing topology capability and use case are reused because capacity arbitration is the scaling dimension of Lumen's production shard topology."
oracle = "The verifier imports and drives the pure capacity arbitration, projection, catalog, and status entry points with EC-owned literal signals, state, clock, profiles, and generation records. It observes action kinds, named reasons, named failing constraints, target selection, and recorded values; expected values are never read from the design and no design-computed validity boolean can satisfy a row."
target = "rust"
command = "capacity-2362-behavior"
evidence_paths = ["evidence/capacity-2362-behavior.json"]

[[tool.aw.python-ec.cases]]
id = "capacity-2362-security"
artifact_id = "artifact:lumen/capacity-arbitration"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/capacity-2362-security.py"
promise = "The #2362 pure capacity design fails closed on incomplete, stale, generation-mismatched, mutation-fenced, cooldown, deadband, pre-convergence, and change-limited input; names every projected downgrade constraint; returns CapacityBlocked for an unavailable installed target; never chooses an undeclared fallback; and never accepts mismatched generation records as bound. The closest existing topology capability and use case are reused because capacity arbitration is the scaling dimension of Lumen's production shard topology."
oracle = "The verifier drives each pure refusal entry point with EC-owned forbidden literals and inspects exact action, reason, field, constraint, catalog, and generation values. It also drives neighbouring admitted input where applicable. No expectation is read from the design and no design-computed pass flag can satisfy any row."
target = "rust"
command = "capacity-2362-security"
evidence_paths = ["evidence/capacity-2362-security.json"]

[[tool.aw.python-ec.cases]]
id = "competitor-verification-2376-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/competitor-verification-2376-behavior.py"
promise = "The #2376 pure competitor-verification design admits only an evidence declaration that establishes semantics before comparable peer/workload metrics and cost, records intentional deltas with an app-domain route, repairs shared and non-domain failures, splits mixed ownership, and terminalizes only completed success or a validated bounded app-domain tracked skip. The closest existing topology capability and use case are reused because no inventory entry specifically covers competitor verification."
oracle = "The verifier imports and drives the pure evidence, failure-disposition, and terminal-result deciders with EC-owned literal declarations. Every expected value is transcribed from #2376 R2/R3/R4 and AC2/AC3; no expectation is read from the design and no design-computed validity flag is accepted in place of an observed value."
target = "rust"
command = "competitor-verification-2376-behavior"
evidence_paths = ["evidence/competitor-verification-2376-behavior.json"]

[[tool.aw.python-ec.cases]]
id = "competitor-verification-2376-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/competitor-verification-2376-security.py"
promise = "The #2376 pure competitor-verification design fails closed on metrics-before-semantics, undeclared peer or workload comparability, incomplete metric/cost or delta routing, unbounded or unreproducible app-domain issues, unchecked authoritative existing WIs, and every shared, non-domain, or mixed terminalization attempt. The closest existing topology capability and use case are reused because no inventory entry specifically covers competitor verification."
oracle = "The verifier supplies explicit forbidden inputs to every applicable pure entry point and checks EC-owned refusal vocabulary, the named offending field, and admitted neighbouring declarations. It never derives expected values from design observations and never accepts a design-computed boolean as proof."
target = "rust"
command = "competitor-verification-2376-security"
evidence_paths = ["evidence/competitor-verification-2376-security.json"]

[[tool.aw.python-ec.cases]]
id = "contraction-2528-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/contraction-2528-behavior.py"
promise = "The #2528 deferred contraction design advances only a fully consolidated state through a versioned catalog cutover, keeps rollback and source retirement distinct, exposes retained or separately reclaimable PVC policy without PVC shrink, admits implementation work only after all four measurements pass, preserves v1 independence from merge, and requires all four review decisions. The closest existing topology capability and use case are reused because #2528 is a deferred topology extension."
oracle = "The verifier imports the pure design and drives its real contraction, entry-gate, v1-dependency, review, and child-admission entry points with EC-owned literal inputs. Every expected value is transcribed from #2528 R1-R4 and AC1-AC2; no expected value is read from the design and no design-computed validity boolean is accepted in place of the observed transition, vocabulary, outcome, or missing-decision value."
target = "rust"
command = "contraction-2528-behavior"
evidence_paths = ["evidence/contraction-2528-behavior.json"]


[tool.aw.python-ec.cases.known_failure]
reason = "The #2528 pure design modules (lumen/topology/contraction_spec.py, lumen/topology/contraction_verdict.py, lumen/topology/contraction_admission.py, lumen/topology/contraction_review.py) have not landed, so the behavior verifier fails closed on import."
expected = "The behavior verifier remains a known failure until the design exposes the entry points #2528 names; the gate reports a known_failure that starts passing, so this block expires loudly once the design lands."

[[tool.aw.python-ec.cases]]
id = "contraction-2528-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/contraction-2528-security.py"
promise = "The #2528 deferred contraction design fails closed on unconsolidated WAL, source retirement while rollback is requested, any PVC-shrink vocabulary, each omitted entry-gate measurement, v1 autoscaling or zero-downtime merge dependencies, incomplete decision review, and implementation children requested from a rejected gate. The closest existing topology capability and use case are reused because #2528 is a deferred topology extension."
oracle = "Each refusal is driven by an EC-owned literal request that explicitly supplies the forbidden field and is checked on its reason or named field. The case observes the closed PVC vocabulary and source-retirement value directly, preserves a fully measured neighbouring admission in the behavior case, and never accepts a design-computed pass or validity flag."
target = "rust"
command = "contraction-2528-security"
evidence_paths = ["evidence/contraction-2528-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2528 pure design modules (lumen/topology/contraction_spec.py, lumen/topology/contraction_verdict.py, lumen/topology/contraction_admission.py, lumen/topology/contraction_review.py) have not landed, so the security verifier fails closed on import."
expected = "The security verifier remains a known failure until the design exposes the entry points #2528 names; the gate reports a known_failure that starts passing, so this block expires loudly once the design lands."

[[tool.aw.python-ec.cases]]
id = "observability-2533-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/observability-2533-behavior.py"
promise = "The #2533 pure topology design publishes immutable, durable-state-derived observations for member handoff, Embedded-to-Raft migration, and shard split; produces phase-specific stall decisions with phase age, identity, generation, and safe operator action; and keeps complete-backup status separate from topology mutation status. The closest existing topology capability/use case is reused because #2533 extends its operator-visible topology contract."
oracle = "The verifier imports and drives the real pure observation, phase-age, stall-signal, and backup-status entry points with EC-owned literal states, epochs, thresholds, identifiers, counters, manifests, and reasons. Expected values are transcribed from #2533 R1-R5 and AC2-AC4; the EC compares observed values rather than accepting design-owned validity booleans."
target = "rust"
command = "observability-2533-behavior"
evidence_paths = ["evidence/observability-2533-behavior.json"]


[tool.aw.python-ec.cases.known_failure]
reason = "The #2533 pure design modules (lumen.topology.observability, lumen.topology.backup_status, lumen.topology.status) have not landed, so the behavior verifier fails closed on import."
expected = "The behavior verifier remains a known failure until the design exposes the entry points #2533 names; the gate reports a known_failure that starts passing, so this block expires loudly once the design lands."

[[tool.aw.python-ec.cases]]
id = "observability-2533-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/observability-2533-security.py"
promise = "The #2533 pure topology design fails closed for a phase without its own stall threshold, never overclaims a threshold-equal phase as stalled or returns negative phase age, publishes immutable observations, and prevents complete-backup status from carrying topology-mutation kind or phase fields. The closest existing topology capability/use case is reused because #2533 extends its operator-visible topology contract."
oracle = "The verifier drives explicit phase timestamps and explicit per-phase policies, verifies the refusal vocabulary and named missing threshold, and observes backup values directly for the absence of topology-mutation fields. Every expected literal is EC-owned from #2533 R1-R5 and AC2-AC4; no expected value is read from the design and no design-owned boolean decides a row."
target = "rust"
command = "observability-2533-security"
evidence_paths = ["evidence/observability-2533-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2533 pure design modules (lumen.topology.observability, lumen.topology.backup_status, lumen.topology.status) have not landed, so the security verifier fails closed on import."
expected = "The security verifier remains a known failure until the design exposes the entry points #2533 names; the gate reports a known_failure that starts passing, so this block expires loudly once the design lands."

[[tool.aw.python-ec.cases]]
id = "capacity-2538-behavior"
artifact_id = "artifact:lumen/capacity-guidance"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/capacity-2538-behavior.py"
promise = "The #2538 pure capacity-guidance design admits only completed, attested capacity inputs, selects E2 with pd-balanced by default, permits qualified N2 only after explicit E2/pd-balanced rejection, and describes pd-ssd solely as initial-placement/future in v1. The reused topology use case is the closest existing inventory entry; it covers the same Raft-hosted deployment shape but does not claim that #2538 is a topology feature."
oracle = "The verifier imports the capacity design and drives its admission entry point with EC-owned literal specs. Every expected value is transcribed from issue #2538 R4/R5/R6/R7/AC3/AC4; no expectation is read back out of the design, and no design-supplied validity boolean is accepted in place of the observed verdict value."
target = "rust"
command = "capacity-2538-behavior"
evidence_paths = ["evidence/capacity-2538-behavior.json"]


[tool.aw.python-ec.cases.known_failure]
reason = "The #2538 pure design modules (lumen.capacity.spec, lumen.capacity.verdict, lumen.capacity.admission, lumen.capacity.__init__) have not landed, so the behavior verifier fails closed on import."
expected = "The behavior verifier remains a known failure until the design exposes the entry points #2538 names; the gate reports a known_failure that starts passing, so this block expires loudly once the design lands."

[[tool.aw.python-ec.cases]]
id = "capacity-2538-security"
artifact_id = "artifact:lumen/capacity-guidance"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/capacity-2538-security.py"
promise = "The #2538 pure capacity-guidance design refuses price or regional-billing record fields, automatic storage-class migration, the legacy whole-state JSON store, missing completed-storage and bounded-write-amplification prerequisites, and unearned N2; each refusal identifies the offending field and cannot overclaim a threshold or machine recommendation. The reused topology use case is the closest existing inventory entry."
oracle = "The verifier drives every refusal with an EC-owned literal capacity spec and observes exact reason, offending field, or absent recommendation values rather than a design-supplied boolean. Neighbouring admissible inputs prove the refusal predicates are neither ignored nor overbroad."
target = "rust"
command = "capacity-2538-security"
evidence_paths = ["evidence/capacity-2538-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2538 pure design modules (lumen.capacity.spec, lumen.capacity.verdict, lumen.capacity.admission, lumen.capacity.__init__) have not landed, so the security verifier fails closed on import."
expected = "The security verifier remains a known failure until the design exposes the entry points #2538 names; the gate reports a known_failure that starts passing, so this block expires loudly once the design lands."

[[tool.aw.python-ec.cases]]
id = "capacity-2680-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/capacity-2680-behavior.py"
promise = "The #2680 capacity design admits supported serving fields that omit retired autoscaling, excludes retired autoscaling vocabulary, and deletes only an unwanted stale HPA whose name and complete labels exactly match the operator-rendered object. The closest existing topology capability and use case are reused because this is the serving deployment surface's capacity-removal dimension."
oracle = "The verifier imports and drives the pure capacity admission and stale-HPA handoff entry points with EC-owned literal field vocabularies, names, labels, and wanted state. Every expected value is transcribed from #2680 R1/R2/R4/AC1/AC3; no expected value is read from the design, and no design-computed validity flag is accepted in place of the observed outcome, vocabulary, action, refusal, or field path."
target = "rust"
command = "capacity-2680-behavior"
evidence_paths = ["evidence/capacity-2680-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2680 pure capacity design modules have not landed, so the EC import fails closed."
expected = "The behavior verifier remains a known failure until the design exposes the frozen serving-surface admission and stale-HPA cleanup entry points with typed admitted and cleanup decisions."

[[tool.aw.python-ec.cases]]
id = "capacity-2680-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/capacity-2680-security.py"
promise = "The #2680 capacity design fails closed on every retired autoscaling field and replacement CPU, memory, or disk threshold field, identifies the offending field, still admits the neighbouring supported serving surface, and never deletes a stale-HPA candidate with the wrong name. The closest existing topology capability and use case are reused because this is the serving deployment surface's capacity-removal dimension."
oracle = "Each refusal is driven through the pure admission entry point with an EC-owned literal forbidden field and checked against the exact reason and field path, while an independently supplied neighbouring supported field set must be admitted. The stale-HPA handoff receives explicit expected and live identities; no expected value is read from the design and no boolean pass flag substitutes for an observed outcome, reason, field path, or action."
target = "rust"
command = "capacity-2680-security"
evidence_paths = ["evidence/capacity-2680-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2680 pure capacity design modules have not landed, so the EC import fails closed."
expected = "The security verifier remains a known failure until the design exposes structured retired-field and replacement-threshold refusals plus fail-closed stale-HPA cleanup decisions."

[[tool.aw.python-ec.cases]]
id = "peer-identity-2890-behavior"
artifact_id = "artifact:lumen/peer-identity-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/peer-identity-2890-behavior.py"
promise = "The #2890 pure peer-identity design projects an explicitly configured three-key peer TLS Secret read-only with four exact Lumen bindings, selects dedicated HTTPS mTLS transport on port 7374 for replicated members, carries the requested DNS identity and trust domain into mutual client-certificate verification, and permits only the explicit single-replica development non-mTLS mode. The closest existing topology capability and use case are reused because peer identity is the replicated-shard topology security dimension."
oracle = "The verifier imports and drives the pure peer-identity projection, transport, expectation, profile, and status entry points with EC-owned literal specs. Every expected value is transcribed from #2890 R1/R2/R3/R4/R5/R7; no expected value is read from the design, and no design-computed validity flag is accepted in place of an observed plan, policy, expectation, or status value."
target = "rust"
command = "peer-identity-2890-behavior"
evidence_paths = ["evidence/peer-identity-2890-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2890 pure peer-identity design modules have not landed, so the EC import fails closed."
expected = "The behavior verifier remains a known failure until the design exposes the frozen specification, projection, transport, peer-expectation, profile-policy, and status entry points."

[[tool.aw.python-ec.cases]]
id = "peer-identity-2890-security"
artifact_id = "artifact:lumen/peer-identity-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/peer-identity-2890-security.py"
promise = "The #2890 pure peer-identity design fails closed when replicated production omits the explicit peer TLS Secret and when replicated material is absent, incomplete, or unreadable; every refusal uses stable vocabulary and names the Secret, missing key, or offending field, while a readable complete-material neighbour is ready. The closest existing topology capability and use case are reused because peer identity is the replicated-shard topology security dimension."
oracle = "The verifier drives explicit replicated production and single development profile inputs plus absent, incomplete, unreadable, and readable peer-material states. It checks EC-owned refusal reason and named repair values rather than a design-computed boolean; expected values are transcribed from #2890 R4/R7 and are never read from the design."
target = "rust"
command = "peer-identity-2890-security"
evidence_paths = ["evidence/peer-identity-2890-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2890 pure peer-identity design modules have not landed, so the EC import fails closed."
expected = "The security verifier remains a known failure until the design exposes fail-closed profile and material-status verdicts with stable refusal reasons, named fields, and repair messages."

[[tool.aw.python-ec.cases]]
id = "tls-policy-2891-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/tls-policy-2891-behavior.py"
promise = "The #2891 pure TLS policy design consumes externally owned serving and peer Secret surfaces; admits the named client-KSA/SAR path, secure localhost-only Service-DNS connect path, and trusted peer mTLS path; requires coupled external generations with changed active fingerprints; and retains only named public evidence fields. The closest existing topology capability and use case are reused because this is the TLS-policy dimension of Kubernetes-native Lumen topology."
oracle = "The verifier imports the real pure ownership, TLS-policy, TLS-verdict, and evidence-policy entry points and drives them with EC-owned literal inputs. It observes classifications, admitted values, secure connect plan fields, peer routing reachability, rotation values, and evidence classifications; expected values are never read from the design and no generic validity flag can satisfy a row."
target = "rust"
command = "tls-policy-2891-behavior"
evidence_paths = ["evidence/tls-policy-2891-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2891 pure TLS policy, verdict, and evidence-policy design modules have not landed, so the EC imports fail closed."
expected = "The behavior verifier remains a known failure until the design exposes the frozen externally-owned TLS, serving/KSA, connect, peer, rotation, and evidence-policy entry points."

[[tool.aw.python-ec.cases]]
id = "tls-policy-2891-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/tls-policy-2891-security.py"
promise = "The #2891 pure TLS policy design fails closed for direct Google credentials, denied SAR, hostname bypass, plaintext and wrong peer credentials before Raft routing, sensitive retained-evidence fields, and retired issuer ownership. The closest existing topology capability and use case are reused because this is the TLS-policy dimension of Kubernetes-native Lumen topology."
oracle = "The verifier drives each pure refusal entry point with explicit EC-owned bad inputs, checks exact refusal vocabulary and named fields where applicable, preserves nearby secure admissions, observes that rejected peer cases cannot reach Raft, and classifies every named sensitive evidence field. Expected values are EC-owned literals and are never derived from observed design values."
target = "rust"
command = "tls-policy-2891-security"
evidence_paths = ["evidence/tls-policy-2891-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2891 pure TLS policy, verdict, and evidence-policy design modules have not landed, so the EC imports fail closed."
expected = "The security verifier remains a known failure until the design exposes fail-closed serving, connect, peer, evidence, and retired-ownership policy entry points."

[[tool.aw.python-ec.cases]]
id = "catalog-2939-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/catalog-2939-behavior.py"
promise = "The #2939 catalog design admits one-voter non-HA or three-voter HA catalog plans with deterministic hostname and zone placement, exposes immutable versioned catalog-owned topology and schema state, admits catalog and retained-cache serving authority, and advances last-converged state monotonically. The closest existing topology capability and use case are reused because this is the durable catalog-authority dimension of production shard topology."
oracle = "The verifier imports and drives the pure catalog design with EC-owned literal catalog modes, eligible members, state, serving sources, seeds, and cache candidates. Every expected value is transcribed from #2939 R1/R2/R3/R4/R6 and AC1/AC2/AC3; no expected value is read from the design and no design-computed validity flag is accepted in place of an observed plan, state field, decision source, or generation."
target = "rust"
command = "catalog-2939-behavior"
evidence_paths = ["evidence/catalog-2939-behavior.json"]

[[tool.aw.python-ec.cases]]
id = "catalog-2939-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/catalog-2939-security.py"
promise = "The #2939 catalog design fails closed on unsupported modes, insufficient HA voters, operator-only serving authority, foreign-instance and stale bootstrap seeds, stale cache replacement, and mutations without current catalog quorum; each refusal identifies its unsafe field while a neighbouring admissible input remains admitted. The closest existing topology capability and use case are reused because this is the durable catalog-authority dimension of production shard topology."
oracle = "The verifier drives every pure catalog entry point with EC-owned explicit forbidden inputs and checks exact refusal vocabulary, the named offending field, deterministic stale-seed refusal, and neighbouring admitted inputs. Expected values are never read from the design and a design-computed boolean cannot satisfy a row."
target = "rust"
command = "catalog-2939-security"
evidence_paths = ["evidence/catalog-2939-security.json"]

[[tool.aw.python-ec.cases]]
id = "workload-2941-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/workload-2941-behavior.py"
promise = "The #2941 pure workload design keeps existing members in their original per-shard StatefulSets and PVCs as shards grow, emits deterministic peer identity and retained claims, declares hostname and zone voter placement requirements, and derives one public eligible-data Service plus one headless peer Service. This reuses the closest existing topology capability and use case because #2941 extends the same Kubernetes topology boundary."
oracle = "The verifier imports the pure workload design and drives its real workload, identity, placement-requirement, and service-plan entry points with EC-owned literal inputs. Every expected value is transcribed from #2941 R1/R2/R4/R8/AC1/AC4; expectations are never read from the design and no pass flag substitutes for the observed plan value."
target = "rust"
command = "workload-2941-behavior"
evidence_paths = ["evidence/workload-2941-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2941 pure workload design module has not landed, so the EC import fails closed."
expected = "The behavior verifier remains a known failure until the design exposes the frozen per-shard workload, member identity, voter placement, and service-plan entry points."

[[tool.aw.python-ec.cases]]
id = "workload-2941-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/workload-2941-security.py"
promise = "The #2941 pure topology design fails closed when data members from different instances share a node, protects exactly committed voters or the joint old/new union, excludes readers, refuses voluntary eviction that loses either joint quorum, and protects a replacement before unprotecting an old voter. This reuses the closest existing topology capability and use case because #2941 extends the same Kubernetes topology boundary."
oracle = "The verifier drives cross-instance placement, committed and joint voter selection, voluntary eviction, and membership-update entry points with EC-owned literal inputs. It checks exact rejection vocabulary and field path, a neighbouring admissible placement, literal voter sets, and literal update ordering and coverage; no design-owned validity boolean substitutes for those observed values."
target = "rust"
command = "workload-2941-security"
evidence_paths = ["evidence/workload-2941-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2941 pure disruption design module has not landed, so the EC import fails closed."
expected = "The security verifier remains a known failure until the design exposes the frozen cross-instance placement and committed/joint voter disruption entry points."

[[tool.aw.python-ec.cases]]
id = "handoff-2942-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/handoff-2942-behavior.py"
promise = "The #2942 pure handoff design admits only the ordered member-handoff phase progression, exposes temporary workload-generation surge roles and cost, protects committed voters without counting learners as quorum, resumes a persisted phase with one action, keeps the old member authoritative on target failure, and distinguishes planned-handoff availability from unexpected-loss promises. This reuses the closest existing topology capability and use case because the manifest has no member-handoff-specific identifiers."
oracle = "The verifier imports the pure handoff design and drives transition, status, voter-protection, resume, target-failure, and planned-availability entry points with EC-owned literal facts. Expected values are transcribed from issue #2942 R1-R6 and AC2/AC4; no expectation is read back out of the design and no model-supplied pass boolean is accepted."
target = "rust"
command = "handoff-2942-behavior"
evidence_paths = ["evidence/handoff-2942-behavior.json"]


[tool.aw.python-ec.cases.known_failure]
reason = "The #2942 pure design modules (lumen.topology.handoff_spec, lumen.topology.handoff_admission, lumen.topology.handoff_status, lumen.topology.handoff_verdict) have not landed, so the behavior verifier fails closed on import."
expected = "The behavior verifier remains a known failure until the design exposes the entry points #2942 names; the gate reports a known_failure that starts passing, so this block expires loudly once the design lands."

[[tool.aw.python-ec.cases]]
id = "handoff-2942-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/handoff-2942-security.py"
promise = "The #2942 pure handoff design fails closed on skipped phases, unprotected committed voters, learner quorum counting, duplicate resume adds, unrelated PVC cleanup, and premature leader termination; it never overclaims convergence while more than one steady generation remains. This reuses the closest existing topology capability and use case because the manifest has no member-handoff-specific identifiers."
oracle = "Each refusal is driven with EC-owned literal facts and checked against an exact reason and named offending field, with an adjacent admitted input where the entry point admits one. Leader termination and status overclaim are checked at their own entry points. Expected values are transcribed from issue #2942 R1/R2/R3/R4/R7 and AC3/AC4 rather than read from the design."
target = "rust"
command = "handoff-2942-security"
evidence_paths = ["evidence/handoff-2942-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2942 pure design modules (lumen.topology.handoff_spec, lumen.topology.handoff_admission, lumen.topology.handoff_status, lumen.topology.handoff_verdict) have not landed, so the security verifier fails closed on import."
expected = "The security verifier remains a known failure until the design exposes the entry points #2942 names; the gate reports a known_failure that starts passing, so this block expires loudly once the design lands."

[[tool.aw.python-ec.cases]]
id = "routing-2943-behavior"
artifact_id = "artifact:lumen/routing-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/routing-2943-behavior.py"
promise = "The #2943 routing design resolves every supported request against the live catalog generation, routes keyed operations to owners and global mutations through generation-bound reconcile intent, exposes success only after matching all-shard acknowledgement, forwards writes and leader reads, admits eligible any-read replicas and every data-pod coordinator, performs exactly one stale-map refresh/retry, and retains caller identity, authorization, request, and trace context. The closest existing topology capability and use case are reused because this is the topology's request-routing dimension."
oracle = "The verifier imports the pure routing design and drives every routing, visibility, read-target, coordinator, stale-map, and forwarding entry point with EC-owned literal inputs. Each expected value is transcribed from #2943 R1-R8/AC5, no value is read from the design as an expectation, and no design-computed boolean is accepted in place of an observed route, generation, intent, target, state, action, or context field."
target = "rust"
command = "routing-2943-behavior"
evidence_paths = ["evidence/routing-2943-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2943 pure routing design modules have not landed, so the EC import fails closed."
expected = "The behavior verifier remains a known failure until the design exposes frozen routing inputs and pure generation-aware routing, visibility, read-target, coordinator, retry, and forwarding entry points."

[[tool.aw.python-ec.cases]]
id = "routing-2943-security"
artifact_id = "artifact:lumen/routing-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/routing-2943-security.py"
promise = "The #2943 routing design fails closed for incomplete or wrong-generation global acknowledgements, bounded reads without enforced apply-lag bounds, pod-pinned or restart-only coordinator maps, and a second stale-map retry; it names the unsafe field, admits neighbouring safe inputs, and never lets peer mTLS identity replace or elevate the caller's authorization context. The closest existing topology capability and use case are reused because this is the topology's request-routing dimension."
oracle = "The verifier drives pure routing deciders with EC-owned incomplete, mismatched, unbounded, pinned, restart-map, exhausted-retry, and peer-identity inputs. It observes exact refusal vocabulary, named fields, reconcile actions, admitted neighbours, and unmodified caller authority; expected values are never read from the design and a design-computed boolean cannot satisfy a row."
target = "rust"
command = "routing-2943-security"
evidence_paths = ["evidence/routing-2943-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2943 pure routing design modules have not landed, so the EC import fails closed."
expected = "The security verifier remains a known failure until the design exposes frozen routing inputs plus fail-closed visibility, bounded-read, coordinator, retry, and caller-context verdicts."

[[tool.aw.python-ec.cases]]
id = "query-2944-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/query-2944-behavior.py"
promise = "The #2944 pure query design admits globally comparable lexical protocols, plans sufficient per-shard vector candidates, returns deterministic filtered/deduplicated hybrid pages with fusion, ordering, and continuation values, and completes only at one pinned generation. The closest existing topology capability and use case are reused because cross-shard query correctness depends on the production shard topology."
oracle = "The verifier imports and drives the pure query design with EC-owned literal protocol, request, candidate, shard-result, canonical-result, and generation fixtures. Every expected value is transcribed from #2944 R1-R5 and AC1/AC3; no expected value is read from the design, and no design-computed validity flag is accepted in place of an observed protocol, depth, page, cursor, comparison, or verdict value."
target = "rust"
command = "query-2944-behavior"
evidence_paths = ["evidence/query-2944-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2944 pure query design modules have not landed, so the EC import fails closed."
expected = "The behavior verifier remains a known failure until the design exposes frozen query inputs plus pure lexical-protocol, vector-budget/merge, hybrid-merge, canonical-comparison, and pinned-generation entry points."

[[tool.aw.python-ec.cases]]
id = "query-2944-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/query-2944-security.py"
promise = "The #2944 pure query design fails closed on raw shard-local lexical scoring, missing/timed-out/failed required shards, generation changes, and canonical score/order/filter/cursor mismatches; hybrid merge never overclaims a split-cutover page. The closest existing topology capability and use case are reused because cross-shard query correctness depends on the production shard topology."
oracle = "Each refusal is driven with an EC-owned literal input and checked against the exact reason, named value, and neighbouring admitted input where applicable. The verifier observes concrete verdict fields and canonical mismatch dimensions rather than a design-computed boolean; expected values are never read from the design."
target = "rust"
command = "query-2944-security"
evidence_paths = ["evidence/query-2944-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2944 pure query design modules have not landed, so the EC import fails closed."
expected = "The security verifier remains a known failure until the design exposes typed rejection and retryable-generation-change verdicts with shard and generation diagnostics, plus canonical mismatch and split-cutover handling."

[[tool.aw.python-ec.cases]]
id = "split-2945-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/split-2945-behavior.py"
promise = "The #2945 pure split design admits one- and three-voter source groups, preserves requested non-voting read replicas on both target groups, advances the complete split checkpoint sequence without early source retirement, serializes one topology mutation per instance, admits split, and permits source retirement only after supplied target durability and committed catalog cutover facts. The closest existing topology capability/use case is reused because the inventory has no replicated-reshard-specific entry."
oracle = "The verifier imports the pure split design and drives every listed split admission, checkpoint, catalog-cutover, mutation, and source-retirement entry point using EC-owned literal inputs. Expected values are transcribed from #2945 R1-R5 and AC3; no expectation is read from the design and no design-computed validity boolean is accepted."
target = "rust"
command = "split-2945-behavior"
evidence_paths = ["evidence/split-2945-behavior.json"]


[tool.aw.python-ec.cases.known_failure]
reason = "The #2945 pure design modules (lumen.topology.split, lumen.topology.split_recovery) have not landed, so the behavior verifier fails closed on import."
expected = "The behavior verifier remains a known failure until the design exposes the entry points #2945 names; the gate reports a known_failure that starts passing, so this block expires loudly once the design lands."

[[tool.aw.python-ec.cases]]
id = "split-2945-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/split-2945-security.py"
promise = "The #2945 pure split design fails closed for unsupported source voter counts, negative target read replicas, skipped checkpoint predecessors, any uncaught target member, a second mutation on an active instance, automatic merge, shard contraction, PVC shrink, and premature source retirement; it keeps adjacent valid split inputs admitted. The closest existing topology capability/use case is reused because the inventory has no replicated-reshard-specific entry."
oracle = "The verifier drives explicit forbidden inputs through every applicable #2945 entry point and observes EC-owned literal refusal reasons and field paths, alongside explicitly constructed neighbouring admissible inputs. It checks values, not design-computed validity booleans, and never derives expected values from observed design output."
target = "rust"
command = "split-2945-security"
evidence_paths = ["evidence/split-2945-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2945 pure design modules (lumen.topology.split, lumen.topology.split_recovery) have not landed, so the security verifier fails closed on import."
expected = "The security verifier remains a known failure until the design exposes the entry points #2945 names; the gate reports a known_failure that starts passing, so this block expires loudly once the design lands."

[[tool.aw.python-ec.cases]]
id = "capacity-2946-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/capacity-2946-behavior.py"
promise = "The #2946 pure capacity design exposes an immutable direct initial GCE machine type defaulting to e2-standard-2, preserves operator-owned status on unchanged reapplication, resolves allowed types to stable opaque selectors without public pool names, derives requests below allocatable capacity, defaults PVCs to 10Gi standard-rwo/pd-balanced while admitting growth, exposes configured cooldown, and keeps an old healthy member authoritative when capacity is blocked. The closest existing topology capability and use case are reused because capacity is the deployment topology's placement dimension."
oracle = "The verifier imports and drives the pure capacity design with EC-owned literal machine types, catalog records, reserves, headroom, storage, policy, and status inputs. Every expected value is transcribed from #2946 R1/R2/R3/R5/R7/R8/R10; no expected value is read from the design, and no design-computed validity flag is accepted in place of an observed value."
target = "rust"
command = "capacity-2946-behavior"
evidence_paths = ["evidence/capacity-2946-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2946 pure capacity design modules have not landed, so the EC import fails closed."
expected = "The behavior verifier remains a known failure until the design exposes the frozen specification, catalog, resource, policy, placement, and status entry points."

[[tool.aw.python-ec.cases]]
id = "capacity-2946-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/capacity-2946-security.py"
promise = "The #2946 pure capacity design rejects service-tier and unsupported direct-machine input, fails closed on missing, ambiguous, draining, or full catalog data before disruption, rejects insufficient allocatable capacity and cross-instance cross-namespace node sharing, rejects monetary policy fields, and projects CapacityBlocked rather than overclaiming a blocked transition. The closest existing topology capability and use case are reused because capacity is the deployment topology's placement dimension."
oracle = "The verifier drives each public capacity entry point with EC-owned forbidden literals and checks exact refusal vocabulary, named fields, and an explicitly admitted neighbouring direct-machine input. It does not use runtime I/O and never accepts a design-computed boolean as the observation."
target = "rust"
command = "capacity-2946-security"
evidence_paths = ["evidence/capacity-2946-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2946 pure capacity design modules have not landed, so the EC import fails closed."
expected = "The security verifier remains a known failure until the design exposes fail-closed specification, catalog, resource, placement, policy, and status verdicts."

[[tool.aw.python-ec.cases]]
id = "capacity-2947-behavior"
artifact_id = "artifact:lumen/capacity-observation-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/capacity-2947-behavior.py"
promise = "Using the closest existing topology use case, #2947 admits a complete fresh current-generation capacity window with CPU, memory, separately attributable read/write/compaction/snapshot/Raft catch-up work, and complete mounted-filesystem/PVC storage evidence; aggregates it by shard, member role, and instance; and publishes only redacted derived fresh signals."
oracle = "The verifier imports the frozen pure-Python design and drives real admission, aggregation, and projection entry points with EC-owned literal samples. Every expected value is transcribed from #2947 R1/R2/R3/R5 and AC1; no expectation is read back from the design, no design-computed validity boolean is accepted, and the design is red until these imports exist."
target = "rust"
command = "capacity-2947-behavior"
evidence_paths = ["evidence/capacity-2947-behavior.json"]


[tool.aw.python-ec.cases.known_failure]
reason = "The #2947 pure design modules (lumen.capacity.__init__, lumen.capacity.spec, lumen.capacity.verdict, lumen.capacity.admission) have not landed, so the behavior verifier fails closed on import."
expected = "The behavior verifier remains a known failure until the design exposes the entry points #2947 names; the gate reports a known_failure that starts passing, so this block expires loudly once the design lands."

[[tool.aw.python-ec.cases]]
id = "capacity-2947-security"
artifact_id = "artifact:lumen/capacity-observation-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/capacity-2947-security.py"
promise = "Using the closest existing topology use case, #2947 fails closed for missing workload or storage signals, in-memory storage estimates, missing/stale/mixed-generation/partial scale-in populations, and status projections that expose member payload or user content."
oracle = "Each refusal is driven with an explicit EC-owned forbidden input and checked against its literal reason and field path, while a neighbouring complete mounted-filesystem current-generation input remains admitted or eligible. The projection is checked on its actual redacted values, never on a design-computed pass flag."
target = "rust"
command = "capacity-2947-security"
evidence_paths = ["evidence/capacity-2947-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2947 pure design modules (lumen.capacity.__init__, lumen.capacity.spec, lumen.capacity.verdict, lumen.capacity.admission) have not landed, so the security verifier fails closed on import."
expected = "The security verifier remains a known failure until the design exposes the entry points #2947 names; the gate reports a known_failure that starts passing, so this block expires loudly once the design lands."

[[tool.aw.python-ec.cases]]
id = "machine-capacity-transition-2948-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/machine-capacity-transition-2948-behavior.py"
promise = "The #2948 pure design selects only one configured catalog-backed direct machine transition, keeps immutable initial and operator-owned generation/phase state through re-applied input, selects the declared compute or highmem pressure step, projects the complete transition status without monetary cost, admits retirement only after named evidence, and retains a healthy old member during typed capacity holds. The closest existing topology capability and use case are reused because this is the production shard topology's machine-capacity transition dimension."
oracle = "The verifier imports and drives the pure transition and capacity-hold design with EC-owned literal catalogs, requests, states, pressure, evidence, and target observations. Every expected value is transcribed from #2948 R1-R7 and AC2-AC4; no expectation is read from the design, and no design-computed validity flag is accepted in place of an observed value."
target = "rust"
command = "machine-capacity-transition-2948-behavior"
evidence_paths = ["evidence/machine-capacity-transition-2948-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2948 pure machine-capacity-transition and capacity-blocked-state design modules have not landed, so the EC import fails closed."
expected = "The behavior verifier remains a known failure until the design exposes catalog-only direct transition, pressure, resume, status, retirement-evidence, shard-admission, and capacity-hold entry points."

[[tool.aw.python-ec.cases]]
id = "machine-capacity-transition-2948-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/machine-capacity-transition-2948-security.py"
promise = "The #2948 pure design fails closed for missing direct edges, undeclared catalog machines, missing pressure steps, unfinished old-member retirement evidence, and an already-active shard movement; re-applied original input retains its transition; every unavailable target form is CapacityBlocked, uses a bounded retry hold, retains a healthy old member, and never claims a pool create, resize, or delete. The closest existing topology capability and use case are reused because this is the production shard topology's machine-capacity transition dimension."
oracle = "The verifier drives pure deciders with EC-owned invalid directions, absent catalog machines, active-movement states, each missing retirement proof, and missing/draining/full/quota-blocked/unschedulable target observations. It checks literal reason vocabulary, named offending fields, neighbouring admitted input, and structured no-pool-action/hold values; expected values are never read from the design and a design-computed boolean cannot satisfy a row."
target = "rust"
command = "machine-capacity-transition-2948-security"
evidence_paths = ["evidence/machine-capacity-transition-2948-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2948 pure machine-capacity-transition and capacity-blocked-state design modules have not landed, so the EC import fails closed."
expected = "The security verifier remains a known failure until the design exposes fail-closed direct-edge/catalog/pressure/shard/retirement verdicts and structured CapacityBlocked hold decisions."

[[tool.aw.python-ec.cases]]
id = "capacity-2949-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/capacity-2949-behavior.py"
promise = "The #2949 capacity design selects one larger configured geometric desired-storage generation under eligible physical-capacity and I/O pressure, plans direct live-PVC growth without a StatefulSet-template mutation, gives later members at least the committed desired capacity, commits only after every required expansion proof, and preserves resize intent monotonically and idempotently. The closest existing topology capability and use case are reused because this is the persisted storage dimension of primary replicas."
oracle = "The verifier imports and drives the pure capacity design with EC-owned literal generations, pressure, observation provenance, member proofs, and reconciliation results. Every expected value is transcribed from #2949 R1/R2/R3/R4/R5/AC2/AC3; no expected value is read from the design, and no design-computed validity flag is accepted in place of an observed plan, request, admission kind, commit decision, or persisted intent."
target = "rust"
command = "capacity-2949-behavior"
evidence_paths = ["evidence/capacity-2949-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2949 pure capacity design modules do not yet exist, so this black-box case fails closed at its required top-level design imports."
expected = "The behavior verifier remains failing until the TD stage supplies the frozen capacity value objects and real growth, member-request, observation-admission, commit, and intent-transition entry points."

[[tool.aw.python-ec.cases]]
id = "capacity-2949-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/capacity-2949-security.py"
promise = "The #2949 capacity design rejects index-bytes-only evidence, names missing required member expansion proof, refuses shrink, returns split-needed at configured maximum capacity or a measured I/O ceiling, and projects unsupported or failed expansion as a non-converged operator-action or split-needed status. The closest existing topology capability and use case are reused because this is the persisted storage dimension of primary replicas."
oracle = "Each refusal is driven with an EC-owned literal input and checked against the exact capacity reason, named field, split-needed kind, or non-converged status required by #2949 R3/R4/R6/AC4, rather than a design-provided boolean. A neighbouring physical-capacity plus I/O observation must still be admitted, closing the reject-everything loophole."
target = "rust"
command = "capacity-2949-security"
evidence_paths = ["evidence/capacity-2949-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2949 pure capacity design modules do not yet exist, so this black-box case fails closed at its required top-level design imports."
expected = "The security verifier remains failing until the TD stage supplies fail-closed capacity observation, expansion-proof, shrink, split-needed, and status-projection decisions."

[[tool.aw.python-ec.cases]]
id = "replica-2950-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/replica-2950-behavior.py"
promise = "The #2950 pure design changes only non-voting read-replica count while preserving voter and shard policy, admits zero replicas, qualifies caught-up replicas at the configured lag bound, routes leader/strong reads to the leader and any/bounded reads only to eligible replicas, orders drain through removal, tombstone, and grace, reclaims only an exact safely removed reconstructible non-voter, and retains rollback before committed removal. This reuses the closest existing topology capability/use case because #2950 extends primary-replicas topology."
oracle = "The verifier imports the pure #2950 design entry points and drives them with EC-owned literals. It observes target counts, eligibility and lifecycle vocabulary, routing/reclamation identities, and resume outcome values; no expected value is read from the design and no design-computed validity boolean is accepted."
target = "rust"
command = "replica-2950-behavior"
evidence_paths = ["evidence/replica-2950-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2950 pure read-replica design modules have not landed, so the EC import fails closed."
expected = "The behavior verifier remains a known failure until the design exposes frozen replica-change, read-routing, scale-in, reclamation, and interruption-resume entry points."

[[tool.aw.python-ec.cases]]
id = "replica-2950-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/replica-2950-security.py"
promise = "The #2950 pure design fails closed on negative replica targets, incomplete or over-bound replica reads, skipped scale-in ordering, voter/shard/authoritative-handoff PVC reclamation, routing re-entry after committed removal, and reused cleanup identity or generation; it keeps zero replicas, at-bound reads, ordered removal, and safe non-voter reclamation admitted. This reuses the closest existing topology capability/use case because #2950 extends primary-replicas topology."
oracle = "Each forbidden input is explicit and EC-owned. The verifier observes exact refusal vocabulary and named field paths where the decision can refuse, plus neighbouring admitted values, rather than accepting a design-provided boolean or a generic rejection."
target = "rust"
command = "replica-2950-security"
evidence_paths = ["evidence/replica-2950-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2950 pure read-replica design modules have not landed, so the EC import fails closed."
expected = "The security verifier remains a known failure until the design supplies exact refusal vocabulary and field paths for unsafe replica changes, routing, lifecycle transitions, reclamation, and committed-resume requests."

[[tool.aw.python-ec.cases]]
id = "capacity-2951-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/capacity-2951-behavior.py"
promise = "The #2951 pure capacity design independently classifies user shard minimum, PVC-growth impossibility, maximum-useful-machine saturation, and recovery-policy inputs into a split request; routes eligible read-only pressure to read replicas; selects one deterministic hottest eligible shard and the supplied next generation; and exposes a v1 decision vocabulary with no merge. The closest existing topology capability and use case are reused because split policy is a production shard-topology capacity dimension."
oracle = "The verifier imports and drives the pure trigger, selection, headroom, and verdict entry points with EC-owned literal inputs. Every expected value is transcribed from #2951 R1/R2/R3/R5 and AC1/AC2; no expected value is read from the design, and no design-computed boolean can satisfy a row."
target = "rust"
command = "capacity-2951-behavior"
evidence_paths = ["evidence/capacity-2951-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2951 pure capacity design modules have not landed, so the EC import fails closed."
expected = "The behavior verifier remains a known failure until the design exposes frozen trigger, selection, headroom, and verdict entry points for policy-driven split classification."

[[tool.aw.python-ec.cases]]
id = "capacity-2951-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/capacity-2951-security.py"
promise = "The #2951 pure capacity design fails closed with CapacityBlocked for missing, full, unschedulable, and insufficient temporary-member/copy/catch-up capacity, or an in-progress mutation; names the hold context, proposes no topology on a hold, admits the neighbouring complete declared capacity, and exposes no monetary ceiling input. The closest existing topology capability and use case are reused because split capacity holds are a production shard-topology capacity dimension."
oracle = "The verifier drives the pure headroom decider with EC-owned declared profile, pool maximum, target-capacity, migration-headroom, and mutation inputs. It checks typed holds, literal reason and field values, no proposed target topology, the neighbouring admitted request, and absence of monetary input; expected values are never read from the design and a design-computed boolean cannot satisfy a row."
target = "rust"
command = "capacity-2951-security"
evidence_paths = ["evidence/capacity-2951-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2951 pure capacity design modules have not landed, so the EC import fails closed."
expected = "The security verifier remains a known failure until the design exposes fail-closed CapacityBlocked holds with reasons, field paths, and no proposed topology."

[[tool.aw.python-ec.cases]]
id = "control-plane-2952-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/control-plane-2952-behavior.py"
promise = "The #2952 control-plane design admits one shared control plane, the fixed two-replica production HA profile and explicit one-replica non-HA development profile, an installation-owned direct-machine and transition/spend policy without a Lumen tier or node-pool name, an explicit no-default state pending #2953 calibration, and reapply that preserves controller-owned machine and transition state. The closest existing topology capability and use case are reused because this is the operator control-plane dimension of the production topology."
oracle = "The verifier imports and drives the pure control-plane installation admission, machine admission, and reapply entry points using EC-owned literal specs. Every expected value is transcribed from #2952 R1/R2/R3/R4/R5 and AC1/AC2/AC3; no expectation is read back out of the design, and no design-computed validity flag is accepted in place of an observed policy or state value."
target = "rust"
command = "control-plane-2952-behavior"
evidence_paths = ["evidence/control-plane-2952-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2952 pure control-plane design modules have not landed, so the EC import fails closed."
expected = "The behavior verifier remains a known failure until the design exposes the frozen installation and machine policy specifications, admitted verdicts, and pure reapply entry point."

[[tool.aw.python-ec.cases]]
id = "control-plane-2952-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/control-plane-2952-security.py"
promise = "The #2952 control-plane design fails closed on an instance-scoped operator, every weakened production or development profile, Lumen tiers, node-pool names, and an implicit machine default; and pure missing or unsupported capacity preflight reports actionable status while retaining the supplied live control-plane state. The closest existing topology capability and use case are reused because this is the operator control-plane dimension of the production topology."
oracle = "The verifier drives each admission and pure capacity-preflight entry point with EC-owned explicitly invalid input fields. It checks exact refusal vocabulary, named field paths, actionable preflight status, and separately observed unchanged live-state values; expected values are never read from the design and a design-computed boolean cannot satisfy a row."
target = "rust"
command = "control-plane-2952-security"
evidence_paths = ["evidence/control-plane-2952-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2952 pure control-plane design modules have not landed, so the EC import fails closed."
expected = "The security verifier remains a known failure until the design exposes fail-closed installation, machine-policy, and capacity-preflight verdicts with named refusal and actionable-status fields."

[[tool.aw.python-ec.cases]]
id = "control-2953-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/control-2953-behavior.py"
promise = "The #2953 pure control-plane design selects one calibrated direct E2/N2 machine with one bounded standard/highmem transition step, distinguishes CPU/backlog from memory/highmem reasons, permits an expected-generation commit only from the elected leader, fixes the control plane at two non-HPA replicas, and permits target changes only through the supplied bounded transition predicates. The closest existing topology capability and use case are reused because this is the control-plane workload's topology dimension."
oracle = "The verifier imports and drives only the pure control-plane calibration, policy, generation, and transition deciders with EC-owned literal benchmark rows, leader/generation requests, two-replica specs, telemetry, history, and timestamps. Every expected value is pinned in this case from #2953 R3/R4/R5/R6/R7/AC2/AC4; no expected value is read from the design and no design-computed validity flag is accepted."
target = "rust"
command = "control-2953-behavior"
evidence_paths = ["evidence/control-2953-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2953 pure control-plane spec, verdict, and admission modules have not landed, so this EC import fails closed."
expected = "The behavior verifier remains a known failure until the design exposes calibrated initial-target selection, leader-and-generation commit eligibility, fixed two-replica policy admission, and bounded transition decisions."

[[tool.aw.python-ec.cases]]
id = "control-2953-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/control-2953-security.py"
promise = "The #2953 pure control-plane design fails closed for incomplete calibration, a non-elected leader, generation conflict, a non-two-replica or HPA policy, incomplete telemetry, insufficient downgrade headroom, cooldown, exhausted daily-change budget, and spend ceiling. The closest existing topology capability and use case are reused because this is the control-plane workload's topology dimension."
oracle = "The verifier supplies explicit forbidden inputs to every relevant pure entry point and observes the literal reason plus the named field where applicable, with a complete-calibration neighbouring admission. Expected values are EC-owned literals from #2953 R3/R4/R5/R6/R7/AC2/AC3/AC4/AC5; runtime leader contention, durable restart state, telemetry collection, and manifest rendering are not asserted."
target = "rust"
command = "control-2953-security"
evidence_paths = ["evidence/control-2953-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2953 pure control-plane spec, verdict, and admission modules have not landed, so this EC import fails closed."
expected = "The security verifier remains a known failure until the design exposes typed fail-closed calibration, commit, policy, telemetry, headroom, cooldown, daily-budget, and spend-ceiling verdicts."

[[tool.aw.python-ec.cases]]
id = "control-plane-2954-behavior"
artifact_id = "artifact:lumen/control-plane-shared-transition"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/control-plane-2954-behavior.py"
promise = "The #2954 Lumen design projects one persisted current/target machine generation through a supplied shared Deployment transition into deterministic Lumen status and observability, keeps installation inputs separate from capacity inputs, delegates rollout mechanics, retains equal reapply intent, and records the complete machine transition. The closest existing topology capability and use case are reused because this is the Lumen deployment control-plane transition dimension."
oracle = "The verifier imports and drives only the frozen pure control-plane design entry points with EC-owned literal intent, shared-transition, installation, and capacity inputs. It reads projection, ownership, resume, status, and observability values directly; every expected value is transcribed from #2954 R1/R2/R3/R5/R6 and AC1/AC3/AC4, never derived from the design or a design-computed boolean."
target = "rust"
command = "control-plane-2954-behavior"
evidence_paths = ["evidence/control-plane-2954-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2954 pure Lumen control-plane design modules have not landed, so the EC import fails closed."
expected = "The behavior verifier remains a known failure until the design exposes the frozen shared-transition adapter, ownership partition, persisted-intent resume, and deterministic observability entry points."

[[tool.aw.python-ec.cases]]
id = "control-plane-2954-security"
artifact_id = "artifact:lumen/control-plane-shared-transition"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/control-plane-2954-security.py"
promise = "The #2954 Lumen design fails closed when either owner supplies the other owner's fields, when the adapter receives a local rollout decision or unknown shared phase, and when an unschedulable target or timeout would promote the target. It retains equal persisted intent and distinguishes held outcomes in observable records. The closest existing topology capability and use case are reused because this is the Lumen deployment control-plane transition dimension."
oracle = "The verifier drives explicit wrong-owner, local-decision, unknown-phase, UNSCHEDULABLE, TIMEOUT, and equal-reapply inputs through the pure design. It observes exact refusal reason and field vocabulary, admitted neighbouring inputs, authoritative-machine values, retained identity/generation/disposition, and event values. Expected values are EC-owned literals rather than design booleans."
target = "rust"
command = "control-plane-2954-security"
evidence_paths = ["evidence/control-plane-2954-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2954 pure Lumen control-plane design modules have not landed, so the EC import fails closed."
expected = "The security verifier remains a known failure until the design exposes fail-closed owner and adapter refusals plus typed hold and retained-intent projections."

[[tool.aw.python-ec.cases]]
id = "control-plane-2955-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/control-plane-2955-behavior.py"
promise = "The #2955 pure control-plane design freezes serving generations and starts no mutation during an outage, resumes at most one persisted intent at its durable phase, preserves controller-owned current and target capacity through fixed reapply, admits only feasible target capacity, and separates control-plane cost from per-instance cost. The closest existing topology capability and use case are reused because this controls the converged production topology."
oracle = "The verifier imports and drives the pure outage, recovery, ownership, capacity, and cost entry points with EC-owned literal inputs. Every expected value is transcribed from #2955 R2/R3/R4/R5/R6; no expectation is read from the design and no design-computed boolean is accepted in place of the observed verdict or value."
target = "rust"
command = "control-plane-2955-behavior"
evidence_paths = ["evidence/control-plane-2955-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2955 pure control-plane design modules have not landed, so the EC import fails closed."
expected = "The behavior verifier remains a known failure until the design exposes frozen-serving, recovery, reapply, target-capacity, and separate-cost decision values."

[[tool.aw.python-ec.cases]]
id = "control-plane-2955-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/control-plane-2955-security.py"
promise = "The #2955 pure control-plane design fails closed on outage mutation requests, more than one resumable persisted intent, rendered overwrites of current or target capacity, conflicting active transitions, and target capacity beyond supplied availability; each refusal uses closed reason vocabulary and names the offending field. The closest existing topology capability and use case are reused because this protects the converged production topology."
oracle = "The verifier drives each pure refusal entry point with explicit EC-owned forbidden inputs and checks the exact reason, field path, and a neighbouring admitted input. Expected values are never read from the design and a design-computed boolean cannot satisfy a row."
target = "rust"
command = "control-plane-2955-security"
evidence_paths = ["evidence/control-plane-2955-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2955 pure control-plane design modules have not landed, so the EC import fails closed."
expected = "The security verifier remains a known failure until the design exposes closed outage, recovery, ownership, and capacity refusal verdicts with actionable field paths."

[[tool.aw.python-ec.cases]]
id = "migration-2961-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/migration-2961-behavior.py"
promise = "The #2961 migration design keeps non-empty legacy state authoritative when Raft is empty, admits the final write fence only after durable caught-up state, admits one target catalog generation only at the verified watermark, derives legacy before and Raft after persisted cutover, retains source evidence, and sends new installations directly to canonical Raft. The closest existing topology capability and use case are reused because this is the one-voter production topology's embedded-to-Raft migration dimension."
oracle = "The verifier imports and drives the pure migration model with EC-owned literal legacy/Raft observations, persisted phases, watermarks, catalog generations, source predicates, and installation kind. Every expected value is transcribed from #2961 R1/R3/R4/R5/R6/R7/AC2/AC3; no expected value is read from the design, and no design-computed validity flag is accepted in place of the observed phase, authority, action, generation, retention, or installation path."
target = "rust"
command = "migration-2961-behavior"
evidence_paths = ["evidence/migration-2961-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2961 pure migration design modules have not landed, so the EC import fails closed."
expected = "The behavior verifier remains a known failure until the design exposes frozen migration observations, persisted phase and watermark state, typed verdicts, and pure migration, phase, catalog, recovery, and installation deciders."

[[tool.aw.python-ec.cases]]
id = "migration-2961-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/migration-2961-security.py"
promise = "The #2961 migration design fails closed for final fence without durable checkpoint or caught-up target, catalog cutover before the verified watermark, and legacy-source cleanup before restart and oracle proof or when source is unrelated or authoritative; each refusal names its failed field and its neighbouring safe request remains admitted. The closest existing topology capability and use case are reused because this is the one-voter production topology's embedded-to-Raft migration dimension."
oracle = "The verifier drives pure phase, watermark, catalog, restart, oracle, relationship, and authority inputs at every migration admission entry point. It checks exact refusal vocabulary, named offending fields, neighbouring admissions, and structured source retention; expected values are never read from the design and a design-computed boolean cannot satisfy a row."
target = "rust"
command = "migration-2961-security"
evidence_paths = ["evidence/migration-2961-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2961 pure migration design modules have not landed, so the EC import fails closed."
expected = "The security verifier remains a known failure until the design exposes fail-closed phase, catalog-cutover, and source-cleanup deciders with typed rejection reasons and field paths."

[[tool.aw.python-ec.cases]]
id = "spec-2962-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/spec-2962-behavior.py"
promise = "The #2962 pure topology-backup design admits a complete generation-pinned catalog-plus-all-shards BackupSet, permits only stable generation-consistent barriers, routes public requests to the topology coordinator, orders catalog and all shard application before rebind, makes valid replay idempotent, derives the daily/two-successful-complete-sets destination default while preserving explicit operator policy, and keeps the closest existing topology capability and use case because backup/restore is its topology-consistency dimension."
oracle = "The verifier imports and drives the pure backup admission, coordinator routing, restore planning/state-transition, and policy entry points with EC-owned literal topology, metadata, target, event, and policy inputs. Every expected value is transcribed from #2962 R1/R2/R4/R5/R6/R7; no expected value is read from the design and no design-computed validity boolean can satisfy a row."
target = "rust"
command = "spec-2962-behavior"
evidence_paths = ["evidence/spec-2962-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2962 pure topology backup design modules have not landed, so the EC imports fail closed."
expected = "The behavior verifier remains a known failure until the design exposes frozen backup metadata, admitted/rejected verdicts, pure backup barrier and coordinator routing, restore planning/transitions, and destination-policy derivation."

[[tool.aw.python-ec.cases]]
id = "spec-2962-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/spec-2962-security.py"
promise = "The #2962 pure topology-backup design fails closed on incomplete metadata, artifact-generation mismatch, active split or handoff, unsupported topology, ready or non-empty restore targets, and corrupt restore input; it returns actionable catalog/shard/generation detail, preserves the prior successful set, and never represents schedule configuration as verified backup success. The closest existing topology capability and use case are reused because this is the backup/restore dimension of the same topology contract."
oracle = "The verifier drives each pure admission, barrier, coordinator, restore-plan, restore-transition, and status-projection entry point with explicit EC-owned forbidden values. It checks exact outcome vocabulary, named offending fields, neighbouring admissible metadata, and structured unready detail; expected values are never derived from observations and a design-computed boolean cannot satisfy a row."
target = "rust"
command = "spec-2962-security"
evidence_paths = ["evidence/spec-2962-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2962 pure topology backup design modules have not landed, so the EC imports fail closed."
expected = "The security verifier remains a known failure until the design exposes fail-closed metadata/barrier/routing/restore verdicts, actionable unready restore state, retained prior-successful-set detail, and evidence-bound backup-status projection."

[[tool.aw.python-ec.cases]]
id = "vector-2996-behavior"
artifact_id = "artifact:lumen/vector-determinism"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/vector-2996-behavior.py"
promise = "Using the closest existing Lumen capability/use-case identifiers because #2996 has no dedicated vector entry, the #2996 design defines the exact five allowed nearest IDs, deterministic equal-distance top-K order across equivalent restore values, a brute-force candidate oracle, and failure context retaining the supplied seed and HNSW construction/search parameters."
oracle = "The verifier drives only the pure vector design with EC-owned literal corpora, queries, allow-sets, candidates, seeds, and parameter mappings. It reads exact IDs, typed mismatch vocabulary/field names, and context values; no expected value is read from the design and no design-computed boolean is accepted as an oracle."
target = "rust"
command = "vector-2996-behavior"
evidence_paths = ["evidence/vector-2996-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2996 pure vector design modules have not landed, so importing lumen.vector.determinism and lumen.vector.evidence fails closed."
expected = "The behavior verifier remains red until the design supplies the EC-declared exact-order, brute-force comparison, and failure-context surface."

[[tool.aw.python-ec.cases]]
id = "vector-2996-security"
artifact_id = "artifact:lumen/vector-determinism"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/vector-2996-security.py"
promise = "Using the closest existing Lumen capability/use-case identifiers because #2996 has no dedicated vector entry, the #2996 design never admits excluded nearest IDs or reordered equal-distance candidates as exact, names candidate mismatches, and preserves explicitly supplied empty diagnostic parameter fields."
oracle = "The verifier uses EC-owned adversarial corpus order, allow-sets, and candidate IDs to require typed mismatch values and named offending fields, alongside their neighbouring exact-match cases. It does not assert HNSW traversal, backup I/O, load scheduling, retries, or a runtime binary result."
target = "rust"
command = "vector-2996-security"
evidence_paths = ["evidence/vector-2996-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2996 pure vector design modules have not landed, so importing lumen.vector.determinism and lumen.vector.evidence fails closed."
expected = "The security verifier remains red until the design exposes exact-oracle mismatch outcomes and named candidate fields without relying on a boolean pass flag."

[[tool.aw.python-ec.cases]]
id = "capacity-3066-behavior"
artifact_id = "artifact:lumen/capacity-existing-cluster-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/capacity-3066-behavior.py"
promise = "Under the closest existing Lumen capability/use-case binding, #3066 defines the existing-cluster capacity substrate: finite direct-machine-type profiles, catalog-authoritative stable selectors and zero-node-readable profile shape, Terraform-owned lifecycle admission, a non-data system-capacity prerequisite for zero-minimum data pools, and draining-before-retirement."
oracle = "The verifier imports the frozen capacity design and drives its real profile-admission, catalog, selector/evidence, ownership, installation, and retirement entry points with EC-owned literal inputs. Every expected value is transcribed from #3066 R2/R3/R5/R6/R7/R8/R9; no expectation is read back from the design and no boolean pass flag substitutes for an observed value."
target = "rust"
command = "capacity-3066-behavior"
evidence_paths = ["evidence/capacity-3066-behavior.json"]


[tool.aw.python-ec.cases.known_failure]
reason = "The #3066 pure design modules (lumen.capacity.spec, lumen.capacity.verdict, lumen.capacity.admission, lumen.capacity.catalog) have not landed, so the behavior verifier fails closed on import."
expected = "The behavior verifier remains a known failure until the design exposes the entry points #3066 names; the gate reports a known_failure that starts passing, so this block expires loudly once the design lands."

[[tool.aw.python-ec.cases]]
id = "capacity-3066-security"
artifact_id = "artifact:lumen/capacity-existing-cluster-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/capacity-3066-security.py"
promise = "Under the closest existing Lumen capability/use-case binding, #3066 fails closed on invalid profile bounds and non-direct keys, unknown catalog selectors and inconsistent Node evidence, runtime/operator pool mutation, missing non-data system capacity, and destructive retirement before both zero assigned members and a separately explicit authorization."
oracle = "Each refusal is driven with an explicit EC-owned forbidden input and checked against the exact reason and field path required by #3066 R2/R3/R5/R6/R7/R8/R9. The final adjacent valid profile proves admission remains narrow; booleans computed by the design are not accepted as the oracle."
target = "rust"
command = "capacity-3066-security"
evidence_paths = ["evidence/capacity-3066-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #3066 pure design modules (lumen.capacity.spec, lumen.capacity.verdict, lumen.capacity.admission, lumen.capacity.catalog) have not landed, so the security verifier fails closed on import."
expected = "The security verifier remains a known failure until the design exposes the entry points #3066 names; the gate reports a known_failure that starts passing, so this block expires loudly once the design lands."

[[tool.aw.python-ec.cases]]
id = "release-compatibility-3085-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/release-compatibility-3085-behavior.py"
promise = "The #3085 release-compatibility design exposes release, API, peer, durable-format, and immutable-digest facts; projects an authorized descriptor and separately observed image digest; admits only the adjacent overlapping N-1/N window; and serves the mixed-member public-contract intersection. The existing topology capability/use case is the closest available inventory anchor."
oracle = "The verifier imports the pure release-compatibility design and drives its descriptor, inventory, operator projection, status, adjacent-release, mixed-contract, and target-digest entry points with EC-owned literal inputs. Every expected value is transcribed from #3085 R1-R7; no expectation is read back out of the design, and verdict booleans are never accepted in place of the observed value."
target = "rust"
command = "release-compatibility-3085-behavior"
evidence_paths = ["evidence/release-compatibility-3085-behavior.json"]


[tool.aw.python-ec.cases.known_failure]
reason = "The #3085 pure design modules (lumen.release_compatibility.__init__, lumen.release_compatibility.spec, lumen.release_compatibility.verdict, lumen.release_compatibility.admission) have not landed, so the behavior verifier fails closed on import."
expected = "The behavior verifier remains a known failure until the design exposes the entry points #3085 names; the gate reports a known_failure that starts passing, so this block expires loudly once the design lands."

[[tool.aw.python-ec.cases]]
id = "release-compatibility-3085-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/release-compatibility-3085-security.py"
promise = "The #3085 release-compatibility design fails closed for unauthorized descriptor access, skipped or non-overlapping binary, public-API, peer, and durable-format compatibility, N-only public surface while an N-1 member serves, and a member digest differing from the recorded immutable target. The existing topology capability/use case is the closest available inventory anchor."
oracle = "Each refusal is driven with an EC-owned literal input and checked for the exact #3085 rejection reason and offending field, while a neighbouring valid N-1/N release remains admitted. A different reported member digest is checked as a compatibility failure rather than a target-generation choice."
target = "rust"
command = "release-compatibility-3085-security"
evidence_paths = ["evidence/release-compatibility-3085-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #3085 pure design modules (lumen.release_compatibility.__init__, lumen.release_compatibility.spec, lumen.release_compatibility.verdict, lumen.release_compatibility.admission) have not landed, so the security verifier fails closed on import."
expected = "The security verifier remains a known failure until the design exposes the entry points #3085 names; the gate reports a known_failure that starts passing, so this block expires loudly once the design lands."

[[tool.aw.python-ec.cases]]
id = "lifecycle-3086-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/lifecycle-3086-behavior.py"
promise = "The #3086 lifecycle design admits complete generation-bound operation records, deterministically claims, advances, releases, or resumes them, sends member handoff, machine/capacity action, shard split, restore, and upgrade through one Lumen operation policy, and projects distinct Progressing/blocked/bounded-age lifecycle conditions. The closest existing topology capability and use case are reused because lifecycle arbitration is the production instance topology-control dimension."
oracle = "The verifier imports and drives the pure lifecycle record, transition, resume, policy, and status entry points with EC-owned literal records and requests. Every expected value is transcribed from #3086 R1/R2/R3/R4/R6/AC4; no expectation is read from the design, and no design-computed validity boolean can satisfy a row."
target = "rust"
command = "lifecycle-3086-behavior"
evidence_paths = ["evidence/lifecycle-3086-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #3086 pure lifecycle design modules have not landed, so the EC import fails closed."
expected = "The behavior verifier remains a known failure until the design exposes frozen lifecycle records, typed operation verdicts, common policy decisions, generation resume fencing, and pure status projection."

[[tool.aw.python-ec.cases]]
id = "lifecycle-3086-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/lifecycle-3086-security.py"
promise = "The #3086 lifecycle design fails closed for incomplete records, stale generations, conflicting mutations, and unknown operation kinds; names the active operation, active phase, and safe retry condition; retains failed authority only under an explicit retry rule; and exposes failed status as blocked. The closest existing topology capability and use case are reused because lifecycle arbitration is the production instance topology-control dimension."
oracle = "The verifier drives the pure lifecycle admission, transition, resume, policy, and status entry points with EC-owned literal invalid and neighbouring admissible inputs. It checks exact refusal and conflict vocabulary, named fields, active identity and phase, safe retry condition, retry retention, and the closed Lumen kind set; expected values are never read from the design and a design-computed boolean cannot satisfy a row."
target = "rust"
command = "lifecycle-3086-security"
evidence_paths = ["evidence/lifecycle-3086-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #3086 pure lifecycle design modules have not landed, so the EC import fails closed."
expected = "The security verifier remains a known failure until the design exposes typed incomplete-record, stale-generation, conflict, unsupported-kind, and finalization verdicts plus explicit blocked status projection."

[[tool.aw.python-ec.cases]]
id = "operator-upgrade-3087-behavior"
artifact_id = "artifact:lumen/operator-upgrade"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/operator-upgrade-3087-behavior.py"
promise = "The #3087 pure operator-upgrade design admits only an established additive CRD, derives one N/N-1 reconciliation owner including N leader loss, preserves unowned and unknown additive CR spec/status fields, changes a managed data-plane image only for an explicit desired-image change, admits compatible rollback, and orders abstract artifacts CRD, operator, then instance. The closest existing topology capability and use case are reused because this is the Kubernetes operator handover dimension of the primary replicas topology."
oracle = "The verifier imports and drives the pure operator-upgrade design with EC-owned literal CRD, replica/Lease, custom-resource patch, image, and rollback inputs. Every expected value is transcribed from #3087 R1-R7; no expectation is read from the design, and no design-computed validity flag is accepted in place of an observed verdict reason, owner version, field value, rollout action, blocking identifier, or artifact sequence."
target = "rust"
command = "operator-upgrade-3087-behavior"
evidence_paths = ["evidence/operator-upgrade-3087-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #3087 pure operator-upgrade design modules have not landed, so the EC import fails closed."
expected = "The behavior verifier remains a known failure until the design exposes frozen upgrade specifications, structured admitted/refused verdicts, and pure admission, ownership, patch, image-release, rollback, and ordering entry points."

[[tool.aw.python-ec.cases]]
id = "operator-upgrade-3087-security"
artifact_id = "artifact:lumen/operator-upgrade"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/operator-upgrade-3087-security.py"
promise = "The #3087 pure operator-upgrade design fails closed for unestablished or non-additive CRDs, zero or multiple modeled reconciliation owners, finalized instances, and N-only behavior during rollback; every relevant refusal names its offending field or blocking instance, while a neighboring compatible rollback remains admitted. The closest existing topology capability and use case are reused because this is the Kubernetes operator handover dimension of the primary replicas topology."
oracle = "The verifier drives each forbidden argument explicitly, rather than accepting a default-satisfying request, and checks the exact refusal vocabulary, named field, or blocking instance literal from #3087 R1/R2/R5/R6. It separately drives a neighboring compatible rollback so a blanket refusal cannot pass."
target = "rust"
command = "operator-upgrade-3087-security"
evidence_paths = ["evidence/operator-upgrade-3087-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #3087 pure operator-upgrade design modules have not landed, so the EC import fails closed."
expected = "The security verifier remains a known failure until the design exposes structured fail-closed CRD, ownership, and rollback verdicts with exact reason, field-path, and blocking-instance values."

[[tool.aw.python-ec.cases]]
id = "upgrade-3088-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/upgrade-3088-behavior.py"
promise = "The #3088 pure upgrade design preserves the authoritative current generation while staging a changed image, records an immutable CR-generation-keyed image and digest operation, derives one target generation and stable-member ownership, admits complete target evidence, resumes the same ownership, and projects the complete upgrade status. The closest existing topology capability and use case are reused because this is the staged-generation upgrade dimension of the production shard topology."
oracle = "The verifier imports and drives the pure upgrade design with EC-owned literal current-generation, requested-image, member, authority, and capacity inputs. Every expected value is transcribed from #3088 R1/R2/R3/R4/R6/R8; no expected value is read from the design, and no design-computed validity flag is accepted in place of an observed disposition, identity, plan, authority verdict, resume value, or status field."
target = "rust"
command = "upgrade-3088-behavior"
evidence_paths = ["evidence/upgrade-3088-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #3088 pure upgrade design modules have not landed, so the EC import fails closed."
expected = "The behavior verifier remains a known failure until the design exposes frozen upgrade inputs, operation identity, admitted target plan and authority verdicts, deterministic resume identity, and status projection entry points."

[[tool.aw.python-ec.cases]]
id = "upgrade-3088-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/upgrade-3088-security.py"
promise = "The #3088 pure upgrade design fails closed unless recorded digest, compatibility, and readiness all admit authority; latches the first accepted digest and refuses a different later digest without a replacement target; and classifies incompatible, inconsistent-digest, unschedulable, crash-looping, and unready targets as UpgradeBlocked, non-authoritative, and limited to target-owned cleanup. The closest existing topology capability and use case are reused because this is the staged-generation upgrade dimension of the production shard topology."
oracle = "The verifier supplies every forbidden authority and digest input explicitly, checks exact refusal vocabulary and named fields, preserves a neighbouring admissible input, and reads the failure blocker, authority state, and cleanup scope as values. Expected values are EC-owned literals from #3088 R4/R5/R7; no expected value is derived from the design and a design-computed boolean cannot satisfy a row."
target = "rust"
command = "upgrade-3088-security"
evidence_paths = ["evidence/upgrade-3088-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #3088 pure upgrade design modules have not landed, so the EC import fails closed."
expected = "The security verifier remains a known failure until the design exposes fail-closed authority and digest verdicts plus typed target-failure blocker, authority, and cleanup values."

[[tool.aw.python-ec.cases]]
id = "upgrade-3089-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/upgrade-3089-behavior.py"
promise = "The #3089 design replaces one voter through the persisted Provision, Learner, CatchUp, Cutover, Drain, and Cleanup handoff, admits only a distinct node with approved temporary capacity, gates cutover on compatible catch-up evidence, preserves membership configuration while transferring authority, drains before removal, recovers backward before cutover and forward after it, retains the authoritative PVC through verification, and converges only to the exact one-voter steady inventory. The closest existing topology capability and use case are reused because this is the production primary-replica topology upgrade dimension."
oracle = "The verifier imports and drives the pure #3089 upgrade handoff, membership, recovery, reclamation, and status design with EC-owned literal requests. Every expected value is transcribed from #3089 R1-R8; no expected value is read from the design, and no validity flag is accepted in place of the observed phase, node, configuration, authority, route action, recovery action, retained resource, or inventory result."
target = "rust"
command = "upgrade-3089-behavior"
evidence_paths = ["evidence/upgrade-3089-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #3089 pure upgrade design modules have not landed, so the EC import fails closed."
expected = "The behavior verifier remains a known failure until the design exposes frozen upgrade specification and verdict models plus pure start, transition, membership, recovery, reclamation, and convergence entry points."

[[tool.aw.python-ec.cases]]
id = "upgrade-3089-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/upgrade-3089-security.py"
promise = "The #3089 design fails closed for unrecognized or bypassing phases, collocated or unapproved target capacity, each missing cutover predicate, learner quorum, premature routing, and a non-exact steady inventory. It names the refusing phase, capacity identifier, and quorum field rather than overclaiming an upgrade is safe. The closest existing topology capability and use case are reused because this is the production primary-replica topology upgrade dimension."
oracle = "The verifier supplies literal invalid phases, explicit collocated and unapproved node candidates, each false evidence predicate, an explicit learner quorum, and a pre-commit route switch to pure design deciders. It checks EC-owned refusal vocabulary and named fields plus the concrete non-cutover or non-converged values; expected values are never read from the design and a design-computed pass flag cannot satisfy a row."
target = "rust"
command = "upgrade-3089-security"
evidence_paths = ["evidence/upgrade-3089-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #3089 pure upgrade design modules have not landed, so the EC import fails closed."
expected = "The security verifier remains a known failure until the design exposes frozen fail-closed upgrade verdicts and pure start, transition, membership, and convergence entry points."

[[tool.aw.python-ec.cases]]
id = "release-3090-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/release-3090-behavior.py"
promise = "The #3090 release-rollout design serializes replicated-shard member work, canaries a non-voter outside quorum, rolls voters before a leader, transfers leadership before draining that leader, derives joint voter protection, routes mixed-version traffic from supplied facts, pauses exact failed replacements, and admits replica work and cleanup only after voter convergence. This reuses the closest existing topology capability and use case because #3090 extends replicated topology rollout behavior."
oracle = "The verifier imports the pure rollout design and drives every #3090 observable entry point with EC-owned member, quorum, lag, term, request, failure, and convergence literals. It compares actual action names, member identities, voter sets, routes, pause fields, and desired temporary generations to the issue literals; it never accepts a design-provided boolean as proof."
target = "rust"
command = "release-3090-behavior"
evidence_paths = ["evidence/release-3090-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "Issue #3090 is blocked by #3089 and the checked-out raft runtime has no joint-consensus member-handoff workflow; the release-rollout pure design module has not landed."
expected = "The behavior verifier fails closed with ImportError until the #3090 design exposes the frozen release_rollout and release_rollout_verdict API, then passes only for the EC-owned serial rollout literals."

[[tool.aw.python-ec.cases]]
id = "release-3090-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/release-3090-security.py"
promise = "The #3090 release-rollout design fails closed on uncommitted membership, unhealthy quorum, applied lag, leader-first replacement, unconfirmed leader drain, two unavailable joint voters, inconsistent reads, post-failure later-shard work, and pre-convergence replica work or temporary cleanup. This reuses the closest existing topology capability and use case because #3090 extends replicated topology rollout behavior."
oracle = "The verifier drives explicit forbidden inputs at decide_release_rollout, decide_disruption_protection, decide_request_route, and record_replacement_failure. It compares literal refusal reasons and field paths to #3090, while the neighboring admissible routes and actions are independently held by the behavior case."
target = "rust"
command = "release-3090-security"
evidence_paths = ["evidence/release-3090-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "Issue #3090 is blocked by #3089 and the checked-out raft runtime has no joint-consensus member-handoff workflow; the release-rollout pure design module has not landed."
expected = "The security verifier fails closed with ImportError until the #3090 design exposes its frozen fail-closed verdict vocabulary, then passes only when every forbidden supplied state is named and refused."

[[tool.aw.python-ec.cases]]
id = "release-3091-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/release-3091-behavior.py"
promise = "The #3091 release design selects only an N-1/N-readable write format, admits declared current and previous readers, persists active epochs and generation, gates activation and explicit-generation finalization on its pure authorization predicates and verified old-epoch BackupSet, permits pre-finalize rollback, activates additive N behavior while retaining prior calls after finalization, converges repeated intents, and admits only additive dual-readable evolution. The existing topology capability/use case is the closest inventory mapping."
oracle = "The verifier imports the release design and drives every listed pure entry point with EC-owned literal release specs, statuses, descriptors, member formats, reader versions, and intents. Every expected value is transcribed from issue #3091 R1-R11; no expectation is read back out of the design, and no design-computed pass flag is accepted in place of an observed value."
target = "rust"
command = "release-3091-behavior"
evidence_paths = ["evidence/release-3091-behavior.json"]


[tool.aw.python-ec.cases.known_failure]
reason = "The #3091 pure design modules (lumen.release.spec, lumen.release.verdict, lumen.release.admission, lumen.release.status) have not landed, so the behavior verifier fails closed on import."
expected = "The behavior verifier remains a known failure until the design exposes the entry points #3091 names; the gate reports a known_failure that starts passing, so this block expires loudly once the design lands."

[[tool.aw.python-ec.cases]]
id = "release-3091-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/release-3091-security.py"
promise = "The #3091 release design fails closed on no common write format, unknown reader versions, every independent activation authorization failure, missing or stale finalize generation, unsatisfied soak, incomplete or wrong-epoch BackupSet, post-finalize rollback, N-1 downgrade after finalization, and destructive or unversioned evolution. The existing topology capability/use case is the closest inventory mapping."
oracle = "Each refusal is driven with an EC-owned literal forbidden input and checked against the exact typed reason and, where actionable, field path named by issue #3091 R1/R2/R5-R9/R11. Neighbouring valid inputs remain admitted, so a blanket-reject implementation cannot pass; runtime storage, process, traffic, and replay claims are intentionally not modeled."
target = "rust"
command = "release-3091-security"
evidence_paths = ["evidence/release-3091-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #3091 pure design modules (lumen.release.spec, lumen.release.verdict, lumen.release.admission, lumen.release.status) have not landed, so the security verifier fails closed on import."
expected = "The security verifier remains a known failure until the design exposes the entry points #3091 names; the gate reports a known_failure that starts passing, so this block expires loudly once the design lands."

[[tool.aw.python-ec.cases]]
id = "release-upgrade-3092-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/release-upgrade-3092-behavior.py"
promise = "The #3092 release-upgrade design admits an N-1-to-N journey with distinct immutable release identities, CRD-first two-operator ordering, safe one- and three-voter transition orders, explicit shared/N-only API activation, pinned image observations, and the complete pure evidence-manifest vocabulary. The closest existing topology capability and use case are reused because release upgrade is a production topology lifecycle dimension."
oracle = "The verifier imports and drives the pure release-upgrade design using EC-owned literal metadata, rollout, membership, API visibility, image-observation, and evidence requests. Every expected value is transcribed from #3092 R1-R6/R11/AC3; no expected value is read from the design, and no design-computed validity boolean is accepted in place of an observed contract value."
target = "rust"
command = "release-upgrade-3092-behavior"
evidence_paths = ["evidence/release-upgrade-3092-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #3092 pure release-upgrade design modules have not landed, so the EC import fails closed."
expected = "The behavior verifier remains a known failure until the design exposes frozen release identities and pure metadata, rollout, membership, API-visibility, image-observation, and evidence-validation entry points."

[[tool.aw.python-ec.cases]]
id = "release-upgrade-3092-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/release-upgrade-3092-security.py"
promise = "The #3092 release-upgrade design fails closed on equal release identity facts, non-CRD-first and unsafe membership orders, digest mismatches, concurrent lifecycle operations, missing finalize prerequisites, N-1 readiness after finalize, and overlapping operator leadership while retaining the pinned target, old authority, exact blocked facts, and CRD N rollback state. The closest existing topology capability and use case are reused because release upgrade is a production topology lifecycle dimension."
oracle = "The verifier drives every pure refusal entry point with explicit forbidden literals and checks exact reason vocabulary, named fields, retained digests, non-authoritative classification, per-kind lifecycle conflicts, the admitted neighbouring request, finalization state, readiness result, and operator handover value. Expected values are EC-owned literals from #3092 R1-R10/AC2/AC4-AC6; a design-computed pass boolean cannot satisfy a row."
target = "rust"
command = "release-upgrade-3092-security"
evidence_paths = ["evidence/release-upgrade-3092-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #3092 pure release-upgrade design modules have not landed, so the EC import fails closed."
expected = "The security verifier remains a known failure until the design exposes frozen refusal vocabulary and pure metadata, rollout, membership, image-observation, lifecycle, finalize, status, and operator-handover entry points."

[[tool.aw.python-ec.cases]]
id = "data-artifacts-3094-behavior"
artifact_id = "artifact:lumen/data-artifact-identity"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/data-artifacts-3094-behavior.py"
promise = "The #3094 data-artifact design exposes frozen complete identity, classification, UID-and-catalog-derived identity, and UID-fenced strict selection; kubectl inventory and real producer emission remain runtime proof. This reuses the closest existing Kubernetes topology capability and use case."
oracle = "The verifier drives only the pure data-artifact design with EC-owned literal facts and compares observed identity, classification, selector, and projection values against issue #3094 R1/R3/R4/R6/R7 and AC2; no expected value is read from the design."
target = "rust"
command = "data-artifacts-3094-behavior"
evidence_paths = ["evidence/data-artifacts-3094-behavior.json"]


[tool.aw.python-ec.cases.known_failure]
reason = "The #3094 pure design modules (lumen.data_artifacts.spec, lumen.data_artifacts.verdict, lumen.data_artifacts.identity, lumen.data_artifacts.inventory) have not landed, so the behavior verifier fails closed on import."
expected = "The behavior verifier remains a known failure until the design exposes the entry points #3094 names; the gate reports a known_failure that starts passing, so this block expires loudly once the design lands."

[[tool.aw.python-ec.cases]]
id = "data-artifacts-3094-security"
artifact_id = "artifact:lumen/data-artifact-identity"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/data-artifacts-3094-security.py"
promise = "The #3094 data-artifact design refuses mutable-label authority, incomplete selectors, and unsafe legacy or deletion candidates; it admits only the neighbouring exact legacy match. This reuses the closest existing Kubernetes topology capability and use case."
oracle = "The verifier supplies explicit forbidden facts at every applicable design entry point and checks EC-owned refusal reasons, named fields, admitted neighbours, deletion candidates, and exact inventory projection against issue #3094 R4/R5/R6/R7 and AC3/AC4."
target = "rust"
command = "data-artifacts-3094-security"
evidence_paths = ["evidence/data-artifacts-3094-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #3094 pure design modules (lumen.data_artifacts.spec, lumen.data_artifacts.verdict, lumen.data_artifacts.identity, lumen.data_artifacts.inventory) have not landed, so the security verifier fails closed on import."
expected = "The security verifier remains a known failure until the design exposes the entry points #3094 names; the gate reports a known_failure that starts passing, so this block expires loudly once the design lands."

[[tool.aw.python-ec.cases]]
id = "deletion-3095-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/deletion-3095-behavior.py"
promise = "The #3095 deletion design installs one Retain-only Lumen data-lifecycle finalizer, claims DeleteInstance, settles active operations at a rollback or complete-forward boundary before cleanup, orders non-data quiesce, retains exact closed-UID PVC and BackupSet inventories, chooses one idempotent resume action, and projects actionable deletion status. The closest existing topology capability and use case are reused because deletion finalization protects that deployed Lumen topology."
oracle = "The verifier imports and drives the pure deletion design with EC-owned literal finalizer, deletion, active-operation, child-presence, retained-resource, and wait facts. Every expected value is transcribed from #3095 R1-R7; no expectation is read back out of the design, and no design-computed validity flag is accepted in place of the observed identifier, policy, admission, boundary, action order, inventory, resume action, or status value."
target = "rust"
command = "deletion-3095-behavior"
evidence_paths = ["evidence/deletion-3095-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #3095 pure deletion design modules have not landed, so the EC import fails closed."
expected = "The behavior verifier remains a known failure until the design exposes the frozen deletion specification, admitted finalizer and lifecycle verdicts, and pure finalizer, admission, phase, inventory, resume, and status entry points."

[[tool.aw.python-ec.cases]]
id = "deletion-3095-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/deletion-3095-security.py"
promise = "The #3095 deletion design refuses Destroy policy, every named competing lifecycle mutation and duplicate deletion during a claimed deletion, and ambiguous closed-UID inventory; it keeps the Retain finalizer on ambiguity and turns unsafe waits, including timed-out waits, into actionable DeletionBlocked outcomes rather than silent data deletion. The closest existing topology capability and use case are reused because deletion finalization protects that deployed Lumen topology."
oracle = "The verifier drives pure finalizer, mutation-admission, inventory, status, and wait deciders with EC-owned literal forbidden inputs. It checks exact refusal and blocked-state vocabulary, the named offending policy field, a neighbouring Retain admission, finalizer disposition, and next action; expected values are never read from the design and a design-computed boolean cannot satisfy a row."
target = "rust"
command = "deletion-3095-security"
evidence_paths = ["evidence/deletion-3095-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #3095 pure deletion design modules have not landed, so the EC import fails closed."
expected = "The security verifier remains a known failure until the design exposes fail-closed finalizer, admission, inventory, status, and blocked-wait verdicts."

[[tool.aw.python-ec.cases]]
id = "retained-data-3096-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/retained-data-3096-behavior.py"
promise = "The #3096 retained-data design preserves complete BackupSet lineage and daily latest-two selection, immutable authoritative PVC metadata, deterministic exact-UID kubectl and lumen llm inventory commands, new-UID restore membership and catalog authority, and age/count/bytes/recovery-pointer evidence. The closest existing topology capability and use case are reused because retained deletion and recovery evidence is the production shard topology's data-recovery dimension."
oracle = "The verifier imports and drives the pure retained-data design with EC-owned literal BackupSet, PVC, retention-policy, deleted-UID, target-UID, and recovery-pointer inputs. Every expected value is transcribed from #3096 R1/R2/R3/R4/R7; no expectation is read from the design and no design-computed validity flag is accepted in place of observed lineage, selection, command, binding, or status values."
target = "rust"
command = "retained-data-3096-behavior"
evidence_paths = ["evidence/retained-data-3096-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #3096 pure retained-data design modules have not landed, so the EC import fails closed."
expected = "The behavior verifier remains a known failure until the design exposes frozen retained metadata plus pure retention, inventory, restore-binding, and status-projection entry points."

[[tool.aw.python-ec.cases]]
id = "retained-data-3096-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/retained-data-3096-security.py"
promise = "The #3096 retained-data design fails closed for incomplete authoritative PVC identity, prefix UID inventory, namespace/name-only, StatefulSet-name-only, or claim-name-only PVC adoption, and incomplete, corrupt, incompatible, wrong-UID, or wrong-generation retained sets; only an explicit exact-UID adoption procedure and an eligible complete retained set are admitted, and status never claims price or automatic expiry. The closest existing topology capability and use case are reused because this is the production shard topology's retained-data safety dimension."
oracle = "The verifier drives all pure retained-data entry points with EC-owned malformed and neighbouring admissible literal inputs. It checks exact refusal vocabulary, named offending fields, explicit exact-UID admission, and absence of overclaiming status fields. Expected values are never read from the design and a design-computed boolean cannot satisfy a row."
target = "rust"
command = "retained-data-3096-security"
evidence_paths = ["evidence/retained-data-3096-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #3096 pure retained-data design modules have not landed, so the EC import fails closed."
expected = "The security verifier remains a known failure until the design exposes fail-closed retained-PVC and retained-set admission, exact-UID inventory, and non-overclaiming status projection."

[[tool.aw.python-ec.cases]]
id = "deletion-3097-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/deletion-3097-behavior.py"
promise = "The #3097 deletion design keeps Retain as the default, enters destructive work only for an explicit Delete at finalizer start, closes an immutable exact-UID inventory before ordered destruction, resumes only persisted closed work, and projects safe deletion status. The reused topology capability/use-case is the closest existing registration because pyproject.toml has no deletion-specific pair."
oracle = "The verifier imports the pure deletion design and drives its policy, decision, inventory, plan, progress, finalizer, and status entry points with EC-owned literal inputs. Every expected value is transcribed from #3097 R1-R8 and AC1-AC4; no expectation is read back out of the design and no design-computed pass flag is accepted."
target = "rust"
command = "deletion-3097-behavior"
evidence_paths = ["evidence/deletion-3097-behavior.json"]


[tool.aw.python-ec.cases.known_failure]
reason = "The #3097 pure design modules (lumen.deletion.__init__, lumen.deletion.policy, lumen.deletion.inventory, lumen.deletion.plan) have not landed, so the behavior verifier fails closed on import."
expected = "The behavior verifier remains a known failure until the design exposes the entry points #3097 names; the gate reports a known_failure that starts passing, so this block expires loudly once the design lands."

[[tool.aw.python-ec.cases]]
id = "deletion-3097-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/deletion-3097-security.py"
promise = "The #3097 deletion design fails closed on an unknown policy, ordinary reconciliation, missing, extra, unlabeled, or contradictory inventories, unquiesced destruction, unknown object keys, permission failures, and any status projection that exposes credentials. The reused topology capability/use-case is the closest existing registration because pyproject.toml has no deletion-specific pair."
oracle = "The verifier supplies explicitly invalid arguments at every applicable deletion entry point and compares values to EC-owned literals: refusal vocabulary, named policy field, absence of a plan, exact UID targets, nonterminal progress, closed-inventory-only resume, and credential-free DeletionBlocked projection."
target = "rust"
command = "deletion-3097-security"
evidence_paths = ["evidence/deletion-3097-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #3097 pure design modules (lumen.deletion.__init__, lumen.deletion.policy, lumen.deletion.inventory, lumen.deletion.plan) have not landed, so the security verifier fails closed on import."
expected = "The security verifier remains a known failure until the design exposes the entry points #3097 names; the gate reports a known_failure that starts passing, so this block expires loudly once the design lands."

[[tool.aw.python-ec.cases]]
id = "deletion-3098-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/deletion-3098-behavior.py"
promise = "The #3098 deletion design classifies omitted policy as Retain, preserves exact closed-UID authoritative PVC and complete BackupSet inventories while selecting public and compute resources, selects only the closed UID inventory for explicit Delete, and admits retained complete BackupSet recovery only to a new UID. The closest existing topology capability and use case are reused because deletion safety is a Kubernetes-native instance lifecycle dimension."
oracle = "The verifier imports and drives the pure deletion design with EC-owned literal UID-bound resource inventories, policy, completeness, labels, ambiguity, BackupSet, and restore-target inputs. Every expected value is transcribed from #3098 R2/R3/R4/R6 and AC1/AC2/AC3/AC4; no expected value is read from the design, and no design-computed validity flag is accepted in place of an observed inventory, identity, target, field, or refusal reason."
target = "rust"
command = "deletion-3098-behavior"
evidence_paths = ["evidence/deletion-3098-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #3098 pure deletion design modules have not landed, so the EC import fails closed."
expected = "The behavior verifier remains a known failure until the design exposes frozen UID-bound inventory and restore specifications, admitted deletion and restore verdicts, and pure Retain/Delete and restore-target deciders."

[[tool.aw.python-ec.cases]]
id = "deletion-3098-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/deletion-3098-security.py"
promise = "The #3098 deletion design fails closed for incomplete, unlabeled, and ambiguous inventories without selecting a destructive subset; requires a new UID for recovery; and refuses an old-UID PVC as a restore mount target. The closest existing topology capability and use case are reused because deletion safety is a Kubernetes-native instance lifecycle dimension."
oracle = "The verifier drives the pure deletion inventory and restore-target deciders with EC-owned literal bad inventories and explicit old-UID mount and same-UID target inputs. It checks exact typed refusal vocabulary, the named offending field, and empty destructive selection; expected values are never read from the design and a design-computed boolean cannot satisfy a row."
target = "rust"
command = "deletion-3098-security"
evidence_paths = ["evidence/deletion-3098-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #3098 pure deletion design modules have not landed, so the EC import fails closed."
expected = "The security verifier remains a known failure until the design exposes fail-closed inventory and restore verdicts with typed reasons, offending field paths, and an empty destructive selection for every blocked inventory."

[[tool.aw.python-ec.cases]]
id = "tls-rotation-3112-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/tls-rotation-3112-behavior.py"
promise = "The #3112 TLS-rotation design admits distinct externally provisioned read-only serving and peer Secret projections, activates valid serving and peer material at the next generation while preserving overlap trust, exposes a bounded redacted status snapshot, and classifies both TLS Secret surfaces as external. The closest existing topology capability and use case are reused because this is the production Lumen topology's TLS boundary."
oracle = "The verifier imports and drives the pure projection, serving-rotation, peer-rotation, status, and issuer-ownership design entry points with EC-owned literal inputs. Every expected value is transcribed from #3112 R1/R2/R3/R4/R5; no expected value is read from the design, and no design-computed boolean is accepted in place of an observed projection, generation, trust value, status value, or classification."
target = "rust"
command = "tls-rotation-3112-behavior"
evidence_paths = ["evidence/tls-rotation-3112-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #3112 pure TLS-rotation design modules have not landed, so the EC imports fail closed."
expected = "The behavior verifier remains a known failure until the design exposes frozen projection/material/state types, typed admitted and rejection results, pure admission deciders, redacted status derivation, and the ownership classifier boundary."

[[tool.aw.python-ec.cases]]
id = "tls-rotation-3112-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/tls-rotation-3112-security.py"
promise = "The #3112 TLS-rotation design fails closed for shared, writable, or controller-owned Secret projections; invalid serving material retains the last valid generation at both serving and generic rotation entry points; wrong-identity, untrusted, expired, and plaintext peers are refused; status cannot expose credential material; and prohibited or unknown issuer-ownership surfaces never admit. The closest existing topology capability and use case are reused because this is the production Lumen topology's TLS boundary."
oracle = "The verifier supplies explicit forbidden literals to every applicable pure entry point and checks exact refusal vocabulary, named fields, retained generation, neighbouring admitted inputs, sensitive-field absence, and fail-closed ownership classification. Expected values are never read from the design and a design-computed boolean cannot satisfy a row."
target = "rust"
command = "tls-rotation-3112-security"
evidence_paths = ["evidence/tls-rotation-3112-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #3112 pure TLS-rotation design modules have not landed, so the EC imports fail closed."
expected = "The security verifier remains a known failure until the design exposes fail-closed projection, serving, generic-rotation, and peer-rotation deciders, bounded redacted status, and the frozen issuer-ownership classifier."

[[tool.aw.python-ec.cases]]
id = "serving-tls-3113-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/serving-tls-3113-behavior.py"
promise = "The #3113 pure serving-TLS design admits an explicitly named external serving Secret over HTTPS on private Service port 7373, binds supplied private CA trust to the exact Service DNS/SNI name, pairs verified TLS only with the selected KSA token source, separates client trust input from external public-CA handoff, and names the required retained-proof vocabulary. The closest existing topology capability and use case are reused because this serving path is the production Service topology's private TLS dimension."
oracle = "The verifier imports and drives the pure #3113 admission, evidence, and trust-handoff entry points with EC-owned literal requests and expectations. It observes plan fields, typed rejection vocabulary, ownership classifications, and required-row values rather than any design-computed pass flag; no expected value is read back from the design."
target = "rust"
command = "serving-tls-3113-behavior"
evidence_paths = ["evidence/serving-tls-3113-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #3113 pure serving-TLS design modules have not landed, so the EC imports fail closed."
expected = "The behavior verifier remains a known failure until the design exposes the frozen request values, admitted plans, pure serving/connect deciders, required-evidence vocabulary, and trust-handoff classifications."

[[tool.aw.python-ec.cases]]
id = "serving-tls-3113-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/serving-tls-3113-security.py"
promise = "The #3113 pure serving-TLS design fails closed for an unnamed serving Secret, plaintext serving, localhost or loopback Service names, absent private CA, unverified TLS, and direct Google, ADC, GSA, or metadata tokens; it also forbids public-CA publishing and discovery surfaces. The closest existing topology capability and use case are reused because this serving path is the production Service topology's private TLS dimension."
oracle = "The verifier drives each pure admission entry point with explicit EC-owned forbidden values, checks typed refusal vocabulary and offending field paths, and preserves an explicit neighbouring secure admission. It separately observes every forbidden trust-handoff surface; expectations are literals, never design-derived values or design-computed booleans."
target = "rust"
command = "serving-tls-3113-security"
evidence_paths = ["evidence/serving-tls-3113-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #3113 pure serving-TLS design modules have not landed, so the EC imports fail closed."
expected = "The security verifier remains a known failure until the design exposes fail-closed serving, private-trust, and selected-KSA admission verdicts plus the closed trust-handoff classifier."

[[tool.aw.python-ec.cases]]
id = "release-artifacts-3130-behavior"
artifact_id = "artifact:lumen/release-artifacts"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/release-artifacts-3130-behavior.py"
promise = "The #3130 release-artifact design admits only the canonical immutable GHCR Lumen digest, admits a signed request with the intended repository/workflow identity, and renders digest-bound cosign, provenance, and SBOM verification guidance. Kubernetes-native-deployment / primary-replicas-topology are the closest existing registry identifiers; #3130 has no dedicated release-artifact capability or use case yet."
oracle = "The verifier imports the pure release-artifact design and drives every declared admission and handoff entry point with EC-owned literal references and identity values. Every expected value is transcribed from #3130 R2/R6/R8/AC3; no expected value is read from the design and no design-provided validity boolean is accepted."
target = "rust"
command = "release-artifacts-3130-behavior"
evidence_paths = ["evidence/release-artifacts-3130-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #3130 release-artifact design modules have not landed yet, so this import-first EC fails closed until the typed digest admission, verification identity, and handoff plan surface exists."
expected = "The behavior verifier remains red until the design admits the canonical GHCR digest and produces the EC-owned digest-bound verification-plan values."

[[tool.aw.python-ec.cases]]
id = "release-artifacts-3130-security"
artifact_id = "artifact:lumen/release-artifacts"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/release-artifacts-3130-security.py"
promise = "The #3130 release-artifact design fails closed on tag-only and unresolved subjects, absent signature evidence, repository/workflow identity mismatches, and mutable-tag handoff proofs. Kubernetes-native-deployment / primary-replicas-topology are the closest existing registry identifiers; #3130 has no dedicated release-artifact capability or use case yet."
oracle = "Each refusal is driven through its own pure entry point with an explicit EC-owned bad field and checked for the exact closed reason and, where applicable, field path. The companion behavior case supplies the nearby admissible digest handoff; no design-provided boolean is treated as proof."
target = "rust"
command = "release-artifacts-3130-security"
evidence_paths = ["evidence/release-artifacts-3130-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #3130 release-artifact design modules have not landed yet, so no typed fail-closed verdict vocabulary or entry-point enforcement exists for the external contract to import."
expected = "The security verifier remains red until each pure decider rejects the EC-owned tag-only, unresolved, unsigned, and wrong-identity inputs with the asserted reason and field path."

[[tool.aw.python-ec.cases]]
id = "lifecycle-3146-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/lifecycle-3146-behavior.py"
promise = "The #3146 lifecycle design gives Lumen one fail-closed lifecycle generation and reason; gates readiness on startup admission while keeping recovery and temporary global dependency loss health-positive; drains public admission with h2c GOAWAY; defines ordered hooks, a rendered total/runtime/reserve budget, and stable terminal Drained evidence. The closest existing topology capability and use case are reused because lifecycle is the production shard topology's serving dimension."
oracle = "The verifier imports and drives the pure lifecycle admission, probe, hook, budget, and status entry points with EC-owned literal facts and requests. Every expected value is transcribed from #3146 R1-R7; no expected value is read from the design, and no design-computed validity flag is accepted in place of an observed phase, generation, reason, action, plan, budget, verdict, or terminal condition."
target = "rust"
command = "lifecycle-3146-behavior"
evidence_paths = ["evidence/lifecycle-3146-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #3146 pure lifecycle design modules have not landed, so the EC import fails closed."
expected = "The behavior verifier remains a known failure until the design exposes frozen lifecycle facts and verdicts plus pure admission, probe, hook, budget, and status entry points."

[[tool.aw.python-ec.cases]]
id = "lifecycle-3146-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/lifecycle-3146-security.py"
promise = "The #3146 lifecycle design fails closed on unauthorised and unsupported triggers, incorrect-read or wrong-write-outcome read-only degradation, hooks outside Draining, budgets without SIGKILL reserve, and terminal status that overclaims Drained while a hook has not finished. The closest existing topology capability and use case are reused because lifecycle is the production shard topology's serving dimension."
oracle = "The verifier drives each pure lifecycle entry point with explicit forbidden literals and checks exact refusal vocabulary, the named field, neighbouring admitted input, the required unhealthy/not-ready values, and terminal condition values. Expected values are never read from the design and a design-computed boolean cannot satisfy a row."
target = "rust"
command = "lifecycle-3146-security"
evidence_paths = ["evidence/lifecycle-3146-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #3146 pure lifecycle design modules have not landed, so the EC import fails closed."
expected = "The security verifier remains a known failure until the design exposes fail-closed lifecycle admission, probe, hook, budget, and terminal-status verdicts."

[[tool.aw.python-ec.cases]]
id = "operational-log-integration-2377-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/operational-log-integration-2377-behavior.py"
promise = "The #2377 pure operational-log integration design admits complete reusable-gate records and complete structured-event coverage, classifies shared/non-domain, app-domain-only, and mixed failures, splits mixed repair from issue-backed app-domain skip, and terminalizes only passed or validated tracked_skip(#issue). The closest existing topology capability and use case are reused because the inventory has no operational-observability entry for this verification journey."
oracle = "The verifier imports and drives only the frozen pure gate-record, coverage, ownership, mixed-disposition, and terminal-result deciders with EC-owned literals. Every expected value is transcribed from #2377 R1-R4 and AC1/AC3; no expected value is read from the design and no design-computed validity boolean is accepted in place of an observed value."
target = "rust"
command = "operational-log-integration-2377-behavior"
evidence_paths = ["evidence/operational-log-integration-2377-behavior.json"]

[[tool.aw.python-ec.cases]]
id = "operational-log-integration-2377-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/operational-log-integration-2377-security.py"
promise = "The #2377 pure operational-log integration design fails closed on each missing gate-record field, each missing required event kind, missing bounded issue or exact reproduction, unchecked authoritative existing domain WIs, and shared/non-domain or unresolved mixed terminalization. The closest existing topology capability and use case are reused because the inventory has no operational-observability entry for this verification journey."
oracle = "The verifier supplies explicit forbidden inputs to every applicable pure entry point and checks EC-owned refusal vocabulary, the named offending field or missing kind, and admitted neighbouring declarations. It never derives expected values from design observations and never accepts a design-computed boolean as proof."
target = "rust"
command = "operational-log-integration-2377-security"
evidence_paths = ["evidence/operational-log-integration-2377-security.json"]

[[tool.aw.python-ec.cases]]
id = "body-limit-2584-behavior"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "behavior"
applicability = "td"
test_path = "src/body-limit-2584-behavior.py"
promise = "The #2584 pure body-limit design admits only the optional positive-u64 bodyLimitBytes contract, preserves Lumen's compiled 8388608-byte default when omitted, and makes a valid explicit override both effective and observable. The closest existing topology capability and use case are reused because this is an operator-configured serving-topology control-plane setting."
oracle = "The verifier imports and drives the pure body-limit admission decider with EC-owned literal omitted, lower-bound, upper-bound, and valid-override inputs. Every expected value is transcribed from #2584 R1/R2/R4 and AC1; no expected value is read from the design and no design-computed boolean can satisfy a row."
target = "rust"
command = "body-limit-2584-behavior"
evidence_paths = ["evidence/body-limit-2584-behavior.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2584 pure body-limit design modules have not landed, so the EC import fails closed."
expected = "The behavior verifier remains a known failure until the design exposes the frozen optional body-limit specification, admitted effective-limit verdict, and pure admission entry point."

[[tool.aw.python-ec.cases]]
id = "body-limit-2584-security"
artifact_id = "artifact:lumen/topology-public-contract"
capability_id = "kubernetes-native-deployment"
use_case_id = "primary-replicas-topology"
dimension = "security"
applicability = "td"
test_path = "src/body-limit-2584-security.py"
promise = "The #2584 pure body-limit design fails closed on boolean, zero, and u64-overflow bodyLimitBytes inputs, names body_limit_bytes for actionable refusal, never substitutes the compiled default for an invalid value, and still admits the neighbouring positive lower-bound value. The closest existing topology capability and use case are reused because this is an operator-configured serving-topology control-plane setting."
oracle = "The verifier drives explicit hostile body-limit inputs into the pure admission entry point and checks EC-owned literal refusal vocabulary, field paths, absence of an effective fallback, and the neighbouring admitted lower-bound value. Expected values are never read from the design and no validity boolean can satisfy a row."
target = "rust"
command = "body-limit-2584-security"
evidence_paths = ["evidence/body-limit-2584-security.json"]

[tool.aw.python-ec.cases.known_failure]
reason = "The #2584 pure body-limit design modules have not landed, so the EC import fails closed."
expected = "The security verifier remains a known failure until the design exposes the exact fail-closed rejection vocabulary, body_limit_bytes field path, and no-fallback invalid-value verdicts."
