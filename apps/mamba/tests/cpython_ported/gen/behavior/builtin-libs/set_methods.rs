use super::super::super::super::harness::*;

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_bytes__test_copy.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_bytes__test_copy() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_bytes__test_copy"
# subject = "cpython.test_set.TestBasicOpsBytes.test_copy"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsBytes::test_copy
"""Auto-ported test: TestBasicOpsBytes::test_copy (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'bytes set'
self_values = [b'a', b'b', b'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3

assert self_set.copy() == self_dup
print("TestBasicOpsBytes::test_copy: ok")
"###);
    assert_output(&out, r###"TestBasicOpsBytes::test_copy: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_bytes__test_empty_difference.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_bytes__test_empty_difference() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_bytes__test_empty_difference"
# subject = "cpython.test_set.TestBasicOpsBytes.test_empty_difference"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsBytes::test_empty_difference
"""Auto-ported test: TestBasicOpsBytes::test_empty_difference (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'bytes set'
self_values = [b'a', b'b', b'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
result = self_set - empty_set

assert result == self_dup
print("TestBasicOpsBytes::test_empty_difference: ok")
"###);
    assert_output(&out, r###"TestBasicOpsBytes::test_empty_difference: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_bytes__test_empty_difference_rev.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_bytes__test_empty_difference_rev() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_bytes__test_empty_difference_rev"
# subject = "cpython.test_set.TestBasicOpsBytes.test_empty_difference_rev"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsBytes::test_empty_difference_rev
"""Auto-ported test: TestBasicOpsBytes::test_empty_difference_rev (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'bytes set'
self_values = [b'a', b'b', b'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
result = empty_set - self_set

assert result == empty_set
print("TestBasicOpsBytes::test_empty_difference_rev: ok")
"###);
    assert_output(&out, r###"TestBasicOpsBytes::test_empty_difference_rev: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_bytes__test_empty_intersection.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_bytes__test_empty_intersection() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_bytes__test_empty_intersection"
# subject = "cpython.test_set.TestBasicOpsBytes.test_empty_intersection"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsBytes::test_empty_intersection
"""Auto-ported test: TestBasicOpsBytes::test_empty_intersection (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'bytes set'
self_values = [b'a', b'b', b'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
result = self_set & empty_set

assert result == empty_set
print("TestBasicOpsBytes::test_empty_intersection: ok")
"###);
    assert_output(&out, r###"TestBasicOpsBytes::test_empty_intersection: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_bytes__test_empty_isdisjoint.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_bytes__test_empty_isdisjoint() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_bytes__test_empty_isdisjoint"
# subject = "cpython.test_set.TestBasicOpsBytes.test_empty_isdisjoint"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsBytes::test_empty_isdisjoint
"""Auto-ported test: TestBasicOpsBytes::test_empty_isdisjoint (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'bytes set'
self_values = [b'a', b'b', b'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
result = self_set.isdisjoint(empty_set)

assert result == True
print("TestBasicOpsBytes::test_empty_isdisjoint: ok")
"###);
    assert_output(&out, r###"TestBasicOpsBytes::test_empty_isdisjoint: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_bytes__test_empty_symmetric_difference.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_bytes__test_empty_symmetric_difference() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_bytes__test_empty_symmetric_difference"
# subject = "cpython.test_set.TestBasicOpsBytes.test_empty_symmetric_difference"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsBytes::test_empty_symmetric_difference
"""Auto-ported test: TestBasicOpsBytes::test_empty_symmetric_difference (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'bytes set'
self_values = [b'a', b'b', b'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
result = self_set ^ empty_set

assert result == self_set
print("TestBasicOpsBytes::test_empty_symmetric_difference: ok")
"###);
    assert_output(&out, r###"TestBasicOpsBytes::test_empty_symmetric_difference: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_bytes__test_empty_union.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_bytes__test_empty_union() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_bytes__test_empty_union"
# subject = "cpython.test_set.TestBasicOpsBytes.test_empty_union"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsBytes::test_empty_union
"""Auto-ported test: TestBasicOpsBytes::test_empty_union (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'bytes set'
self_values = [b'a', b'b', b'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
result = self_set | empty_set

assert result == self_dup
print("TestBasicOpsBytes::test_empty_union: ok")
"###);
    assert_output(&out, r###"TestBasicOpsBytes::test_empty_union: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_bytes__test_equivalent_equality.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_bytes__test_equivalent_equality() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_bytes__test_equivalent_equality"
# subject = "cpython.test_set.TestBasicOpsBytes.test_equivalent_equality"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsBytes::test_equivalent_equality
"""Auto-ported test: TestBasicOpsBytes::test_equivalent_equality (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'bytes set'
self_values = [b'a', b'b', b'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3

assert self_set == self_dup
print("TestBasicOpsBytes::test_equivalent_equality: ok")
"###);
    assert_output(&out, r###"TestBasicOpsBytes::test_equivalent_equality: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_bytes__test_intersection_empty.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_bytes__test_intersection_empty() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_bytes__test_intersection_empty"
# subject = "cpython.test_set.TestBasicOpsBytes.test_intersection_empty"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsBytes::test_intersection_empty
"""Auto-ported test: TestBasicOpsBytes::test_intersection_empty (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'bytes set'
self_values = [b'a', b'b', b'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
result = empty_set & self_set

assert result == empty_set
print("TestBasicOpsBytes::test_intersection_empty: ok")
"###);
    assert_output(&out, r###"TestBasicOpsBytes::test_intersection_empty: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_bytes__test_isdisjoint_empty.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_bytes__test_isdisjoint_empty() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_bytes__test_isdisjoint_empty"
# subject = "cpython.test_set.TestBasicOpsBytes.test_isdisjoint_empty"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsBytes::test_isdisjoint_empty
"""Auto-ported test: TestBasicOpsBytes::test_isdisjoint_empty (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'bytes set'
self_values = [b'a', b'b', b'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
result = empty_set.isdisjoint(self_set)

assert result == True
print("TestBasicOpsBytes::test_isdisjoint_empty: ok")
"###);
    assert_output(&out, r###"TestBasicOpsBytes::test_isdisjoint_empty: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_bytes__test_length.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_bytes__test_length() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_bytes__test_length"
# subject = "cpython.test_set.TestBasicOpsBytes.test_length"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsBytes::test_length
"""Auto-ported test: TestBasicOpsBytes::test_length (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'bytes set'
self_values = [b'a', b'b', b'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3

assert len(self_set) == self_length
print("TestBasicOpsBytes::test_length: ok")
"###);
    assert_output(&out, r###"TestBasicOpsBytes::test_length: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_bytes__test_pickling.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_bytes__test_pickling() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_bytes__test_pickling"
# subject = "cpython.test_set.TestBasicOpsBytes.test_pickling"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsBytes::test_pickling
"""Auto-ported test: TestBasicOpsBytes::test_pickling (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'bytes set'
self_values = [b'a', b'b', b'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
for proto in range(pickle.HIGHEST_PROTOCOL + 1):
    p = pickle.dumps(self_set, proto)
    copy = pickle.loads(p)

    assert self_set == copy
print("TestBasicOpsBytes::test_pickling: ok")
"###);
    assert_output(&out, r###"TestBasicOpsBytes::test_pickling: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_bytes__test_repr.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_bytes__test_repr() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_bytes__test_repr"
# subject = "cpython.test_set.TestBasicOpsBytes.test_repr"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsBytes::test_repr
"""Auto-ported test: TestBasicOpsBytes::test_repr (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
def check_repr_against_values():
    text = repr(self_set)

    assert text.startswith('{')

    assert text.endswith('}')
    result = text[1:-1].split(', ')
    result.sort()
    sorted_repr_values = [repr(value) for value in self_values]
    sorted_repr_values.sort()

    assert result == sorted_repr_values
self_case = 'bytes set'
self_values = [b'a', b'b', b'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
check_repr_against_values()
print("TestBasicOpsBytes::test_repr: ok")
"###);
    assert_output(&out, r###"TestBasicOpsBytes::test_repr: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_bytes__test_self_difference.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_bytes__test_self_difference() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_bytes__test_self_difference"
# subject = "cpython.test_set.TestBasicOpsBytes.test_self_difference"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsBytes::test_self_difference
"""Auto-ported test: TestBasicOpsBytes::test_self_difference (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'bytes set'
self_values = [b'a', b'b', b'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
result = self_set - self_set

assert result == empty_set
print("TestBasicOpsBytes::test_self_difference: ok")
"###);
    assert_output(&out, r###"TestBasicOpsBytes::test_self_difference: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_bytes__test_self_equality.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_bytes__test_self_equality() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_bytes__test_self_equality"
# subject = "cpython.test_set.TestBasicOpsBytes.test_self_equality"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsBytes::test_self_equality
"""Auto-ported test: TestBasicOpsBytes::test_self_equality (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'bytes set'
self_values = [b'a', b'b', b'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3

assert self_set == self_set
print("TestBasicOpsBytes::test_self_equality: ok")
"###);
    assert_output(&out, r###"TestBasicOpsBytes::test_self_equality: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_bytes__test_self_intersection.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_bytes__test_self_intersection() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_bytes__test_self_intersection"
# subject = "cpython.test_set.TestBasicOpsBytes.test_self_intersection"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsBytes::test_self_intersection
"""Auto-ported test: TestBasicOpsBytes::test_self_intersection (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'bytes set'
self_values = [b'a', b'b', b'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
result = self_set & self_set

assert result == self_dup
print("TestBasicOpsBytes::test_self_intersection: ok")
"###);
    assert_output(&out, r###"TestBasicOpsBytes::test_self_intersection: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_bytes__test_self_isdisjoint.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_bytes__test_self_isdisjoint() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_bytes__test_self_isdisjoint"
# subject = "cpython.test_set.TestBasicOpsBytes.test_self_isdisjoint"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsBytes::test_self_isdisjoint
"""Auto-ported test: TestBasicOpsBytes::test_self_isdisjoint (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'bytes set'
self_values = [b'a', b'b', b'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
result = self_set.isdisjoint(self_set)

assert result == (not self_set)
print("TestBasicOpsBytes::test_self_isdisjoint: ok")
"###);
    assert_output(&out, r###"TestBasicOpsBytes::test_self_isdisjoint: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_bytes__test_self_symmetric_difference.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_bytes__test_self_symmetric_difference() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_bytes__test_self_symmetric_difference"
# subject = "cpython.test_set.TestBasicOpsBytes.test_self_symmetric_difference"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsBytes::test_self_symmetric_difference
"""Auto-ported test: TestBasicOpsBytes::test_self_symmetric_difference (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'bytes set'
self_values = [b'a', b'b', b'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
result = self_set ^ self_set

assert result == empty_set
print("TestBasicOpsBytes::test_self_symmetric_difference: ok")
"###);
    assert_output(&out, r###"TestBasicOpsBytes::test_self_symmetric_difference: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_bytes__test_self_union.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_bytes__test_self_union() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_bytes__test_self_union"
# subject = "cpython.test_set.TestBasicOpsBytes.test_self_union"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsBytes::test_self_union
"""Auto-ported test: TestBasicOpsBytes::test_self_union (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'bytes set'
self_values = [b'a', b'b', b'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
result = self_set | self_set

assert result == self_dup
print("TestBasicOpsBytes::test_self_union: ok")
"###);
    assert_output(&out, r###"TestBasicOpsBytes::test_self_union: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_bytes__test_union_empty.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_bytes__test_union_empty() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_bytes__test_union_empty"
# subject = "cpython.test_set.TestBasicOpsBytes.test_union_empty"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsBytes::test_union_empty
"""Auto-ported test: TestBasicOpsBytes::test_union_empty (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'bytes set'
self_values = [b'a', b'b', b'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
result = empty_set | self_set

assert result == self_dup
print("TestBasicOpsBytes::test_union_empty: ok")
"###);
    assert_output(&out, r###"TestBasicOpsBytes::test_union_empty: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_empty__test_copy.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_empty__test_copy() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_empty__test_copy"
# subject = "cpython.test_set.TestBasicOpsEmpty.test_copy"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsEmpty::test_copy
"""Auto-ported test: TestBasicOpsEmpty::test_copy (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'empty set'
self_values = []
self_set = set(self_values)
self_dup = set(self_values)
self_length = 0
self_repr = 'set()'

assert self_set.copy() == self_dup
print("TestBasicOpsEmpty::test_copy: ok")
"###);
    assert_output(&out, r###"TestBasicOpsEmpty::test_copy: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_empty__test_empty_difference.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_empty__test_empty_difference() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_empty__test_empty_difference"
# subject = "cpython.test_set.TestBasicOpsEmpty.test_empty_difference"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsEmpty::test_empty_difference
"""Auto-ported test: TestBasicOpsEmpty::test_empty_difference (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'empty set'
self_values = []
self_set = set(self_values)
self_dup = set(self_values)
self_length = 0
self_repr = 'set()'
result = self_set - empty_set

assert result == self_dup
print("TestBasicOpsEmpty::test_empty_difference: ok")
"###);
    assert_output(&out, r###"TestBasicOpsEmpty::test_empty_difference: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_empty__test_empty_difference_rev.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_empty__test_empty_difference_rev() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_empty__test_empty_difference_rev"
# subject = "cpython.test_set.TestBasicOpsEmpty.test_empty_difference_rev"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsEmpty::test_empty_difference_rev
"""Auto-ported test: TestBasicOpsEmpty::test_empty_difference_rev (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'empty set'
self_values = []
self_set = set(self_values)
self_dup = set(self_values)
self_length = 0
self_repr = 'set()'
result = empty_set - self_set

assert result == empty_set
print("TestBasicOpsEmpty::test_empty_difference_rev: ok")
"###);
    assert_output(&out, r###"TestBasicOpsEmpty::test_empty_difference_rev: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_empty__test_empty_intersection.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_empty__test_empty_intersection() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_empty__test_empty_intersection"
# subject = "cpython.test_set.TestBasicOpsEmpty.test_empty_intersection"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsEmpty::test_empty_intersection
"""Auto-ported test: TestBasicOpsEmpty::test_empty_intersection (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'empty set'
self_values = []
self_set = set(self_values)
self_dup = set(self_values)
self_length = 0
self_repr = 'set()'
result = self_set & empty_set

assert result == empty_set
print("TestBasicOpsEmpty::test_empty_intersection: ok")
"###);
    assert_output(&out, r###"TestBasicOpsEmpty::test_empty_intersection: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_empty__test_empty_isdisjoint.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_empty__test_empty_isdisjoint() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_empty__test_empty_isdisjoint"
# subject = "cpython.test_set.TestBasicOpsEmpty.test_empty_isdisjoint"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsEmpty::test_empty_isdisjoint
"""Auto-ported test: TestBasicOpsEmpty::test_empty_isdisjoint (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'empty set'
self_values = []
self_set = set(self_values)
self_dup = set(self_values)
self_length = 0
self_repr = 'set()'
result = self_set.isdisjoint(empty_set)

assert result == True
print("TestBasicOpsEmpty::test_empty_isdisjoint: ok")
"###);
    assert_output(&out, r###"TestBasicOpsEmpty::test_empty_isdisjoint: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_empty__test_empty_symmetric_difference.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_empty__test_empty_symmetric_difference() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_empty__test_empty_symmetric_difference"
# subject = "cpython.test_set.TestBasicOpsEmpty.test_empty_symmetric_difference"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsEmpty::test_empty_symmetric_difference
"""Auto-ported test: TestBasicOpsEmpty::test_empty_symmetric_difference (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'empty set'
self_values = []
self_set = set(self_values)
self_dup = set(self_values)
self_length = 0
self_repr = 'set()'
result = self_set ^ empty_set

assert result == self_set
print("TestBasicOpsEmpty::test_empty_symmetric_difference: ok")
"###);
    assert_output(&out, r###"TestBasicOpsEmpty::test_empty_symmetric_difference: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_empty__test_empty_union.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_empty__test_empty_union() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_empty__test_empty_union"
# subject = "cpython.test_set.TestBasicOpsEmpty.test_empty_union"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsEmpty::test_empty_union
"""Auto-ported test: TestBasicOpsEmpty::test_empty_union (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'empty set'
self_values = []
self_set = set(self_values)
self_dup = set(self_values)
self_length = 0
self_repr = 'set()'
result = self_set | empty_set

assert result == self_dup
print("TestBasicOpsEmpty::test_empty_union: ok")
"###);
    assert_output(&out, r###"TestBasicOpsEmpty::test_empty_union: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_empty__test_equivalent_equality.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_empty__test_equivalent_equality() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_empty__test_equivalent_equality"
# subject = "cpython.test_set.TestBasicOpsEmpty.test_equivalent_equality"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsEmpty::test_equivalent_equality
"""Auto-ported test: TestBasicOpsEmpty::test_equivalent_equality (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'empty set'
self_values = []
self_set = set(self_values)
self_dup = set(self_values)
self_length = 0
self_repr = 'set()'

assert self_set == self_dup
print("TestBasicOpsEmpty::test_equivalent_equality: ok")
"###);
    assert_output(&out, r###"TestBasicOpsEmpty::test_equivalent_equality: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_empty__test_intersection_empty.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_empty__test_intersection_empty() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_empty__test_intersection_empty"
# subject = "cpython.test_set.TestBasicOpsEmpty.test_intersection_empty"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsEmpty::test_intersection_empty
"""Auto-ported test: TestBasicOpsEmpty::test_intersection_empty (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'empty set'
self_values = []
self_set = set(self_values)
self_dup = set(self_values)
self_length = 0
self_repr = 'set()'
result = empty_set & self_set

assert result == empty_set
print("TestBasicOpsEmpty::test_intersection_empty: ok")
"###);
    assert_output(&out, r###"TestBasicOpsEmpty::test_intersection_empty: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_empty__test_isdisjoint_empty.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_empty__test_isdisjoint_empty() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_empty__test_isdisjoint_empty"
# subject = "cpython.test_set.TestBasicOpsEmpty.test_isdisjoint_empty"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsEmpty::test_isdisjoint_empty
"""Auto-ported test: TestBasicOpsEmpty::test_isdisjoint_empty (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'empty set'
self_values = []
self_set = set(self_values)
self_dup = set(self_values)
self_length = 0
self_repr = 'set()'
result = empty_set.isdisjoint(self_set)

assert result == True
print("TestBasicOpsEmpty::test_isdisjoint_empty: ok")
"###);
    assert_output(&out, r###"TestBasicOpsEmpty::test_isdisjoint_empty: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_empty__test_length.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_empty__test_length() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_empty__test_length"
# subject = "cpython.test_set.TestBasicOpsEmpty.test_length"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsEmpty::test_length
"""Auto-ported test: TestBasicOpsEmpty::test_length (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'empty set'
self_values = []
self_set = set(self_values)
self_dup = set(self_values)
self_length = 0
self_repr = 'set()'

assert len(self_set) == self_length
print("TestBasicOpsEmpty::test_length: ok")
"###);
    assert_output(&out, r###"TestBasicOpsEmpty::test_length: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_empty__test_pickling.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_empty__test_pickling() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_empty__test_pickling"
# subject = "cpython.test_set.TestBasicOpsEmpty.test_pickling"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsEmpty::test_pickling
"""Auto-ported test: TestBasicOpsEmpty::test_pickling (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'empty set'
self_values = []
self_set = set(self_values)
self_dup = set(self_values)
self_length = 0
self_repr = 'set()'
for proto in range(pickle.HIGHEST_PROTOCOL + 1):
    p = pickle.dumps(self_set, proto)
    copy = pickle.loads(p)

    assert self_set == copy
print("TestBasicOpsEmpty::test_pickling: ok")
"###);
    assert_output(&out, r###"TestBasicOpsEmpty::test_pickling: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_empty__test_repr.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_empty__test_repr() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_empty__test_repr"
# subject = "cpython.test_set.TestBasicOpsEmpty.test_repr"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsEmpty::test_repr
"""Auto-ported test: TestBasicOpsEmpty::test_repr (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'empty set'
self_values = []
self_set = set(self_values)
self_dup = set(self_values)
self_length = 0
self_repr = 'set()'
if self_repr is not None:

    assert repr(self_set) == self_repr
print("TestBasicOpsEmpty::test_repr: ok")
"###);
    assert_output(&out, r###"TestBasicOpsEmpty::test_repr: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_empty__test_self_difference.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_empty__test_self_difference() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_empty__test_self_difference"
# subject = "cpython.test_set.TestBasicOpsEmpty.test_self_difference"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsEmpty::test_self_difference
"""Auto-ported test: TestBasicOpsEmpty::test_self_difference (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'empty set'
self_values = []
self_set = set(self_values)
self_dup = set(self_values)
self_length = 0
self_repr = 'set()'
result = self_set - self_set

assert result == empty_set
print("TestBasicOpsEmpty::test_self_difference: ok")
"###);
    assert_output(&out, r###"TestBasicOpsEmpty::test_self_difference: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_empty__test_self_equality.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_empty__test_self_equality() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_empty__test_self_equality"
# subject = "cpython.test_set.TestBasicOpsEmpty.test_self_equality"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsEmpty::test_self_equality
"""Auto-ported test: TestBasicOpsEmpty::test_self_equality (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'empty set'
self_values = []
self_set = set(self_values)
self_dup = set(self_values)
self_length = 0
self_repr = 'set()'

assert self_set == self_set
print("TestBasicOpsEmpty::test_self_equality: ok")
"###);
    assert_output(&out, r###"TestBasicOpsEmpty::test_self_equality: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_empty__test_self_intersection.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_empty__test_self_intersection() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_empty__test_self_intersection"
# subject = "cpython.test_set.TestBasicOpsEmpty.test_self_intersection"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsEmpty::test_self_intersection
"""Auto-ported test: TestBasicOpsEmpty::test_self_intersection (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'empty set'
self_values = []
self_set = set(self_values)
self_dup = set(self_values)
self_length = 0
self_repr = 'set()'
result = self_set & self_set

assert result == self_dup
print("TestBasicOpsEmpty::test_self_intersection: ok")
"###);
    assert_output(&out, r###"TestBasicOpsEmpty::test_self_intersection: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_empty__test_self_isdisjoint.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_empty__test_self_isdisjoint() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_empty__test_self_isdisjoint"
# subject = "cpython.test_set.TestBasicOpsEmpty.test_self_isdisjoint"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsEmpty::test_self_isdisjoint
"""Auto-ported test: TestBasicOpsEmpty::test_self_isdisjoint (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'empty set'
self_values = []
self_set = set(self_values)
self_dup = set(self_values)
self_length = 0
self_repr = 'set()'
result = self_set.isdisjoint(self_set)

assert result == (not self_set)
print("TestBasicOpsEmpty::test_self_isdisjoint: ok")
"###);
    assert_output(&out, r###"TestBasicOpsEmpty::test_self_isdisjoint: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_empty__test_self_symmetric_difference.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_empty__test_self_symmetric_difference() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_empty__test_self_symmetric_difference"
# subject = "cpython.test_set.TestBasicOpsEmpty.test_self_symmetric_difference"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsEmpty::test_self_symmetric_difference
"""Auto-ported test: TestBasicOpsEmpty::test_self_symmetric_difference (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'empty set'
self_values = []
self_set = set(self_values)
self_dup = set(self_values)
self_length = 0
self_repr = 'set()'
result = self_set ^ self_set

assert result == empty_set
print("TestBasicOpsEmpty::test_self_symmetric_difference: ok")
"###);
    assert_output(&out, r###"TestBasicOpsEmpty::test_self_symmetric_difference: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_empty__test_self_union.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_empty__test_self_union() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_empty__test_self_union"
# subject = "cpython.test_set.TestBasicOpsEmpty.test_self_union"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsEmpty::test_self_union
"""Auto-ported test: TestBasicOpsEmpty::test_self_union (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'empty set'
self_values = []
self_set = set(self_values)
self_dup = set(self_values)
self_length = 0
self_repr = 'set()'
result = self_set | self_set

assert result == self_dup
print("TestBasicOpsEmpty::test_self_union: ok")
"###);
    assert_output(&out, r###"TestBasicOpsEmpty::test_self_union: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_empty__test_union_empty.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_empty__test_union_empty() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_empty__test_union_empty"
# subject = "cpython.test_set.TestBasicOpsEmpty.test_union_empty"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsEmpty::test_union_empty
"""Auto-ported test: TestBasicOpsEmpty::test_union_empty (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'empty set'
self_values = []
self_set = set(self_values)
self_dup = set(self_values)
self_length = 0
self_repr = 'set()'
result = empty_set | self_set

assert result == self_dup
print("TestBasicOpsEmpty::test_union_empty: ok")
"###);
    assert_output(&out, r###"TestBasicOpsEmpty::test_union_empty: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_singleton__test_copy.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_singleton__test_copy() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_singleton__test_copy"
# subject = "cpython.test_set.TestBasicOpsSingleton.test_copy"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsSingleton::test_copy
"""Auto-ported test: TestBasicOpsSingleton::test_copy (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (number)'
self_values = [3]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = '{3}'

assert self_set.copy() == self_dup
print("TestBasicOpsSingleton::test_copy: ok")
"###);
    assert_output(&out, r###"TestBasicOpsSingleton::test_copy: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_singleton__test_empty_difference.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_singleton__test_empty_difference() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_singleton__test_empty_difference"
# subject = "cpython.test_set.TestBasicOpsSingleton.test_empty_difference"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsSingleton::test_empty_difference
"""Auto-ported test: TestBasicOpsSingleton::test_empty_difference (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (number)'
self_values = [3]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = '{3}'
result = self_set - empty_set

assert result == self_dup
print("TestBasicOpsSingleton::test_empty_difference: ok")
"###);
    assert_output(&out, r###"TestBasicOpsSingleton::test_empty_difference: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_singleton__test_empty_difference_rev.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_singleton__test_empty_difference_rev() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_singleton__test_empty_difference_rev"
# subject = "cpython.test_set.TestBasicOpsSingleton.test_empty_difference_rev"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsSingleton::test_empty_difference_rev
"""Auto-ported test: TestBasicOpsSingleton::test_empty_difference_rev (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (number)'
self_values = [3]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = '{3}'
result = empty_set - self_set

assert result == empty_set
print("TestBasicOpsSingleton::test_empty_difference_rev: ok")
"###);
    assert_output(&out, r###"TestBasicOpsSingleton::test_empty_difference_rev: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_singleton__test_empty_intersection.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_singleton__test_empty_intersection() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_singleton__test_empty_intersection"
# subject = "cpython.test_set.TestBasicOpsSingleton.test_empty_intersection"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsSingleton::test_empty_intersection
"""Auto-ported test: TestBasicOpsSingleton::test_empty_intersection (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (number)'
self_values = [3]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = '{3}'
result = self_set & empty_set

assert result == empty_set
print("TestBasicOpsSingleton::test_empty_intersection: ok")
"###);
    assert_output(&out, r###"TestBasicOpsSingleton::test_empty_intersection: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_singleton__test_empty_isdisjoint.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_singleton__test_empty_isdisjoint() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_singleton__test_empty_isdisjoint"
# subject = "cpython.test_set.TestBasicOpsSingleton.test_empty_isdisjoint"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsSingleton::test_empty_isdisjoint
"""Auto-ported test: TestBasicOpsSingleton::test_empty_isdisjoint (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (number)'
self_values = [3]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = '{3}'
result = self_set.isdisjoint(empty_set)

assert result == True
print("TestBasicOpsSingleton::test_empty_isdisjoint: ok")
"###);
    assert_output(&out, r###"TestBasicOpsSingleton::test_empty_isdisjoint: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_singleton__test_empty_symmetric_difference.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_singleton__test_empty_symmetric_difference() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_singleton__test_empty_symmetric_difference"
# subject = "cpython.test_set.TestBasicOpsSingleton.test_empty_symmetric_difference"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsSingleton::test_empty_symmetric_difference
"""Auto-ported test: TestBasicOpsSingleton::test_empty_symmetric_difference (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (number)'
self_values = [3]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = '{3}'
result = self_set ^ empty_set

assert result == self_set
print("TestBasicOpsSingleton::test_empty_symmetric_difference: ok")
"###);
    assert_output(&out, r###"TestBasicOpsSingleton::test_empty_symmetric_difference: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_singleton__test_empty_union.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_singleton__test_empty_union() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_singleton__test_empty_union"
# subject = "cpython.test_set.TestBasicOpsSingleton.test_empty_union"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsSingleton::test_empty_union
"""Auto-ported test: TestBasicOpsSingleton::test_empty_union (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (number)'
self_values = [3]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = '{3}'
result = self_set | empty_set

assert result == self_dup
print("TestBasicOpsSingleton::test_empty_union: ok")
"###);
    assert_output(&out, r###"TestBasicOpsSingleton::test_empty_union: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_singleton__test_equivalent_equality.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_singleton__test_equivalent_equality() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_singleton__test_equivalent_equality"
# subject = "cpython.test_set.TestBasicOpsSingleton.test_equivalent_equality"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsSingleton::test_equivalent_equality
"""Auto-ported test: TestBasicOpsSingleton::test_equivalent_equality (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (number)'
self_values = [3]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = '{3}'

assert self_set == self_dup
print("TestBasicOpsSingleton::test_equivalent_equality: ok")
"###);
    assert_output(&out, r###"TestBasicOpsSingleton::test_equivalent_equality: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_singleton__test_in.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_singleton__test_in() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_singleton__test_in"
# subject = "cpython.test_set.TestBasicOpsSingleton.test_in"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsSingleton::test_in
"""Auto-ported test: TestBasicOpsSingleton::test_in (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (number)'
self_values = [3]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = '{3}'

assert 3 in self_set
print("TestBasicOpsSingleton::test_in: ok")
"###);
    assert_output(&out, r###"TestBasicOpsSingleton::test_in: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_singleton__test_intersection_empty.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_singleton__test_intersection_empty() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_singleton__test_intersection_empty"
# subject = "cpython.test_set.TestBasicOpsSingleton.test_intersection_empty"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsSingleton::test_intersection_empty
"""Auto-ported test: TestBasicOpsSingleton::test_intersection_empty (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (number)'
self_values = [3]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = '{3}'
result = empty_set & self_set

assert result == empty_set
print("TestBasicOpsSingleton::test_intersection_empty: ok")
"###);
    assert_output(&out, r###"TestBasicOpsSingleton::test_intersection_empty: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_singleton__test_isdisjoint_empty.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_singleton__test_isdisjoint_empty() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_singleton__test_isdisjoint_empty"
# subject = "cpython.test_set.TestBasicOpsSingleton.test_isdisjoint_empty"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsSingleton::test_isdisjoint_empty
"""Auto-ported test: TestBasicOpsSingleton::test_isdisjoint_empty (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (number)'
self_values = [3]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = '{3}'
result = empty_set.isdisjoint(self_set)

assert result == True
print("TestBasicOpsSingleton::test_isdisjoint_empty: ok")
"###);
    assert_output(&out, r###"TestBasicOpsSingleton::test_isdisjoint_empty: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_singleton__test_length.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_singleton__test_length() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_singleton__test_length"
# subject = "cpython.test_set.TestBasicOpsSingleton.test_length"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsSingleton::test_length
"""Auto-ported test: TestBasicOpsSingleton::test_length (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (number)'
self_values = [3]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = '{3}'

assert len(self_set) == self_length
print("TestBasicOpsSingleton::test_length: ok")
"###);
    assert_output(&out, r###"TestBasicOpsSingleton::test_length: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_singleton__test_not_in.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_singleton__test_not_in() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_singleton__test_not_in"
# subject = "cpython.test_set.TestBasicOpsSingleton.test_not_in"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsSingleton::test_not_in
"""Auto-ported test: TestBasicOpsSingleton::test_not_in (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (number)'
self_values = [3]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = '{3}'

assert 2 not in self_set
print("TestBasicOpsSingleton::test_not_in: ok")
"###);
    assert_output(&out, r###"TestBasicOpsSingleton::test_not_in: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_singleton__test_pickling.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_singleton__test_pickling() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_singleton__test_pickling"
# subject = "cpython.test_set.TestBasicOpsSingleton.test_pickling"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsSingleton::test_pickling
"""Auto-ported test: TestBasicOpsSingleton::test_pickling (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (number)'
self_values = [3]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = '{3}'
for proto in range(pickle.HIGHEST_PROTOCOL + 1):
    p = pickle.dumps(self_set, proto)
    copy = pickle.loads(p)

    assert self_set == copy
print("TestBasicOpsSingleton::test_pickling: ok")
"###);
    assert_output(&out, r###"TestBasicOpsSingleton::test_pickling: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_singleton__test_repr.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_singleton__test_repr() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_singleton__test_repr"
# subject = "cpython.test_set.TestBasicOpsSingleton.test_repr"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsSingleton::test_repr
"""Auto-ported test: TestBasicOpsSingleton::test_repr (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (number)'
self_values = [3]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = '{3}'
if self_repr is not None:

    assert repr(self_set) == self_repr
print("TestBasicOpsSingleton::test_repr: ok")
"###);
    assert_output(&out, r###"TestBasicOpsSingleton::test_repr: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_singleton__test_self_difference.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_singleton__test_self_difference() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_singleton__test_self_difference"
# subject = "cpython.test_set.TestBasicOpsSingleton.test_self_difference"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsSingleton::test_self_difference
"""Auto-ported test: TestBasicOpsSingleton::test_self_difference (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (number)'
self_values = [3]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = '{3}'
result = self_set - self_set

assert result == empty_set
print("TestBasicOpsSingleton::test_self_difference: ok")
"###);
    assert_output(&out, r###"TestBasicOpsSingleton::test_self_difference: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_singleton__test_self_equality.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_singleton__test_self_equality() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_singleton__test_self_equality"
# subject = "cpython.test_set.TestBasicOpsSingleton.test_self_equality"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsSingleton::test_self_equality
"""Auto-ported test: TestBasicOpsSingleton::test_self_equality (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (number)'
self_values = [3]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = '{3}'

assert self_set == self_set
print("TestBasicOpsSingleton::test_self_equality: ok")
"###);
    assert_output(&out, r###"TestBasicOpsSingleton::test_self_equality: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_singleton__test_self_intersection.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_singleton__test_self_intersection() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_singleton__test_self_intersection"
# subject = "cpython.test_set.TestBasicOpsSingleton.test_self_intersection"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsSingleton::test_self_intersection
"""Auto-ported test: TestBasicOpsSingleton::test_self_intersection (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (number)'
self_values = [3]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = '{3}'
result = self_set & self_set

assert result == self_dup
print("TestBasicOpsSingleton::test_self_intersection: ok")
"###);
    assert_output(&out, r###"TestBasicOpsSingleton::test_self_intersection: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_singleton__test_self_isdisjoint.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_singleton__test_self_isdisjoint() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_singleton__test_self_isdisjoint"
# subject = "cpython.test_set.TestBasicOpsSingleton.test_self_isdisjoint"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsSingleton::test_self_isdisjoint
"""Auto-ported test: TestBasicOpsSingleton::test_self_isdisjoint (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (number)'
self_values = [3]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = '{3}'
result = self_set.isdisjoint(self_set)

assert result == (not self_set)
print("TestBasicOpsSingleton::test_self_isdisjoint: ok")
"###);
    assert_output(&out, r###"TestBasicOpsSingleton::test_self_isdisjoint: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_singleton__test_self_symmetric_difference.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_singleton__test_self_symmetric_difference() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_singleton__test_self_symmetric_difference"
# subject = "cpython.test_set.TestBasicOpsSingleton.test_self_symmetric_difference"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsSingleton::test_self_symmetric_difference
"""Auto-ported test: TestBasicOpsSingleton::test_self_symmetric_difference (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (number)'
self_values = [3]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = '{3}'
result = self_set ^ self_set

assert result == empty_set
print("TestBasicOpsSingleton::test_self_symmetric_difference: ok")
"###);
    assert_output(&out, r###"TestBasicOpsSingleton::test_self_symmetric_difference: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_singleton__test_self_union.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_singleton__test_self_union() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_singleton__test_self_union"
# subject = "cpython.test_set.TestBasicOpsSingleton.test_self_union"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsSingleton::test_self_union
"""Auto-ported test: TestBasicOpsSingleton::test_self_union (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (number)'
self_values = [3]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = '{3}'
result = self_set | self_set

assert result == self_dup
print("TestBasicOpsSingleton::test_self_union: ok")
"###);
    assert_output(&out, r###"TestBasicOpsSingleton::test_self_union: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_singleton__test_union_empty.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_singleton__test_union_empty() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_singleton__test_union_empty"
# subject = "cpython.test_set.TestBasicOpsSingleton.test_union_empty"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsSingleton::test_union_empty
"""Auto-ported test: TestBasicOpsSingleton::test_union_empty (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (number)'
self_values = [3]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = '{3}'
result = empty_set | self_set

assert result == self_dup
print("TestBasicOpsSingleton::test_union_empty: ok")
"###);
    assert_output(&out, r###"TestBasicOpsSingleton::test_union_empty: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_string__test_copy.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_string__test_copy() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_string__test_copy"
# subject = "cpython.test_set.TestBasicOpsString.test_copy"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsString::test_copy
"""Auto-ported test: TestBasicOpsString::test_copy (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'string set'
self_values = ['a', 'b', 'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3

assert self_set.copy() == self_dup
print("TestBasicOpsString::test_copy: ok")
"###);
    assert_output(&out, r###"TestBasicOpsString::test_copy: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_string__test_empty_difference.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_string__test_empty_difference() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_string__test_empty_difference"
# subject = "cpython.test_set.TestBasicOpsString.test_empty_difference"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsString::test_empty_difference
"""Auto-ported test: TestBasicOpsString::test_empty_difference (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'string set'
self_values = ['a', 'b', 'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
result = self_set - empty_set

assert result == self_dup
print("TestBasicOpsString::test_empty_difference: ok")
"###);
    assert_output(&out, r###"TestBasicOpsString::test_empty_difference: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_string__test_empty_difference_rev.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_string__test_empty_difference_rev() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_string__test_empty_difference_rev"
# subject = "cpython.test_set.TestBasicOpsString.test_empty_difference_rev"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsString::test_empty_difference_rev
"""Auto-ported test: TestBasicOpsString::test_empty_difference_rev (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'string set'
self_values = ['a', 'b', 'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
result = empty_set - self_set

assert result == empty_set
print("TestBasicOpsString::test_empty_difference_rev: ok")
"###);
    assert_output(&out, r###"TestBasicOpsString::test_empty_difference_rev: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_string__test_empty_intersection.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_string__test_empty_intersection() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_string__test_empty_intersection"
# subject = "cpython.test_set.TestBasicOpsString.test_empty_intersection"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsString::test_empty_intersection
"""Auto-ported test: TestBasicOpsString::test_empty_intersection (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'string set'
self_values = ['a', 'b', 'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
result = self_set & empty_set

assert result == empty_set
print("TestBasicOpsString::test_empty_intersection: ok")
"###);
    assert_output(&out, r###"TestBasicOpsString::test_empty_intersection: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_string__test_empty_isdisjoint.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_string__test_empty_isdisjoint() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_string__test_empty_isdisjoint"
# subject = "cpython.test_set.TestBasicOpsString.test_empty_isdisjoint"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsString::test_empty_isdisjoint
"""Auto-ported test: TestBasicOpsString::test_empty_isdisjoint (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'string set'
self_values = ['a', 'b', 'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
result = self_set.isdisjoint(empty_set)

assert result == True
print("TestBasicOpsString::test_empty_isdisjoint: ok")
"###);
    assert_output(&out, r###"TestBasicOpsString::test_empty_isdisjoint: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_string__test_empty_symmetric_difference.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_string__test_empty_symmetric_difference() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_string__test_empty_symmetric_difference"
# subject = "cpython.test_set.TestBasicOpsString.test_empty_symmetric_difference"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsString::test_empty_symmetric_difference
"""Auto-ported test: TestBasicOpsString::test_empty_symmetric_difference (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'string set'
self_values = ['a', 'b', 'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
result = self_set ^ empty_set

assert result == self_set
print("TestBasicOpsString::test_empty_symmetric_difference: ok")
"###);
    assert_output(&out, r###"TestBasicOpsString::test_empty_symmetric_difference: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_string__test_empty_union.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_string__test_empty_union() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_string__test_empty_union"
# subject = "cpython.test.test_set.TestBasicOpsString.test_empty_union"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsString::test_empty_union
"""Auto-ported test: TestBasicOpsString::test_empty_union (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'string set'
self_values = ['a', 'b', 'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
result = self_set | empty_set

assert result == self_dup
print("TestBasicOpsString::test_empty_union: ok")
"###);
    assert_output(&out, r###"TestBasicOpsString::test_empty_union: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_string__test_equivalent_equality.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_string__test_equivalent_equality() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_string__test_equivalent_equality"
# subject = "cpython.test_set.TestBasicOpsString.test_equivalent_equality"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsString::test_equivalent_equality
"""Auto-ported test: TestBasicOpsString::test_equivalent_equality (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'string set'
self_values = ['a', 'b', 'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3

assert self_set == self_dup
print("TestBasicOpsString::test_equivalent_equality: ok")
"###);
    assert_output(&out, r###"TestBasicOpsString::test_equivalent_equality: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_string__test_intersection_empty.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_string__test_intersection_empty() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_string__test_intersection_empty"
# subject = "cpython.test_set.TestBasicOpsString.test_intersection_empty"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsString::test_intersection_empty
"""Auto-ported test: TestBasicOpsString::test_intersection_empty (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'string set'
self_values = ['a', 'b', 'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
result = empty_set & self_set

assert result == empty_set
print("TestBasicOpsString::test_intersection_empty: ok")
"###);
    assert_output(&out, r###"TestBasicOpsString::test_intersection_empty: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_string__test_isdisjoint_empty.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_string__test_isdisjoint_empty() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_string__test_isdisjoint_empty"
# subject = "cpython.test_set.TestBasicOpsString.test_isdisjoint_empty"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsString::test_isdisjoint_empty
"""Auto-ported test: TestBasicOpsString::test_isdisjoint_empty (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'string set'
self_values = ['a', 'b', 'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
result = empty_set.isdisjoint(self_set)

assert result == True
print("TestBasicOpsString::test_isdisjoint_empty: ok")
"###);
    assert_output(&out, r###"TestBasicOpsString::test_isdisjoint_empty: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_string__test_length.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_string__test_length() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_string__test_length"
# subject = "cpython.test_set.TestBasicOpsString.test_length"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsString::test_length
"""Auto-ported test: TestBasicOpsString::test_length (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'string set'
self_values = ['a', 'b', 'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3

assert len(self_set) == self_length
print("TestBasicOpsString::test_length: ok")
"###);
    assert_output(&out, r###"TestBasicOpsString::test_length: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_string__test_pickling.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_string__test_pickling() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_string__test_pickling"
# subject = "cpython.test_set.TestBasicOpsString.test_pickling"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsString::test_pickling
"""Auto-ported test: TestBasicOpsString::test_pickling (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'string set'
self_values = ['a', 'b', 'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
for proto in range(pickle.HIGHEST_PROTOCOL + 1):
    p = pickle.dumps(self_set, proto)
    copy = pickle.loads(p)

    assert self_set == copy
print("TestBasicOpsString::test_pickling: ok")
"###);
    assert_output(&out, r###"TestBasicOpsString::test_pickling: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_string__test_self_difference.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_string__test_self_difference() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_string__test_self_difference"
# subject = "cpython.test_set.TestBasicOpsString.test_self_difference"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsString::test_self_difference
"""Auto-ported test: TestBasicOpsString::test_self_difference (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'string set'
self_values = ['a', 'b', 'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
result = self_set - self_set

assert result == empty_set
print("TestBasicOpsString::test_self_difference: ok")
"###);
    assert_output(&out, r###"TestBasicOpsString::test_self_difference: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_string__test_self_equality.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_string__test_self_equality() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_string__test_self_equality"
# subject = "cpython.test_set.TestBasicOpsString.test_self_equality"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsString::test_self_equality
"""Auto-ported test: TestBasicOpsString::test_self_equality (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'string set'
self_values = ['a', 'b', 'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3

assert self_set == self_set
print("TestBasicOpsString::test_self_equality: ok")
"###);
    assert_output(&out, r###"TestBasicOpsString::test_self_equality: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_string__test_self_intersection.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_string__test_self_intersection() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_string__test_self_intersection"
# subject = "cpython.test_set.TestBasicOpsString.test_self_intersection"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsString::test_self_intersection
"""Auto-ported test: TestBasicOpsString::test_self_intersection (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'string set'
self_values = ['a', 'b', 'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
result = self_set & self_set

assert result == self_dup
print("TestBasicOpsString::test_self_intersection: ok")
"###);
    assert_output(&out, r###"TestBasicOpsString::test_self_intersection: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_string__test_self_isdisjoint.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_string__test_self_isdisjoint() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_string__test_self_isdisjoint"
# subject = "cpython.test_set.TestBasicOpsString.test_self_isdisjoint"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsString::test_self_isdisjoint
"""Auto-ported test: TestBasicOpsString::test_self_isdisjoint (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'string set'
self_values = ['a', 'b', 'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
result = self_set.isdisjoint(self_set)

assert result == (not self_set)
print("TestBasicOpsString::test_self_isdisjoint: ok")
"###);
    assert_output(&out, r###"TestBasicOpsString::test_self_isdisjoint: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_string__test_self_symmetric_difference.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_string__test_self_symmetric_difference() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_string__test_self_symmetric_difference"
# subject = "cpython.test_set.TestBasicOpsString.test_self_symmetric_difference"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsString::test_self_symmetric_difference
"""Auto-ported test: TestBasicOpsString::test_self_symmetric_difference (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'string set'
self_values = ['a', 'b', 'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
result = self_set ^ self_set

assert result == empty_set
print("TestBasicOpsString::test_self_symmetric_difference: ok")
"###);
    assert_output(&out, r###"TestBasicOpsString::test_self_symmetric_difference: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_string__test_self_union.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_string__test_self_union() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_string__test_self_union"
# subject = "cpython.test_set.TestBasicOpsString.test_self_union"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsString::test_self_union
"""Auto-ported test: TestBasicOpsString::test_self_union (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'string set'
self_values = ['a', 'b', 'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
result = self_set | self_set

assert result == self_dup
print("TestBasicOpsString::test_self_union: ok")
"###);
    assert_output(&out, r###"TestBasicOpsString::test_self_union: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_string__test_union_empty.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_string__test_union_empty() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_string__test_union_empty"
# subject = "cpython.test_set.TestBasicOpsString.test_union_empty"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsString::test_union_empty
"""Auto-ported test: TestBasicOpsString::test_union_empty (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'string set'
self_values = ['a', 'b', 'c']
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
result = empty_set | self_set

assert result == self_dup
print("TestBasicOpsString::test_union_empty: ok")
"###);
    assert_output(&out, r###"TestBasicOpsString::test_union_empty: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_triple__test_copy.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_triple__test_copy() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_triple__test_copy"
# subject = "cpython.test_set.TestBasicOpsTriple.test_copy"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTriple::test_copy
"""Auto-ported test: TestBasicOpsTriple::test_copy (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'triple set'
self_values = [0, 'zero', operator.add]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
self_repr = None

assert self_set.copy() == self_dup
print("TestBasicOpsTriple::test_copy: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTriple::test_copy: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_triple__test_empty_difference.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_triple__test_empty_difference() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_triple__test_empty_difference"
# subject = "cpython.test_set.TestBasicOpsTriple.test_empty_difference"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTriple::test_empty_difference
"""Auto-ported test: TestBasicOpsTriple::test_empty_difference (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'triple set'
self_values = [0, 'zero', operator.add]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
self_repr = None
result = self_set - empty_set

assert result == self_dup
print("TestBasicOpsTriple::test_empty_difference: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTriple::test_empty_difference: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_triple__test_empty_difference_rev.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_triple__test_empty_difference_rev() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_triple__test_empty_difference_rev"
# subject = "cpython.test_set.TestBasicOpsTriple.test_empty_difference_rev"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTriple::test_empty_difference_rev
"""Auto-ported test: TestBasicOpsTriple::test_empty_difference_rev (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'triple set'
self_values = [0, 'zero', operator.add]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
self_repr = None
result = empty_set - self_set

assert result == empty_set
print("TestBasicOpsTriple::test_empty_difference_rev: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTriple::test_empty_difference_rev: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_triple__test_empty_intersection.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_triple__test_empty_intersection() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_triple__test_empty_intersection"
# subject = "cpython.test_set.TestBasicOpsTriple.test_empty_intersection"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTriple::test_empty_intersection
"""Auto-ported test: TestBasicOpsTriple::test_empty_intersection (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'triple set'
self_values = [0, 'zero', operator.add]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
self_repr = None
result = self_set & empty_set

assert result == empty_set
print("TestBasicOpsTriple::test_empty_intersection: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTriple::test_empty_intersection: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_triple__test_empty_isdisjoint.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_triple__test_empty_isdisjoint() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_triple__test_empty_isdisjoint"
# subject = "cpython.test_set.TestBasicOpsTriple.test_empty_isdisjoint"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTriple::test_empty_isdisjoint
"""Auto-ported test: TestBasicOpsTriple::test_empty_isdisjoint (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'triple set'
self_values = [0, 'zero', operator.add]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
self_repr = None
result = self_set.isdisjoint(empty_set)

assert result == True
print("TestBasicOpsTriple::test_empty_isdisjoint: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTriple::test_empty_isdisjoint: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_triple__test_empty_symmetric_difference.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_triple__test_empty_symmetric_difference() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_triple__test_empty_symmetric_difference"
# subject = "cpython.test_set.TestBasicOpsTriple.test_empty_symmetric_difference"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTriple::test_empty_symmetric_difference
"""Auto-ported test: TestBasicOpsTriple::test_empty_symmetric_difference (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'triple set'
self_values = [0, 'zero', operator.add]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
self_repr = None
result = self_set ^ empty_set

assert result == self_set
print("TestBasicOpsTriple::test_empty_symmetric_difference: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTriple::test_empty_symmetric_difference: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_triple__test_empty_union.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_triple__test_empty_union() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_triple__test_empty_union"
# subject = "cpython.test_set.TestBasicOpsTriple.test_empty_union"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTriple::test_empty_union
"""Auto-ported test: TestBasicOpsTriple::test_empty_union (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'triple set'
self_values = [0, 'zero', operator.add]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
self_repr = None
result = self_set | empty_set

assert result == self_dup
print("TestBasicOpsTriple::test_empty_union: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTriple::test_empty_union: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_triple__test_equivalent_equality.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_triple__test_equivalent_equality() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_triple__test_equivalent_equality"
# subject = "cpython.test_set.TestBasicOpsTriple.test_equivalent_equality"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTriple::test_equivalent_equality
"""Auto-ported test: TestBasicOpsTriple::test_equivalent_equality (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'triple set'
self_values = [0, 'zero', operator.add]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
self_repr = None

assert self_set == self_dup
print("TestBasicOpsTriple::test_equivalent_equality: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTriple::test_equivalent_equality: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_triple__test_intersection_empty.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_triple__test_intersection_empty() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_triple__test_intersection_empty"
# subject = "cpython.test_set.TestBasicOpsTriple.test_intersection_empty"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTriple::test_intersection_empty
"""Auto-ported test: TestBasicOpsTriple::test_intersection_empty (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'triple set'
self_values = [0, 'zero', operator.add]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
self_repr = None
result = empty_set & self_set

assert result == empty_set
print("TestBasicOpsTriple::test_intersection_empty: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTriple::test_intersection_empty: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_triple__test_isdisjoint_empty.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_triple__test_isdisjoint_empty() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_triple__test_isdisjoint_empty"
# subject = "cpython.test_set.TestBasicOpsTriple.test_isdisjoint_empty"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTriple::test_isdisjoint_empty
"""Auto-ported test: TestBasicOpsTriple::test_isdisjoint_empty (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'triple set'
self_values = [0, 'zero', operator.add]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
self_repr = None
result = empty_set.isdisjoint(self_set)

assert result == True
print("TestBasicOpsTriple::test_isdisjoint_empty: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTriple::test_isdisjoint_empty: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_triple__test_length.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_triple__test_length() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_triple__test_length"
# subject = "cpython.test_set.TestBasicOpsTriple.test_length"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTriple::test_length
"""Auto-ported test: TestBasicOpsTriple::test_length (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'triple set'
self_values = [0, 'zero', operator.add]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
self_repr = None

assert len(self_set) == self_length
print("TestBasicOpsTriple::test_length: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTriple::test_length: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_triple__test_repr.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_triple__test_repr() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_triple__test_repr"
# subject = "cpython.test_set.TestBasicOpsTriple.test_repr"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTriple::test_repr
"""Auto-ported test: TestBasicOpsTriple::test_repr (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'triple set'
self_values = [0, 'zero', operator.add]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
self_repr = None
if self_repr is not None:

    assert repr(self_set) == self_repr
print("TestBasicOpsTriple::test_repr: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTriple::test_repr: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_triple__test_self_difference.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_triple__test_self_difference() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_triple__test_self_difference"
# subject = "cpython.test_set.TestBasicOpsTriple.test_self_difference"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTriple::test_self_difference
"""Auto-ported test: TestBasicOpsTriple::test_self_difference (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'triple set'
self_values = [0, 'zero', operator.add]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
self_repr = None
result = self_set - self_set

assert result == empty_set
print("TestBasicOpsTriple::test_self_difference: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTriple::test_self_difference: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_triple__test_self_equality.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_triple__test_self_equality() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_triple__test_self_equality"
# subject = "cpython.test_set.TestBasicOpsTriple.test_self_equality"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTriple::test_self_equality
"""Auto-ported test: TestBasicOpsTriple::test_self_equality (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'triple set'
self_values = [0, 'zero', operator.add]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
self_repr = None

assert self_set == self_set
print("TestBasicOpsTriple::test_self_equality: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTriple::test_self_equality: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_triple__test_self_intersection.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_triple__test_self_intersection() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_triple__test_self_intersection"
# subject = "cpython.test_set.TestBasicOpsTriple.test_self_intersection"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTriple::test_self_intersection
"""Auto-ported test: TestBasicOpsTriple::test_self_intersection (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'triple set'
self_values = [0, 'zero', operator.add]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
self_repr = None
result = self_set & self_set

assert result == self_dup
print("TestBasicOpsTriple::test_self_intersection: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTriple::test_self_intersection: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_triple__test_self_isdisjoint.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_triple__test_self_isdisjoint() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_triple__test_self_isdisjoint"
# subject = "cpython.test_set.TestBasicOpsTriple.test_self_isdisjoint"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTriple::test_self_isdisjoint
"""Auto-ported test: TestBasicOpsTriple::test_self_isdisjoint (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'triple set'
self_values = [0, 'zero', operator.add]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
self_repr = None
result = self_set.isdisjoint(self_set)

assert result == (not self_set)
print("TestBasicOpsTriple::test_self_isdisjoint: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTriple::test_self_isdisjoint: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_triple__test_self_symmetric_difference.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_triple__test_self_symmetric_difference() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_triple__test_self_symmetric_difference"
# subject = "cpython.test_set.TestBasicOpsTriple.test_self_symmetric_difference"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTriple::test_self_symmetric_difference
"""Auto-ported test: TestBasicOpsTriple::test_self_symmetric_difference (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'triple set'
self_values = [0, 'zero', operator.add]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
self_repr = None
result = self_set ^ self_set

assert result == empty_set
print("TestBasicOpsTriple::test_self_symmetric_difference: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTriple::test_self_symmetric_difference: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_triple__test_self_union.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_triple__test_self_union() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_triple__test_self_union"
# subject = "cpython.test_set.TestBasicOpsTriple.test_self_union"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTriple::test_self_union
"""Auto-ported test: TestBasicOpsTriple::test_self_union (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'triple set'
self_values = [0, 'zero', operator.add]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
self_repr = None
result = self_set | self_set

assert result == self_dup
print("TestBasicOpsTriple::test_self_union: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTriple::test_self_union: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_triple__test_union_empty.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_triple__test_union_empty() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_triple__test_union_empty"
# subject = "cpython.test_set.TestBasicOpsTriple.test_union_empty"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTriple::test_union_empty
"""Auto-ported test: TestBasicOpsTriple::test_union_empty (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'triple set'
self_values = [0, 'zero', operator.add]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 3
self_repr = None
result = empty_set | self_set

assert result == self_dup
print("TestBasicOpsTriple::test_union_empty: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTriple::test_union_empty: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_tuple__test_copy.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_tuple__test_copy() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_tuple__test_copy"
# subject = "cpython.test_set.TestBasicOpsTuple.test_copy"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTuple::test_copy
"""Auto-ported test: TestBasicOpsTuple::test_copy (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (tuple)'
self_values = [(0, 'zero')]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = "{(0, 'zero')}"

assert self_set.copy() == self_dup
print("TestBasicOpsTuple::test_copy: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTuple::test_copy: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_tuple__test_empty_difference.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_tuple__test_empty_difference() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_tuple__test_empty_difference"
# subject = "cpython.test_set.TestBasicOpsTuple.test_empty_difference"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTuple::test_empty_difference
"""Auto-ported test: TestBasicOpsTuple::test_empty_difference (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (tuple)'
self_values = [(0, 'zero')]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = "{(0, 'zero')}"
result = self_set - empty_set

assert result == self_dup
print("TestBasicOpsTuple::test_empty_difference: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTuple::test_empty_difference: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_tuple__test_empty_difference_rev.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_tuple__test_empty_difference_rev() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_tuple__test_empty_difference_rev"
# subject = "cpython.test_set.TestBasicOpsTuple.test_empty_difference_rev"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTuple::test_empty_difference_rev
"""Auto-ported test: TestBasicOpsTuple::test_empty_difference_rev (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (tuple)'
self_values = [(0, 'zero')]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = "{(0, 'zero')}"
result = empty_set - self_set

assert result == empty_set
print("TestBasicOpsTuple::test_empty_difference_rev: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTuple::test_empty_difference_rev: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_tuple__test_empty_intersection.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_tuple__test_empty_intersection() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_tuple__test_empty_intersection"
# subject = "cpython.test_set.TestBasicOpsTuple.test_empty_intersection"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTuple::test_empty_intersection
"""Auto-ported test: TestBasicOpsTuple::test_empty_intersection (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (tuple)'
self_values = [(0, 'zero')]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = "{(0, 'zero')}"
result = self_set & empty_set

assert result == empty_set
print("TestBasicOpsTuple::test_empty_intersection: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTuple::test_empty_intersection: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_tuple__test_empty_isdisjoint.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_tuple__test_empty_isdisjoint() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_tuple__test_empty_isdisjoint"
# subject = "cpython.test_set.TestBasicOpsTuple.test_empty_isdisjoint"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTuple::test_empty_isdisjoint
"""Auto-ported test: TestBasicOpsTuple::test_empty_isdisjoint (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (tuple)'
self_values = [(0, 'zero')]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = "{(0, 'zero')}"
result = self_set.isdisjoint(empty_set)

assert result == True
print("TestBasicOpsTuple::test_empty_isdisjoint: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTuple::test_empty_isdisjoint: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_tuple__test_empty_symmetric_difference.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_tuple__test_empty_symmetric_difference() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_tuple__test_empty_symmetric_difference"
# subject = "cpython.test_set.TestBasicOpsTuple.test_empty_symmetric_difference"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTuple::test_empty_symmetric_difference
"""Auto-ported test: TestBasicOpsTuple::test_empty_symmetric_difference (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (tuple)'
self_values = [(0, 'zero')]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = "{(0, 'zero')}"
result = self_set ^ empty_set

assert result == self_set
print("TestBasicOpsTuple::test_empty_symmetric_difference: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTuple::test_empty_symmetric_difference: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_tuple__test_empty_union.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_tuple__test_empty_union() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_tuple__test_empty_union"
# subject = "cpython.test_set.TestBasicOpsTuple.test_empty_union"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTuple::test_empty_union
"""Auto-ported test: TestBasicOpsTuple::test_empty_union (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (tuple)'
self_values = [(0, 'zero')]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = "{(0, 'zero')}"
result = self_set | empty_set

assert result == self_dup
print("TestBasicOpsTuple::test_empty_union: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTuple::test_empty_union: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_tuple__test_equivalent_equality.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_tuple__test_equivalent_equality() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_tuple__test_equivalent_equality"
# subject = "cpython.test_set.TestBasicOpsTuple.test_equivalent_equality"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTuple::test_equivalent_equality
"""Auto-ported test: TestBasicOpsTuple::test_equivalent_equality (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (tuple)'
self_values = [(0, 'zero')]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = "{(0, 'zero')}"

assert self_set == self_dup
print("TestBasicOpsTuple::test_equivalent_equality: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTuple::test_equivalent_equality: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_tuple__test_in.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_tuple__test_in() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_tuple__test_in"
# subject = "cpython.test_set.TestBasicOpsTuple.test_in"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTuple::test_in
"""Auto-ported test: TestBasicOpsTuple::test_in (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (tuple)'
self_values = [(0, 'zero')]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = "{(0, 'zero')}"

assert (0, 'zero') in self_set
print("TestBasicOpsTuple::test_in: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTuple::test_in: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_tuple__test_intersection_empty.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_tuple__test_intersection_empty() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_tuple__test_intersection_empty"
# subject = "cpython.test_set.TestBasicOpsTuple.test_intersection_empty"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTuple::test_intersection_empty
"""Auto-ported test: TestBasicOpsTuple::test_intersection_empty (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (tuple)'
self_values = [(0, 'zero')]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = "{(0, 'zero')}"
result = empty_set & self_set

assert result == empty_set
print("TestBasicOpsTuple::test_intersection_empty: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTuple::test_intersection_empty: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_tuple__test_isdisjoint_empty.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_tuple__test_isdisjoint_empty() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_tuple__test_isdisjoint_empty"
# subject = "cpython.test_set.TestBasicOpsTuple.test_isdisjoint_empty"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTuple::test_isdisjoint_empty
"""Auto-ported test: TestBasicOpsTuple::test_isdisjoint_empty (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (tuple)'
self_values = [(0, 'zero')]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = "{(0, 'zero')}"
result = empty_set.isdisjoint(self_set)

assert result == True
print("TestBasicOpsTuple::test_isdisjoint_empty: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTuple::test_isdisjoint_empty: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_tuple__test_length.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_tuple__test_length() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_tuple__test_length"
# subject = "cpython.test_set.TestBasicOpsTuple.test_length"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTuple::test_length
"""Auto-ported test: TestBasicOpsTuple::test_length (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (tuple)'
self_values = [(0, 'zero')]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = "{(0, 'zero')}"

assert len(self_set) == self_length
print("TestBasicOpsTuple::test_length: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTuple::test_length: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_tuple__test_not_in.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_tuple__test_not_in() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_tuple__test_not_in"
# subject = "cpython.test_set.TestBasicOpsTuple.test_not_in"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTuple::test_not_in
"""Auto-ported test: TestBasicOpsTuple::test_not_in (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (tuple)'
self_values = [(0, 'zero')]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = "{(0, 'zero')}"

assert 9 not in self_set
print("TestBasicOpsTuple::test_not_in: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTuple::test_not_in: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_tuple__test_pickling.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_tuple__test_pickling() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_tuple__test_pickling"
# subject = "cpython.test_set.TestBasicOpsTuple.test_pickling"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTuple::test_pickling
"""Auto-ported test: TestBasicOpsTuple::test_pickling (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (tuple)'
self_values = [(0, 'zero')]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = "{(0, 'zero')}"
for proto in range(pickle.HIGHEST_PROTOCOL + 1):
    p = pickle.dumps(self_set, proto)
    copy = pickle.loads(p)

    assert self_set == copy
print("TestBasicOpsTuple::test_pickling: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTuple::test_pickling: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_tuple__test_repr.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_tuple__test_repr() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_tuple__test_repr"
# subject = "cpython.test_set.TestBasicOpsTuple.test_repr"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTuple::test_repr
"""Auto-ported test: TestBasicOpsTuple::test_repr (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (tuple)'
self_values = [(0, 'zero')]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = "{(0, 'zero')}"
if self_repr is not None:

    assert repr(self_set) == self_repr
print("TestBasicOpsTuple::test_repr: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTuple::test_repr: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_tuple__test_self_difference.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_tuple__test_self_difference() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_tuple__test_self_difference"
# subject = "cpython.test_set.TestBasicOpsTuple.test_self_difference"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTuple::test_self_difference
"""Auto-ported test: TestBasicOpsTuple::test_self_difference (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (tuple)'
self_values = [(0, 'zero')]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = "{(0, 'zero')}"
result = self_set - self_set

assert result == empty_set
print("TestBasicOpsTuple::test_self_difference: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTuple::test_self_difference: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_tuple__test_self_equality.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_tuple__test_self_equality() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_tuple__test_self_equality"
# subject = "cpython.test_set.TestBasicOpsTuple.test_self_equality"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTuple::test_self_equality
"""Auto-ported test: TestBasicOpsTuple::test_self_equality (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (tuple)'
self_values = [(0, 'zero')]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = "{(0, 'zero')}"

assert self_set == self_set
print("TestBasicOpsTuple::test_self_equality: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTuple::test_self_equality: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_tuple__test_self_intersection.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_tuple__test_self_intersection() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_tuple__test_self_intersection"
# subject = "cpython.test_set.TestBasicOpsTuple.test_self_intersection"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTuple::test_self_intersection
"""Auto-ported test: TestBasicOpsTuple::test_self_intersection (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (tuple)'
self_values = [(0, 'zero')]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = "{(0, 'zero')}"
result = self_set & self_set

assert result == self_dup
print("TestBasicOpsTuple::test_self_intersection: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTuple::test_self_intersection: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_tuple__test_self_isdisjoint.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_tuple__test_self_isdisjoint() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_tuple__test_self_isdisjoint"
# subject = "cpython.test_set.TestBasicOpsTuple.test_self_isdisjoint"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTuple::test_self_isdisjoint
"""Auto-ported test: TestBasicOpsTuple::test_self_isdisjoint (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (tuple)'
self_values = [(0, 'zero')]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = "{(0, 'zero')}"
result = self_set.isdisjoint(self_set)

assert result == (not self_set)
print("TestBasicOpsTuple::test_self_isdisjoint: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTuple::test_self_isdisjoint: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_tuple__test_self_symmetric_difference.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_tuple__test_self_symmetric_difference() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_tuple__test_self_symmetric_difference"
# subject = "cpython.test_set.TestBasicOpsTuple.test_self_symmetric_difference"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTuple::test_self_symmetric_difference
"""Auto-ported test: TestBasicOpsTuple::test_self_symmetric_difference (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (tuple)'
self_values = [(0, 'zero')]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = "{(0, 'zero')}"
result = self_set ^ self_set

assert result == empty_set
print("TestBasicOpsTuple::test_self_symmetric_difference: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTuple::test_self_symmetric_difference: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_tuple__test_self_union.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_tuple__test_self_union() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_tuple__test_self_union"
# subject = "cpython.test_set.TestBasicOpsTuple.test_self_union"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTuple::test_self_union
"""Auto-ported test: TestBasicOpsTuple::test_self_union (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (tuple)'
self_values = [(0, 'zero')]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = "{(0, 'zero')}"
result = self_set | self_set

assert result == self_dup
print("TestBasicOpsTuple::test_self_union: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTuple::test_self_union: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_basic_ops_tuple__test_union_empty.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_basic_ops_tuple__test_union_empty() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_basic_ops_tuple__test_union_empty"
# subject = "cpython.test_set.TestBasicOpsTuple.test_union_empty"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBasicOpsTuple::test_union_empty
"""Auto-ported test: TestBasicOpsTuple::test_union_empty (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_case = 'unit set (tuple)'
self_values = [(0, 'zero')]
self_set = set(self_values)
self_dup = set(self_values)
self_length = 1
self_repr = "{(0, 'zero')}"
result = empty_set | self_set

assert result == self_dup
print("TestBasicOpsTuple::test_union_empty: ok")
"###);
    assert_output(&out, r###"TestBasicOpsTuple::test_union_empty: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops__test_eq.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops__test_eq() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops__test_eq"
# subject = "cpython.test_set.TestBinaryOps.test_eq"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOps::test_eq
"""Auto-ported test: TestBinaryOps::test_eq (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))

assert self_set == set({2: 1, 4: 3, 6: 5})
print("TestBinaryOps::test_eq: ok")
"###);
    assert_output(&out, r###"TestBinaryOps::test_eq: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops__test_intersection_non_overlap.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops__test_intersection_non_overlap() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops__test_intersection_non_overlap"
# subject = "cpython.test_set.TestBinaryOps.test_intersection_non_overlap"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOps::test_intersection_non_overlap
"""Auto-ported test: TestBinaryOps::test_intersection_non_overlap (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
result = self_set & set([8])

assert result == empty_set
print("TestBinaryOps::test_intersection_non_overlap: ok")
"###);
    assert_output(&out, r###"TestBinaryOps::test_intersection_non_overlap: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops__test_intersection_overlap.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops__test_intersection_overlap() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops__test_intersection_overlap"
# subject = "cpython.test_set.TestBinaryOps.test_intersection_overlap"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOps::test_intersection_overlap
"""Auto-ported test: TestBinaryOps::test_intersection_overlap (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
result = self_set & set([3, 4, 5])

assert result == set([4])
print("TestBinaryOps::test_intersection_overlap: ok")
"###);
    assert_output(&out, r###"TestBinaryOps::test_intersection_overlap: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops__test_intersection_subset.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops__test_intersection_subset() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops__test_intersection_subset"
# subject = "cpython.test_set.TestBinaryOps.test_intersection_subset"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOps::test_intersection_subset
"""Auto-ported test: TestBinaryOps::test_intersection_subset (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
result = self_set & set((2, 4))

assert result == set((2, 4))
print("TestBinaryOps::test_intersection_subset: ok")
"###);
    assert_output(&out, r###"TestBinaryOps::test_intersection_subset: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops__test_intersection_superset.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops__test_intersection_superset() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops__test_intersection_superset"
# subject = "cpython.test_set.TestBinaryOps.test_intersection_superset"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOps::test_intersection_superset
"""Auto-ported test: TestBinaryOps::test_intersection_superset (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
result = self_set & set([2, 4, 6, 8])

assert result == set([2, 4, 6])
print("TestBinaryOps::test_intersection_superset: ok")
"###);
    assert_output(&out, r###"TestBinaryOps::test_intersection_superset: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops__test_isdisjoint_non_overlap.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops__test_isdisjoint_non_overlap() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops__test_isdisjoint_non_overlap"
# subject = "cpython.test_set.TestBinaryOps.test_isdisjoint_non_overlap"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOps::test_isdisjoint_non_overlap
"""Auto-ported test: TestBinaryOps::test_isdisjoint_non_overlap (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
result = self_set.isdisjoint(set([8]))

assert result == True
print("TestBinaryOps::test_isdisjoint_non_overlap: ok")
"###);
    assert_output(&out, r###"TestBinaryOps::test_isdisjoint_non_overlap: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops__test_isdisjoint_overlap.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops__test_isdisjoint_overlap() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops__test_isdisjoint_overlap"
# subject = "cpython.test_set.TestBinaryOps.test_isdisjoint_overlap"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOps::test_isdisjoint_overlap
"""Auto-ported test: TestBinaryOps::test_isdisjoint_overlap (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
result = self_set.isdisjoint(set([3, 4, 5]))

assert result == False
print("TestBinaryOps::test_isdisjoint_overlap: ok")
"###);
    assert_output(&out, r###"TestBinaryOps::test_isdisjoint_overlap: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops__test_isdisjoint_subset.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops__test_isdisjoint_subset() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops__test_isdisjoint_subset"
# subject = "cpython.test_set.TestBinaryOps.test_isdisjoint_subset"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOps::test_isdisjoint_subset
"""Auto-ported test: TestBinaryOps::test_isdisjoint_subset (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
result = self_set.isdisjoint(set((2, 4)))

assert result == False
print("TestBinaryOps::test_isdisjoint_subset: ok")
"###);
    assert_output(&out, r###"TestBinaryOps::test_isdisjoint_subset: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops__test_isdisjoint_superset.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops__test_isdisjoint_superset() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops__test_isdisjoint_superset"
# subject = "cpython.test_set.TestBinaryOps.test_isdisjoint_superset"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOps::test_isdisjoint_superset
"""Auto-ported test: TestBinaryOps::test_isdisjoint_superset (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
result = self_set.isdisjoint(set([2, 4, 6, 8]))

assert result == False
print("TestBinaryOps::test_isdisjoint_superset: ok")
"###);
    assert_output(&out, r###"TestBinaryOps::test_isdisjoint_superset: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops__test_sym_difference_non_overlap.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops__test_sym_difference_non_overlap() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops__test_sym_difference_non_overlap"
# subject = "cpython.test_set.TestBinaryOps.test_sym_difference_non_overlap"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOps::test_sym_difference_non_overlap
"""Auto-ported test: TestBinaryOps::test_sym_difference_non_overlap (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
result = self_set ^ set([8])

assert result == set([2, 4, 6, 8])
print("TestBinaryOps::test_sym_difference_non_overlap: ok")
"###);
    assert_output(&out, r###"TestBinaryOps::test_sym_difference_non_overlap: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops__test_sym_difference_overlap.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops__test_sym_difference_overlap() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops__test_sym_difference_overlap"
# subject = "cpython.test_set.TestBinaryOps.test_sym_difference_overlap"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOps::test_sym_difference_overlap
"""Auto-ported test: TestBinaryOps::test_sym_difference_overlap (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
result = self_set ^ set((3, 4, 5))

assert result == set([2, 3, 5, 6])
print("TestBinaryOps::test_sym_difference_overlap: ok")
"###);
    assert_output(&out, r###"TestBinaryOps::test_sym_difference_overlap: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops__test_sym_difference_subset.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops__test_sym_difference_subset() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops__test_sym_difference_subset"
# subject = "cpython.test_set.TestBinaryOps.test_sym_difference_subset"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOps::test_sym_difference_subset
"""Auto-ported test: TestBinaryOps::test_sym_difference_subset (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
result = self_set ^ set((2, 4))

assert result == set([6])
print("TestBinaryOps::test_sym_difference_subset: ok")
"###);
    assert_output(&out, r###"TestBinaryOps::test_sym_difference_subset: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops__test_sym_difference_superset.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops__test_sym_difference_superset() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops__test_sym_difference_superset"
# subject = "cpython.test_set.TestBinaryOps.test_sym_difference_superset"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOps::test_sym_difference_superset
"""Auto-ported test: TestBinaryOps::test_sym_difference_superset (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
result = self_set ^ set((2, 4, 6, 8))

assert result == set([8])
print("TestBinaryOps::test_sym_difference_superset: ok")
"###);
    assert_output(&out, r###"TestBinaryOps::test_sym_difference_superset: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops__test_union_non_overlap.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops__test_union_non_overlap() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops__test_union_non_overlap"
# subject = "cpython.test_set.TestBinaryOps.test_union_non_overlap"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOps::test_union_non_overlap
"""Auto-ported test: TestBinaryOps::test_union_non_overlap (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
result = self_set | set([8])

assert result == set([2, 4, 6, 8])
print("TestBinaryOps::test_union_non_overlap: ok")
"###);
    assert_output(&out, r###"TestBinaryOps::test_union_non_overlap: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops__test_union_overlap.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops__test_union_overlap() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops__test_union_overlap"
# subject = "cpython.test_set.TestBinaryOps.test_union_overlap"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOps::test_union_overlap
"""Auto-ported test: TestBinaryOps::test_union_overlap (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
result = self_set | set([3, 4, 5])

assert result == set([2, 3, 4, 5, 6])
print("TestBinaryOps::test_union_overlap: ok")
"###);
    assert_output(&out, r###"TestBinaryOps::test_union_overlap: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops__test_union_subset.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops__test_union_subset() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops__test_union_subset"
# subject = "cpython.test_set.TestBinaryOps.test_union_subset"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOps::test_union_subset
"""Auto-ported test: TestBinaryOps::test_union_subset (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
result = self_set | set([2])

assert result == set((2, 4, 6))
print("TestBinaryOps::test_union_subset: ok")
"###);
    assert_output(&out, r###"TestBinaryOps::test_union_subset: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops__test_union_superset.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops__test_union_superset() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops__test_union_superset"
# subject = "cpython.test_set.TestBinaryOps.test_union_superset"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOps::test_union_superset
"""Auto-ported test: TestBinaryOps::test_union_superset (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
result = self_set | set([2, 4, 6, 8])

assert result == set([2, 4, 6, 8])
print("TestBinaryOps::test_union_superset: ok")
"###);
    assert_output(&out, r###"TestBinaryOps::test_union_superset: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops_mutating_set_set__test_eq_with_mutation.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops_mutating_set_set__test_eq_with_mutation() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops_mutating_set_set__test_eq_with_mutation"
# subject = "cpython.test_set.TestBinaryOpsMutating_Set_Set.test_eq_with_mutation"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOpsMutating_Set_Set::test_eq_with_mutation
"""Auto-ported test: TestBinaryOpsMutating_Set_Set::test_eq_with_mutation (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
constructor1 = None
constructor2 = None
constructor1 = set
constructor2 = set

def check_set_op_does_not_crash(function):
    for _ in range(100):
        set1, set2 = make_sets_of_bad_objects()
        try:
            function(set1, set2)
        except RuntimeError as e:

            assert 'changed size during iteration' in str(e)

def make_sets_of_bad_objects():

    class Bad:

        def __eq__(self, other):
            if not enabled:
                return False
            if randrange(20) == 0:
                set1.clear()
            if randrange(20) == 0:
                set2.clear()
            return bool(randrange(2))

        def __hash__(self):
            return randrange(2)
    enabled = False
    set1 = constructor1((Bad() for _ in range(randrange(50))))
    set2 = constructor2((Bad() for _ in range(randrange(50))))
    enabled = True
    return (set1, set2)
check_set_op_does_not_crash(lambda a, b: a == b)
print("TestBinaryOpsMutating_Set_Set::test_eq_with_mutation: ok")
"###);
    assert_output(&out, r###"TestBinaryOpsMutating_Set_Set::test_eq_with_mutation: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops_mutating_set_set__test_ge_with_mutation.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops_mutating_set_set__test_ge_with_mutation() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops_mutating_set_set__test_ge_with_mutation"
# subject = "cpython.test_set.TestBinaryOpsMutating_Set_Set.test_ge_with_mutation"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOpsMutating_Set_Set::test_ge_with_mutation
"""Auto-ported test: TestBinaryOpsMutating_Set_Set::test_ge_with_mutation (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
constructor1 = None
constructor2 = None
constructor1 = set
constructor2 = set

def check_set_op_does_not_crash(function):
    for _ in range(100):
        set1, set2 = make_sets_of_bad_objects()
        try:
            function(set1, set2)
        except RuntimeError as e:

            assert 'changed size during iteration' in str(e)

def make_sets_of_bad_objects():

    class Bad:

        def __eq__(self, other):
            if not enabled:
                return False
            if randrange(20) == 0:
                set1.clear()
            if randrange(20) == 0:
                set2.clear()
            return bool(randrange(2))

        def __hash__(self):
            return randrange(2)
    enabled = False
    set1 = constructor1((Bad() for _ in range(randrange(50))))
    set2 = constructor2((Bad() for _ in range(randrange(50))))
    enabled = True
    return (set1, set2)
check_set_op_does_not_crash(lambda a, b: a >= b)
print("TestBinaryOpsMutating_Set_Set::test_ge_with_mutation: ok")
"###);
    assert_output(&out, r###"TestBinaryOpsMutating_Set_Set::test_ge_with_mutation: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops_mutating_set_set__test_gt_with_mutation.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops_mutating_set_set__test_gt_with_mutation() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops_mutating_set_set__test_gt_with_mutation"
# subject = "cpython.test_set.TestBinaryOpsMutating_Set_Set.test_gt_with_mutation"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOpsMutating_Set_Set::test_gt_with_mutation
"""Auto-ported test: TestBinaryOpsMutating_Set_Set::test_gt_with_mutation (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
constructor1 = None
constructor2 = None
constructor1 = set
constructor2 = set

def check_set_op_does_not_crash(function):
    for _ in range(100):
        set1, set2 = make_sets_of_bad_objects()
        try:
            function(set1, set2)
        except RuntimeError as e:

            assert 'changed size during iteration' in str(e)

def make_sets_of_bad_objects():

    class Bad:

        def __eq__(self, other):
            if not enabled:
                return False
            if randrange(20) == 0:
                set1.clear()
            if randrange(20) == 0:
                set2.clear()
            return bool(randrange(2))

        def __hash__(self):
            return randrange(2)
    enabled = False
    set1 = constructor1((Bad() for _ in range(randrange(50))))
    set2 = constructor2((Bad() for _ in range(randrange(50))))
    enabled = True
    return (set1, set2)
check_set_op_does_not_crash(lambda a, b: a > b)
print("TestBinaryOpsMutating_Set_Set::test_gt_with_mutation: ok")
"###);
    assert_output(&out, r###"TestBinaryOpsMutating_Set_Set::test_gt_with_mutation: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops_mutating_set_set__test_le_with_mutation.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops_mutating_set_set__test_le_with_mutation() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops_mutating_set_set__test_le_with_mutation"
# subject = "cpython.test_set.TestBinaryOpsMutating_Set_Set.test_le_with_mutation"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOpsMutating_Set_Set::test_le_with_mutation
"""Auto-ported test: TestBinaryOpsMutating_Set_Set::test_le_with_mutation (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
constructor1 = None
constructor2 = None
constructor1 = set
constructor2 = set

def check_set_op_does_not_crash(function):
    for _ in range(100):
        set1, set2 = make_sets_of_bad_objects()
        try:
            function(set1, set2)
        except RuntimeError as e:

            assert 'changed size during iteration' in str(e)

def make_sets_of_bad_objects():

    class Bad:

        def __eq__(self, other):
            if not enabled:
                return False
            if randrange(20) == 0:
                set1.clear()
            if randrange(20) == 0:
                set2.clear()
            return bool(randrange(2))

        def __hash__(self):
            return randrange(2)
    enabled = False
    set1 = constructor1((Bad() for _ in range(randrange(50))))
    set2 = constructor2((Bad() for _ in range(randrange(50))))
    enabled = True
    return (set1, set2)
check_set_op_does_not_crash(lambda a, b: a <= b)
print("TestBinaryOpsMutating_Set_Set::test_le_with_mutation: ok")
"###);
    assert_output(&out, r###"TestBinaryOpsMutating_Set_Set::test_le_with_mutation: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops_mutating_set_set__test_lt_with_mutation.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops_mutating_set_set__test_lt_with_mutation() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops_mutating_set_set__test_lt_with_mutation"
# subject = "cpython.test_set.TestBinaryOpsMutating_Set_Set.test_lt_with_mutation"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOpsMutating_Set_Set::test_lt_with_mutation
"""Auto-ported test: TestBinaryOpsMutating_Set_Set::test_lt_with_mutation (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
constructor1 = None
constructor2 = None
constructor1 = set
constructor2 = set

def check_set_op_does_not_crash(function):
    for _ in range(100):
        set1, set2 = make_sets_of_bad_objects()
        try:
            function(set1, set2)
        except RuntimeError as e:

            assert 'changed size during iteration' in str(e)

def make_sets_of_bad_objects():

    class Bad:

        def __eq__(self, other):
            if not enabled:
                return False
            if randrange(20) == 0:
                set1.clear()
            if randrange(20) == 0:
                set2.clear()
            return bool(randrange(2))

        def __hash__(self):
            return randrange(2)
    enabled = False
    set1 = constructor1((Bad() for _ in range(randrange(50))))
    set2 = constructor2((Bad() for _ in range(randrange(50))))
    enabled = True
    return (set1, set2)
check_set_op_does_not_crash(lambda a, b: a < b)
print("TestBinaryOpsMutating_Set_Set::test_lt_with_mutation: ok")
"###);
    assert_output(&out, r###"TestBinaryOpsMutating_Set_Set::test_lt_with_mutation: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops_mutating_set_set__test_ne_with_mutation.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops_mutating_set_set__test_ne_with_mutation() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops_mutating_set_set__test_ne_with_mutation"
# subject = "cpython.test_set.TestBinaryOpsMutating_Set_Set.test_ne_with_mutation"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOpsMutating_Set_Set::test_ne_with_mutation
"""Auto-ported test: TestBinaryOpsMutating_Set_Set::test_ne_with_mutation (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
constructor1 = None
constructor2 = None
constructor1 = set
constructor2 = set

def check_set_op_does_not_crash(function):
    for _ in range(100):
        set1, set2 = make_sets_of_bad_objects()
        try:
            function(set1, set2)
        except RuntimeError as e:

            assert 'changed size during iteration' in str(e)

def make_sets_of_bad_objects():

    class Bad:

        def __eq__(self, other):
            if not enabled:
                return False
            if randrange(20) == 0:
                set1.clear()
            if randrange(20) == 0:
                set2.clear()
            return bool(randrange(2))

        def __hash__(self):
            return randrange(2)
    enabled = False
    set1 = constructor1((Bad() for _ in range(randrange(50))))
    set2 = constructor2((Bad() for _ in range(randrange(50))))
    enabled = True
    return (set1, set2)
check_set_op_does_not_crash(lambda a, b: a != b)
print("TestBinaryOpsMutating_Set_Set::test_ne_with_mutation: ok")
"###);
    assert_output(&out, r###"TestBinaryOpsMutating_Set_Set::test_ne_with_mutation: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops_mutating_set_subclass__test_eq_with_mutation.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops_mutating_set_subclass__test_eq_with_mutation() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops_mutating_set_subclass__test_eq_with_mutation"
# subject = "cpython.test_set.TestBinaryOpsMutating_Set_Subclass.test_eq_with_mutation"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOpsMutating_Set_Subclass::test_eq_with_mutation
"""Auto-ported test: TestBinaryOpsMutating_Set_Subclass::test_eq_with_mutation (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
constructor1 = None
constructor2 = None
constructor1 = set
constructor2 = SetSubclass

def check_set_op_does_not_crash(function):
    for _ in range(100):
        set1, set2 = make_sets_of_bad_objects()
        try:
            function(set1, set2)
        except RuntimeError as e:

            assert 'changed size during iteration' in str(e)

def make_sets_of_bad_objects():

    class Bad:

        def __eq__(self, other):
            if not enabled:
                return False
            if randrange(20) == 0:
                set1.clear()
            if randrange(20) == 0:
                set2.clear()
            return bool(randrange(2))

        def __hash__(self):
            return randrange(2)
    enabled = False
    set1 = constructor1((Bad() for _ in range(randrange(50))))
    set2 = constructor2((Bad() for _ in range(randrange(50))))
    enabled = True
    return (set1, set2)
check_set_op_does_not_crash(lambda a, b: a == b)
print("TestBinaryOpsMutating_Set_Subclass::test_eq_with_mutation: ok")
"###);
    assert_output(&out, r###"TestBinaryOpsMutating_Set_Subclass::test_eq_with_mutation: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops_mutating_set_subclass__test_ge_with_mutation.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops_mutating_set_subclass__test_ge_with_mutation() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops_mutating_set_subclass__test_ge_with_mutation"
# subject = "cpython.test_set.TestBinaryOpsMutating_Set_Subclass.test_ge_with_mutation"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOpsMutating_Set_Subclass::test_ge_with_mutation
"""Auto-ported test: TestBinaryOpsMutating_Set_Subclass::test_ge_with_mutation (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
constructor1 = None
constructor2 = None
constructor1 = set
constructor2 = SetSubclass

def check_set_op_does_not_crash(function):
    for _ in range(100):
        set1, set2 = make_sets_of_bad_objects()
        try:
            function(set1, set2)
        except RuntimeError as e:

            assert 'changed size during iteration' in str(e)

def make_sets_of_bad_objects():

    class Bad:

        def __eq__(self, other):
            if not enabled:
                return False
            if randrange(20) == 0:
                set1.clear()
            if randrange(20) == 0:
                set2.clear()
            return bool(randrange(2))

        def __hash__(self):
            return randrange(2)
    enabled = False
    set1 = constructor1((Bad() for _ in range(randrange(50))))
    set2 = constructor2((Bad() for _ in range(randrange(50))))
    enabled = True
    return (set1, set2)
check_set_op_does_not_crash(lambda a, b: a >= b)
print("TestBinaryOpsMutating_Set_Subclass::test_ge_with_mutation: ok")
"###);
    assert_output(&out, r###"TestBinaryOpsMutating_Set_Subclass::test_ge_with_mutation: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops_mutating_set_subclass__test_gt_with_mutation.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops_mutating_set_subclass__test_gt_with_mutation() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops_mutating_set_subclass__test_gt_with_mutation"
# subject = "cpython.test_set.TestBinaryOpsMutating_Set_Subclass.test_gt_with_mutation"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOpsMutating_Set_Subclass::test_gt_with_mutation
"""Auto-ported test: TestBinaryOpsMutating_Set_Subclass::test_gt_with_mutation (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
constructor1 = None
constructor2 = None
constructor1 = set
constructor2 = SetSubclass

def check_set_op_does_not_crash(function):
    for _ in range(100):
        set1, set2 = make_sets_of_bad_objects()
        try:
            function(set1, set2)
        except RuntimeError as e:

            assert 'changed size during iteration' in str(e)

def make_sets_of_bad_objects():

    class Bad:

        def __eq__(self, other):
            if not enabled:
                return False
            if randrange(20) == 0:
                set1.clear()
            if randrange(20) == 0:
                set2.clear()
            return bool(randrange(2))

        def __hash__(self):
            return randrange(2)
    enabled = False
    set1 = constructor1((Bad() for _ in range(randrange(50))))
    set2 = constructor2((Bad() for _ in range(randrange(50))))
    enabled = True
    return (set1, set2)
check_set_op_does_not_crash(lambda a, b: a > b)
print("TestBinaryOpsMutating_Set_Subclass::test_gt_with_mutation: ok")
"###);
    assert_output(&out, r###"TestBinaryOpsMutating_Set_Subclass::test_gt_with_mutation: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops_mutating_set_subclass__test_le_with_mutation.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops_mutating_set_subclass__test_le_with_mutation() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops_mutating_set_subclass__test_le_with_mutation"
# subject = "cpython.test_set.TestBinaryOpsMutating_Set_Subclass.test_le_with_mutation"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOpsMutating_Set_Subclass::test_le_with_mutation
"""Auto-ported test: TestBinaryOpsMutating_Set_Subclass::test_le_with_mutation (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
constructor1 = None
constructor2 = None
constructor1 = set
constructor2 = SetSubclass

def check_set_op_does_not_crash(function):
    for _ in range(100):
        set1, set2 = make_sets_of_bad_objects()
        try:
            function(set1, set2)
        except RuntimeError as e:

            assert 'changed size during iteration' in str(e)

def make_sets_of_bad_objects():

    class Bad:

        def __eq__(self, other):
            if not enabled:
                return False
            if randrange(20) == 0:
                set1.clear()
            if randrange(20) == 0:
                set2.clear()
            return bool(randrange(2))

        def __hash__(self):
            return randrange(2)
    enabled = False
    set1 = constructor1((Bad() for _ in range(randrange(50))))
    set2 = constructor2((Bad() for _ in range(randrange(50))))
    enabled = True
    return (set1, set2)
check_set_op_does_not_crash(lambda a, b: a <= b)
print("TestBinaryOpsMutating_Set_Subclass::test_le_with_mutation: ok")
"###);
    assert_output(&out, r###"TestBinaryOpsMutating_Set_Subclass::test_le_with_mutation: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops_mutating_set_subclass__test_lt_with_mutation.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops_mutating_set_subclass__test_lt_with_mutation() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops_mutating_set_subclass__test_lt_with_mutation"
# subject = "cpython.test_set.TestBinaryOpsMutating_Set_Subclass.test_lt_with_mutation"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOpsMutating_Set_Subclass::test_lt_with_mutation
"""Auto-ported test: TestBinaryOpsMutating_Set_Subclass::test_lt_with_mutation (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
constructor1 = None
constructor2 = None
constructor1 = set
constructor2 = SetSubclass

def check_set_op_does_not_crash(function):
    for _ in range(100):
        set1, set2 = make_sets_of_bad_objects()
        try:
            function(set1, set2)
        except RuntimeError as e:

            assert 'changed size during iteration' in str(e)

def make_sets_of_bad_objects():

    class Bad:

        def __eq__(self, other):
            if not enabled:
                return False
            if randrange(20) == 0:
                set1.clear()
            if randrange(20) == 0:
                set2.clear()
            return bool(randrange(2))

        def __hash__(self):
            return randrange(2)
    enabled = False
    set1 = constructor1((Bad() for _ in range(randrange(50))))
    set2 = constructor2((Bad() for _ in range(randrange(50))))
    enabled = True
    return (set1, set2)
check_set_op_does_not_crash(lambda a, b: a < b)
print("TestBinaryOpsMutating_Set_Subclass::test_lt_with_mutation: ok")
"###);
    assert_output(&out, r###"TestBinaryOpsMutating_Set_Subclass::test_lt_with_mutation: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops_mutating_set_subclass__test_ne_with_mutation.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops_mutating_set_subclass__test_ne_with_mutation() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops_mutating_set_subclass__test_ne_with_mutation"
# subject = "cpython.test_set.TestBinaryOpsMutating_Set_Subclass.test_ne_with_mutation"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOpsMutating_Set_Subclass::test_ne_with_mutation
"""Auto-ported test: TestBinaryOpsMutating_Set_Subclass::test_ne_with_mutation (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
constructor1 = None
constructor2 = None
constructor1 = set
constructor2 = SetSubclass

def check_set_op_does_not_crash(function):
    for _ in range(100):
        set1, set2 = make_sets_of_bad_objects()
        try:
            function(set1, set2)
        except RuntimeError as e:

            assert 'changed size during iteration' in str(e)

def make_sets_of_bad_objects():

    class Bad:

        def __eq__(self, other):
            if not enabled:
                return False
            if randrange(20) == 0:
                set1.clear()
            if randrange(20) == 0:
                set2.clear()
            return bool(randrange(2))

        def __hash__(self):
            return randrange(2)
    enabled = False
    set1 = constructor1((Bad() for _ in range(randrange(50))))
    set2 = constructor2((Bad() for _ in range(randrange(50))))
    enabled = True
    return (set1, set2)
check_set_op_does_not_crash(lambda a, b: a != b)
print("TestBinaryOpsMutating_Set_Subclass::test_ne_with_mutation: ok")
"###);
    assert_output(&out, r###"TestBinaryOpsMutating_Set_Subclass::test_ne_with_mutation: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops_mutating_subclass_set__test_eq_with_mutation.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops_mutating_subclass_set__test_eq_with_mutation() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops_mutating_subclass_set__test_eq_with_mutation"
# subject = "cpython.test_set.TestBinaryOpsMutating_Subclass_Set.test_eq_with_mutation"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOpsMutating_Subclass_Set::test_eq_with_mutation
"""Auto-ported test: TestBinaryOpsMutating_Subclass_Set::test_eq_with_mutation (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
constructor1 = None
constructor2 = None
constructor1 = SetSubclass
constructor2 = set

def check_set_op_does_not_crash(function):
    for _ in range(100):
        set1, set2 = make_sets_of_bad_objects()
        try:
            function(set1, set2)
        except RuntimeError as e:

            assert 'changed size during iteration' in str(e)

def make_sets_of_bad_objects():

    class Bad:

        def __eq__(self, other):
            if not enabled:
                return False
            if randrange(20) == 0:
                set1.clear()
            if randrange(20) == 0:
                set2.clear()
            return bool(randrange(2))

        def __hash__(self):
            return randrange(2)
    enabled = False
    set1 = constructor1((Bad() for _ in range(randrange(50))))
    set2 = constructor2((Bad() for _ in range(randrange(50))))
    enabled = True
    return (set1, set2)
check_set_op_does_not_crash(lambda a, b: a == b)
print("TestBinaryOpsMutating_Subclass_Set::test_eq_with_mutation: ok")
"###);
    assert_output(&out, r###"TestBinaryOpsMutating_Subclass_Set::test_eq_with_mutation: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops_mutating_subclass_set__test_ge_with_mutation.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops_mutating_subclass_set__test_ge_with_mutation() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops_mutating_subclass_set__test_ge_with_mutation"
# subject = "cpython.test_set.TestBinaryOpsMutating_Subclass_Set.test_ge_with_mutation"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOpsMutating_Subclass_Set::test_ge_with_mutation
"""Auto-ported test: TestBinaryOpsMutating_Subclass_Set::test_ge_with_mutation (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
constructor1 = None
constructor2 = None
constructor1 = SetSubclass
constructor2 = set

def check_set_op_does_not_crash(function):
    for _ in range(100):
        set1, set2 = make_sets_of_bad_objects()
        try:
            function(set1, set2)
        except RuntimeError as e:

            assert 'changed size during iteration' in str(e)

def make_sets_of_bad_objects():

    class Bad:

        def __eq__(self, other):
            if not enabled:
                return False
            if randrange(20) == 0:
                set1.clear()
            if randrange(20) == 0:
                set2.clear()
            return bool(randrange(2))

        def __hash__(self):
            return randrange(2)
    enabled = False
    set1 = constructor1((Bad() for _ in range(randrange(50))))
    set2 = constructor2((Bad() for _ in range(randrange(50))))
    enabled = True
    return (set1, set2)
check_set_op_does_not_crash(lambda a, b: a >= b)
print("TestBinaryOpsMutating_Subclass_Set::test_ge_with_mutation: ok")
"###);
    assert_output(&out, r###"TestBinaryOpsMutating_Subclass_Set::test_ge_with_mutation: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops_mutating_subclass_set__test_gt_with_mutation.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops_mutating_subclass_set__test_gt_with_mutation() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops_mutating_subclass_set__test_gt_with_mutation"
# subject = "cpython.test_set.TestBinaryOpsMutating_Subclass_Set.test_gt_with_mutation"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOpsMutating_Subclass_Set::test_gt_with_mutation
"""Auto-ported test: TestBinaryOpsMutating_Subclass_Set::test_gt_with_mutation (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
constructor1 = None
constructor2 = None
constructor1 = SetSubclass
constructor2 = set

def check_set_op_does_not_crash(function):
    for _ in range(100):
        set1, set2 = make_sets_of_bad_objects()
        try:
            function(set1, set2)
        except RuntimeError as e:

            assert 'changed size during iteration' in str(e)

def make_sets_of_bad_objects():

    class Bad:

        def __eq__(self, other):
            if not enabled:
                return False
            if randrange(20) == 0:
                set1.clear()
            if randrange(20) == 0:
                set2.clear()
            return bool(randrange(2))

        def __hash__(self):
            return randrange(2)
    enabled = False
    set1 = constructor1((Bad() for _ in range(randrange(50))))
    set2 = constructor2((Bad() for _ in range(randrange(50))))
    enabled = True
    return (set1, set2)
check_set_op_does_not_crash(lambda a, b: a > b)
print("TestBinaryOpsMutating_Subclass_Set::test_gt_with_mutation: ok")
"###);
    assert_output(&out, r###"TestBinaryOpsMutating_Subclass_Set::test_gt_with_mutation: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops_mutating_subclass_set__test_le_with_mutation.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops_mutating_subclass_set__test_le_with_mutation() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops_mutating_subclass_set__test_le_with_mutation"
# subject = "cpython.test_set.TestBinaryOpsMutating_Subclass_Set.test_le_with_mutation"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOpsMutating_Subclass_Set::test_le_with_mutation
"""Auto-ported test: TestBinaryOpsMutating_Subclass_Set::test_le_with_mutation (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
constructor1 = None
constructor2 = None
constructor1 = SetSubclass
constructor2 = set

def check_set_op_does_not_crash(function):
    for _ in range(100):
        set1, set2 = make_sets_of_bad_objects()
        try:
            function(set1, set2)
        except RuntimeError as e:

            assert 'changed size during iteration' in str(e)

def make_sets_of_bad_objects():

    class Bad:

        def __eq__(self, other):
            if not enabled:
                return False
            if randrange(20) == 0:
                set1.clear()
            if randrange(20) == 0:
                set2.clear()
            return bool(randrange(2))

        def __hash__(self):
            return randrange(2)
    enabled = False
    set1 = constructor1((Bad() for _ in range(randrange(50))))
    set2 = constructor2((Bad() for _ in range(randrange(50))))
    enabled = True
    return (set1, set2)
check_set_op_does_not_crash(lambda a, b: a <= b)
print("TestBinaryOpsMutating_Subclass_Set::test_le_with_mutation: ok")
"###);
    assert_output(&out, r###"TestBinaryOpsMutating_Subclass_Set::test_le_with_mutation: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops_mutating_subclass_set__test_lt_with_mutation.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops_mutating_subclass_set__test_lt_with_mutation() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops_mutating_subclass_set__test_lt_with_mutation"
# subject = "cpython.test_set.TestBinaryOpsMutating_Subclass_Set.test_lt_with_mutation"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOpsMutating_Subclass_Set::test_lt_with_mutation
"""Auto-ported test: TestBinaryOpsMutating_Subclass_Set::test_lt_with_mutation (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
constructor1 = None
constructor2 = None
constructor1 = SetSubclass
constructor2 = set

def check_set_op_does_not_crash(function):
    for _ in range(100):
        set1, set2 = make_sets_of_bad_objects()
        try:
            function(set1, set2)
        except RuntimeError as e:

            assert 'changed size during iteration' in str(e)

def make_sets_of_bad_objects():

    class Bad:

        def __eq__(self, other):
            if not enabled:
                return False
            if randrange(20) == 0:
                set1.clear()
            if randrange(20) == 0:
                set2.clear()
            return bool(randrange(2))

        def __hash__(self):
            return randrange(2)
    enabled = False
    set1 = constructor1((Bad() for _ in range(randrange(50))))
    set2 = constructor2((Bad() for _ in range(randrange(50))))
    enabled = True
    return (set1, set2)
check_set_op_does_not_crash(lambda a, b: a < b)
print("TestBinaryOpsMutating_Subclass_Set::test_lt_with_mutation: ok")
"###);
    assert_output(&out, r###"TestBinaryOpsMutating_Subclass_Set::test_lt_with_mutation: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops_mutating_subclass_set__test_ne_with_mutation.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops_mutating_subclass_set__test_ne_with_mutation() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops_mutating_subclass_set__test_ne_with_mutation"
# subject = "cpython.test_set.TestBinaryOpsMutating_Subclass_Set.test_ne_with_mutation"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOpsMutating_Subclass_Set::test_ne_with_mutation
"""Auto-ported test: TestBinaryOpsMutating_Subclass_Set::test_ne_with_mutation (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
constructor1 = None
constructor2 = None
constructor1 = SetSubclass
constructor2 = set

def check_set_op_does_not_crash(function):
    for _ in range(100):
        set1, set2 = make_sets_of_bad_objects()
        try:
            function(set1, set2)
        except RuntimeError as e:

            assert 'changed size during iteration' in str(e)

def make_sets_of_bad_objects():

    class Bad:

        def __eq__(self, other):
            if not enabled:
                return False
            if randrange(20) == 0:
                set1.clear()
            if randrange(20) == 0:
                set2.clear()
            return bool(randrange(2))

        def __hash__(self):
            return randrange(2)
    enabled = False
    set1 = constructor1((Bad() for _ in range(randrange(50))))
    set2 = constructor2((Bad() for _ in range(randrange(50))))
    enabled = True
    return (set1, set2)
check_set_op_does_not_crash(lambda a, b: a != b)
print("TestBinaryOpsMutating_Subclass_Set::test_ne_with_mutation: ok")
"###);
    assert_output(&out, r###"TestBinaryOpsMutating_Subclass_Set::test_ne_with_mutation: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops_mutating_subclass_subclass__test_eq_with_mutation.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops_mutating_subclass_subclass__test_eq_with_mutation() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops_mutating_subclass_subclass__test_eq_with_mutation"
# subject = "cpython.test_set.TestBinaryOpsMutating_Subclass_Subclass.test_eq_with_mutation"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOpsMutating_Subclass_Subclass::test_eq_with_mutation
"""Auto-ported test: TestBinaryOpsMutating_Subclass_Subclass::test_eq_with_mutation (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
constructor1 = None
constructor2 = None
constructor1 = SetSubclass
constructor2 = SetSubclass

def check_set_op_does_not_crash(function):
    for _ in range(100):
        set1, set2 = make_sets_of_bad_objects()
        try:
            function(set1, set2)
        except RuntimeError as e:

            assert 'changed size during iteration' in str(e)

def make_sets_of_bad_objects():

    class Bad:

        def __eq__(self, other):
            if not enabled:
                return False
            if randrange(20) == 0:
                set1.clear()
            if randrange(20) == 0:
                set2.clear()
            return bool(randrange(2))

        def __hash__(self):
            return randrange(2)
    enabled = False
    set1 = constructor1((Bad() for _ in range(randrange(50))))
    set2 = constructor2((Bad() for _ in range(randrange(50))))
    enabled = True
    return (set1, set2)
check_set_op_does_not_crash(lambda a, b: a == b)
print("TestBinaryOpsMutating_Subclass_Subclass::test_eq_with_mutation: ok")
"###);
    assert_output(&out, r###"TestBinaryOpsMutating_Subclass_Subclass::test_eq_with_mutation: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops_mutating_subclass_subclass__test_ge_with_mutation.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops_mutating_subclass_subclass__test_ge_with_mutation() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops_mutating_subclass_subclass__test_ge_with_mutation"
# subject = "cpython.test_set.TestBinaryOpsMutating_Subclass_Subclass.test_ge_with_mutation"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOpsMutating_Subclass_Subclass::test_ge_with_mutation
"""Auto-ported test: TestBinaryOpsMutating_Subclass_Subclass::test_ge_with_mutation (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
constructor1 = None
constructor2 = None
constructor1 = SetSubclass
constructor2 = SetSubclass

def check_set_op_does_not_crash(function):
    for _ in range(100):
        set1, set2 = make_sets_of_bad_objects()
        try:
            function(set1, set2)
        except RuntimeError as e:

            assert 'changed size during iteration' in str(e)

def make_sets_of_bad_objects():

    class Bad:

        def __eq__(self, other):
            if not enabled:
                return False
            if randrange(20) == 0:
                set1.clear()
            if randrange(20) == 0:
                set2.clear()
            return bool(randrange(2))

        def __hash__(self):
            return randrange(2)
    enabled = False
    set1 = constructor1((Bad() for _ in range(randrange(50))))
    set2 = constructor2((Bad() for _ in range(randrange(50))))
    enabled = True
    return (set1, set2)
check_set_op_does_not_crash(lambda a, b: a >= b)
print("TestBinaryOpsMutating_Subclass_Subclass::test_ge_with_mutation: ok")
"###);
    assert_output(&out, r###"TestBinaryOpsMutating_Subclass_Subclass::test_ge_with_mutation: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops_mutating_subclass_subclass__test_gt_with_mutation.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops_mutating_subclass_subclass__test_gt_with_mutation() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops_mutating_subclass_subclass__test_gt_with_mutation"
# subject = "cpython.test_set.TestBinaryOpsMutating_Subclass_Subclass.test_gt_with_mutation"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOpsMutating_Subclass_Subclass::test_gt_with_mutation
"""Auto-ported test: TestBinaryOpsMutating_Subclass_Subclass::test_gt_with_mutation (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
constructor1 = None
constructor2 = None
constructor1 = SetSubclass
constructor2 = SetSubclass

def check_set_op_does_not_crash(function):
    for _ in range(100):
        set1, set2 = make_sets_of_bad_objects()
        try:
            function(set1, set2)
        except RuntimeError as e:

            assert 'changed size during iteration' in str(e)

def make_sets_of_bad_objects():

    class Bad:

        def __eq__(self, other):
            if not enabled:
                return False
            if randrange(20) == 0:
                set1.clear()
            if randrange(20) == 0:
                set2.clear()
            return bool(randrange(2))

        def __hash__(self):
            return randrange(2)
    enabled = False
    set1 = constructor1((Bad() for _ in range(randrange(50))))
    set2 = constructor2((Bad() for _ in range(randrange(50))))
    enabled = True
    return (set1, set2)
check_set_op_does_not_crash(lambda a, b: a > b)
print("TestBinaryOpsMutating_Subclass_Subclass::test_gt_with_mutation: ok")
"###);
    assert_output(&out, r###"TestBinaryOpsMutating_Subclass_Subclass::test_gt_with_mutation: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops_mutating_subclass_subclass__test_le_with_mutation.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops_mutating_subclass_subclass__test_le_with_mutation() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops_mutating_subclass_subclass__test_le_with_mutation"
# subject = "cpython.test_set.TestBinaryOpsMutating_Subclass_Subclass.test_le_with_mutation"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOpsMutating_Subclass_Subclass::test_le_with_mutation
"""Auto-ported test: TestBinaryOpsMutating_Subclass_Subclass::test_le_with_mutation (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
constructor1 = None
constructor2 = None
constructor1 = SetSubclass
constructor2 = SetSubclass

def check_set_op_does_not_crash(function):
    for _ in range(100):
        set1, set2 = make_sets_of_bad_objects()
        try:
            function(set1, set2)
        except RuntimeError as e:

            assert 'changed size during iteration' in str(e)

def make_sets_of_bad_objects():

    class Bad:

        def __eq__(self, other):
            if not enabled:
                return False
            if randrange(20) == 0:
                set1.clear()
            if randrange(20) == 0:
                set2.clear()
            return bool(randrange(2))

        def __hash__(self):
            return randrange(2)
    enabled = False
    set1 = constructor1((Bad() for _ in range(randrange(50))))
    set2 = constructor2((Bad() for _ in range(randrange(50))))
    enabled = True
    return (set1, set2)
check_set_op_does_not_crash(lambda a, b: a <= b)
print("TestBinaryOpsMutating_Subclass_Subclass::test_le_with_mutation: ok")
"###);
    assert_output(&out, r###"TestBinaryOpsMutating_Subclass_Subclass::test_le_with_mutation: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops_mutating_subclass_subclass__test_lt_with_mutation.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops_mutating_subclass_subclass__test_lt_with_mutation() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops_mutating_subclass_subclass__test_lt_with_mutation"
# subject = "cpython.test_set.TestBinaryOpsMutating_Subclass_Subclass.test_lt_with_mutation"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOpsMutating_Subclass_Subclass::test_lt_with_mutation
"""Auto-ported test: TestBinaryOpsMutating_Subclass_Subclass::test_lt_with_mutation (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
constructor1 = None
constructor2 = None
constructor1 = SetSubclass
constructor2 = SetSubclass

def check_set_op_does_not_crash(function):
    for _ in range(100):
        set1, set2 = make_sets_of_bad_objects()
        try:
            function(set1, set2)
        except RuntimeError as e:

            assert 'changed size during iteration' in str(e)

def make_sets_of_bad_objects():

    class Bad:

        def __eq__(self, other):
            if not enabled:
                return False
            if randrange(20) == 0:
                set1.clear()
            if randrange(20) == 0:
                set2.clear()
            return bool(randrange(2))

        def __hash__(self):
            return randrange(2)
    enabled = False
    set1 = constructor1((Bad() for _ in range(randrange(50))))
    set2 = constructor2((Bad() for _ in range(randrange(50))))
    enabled = True
    return (set1, set2)
check_set_op_does_not_crash(lambda a, b: a < b)
print("TestBinaryOpsMutating_Subclass_Subclass::test_lt_with_mutation: ok")
"###);
    assert_output(&out, r###"TestBinaryOpsMutating_Subclass_Subclass::test_lt_with_mutation: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_binary_ops_mutating_subclass_subclass__test_ne_with_mutation.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_binary_ops_mutating_subclass_subclass__test_ne_with_mutation() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_binary_ops_mutating_subclass_subclass__test_ne_with_mutation"
# subject = "cpython.test_set.TestBinaryOpsMutating_Subclass_Subclass.test_ne_with_mutation"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestBinaryOpsMutating_Subclass_Subclass::test_ne_with_mutation
"""Auto-ported test: TestBinaryOpsMutating_Subclass_Subclass::test_ne_with_mutation (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
constructor1 = None
constructor2 = None
constructor1 = SetSubclass
constructor2 = SetSubclass

def check_set_op_does_not_crash(function):
    for _ in range(100):
        set1, set2 = make_sets_of_bad_objects()
        try:
            function(set1, set2)
        except RuntimeError as e:

            assert 'changed size during iteration' in str(e)

def make_sets_of_bad_objects():

    class Bad:

        def __eq__(self, other):
            if not enabled:
                return False
            if randrange(20) == 0:
                set1.clear()
            if randrange(20) == 0:
                set2.clear()
            return bool(randrange(2))

        def __hash__(self):
            return randrange(2)
    enabled = False
    set1 = constructor1((Bad() for _ in range(randrange(50))))
    set2 = constructor2((Bad() for _ in range(randrange(50))))
    enabled = True
    return (set1, set2)
check_set_op_does_not_crash(lambda a, b: a != b)
print("TestBinaryOpsMutating_Subclass_Subclass::test_ne_with_mutation: ok")
"###);
    assert_output(&out, r###"TestBinaryOpsMutating_Subclass_Subclass::test_ne_with_mutation: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_copying_empty__test_copy.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_copying_empty__test_copy() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_copying_empty__test_copy"
# subject = "cpython.test_set.TestCopyingEmpty.test_copy"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestCopyingEmpty::test_copy
"""Auto-ported test: TestCopyingEmpty::test_copy (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set()
dup = self_set.copy()
dup_list = sorted(dup, key=repr)
set_list = sorted(self_set, key=repr)

assert len(dup_list) == len(set_list)
for i in range(len(dup_list)):

    assert dup_list[i] is set_list[i]
print("TestCopyingEmpty::test_copy: ok")
"###);
    assert_output(&out, r###"TestCopyingEmpty::test_copy: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_copying_empty__test_deep_copy.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_copying_empty__test_deep_copy() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_copying_empty__test_deep_copy"
# subject = "cpython.test_set.TestCopyingEmpty.test_deep_copy"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestCopyingEmpty::test_deep_copy
"""Auto-ported test: TestCopyingEmpty::test_deep_copy (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set()
dup = copy.deepcopy(self_set)
dup_list = sorted(dup, key=repr)
set_list = sorted(self_set, key=repr)

assert len(dup_list) == len(set_list)
for i in range(len(dup_list)):

    assert dup_list[i] == set_list[i]
print("TestCopyingEmpty::test_deep_copy: ok")
"###);
    assert_output(&out, r###"TestCopyingEmpty::test_deep_copy: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_copying_nested__test_copy.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_copying_nested__test_copy() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_copying_nested__test_copy"
# subject = "cpython.test_set.TestCopyingNested.test_copy"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestCopyingNested::test_copy
"""Auto-ported test: TestCopyingNested::test_copy (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set([((1, 2), (3, 4))])
dup = self_set.copy()
dup_list = sorted(dup, key=repr)
set_list = sorted(self_set, key=repr)

assert len(dup_list) == len(set_list)
for i in range(len(dup_list)):

    assert dup_list[i] is set_list[i]
print("TestCopyingNested::test_copy: ok")
"###);
    assert_output(&out, r###"TestCopyingNested::test_copy: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_copying_nested__test_deep_copy.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_copying_nested__test_deep_copy() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_copying_nested__test_deep_copy"
# subject = "cpython.test_set.TestCopyingNested.test_deep_copy"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestCopyingNested::test_deep_copy
"""Auto-ported test: TestCopyingNested::test_deep_copy (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set([((1, 2), (3, 4))])
dup = copy.deepcopy(self_set)
dup_list = sorted(dup, key=repr)
set_list = sorted(self_set, key=repr)

assert len(dup_list) == len(set_list)
for i in range(len(dup_list)):

    assert dup_list[i] == set_list[i]
print("TestCopyingNested::test_deep_copy: ok")
"###);
    assert_output(&out, r###"TestCopyingNested::test_deep_copy: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_copying_singleton__test_copy.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_copying_singleton__test_copy() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_copying_singleton__test_copy"
# subject = "cpython.test_set.TestCopyingSingleton.test_copy"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestCopyingSingleton::test_copy
"""Auto-ported test: TestCopyingSingleton::test_copy (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set(['hello'])
dup = self_set.copy()
dup_list = sorted(dup, key=repr)
set_list = sorted(self_set, key=repr)

assert len(dup_list) == len(set_list)
for i in range(len(dup_list)):

    assert dup_list[i] is set_list[i]
print("TestCopyingSingleton::test_copy: ok")
"###);
    assert_output(&out, r###"TestCopyingSingleton::test_copy: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_copying_singleton__test_deep_copy.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_copying_singleton__test_deep_copy() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_copying_singleton__test_deep_copy"
# subject = "cpython.test_set.TestCopyingSingleton.test_deep_copy"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestCopyingSingleton::test_deep_copy
"""Auto-ported test: TestCopyingSingleton::test_deep_copy (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set(['hello'])
dup = copy.deepcopy(self_set)
dup_list = sorted(dup, key=repr)
set_list = sorted(self_set, key=repr)

assert len(dup_list) == len(set_list)
for i in range(len(dup_list)):

    assert dup_list[i] == set_list[i]
print("TestCopyingSingleton::test_deep_copy: ok")
"###);
    assert_output(&out, r###"TestCopyingSingleton::test_deep_copy: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_copying_triple__test_copy.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_copying_triple__test_copy() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_copying_triple__test_copy"
# subject = "cpython.test_set.TestCopyingTriple.test_copy"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestCopyingTriple::test_copy
"""Auto-ported test: TestCopyingTriple::test_copy (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set(['zero', 0, None])
dup = self_set.copy()
dup_list = sorted(dup, key=repr)
set_list = sorted(self_set, key=repr)

assert len(dup_list) == len(set_list)
for i in range(len(dup_list)):

    assert dup_list[i] is set_list[i]
print("TestCopyingTriple::test_copy: ok")
"###);
    assert_output(&out, r###"TestCopyingTriple::test_copy: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_copying_triple__test_deep_copy.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_copying_triple__test_deep_copy() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_copying_triple__test_deep_copy"
# subject = "cpython.test_set.TestCopyingTriple.test_deep_copy"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestCopyingTriple::test_deep_copy
"""Auto-ported test: TestCopyingTriple::test_deep_copy (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set(['zero', 0, None])
dup = copy.deepcopy(self_set)
dup_list = sorted(dup, key=repr)
set_list = sorted(self_set, key=repr)

assert len(dup_list) == len(set_list)
for i in range(len(dup_list)):

    assert dup_list[i] == set_list[i]
print("TestCopyingTriple::test_deep_copy: ok")
"###);
    assert_output(&out, r###"TestCopyingTriple::test_deep_copy: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_copying_tuple__test_copy.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_copying_tuple__test_copy() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_copying_tuple__test_copy"
# subject = "cpython.test_set.TestCopyingTuple.test_copy"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestCopyingTuple::test_copy
"""Auto-ported test: TestCopyingTuple::test_copy (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set([(1, 2)])
dup = self_set.copy()
dup_list = sorted(dup, key=repr)
set_list = sorted(self_set, key=repr)

assert len(dup_list) == len(set_list)
for i in range(len(dup_list)):

    assert dup_list[i] is set_list[i]
print("TestCopyingTuple::test_copy: ok")
"###);
    assert_output(&out, r###"TestCopyingTuple::test_copy: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_copying_tuple__test_deep_copy.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_copying_tuple__test_deep_copy() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_copying_tuple__test_deep_copy"
# subject = "cpython.test_set.TestCopyingTuple.test_deep_copy"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestCopyingTuple::test_deep_copy
"""Auto-ported test: TestCopyingTuple::test_deep_copy (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set([(1, 2)])
dup = copy.deepcopy(self_set)
dup_list = sorted(dup, key=repr)
set_list = sorted(self_set, key=repr)

assert len(dup_list) == len(set_list)
for i in range(len(dup_list)):

    assert dup_list[i] == set_list[i]
print("TestCopyingTuple::test_deep_copy: ok")
"###);
    assert_output(&out, r###"TestCopyingTuple::test_deep_copy: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_exception_propagation__test_instances_without_exception.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_exception_propagation__test_instances_without_exception() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_exception_propagation__test_instances_without_exception"
# subject = "cpython.test_set.TestExceptionPropagation.test_instancesWithoutException"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestExceptionPropagation::test_instancesWithoutException
"""Auto-ported test: TestExceptionPropagation::test_instancesWithoutException (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
set([1, 2, 3])
set((1, 2, 3))
set({'one': 1, 'two': 2, 'three': 3})
set(range(3))
set('abc')
set(gooditer())
print("TestExceptionPropagation::test_instancesWithoutException: ok")
"###);
    assert_output(&out, r###"TestExceptionPropagation::test_instancesWithoutException: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_frozen_set__test_badcmp.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_frozen_set__test_badcmp() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_frozen_set__test_badcmp"
# subject = "cpython.test_set.TestFrozenSet.test_badcmp"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestFrozenSet::test_badcmp
"""Auto-ported test: TestFrozenSet::test_badcmp (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = frozenset
basetype = frozenset
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)
s = thetype([BadCmp()])

try:
    thetype([BadCmp(), BadCmp()])
    raise AssertionError('expected RuntimeError')
except RuntimeError:
    pass

try:
    s.__contains__(BadCmp())
    raise AssertionError('expected RuntimeError')
except RuntimeError:
    pass
if hasattr(s, 'add'):

    try:
        s.add(BadCmp())
        raise AssertionError('expected RuntimeError')
    except RuntimeError:
        pass

    try:
        s.discard(BadCmp())
        raise AssertionError('expected RuntimeError')
    except RuntimeError:
        pass

    try:
        s.remove(BadCmp())
        raise AssertionError('expected RuntimeError')
    except RuntimeError:
        pass
print("TestFrozenSet::test_badcmp: ok")
"###);
    assert_output(&out, r###"TestFrozenSet::test_badcmp: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_frozen_set__test_contains.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_frozen_set__test_contains() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_frozen_set__test_contains"
# subject = "cpython.test_set.TestFrozenSet.test_contains"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestFrozenSet::test_contains
"""Auto-ported test: TestFrozenSet::test_contains (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = frozenset
basetype = frozenset
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)
for c in self_letters:

    assert (c in self_s) == (c in self_d)

try:
    self_s.__contains__([[]])
    raise AssertionError('expected TypeError')
except TypeError:
    pass
s = thetype([frozenset(self_letters)])

assert thetype(self_letters) in s
print("TestFrozenSet::test_contains: ok")
"###);
    assert_output(&out, r###"TestFrozenSet::test_contains: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_frozen_set__test_equality.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_frozen_set__test_equality() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_frozen_set__test_equality"
# subject = "cpython.test_set.TestFrozenSet.test_equality"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestFrozenSet::test_equality
"""Auto-ported test: TestFrozenSet::test_equality (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = frozenset
basetype = frozenset
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)

assert self_s == set(self_word)

assert self_s == frozenset(self_word)

assert (self_s == self_word) == False

assert self_s != set(self_otherword)

assert self_s != frozenset(self_otherword)

assert (self_s != self_word) == True
print("TestFrozenSet::test_equality: ok")
"###);
    assert_output(&out, r###"TestFrozenSet::test_equality: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_frozen_set__test_frozen_as_dictkey.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_frozen_set__test_frozen_as_dictkey() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_frozen_set__test_frozen_as_dictkey"
# subject = "cpython.test_set.TestFrozenSet.test_frozen_as_dictkey"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestFrozenSet::test_frozen_as_dictkey
"""Auto-ported test: TestFrozenSet::test_frozen_as_dictkey (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = frozenset
basetype = frozenset
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)
seq = list(range(10)) + list('abcdefg') + ['apple']
key1 = thetype(seq)
key2 = thetype(reversed(seq))

assert key1 == key2

assert id(key1) != id(key2)
d = {}
d[key1] = 42

assert d[key2] == 42
print("TestFrozenSet::test_frozen_as_dictkey: ok")
"###);
    assert_output(&out, r###"TestFrozenSet::test_frozen_as_dictkey: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_frozen_set__test_gc.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_frozen_set__test_gc() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_frozen_set__test_gc"
# subject = "cpython.test_set.TestFrozenSet.test_gc"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestFrozenSet::test_gc
"""Auto-ported test: TestFrozenSet::test_gc (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = frozenset
basetype = frozenset
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)

class A:
    pass
s = set((A() for i in range(1000)))
for elem in s:
    elem.cycle = s
    elem.sub = elem
    elem.set = set([elem])
print("TestFrozenSet::test_gc: ok")
"###);
    assert_output(&out, r###"TestFrozenSet::test_gc: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_frozen_set__test_hash.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_frozen_set__test_hash() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_frozen_set__test_hash"
# subject = "cpython.test_set.TestFrozenSet.test_hash"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestFrozenSet::test_hash
"""Auto-ported test: TestFrozenSet::test_hash (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = frozenset
basetype = frozenset
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)

assert hash(thetype('abcdeb')) == hash(thetype('ebecda'))
n = 100
seq = [randrange(n) for i in range(n)]
results = set()
for i in range(200):
    shuffle(seq)
    results.add(hash(thetype(seq)))

assert len(results) == 1
print("TestFrozenSet::test_hash: ok")
"###);
    assert_output(&out, r###"TestFrozenSet::test_hash: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_frozen_set__test_hash_caching.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_frozen_set__test_hash_caching() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_frozen_set__test_hash_caching"
# subject = "cpython.test_set.TestFrozenSet.test_hash_caching"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestFrozenSet::test_hash_caching
"""Auto-ported test: TestFrozenSet::test_hash_caching (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = frozenset
basetype = frozenset
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)
f = thetype('abcdcda')

assert hash(f) == hash(f)
print("TestFrozenSet::test_hash_caching: ok")
"###);
    assert_output(&out, r###"TestFrozenSet::test_hash_caching: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_frozen_set__test_len.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_frozen_set__test_len() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_frozen_set__test_len"
# subject = "cpython.test_set.TestFrozenSet.test_len"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestFrozenSet::test_len
"""Auto-ported test: TestFrozenSet::test_len (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = frozenset
basetype = frozenset
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)

assert len(self_s) == len(self_d)
print("TestFrozenSet::test_len: ok")
"###);
    assert_output(&out, r###"TestFrozenSet::test_len: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_frozen_set__test_pickling.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_frozen_set__test_pickling() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_frozen_set__test_pickling"
# subject = "cpython.test_set.TestFrozenSet.test_pickling"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestFrozenSet::test_pickling
"""Auto-ported test: TestFrozenSet::test_pickling (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = frozenset
basetype = frozenset
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)
for i in range(pickle.HIGHEST_PROTOCOL + 1):
    if type(self_s) not in (set, frozenset):
        self_s.x = ['x']
        self_s.z = ['z']
    p = pickle.dumps(self_s, i)
    dup = pickle.loads(p)

    assert self_s == dup
    if type(self_s) not in (set, frozenset):

        assert self_s.x == dup.x

        assert self_s.z == dup.z

        assert not hasattr(self_s, 'y')
        del self_s.x, self_s.z
print("TestFrozenSet::test_pickling: ok")
"###);
    assert_output(&out, r###"TestFrozenSet::test_pickling: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_frozen_set__test_set_of_frozensets.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_frozen_set__test_set_of_frozensets() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_frozen_set__test_set_of_frozensets"
# subject = "cpython.test_set.TestFrozenSet.test_setOfFrozensets"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestFrozenSet::test_setOfFrozensets
"""Auto-ported test: TestFrozenSet::test_setOfFrozensets (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = frozenset
basetype = frozenset
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)
t = map(frozenset, ['abcdef', 'bcd', 'bdcb', 'fed', 'fedccba'])
s = thetype(t)

assert len(s) == 3
print("TestFrozenSet::test_setOfFrozensets: ok")
"###);
    assert_output(&out, r###"TestFrozenSet::test_setOfFrozensets: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_frozen_set_subclass__test_constructor_identity.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_frozen_set_subclass__test_constructor_identity() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_frozen_set_subclass__test_constructor_identity"
# subject = "cpython.test_set.TestFrozenSetSubclass.test_constructor_identity"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestFrozenSetSubclass::test_constructor_identity
"""Auto-ported test: TestFrozenSetSubclass::test_constructor_identity (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = frozenset
basetype = frozenset
thetype = FrozenSetSubclass
basetype = frozenset
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)
s = thetype(range(3))
t = thetype(s)

assert id(s) != id(t)
print("TestFrozenSetSubclass::test_constructor_identity: ok")
"###);
    assert_output(&out, r###"TestFrozenSetSubclass::test_constructor_identity: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_frozen_set_subclass__test_gc.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_frozen_set_subclass__test_gc() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_frozen_set_subclass__test_gc"
# subject = "cpython.test_set.TestFrozenSetSubclass.test_gc"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestFrozenSetSubclass::test_gc
"""Auto-ported test: TestFrozenSetSubclass::test_gc (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = frozenset
basetype = frozenset
thetype = FrozenSetSubclass
basetype = frozenset
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)

class A:
    pass
s = set((A() for i in range(1000)))
for elem in s:
    elem.cycle = s
    elem.sub = elem
    elem.set = set([elem])
print("TestFrozenSetSubclass::test_gc: ok")
"###);
    assert_output(&out, r###"TestFrozenSetSubclass::test_gc: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_frozen_set_subclass__test_hash_caching.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_frozen_set_subclass__test_hash_caching() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_frozen_set_subclass__test_hash_caching"
# subject = "cpython.test_set.TestFrozenSetSubclass.test_hash_caching"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestFrozenSetSubclass::test_hash_caching
"""Auto-ported test: TestFrozenSetSubclass::test_hash_caching (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = frozenset
basetype = frozenset
thetype = FrozenSetSubclass
basetype = frozenset
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)
f = thetype('abcdcda')

assert hash(f) == hash(f)
print("TestFrozenSetSubclass::test_hash_caching: ok")
"###);
    assert_output(&out, r###"TestFrozenSetSubclass::test_hash_caching: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_frozen_set_subclass__test_singleton_empty_frozenset.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_frozen_set_subclass__test_singleton_empty_frozenset() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_frozen_set_subclass__test_singleton_empty_frozenset"
# subject = "cpython.test_set.TestFrozenSetSubclass.test_singleton_empty_frozenset"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestFrozenSetSubclass::test_singleton_empty_frozenset
"""Auto-ported test: TestFrozenSetSubclass::test_singleton_empty_frozenset (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = frozenset
basetype = frozenset
thetype = FrozenSetSubclass
basetype = frozenset
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)
Frozenset = thetype
f = frozenset()
F = Frozenset()
efs = [Frozenset(), Frozenset([]), Frozenset(()), Frozenset(''), Frozenset(), Frozenset([]), Frozenset(()), Frozenset(''), Frozenset(range(0)), Frozenset(Frozenset()), Frozenset(frozenset()), f, F, Frozenset(f), Frozenset(F)]

assert len(set(map(id, efs))) == len(efs)
print("TestFrozenSetSubclass::test_singleton_empty_frozenset: ok")
"###);
    assert_output(&out, r###"TestFrozenSetSubclass::test_singleton_empty_frozenset: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_identities__test_binops_vs_subsets.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_identities__test_binops_vs_subsets() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_identities__test_binops_vs_subsets"
# subject = "cpython.test_set.TestIdentities.test_binopsVsSubsets"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestIdentities::test_binopsVsSubsets
"""Auto-ported test: TestIdentities::test_binopsVsSubsets (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_a = set('abracadabra')
self_b = set('alacazam')
a, b = (self_a, self_b)

assert a - b < a

assert b - a < b

assert a & b < a

assert a & b < b

assert a | b > a

assert a | b > b

assert a ^ b < a | b
print("TestIdentities::test_binopsVsSubsets: ok")
"###);
    assert_output(&out, r###"TestIdentities::test_binopsVsSubsets: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_identities__test_commutativity.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_identities__test_commutativity() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_identities__test_commutativity"
# subject = "cpython.test_set.TestIdentities.test_commutativity"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestIdentities::test_commutativity
"""Auto-ported test: TestIdentities::test_commutativity (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_a = set('abracadabra')
self_b = set('alacazam')
a, b = (self_a, self_b)

assert a & b == b & a

assert a | b == b | a

assert a ^ b == b ^ a
if a != b:

    assert a - b != b - a
print("TestIdentities::test_commutativity: ok")
"###);
    assert_output(&out, r###"TestIdentities::test_commutativity: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_identities__test_exclusion.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_identities__test_exclusion() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_identities__test_exclusion"
# subject = "cpython.test_set.TestIdentities.test_exclusion"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestIdentities::test_exclusion
"""Auto-ported test: TestIdentities::test_exclusion (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_a = set('abracadabra')
self_b = set('alacazam')
a, b, zero = (self_a, self_b, set())

assert a - b & b == zero

assert b - a & a == zero

assert a & b & (a ^ b) == zero
print("TestIdentities::test_exclusion: ok")
"###);
    assert_output(&out, r###"TestIdentities::test_exclusion: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_identities__test_summations.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_identities__test_summations() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_identities__test_summations"
# subject = "cpython.test_set.TestIdentities.test_summations"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestIdentities::test_summations
"""Auto-ported test: TestIdentities::test_summations (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_a = set('abracadabra')
self_b = set('alacazam')
a, b = (self_a, self_b)

assert a - b | a & b | b - a == a | b

assert a & b | a ^ b == a | b

assert a | b - a == a | b

assert a - b | b == a | b

assert a - b | a & b == a

assert b - a | a & b == b

assert a - b | b - a == a ^ b
print("TestIdentities::test_summations: ok")
"###);
    assert_output(&out, r###"TestIdentities::test_summations: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_mutate__test_add_absent.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_mutate__test_add_absent() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_mutate__test_add_absent"
# subject = "cpython.test_set.TestMutate.test_add_absent"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestMutate::test_add_absent
"""Auto-ported test: TestMutate::test_add_absent (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_values = ['a', 'b', 'c']
self_set = set(self_values)
self_set.add('d')

assert self_set == set('abcd')
print("TestMutate::test_add_absent: ok")
"###);
    assert_output(&out, r###"TestMutate::test_add_absent: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_mutate__test_add_present.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_mutate__test_add_present() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_mutate__test_add_present"
# subject = "cpython.test_set.TestMutate.test_add_present"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestMutate::test_add_present
"""Auto-ported test: TestMutate::test_add_present (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_values = ['a', 'b', 'c']
self_set = set(self_values)
self_set.add('c')

assert self_set == set('abc')
print("TestMutate::test_add_present: ok")
"###);
    assert_output(&out, r###"TestMutate::test_add_present: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_mutate__test_add_until_full.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_mutate__test_add_until_full() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_mutate__test_add_until_full"
# subject = "cpython.test_set.TestMutate.test_add_until_full"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestMutate::test_add_until_full
"""Auto-ported test: TestMutate::test_add_until_full (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_values = ['a', 'b', 'c']
self_set = set(self_values)
tmp = set()
expected_len = 0
for v in self_values:
    tmp.add(v)
    expected_len += 1

    assert len(tmp) == expected_len

assert tmp == self_set
print("TestMutate::test_add_until_full: ok")
"###);
    assert_output(&out, r###"TestMutate::test_add_until_full: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_mutate__test_clear.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_mutate__test_clear() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_mutate__test_clear"
# subject = "cpython.test_set.TestMutate.test_clear"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestMutate::test_clear
"""Auto-ported test: TestMutate::test_clear (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_values = ['a', 'b', 'c']
self_set = set(self_values)
self_set.clear()

assert len(self_set) == 0
print("TestMutate::test_clear: ok")
"###);
    assert_output(&out, r###"TestMutate::test_clear: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_mutate__test_discard_absent.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_mutate__test_discard_absent() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_mutate__test_discard_absent"
# subject = "cpython.test_set.TestMutate.test_discard_absent"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestMutate::test_discard_absent
"""Auto-ported test: TestMutate::test_discard_absent (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_values = ['a', 'b', 'c']
self_set = set(self_values)
self_set.discard('d')

assert self_set == set('abc')
print("TestMutate::test_discard_absent: ok")
"###);
    assert_output(&out, r###"TestMutate::test_discard_absent: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_mutate__test_discard_present.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_mutate__test_discard_present() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_mutate__test_discard_present"
# subject = "cpython.test_set.TestMutate.test_discard_present"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestMutate::test_discard_present
"""Auto-ported test: TestMutate::test_discard_present (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_values = ['a', 'b', 'c']
self_set = set(self_values)
self_set.discard('c')

assert self_set == set('ab')
print("TestMutate::test_discard_present: ok")
"###);
    assert_output(&out, r###"TestMutate::test_discard_present: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_mutate__test_pop.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_mutate__test_pop() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_mutate__test_pop"
# subject = "cpython.test_set.TestMutate.test_pop"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestMutate::test_pop
"""Auto-ported test: TestMutate::test_pop (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_values = ['a', 'b', 'c']
self_set = set(self_values)
popped = {}
while self_set:
    popped[self_set.pop()] = None

assert len(popped) == len(self_values)
for v in self_values:

    assert v in popped
print("TestMutate::test_pop: ok")
"###);
    assert_output(&out, r###"TestMutate::test_pop: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_mutate__test_remove_absent.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_mutate__test_remove_absent() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_mutate__test_remove_absent"
# subject = "cpython.test_set.TestMutate.test_remove_absent"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestMutate::test_remove_absent
"""Auto-ported test: TestMutate::test_remove_absent (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_values = ['a', 'b', 'c']
self_set = set(self_values)
try:
    self_set.remove('d')

    raise AssertionError('Removing missing element should have raised LookupError')
except LookupError:
    pass
print("TestMutate::test_remove_absent: ok")
"###);
    assert_output(&out, r###"TestMutate::test_remove_absent: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_mutate__test_remove_present.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_mutate__test_remove_present() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_mutate__test_remove_present"
# subject = "cpython.test_set.TestMutate.test_remove_present"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestMutate::test_remove_present
"""Auto-ported test: TestMutate::test_remove_present (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_values = ['a', 'b', 'c']
self_set = set(self_values)
self_set.remove('b')

assert self_set == set('ac')
print("TestMutate::test_remove_present: ok")
"###);
    assert_output(&out, r###"TestMutate::test_remove_present: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_mutate__test_remove_until_empty.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_mutate__test_remove_until_empty() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_mutate__test_remove_until_empty"
# subject = "cpython.test_set.TestMutate.test_remove_until_empty"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestMutate::test_remove_until_empty
"""Auto-ported test: TestMutate::test_remove_until_empty (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_values = ['a', 'b', 'c']
self_set = set(self_values)
expected_len = len(self_set)
for v in self_values:
    self_set.remove(v)
    expected_len -= 1

    assert len(self_set) == expected_len
print("TestMutate::test_remove_until_empty: ok")
"###);
    assert_output(&out, r###"TestMutate::test_remove_until_empty: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_mutate__test_update_empty_tuple.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_mutate__test_update_empty_tuple() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_mutate__test_update_empty_tuple"
# subject = "cpython.test_set.TestMutate.test_update_empty_tuple"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestMutate::test_update_empty_tuple
"""Auto-ported test: TestMutate::test_update_empty_tuple (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_values = ['a', 'b', 'c']
self_set = set(self_values)
self_set.update(())

assert self_set == set(self_values)
print("TestMutate::test_update_empty_tuple: ok")
"###);
    assert_output(&out, r###"TestMutate::test_update_empty_tuple: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_mutate__test_update_unit_tuple_non_overlap.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_mutate__test_update_unit_tuple_non_overlap() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_mutate__test_update_unit_tuple_non_overlap"
# subject = "cpython.test_set.TestMutate.test_update_unit_tuple_non_overlap"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestMutate::test_update_unit_tuple_non_overlap
"""Auto-ported test: TestMutate::test_update_unit_tuple_non_overlap (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_values = ['a', 'b', 'c']
self_set = set(self_values)
self_set.update(('a', 'z'))

assert self_set == set(self_values + ['z'])
print("TestMutate::test_update_unit_tuple_non_overlap: ok")
"###);
    assert_output(&out, r###"TestMutate::test_update_unit_tuple_non_overlap: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_mutate__test_update_unit_tuple_overlap.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_mutate__test_update_unit_tuple_overlap() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_mutate__test_update_unit_tuple_overlap"
# subject = "cpython.test_set.TestMutate.test_update_unit_tuple_overlap"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestMutate::test_update_unit_tuple_overlap
"""Auto-ported test: TestMutate::test_update_unit_tuple_overlap (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_values = ['a', 'b', 'c']
self_set = set(self_values)
self_set.update(('a',))

assert self_set == set(self_values)
print("TestMutate::test_update_unit_tuple_overlap: ok")
"###);
    assert_output(&out, r###"TestMutate::test_update_unit_tuple_overlap: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_only_sets_dict__test_difference_update.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_only_sets_dict__test_difference_update() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_only_sets_dict__test_difference_update"
# subject = "cpython.test_set.TestOnlySetsDict.test_difference_update"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestOnlySetsDict::test_difference_update
"""Auto-ported test: TestOnlySetsDict::test_difference_update (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((1, 2, 3))
self_other = {1: 2, 3: 4}
self_otherIsIterable = True
if self_otherIsIterable:
    self_set.difference_update(self_other)
else:

    try:
        self_set.difference_update(self_other)
        raise AssertionError('expected TypeError')
    except TypeError:
        pass
print("TestOnlySetsDict::test_difference_update: ok")
"###);
    assert_output(&out, r###"TestOnlySetsDict::test_difference_update: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_only_sets_dict__test_eq_ne.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_only_sets_dict__test_eq_ne() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_only_sets_dict__test_eq_ne"
# subject = "cpython.test_set.TestOnlySetsDict.test_eq_ne"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestOnlySetsDict::test_eq_ne
"""Auto-ported test: TestOnlySetsDict::test_eq_ne (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((1, 2, 3))
self_other = {1: 2, 3: 4}
self_otherIsIterable = True

assert (self_other == self_set) == False

assert (self_set == self_other) == False

assert (self_other != self_set) == True

assert (self_set != self_other) == True
print("TestOnlySetsDict::test_eq_ne: ok")
"###);
    assert_output(&out, r###"TestOnlySetsDict::test_eq_ne: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_only_sets_dict__test_intersection_update.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_only_sets_dict__test_intersection_update() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_only_sets_dict__test_intersection_update"
# subject = "cpython.test_set.TestOnlySetsDict.test_intersection_update"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestOnlySetsDict::test_intersection_update
"""Auto-ported test: TestOnlySetsDict::test_intersection_update (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((1, 2, 3))
self_other = {1: 2, 3: 4}
self_otherIsIterable = True
if self_otherIsIterable:
    self_set.intersection_update(self_other)
else:

    try:
        self_set.intersection_update(self_other)
        raise AssertionError('expected TypeError')
    except TypeError:
        pass
print("TestOnlySetsDict::test_intersection_update: ok")
"###);
    assert_output(&out, r###"TestOnlySetsDict::test_intersection_update: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_only_sets_dict__test_sym_difference_update.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_only_sets_dict__test_sym_difference_update() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_only_sets_dict__test_sym_difference_update"
# subject = "cpython.test_set.TestOnlySetsDict.test_sym_difference_update"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestOnlySetsDict::test_sym_difference_update
"""Auto-ported test: TestOnlySetsDict::test_sym_difference_update (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((1, 2, 3))
self_other = {1: 2, 3: 4}
self_otherIsIterable = True
if self_otherIsIterable:
    self_set.symmetric_difference_update(self_other)
else:

    try:
        self_set.symmetric_difference_update(self_other)
        raise AssertionError('expected TypeError')
    except TypeError:
        pass
print("TestOnlySetsDict::test_sym_difference_update: ok")
"###);
    assert_output(&out, r###"TestOnlySetsDict::test_sym_difference_update: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_only_sets_dict__test_update.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_only_sets_dict__test_update() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_only_sets_dict__test_update"
# subject = "cpython.test_set.TestOnlySetsDict.test_update"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestOnlySetsDict::test_update
"""Auto-ported test: TestOnlySetsDict::test_update (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((1, 2, 3))
self_other = {1: 2, 3: 4}
self_otherIsIterable = True
if self_otherIsIterable:
    self_set.update(self_other)
else:

    try:
        self_set.update(self_other)
        raise AssertionError('expected TypeError')
    except TypeError:
        pass
print("TestOnlySetsDict::test_update: ok")
"###);
    assert_output(&out, r###"TestOnlySetsDict::test_update: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_only_sets_generator__test_difference.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_only_sets_generator__test_difference() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_only_sets_generator__test_difference"
# subject = "cpython.test_set.TestOnlySetsGenerator.test_difference"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestOnlySetsGenerator::test_difference
"""Auto-ported test: TestOnlySetsGenerator::test_difference (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
def gen():
    for i in range(0, 10, 2):
        yield i
self_set = set((1, 2, 3))
self_other = gen()
self_otherIsIterable = True

try:
    (lambda: self_set - self_other)()
    raise AssertionError('expected TypeError')
except TypeError:
    pass

try:
    (lambda: self_other - self_set)()
    raise AssertionError('expected TypeError')
except TypeError:
    pass
if self_otherIsIterable:
    self_set.difference(self_other)
else:

    try:
        self_set.difference(self_other)
        raise AssertionError('expected TypeError')
    except TypeError:
        pass
print("TestOnlySetsGenerator::test_difference: ok")
"###);
    assert_output(&out, r###"TestOnlySetsGenerator::test_difference: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_only_sets_generator__test_difference_update.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_only_sets_generator__test_difference_update() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_only_sets_generator__test_difference_update"
# subject = "cpython.test_set.TestOnlySetsGenerator.test_difference_update"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestOnlySetsGenerator::test_difference_update
"""Auto-ported test: TestOnlySetsGenerator::test_difference_update (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
def gen():
    for i in range(0, 10, 2):
        yield i
self_set = set((1, 2, 3))
self_other = gen()
self_otherIsIterable = True
if self_otherIsIterable:
    self_set.difference_update(self_other)
else:

    try:
        self_set.difference_update(self_other)
        raise AssertionError('expected TypeError')
    except TypeError:
        pass
print("TestOnlySetsGenerator::test_difference_update: ok")
"###);
    assert_output(&out, r###"TestOnlySetsGenerator::test_difference_update: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_only_sets_generator__test_eq_ne.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_only_sets_generator__test_eq_ne() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_only_sets_generator__test_eq_ne"
# subject = "cpython.test_set.TestOnlySetsGenerator.test_eq_ne"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestOnlySetsGenerator::test_eq_ne
"""Auto-ported test: TestOnlySetsGenerator::test_eq_ne (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
def gen():
    for i in range(0, 10, 2):
        yield i
self_set = set((1, 2, 3))
self_other = gen()
self_otherIsIterable = True

assert (self_other == self_set) == False

assert (self_set == self_other) == False

assert (self_other != self_set) == True

assert (self_set != self_other) == True
print("TestOnlySetsGenerator::test_eq_ne: ok")
"###);
    assert_output(&out, r###"TestOnlySetsGenerator::test_eq_ne: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_only_sets_generator__test_intersection_update.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_only_sets_generator__test_intersection_update() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_only_sets_generator__test_intersection_update"
# subject = "cpython.test_set.TestOnlySetsGenerator.test_intersection_update"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestOnlySetsGenerator::test_intersection_update
"""Auto-ported test: TestOnlySetsGenerator::test_intersection_update (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
def gen():
    for i in range(0, 10, 2):
        yield i
self_set = set((1, 2, 3))
self_other = gen()
self_otherIsIterable = True
if self_otherIsIterable:
    self_set.intersection_update(self_other)
else:

    try:
        self_set.intersection_update(self_other)
        raise AssertionError('expected TypeError')
    except TypeError:
        pass
print("TestOnlySetsGenerator::test_intersection_update: ok")
"###);
    assert_output(&out, r###"TestOnlySetsGenerator::test_intersection_update: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_only_sets_generator__test_sym_difference_update.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_only_sets_generator__test_sym_difference_update() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_only_sets_generator__test_sym_difference_update"
# subject = "cpython.test_set.TestOnlySetsGenerator.test_sym_difference_update"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestOnlySetsGenerator::test_sym_difference_update
"""Auto-ported test: TestOnlySetsGenerator::test_sym_difference_update (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
def gen():
    for i in range(0, 10, 2):
        yield i
self_set = set((1, 2, 3))
self_other = gen()
self_otherIsIterable = True
if self_otherIsIterable:
    self_set.symmetric_difference_update(self_other)
else:

    try:
        self_set.symmetric_difference_update(self_other)
        raise AssertionError('expected TypeError')
    except TypeError:
        pass
print("TestOnlySetsGenerator::test_sym_difference_update: ok")
"###);
    assert_output(&out, r###"TestOnlySetsGenerator::test_sym_difference_update: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_only_sets_generator__test_update.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_only_sets_generator__test_update() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_only_sets_generator__test_update"
# subject = "cpython.test_set.TestOnlySetsGenerator.test_update"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestOnlySetsGenerator::test_update
"""Auto-ported test: TestOnlySetsGenerator::test_update (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
def gen():
    for i in range(0, 10, 2):
        yield i
self_set = set((1, 2, 3))
self_other = gen()
self_otherIsIterable = True
if self_otherIsIterable:
    self_set.update(self_other)
else:

    try:
        self_set.update(self_other)
        raise AssertionError('expected TypeError')
    except TypeError:
        pass
print("TestOnlySetsGenerator::test_update: ok")
"###);
    assert_output(&out, r###"TestOnlySetsGenerator::test_update: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_only_sets_numeric__test_eq_ne.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_only_sets_numeric__test_eq_ne() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_only_sets_numeric__test_eq_ne"
# subject = "cpython.test_set.TestOnlySetsNumeric.test_eq_ne"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestOnlySetsNumeric::test_eq_ne
"""Auto-ported test: TestOnlySetsNumeric::test_eq_ne (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((1, 2, 3))
self_other = 19
self_otherIsIterable = False

assert (self_other == self_set) == False

assert (self_set == self_other) == False

assert (self_other != self_set) == True

assert (self_set != self_other) == True
print("TestOnlySetsNumeric::test_eq_ne: ok")
"###);
    assert_output(&out, r###"TestOnlySetsNumeric::test_eq_ne: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_only_sets_operator__test_difference_update.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_only_sets_operator__test_difference_update() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_only_sets_operator__test_difference_update"
# subject = "cpython.test_set.TestOnlySetsOperator.test_difference_update"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestOnlySetsOperator::test_difference_update
"""Auto-ported test: TestOnlySetsOperator::test_difference_update (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((1, 2, 3))
self_other = operator.add
self_otherIsIterable = False
if self_otherIsIterable:
    self_set.difference_update(self_other)
else:

    try:
        self_set.difference_update(self_other)
        raise AssertionError('expected TypeError')
    except TypeError:
        pass
print("TestOnlySetsOperator::test_difference_update: ok")
"###);
    assert_output(&out, r###"TestOnlySetsOperator::test_difference_update: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_only_sets_operator__test_eq_ne.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_only_sets_operator__test_eq_ne() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_only_sets_operator__test_eq_ne"
# subject = "cpython.test_set.TestOnlySetsOperator.test_eq_ne"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestOnlySetsOperator::test_eq_ne
"""Auto-ported test: TestOnlySetsOperator::test_eq_ne (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((1, 2, 3))
self_other = operator.add
self_otherIsIterable = False

assert (self_other == self_set) == False

assert (self_set == self_other) == False

assert (self_other != self_set) == True

assert (self_set != self_other) == True
print("TestOnlySetsOperator::test_eq_ne: ok")
"###);
    assert_output(&out, r###"TestOnlySetsOperator::test_eq_ne: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_only_sets_operator__test_intersection_update.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_only_sets_operator__test_intersection_update() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_only_sets_operator__test_intersection_update"
# subject = "cpython.test_set.TestOnlySetsOperator.test_intersection_update"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestOnlySetsOperator::test_intersection_update
"""Auto-ported test: TestOnlySetsOperator::test_intersection_update (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((1, 2, 3))
self_other = operator.add
self_otherIsIterable = False
if self_otherIsIterable:
    self_set.intersection_update(self_other)
else:

    try:
        self_set.intersection_update(self_other)
        raise AssertionError('expected TypeError')
    except TypeError:
        pass
print("TestOnlySetsOperator::test_intersection_update: ok")
"###);
    assert_output(&out, r###"TestOnlySetsOperator::test_intersection_update: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_only_sets_operator__test_sym_difference_update.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_only_sets_operator__test_sym_difference_update() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_only_sets_operator__test_sym_difference_update"
# subject = "cpython.test_set.TestOnlySetsOperator.test_sym_difference_update"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestOnlySetsOperator::test_sym_difference_update
"""Auto-ported test: TestOnlySetsOperator::test_sym_difference_update (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((1, 2, 3))
self_other = operator.add
self_otherIsIterable = False
if self_otherIsIterable:
    self_set.symmetric_difference_update(self_other)
else:

    try:
        self_set.symmetric_difference_update(self_other)
        raise AssertionError('expected TypeError')
    except TypeError:
        pass
print("TestOnlySetsOperator::test_sym_difference_update: ok")
"###);
    assert_output(&out, r###"TestOnlySetsOperator::test_sym_difference_update: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_only_sets_string__test_difference_update.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_only_sets_string__test_difference_update() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_only_sets_string__test_difference_update"
# subject = "cpython.test_set.TestOnlySetsString.test_difference_update"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestOnlySetsString::test_difference_update
"""Auto-ported test: TestOnlySetsString::test_difference_update (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((1, 2, 3))
self_other = 'abc'
self_otherIsIterable = True
if self_otherIsIterable:
    self_set.difference_update(self_other)
else:

    try:
        self_set.difference_update(self_other)
        raise AssertionError('expected TypeError')
    except TypeError:
        pass
print("TestOnlySetsString::test_difference_update: ok")
"###);
    assert_output(&out, r###"TestOnlySetsString::test_difference_update: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_only_sets_string__test_eq_ne.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_only_sets_string__test_eq_ne() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_only_sets_string__test_eq_ne"
# subject = "cpython.test_set.TestOnlySetsString.test_eq_ne"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestOnlySetsString::test_eq_ne
"""Auto-ported test: TestOnlySetsString::test_eq_ne (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((1, 2, 3))
self_other = 'abc'
self_otherIsIterable = True

assert (self_other == self_set) == False

assert (self_set == self_other) == False

assert (self_other != self_set) == True

assert (self_set != self_other) == True
print("TestOnlySetsString::test_eq_ne: ok")
"###);
    assert_output(&out, r###"TestOnlySetsString::test_eq_ne: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_only_sets_string__test_intersection_update.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_only_sets_string__test_intersection_update() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_only_sets_string__test_intersection_update"
# subject = "cpython.test_set.TestOnlySetsString.test_intersection_update"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestOnlySetsString::test_intersection_update
"""Auto-ported test: TestOnlySetsString::test_intersection_update (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((1, 2, 3))
self_other = 'abc'
self_otherIsIterable = True
if self_otherIsIterable:
    self_set.intersection_update(self_other)
else:

    try:
        self_set.intersection_update(self_other)
        raise AssertionError('expected TypeError')
    except TypeError:
        pass
print("TestOnlySetsString::test_intersection_update: ok")
"###);
    assert_output(&out, r###"TestOnlySetsString::test_intersection_update: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_only_sets_string__test_sym_difference_update.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_only_sets_string__test_sym_difference_update() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_only_sets_string__test_sym_difference_update"
# subject = "cpython.test_set.TestOnlySetsString.test_sym_difference_update"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestOnlySetsString::test_sym_difference_update
"""Auto-ported test: TestOnlySetsString::test_sym_difference_update (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((1, 2, 3))
self_other = 'abc'
self_otherIsIterable = True
if self_otherIsIterable:
    self_set.symmetric_difference_update(self_other)
else:

    try:
        self_set.symmetric_difference_update(self_other)
        raise AssertionError('expected TypeError')
    except TypeError:
        pass
print("TestOnlySetsString::test_sym_difference_update: ok")
"###);
    assert_output(&out, r###"TestOnlySetsString::test_sym_difference_update: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_only_sets_string__test_update.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_only_sets_string__test_update() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_only_sets_string__test_update"
# subject = "cpython.test_set.TestOnlySetsString.test_update"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestOnlySetsString::test_update
"""Auto-ported test: TestOnlySetsString::test_update (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((1, 2, 3))
self_other = 'abc'
self_otherIsIterable = True
if self_otherIsIterable:
    self_set.update(self_other)
else:

    try:
        self_set.update(self_other)
        raise AssertionError('expected TypeError')
    except TypeError:
        pass
print("TestOnlySetsString::test_update: ok")
"###);
    assert_output(&out, r###"TestOnlySetsString::test_update: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_only_sets_tuple__test_difference_update.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_only_sets_tuple__test_difference_update() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_only_sets_tuple__test_difference_update"
# subject = "cpython.test_set.TestOnlySetsTuple.test_difference_update"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestOnlySetsTuple::test_difference_update
"""Auto-ported test: TestOnlySetsTuple::test_difference_update (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((1, 2, 3))
self_other = (2, 4, 6)
self_otherIsIterable = True
if self_otherIsIterable:
    self_set.difference_update(self_other)
else:

    try:
        self_set.difference_update(self_other)
        raise AssertionError('expected TypeError')
    except TypeError:
        pass
print("TestOnlySetsTuple::test_difference_update: ok")
"###);
    assert_output(&out, r###"TestOnlySetsTuple::test_difference_update: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_only_sets_tuple__test_eq_ne.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_only_sets_tuple__test_eq_ne() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_only_sets_tuple__test_eq_ne"
# subject = "cpython.test_set.TestOnlySetsTuple.test_eq_ne"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestOnlySetsTuple::test_eq_ne
"""Auto-ported test: TestOnlySetsTuple::test_eq_ne (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((1, 2, 3))
self_other = (2, 4, 6)
self_otherIsIterable = True

assert (self_other == self_set) == False

assert (self_set == self_other) == False

assert (self_other != self_set) == True

assert (self_set != self_other) == True
print("TestOnlySetsTuple::test_eq_ne: ok")
"###);
    assert_output(&out, r###"TestOnlySetsTuple::test_eq_ne: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_only_sets_tuple__test_intersection_update.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_only_sets_tuple__test_intersection_update() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_only_sets_tuple__test_intersection_update"
# subject = "cpython.test_set.TestOnlySetsTuple.test_intersection_update"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestOnlySetsTuple::test_intersection_update
"""Auto-ported test: TestOnlySetsTuple::test_intersection_update (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((1, 2, 3))
self_other = (2, 4, 6)
self_otherIsIterable = True
if self_otherIsIterable:
    self_set.intersection_update(self_other)
else:

    try:
        self_set.intersection_update(self_other)
        raise AssertionError('expected TypeError')
    except TypeError:
        pass
print("TestOnlySetsTuple::test_intersection_update: ok")
"###);
    assert_output(&out, r###"TestOnlySetsTuple::test_intersection_update: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_only_sets_tuple__test_sym_difference_update.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_only_sets_tuple__test_sym_difference_update() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_only_sets_tuple__test_sym_difference_update"
# subject = "cpython.test_set.TestOnlySetsTuple.test_sym_difference_update"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestOnlySetsTuple::test_sym_difference_update
"""Auto-ported test: TestOnlySetsTuple::test_sym_difference_update (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((1, 2, 3))
self_other = (2, 4, 6)
self_otherIsIterable = True
if self_otherIsIterable:
    self_set.symmetric_difference_update(self_other)
else:

    try:
        self_set.symmetric_difference_update(self_other)
        raise AssertionError('expected TypeError')
    except TypeError:
        pass
print("TestOnlySetsTuple::test_sym_difference_update: ok")
"###);
    assert_output(&out, r###"TestOnlySetsTuple::test_sym_difference_update: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_only_sets_tuple__test_update.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_only_sets_tuple__test_update() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_only_sets_tuple__test_update"
# subject = "cpython.test_set.TestOnlySetsTuple.test_update"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestOnlySetsTuple::test_update
"""Auto-ported test: TestOnlySetsTuple::test_update (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((1, 2, 3))
self_other = (2, 4, 6)
self_otherIsIterable = True
if self_otherIsIterable:
    self_set.update(self_other)
else:

    try:
        self_set.update(self_other)
        raise AssertionError('expected TypeError')
    except TypeError:
        pass
print("TestOnlySetsTuple::test_update: ok")
"###);
    assert_output(&out, r###"TestOnlySetsTuple::test_update: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_set__test_clear.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_set__test_clear() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_set__test_clear"
# subject = "cpython.test_set.TestSet.test_clear"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestSet::test_clear
"""Auto-ported test: TestSet::test_clear (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = set
basetype = set
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)
self_s.clear()

assert self_s == set()

assert len(self_s) == 0
print("TestSet::test_clear: ok")
"###);
    assert_output(&out, r###"TestSet::test_clear: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_set__test_constructor_identity.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_set__test_constructor_identity() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_set__test_constructor_identity"
# subject = "cpython.test_set.TestSet.test_constructor_identity"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestSet::test_constructor_identity
"""Auto-ported test: TestSet::test_constructor_identity (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = set
basetype = set
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)
s = thetype(range(3))
t = thetype(s)

assert id(s) != id(t)
print("TestSet::test_constructor_identity: ok")
"###);
    assert_output(&out, r###"TestSet::test_constructor_identity: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_set__test_contains.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_set__test_contains() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_set__test_contains"
# subject = "cpython.test_set.TestSet.test_contains"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestSet::test_contains
"""Auto-ported test: TestSet::test_contains (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = set
basetype = set
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)
for c in self_letters:

    assert (c in self_s) == (c in self_d)

try:
    self_s.__contains__([[]])
    raise AssertionError('expected TypeError')
except TypeError:
    pass
s = thetype([frozenset(self_letters)])

assert thetype(self_letters) in s
print("TestSet::test_contains: ok")
"###);
    assert_output(&out, r###"TestSet::test_contains: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_set__test_copy.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_set__test_copy() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_set__test_copy"
# subject = "cpython.test_set.TestSet.test_copy"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestSet::test_copy
"""Auto-ported test: TestSet::test_copy (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = set
basetype = set
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)
dup = self_s.copy()

assert self_s == dup

assert id(self_s) != id(dup)

assert type(dup) == basetype
print("TestSet::test_copy: ok")
"###);
    assert_output(&out, r###"TestSet::test_copy: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_set__test_equality.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_set__test_equality() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_set__test_equality"
# subject = "cpython.test_set.TestSet.test_equality"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestSet::test_equality
"""Auto-ported test: TestSet::test_equality (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = set
basetype = set
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)

assert self_s == set(self_word)

assert self_s == frozenset(self_word)

assert (self_s == self_word) == False

assert self_s != set(self_otherword)

assert self_s != frozenset(self_otherword)

assert (self_s != self_word) == True
print("TestSet::test_equality: ok")
"###);
    assert_output(&out, r###"TestSet::test_equality: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_set__test_gc.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_set__test_gc() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_set__test_gc"
# subject = "cpython.test_set.TestSet.test_gc"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestSet::test_gc
"""Auto-ported test: TestSet::test_gc (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = set
basetype = set
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)

class A:
    pass
s = set((A() for i in range(1000)))
for elem in s:
    elem.cycle = s
    elem.sub = elem
    elem.set = set([elem])
print("TestSet::test_gc: ok")
"###);
    assert_output(&out, r###"TestSet::test_gc: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_set__test_iand.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_set__test_iand() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_set__test_iand"
# subject = "cpython.test_set.TestSet.test_iand"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestSet::test_iand
"""Auto-ported test: TestSet::test_iand (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = set
basetype = set
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)
self_s &= set(self_otherword)
for c in self_word + self_otherword:
    if c in self_otherword and c in self_word:

        assert c in self_s
    else:

        assert c not in self_s
print("TestSet::test_iand: ok")
"###);
    assert_output(&out, r###"TestSet::test_iand: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_set__test_ior.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_set__test_ior() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_set__test_ior"
# subject = "cpython.test_set.TestSet.test_ior"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestSet::test_ior
"""Auto-ported test: TestSet::test_ior (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = set
basetype = set
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)
self_s |= set(self_otherword)
for c in self_word + self_otherword:

    assert c in self_s
print("TestSet::test_ior: ok")
"###);
    assert_output(&out, r###"TestSet::test_ior: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_set__test_isub.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_set__test_isub() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_set__test_isub"
# subject = "cpython.test_set.TestSet.test_isub"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestSet::test_isub
"""Auto-ported test: TestSet::test_isub (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = set
basetype = set
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)
self_s -= set(self_otherword)
for c in self_word + self_otherword:
    if c in self_word and c not in self_otherword:

        assert c in self_s
    else:

        assert c not in self_s
print("TestSet::test_isub: ok")
"###);
    assert_output(&out, r###"TestSet::test_isub: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_set__test_len.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_set__test_len() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_set__test_len"
# subject = "cpython.test_set.TestSet.test_len"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestSet::test_len
"""Auto-ported test: TestSet::test_len (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = set
basetype = set
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)

assert len(self_s) == len(self_d)
print("TestSet::test_len: ok")
"###);
    assert_output(&out, r###"TestSet::test_len: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_set__test_pickling.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_set__test_pickling() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_set__test_pickling"
# subject = "cpython.test_set.TestSet.test_pickling"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestSet::test_pickling
"""Auto-ported test: TestSet::test_pickling (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = set
basetype = set
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)
for i in range(pickle.HIGHEST_PROTOCOL + 1):
    if type(self_s) not in (set, frozenset):
        self_s.x = ['x']
        self_s.z = ['z']
    p = pickle.dumps(self_s, i)
    dup = pickle.loads(p)

    assert self_s == dup
    if type(self_s) not in (set, frozenset):

        assert self_s.x == dup.x

        assert self_s.z == dup.z

        assert not hasattr(self_s, 'y')
        del self_s.x, self_s.z
print("TestSet::test_pickling: ok")
"###);
    assert_output(&out, r###"TestSet::test_pickling: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_set__test_pop.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_set__test_pop() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_set__test_pop"
# subject = "cpython.test_set.TestSet.test_pop"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestSet::test_pop
"""Auto-ported test: TestSet::test_pop (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = set
basetype = set
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)
for i in range(len(self_s)):
    elem = self_s.pop()

    assert elem not in self_s

try:
    self_s.pop()
    raise AssertionError('expected KeyError')
except KeyError:
    pass
print("TestSet::test_pop: ok")
"###);
    assert_output(&out, r###"TestSet::test_pop: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_set__test_set_literal.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_set__test_set_literal() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_set__test_set_literal"
# subject = "cpython.test_set.TestSet.test_set_literal"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestSet::test_set_literal
"""Auto-ported test: TestSet::test_set_literal (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = set
basetype = set
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)
s = set([1, 2, 3])
t = {1, 2, 3}

assert s == t
print("TestSet::test_set_literal: ok")
"###);
    assert_output(&out, r###"TestSet::test_set_literal: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_set__test_set_literal_evaluation_order.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_set__test_set_literal_evaluation_order() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_set__test_set_literal_evaluation_order"
# subject = "cpython.test_set.TestSet.test_set_literal_evaluation_order"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestSet::test_set_literal_evaluation_order
"""Auto-ported test: TestSet::test_set_literal_evaluation_order (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = set
basetype = set
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)
events = []

def record(obj):
    events.append(obj)
s = {record(1), record(2), record(3)}

assert events == [1, 2, 3]
print("TestSet::test_set_literal_evaluation_order: ok")
"###);
    assert_output(&out, r###"TestSet::test_set_literal_evaluation_order: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_set__test_set_literal_insertion_order.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_set__test_set_literal_insertion_order() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_set__test_set_literal_insertion_order"
# subject = "cpython.test_set.TestSet.test_set_literal_insertion_order"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestSet::test_set_literal_insertion_order
"""Auto-ported test: TestSet::test_set_literal_insertion_order (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = set
basetype = set
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)
s = {1, 1.0, True}

assert len(s) == 1
stored_value = s.pop()

assert type(stored_value) == int
print("TestSet::test_set_literal_insertion_order: ok")
"###);
    assert_output(&out, r###"TestSet::test_set_literal_insertion_order: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_set__test_set_of_frozensets.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_set__test_set_of_frozensets() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_set__test_set_of_frozensets"
# subject = "cpython.test_set.TestSet.test_setOfFrozensets"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestSet::test_setOfFrozensets
"""Auto-ported test: TestSet::test_setOfFrozensets (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = set
basetype = set
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)
t = map(frozenset, ['abcdef', 'bcd', 'bdcb', 'fed', 'fedccba'])
s = thetype(t)

assert len(s) == 3
print("TestSet::test_setOfFrozensets: ok")
"###);
    assert_output(&out, r###"TestSet::test_setOfFrozensets: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_set__test_sub_and_super.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_set__test_sub_and_super() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_set__test_sub_and_super"
# subject = "cpython.test_set.TestSet.test_sub_and_super"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestSet::test_sub_and_super
"""Auto-ported test: TestSet::test_sub_and_super (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = set
basetype = set
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)
p, q, r = map(thetype, ['ab', 'abcde', 'def'])

assert p < q

assert p <= q

assert q <= q

assert q > p

assert q >= p

assert not q < r

assert not q <= r

assert not q > r

assert not q >= r

assert set('a').issubset('abc')

assert set('abc').issuperset('a')

assert not set('a').issubset('cbs')

assert not set('cbs').issuperset('a')
print("TestSet::test_sub_and_super: ok")
"###);
    assert_output(&out, r###"TestSet::test_sub_and_super: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_set_of_sets__test_constructor.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_set_of_sets__test_constructor() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_set_of_sets__test_constructor"
# subject = "cpython.test_set.TestSetOfSets.test_constructor"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestSetOfSets::test_constructor
"""Auto-ported test: TestSetOfSets::test_constructor (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
inner = frozenset([1])
outer = set([inner])
element = outer.pop()

assert type(element) == frozenset
outer.add(inner)
outer.remove(inner)

assert outer == set()
outer.discard(inner)
print("TestSetOfSets::test_constructor: ok")
"###);
    assert_output(&out, r###"TestSetOfSets::test_constructor: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_set_subclass__test_constructor_identity.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_set_subclass__test_constructor_identity() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_set_subclass__test_constructor_identity"
# subject = "cpython.test_set.TestSetSubclass.test_constructor_identity"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestSetSubclass::test_constructor_identity
"""Auto-ported test: TestSetSubclass::test_constructor_identity (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = set
basetype = set
thetype = SetSubclass
basetype = set
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)
s = thetype(range(3))
t = thetype(s)

assert id(s) != id(t)
print("TestSetSubclass::test_constructor_identity: ok")
"###);
    assert_output(&out, r###"TestSetSubclass::test_constructor_identity: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_set_subclass__test_gc.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_set_subclass__test_gc() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_set_subclass__test_gc"
# subject = "cpython.test_set.TestSetSubclass.test_gc"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestSetSubclass::test_gc
"""Auto-ported test: TestSetSubclass::test_gc (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = set
basetype = set
thetype = SetSubclass
basetype = set
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)

class A:
    pass
s = set((A() for i in range(1000)))
for elem in s:
    elem.cycle = s
    elem.sub = elem
    elem.set = set([elem])
print("TestSetSubclass::test_gc: ok")
"###);
    assert_output(&out, r###"TestSetSubclass::test_gc: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_set_subclass__test_ixor.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_set_subclass__test_ixor() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_set_subclass__test_ixor"
# subject = "cpython.test_set.TestSetSubclass.test_ixor"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestSetSubclass::test_ixor
"""Auto-ported test: TestSetSubclass::test_ixor (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = set
basetype = set
thetype = SetSubclass
basetype = set
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)
self_s ^= set(self_otherword)
for c in self_word + self_otherword:
    if (c in self_word) ^ (c in self_otherword):

        assert c in self_s
    else:

        assert c not in self_s
print("TestSetSubclass::test_ixor: ok")
"###);
    assert_output(&out, r###"TestSetSubclass::test_ixor: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_set_subclass__test_set_literal.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_set_subclass__test_set_literal() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_set_subclass__test_set_literal"
# subject = "cpython.test_set.TestSetSubclass.test_set_literal"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestSetSubclass::test_set_literal
"""Auto-ported test: TestSetSubclass::test_set_literal (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = set
basetype = set
thetype = SetSubclass
basetype = set
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)
s = set([1, 2, 3])
t = {1, 2, 3}

assert s == t
print("TestSetSubclass::test_set_literal: ok")
"###);
    assert_output(&out, r###"TestSetSubclass::test_set_literal: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_set_subclass__test_set_literal_evaluation_order.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_set_subclass__test_set_literal_evaluation_order() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_set_subclass__test_set_literal_evaluation_order"
# subject = "cpython.test_set.TestSetSubclass.test_set_literal_evaluation_order"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestSetSubclass::test_set_literal_evaluation_order
"""Auto-ported test: TestSetSubclass::test_set_literal_evaluation_order (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = set
basetype = set
thetype = SetSubclass
basetype = set
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)
events = []

def record(obj):
    events.append(obj)
s = {record(1), record(2), record(3)}

assert events == [1, 2, 3]
print("TestSetSubclass::test_set_literal_evaluation_order: ok")
"###);
    assert_output(&out, r###"TestSetSubclass::test_set_literal_evaluation_order: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_set_subclass__test_set_literal_insertion_order.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_set_subclass__test_set_literal_insertion_order() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_set_subclass__test_set_literal_insertion_order"
# subject = "cpython.test_set.TestSetSubclass.test_set_literal_insertion_order"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestSetSubclass::test_set_literal_insertion_order
"""Auto-ported test: TestSetSubclass::test_set_literal_insertion_order (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
thetype = set
basetype = set
thetype = SetSubclass
basetype = set
self_word = word = 'simsalabim'
self_otherword = 'madagascar'
self_letters = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'
self_s = thetype(word)
self_d = dict.fromkeys(word)
s = {1, 1.0, True}

assert len(s) == 1
stored_value = s.pop()

assert type(stored_value) == int
print("TestSetSubclass::test_set_literal_insertion_order: ok")
"###);
    assert_output(&out, r###"TestSetSubclass::test_set_literal_insertion_order: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_update_ops__test_difference_method_call.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_update_ops__test_difference_method_call() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_update_ops__test_difference_method_call"
# subject = "cpython.test_set.TestUpdateOps.test_difference_method_call"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestUpdateOps::test_difference_method_call
"""Auto-ported test: TestUpdateOps::test_difference_method_call (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
self_set.difference_update(set([3, 4, 5]))

assert self_set == set([2, 6])
print("TestUpdateOps::test_difference_method_call: ok")
"###);
    assert_output(&out, r###"TestUpdateOps::test_difference_method_call: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_update_ops__test_difference_non_overlap.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_update_ops__test_difference_non_overlap() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_update_ops__test_difference_non_overlap"
# subject = "cpython.test_set.TestUpdateOps.test_difference_non_overlap"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestUpdateOps::test_difference_non_overlap
"""Auto-ported test: TestUpdateOps::test_difference_non_overlap (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
self_set -= set([8])

assert self_set == set([2, 4, 6])
print("TestUpdateOps::test_difference_non_overlap: ok")
"###);
    assert_output(&out, r###"TestUpdateOps::test_difference_non_overlap: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_update_ops__test_difference_overlap.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_update_ops__test_difference_overlap() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_update_ops__test_difference_overlap"
# subject = "cpython.test_set.TestUpdateOps.test_difference_overlap"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestUpdateOps::test_difference_overlap
"""Auto-ported test: TestUpdateOps::test_difference_overlap (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
self_set -= set((3, 4, 5))

assert self_set == set([2, 6])
print("TestUpdateOps::test_difference_overlap: ok")
"###);
    assert_output(&out, r###"TestUpdateOps::test_difference_overlap: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_update_ops__test_difference_subset.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_update_ops__test_difference_subset() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_update_ops__test_difference_subset"
# subject = "cpython.test_set.TestUpdateOps.test_difference_subset"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestUpdateOps::test_difference_subset
"""Auto-ported test: TestUpdateOps::test_difference_subset (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
self_set -= set((2, 4))

assert self_set == set([6])
print("TestUpdateOps::test_difference_subset: ok")
"###);
    assert_output(&out, r###"TestUpdateOps::test_difference_subset: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_update_ops__test_difference_superset.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_update_ops__test_difference_superset() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_update_ops__test_difference_superset"
# subject = "cpython.test_set.TestUpdateOps.test_difference_superset"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestUpdateOps::test_difference_superset
"""Auto-ported test: TestUpdateOps::test_difference_superset (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
self_set -= set((2, 4, 6, 8))

assert self_set == set([])
print("TestUpdateOps::test_difference_superset: ok")
"###);
    assert_output(&out, r###"TestUpdateOps::test_difference_superset: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_update_ops__test_intersection_method_call.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_update_ops__test_intersection_method_call() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_update_ops__test_intersection_method_call"
# subject = "cpython.test_set.TestUpdateOps.test_intersection_method_call"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestUpdateOps::test_intersection_method_call
"""Auto-ported test: TestUpdateOps::test_intersection_method_call (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
self_set.intersection_update(set([3, 4, 5]))

assert self_set == set([4])
print("TestUpdateOps::test_intersection_method_call: ok")
"###);
    assert_output(&out, r###"TestUpdateOps::test_intersection_method_call: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_update_ops__test_intersection_non_overlap.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_update_ops__test_intersection_non_overlap() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_update_ops__test_intersection_non_overlap"
# subject = "cpython.test_set.TestUpdateOps.test_intersection_non_overlap"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestUpdateOps::test_intersection_non_overlap
"""Auto-ported test: TestUpdateOps::test_intersection_non_overlap (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
self_set &= set([8])

assert self_set == empty_set
print("TestUpdateOps::test_intersection_non_overlap: ok")
"###);
    assert_output(&out, r###"TestUpdateOps::test_intersection_non_overlap: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_update_ops__test_intersection_overlap.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_update_ops__test_intersection_overlap() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_update_ops__test_intersection_overlap"
# subject = "cpython.test_set.TestUpdateOps.test_intersection_overlap"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestUpdateOps::test_intersection_overlap
"""Auto-ported test: TestUpdateOps::test_intersection_overlap (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
self_set &= set([3, 4, 5])

assert self_set == set([4])
print("TestUpdateOps::test_intersection_overlap: ok")
"###);
    assert_output(&out, r###"TestUpdateOps::test_intersection_overlap: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_update_ops__test_intersection_subset.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_update_ops__test_intersection_subset() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_update_ops__test_intersection_subset"
# subject = "cpython.test_set.TestUpdateOps.test_intersection_subset"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestUpdateOps::test_intersection_subset
"""Auto-ported test: TestUpdateOps::test_intersection_subset (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
self_set &= set((2, 4))

assert self_set == set((2, 4))
print("TestUpdateOps::test_intersection_subset: ok")
"###);
    assert_output(&out, r###"TestUpdateOps::test_intersection_subset: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_update_ops__test_intersection_superset.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_update_ops__test_intersection_superset() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_update_ops__test_intersection_superset"
# subject = "cpython.test_set.TestUpdateOps.test_intersection_superset"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestUpdateOps::test_intersection_superset
"""Auto-ported test: TestUpdateOps::test_intersection_superset (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
self_set &= set([2, 4, 6, 8])

assert self_set == set([2, 4, 6])
print("TestUpdateOps::test_intersection_superset: ok")
"###);
    assert_output(&out, r###"TestUpdateOps::test_intersection_superset: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_update_ops__test_sym_difference_method_call.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_update_ops__test_sym_difference_method_call() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_update_ops__test_sym_difference_method_call"
# subject = "cpython.test_set.TestUpdateOps.test_sym_difference_method_call"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestUpdateOps::test_sym_difference_method_call
"""Auto-ported test: TestUpdateOps::test_sym_difference_method_call (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
self_set.symmetric_difference_update(set([3, 4, 5]))

assert self_set == set([2, 3, 5, 6])
print("TestUpdateOps::test_sym_difference_method_call: ok")
"###);
    assert_output(&out, r###"TestUpdateOps::test_sym_difference_method_call: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_update_ops__test_sym_difference_non_overlap.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_update_ops__test_sym_difference_non_overlap() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_update_ops__test_sym_difference_non_overlap"
# subject = "cpython.test_set.TestUpdateOps.test_sym_difference_non_overlap"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestUpdateOps::test_sym_difference_non_overlap
"""Auto-ported test: TestUpdateOps::test_sym_difference_non_overlap (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
self_set ^= set([8])

assert self_set == set([2, 4, 6, 8])
print("TestUpdateOps::test_sym_difference_non_overlap: ok")
"###);
    assert_output(&out, r###"TestUpdateOps::test_sym_difference_non_overlap: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_update_ops__test_sym_difference_overlap.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_update_ops__test_sym_difference_overlap() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_update_ops__test_sym_difference_overlap"
# subject = "cpython.test_set.TestUpdateOps.test_sym_difference_overlap"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestUpdateOps::test_sym_difference_overlap
"""Auto-ported test: TestUpdateOps::test_sym_difference_overlap (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
self_set ^= set((3, 4, 5))

assert self_set == set([2, 3, 5, 6])
print("TestUpdateOps::test_sym_difference_overlap: ok")
"###);
    assert_output(&out, r###"TestUpdateOps::test_sym_difference_overlap: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_update_ops__test_sym_difference_subset.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_update_ops__test_sym_difference_subset() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_update_ops__test_sym_difference_subset"
# subject = "cpython.test_set.TestUpdateOps.test_sym_difference_subset"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestUpdateOps::test_sym_difference_subset
"""Auto-ported test: TestUpdateOps::test_sym_difference_subset (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
self_set ^= set((2, 4))

assert self_set == set([6])
print("TestUpdateOps::test_sym_difference_subset: ok")
"###);
    assert_output(&out, r###"TestUpdateOps::test_sym_difference_subset: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_update_ops__test_sym_difference_superset.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_update_ops__test_sym_difference_superset() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_update_ops__test_sym_difference_superset"
# subject = "cpython.test_set.TestUpdateOps.test_sym_difference_superset"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestUpdateOps::test_sym_difference_superset
"""Auto-ported test: TestUpdateOps::test_sym_difference_superset (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
self_set ^= set((2, 4, 6, 8))

assert self_set == set([8])
print("TestUpdateOps::test_sym_difference_superset: ok")
"###);
    assert_output(&out, r###"TestUpdateOps::test_sym_difference_superset: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_update_ops__test_union_method_call.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_update_ops__test_union_method_call() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_update_ops__test_union_method_call"
# subject = "cpython.test_set.TestUpdateOps.test_union_method_call"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestUpdateOps::test_union_method_call
"""Auto-ported test: TestUpdateOps::test_union_method_call (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
self_set.update(set([3, 4, 5]))

assert self_set == set([2, 3, 4, 5, 6])
print("TestUpdateOps::test_union_method_call: ok")
"###);
    assert_output(&out, r###"TestUpdateOps::test_union_method_call: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_update_ops__test_union_non_overlap.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_update_ops__test_union_non_overlap() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_update_ops__test_union_non_overlap"
# subject = "cpython.test_set.TestUpdateOps.test_union_non_overlap"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestUpdateOps::test_union_non_overlap
"""Auto-ported test: TestUpdateOps::test_union_non_overlap (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
self_set |= set([8])

assert self_set == set([2, 4, 6, 8])
print("TestUpdateOps::test_union_non_overlap: ok")
"###);
    assert_output(&out, r###"TestUpdateOps::test_union_non_overlap: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_update_ops__test_union_overlap.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_update_ops__test_union_overlap() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_update_ops__test_union_overlap"
# subject = "cpython.test_set.TestUpdateOps.test_union_overlap"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestUpdateOps::test_union_overlap
"""Auto-ported test: TestUpdateOps::test_union_overlap (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
self_set |= set([3, 4, 5])

assert self_set == set([2, 3, 4, 5, 6])
print("TestUpdateOps::test_union_overlap: ok")
"###);
    assert_output(&out, r###"TestUpdateOps::test_union_overlap: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_update_ops__test_union_subset.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_update_ops__test_union_subset() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_update_ops__test_union_subset"
# subject = "cpython.test_set.TestUpdateOps.test_union_subset"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestUpdateOps::test_union_subset
"""Auto-ported test: TestUpdateOps::test_union_subset (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
self_set |= set([2])

assert self_set == set((2, 4, 6))
print("TestUpdateOps::test_union_subset: ok")
"###);
    assert_output(&out, r###"TestUpdateOps::test_union_subset: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_update_ops__test_union_superset.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_update_ops__test_union_superset() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_update_ops__test_union_superset"
# subject = "cpython.test_set.TestUpdateOps.test_union_superset"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestUpdateOps::test_union_superset
"""Auto-ported test: TestUpdateOps::test_union_superset (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
self_set = set((2, 4, 6))
self_set |= set([2, 4, 6, 8])

assert self_set == set([2, 4, 6, 8])
print("TestUpdateOps::test_union_superset: ok")
"###);
    assert_output(&out, r###"TestUpdateOps::test_union_superset: ok
"###);
}

/// Ported from `tests/cpython/behavior/builtin-libs/set_methods/test_weird_bugs__test_merge_and_mutate.py`.
#[test]
fn test_gen_behavior_builtin_libs_set_methods_test_weird_bugs__test_merge_and_mutate() {
    let out = jit_capture(r###"# /// script
# requires-python = ">=3.12"
# dependencies = []
#
# [tool.mamba]
# bucket = "builtin-libs"
# lib = "set_methods"
# dimension = "behavior"
# case = "test_weird_bugs__test_merge_and_mutate"
# subject = "cpython.test_set.TestWeirdBugs.test_merge_and_mutate"
# kind = "semantic"
# xfail = ""
# mem_carveout = ""
# source = "Lib/test/test_set.py"
# status = "filled"
# ///
# Auto-ported from CPython 3.12 test_set.py::TestWeirdBugs::test_merge_and_mutate
"""Auto-ported test: TestWeirdBugs::test_merge_and_mutate (CPython 3.12 oracle)."""


import unittest
from test import support
from test.support import warnings_helper
import gc
import weakref
import operator
import copy
import pickle
from random import randrange, shuffle
import warnings
import collections
import collections.abc
import itertools
from itertools import chain


class PassThru(Exception):
    pass

def check_pass_thru():
    raise PassThru
    yield 1

class BadCmp:

    def __hash__(self):
        return 1

    def __eq__(self, other):
        raise RuntimeError

class ReprWrapper:
    """Used to test self-referential repr() calls"""

    def __repr__(self):
        return repr(self.value)

class HashCountingInt(int):
    """int-like object that counts the number of times __hash__ is called"""

    def __init__(self, *args):
        self.hash_count = 0

    def __hash__(self):
        self.hash_count += 1
        return int.__hash__(self)

class SetSubclass(set):
    pass

class FrozenSetSubclass(frozenset):
    pass

class SetSubclassWithSlots(set):
    __slots__ = ('x', 'y', '__dict__')

class FrozenSetSubclassWithSlots(frozenset):
    __slots__ = ('x', 'y', '__dict__')

empty_set = set()

def baditer():
    raise TypeError
    yield True

def gooditer():
    yield True

def R(seqn):
    """Regular generator"""
    for i in seqn:
        yield i

class G:
    """Sequence using __getitem__"""

    def __init__(self, seqn):
        self.seqn = seqn

    def __getitem__(self, i):
        return self.seqn[i]

class I:
    """Sequence using iterator protocol"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class Ig:
    """Sequence using iterator protocol defined with a generator"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        for val in self.seqn:
            yield val

class X:
    """Missing __getitem__ and __iter__"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __next__(self):
        if self.i >= len(self.seqn):
            raise StopIteration
        v = self.seqn[self.i]
        self.i += 1
        return v

class N:
    """Iterator missing __next__()"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

class E:
    """Test propagation of exceptions"""

    def __init__(self, seqn):
        self.seqn = seqn
        self.i = 0

    def __iter__(self):
        return self

    def __next__(self):
        3 // 0

class S:
    """Test immediate stop"""

    def __init__(self, seqn):
        pass

    def __iter__(self):
        return self

    def __next__(self):
        raise StopIteration

def L(seqn):
    """Test multiple tiers of iterators"""
    return chain(map(lambda x: x, R(Ig(G(seqn)))))

class bad_eq:

    def __eq__(self, other):
        if be_bad:
            set2.clear()
            raise ZeroDivisionError
        return self is other

    def __hash__(self):
        return 0

class bad_dict_clear:

    def __eq__(self, other):
        if be_bad:
            dict2.clear()
        return self is other

    def __hash__(self):
        return 0

def powerset(U):
    """Generates all subsets of a set or sequence U."""
    U = iter(U)
    try:
        x = frozenset([next(U)])
        for S in powerset(U):
            yield S
            yield (S | x)
    except StopIteration:
        yield frozenset()

def cube(n):
    """Graph of n-dimensional hypercube."""
    singletons = [frozenset([x]) for x in range(n)]
    return dict([(x, frozenset([x ^ s for s in singletons])) for x in powerset(range(n))])

def linegraph(G):
    """Graph, the vertices of which are edges of G,
    with two vertices being adjacent iff the corresponding
    edges share a vertex."""
    L = {}
    for x in G:
        for y in G[x]:
            nx = [frozenset([x, z]) for z in G[x] if z != y]
            ny = [frozenset([y, z]) for z in G[y] if z != x]
            L[frozenset([x, y])] = frozenset(nx + ny)
    return L

def faces(G):
    """Return a set of faces in G.  Where a face is a set of vertices on that face"""
    f = set()
    for v1, edges in G.items():
        for v2 in edges:
            for v3 in G[v2]:
                if v1 == v3:
                    continue
                if v1 in G[v3]:
                    f.add(frozenset([v1, v2, v3]))
                else:
                    for v4 in G[v3]:
                        if v4 == v2:
                            continue
                        if v1 in G[v4]:
                            f.add(frozenset([v1, v2, v3, v4]))
                        else:
                            for v5 in G[v4]:
                                if v5 == v3 or v5 == v2:
                                    continue
                                if v1 in G[v5]:
                                    f.add(frozenset([v1, v2, v3, v4, v5]))
    return f


# --- test body ---
class X:

    def __hash__(self):
        return hash(0)

    def __eq__(self, o):
        other.clear()
        return False
other = set()
other = {X() for i in range(10)}
s = {0}
s.update(other)
print("TestWeirdBugs::test_merge_and_mutate: ok")
"###);
    assert_output(&out, r###"TestWeirdBugs::test_merge_and_mutate: ok
"###);
}
