bucket = "core"
lib = "iter"

[[case]]
dimension = "behavior"
case = "reversed_tuple_heap_strings_survive_materialize"
subject = "builtins.reversed"
kind = "semantic"
intent = "the tuple remains readable after a helper materializes and releases list(reversed(source))"

[[case]]
dimension = "behavior"
case = "reversed_tuple_heap_strings_observe_materialize_and_churn"
subject = "builtins.reversed"
kind = "semantic"
intent = "the helper observes exact reversed tuple contents and length while list(reversed(source)) is alive, then deterministic churn and source survival after return"

[[case]]
dimension = "behavior"
case = "reversed_heap_tuple_scope_escape"
subject = "builtins.reversed"
kind = "semantic"
intent = "a function-local tuple of runtime-built heap strings remains observable through reversed after the function returns"
