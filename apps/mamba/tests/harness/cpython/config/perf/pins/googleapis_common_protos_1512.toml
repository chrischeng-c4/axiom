# Perf-pin regression gate (#1512).
# Source: tests/cpython/perf_pin.rs (per-pin: tests/cpython/perf/pins/googleapis_common_protos_1512.toml)
issue = 1512
lib = "googleapis_common_protos"
fixture = "tests/cpython/_regression/3rd-libs/googleapis_common_protos/bench/googleapis_common_protos_type_read_hot.py"
floor = 1.0
mem_floor = 1.0
samples = 9
prereq_imports = ["google.rpc.status_pb2"]

# Sentinel-shim quarantine (#1514): this pin measures
# src/runtime/stdlib/googleapis_common_protos_mod.rs, a hand-written module-attribute-identity
# shim (no pyo3/tonic/prost-backed implementation behind it). It is a real,
# enforced perf/RSS regression gate for that shim's own code path, but it must
# NOT be read as third-party-conformance readiness evidence for `googleapis_common_protos`. Any
# readiness rollup must exclude sentinel = true pins from its evidence count.
# Full quarantine list + marking rule: #1119.
sentinel = true
