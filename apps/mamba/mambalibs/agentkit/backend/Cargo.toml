[package]
name = "agentkit-backend"
version = "0.1.0"
edition = "2021"
description = "Backend bridge — Python interop hooks for the mamba binding (Epic 6)"
license = "MIT"
authors = ["cclab"]

[lib]
name = "agentkit_backend"
crate-type = ["rlib"]

[dependencies]
