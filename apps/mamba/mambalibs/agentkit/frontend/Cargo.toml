[package]
name = "agentkit-frontend"
version = "0.1.0"
edition = "2021"
description = "Frontend bridge — jet/wasm visualizer + graph debugger surface (Epic 5)"
license = "MIT"
authors = ["cclab"]

[lib]
name = "agentkit_frontend"
crate-type = ["rlib"]

[dependencies]
