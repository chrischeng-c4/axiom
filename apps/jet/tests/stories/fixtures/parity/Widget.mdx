import * as WidgetStories from './Widget.stories';

<Meta title="Components/Widget" />

# Widget

The Widget fixture combines a decorator, an `argTypes` override, and a
`play()` interaction in one story, wired to this MDX docs page.

<Canvas of={WidgetStories.Interactive} />
<ArgTypes of={WidgetStories} />
<Source of={WidgetStories.Interactive} />
