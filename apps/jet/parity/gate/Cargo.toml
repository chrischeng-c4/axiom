# <HANDWRITE gap="missing-generator:hand-written:jet-parity-gate" tracker="standardize-gap-projects-jet-parity-gate-cargo-toml" reason="codegen for the parity gate crate is not yet wired; see #2144">
[package]
name = "jet-parity-gate"
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "CI gating manifest + gate CLI for jet parity (issue #2144)."

[lib]
name = "jet_parity_gate"
path = "src/lib.rs"

[[bin]]
name = "jet-parity-gate"
path = "src/main.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
toml = "0.8"
clap = { workspace = true }
thiserror = { workspace = true }
chrono = { workspace = true }
walkdir = { workspace = true }

[dev-dependencies]
tempfile = "3"
# </HANDWRITE>
