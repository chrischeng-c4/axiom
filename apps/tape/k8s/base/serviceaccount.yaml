# HANDWRITE-BEGIN gap="missing-generator:deployment:direct-install-statefulset" tracker="#1809" reason="Direct-install serving identity mirrors the shared service-k8s ServiceAccount rendered by Tape's operator."
apiVersion: v1
kind: ServiceAccount
metadata:
  name: tape
  namespace: tape
  labels:
    app: tape
    role: server
# HANDWRITE-END
