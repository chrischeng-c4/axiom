# HANDWRITE-BEGIN gap="missing-generator:deployment:direct-install-statefulset" tracker="#1809" reason="Tape's direct path needs a PVC-backed StatefulSet rather than Lumen's disposable Deployment; HA/membership remains operator-owned."
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: tape
  namespace: tape
  labels:
    app: tape
    role: server
spec:
  # This direct path is deliberately a one-member durable journal. Do not
  # scale it by changing replicas: Tape HA needs the operator to render a
  # complete raft membership and per-pod topology environment.
  serviceName: tape-headless
  replicas: 1
  revisionHistoryLimit: 5
  podManagementPolicy: Parallel
  selector:
    matchLabels:
      app: tape
      role: server
  updateStrategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: tape
        role: server
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "7137"
        prometheus.io/path: /metrics
    spec:
      serviceAccountName: tape
      terminationGracePeriodSeconds: 30
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: tape
          image: tape:0.4.7
          imagePullPolicy: IfNotPresent
          command: ["tape", "serve"]
          ports:
            - name: http
              containerPort: 7137
              protocol: TCP
            - name: raft
              containerPort: 7138
              protocol: TCP
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            # These values keep direct mode explicitly single-member. The
            # operator changes all three as one validated raft topology.
            - name: SHARD_COUNT
              value: "1"
            - name: REPLICAS_PER_SHARD
              value: "1"
            - name: VOTER_COUNT
              value: "1"
            - name: TAPE_PEER_SERVICE
              value: tape-headless
            - name: TAPE_DATA_DIR
              value: /data
            - name: TAPE_BIND
              valueFrom:
                configMapKeyRef:
                  name: tape-config
                  key: TAPE_BIND
            - name: TAPE_GRACE_SECS
              valueFrom:
                configMapKeyRef:
                  name: tape-config
                  key: TAPE_GRACE_SECS
            - name: TAPE_LOG_FORMAT
              valueFrom:
                configMapKeyRef:
                  name: tape-config
                  key: TAPE_LOG_FORMAT
            - name: RUST_LOG
              valueFrom:
                configMapKeyRef:
                  name: tape-config
                  key: RUST_LOG
            - name: TAPE_AUTH
              valueFrom:
                configMapKeyRef:
                  name: tape-config
                  key: TAPE_AUTH
          # Request-only baseline, matching Tape's shared service-k8s
          # defaults. Tune the overlay for its actual node pool.
          resources:
            requests:
              cpu: "1"
              memory: 4Gi
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 2
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 60
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 120
          securityContext:
            runAsNonRoot: true
            runAsUser: 65532
            runAsGroup: 65532
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: data
              mountPath: /data
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
  volumeClaimTemplates:
    - metadata:
        name: data
        labels:
          app: tape
          role: server
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 10Gi
# HANDWRITE-END
