# HANDWRITE-BEGIN gap="missing-generator:logic:c7a9fd07" tracker="#1588" reason="Define an opt-in Prometheus Operator component containing the Tape ServiceMonitor and PrometheusRule; avoid base inclusion so vanilla clusters do not need monitoring CRDs. generator gap: missing-generator:k8s-observability-component (#1588)."
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# Optional: this component requires Prometheus Operator CRDs. It is deliberately
# not included by Tape's vanilla operator artifacts.
resources:
  - servicemonitor.yaml
  - prometheusrule.yaml
# HANDWRITE-END
