# HANDWRITE-BEGIN gap="missing-generator:manifest:c207ddd3" tracker="#1593" reason="Declare the opt-in NetworkPolicy component without automatically composing it into an instance profile. generator gap: missing-generator:kustomize-component-manifest."
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# Opt in from an environment-specific Kustomization only after the cluster's
# CNI enforces NetworkPolicy. Keeping this out of the vanilla operator bundle
# avoids claiming portable enforcement on clusters where NetworkPolicy is
# ignored.
resources:
  - networkpolicy.yaml
# HANDWRITE-END
