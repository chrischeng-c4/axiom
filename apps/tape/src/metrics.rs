// HANDWRITE-BEGIN gap="missing-generator:logic:5989f44e" tracker="pending-tracker" reason="TapeMetrics on libs/service-metrics primitives (Latency = count + sum, render): append / replay / checkpoint_get / checkpoint_put / other request counts + latency ms, plus the track route_layer middleware that maps the matched route pattern to its op family (mirrors relay/keep's metrics::track)."
// TODO: hand-write content for `apps/tape/src/metrics.rs`.
// HANDWRITE-END
