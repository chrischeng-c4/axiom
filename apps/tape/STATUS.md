# Tape status

## Scope

This document describes the support contract in the current repository source.
It does not claim that every listed gate ran in this working session. A release
can contain an older contract.

The rows are the Cloud Pub/Sub feature checklist plus the operational surfaces
tape owns. Use the [README](README.md) for the product workflow and
capabilities. Use the [roadmap](ROADMAP.md) for future outcomes and explicit
non-goals.

## State definitions

| State | Meaning |
|---|---|
| Supported | The current source has a public contract, an implementation, and a named executable gate for the stated scope. |
| Limited | The current source supports the stated scope, but the Limits cell names a material boundary. |
| Not supported | The behavior is not part of the current product contract. The Evidence cell points to a future outcome or a non-goal. |

## Support matrix

| Surface | ID | State | Supported scope | Limits | Evidence |
|---|---|---|---|---|---|
| Single-message publish | `publish-single` | Supported | `POST /topics/{topic}/append` accepts one JSON envelope (`key`, `payload`, optional `timestamp_ms`) and returns the assigned offset after the durable write. Bodies over `--body-limit-bytes` (default 8 MiB) are refused with 413. | One message per request. The only per-message metadata is the single `key` string. | `cargo test -p tape --test http_transport` |
| Durable write path | `durable-write-path` | Supported | Every acknowledged append is fsynced through the WAL before the response. SIGKILL then restart recovers every acked append and never duplicates. ENOSPC inside the WAL answers 507, latches a sticky degraded flag, and keeps reads serving. | Recovery from a full volume needs free space and a re-probe (`TAPE_STORAGE_FULL_REPROBE_SECS`, default 30 s). RSS stays within an order of magnitude of 1.6x the logical payload, no tighter. | `cargo test -p tape --test durable_crash_recovery --test durable_write_path` |
| Storage-full degraded mode | `storage-full-degraded-mode` | Supported | An injected ENOSPC poisons the WAL store itself, not only the metrics latch, and the node reports `tape_storage_degraded` and `tape_storage_full_errors_total`. | The degraded flag is process-local; a restart re-probes from scratch. | `cargo test -p tape --lib` |
| Raft replication | `raft-replication` | Supported | A three-member group elects a leader, replicates appends, forwards writes from followers, and catches a fresh member up by install-snapshot. A follower answers a direct `POST /raft/publish` with 421 and the leader's id before it judges the body. A restarted single member recovers its applied floor and does not re-apply a checkpoint put on cold replay. | Membership is fixed at process start; adding or removing a member needs a restart. See [Live replica membership](ROADMAP.md#live-replica-membership). | `cargo test -p tape --test raft_cluster --test raft_persistence --test raft_publish_misdirect` |
| Leader failover | `leader-failover` | Supported | A `kill -9` of the leader lets the survivors re-elect with no committed append lost, and concurrent ingress across every replica commits without raft timeouts. | Every wait mints its own 60 s budget from the moment it starts, so no two phases share a clock; a host slow enough to exhaust one phase's budget still fails the case. | `cargo test -p tape --test raft_failover` |
| Peer mTLS | `raft-peer-mtls` | Supported | With `TAPE_PEER_MTLS=on` the raft listener requires a client certificate from the configured CA. An untrusted peer is rejected before it reaches the raft router, and raft routes never mount on the public port. | Certificate material is file-based (`TAPE_PEER_TLS_CERT`, `_KEY`, `_CA`); there is no automatic rotation. | `cargo test -p tape --test raft_peer_mtls` |
| Cold seed bootstrap | `cold-seed-bootstrap` | Supported | `--bootstrap-seed-uri` restores a snapshot only into an empty data directory. A dirty volume is left alone, and HA members seed consistently. | The seed source must be reachable at start. There is no incremental restore. | `cargo test -p tape --test bootstrap --test seed_ha_bootstrap` |
| Backup to sink | `backup-to-sink` | Limited | `GET /admin/backup` streams a whole-journal snapshot and requires the admin role when auth is required. `tape backup` ships it to a `file://`, `s3://` (feature `backup`), or `gs://` destination with retention pruning, and the README, handoff runbook, CLI help, and LLM topic all list every accepted scheme. | Only the `file://` sink is exercised end to end here; `s3://` and `gs://` are linked but not proven in this repository. The restore runbook still lives in the deployment handoff page. See [Pub/Sub rebaseline](ROADMAP.md#pubsub-rebaseline). | `cargo test -p tape --features backup --test backup --test backup_destination_docs` |
| Management audit | `management-audit` | Supported | Backup access is audited with the token redacted, and the audit sink stays off the hot data-plane routes. | Only backup is audited. Data-plane writes leave no audit record. | `cargo test -p tape --test audit_contract` |
| Per-topic authorization | `per-topic-authorization` | Limited | With `--auth required`, append needs a write grant on the topic, replay and checkpoint need a read grant, probes stay tokenless, and `--auth off` keeps every route tokenless. | Grants are per topic. There is no subscription-scoped grant, so a reader of one subscription can read every subscription on the topic. See [Subscription ack and competing subscribers](ROADMAP.md#subscription-ack-and-competing-subscribers). | `cargo test -p tape --test service_auth` |
| Flow control and quotas | `flow-control-quotas` | Limited | The body limit is enforced per request, and append is classified as write admission for the shared bounded-admission mechanism. | The default router keeps admission disabled, and there is no per-topic or per-subscription quota. See [Quotas and scale transition](ROADMAP.md#quotas-and-scale-transition). | `cargo test -p tape --test service_admission --test http_transport` |
| Standard operational endpoints | `standard-operational-endpoints` | Supported | `/healthz`, `/readyz` (503 while draining), `/metrics`, `/openapi.json`, and `/docs` answer on the serve port. Responses carry `Server-Timing`, errors use the shared envelope, and the served routes equal the spec inventory. | Probes are always tokenless. | `cargo test -p tape --test http_transport --test spec_route_parity` |
| Delivery metrics | `delivery-metrics` | Limited | `tape_topic_latest_offset` and `tape_subscription_lag` gauges plus per-route request counters and latency sums. | There is no oldest-unacked-age, delivery-attempt, or dead-letter counter because there is no per-message delivery state. See [Subscription ack and competing subscribers](ROADMAP.md#subscription-ack-and-competing-subscribers). | `cargo test -p tape --test http_transport` |
| OTLP tracing | `otlp-tracing` | Supported | `--otlp-endpoint` exports request spans through the shared service tracing layer. | Sampling and resource attributes come from the shared layer, not from tape flags. | `cargo test -p tape --test shared_otlp_tracing` |
| Generated clients | `generated-clients` | Supported | `tape spec gen --lang ts`, `py`, and `rust` emit clients scoped to tape's routes from the committed OpenAPI snapshot. | Clients are not committed; regenerate them when the contract moves. | `cargo test -p tape --test spec_generated_clients --test cli_contract` |
| Kubernetes deployment assets | `k8s-deployment-assets` | Supported | The direct-install base is a durable singleton, the production profile uses projected auth without an HPA, the NetworkPolicy component is opt-in and selects only tape servers, the observability pair renders a ServiceMonitor and PrometheusRule, and the render verbs emit parseable YAML offline. | The direct base is single-member by design. Replication is the operator path. | `cargo test -p tape --test observability_assets --test network_policy_assets --test direct_k8s_assets --test deploy_cli` |
| Kubernetes operator | `k8s-operator` | Limited | A `Tape` custom resource renders a StatefulSet, Services, PodDisruptionBudget, ConfigMap, backup CronJob, and observability pair; status conditions track readiness; auth defaults to required; stale objects are pruned. | `shardCount` is fixed at 1, and a replica-count change restarts members instead of adding a learner live. See [Live replica membership](ROADMAP.md#live-replica-membership). | `cargo test -p tape --features operator --test operator --test operator_render_provision_topics` |
| Declarative provisioning | `declarative-subscription-provisioning` | Limited | `TAPE_PROVISION_TOPICS` and the custom resource's topic list create topics and subscriptions at start. | Provisioning writes the journal directly instead of proposing through Raft, so a provisioned subscription is not replicated the way an appended message is. See [Subscription ack and competing subscribers](ROADMAP.md#subscription-ack-and-competing-subscribers). | `cargo test -p tape --test provision_topics_via_spec` |
| Bounded stability run | `bounded-stability-run` | Supported | Repeated restarts preserve append history and checkpoint progress, and the shared stateful runner drives replay continuity. | The run is bounded to minutes. The multi-hour soak in `apps/tape/scripts/soak.sh` is manual and not a gate. | `cargo test -p tape --test long_running_stability --test rig_stateful_adapter` |
| Local performance ceiling | `local-performance-ceiling` | Supported | Append, replay, and checkpoint stay inside the release-mode budget against tape's own baseline, durable append throughput rises with connection count, and the report never claims a win over another broker. | Single host, release build. There is no cross-hardware publication. | `cargo test --release -p tape --test tape_perf_gate` |
| kind cluster acceptance | `kind-cluster-acceptance` | Limited | The script deploys the direct-install base into a local kind cluster and runs the smoke sequence. | The script is manual and its readback uses the legacy replay and checkpoint routes. See [Pub/Sub rebaseline](ROADMAP.md#pubsub-rebaseline). | `bash apps/tape/scripts/kind-e2e.sh` |
| GKE zonal acceptance | `gke-zonal-acceptance` | Limited | The GCP acceptance path provisions and checks tape on a zonal GKE Standard cluster; release 0.4.11 passed it four times in July 2026. | The run is paid and manual, and it does not prove a scale transition or a regional profile. See [Quotas and scale transition](ROADMAP.md#quotas-and-scale-transition). | `bash acceptance/gcp/scripts/verify-tape.sh` |
| Message attributes | `message-attributes` | Limited | A publish carries one optional string `key`, stored and returned with the event. | There is no attribute map. See [Attributes and filters](ROADMAP.md#attributes-and-filters). | `cargo test -p tape --test http_transport` |
| Ordering key on publish | `ordering-key-on-publish` | Limited | `key` is accepted, persisted, and returned on replay and pull. | Delivery does not order by key, and a key does not pin messages to one puller. See [Ordering keys](ROADMAP.md#ordering-keys). | `cargo test -p tape --test http_transport --test cli_contract` |
| Ordering keys in delivery | `ordering-keys-in-delivery` | Not supported | None. | Pull returns journal order; nothing guarantees per-key order across redelivery or competing pullers. | [Ordering keys](ROADMAP.md#ordering-keys) |
| Subscription lifecycle and pull | `subscription-lifecycle-pull` | Limited | Create, list, show, and delete subscriptions over HTTP and the offline CLI; pull returns a bounded window from the subscription cursor. | A subscription is `{topic, name}` plus a cursor. It has no ack deadline, push config, filter, retry policy, or dead-letter topic. See [Subscription ack and competing subscribers](ROADMAP.md#subscription-ack-and-competing-subscribers). | `cargo test -p tape --test cli_contract --test http_transport` |
| Synchronous pull | `pull-sync` | Limited | `POST .../subscriptions/{name}/pull` with `limit` returns `cursor`, `events`, and `next_offset`. | There is no lease. Every caller sees the same window until an ack moves the cursor, so two pullers duplicate work rather than share it. See [Subscription ack and competing subscribers](ROADMAP.md#subscription-ack-and-competing-subscribers). | `cargo test -p tape --test cli_contract` |
| Acknowledgement | `ack` | Limited | `POST .../subscriptions/{name}/ack` with `offset` advances the cursor cumulatively. | There are no per-message ackIds; acking offset N acks everything before it. See [Subscription ack and competing subscribers](ROADMAP.md#subscription-ack-and-competing-subscribers). | `cargo test -p tape --test cli_contract` |
| Ack deadline | `ack-deadline-default` | Not supported | None. | No message is ever in flight, so nothing expires. | [Subscription ack and competing subscribers](ROADMAP.md#subscription-ack-and-competing-subscribers) |
| Modify ack deadline | `modify-ack-deadline` | Not supported | None. | There is no per-message deadline to extend. | [Subscription ack and competing subscribers](ROADMAP.md#subscription-ack-and-competing-subscribers) |
| Negative acknowledgement | `nack` | Not supported | None. | A caller can only refrain from advancing the cursor. | [Subscription ack and competing subscribers](ROADMAP.md#subscription-ack-and-competing-subscribers) |
| Competing subscribers and redelivery | `competing-subscribers-redelivery` | Not supported | None. | Two pullers on one subscription receive the same messages instead of splitting them. | [Subscription ack and competing subscribers](ROADMAP.md#subscription-ack-and-competing-subscribers) |
| Dead-letter topic | `dead-letter-topic` | Not supported | None. | There is no delivery-attempt count and no forwarding target. | [Subscription ack and competing subscribers](ROADMAP.md#subscription-ack-and-competing-subscribers) |
| Retry policy backoff | `retry-policy-backoff` | Not supported | None. | Redelivery does not exist, so it has no backoff. | [Subscription ack and competing subscribers](ROADMAP.md#subscription-ack-and-competing-subscribers) |
| Push subscription over HTTPS | `push-subscription-https` | Not supported | None. | Tape makes no outbound delivery call. | [Push subscriptions](ROADMAP.md#push-subscriptions) |
| Push authentication token | `push-auth-token` | Not supported | None. | There is no push endpoint to authenticate to. | [Push subscriptions](ROADMAP.md#push-subscriptions) |
| Push retry backoff | `push-retry-backoff` | Not supported | None. | There is no push attempt to retry. | [Push subscriptions](ROADMAP.md#push-subscriptions) |
| Seek to offset or timestamp | `seek-to-offset-or-timestamp` | Limited | `GET /topics/{topic}/replay?from_offset=` and `?from_timestamp_ms=` read the journal from an offset or a time, and a backfill append lands behind the live head. | Seek is a journal read, not a subscription operation; a subscription's cursor is moved only by ack or checkpoint put. See [Seek, snapshot, and retention](ROADMAP.md#seek-snapshot-and-retention). | `cargo test -p tape --test retention_backfill --test http_transport` |
| Snapshot lifecycle | `snapshot-lifecycle` | Not supported | None. | The whole-journal backup is disaster recovery, not a subscription snapshot. | [Seek, snapshot, and retention](ROADMAP.md#seek-snapshot-and-retention) |
| Seek to snapshot | `seek-to-snapshot` | Not supported | None. | There is no snapshot resource to seek to. | [Seek, snapshot, and retention](ROADMAP.md#seek-snapshot-and-retention) |
| Message retention duration | `message-retention-duration` | Limited | `PUT /topics/{topic}/retention` stores `min_offset`, `max_age_seconds`, and `protected_consumers`; `GET` reads the policy back; the floor never passes a protected consumer. | `max_age_seconds` is stored, but no gate proves age-based expiry. See [Seek, snapshot, and retention](ROADMAP.md#seek-snapshot-and-retention). | `cargo test -p tape --test retention_backfill --test cli_contract` |
| Retain acknowledged messages | `retain-acked-messages` | Limited | Messages stay readable until the retention floor passes them, whether or not any subscription acked them. | There is no per-subscription retain-acked toggle; retention is a topic property only. See [Seek, snapshot, and retention](ROADMAP.md#seek-snapshot-and-retention). | `cargo test -p tape --test retention_backfill` |
| Legacy replay and checkpoint routes | `legacy-replay-and-checkpoint-routes` | Limited | `GET /topics/{topic}/replay`, `GET /topics/{topic}/replay/stream`, and `GET` and `PUT /topics/{topic}/consumers/{consumer}/checkpoint` read and move a cursor without a subscription resource. | These predate subscriptions and remain the readback oracle for the acceptance scripts. They leave the public API when seek moves onto the subscription. See [Seek, snapshot, and retention](ROADMAP.md#seek-snapshot-and-retention). | `cargo test -p tape --test http_transport --test cli_contract` |
| Message filtering | `message-filtering` | Not supported | None. | A subscription receives every message on its topic. | [Attributes and filters](ROADMAP.md#attributes-and-filters) |
| Schema validation on publish | `schema-validation-on-publish` | Not supported | None. | `payload` is any JSON value; nothing validates it. | [Schema validation](ROADMAP.md#schema-validation) |
| Batch publish | `publish-batch` | Not supported | None. | Append takes one message per request. | [Resource lifecycle parity](ROADMAP.md#resource-lifecycle-parity) |
| Topic lifecycle | `topic-lifecycle-create-delete-list` | Not supported | None. | Topics are implicit; there is no create, delete, or list route. | [Resource lifecycle parity](ROADMAP.md#resource-lifecycle-parity) |
| Subscription expiration policy | `subscription-expiration-policy` | Not supported | None. | A subscription lives until deleted. | [Resource lifecycle parity](ROADMAP.md#resource-lifecycle-parity) |
| Detached subscription | `detached-subscription` | Not supported | None. | A subscription cannot be detached from its topic. | [Resource lifecycle parity](ROADMAP.md#resource-lifecycle-parity) |
| Streaming pull | `streaming-pull` | Not supported | None. | Unary pull and, later, push are the only delivery paths. | [Streaming pull](ROADMAP.md#streaming-pull) |
| Exactly-once delivery | `exactly-once-delivery` | Not supported | None. | Delivery is at least once; consumers deduplicate on offset. | [Exactly-once delivery](ROADMAP.md#exactly-once-delivery) |
| Export subscription | `export-subscriptions` | Not supported | None. | The whole-journal backup is disaster recovery, not an export. | [Export subscriptions](ROADMAP.md#export-subscriptions) |
| Peer-broker benchmarks | `peer-broker-benchmarks` | Not supported | None. | Performance is measured only against tape's own baseline. | [Peer-broker benchmarks](ROADMAP.md#peer-broker-benchmarks) |
| Pub/Sub wire compatibility | `pubsub-wire-compatibility` | Not supported | None. | Feature parity is offered only through tape's own h2c and OpenAPI API. | [Pub/Sub wire compatibility](ROADMAP.md#pubsub-wire-compatibility) |

## Evidence policy

The commands above are the required gates for each supported scope. This file
names the gates but does not record a test run. Test output, CI results, and
release evidence stay in their execution systems.

Every `cargo test` gate names a `[[test]]` target declared in `Cargo.toml`;
`autotests = false` means a case that is not declared there does not run.
Update a row in the same change that alters its public contract, implementation
boundary, or gate. Promote a future outcome into the README and STATUS only
after the implementation and its executable gate exist.
