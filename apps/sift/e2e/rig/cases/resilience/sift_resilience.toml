# HANDWRITE-BEGIN gap="sift-rig-resilience-scenario" tracker="1607" reason="Execute the bounded ingest, drain, and recovery stability case through Rig."
[record]
suite = "sift"
dimension = "resilience"
case = "bounded_ingest_drain_recovery"
subject = "bounded ingest remains durable and readiness drains before service shutdown"
kind = "e2e"
expected = "pass"
required = true

[[steps]]
type = "exec"
name = "bounded_ingest_drain_recovery"
cmd = ["cargo", "test", "-p", "sift", "--test", "stability_e2e", "--", "--nocapture"]
timeout_secs = 180
# HANDWRITE-END
