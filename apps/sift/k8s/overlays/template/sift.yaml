# HANDWRITE-BEGIN gap="sift-kustomize-template-instance" tracker="1606" reason="Expose only the currently supported 1x1 Sift deployment topology."
apiVersion: sift.axiom.dev/v1alpha1
kind: Sift
metadata:
  name: REPLACE_ME__NAME
  namespace: REPLACE_ME__NAMESPACE
spec:
  image: REPLACE_ME__SIFT_IMAGE_DIGEST
  peerTlsSecret: REPLACE_ME__SIFT_PEER_TLS_SECRET
  replicasPerShard: 3
  voterCount: 3
  archive:
    destination: REPLACE_ME__GCS_ARCHIVE_URI
  bootstrap:
    archiveManifestUri: REPLACE_ME__OPTIONAL_ARCHIVE_MANIFEST_URI
  storage:
    storeSize: 50Gi
    controlSize: 5Gi
    gatewaySize: 2Gi
    querySize: 2Gi
  ingest:
    maxItemsPerMinute: 720000
    maxConcurrentRequests: 32
  auth: kubernetes
  gcpProjectId: REPLACE_ME__GCP_PROJECT_ID
  gkeClusterName: REPLACE_ME__GKE_CLUSTER_NAME
  gkeLocation: REPLACE_ME__GKE_LOCATION
# HANDWRITE-END
