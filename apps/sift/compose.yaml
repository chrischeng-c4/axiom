services:
  sift:
    build:
      context: ../..
      dockerfile: apps/sift/Dockerfile
    image: sift:dev
    ports:
      - "7380:7380"
    volumes:
      - sift-data:/var/lib/sift
    restart: unless-stopped

volumes:
  sift-data: {}
