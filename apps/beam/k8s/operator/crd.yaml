apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: beams.beam.dev
spec:
  group: beam.dev
  names:
    categories: []
    kind: Beam
    plural: beams
    shortNames:
    - bem
    singular: beam
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for BeamSpec via CustomResource
        properties:
          spec:
            description: Beam spec
            properties:
              image:
                description: Serving container image, e.g. beam:latest.
                type: string
              host:
                description: Bind address.
                type: string
              port:
                description: Client API port.
                type: integer
              logLevel:
                description: Log level.
                type: string
              graceSecs:
                description: Graceful drain window.
                type: integer
              replicas:
                description: Expected replicas.
                type: integer
              storageSize:
                description: Durable storage request size, e.g. 10Gi.
                type: string
              requestGpu:
                description: Request GPU resources.
                type: boolean
            required:
            - image
            type: object
          status:
            description: Status subresource
            properties:
              phase:
                type: string
              observedGeneration:
                type: integer
            type: object
        required:
        - spec
        title: Beam
        type: object
    served: true
    storage: true
    subresources:
      status: {}
