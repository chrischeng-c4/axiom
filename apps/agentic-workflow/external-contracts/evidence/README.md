# External-contract evidence

`src/runner.py` writes one digestable JSON result per case here. The JSON
artifacts are runtime evidence and are ignored by Git; this file keeps the
declared evidence directory present in a clean checkout.
