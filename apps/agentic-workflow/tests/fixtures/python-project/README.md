# Python Artifact Dogfood

Bounded CPython fixture for the `user-model` import boundary. Agentic Workflow
drives its independently reviewed EC, Python TD, native target, unit inventory,
staged evidence, and project rollup to terminal completion.
