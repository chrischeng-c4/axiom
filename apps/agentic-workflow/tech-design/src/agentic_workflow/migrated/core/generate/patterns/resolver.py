"""Canonical Python producer for `apps/agentic-workflow/tech-design/core/generate/patterns/resolver.md`.

Migrated by batch `projection-core-generate-03`.
"""

from __future__ import annotations

from typing import Annotated


__aw_artifact_id__ = "artifact:core-generate/core-generate-patterns-resolver"
__legacy_projection_path__ = "apps/agentic-workflow/tech-design/core/generate/patterns/resolver.md"
__legacy_projection_digest__ = "sha256:9dffce877c7da4a558b36da63dec3008c4b11e76daf00ca9cb9f2a74c9328f46"


def render_markdown() -> Annotated[str, "sha256:9dffce877c7da4a558b36da63dec3008c4b11e76daf00ca9cb9f2a74c9328f46"]:
    """Render the preserved generated projection byte-for-byte."""

    return "---\nid: projects-sdd-src-generate-patterns-resolver-rs\nfill_sections: [overview, source, changes]\ncapability_refs:\n  - id: td-cb-lifecycle-automation\n    role: primary\n    gap: cb-lifecycle-dispatch\n    claim: cb-lifecycle-dispatch\n    coverage: full\n    rationale: \"Generator primitives are part of TD/CB lifecycle automation because they produce reviewable code artifacts from TD sections.\"\n---\n\n# Standardized apps/agentic-workflow/src/generate/patterns/resolver.rs\n\n## Overview\n<!-- type: overview lang: markdown -->\n\nPublic API manifest for `apps/agentic-workflow/src/generate/patterns/resolver.rs` generated from AST during Score force-regeneration standardization.\n\n### Symbols\n\n| Name | Target | Kind | Visibility | Line | Signature |\n|------|--------|------|------------|------|-----------|\n| `expand_pattern` | apps/agentic-workflow/src/generate/patterns/resolver.rs | function | pub | 20 | expand_pattern(     pattern: &UxPattern,     slot_contents: &HashMap<String, SlotContent>, ) -> (Vec<PatternNode>, Vec<String>) |\n| `resolve_pattern` | apps/agentic-workflow/src/generate/patterns/resolver.rs | function | pub | 10 | resolve_pattern(pattern_id: &str) -> Option<&'static UxPattern> |\n## Source\n<!-- type: source lang: rust -->\n<!-- source-from-target: strip-handwrite -->\n\n<!-- source-snapshot: path=apps/agentic-workflow/src/generate/patterns/resolver.rs -->\n```rust\n//! Pattern resolution — expand pattern references into full layout trees.\n\nuse super::{PatternNode, SlotContent, UxPattern};\nuse std::collections::HashMap;\n\n/// Look up a pattern by ID from the built-in registry.\n/// @spec apps/agentic-workflow/tech-design/core/generate/patterns/resolver.md#source\npub fn resolve_pattern(pattern_id: &str) -> Option<&'static UxPattern> {\n    super::registry::PATTERN_REGISTRY\n        .iter()\n        .find(|p| p.id == pattern_id)\n}\n\n/// Expand a pattern into a full layout tree with slots filled from content map.\n///\n/// Returns the expanded layout tree and any validation warnings.\n/// @spec apps/agentic-workflow/tech-design/core/generate/patterns/resolver.md#source\npub fn expand_pattern(\n    pattern: &UxPattern,\n    slot_contents: &HashMap<String, SlotContent>,\n) -> (Vec<PatternNode>, Vec<String>) {\n    let mut warnings = Vec::new();\n\n    // Check for unfilled required slots\n    for slot in &pattern.slots {\n        if slot.required && !slot_contents.contains_key(&slot.name) {\n            warnings.push(format!(\n                \"required slot '{}' in pattern '{}' was not filled\",\n                slot.name, pattern.id\n            ));\n        }\n    }\n\n    let expanded = pattern\n        .layout\n        .iter()\n        .map(|node| expand_node(node, slot_contents))\n        .collect();\n\n    (expanded, warnings)\n}\n\nfn expand_node(node: &PatternNode, slot_contents: &HashMap<String, SlotContent>) -> PatternNode {\n    let mut expanded = node.clone();\n\n    // If this is a slot reference, fill it\n    if node.kind == \"slot\" {\n        if let Some(slot_ref) = &node.slot_ref {\n            if let Some(content) = slot_contents.get(slot_ref) {\n                expanded.kind = content.component.clone();\n                expanded.props = content.props.clone();\n            }\n            // If no content, slot remains as empty container\n        }\n    }\n\n    // Recurse into children\n    expanded.children = node\n        .children\n        .iter()\n        .map(|child| expand_node(child, slot_contents))\n        .collect();\n\n    expanded\n}\n```\n\n## Changes\n<!-- type: changes lang: yaml -->\n\n```yaml\nchanges:\n  - path: apps/agentic-workflow/src/generate/patterns/resolver.rs\n    action: modify\n    section: source\n    impl_mode: codegen\n    description: |\n      Source template owns the complete UX pattern resolver module.\n```\n"
