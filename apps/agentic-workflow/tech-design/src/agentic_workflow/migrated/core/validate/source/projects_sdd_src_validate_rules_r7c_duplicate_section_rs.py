"""Canonical Python producer for `apps/agentic-workflow/tech-design/core/validate/source/projects-sdd-src-validate-rules-r7c_duplicate_section-rs.md`.

Migrated by batch `projection-core-validate-01`.
"""

from __future__ import annotations

from typing import Annotated


__aw_artifact_id__ = "artifact:core-validate/core-validate-source-projects-sdd-src-validate-rules-r7c-duplicate-section-rs"
__legacy_projection_path__ = "apps/agentic-workflow/tech-design/core/validate/source/projects-sdd-src-validate-rules-r7c_duplicate_section-rs.md"
__legacy_projection_digest__ = "sha256:bead3fa8ee448a29259d33e0d556b2bdb023c81466d061a12949f90374463952"


def render_markdown() -> Annotated[str, "sha256:bead3fa8ee448a29259d33e0d556b2bdb023c81466d061a12949f90374463952"]:
    """Render the preserved generated projection byte-for-byte."""

    return "---\nid: projects-sdd-src-validate-rules-r7c_duplicate_section-rs\nfill_sections: [overview, source, changes]\ncapability_refs:\n  - id: existing-project-standardization\n    role: primary\n    gap: managed-and-semantic-production-gates\n    claim: managed-and-semantic-production-gates\n    coverage: full\n    rationale: \"Validation TDs implement managed and semantic production gates for standardization readiness.\"\n---\n\n# Standardized apps/agentic-workflow/src/validate/rules/r7c_duplicate_section.rs\n\n## Overview\n<!-- type: overview lang: markdown -->\n\nPublic API manifest for `apps/agentic-workflow/src/validate/rules/r7c_duplicate_section.rs` generated from AST during Score force-regeneration standardization.\n\n### Symbols\n\n| Name | Target | Kind | Visibility | Line | Signature |\n|------|--------|------|------------|------|-----------|\n| `DuplicateSectionRule` | apps/agentic-workflow/src/validate/rules/r7c_duplicate_section.rs | struct | pub | 15 |  |\n## Source\n<!-- type: source lang: rust -->\n\n````rust\n//! R7c — flag any `## Heading` that appears more than once within the same\n//! spec file.\n//!\n//! Ports `spec_alignment::format_rules::check_duplicate_sections`.\n\nuse crate::spec_alignment::parser;\nuse crate::validate::{Finding, Rule, RuleId, RuleReport};\nuse std::collections::HashMap;\nuse std::path::Path;\n\n#[derive(Debug, Default, Clone)]\npub struct DuplicateSectionRule {}\n\nimpl Rule for DuplicateSectionRule {\n    fn id(&self) -> RuleId {\n        RuleId::DuplicateSection\n    }\n\n    fn check(&self, spec_path: &Path, content: &str, report: &mut RuleReport) {\n        let path_str = spec_path.to_string_lossy();\n        let doc = parser::parse(&path_str, content);\n\n        let mut by_heading: HashMap<String, Vec<usize>> = HashMap::new();\n        for section in &doc.sections {\n            by_heading\n                .entry(section.heading.clone())\n                .or_default()\n                .push(section.line);\n        }\n\n        for (heading, lines) in &by_heading {\n            if lines.len() > 1 {\n                report.push(\n                    Finding::error(\n                        RuleId::DuplicateSection,\n                        spec_path,\n                        format!(\n                            \"duplicate section heading '{}' at lines {:?}\",\n                            heading, lines\n                        ),\n                    )\n                    .with_line(lines[0]),\n                );\n            }\n        }\n    }\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    use std::path::PathBuf;\n\n    fn run(content: &str) -> RuleReport {\n        let mut r = RuleReport::new();\n        DuplicateSectionRule {}.check(&PathBuf::from(\"test.md\"), content, &mut r);\n        r\n    }\n\n    #[test]\n    fn unique_headings_pass() {\n        let spec = \"## A\\n<!-- type: overview lang: markdown -->\\n\\nx\\n## B\\n<!-- type: overview lang: markdown -->\\n\\ny\\n\";\n        assert!(run(spec).is_empty());\n    }\n\n    #[test]\n    fn duplicate_heading_flagged() {\n        let spec = \"## Same\\n\\nx\\n## Same\\n\\ny\\n\";\n        let r = run(spec);\n        assert_eq!(r.findings.len(), 1);\n        assert_eq!(r.findings[0].rule, RuleId::DuplicateSection);\n        assert!(r.findings[0].message.contains(\"Same\"));\n    }\n\n    #[test]\n    fn review_heading_matching_section_name_is_ignored() {\n        let spec = \"## Logic\\n\\\n                    <!-- type: logic lang: mermaid -->\\n\\\n                    body\\n\\\n                    \\n\\\n                    # Reviews\\n\\\n                    \\n\\\n                    ## Logic\\n\\\n                    **Verdict:** approved\\n\";\n        assert!(run(spec).is_empty());\n    }\n}\n````\n## Changes\n<!-- type: changes lang: yaml -->\n\n```yaml\nchanges:\n  - path: apps/agentic-workflow/src/validate/rules/r7c_duplicate_section.rs\n    action: modify\n    section: source\n    impl_mode: codegen\n    description: |\n      Regenerate the remaining validation module source directly from the\n      source section. Existing schema CODEGEN blocks, when present, remain\n      owned by their semantic specs.\n```\n"
