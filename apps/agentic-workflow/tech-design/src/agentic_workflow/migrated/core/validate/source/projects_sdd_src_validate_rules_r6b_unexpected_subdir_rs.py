"""Canonical Python producer for `apps/agentic-workflow/tech-design/core/validate/source/projects-sdd-src-validate-rules-r6b_unexpected_subdir-rs.md`.

Migrated by batch `projection-core-validate-01`.
"""

from __future__ import annotations

from typing import Annotated


__aw_artifact_id__ = "artifact:core-validate/core-validate-source-projects-sdd-src-validate-rules-r6b-unexpected-subdir-rs"
__legacy_projection_path__ = "apps/agentic-workflow/tech-design/core/validate/source/projects-sdd-src-validate-rules-r6b_unexpected_subdir-rs.md"
__legacy_projection_digest__ = "sha256:b3da240a78626b7d3906f57a7269527835ae2e14bba6d4605cc1c34b74798e16"


def render_markdown() -> Annotated[str, "sha256:b3da240a78626b7d3906f57a7269527835ae2e14bba6d4605cc1c34b74798e16"]:
    """Render the preserved generated projection byte-for-byte."""

    return "---\nid: projects-sdd-src-validate-rules-r6b_unexpected_subdir-rs\nfill_sections: [overview, source, changes]\ncapability_refs:\n  - id: existing-project-standardization\n    role: primary\n    gap: managed-and-semantic-production-gates\n    claim: managed-and-semantic-production-gates\n    coverage: full\n    rationale: \"Validation TDs implement managed and semantic production gates for standardization readiness.\"\n---\n\n# Standardized apps/agentic-workflow/src/validate/rules/r6b_unexpected_subdir.rs\n\n## Overview\n<!-- type: overview lang: markdown -->\n\nPublic API manifest for `apps/agentic-workflow/src/validate/rules/r6b_unexpected_subdir.rs` generated from AST during Score force-regeneration standardization.\n\n### Symbols\n\n| Name | Target | Kind | Visibility | Line | Signature |\n|------|--------|------|------------|------|-----------|\n| `UnexpectedSubdirRule` | apps/agentic-workflow/src/validate/rules/r6b_unexpected_subdir.rs | struct | pub | 32 |  |\n## Source\n<!-- type: source lang: rust -->\n\n````rust\n//! R6b — flag spec files inside an unexpected top-level subdirectory of a\n//! crate spec root.\n//!\n//! Allowed top-level subdirs are `interfaces`, `logic`, `config`, `tools`,\n//! `skills`, `generate`, `validate`, `semantic`, `specs`, `commands`,\n//! `bugs`, and `changes`. Anything else (e.g. `docs`, `notes`) is reported.\n\nuse crate::validate::{Finding, Rule, RuleId, RuleReport};\nuse std::path::Path;\n\nuse super::r6a_loose_root_file::{is_codegen_fixture_spec, locate_in_crate_spec_root};\n\nconst ALLOWED_TOP_DIRS: &[&str] = &[\n    \"interfaces\",\n    \"logic\",\n    \"config\",\n    \"tools\",\n    \"skills\",\n    \"generate\",\n    \"validate\",\n    \"semantic\",\n    \"specs\",\n    \"commands\",\n    \"bugs\",\n    \"changes\",\n];\n\n#[derive(Debug, Default, Clone)]\n/// @spec apps/agentic-workflow/tech-design/core/validate/source/projects-sdd-src-validate-rules-r6b_unexpected_subdir-rs.md#source\npub struct UnexpectedSubdirRule {}\n\n/// @spec apps/agentic-workflow/tech-design/core/validate/source/projects-sdd-src-validate-rules-r6b_unexpected_subdir-rs.md#source\nimpl Rule for UnexpectedSubdirRule {\n    fn id(&self) -> RuleId {\n        RuleId::UnexpectedSubdir\n    }\n\n    fn check(&self, spec_path: &Path, content: &str, report: &mut RuleReport) {\n        if is_codegen_fixture_spec(content) {\n            return;\n        }\n        let Some((_, rel)) = locate_in_crate_spec_root(spec_path) else {\n            return;\n        };\n        let parts: Vec<&str> = rel.split('/').collect();\n        if parts.len() < 2 {\n            return; // single-component is R6a's territory\n        }\n        let top = parts[0];\n        if !ALLOWED_TOP_DIRS.contains(&top) {\n            report.push(Finding::error(\n                RuleId::UnexpectedSubdir,\n                spec_path,\n                format!(\n                    \"file lives under unexpected top-level subdir `{}`; allowed: {}\",\n                    top,\n                    ALLOWED_TOP_DIRS.join(\", \")\n                ),\n            ));\n        }\n    }\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    use std::path::PathBuf;\n\n    fn run(path: &str) -> RuleReport {\n        let mut r = RuleReport::new();\n        UnexpectedSubdirRule {}.check(&PathBuf::from(path), \"\", &mut r);\n        r\n    }\n\n    #[test]\n    fn allowed_subdir_is_ok() {\n        for dir in &[\n            \"interfaces\",\n            \"logic\",\n            \"config\",\n            \"tools\",\n            \"skills\",\n            \"generate\",\n            \"validate\",\n        ] {\n            let path = format!(\"apps/agentic-workflow/tech-design/core/{}/foo.md\", dir);\n            assert!(run(&path).is_empty(), \"{} should be ok\", dir);\n        }\n    }\n\n    #[test]\n    fn unexpected_subdir_flagged() {\n        let r = run(\"apps/agentic-workflow/tech-design/core/docs/notes.md\");\n        assert_eq!(r.findings.len(), 1);\n        assert_eq!(r.findings[0].rule, RuleId::UnexpectedSubdir);\n        assert!(r.findings[0].message.contains(\"docs\"));\n    }\n\n    #[test]\n    fn root_level_file_not_handled_here() {\n        // R6a covers single-component files; R6b stays silent.\n        let r = run(\"apps/agentic-workflow/tech-design/core/state.md\");\n        assert!(r.is_empty());\n    }\n\n    #[test]\n    fn nested_unexpected_dir_flagged_only_at_top_level() {\n        // Top-level is allowed (logic/), the nested foo/ is not validated by R6b.\n        let r = run(\"apps/agentic-workflow/tech-design/core/logic/foo/bar.md\");\n        assert!(r.is_empty());\n    }\n}\n````\n## Changes\n<!-- type: changes lang: yaml -->\n\n```yaml\nchanges:\n  - path: apps/agentic-workflow/src/validate/rules/r6b_unexpected_subdir.rs\n    action: modify\n    section: source\n    impl_mode: codegen\n    description: |\n      Regenerate the remaining validation module source directly from the\n      source section. Existing schema CODEGEN blocks, when present, remain\n      owned by their semantic specs.\n```\n"
