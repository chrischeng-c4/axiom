"""Canonical Python producer for `apps/agentic-workflow/tech-design/core/validate/test_util.md`.

Migrated by batch `projection-core-validate-01`.
"""

from __future__ import annotations

from typing import Annotated


__aw_artifact_id__ = "artifact:core-validate/core-validate-test-util"
__legacy_projection_path__ = "apps/agentic-workflow/tech-design/core/validate/test_util.md"
__legacy_projection_digest__ = "sha256:bebc29846ee6e265194fc46d9201a6d578eb26c872f07339f7ff1449a7de9d81"


def render_markdown() -> Annotated[str, "sha256:bebc29846ee6e265194fc46d9201a6d578eb26c872f07339f7ff1449a7de9d81"]:
    """Render the preserved generated projection byte-for-byte."""

    return "---\nid: projects-sdd-src-test-util-rs\nfill_sections: [overview, source, changes]\ncapability_refs:\n  - id: existing-project-standardization\n    role: primary\n    gap: managed-and-semantic-production-gates\n    claim: managed-and-semantic-production-gates\n    coverage: full\n    rationale: \"Validation TDs implement managed and semantic production gates for standardization readiness.\"\n---\n\n# Standardized apps/agentic-workflow/src/test_util.rs\n\n## Overview\n<!-- type: overview lang: markdown -->\n\nPublic API manifest for `apps/agentic-workflow/src/test_util.rs` generated from AST during Score force-regeneration standardization.\n\n### Symbols\n\n| Name | Target | Kind | Visibility | Line | Signature |\n|------|--------|------|------------|------|-----------|\n| `setup_change_with_issue` | apps/agentic-workflow/src/test_util.rs | function | pub | 44 | setup_change_with_issue(slug: &str) -> (TempDir, PathBuf) |\n| `setup_project_with_issue` | apps/agentic-workflow/src/test_util.rs | function | pub | 56 | setup_project_with_issue(slug: &str) -> TempDir |\n| `write_minimal_issue` | apps/agentic-workflow/src/test_util.rs | function | pub | 19 | write_minimal_issue(project_root: &std::path::Path, slug: &str) |\n## Source\n<!-- type: source lang: rust -->\n\n````rust\n//! Test fixtures shared across SDD unit tests.\n//!\n//! @spec apps/agentic-workflow/tech-design/core/interfaces/models/state.md#R4\n//! R4: StateManager::save() requires an issue file to back sync_to_issue().\n//! Tests that exercise save() must create a minimal issue fixture; these\n//! helpers centralise that setup so tests stay uniform across modules.\n\n#![cfg(test)]\n\nuse std::path::PathBuf;\nuse tempfile::TempDir;\n\n/// Create a minimal temp issue working copy for `<slug>` so\n/// `StateManager::sync_to_issue()` can write to it. The file uses the minimum\n/// frontmatter + body the structured-issue gate accepts.\n/// @spec apps/agentic-workflow/tech-design/core/validate/test_util.md#source\npub(crate) fn write_minimal_issue(project_root: &std::path::Path, slug: &str) {\n    let issues_dir = crate::shared::workspace::issues_path(project_root).join(\"open\");\n    std::fs::create_dir_all(&issues_dir).unwrap();\n    let body = format!(\n        \"---\\n\\\n         type: refactor\\n\\\n         title: 'test({}): fixture'\\n\\\n         state: open\\n\\\n         ---\\n\\n\\\n         ## Problem\\n\\n\\\n         Test fixture for {}.\\n\\n\\\n         ## Requirements\\n\\n\\\n         - R1: fixture requirement\\n\\n\\\n         ## Scope\\n\\n\\\n         In scope: fixture.\\n\",\n        slug, slug\n    );\n    std::fs::write(issues_dir.join(format!(\"{}.md\", slug)), body).unwrap();\n}\n\n/// Create a temp project root with a runtime `changes/<slug>/` change dir\n/// (under `/tmp/aw/workspaces/<workspace>/changes`) AND a backing temp issue\n/// file. Returns `(TempDir, change_dir)`; hold the TempDir for lifetime.\n#[allow(dead_code)]\n/// @spec apps/agentic-workflow/tech-design/core/validate/test_util.md#source\npub(crate) fn setup_change_with_issue(slug: &str) -> (TempDir, PathBuf) {\n    let tmp = TempDir::new().unwrap();\n    let change_dir = crate::shared::workspace::change_path(tmp.path(), slug);\n    std::fs::create_dir_all(&change_dir).unwrap();\n    write_minimal_issue(tmp.path(), slug);\n    (tmp, change_dir)\n}\n\n/// Create a temp project root with a runtime `changes/` dir AND a backing\n/// issue file for `slug`. Returns the TempDir only — tests that manage their\n/// own change_dir layout should use this.\n/// @spec apps/agentic-workflow/tech-design/core/validate/test_util.md#source\npub(crate) fn setup_project_with_issue(slug: &str) -> TempDir {\n    let tmp = TempDir::new().unwrap();\n    std::fs::create_dir_all(crate::shared::workspace::changes_path(tmp.path())).unwrap();\n    write_minimal_issue(tmp.path(), slug);\n    tmp\n}\n\n````\n\n## Changes\n<!-- type: changes lang: yaml -->\n\n```yaml\nchanges:\n  - path: apps/agentic-workflow/src/test_util.rs\n    action: modify\n    section: source\n    impl_mode: codegen\n    description: |\n      Regenerate the test utility module directly from the source template.\n      This helper is small and cross-language-neutral enough that a narrower\n      section type would add noise instead of reusable generator capability.\n```\n"
