"""Canonical Python tech design migrated from `apps/agentic-workflow/tech-design/surface/validate/tests/cb_namespace_test.md`.

Migrated by batch `semantic-surface-validate-01`.
"""

from __future__ import annotations

from typing import Annotated


__aw_artifact_id__ = "artifact:surface-validate/surface-validate-tests-cb-namespace-test"
__legacy_td_path__ = "apps/agentic-workflow/tech-design/surface/validate/tests/cb_namespace_test.md"
__legacy_td_digest__ = "sha256:8ebdf8f17bfe480b54ece68e66ee8d6847a8cade005bdb6a16b27cbf4fa28681"


def render_markdown() -> Annotated[str, "sha256:8ebdf8f17bfe480b54ece68e66ee8d6847a8cade005bdb6a16b27cbf4fa28681"]:
    """Render the preserved legacy design byte-for-byte."""

    return "---\nid: projects-score-tests-cb-namespace-test-rs\nfill_sections: [overview, changes]\ncapability_refs:\n  - id: existing-project-standardization\n    role: primary\n    gap: managed-and-semantic-production-gates\n    claim: managed-and-semantic-production-gates\n    coverage: full\n    rationale: \"Standardization TDs support brownfield takeover, semantic coverage, traceability, and production readiness gates.\"\n---\n\n# Standardized apps/agentic-workflow/tests/cli/tests/cb_namespace_test.rs\n\n## Overview\n<!-- type: overview lang: markdown -->\n\nPublic API manifest for `apps/agentic-workflow/tests/cli/tests/cb_namespace_test.rs` generated from AST during Score force-regeneration standardization.\n\n### Symbols\n\nNo public AST symbols.\n## Source\n<!-- type: source lang: rust -->\n<!-- source-from-target: strip-handwrite -->\n\n<!-- source-snapshot: path=apps/agentic-workflow/tests/cli/tests/cb_namespace_test.rs -->\n```rust\n// SPEC-MANAGED: apps/agentic-workflow/tech-design/surface/validate/tests/cb_namespace_test.md#source\n// CODEGEN-BEGIN\n//! Integration tests for the public `aw cb` codebase lifecycle.\n//!\n//! Phase 1 contract: registration, phase advance to `cb_genned`, trailer\n//! `Cb-Gen`, dispatch envelope on stdout, and `--group-by` flag for\n//! `cb check`.\n//!\n//! @spec apps/agentic-workflow/tech-design/surface/specs/score-namespaces.md#test-plan\n\nuse agentic_workflow::cli::Commands;\nuse clap::{CommandFactory, Parser};\n\n#[derive(Parser)]\n#[command(name = \"aw\")]\nstruct Cli {\n    #[command(subcommand)]\n    command: Commands,\n}\n\n/// R1: CB is public and TD stays authoring-only.\n#[test]\nfn test_cb_gen_registered() {\n    let cmd = Cli::command();\n    let cb = cmd.find_subcommand(\"cb\").expect(\"cb namespace registered\");\n    cb.find_subcommand(\"gen\").expect(\"cb gen registered\");\n    let td = cmd.find_subcommand(\"td\").expect(\"td namespace registered\");\n    assert!(td.find_subcommand(\"gen\").is_none());\n}\n\n#[test]\nfn test_cb_check_registered() {\n    let cmd = Cli::command();\n    let cb = cmd.find_subcommand(\"cb\").expect(\"cb namespace registered\");\n    cb.find_subcommand(\"check\").expect(\"cb check registered\");\n}\n\n/// R2: phase-advance verification at the *string* level — `aw cb gen`\n/// must write canonical `cb_genned`, not the legacy `td_gen_coded`.\n#[test]\nfn test_cb_gen_phase_advance() {\n    use agentic_workflow::issues::types::td_phase;\n    assert_eq!(td_phase::CB_GENNED, \"cb_genned\");\n    assert!(td_phase::is_post_gen(\"cb_genned\"));\n    assert!(td_phase::is_post_gen(\"td_gen_coded\"));\n    assert!(!td_phase::is_post_gen(\"td_reviewed\"));\n}\n\n/// R2: trailer constant is `Cb-Gen`, not `Td-GenCode`.\n#[test]\nfn test_cb_gen_trailer() {\n    use agentic_workflow::issues::types::lifecycle_trailer;\n    assert_eq!(lifecycle_trailer::CB_GEN, \"Cb-Gen\");\n    assert_eq!(lifecycle_trailer::LEGACY_TD_GEN_CODE, \"Td-GenCode\");\n    assert_eq!(lifecycle_trailer::normalize(\"Td-GenCode\"), \"Cb-Gen\");\n    assert_eq!(lifecycle_trailer::normalize(\"Cb-Gen\"), \"Cb-Gen\");\n}\n\n/// R2: dispatch envelope shape is preserved by virtue of `cb::run_gen`\n/// delegating to `td::run_gen_code`. Verify the `cb gen` arg shape.\n#[test]\nfn test_cb_gen_envelope() {\n    let cmd = Cli::command();\n    let cb_gen = cmd\n        .find_subcommand(\"cb\")\n        .and_then(|c| c.find_subcommand(\"gen\"))\n        .expect(\"cb gen present\");\n    let positionals: Vec<String> = cb_gen\n        .get_positionals()\n        .map(|p: &clap::Arg| p.get_id().as_str().to_string())\n        .collect();\n    assert!(\n        positionals.iter().any(|p| p == \"slug\"),\n        \"cb gen has slug arg, got {:?}\",\n        positionals\n    );\n}\n\n/// R3: `cb check --group-by` accepts gap | file | status.\n#[test]\nfn test_cb_check_group_by() {\n    let cmd = Cli::command();\n    let cb_check = cmd\n        .find_subcommand(\"cb\")\n        .and_then(|c| c.find_subcommand(\"check\"))\n        .expect(\"cb check present\");\n    let gb = cb_check\n        .get_arguments()\n        .find(|a: &&clap::Arg| a.get_id().as_str() == \"group_by\")\n        .expect(\"--group-by flag present\");\n    let possible: Vec<String> = gb\n        .get_possible_values()\n        .iter()\n        .map(|v: &clap::builder::PossibleValue| v.get_name().to_string())\n        .collect();\n    for variant in [\"gap\", \"file\", \"status\"] {\n        assert!(\n            possible.iter().any(|p| p == variant),\n            \"--group-by missing '{}', got {:?}\",\n            variant,\n            possible\n        );\n    }\n}\n\n// CODEGEN-END\n```\n\n## Changes\n<!-- type: changes lang: yaml -->\n\n```yaml\nchanges:\n  - path: apps/agentic-workflow/tests/cli/tests/cb_namespace_test.rs\n    action: modify\n    impl_mode: codegen\n    section: source\n    description: |\n      Existing source claimed by `aw standardize managed run`. The code is\n      wrapped in a tracked HANDWRITE block until deterministic generator\n      coverage can replace it with CODEGEN.\n```\n"
