"""Canonical Python producer for `apps/agentic-workflow/tech-design/surface/interfaces/src/lib.md`.

Migrated by batch `projection-surface-interfaces-01`.
"""

from __future__ import annotations

from typing import Annotated


__aw_artifact_id__ = "artifact:surface-interfaces/surface-interfaces-src-lib"
__legacy_projection_path__ = "apps/agentic-workflow/tech-design/surface/interfaces/src/lib.md"
__legacy_projection_digest__ = "sha256:b9e8e545536738b73aa89694a2aca65ad4808f248286b60b9b8fc72b2f4e4e6b"


def render_markdown() -> Annotated[str, "sha256:b9e8e545536738b73aa89694a2aca65ad4808f248286b60b9b8fc72b2f4e4e6b"]:
    """Render the preserved generated projection byte-for-byte."""

    return "---\nid: projects-score-src-lib-rs\nfill_sections: [overview, changes]\ncapability_refs:\n  - id: aw-core-client-model-workitem-first-artifact-lifecycle\n    role: primary\n    gap: core-concept-model-and-invariants\n    claim: core-concept-model-and-invariants\n    coverage: full\n    rationale: \"Support CLI surfaces expose AW Core bootstrap, hook, project, META-doc, and workspace invariants.\"\n---\n\n# Standardized apps/agentic-workflow/src/cli/mod.rs\n\n## Overview\n<!-- type: overview lang: markdown -->\n\nPublic API manifest for `apps/agentic-workflow/src/cli/mod.rs` generated from AST during Score force-regeneration standardization.\n\n### Symbols\n\n| Name | Target | Kind | Visibility | Line | Signature |\n|------|--------|------|------------|------|-----------|\n| `artifact_producer` | apps/agentic-workflow/src/cli/mod.rs | module | pub(crate) | 9 |  |\n| `capability` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 10 |  |\n| `capability_type` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 11 |  |\n| `cb` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 12 |  |\n| `cb_fill` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 13 |  |\n| `chain` | apps/agentic-workflow/src/cli/mod.rs | module | pub(crate) | 14 |  |\n| `check_alignment` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 15 |  |\n| `commands` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 16 |  |\n| `conf` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 17 |  |\n| `doc_mirror` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 18 |  |\n| `drift` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 19 |  |\n| `ec` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 20 |  |\n| `fillback` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 21 |  |\n| `find_project_root` | apps/agentic-workflow/src/cli/mod.rs | function | pub | 80 | find_project_root() -> anyhow::Result<std::path::PathBuf> |\n| `generator` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 22 |  |\n| `goal` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 23 |  |\n| `guard` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 24 |  |\n| `guard_sanction` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 25 |  |\n| `hook` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 26 |  |\n| `init` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 27 |  |\n| `issues` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 28 |  |\n| `llm` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 29 |  |\n| `loop_state` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 30 |  |\n| `merge_target` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 57 |  |\n| `meta` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 31 |  |\n| `meta_docs` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 32 |  |\n| `migrate` | apps/agentic-workflow/src/cli/mod.rs | module | pub(crate) | 52 |  |\n| `production` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 33 |  |\n| `project` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 34 |  |\n| `regenerability_policy` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 35 |  |\n| `remote_push` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 36 |  |\n| `review` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 37 |  |\n| `run` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 38 |  |\n| `shell_env` | apps/agentic-workflow/src/cli/mod.rs | module | pub(crate) | 39 |  |\n| `slug_workspace` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 40 |  |\n| `standard_cli` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 41 |  |\n| `standardize` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 42 |  |\n| `td` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 43 |  |\n| `td_check_section_type` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 44 |  |\n| `td_lock` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 45 |  |\n| `td_migrate` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 46 |  |\n| `update` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 47 |  |\n| `validate_spec_structure` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 48 |  |\n| `workflow_guard` | apps/agentic-workflow/src/cli/mod.rs | module | pub | 49 |  |\n## Source\n<!-- type: source lang: rust -->\n<!-- source-from-target: strip-handwrite -->\n\n<!-- source-snapshot: path=apps/agentic-workflow/src/cli/mod.rs -->\n```rust\n// SPEC-MANAGED: apps/agentic-workflow/tech-design/surface/interfaces/src/lib.md#source\n// CODEGEN-BEGIN\n//! Agentic Workflow — local spec-governed workflow orchestrator.\n//!\n//! Standalone binary + library. The binary entry point is `src/bin/aw.rs`;\n//! this library exposes the `Commands` enum and `run_command` dispatch for\n//! programmatic consumers and for the binary itself.\n\npub(crate) mod artifact_producer;\npub(crate) mod agent_prompt;\npub mod capability;\npub mod capability_type;\npub mod cb;\npub mod cb_fill;\npub(crate) mod chain;\npub mod check_alignment;\npub mod commands;\npub mod conf;\npub mod doc_mirror;\npub mod drift;\npub mod ec;\npub mod fillback;\npub mod generator;\npub mod goal;\npub mod guard;\npub mod guard_sanction;\npub mod hook;\npub mod init;\npub mod issues;\npub mod llm;\npub mod loop_state;\npub mod meta;\npub mod meta_docs;\npub mod production;\npub mod project;\npub mod regenerability_policy;\npub mod remote_push;\npub mod review;\npub mod review_doc_projection;\npub mod review_obs_rules;\npub mod review_rules;\npub mod run;\npub(crate) mod shell_env;\npub mod slug_workspace;\npub mod standard_cli;\npub mod standardize;\npub mod td;\npub mod td_check_section_type;\npub mod td_lock;\npub mod td_migrate;\npub mod update;\npub mod validate_spec_structure;\npub mod workflow_guard;\n\n// Legacy modules kept for init.rs / update.rs dependencies\npub(crate) mod migrate;\n\n// Legacy merge-target resolution logic retained for older merge-style recovery\n// surfaces while those APIs age out.\n// Public so integration tests in tests/ can call resolve_merge_target directly.\npub mod merge_target;\n\n// Public library API — the binary (src/main.rs) and any programmatic consumer\n// call into these.\npub use commands::{run_command, Commands};\n\nconst LEGACY_SCORE_WORKSPACE_DIR: &str = concat!(\".\", \"score\");\n\nfn legacy_score_workspace_error(root: &std::path::Path) -> anyhow::Error {\n    anyhow::anyhow!(\n        \"legacy Agentic Workflow state found at {}; active runtime state now lives under /tmp/aw and root config lives in aw.toml. Move or remove the old directory explicitly, then rerun this command.\",\n        root.join(LEGACY_SCORE_WORKSPACE_DIR).display()\n    )\n}\n\n// Find the project root by walking up from CWD looking for `aw.toml`.\n// Falls back to CWD if no `aw.toml` is found (e.g., during greenfield setup).\n///\n// This intentionally returns the repo root for the CLI process's current\n// working tree. In a git linked-worktree checkout, do not use shared git\n// metadata to redirect to another checkout: mutating Agentic Workflow commands must write\n// to the `aw.toml` config visible from the user's current checkout.\n// @spec apps/agentic-workflow/tech-design/surface/interfaces/src/lib.md#source\npub fn find_project_root() -> anyhow::Result<std::path::PathBuf> {\n    let cwd = std::env::current_dir()?;\n    let mut dir = cwd.as_path();\n    let mut legacy_root = None;\n    loop {\n        if dir.join(\"aw.toml\").exists() {\n            if dir.join(LEGACY_SCORE_WORKSPACE_DIR).exists() {\n                return Err(legacy_score_workspace_error(dir));\n            }\n            return Ok(dir.to_path_buf());\n        }\n        if dir.join(LEGACY_SCORE_WORKSPACE_DIR).exists() && legacy_root.is_none() {\n            legacy_root = Some(dir.to_path_buf());\n        }\n        match dir.parent() {\n            Some(parent) => dir = parent,\n            None => {\n                if let Some(root) = legacy_root {\n                    return Err(legacy_score_workspace_error(&root));\n                }\n                // No aw.toml found — fall back to CWD for uninitialized repos.\n                return Ok(cwd);\n            }\n        }\n    }\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n    use std::path::PathBuf;\n\n    // find_project_root() reads process-global CWD; serialize tests that\n    // mutate it against the crate-wide `shell_env::CWD_LOCK` (issue #1401)\n    // so parallel cargo test runs don't race against cwd mutations in\n    // OTHER modules (e.g. `cli/guard.rs`, `cli/cb.rs`), not just this one.\n    use crate::cli::shell_env::CWD_LOCK;\n\n    fn git_available() -> bool {\n        std::process::Command::new(\"git\")\n            .arg(\"--version\")\n            .output()\n            .map(|o| o.status.success())\n            .unwrap_or(false)\n    }\n\n    fn init_git_repo(path: &std::path::Path) {\n        for args in [\n            vec![\"init\", \"-q\", \"-b\", \"main\"],\n            vec![\"config\", \"user.email\", \"test@example.com\"],\n            vec![\"config\", \"user.name\", \"Test\"],\n            vec![\"commit\", \"--allow-empty\", \"-m\", \"init\", \"-q\"],\n        ] {\n            let out = std::process::Command::new(\"git\")\n                .args(&args)\n                .current_dir(path)\n                .output()\n                .expect(\"git command\");\n            assert!(\n                out.status.success(),\n                \"git {:?} failed: {}\",\n                args,\n                String::from_utf8_lossy(&out.stderr)\n            );\n        }\n    }\n\n    // Canonicalize via `std::fs::canonicalize`. On macOS, tempdir paths go\n    // through /private/var/… so comparisons need canonicalized values on both\n    // sides.\n    fn canon(p: &std::path::Path) -> PathBuf {\n        std::fs::canonicalize(p).unwrap()\n    }\n\n    #[test]\n    fn find_project_root_inside_worktree_returns_current_worktree() {\n        if !git_available() {\n            return;\n        }\n        let _guard = CWD_LOCK\n            .lock()\n            .unwrap_or_else(|poisoned| poisoned.into_inner());\n        let prev = std::env::current_dir().unwrap();\n\n        let tmp = tempfile::TempDir::new().unwrap();\n        let main_repo = tmp.path().join(\"main\");\n        std::fs::create_dir_all(&main_repo).unwrap();\n        init_git_repo(&main_repo);\n        std::fs::create_dir_all(main_repo.join(\".aw\")).unwrap();\n        std::fs::write(main_repo.join(\"aw.toml\"), \"\").unwrap();\n\n        let worktree_rel = \"../linked/foo\";\n        let out = std::process::Command::new(\"git\")\n            .args([\"worktree\", \"add\", \"-b\", \"cclab/foo\", worktree_rel])\n            .current_dir(&main_repo)\n            .output()\n            .expect(\"git worktree add\");\n        assert!(\n            out.status.success(),\n            \"worktree add: {}\",\n            String::from_utf8_lossy(&out.stderr)\n        );\n\n        let worktree = main_repo.join(worktree_rel);\n        std::fs::create_dir_all(worktree.join(\".aw\")).unwrap();\n        std::fs::write(worktree.join(\"aw.toml\"), \"\").unwrap();\n\n        std::env::set_current_dir(&worktree).unwrap();\n        let resolved = find_project_root().unwrap();\n        std::env::set_current_dir(&prev).unwrap();\n\n        assert_eq!(\n            canon(&resolved),\n            canon(&worktree),\n            \"linked worktree CWD must resolve to the current worktree root\"\n        );\n    }\n\n    #[test]\n    fn find_project_root_inside_main_repo_unchanged() {\n        if !git_available() {\n            return;\n        }\n        let _guard = CWD_LOCK\n            .lock()\n            .unwrap_or_else(|poisoned| poisoned.into_inner());\n        let prev = std::env::current_dir().unwrap();\n\n        let tmp = tempfile::TempDir::new().unwrap();\n        let repo = tmp.path().join(\"repo\");\n        std::fs::create_dir_all(&repo).unwrap();\n        init_git_repo(&repo);\n        std::fs::create_dir_all(repo.join(\".aw\")).unwrap();\n        std::fs::write(repo.join(\"aw.toml\"), \"\").unwrap();\n        let subdir = repo.join(\"crates/demo\");\n        std::fs::create_dir_all(&subdir).unwrap();\n\n        std::env::set_current_dir(&repo).unwrap();\n        let from_root = find_project_root().unwrap();\n        std::env::set_current_dir(&subdir).unwrap();\n        let from_subdir = find_project_root().unwrap();\n        std::env::set_current_dir(&prev).unwrap();\n\n        assert_eq!(canon(&from_root), canon(&repo));\n        assert_eq!(canon(&from_subdir), canon(&repo));\n    }\n\n    #[test]\n    fn find_project_root_non_git_tempdir_walks_up() {\n        let _guard = CWD_LOCK\n            .lock()\n            .unwrap_or_else(|poisoned| poisoned.into_inner());\n        let prev = std::env::current_dir().unwrap();\n\n        let tmp = tempfile::TempDir::new().unwrap();\n        let proj = tmp.path().join(\"proj\");\n        std::fs::create_dir_all(proj.join(\".aw\")).unwrap();\n        std::fs::write(proj.join(\"aw.toml\"), \"\").unwrap();\n\n        std::env::set_current_dir(&proj).unwrap();\n        let resolved = find_project_root().unwrap();\n        std::env::set_current_dir(&prev).unwrap();\n\n        assert_eq!(\n            canon(&resolved),\n            canon(&proj),\n            \"non-git context must fall back to walk-up\"\n        );\n    }\n\n    #[test]\n    fn find_project_root_signature_unchanged_compile_check() {\n        let _f: fn() -> anyhow::Result<std::path::PathBuf> = find_project_root;\n    }\n}\n\n// CODEGEN-END\n```\n\n## Changes\n<!-- type: changes lang: yaml -->\n\n```yaml\nchanges:\n  - path: apps/agentic-workflow/src/cli/mod.rs\n    action: modify\n    impl_mode: codegen\n    section: source\n    description: |\n      Whole-file source template generated from the standardized target body.\n  - path: apps/agentic-workflow/src/cli/mod.rs\n    action: modify\n    impl_mode: codegen\n    section: source\n    description: |\n      Issue #848: whole-file resync to the current module list (adds\n      capability_type, chain, ec, guard, llm, loop_state, shell_env,\n      standard_cli, sync, td_lock; drops the removed cb_arbitrate,\n      cb_review, cb_revise modules) and current find_project_root body.\n  - path: apps/agentic-workflow/src/cli/mod.rs\n    action: modify\n    impl_mode: codegen\n    section: source\n    description: |\n      Issue #984 (init-projector slice 1/3): registers the new `doc_mirror`\n      module — the one shared whitelist definition consumed by both `aw\n      init`'s AGENTS.md projection and `root_doc_mirror_test`.\n  - path: apps/agentic-workflow/src/cli/mod.rs\n    action: modify\n    impl_mode: codegen\n    section: source\n    description: |\n      Issue #1309: registers the new `drift` module (stale\n      installed-binary-vs-checkout-source version-skew warning invoked from\n      `commands::run_command`).\n  - path: apps/agentic-workflow/src/cli/mod.rs\n    action: modify\n    impl_mode: codegen\n    section: source\n    description: |\n      Issue #1497: registers `meta_docs`, the shared ownership-matrix and\n      validation library consumed by current checks and the dependent META-doc\n      producer surface.\n  - path: apps/agentic-workflow/src/cli/mod.rs\n    action: modify\n    impl_mode: codegen\n    section: source\n    description: |\n      Issue #1897: registers the new `goal` module — the `aw goal`\n      ad-hoc CLI-owned verifiable-condition loop verb family\n      (`set`/`check`/`show`/`list`/`clear`), workspace-scoped and\n      outside the tracked WI/TD/EC lifecycle surface.\n```\n"
