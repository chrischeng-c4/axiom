{
  "assertions": [
    "schema_version is guard.report/1",
    "verb is scan",
    "required report fields have stable JSON types",
    "summary.security_findings equals findings length",
    "status, exit_code, and completion.clean agree",
    "evidence tools are exactly vat,rig,meter with no duplicates",
    "each configured adapter executes exactly once with caller-owned argv",
    "folded commands exactly match the recorded adapter invocation",
    "folded adapter reports preserve schema, independent outcomes, and findings",
    "evidence_count and evidence_failed summarize the actual evidence array",
    "schema_version is guard.report/1",
    "verb is scan",
    "required report fields have stable JSON types",
    "summary.security_findings equals findings length",
    "status, exit_code, and completion.clean agree",
    "--vat-command executes exactly once and preserves its public value",
    "schema_version is guard.report/1",
    "verb is scan",
    "required report fields have stable JSON types",
    "summary.security_findings equals findings length",
    "status, exit_code, and completion.clean agree",
    "--rig-dir executes exactly once and preserves its public value",
    "schema_version is guard.report/1",
    "verb is scan",
    "required report fields have stable JSON types",
    "summary.security_findings equals findings length",
    "status, exit_code, and completion.clean agree",
    "--rig-command executes exactly once and preserves its public value",
    "schema_version is guard.report/1",
    "verb is scan",
    "required report fields have stable JSON types",
    "summary.security_findings equals findings length",
    "status, exit_code, and completion.clean agree",
    "--meter-command executes exactly once and preserves its public value",
    "schema_version is guard.report/1",
    "verb is scan",
    "required report fields have stable JSON types",
    "summary.security_findings equals findings length",
    "status, exit_code, and completion.clean agree",
    "--arena-spec executes exactly once and preserves its public value",
    "schema_version is guard.report/1",
    "verb is scan",
    "required report fields have stable JSON types",
    "summary.security_findings equals findings length",
    "status, exit_code, and completion.clean agree",
    "--arena-command executes exactly once and preserves its public value",
    "all nine public adapter flags map to their exact command grammar"
  ],
  "attempts": [
    {
      "assertion_count": 47,
      "assertions_digest": "sha256:bf2b6b7a5d7fc5971aa99776f69c7b21cfa20cfad9f727155ebfe3ee8dd2b742",
      "elapsed_ms": 988,
      "exit_code": 0
    }
  ],
  "case_id": "guard-dynamic-adapter-routing",
  "exit_code": 0,
  "implementation": "apps/guard/external-contracts/src/evidence/dynamic-adapter-routing.py",
  "mode": "behavior",
  "protocol": "aw.python-ec.evidence.v1"
}
