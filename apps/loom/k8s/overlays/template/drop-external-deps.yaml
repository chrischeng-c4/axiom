apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: loom
  namespace: loom
spec:
  template:
    spec:
      containers:
        - name: loom
          env:
            - name: LOOM_RELAY
              $patch: delete
            - name: LOOM_KEEP
              $patch: delete
