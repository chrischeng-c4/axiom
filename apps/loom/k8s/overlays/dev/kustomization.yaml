# Dev overlay: the single-node base as-is (single-node raft, small footprint) —
# the kind / smoke-test shape.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

images:
  - name: loom
    newTag: dev
