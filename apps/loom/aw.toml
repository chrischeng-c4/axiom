[project]
name = "loom"
cap_path = "README.md"
label = "app:loom"

[capability.profile]
traits = ["long_running", "cli_facing", "network_exposed", "http2_api", "kubernetes_native", "primary_replicas", "stateful_storage"]

[[workspaces]]
name = "loom"
paths = ["**"]
target = "rust"
test_cmd = "cargo test -p loom"
codegen.profile = "rust/score-crate"

# AW-EC-BEGIN
[aw.ec.generated]
version = 1
project = "loom"
generated_from_td_digest = "sha256:d9501506802d87a5d99b97e6c7cbecb9fd852b6cbe7fb0662013a3a4b47eeada"

[[aw.ec.generated.cases]]
id = "loom-cli-interface-issue"
capability_id = "cli-interface"
claim_id = "loom-cli-convention-and-control-verbs"
contract_id = "loom-cli-issue-verbs"
category = "behavior"
td_ref = "apps/loom/external-contracts/behavior/541.md#loom-cli-interface-issue"
test_path = "apps/loom/tests/behavior_loom_cli_interface_issue.rs"
command = "cargo test -p loom --test cli_contract"
required_for_production = true
assertions = [
    "loom --help contains standard subcommands (controller, worker, spec, llm, upgrade, issue).",
    "loom controller --help outputs: strongly-consistent DAG state",
    "loom worker --help outputs: Resident pull-loop worker harness",
    "loom llm outline outputs: architecture, roles, control-api",
    "loom llm architecture outputs: never traverse loom",
    "loom upgrade --check outputs: current:",
    "loom spec --format openapi outputs a valid OpenAPI JSON document",
    "loom spec gen --lang py outputs models.py and client.py",
    "loom worker fails without environment variables (requires LOOM_KEEP)",
    "loom issue search and view fail offline with no online feature error",
    "loom issue create runs offline in dry run mode emitting a pre-filled issue URL",
]
# AW-EC-END
