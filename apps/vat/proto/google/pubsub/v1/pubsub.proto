// Trimmed google.pubsub.v1 — the Publisher/Subscriber subset vat's built-in
// emulator implements, with the OFFICIAL field numbers so real client SDKs are
// wire-compatible. REST (google.api) annotations and the Schema service are
// dropped; well-known types come from protoc.
syntax = "proto3";

package google.pubsub.v1;

import "google/protobuf/empty.proto";
import "google/protobuf/timestamp.proto";

message PubsubMessage {
  bytes data = 1;
  map<string, string> attributes = 2;
  string message_id = 3;
  google.protobuf.Timestamp publish_time = 4;
  string ordering_key = 6;
}

message Topic {
  string name = 1;
  map<string, string> labels = 2;
}

message GetTopicRequest {
  string topic = 1;
}

message DeleteTopicRequest {
  string topic = 1;
}

message ListTopicsRequest {
  string project = 1;
  int32 page_size = 2;
  string page_token = 3;
}

message ListTopicsResponse {
  repeated Topic topics = 1;
  string next_page_token = 2;
}

message ListTopicSubscriptionsRequest {
  string topic = 1;
  int32 page_size = 2;
  string page_token = 3;
}

message ListTopicSubscriptionsResponse {
  repeated string subscriptions = 1;
  string next_page_token = 2;
}

message PublishRequest {
  string topic = 1;
  repeated PubsubMessage messages = 2;
}

message PublishResponse {
  repeated string message_ids = 1;
}

message Subscription {
  string name = 1;
  string topic = 2;
  int32 ack_deadline_seconds = 5;
}

message GetSubscriptionRequest {
  string subscription = 1;
}

message DeleteSubscriptionRequest {
  string subscription = 1;
}

message ListSubscriptionsRequest {
  string project = 1;
  int32 page_size = 2;
  string page_token = 3;
}

message ListSubscriptionsResponse {
  repeated Subscription subscriptions = 1;
  string next_page_token = 2;
}

message ReceivedMessage {
  string ack_id = 1;
  PubsubMessage message = 2;
  int32 delivery_attempt = 3;
}

message PullRequest {
  string subscription = 1;
  int32 max_messages = 3;
}

message PullResponse {
  repeated ReceivedMessage received_messages = 1;
}

message AcknowledgeRequest {
  string subscription = 1;
  repeated string ack_ids = 2;
}

message ModifyAckDeadlineRequest {
  string subscription = 1;
  int32 ack_deadline_seconds = 3;
  repeated string ack_ids = 4;
}

message StreamingPullRequest {
  string subscription = 1;
  repeated string ack_ids = 2;
  repeated int32 modify_deadline_seconds = 3;
  repeated string modify_deadline_ack_ids = 4;
  int32 stream_ack_deadline_seconds = 5;
  string client_id = 6;
  int64 max_outstanding_messages = 7;
  int64 max_outstanding_bytes = 8;
}

message StreamingPullResponse {
  repeated ReceivedMessage received_messages = 1;
}

service Publisher {
  rpc CreateTopic(Topic) returns (Topic);
  rpc GetTopic(GetTopicRequest) returns (Topic);
  rpc ListTopics(ListTopicsRequest) returns (ListTopicsResponse);
  rpc ListTopicSubscriptions(ListTopicSubscriptionsRequest) returns (ListTopicSubscriptionsResponse);
  rpc DeleteTopic(DeleteTopicRequest) returns (google.protobuf.Empty);
  rpc Publish(PublishRequest) returns (PublishResponse);
}

service Subscriber {
  rpc CreateSubscription(Subscription) returns (Subscription);
  rpc GetSubscription(GetSubscriptionRequest) returns (Subscription);
  rpc ListSubscriptions(ListSubscriptionsRequest) returns (ListSubscriptionsResponse);
  rpc DeleteSubscription(DeleteSubscriptionRequest) returns (google.protobuf.Empty);
  rpc ModifyAckDeadline(ModifyAckDeadlineRequest) returns (google.protobuf.Empty);
  rpc Acknowledge(AcknowledgeRequest) returns (google.protobuf.Empty);
  rpc Pull(PullRequest) returns (PullResponse);
  rpc StreamingPull(stream StreamingPullRequest) returns (stream StreamingPullResponse);
}
