[project]
name = "vat"
cap_path = "README.md"
label = "app:vat"

[[workspaces]]
name = "vat"
paths = ["**"]
target = "rust"
test_cmd = "cargo test -p vat"
codegen.profile = "rust/score-crate"

# AW-EC-BEGIN
[aw.ec.generated]
version = 1
project = "vat"
generated_from_td_digest = "sha256:594d991ff8f1de7bb7a6b65b0eba16de5650f1f139590adc0aebf8ed846a0345"

[[aw.ec.generated.cases]]
id = "scenario-failure-keeps-topology-and-logs"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "production-like-integration-scenarios"
contract_id = "production-like-integration-scenarios"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/production-like-integration-scenarios.md#scenario-failure-keeps-topology-and-logs"
test_path = "apps/vat/tests/behavior_scenario_failure_keeps_topology_and_logs.rs"
command = "cargo test -p vat --lib scenario_failure_keeps_topology_and_logs -- --nocapture"
required_for_production = true
assertions = [
    "failing runner forwards its exit code",
    "keep=failed retains the vat directory",
    "vat logs exposes runner output",
    "vat state exposes scenario topology",
]

[[aw.ec.generated.cases]]
id = "scenario-hermetic-requires-http-mock-service"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "production-like-integration-scenarios"
contract_id = "production-like-integration-scenarios"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/production-like-integration-scenarios.md#scenario-hermetic-requires-http-mock-service"
test_path = "apps/vat/tests/behavior_scenario_hermetic_requires_http_mock_service.rs"
command = "cargo test -p vat --lib scenario_hermetic_requires_http_mock_service -- --nocapture"
required_for_production = true
assertions = [
    "hermetic scenario without http-mock exits non-zero",
    "stdout JSONL contains scenario_hermetic_proxy_required",
    "runner command is not executed",
]

[[aw.ec.generated.cases]]
id = "scenario-run-starts-app-dependency-and-runner"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "production-like-integration-scenarios"
contract_id = "production-like-integration-scenarios"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/production-like-integration-scenarios.md#scenario-run-starts-app-dependency-and-runner"
test_path = "apps/vat/tests/behavior_scenario_run_starts_app_dependency_and_runner.rs"
command = "cargo test -p vat --lib scenario_run_starts_app_dependency_and_runner -- --nocapture"
required_for_production = true
assertions = [
    "vat run --scenario prod-like succeeds",
    "app readiness marker exists before runner marker",
    "vat state includes test_run.scenario id/app/runner/services",
    "result JSONL includes scenario and app",
]

[[aw.ec.generated.cases]]
id = "vat-agent-state-and-diff-surface"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "agent-legible-state-and-diff-surface"
contract_id = "agent-legible-state-and-diff-surface"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/external-contracts.md#vat-agent-state-and-diff-surface"
test_path = "apps/vat/tests/behavior_vat_agent_state_and_diff_surface.rs"
command = "rg -n -e 'vat state' -e 'vat diff' -e '--json' -e structured apps/vat/README.md"
required_for_production = true
assertions = [
    "README exposes vat state and vat diff",
    "structured JSON output remains part of the agent-facing contract",
]

[[aw.ec.generated.cases]]
id = "vat-build-container-gated-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "vat-build-dockerfile-build-via-container-cli"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/interfaces/cli/vat-microvm-phase-2-vat-build-dockerfile-build-via-container-cli.md#vat-build-container-gated-smoke"
test_path = "apps/vat/tests/behavior_vat_build_container_gated_smoke.rs"
command = "cargo test -p vat --test vat_build -- --nocapture"
required_for_production = true
assertions = [
    "AC4: build_produces_tagged_image_visible_in_container_image_list (gated on the container_available() skip helper, mirroring vat_cluster.rs's Docker-gated pattern and vat_sandbox_microvm.rs's container-gated tests) writes a minimal, valid Dockerfile to a tempdir, runs vat build against it, and asserts both a successful BuildReport and that `container image list` (singular noun — confirmed correct over the incorrect plural `container images` by the Phase 0 spike #1472) shows the tag.",
    "AC5: the fixture Dockerfile used by this test is a plain, unmodified Dockerfile — vat build never edits, lints, or rewrites the Dockerfile it is given; the same command also succeeds manually against a real, already-existing repo Dockerfile without requiring any edit to it.",
    "Registered in the generated apps/vat/aw.toml EC inventory alongside the container_available() skip helper so `aw ec gen --verify` / `aw health --verify-tests` pick this up as a configured EC-gated test command for the agent-native-gpu-native-dev-containers capability.",
]

[[aw.ec.generated.cases]]
id = "vat-build-lean-and-default-compile"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "vat-build-dockerfile-build-via-container-cli"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/interfaces/cli/vat-microvm-phase-2-vat-build-dockerfile-build-via-container-cli.md#vat-build-lean-and-default-compile"
test_path = "apps/vat/tests/behavior_vat_build_lean_and_default_compile.rs"
command = "cargo build -p vat --no-default-features"
required_for_production = true
assertions = [
    "AC1: `cargo build -p vat` succeeds with commands/build.rs, Cmd::Build, and the two new sandbox/microvm.rs functions (system_up, ensure_system_started) present in the default build.",
    "AC6: apps/vat/Cargo.toml's serde_yaml dependency has no `optional = true` and is absent from the `emulator` feature's dep: list; `cargo build -p vat --no-default-features` still succeeds now that serde_yaml is unconditional (same version 0.9, zero new crate).",
]

[[aw.ec.generated.cases]]
id = "vat-builtin-preset-run-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "built-in-rust-emulators-pub-sub-grpc-firebase-auth-rest"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/built-in-rust-emulators-pub-sub-firebase-auth.md#vat-builtin-preset-run-smoke"
test_path = "apps/vat/tests/behavior_vat_builtin_preset_run_smoke.rs"
command = "cargo test -p vat --test vat_emulator_pubsub -- --nocapture --include-ignored"
required_for_production = true
assertions = ['a `preset = "firebase-auth"` or `preset = "pubsub"` vat.toml run exports FIREBASE_AUTH_EMULATOR_HOST / PUBSUB_EMULATOR_HOST and the runner reaches the emulator; nothing remains after teardown.']

[[aw.ec.generated.cases]]
id = "vat-cli-convention-help-lists-all-three"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "vat-llm-vat-upgrade-vat-issue-mandatory-cli-convention"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/interfaces/cli/vat-upgrade-and-report-issue-subcommands-for-the-mandatory-cli-c.md#vat-cli-convention-help-lists-all-three"
test_path = "apps/vat/tests/behavior_vat_cli_convention_help_lists_all_three.rs"
command = "cargo test -p vat --test vat_cli_convention -- --nocapture"
required_for_production = true
assertions = [
    "`vat --help` output contains `llm`, `upgrade`, and `issue`.",
    "`vat upgrade --check` exits 0 and reports current vs latest without writing the binary (network-permitting; offline it errors cleanly, never panics).",
    "`vat issue create --title X --dry-run` prints a body containing the vat version and OS/arch and submits nothing.",
]

[[aw.ec.generated.cases]]
id = "vat-cli-convention-lean-build"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "vat-llm-vat-upgrade-vat-issue-mandatory-cli-convention"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/interfaces/cli/vat-upgrade-and-report-issue-subcommands-for-the-mandatory-cli-c.md#vat-cli-convention-lean-build"
test_path = "apps/vat/tests/behavior_vat_cli_convention_lean_build.rs"
command = "cargo build -p vat --no-default-features"
required_for_production = true
assertions = ["vat compiles without default features; the self-update HTTPS path is absent but the verbs still parse and the pure helpers remain testable."]

[[aw.ec.generated.cases]]
id = "vat-cli-std-build"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "adopt-the-shared-cli-std-crate"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/interfaces/cli/migrate-upgrade-and-report-issue-to-the-shared-cli-std-crate.md#vat-cli-std-build"
test_path = "apps/vat/tests/behavior_vat_cli_std_build.rs"
command = "cargo build -p vat --no-default-features"
required_for_production = true
assertions = ["vat compiles with and without default features; the hand-rolled modules are gone."]

[[aw.ec.generated.cases]]
id = "vat-cli-std-parity"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "adopt-the-shared-cli-std-crate"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/interfaces/cli/migrate-upgrade-and-report-issue-to-the-shared-cli-std-crate.md#vat-cli-std-parity"
test_path = "apps/vat/tests/behavior_vat_cli_std_parity.rs"
command = "cargo test -p vat --test vat_cli_convention -- --nocapture"
required_for_production = true
assertions = [
    "vat --help lists llm, upgrade, issue.",
    "vat llm --topic outline --format json prints the cli-std JSON shape.",
    "vat issue create --title X --dry-run prints the diagnostics body incl. the vat version and OS.",
    "vat upgrade --check exits cleanly.",
]

[[aw.ec.generated.cases]]
id = "vat-cloud-builtin-preset-run-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "built-in-rust-emulators-cloud-tasks-cloud-scheduler"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/built-in-cloud-tasks-cloud-scheduler-emulators.md#vat-cloud-builtin-preset-run-smoke"
test_path = "apps/vat/tests/behavior_vat_cloud_builtin_preset_run_smoke.rs"
command = "cargo test -p vat --test vat_emulator_tasks -- --nocapture --include-ignored"
required_for_production = true
assertions = ['a `preset = "cloud-tasks"` / `preset = "cloud-scheduler"` vat.toml run exports CLOUD_TASKS_EMULATOR_HOST / CLOUD_SCHEDULER_EMULATOR_HOST and the runner reaches the emulator; nothing remains after teardown.']

[[aw.ec.generated.cases]]
id = "vat-cloud-emulator-lean-build"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "built-in-rust-emulators-cloud-tasks-cloud-scheduler"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/built-in-cloud-tasks-cloud-scheduler-emulators.md#vat-cloud-emulator-lean-build"
test_path = "apps/vat/tests/behavior_vat_cloud_emulator_lean_build.rs"
command = "cargo build -p vat --no-default-features"
required_for_production = true
assertions = ["vat compiles without the emulator feature; the cloud-tasks/cloud-scheduler emulator verbs then error cleanly, never a panic."]

[[aw.ec.generated.cases]]
id = "vat-cloud-grpc-rest-coexist"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "dual-protocol-emulators-cloud-tasks-scheduler-grpc-alongside-rest"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/interfaces/grpc/cloud-tasks-and-cloud-scheduler-emulator-grpc-front-end-alongsid.md#vat-cloud-grpc-rest-coexist"
test_path = "apps/vat/tests/behavior_vat_cloud_grpc_rest_coexist.rs"
command = "cargo test -p vat --test vat_emulator_tasks --test vat_emulator_scheduler -- --nocapture"
required_for_production = true
assertions = ["the existing REST e2e tests pass unchanged (REST + gRPC coexist on one port)."]

[[aw.ec.generated.cases]]
id = "vat-cloud-scheduler-dispatch-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "built-in-rust-emulators-cloud-tasks-cloud-scheduler"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/built-in-cloud-tasks-cloud-scheduler-emulators.md#vat-cloud-scheduler-dispatch-smoke"
test_path = "apps/vat/tests/behavior_vat_cloud_scheduler_dispatch_smoke.rs"
command = "cargo test -p vat --test vat_emulator_scheduler -- --nocapture"
required_for_production = true
assertions = [
    "creating an httpTarget job and calling jobs/{j}:run results in the emulator POSTing to the sink.",
    "no gcloud / Java required.",
]

[[aw.ec.generated.cases]]
id = "vat-cloud-scheduler-grpc-dispatch-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "dual-protocol-emulators-cloud-tasks-scheduler-grpc-alongside-rest"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/interfaces/grpc/cloud-tasks-and-cloud-scheduler-emulator-grpc-front-end-alongsid.md#vat-cloud-scheduler-grpc-dispatch-smoke"
test_path = "apps/vat/tests/behavior_vat_cloud_scheduler_grpc_dispatch_smoke.rs"
command = "cargo test -p vat --test vat_emulator_scheduler_grpc -- --nocapture"
required_for_production = true
assertions = ["a generated google.cloud.scheduler.v1 gRPC client CreateJob + RunJob, and the emulator POSTs the job httpTarget to a local sink."]

[[aw.ec.generated.cases]]
id = "vat-cloud-storage-lean-build"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "built-in-rust-emulator-cloud-storage-gcs"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/built-in-cloud-storage-gcs-emulator.md#vat-cloud-storage-lean-build"
test_path = "apps/vat/tests/behavior_vat_cloud_storage_lean_build.rs"
command = "cargo build -p vat --no-default-features"
required_for_production = true
assertions = ["vat compiles without the emulator feature; the cloud-storage emulator verb then errors cleanly, never a panic."]

[[aw.ec.generated.cases]]
id = "vat-cloud-storage-preset-run-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "built-in-rust-emulator-cloud-storage-gcs"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/built-in-cloud-storage-gcs-emulator.md#vat-cloud-storage-preset-run-smoke"
test_path = "apps/vat/tests/behavior_vat_cloud_storage_preset_run_smoke.rs"
command = "cargo test -p vat --test vat_emulator_storage -- --nocapture --include-ignored"
required_for_production = true
assertions = ["a preset = cloud-storage vat.toml run exports STORAGE_EMULATOR_HOST and the runner uploads then downloads an object byte-identical; nothing remains after teardown."]

[[aw.ec.generated.cases]]
id = "vat-cloud-storage-roundtrip-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "built-in-rust-emulator-cloud-storage-gcs"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/built-in-cloud-storage-gcs-emulator.md#vat-cloud-storage-roundtrip-smoke"
test_path = "apps/vat/tests/behavior_vat_cloud_storage_roundtrip_smoke.rs"
command = "cargo test -p vat --test vat_emulator_storage -- --nocapture"
required_for_production = true
assertions = [
    "a media upload then download (alt=media) returns the same bytes; a multipart upload round-trips; list with a prefix finds the object; delete removes it (404 after).",
    "object names with slashes work; no gcloud / Java / Docker required; the emulator starts in well under a second.",
]

[[aw.ec.generated.cases]]
id = "vat-cloud-tasks-dispatch-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "built-in-rust-emulators-cloud-tasks-cloud-scheduler"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/built-in-cloud-tasks-cloud-scheduler-emulators.md#vat-cloud-tasks-dispatch-smoke"
test_path = "apps/vat/tests/behavior_vat_cloud_tasks_dispatch_smoke.rs"
command = "cargo test -p vat --test vat_emulator_tasks -- --nocapture"
required_for_production = true
assertions = [
    "spawning `vat emulator cloud-tasks`, creating a queue and a task targeting a local sink, results in the emulator POSTing the task body to the sink.",
    "no gcloud / Java required; the emulator starts in well under a second.",
]

[[aw.ec.generated.cases]]
id = "vat-cloud-tasks-grpc-dispatch-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "dual-protocol-emulators-cloud-tasks-scheduler-grpc-alongside-rest"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/interfaces/grpc/cloud-tasks-and-cloud-scheduler-emulator-grpc-front-end-alongsid.md#vat-cloud-tasks-grpc-dispatch-smoke"
test_path = "apps/vat/tests/behavior_vat_cloud_tasks_grpc_dispatch_smoke.rs"
command = "cargo test -p vat --test vat_emulator_tasks_grpc -- --nocapture"
required_for_production = true
assertions = [
    "a generated google.cloud.tasks.v2 gRPC client (insecure channel to the emulator host:port) CreateQueue + CreateTask, and the emulator POSTs the task body to a local sink.",
    "the REST surface on the same port still works.",
]

[[aw.ec.generated.cases]]
id = "vat-cloud-workflows-dispatch-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "built-in-rust-emulator-cloud-workflows-subset-interpreter"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/built-in-cloud-workflows-emulator.md#vat-cloud-workflows-dispatch-smoke"
test_path = "apps/vat/tests/behavior_vat_cloud_workflows_dispatch_smoke.rs"
command = "cargo test -p vat --test vat_emulator_workflows -- --nocapture"
required_for_production = true
assertions = [
    "createWorkflow + createExecution for a workflow that assigns, call: http.post to a local sink, and returns yields a SUCCEEDED execution with the expected result and the sink receives the call.",
    "a try block calling a dead URL falls through to except (no panic); a named subworkflow call returns its value.",
    "no gcloud / Java / Docker required; the emulator starts in well under a second.",
]

[[aw.ec.generated.cases]]
id = "vat-cloud-workflows-lean-build"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "built-in-rust-emulator-cloud-workflows-subset-interpreter"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/built-in-cloud-workflows-emulator.md#vat-cloud-workflows-lean-build"
test_path = "apps/vat/tests/behavior_vat_cloud_workflows_lean_build.rs"
command = "cargo build -p vat --no-default-features"
required_for_production = true
assertions = ["vat compiles without the emulator feature; the cloud-workflows emulator verb then errors cleanly, never a panic."]

[[aw.ec.generated.cases]]
id = "vat-cloud-workflows-orchestrates-sibling"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "built-in-rust-emulator-cloud-workflows-subset-interpreter"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/built-in-cloud-workflows-emulator.md#vat-cloud-workflows-orchestrates-sibling"
test_path = "apps/vat/tests/behavior_vat_cloud_workflows_orchestrates_sibling.rs"
command = "cargo test -p vat --test vat_emulator_workflows -- --nocapture --include-ignored"
required_for_production = true
assertions = ["a vat.toml with preset = cloud-workflows alongside another emulator preset runs a workflow whose http step targets that sibling emulator's exported host, end to end."]

[[aw.ec.generated.cases]]
id = "vat-cluster-backend-unavailable-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "local-kubernetes-cluster-service-and-vat-cluster"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/kind-like-local-kubernetes-clusters.md#vat-cluster-backend-unavailable-smoke"
test_path = "apps/vat/tests/behavior_vat_cluster_backend_unavailable_smoke.rs"
command = "cargo test -p vat --test vat_cluster -- --nocapture"
required_for_production = true
assertions = [
    "a cluster service with no backend on PATH emits a cluster_backend_unavailable JSONL error and a non-zero exit.",
    "vat never panics on the unavailable path.",
]

[[aw.ec.generated.cases]]
id = "vat-cluster-runscoped-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "local-kubernetes-cluster-service-and-vat-cluster"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/kind-like-local-kubernetes-clusters.md#vat-cluster-runscoped-smoke"
test_path = "apps/vat/tests/behavior_vat_cluster_runscoped_smoke.rs"
command = "cargo test -p vat --test vat_cluster -- --nocapture --include-ignored"
required_for_production = true
assertions = [
    "with a real backend and Docker available, vat creates the cluster, the runner reaches the cluster via KUBECONFIG, and vat state shows services[].cluster.backend.",
    "the cluster is deleted at teardown under keep=never; the test skips gracefully when no backend/docker is present.",
]

[[aw.ec.generated.cases]]
id = "vat-cluster-standalone-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "local-kubernetes-cluster-service-and-vat-cluster"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/kind-like-local-kubernetes-clusters.md#vat-cluster-standalone-smoke"
test_path = "apps/vat/tests/behavior_vat_cluster_standalone_smoke.rs"
command = "cargo test -p vat --test vat_cluster -- --nocapture --include-ignored"
required_for_production = true
assertions = [
    "vat cluster create then ls --json lists the cluster, kubeconfig prints a usable path, and delete removes it from the registry and the backend.",
    "the test skips gracefully when no backend/docker is present.",
]

[[aw.ec.generated.cases]]
id = "vat-compose-container-gated-full-cycle"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "vat-compose-bounded-compose-subset-up-down-ps-logs"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-microvm-phase-3-vat-compose-limited-compose-subset-up-down-p.md#vat-compose-container-gated-full-cycle"
test_path = "apps/vat/tests/behavior_vat_compose_container_gated_full_cycle.rs"
command = "cargo test -p vat --test vat_compose -- --nocapture"
required_for_production = true
assertions = [
    "AC5: gated on a container_available() skip helper (mirroring vat_cluster.rs's Docker-gated pattern and vat_sandbox_microvm.rs's container-gated tests): compose up -d against a fixture with one image: service and one build: service, then compose ps reports starting or ready truthfully, compose logs <project> <service> returns non-empty captured output for each, and compose down terminates the backing runner/service processes while retaining project.json as imported metadata ready for retry.",
    "R9: foreground and detached up share one project/token ComposeHandoff; only the token owner publishes the durable VAT id and the parent never performs global VAT-store name/time discovery.",
    "R9: down writes .compose-stop-request and waits for the VAT parent to persist terminal runner/service cleanup before resetting project.json. Runner exit while VAT remains Running projects stopping and retains the binding. Current handoff_protocol: 1 VAT load/read/malformed/missing failure is EvidenceUnavailable, which retains the binding and requests retry rather than terminal reset; only protocol-absent historic JSON plus metadata NotFound may recover. A concurrent up is rejected during that window; runner PID evidence is never used as a direct signal target.",
    "R9: Docker or MicroVM cleanup_error retains the VAT, project binding, and published-port ownership and forces nonzero lifecycle retention. A later down retries only the persisted runtime resource; a failed rm -f releases only after successful bounded exact-name list proof of absence (Docker anchored name filter/exact line, MicroVM parsed JSON/no id).",
    "Registered in the generated apps/vat/aw.toml EC inventory alongside vat_compose_import.rs's pure test, so aw ec gen --verify / aw health --verify-tests pick both up as configured EC-gated test commands for the agent-native-gpu-native-dev-containers capability.",
]

[[aw.ec.generated.cases]]
id = "vat-compose-detached-ownership-and-cleanup"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "microvm-sandbox-backend-for-vat-run"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-microvm-fail-closed-when-published-host-ports-are-unusable.md#vat-compose-detached-ownership-and-cleanup"
test_path = "apps/vat/tests/behavior_vat_compose_detached_ownership_and_cleanup.rs"
command = "cargo test -p vat --test vat_compose -- --nocapture"
required_for_production = true
assertions = [
    "Foreground and detached paths use the same project/token handoff. Only the token owner creates and synchronously publishes the durable VAT id; the parent only rereads the token-owned registry and never polls global VAT-store name/time evidence. handoff_protocol: 1 survives transient PID/token clearing; internal parent/child claim reacquisition waits at most ten seconds while external lifecycle commands remain non-blocking.",
    "A token without a launcher PID expires after its bounded grace period; a stale child cannot overwrite a newer project binding.",
    "down writes the VAT-owned stop request, projects stopping while VAT remains Running with runner/service teardown pending, and treats every current handoff_protocol: 1 metadata load/read/malformed/missing error as EvidenceUnavailable rather than terminal reset. Only a protocol-absent historic record with a separately confirmed metadata NotFound may recover. It waits for Status::Exited plus terminal runner/service state and rejects a concurrent up until cleanup acknowledgement completes.",
    "A persisted cleanup_error retains the VAT, binding, and published-port ownership; retrying down removes the recorded Docker or MicroVM resource and releases only after rm success or successful exact-name list absence proof. The runner or scenario result is nonzero even when keep is never or failed.",
]

[[aw.ec.generated.cases]]
id = "vat-compose-import-pure-fixture-shape"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "vat-compose-bounded-compose-subset-up-down-ps-logs"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-microvm-phase-3-vat-compose-limited-compose-subset-up-down-p.md#vat-compose-import-pure-fixture-shape"
test_path = "apps/vat/tests/behavior_vat_compose_import_pure_fixture_shape.rs"
command = "cargo test -p vat --test vat_compose_import -- --nocapture"
required_for_production = true
assertions = [
    "AC2: image-only expansion-shape assertions over a fixture compose file -- one ServiceConfig per compose service, the synthesized project.up runner with requires listing every service id in expand()'s order, environment injected onto ServiceConfig.image_env, and ports mapped per R2's H:C / bare C rules. These image-only cases run with no container/docker binary on PATH.",
    "R3: one assertion per hard-reject key (deploy, secrets, configs, extends, networks, profiles, healthcheck, command/entrypoint override, bind-mount-form volumes) asserting compose::parse returns the exact error text naming file/service/key.",
    "AC7: feeding a fixture containing deploy: or healthcheck: into vat compose import fails with an error naming the exact file, service, and key.",
]

[[aw.ec.generated.cases]]
id = "vat-compose-mainstream-manual-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "vat-compose-bounded-compose-subset-up-down-ps-logs"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-microvm-phase-3-vat-compose-limited-compose-subset-up-down-p.md#vat-compose-mainstream-manual-smoke"
test_path = "apps/vat/tests/behavior_vat_compose_mainstream_manual_smoke.rs"
command = "vat compose import ./docker-compose.yml && vat compose up -d --project demo && vat compose ps demo && vat compose logs demo web && vat compose down demo"
required_for_production = true
assertions = [
    "AC6: a real, unmodified mainstream docker-compose.yml (one image: service, one build: service, a depends_on entry) succeeds through the full import -> up -d -> ps -> logs -> down cycle, retaining an imported registry ready for another up, and the source compose file itself required no edits.",
    "Not part of the cargo test / generated aw.toml EC gated surface (no CI-portable fixture repo is bundled for this manual smoke); recorded here as the human verification step named by AC6, run once against a developer-supplied compose file during this WI's own close-out.",
]

[[aw.ec.generated.cases]]
id = "vat-compose-runtime-local-build-artifacts"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "compose-runtime-local-build-artifacts"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-microvm-phase-3-vat-compose-limited-compose-subset-up-down-p.md#vat-compose-runtime-local-build-artifacts"
test_path = "apps/vat/tests/behavior_vat_compose_runtime_local_build_artifacts.rs"
command = "cargo test -p vat --test vat_compose_build -- --nocapture"
required_for_production = true
assertions = [
    "#1529: a build-only service resolves short/full build paths relative to the canonical compose source, not the invoking cwd; explicit Dockerfile and deterministically ordered build.args reach the selected builder; generated tags use an OCI-safe readable project/service prefix plus a BLAKE3 raw-pair identity suffix, so normalization or delimiter ambiguity cannot collide.",
    "#1529: auto/native/docker select Docker and MicroVm selects Apple Container; a preflight/build failure occurs before generated vat.toml replacement, preserving a prior materialized import. Image-only compose files remain builder-independent.",
    "#1529: a fresh inactive imported compose up refuses a parseable registry/config service-ID-set mismatch before Docker or Apple Container starts, ignoring service-table order; it accepts a user-edited valid vat.toml when its identity set still matches project.json. Bound or active records bypass this gate for VAT-evidence cleanup, and malformed configs retain vat run's existing parse failure; no full config digest blocks compatible local edits.",
]

[[aw.ec.generated.cases]]
id = "vat-copy-on-write-lifecycle"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "copy-on-write-fork-and-snapshot-lifecycle"
contract_id = "copy-on-write-fork-and-snapshot-lifecycle"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/external-contracts.md#vat-copy-on-write-lifecycle"
test_path = "apps/vat/tests/behavior_vat_copy_on_write_lifecycle.rs"
command = "rg -n -e copy-on-write -e fork -e snapshot -e clonefile -e APFS apps/vat/README.md"
required_for_production = true
assertions = [
    "README preserves copy-on-write lifecycle language",
    "fork, snapshot, clonefile, and APFS remain visible contract terms",
]

[[aw.ec.generated.cases]]
id = "vat-docker-build-json-fake"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "headless-docker-command-shim"
contract_id = "vat-docker-build-json-fake"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-headless-docker-command-shim.md#vat-docker-build-json-fake"
test_path = "apps/vat/tests/behavior_vat_docker_build_json_fake.rs"
command = "cargo test -p vat --test vat_docker_shim docker_build_json -- --nocapture"
required_for_production = true
assertions = [
    "The deterministic contract accepts only direct build with exact format/1..=1200-timeout/tag selectors, documented optional fields before one local-directory context, and maps only supported non-selector fields to public container build argv.",
    "It emits one bounded vat.docker.build.v1/vat_json receipt after normal client completion or child nonzero, retains image lifecycle with no product auto-cleanup, safely inspects only a success, and emits no receipt on timeout/setup/capture failure.",
    "Current validation: cargo check passed; docker_shim lib 62/62; focused build suite 4 plus 1 ignored (63 filtered); native_image_owner_guard 1/1 (67 filtered). It does not claim Engine/API, provenance, ownership, readiness, security, secret-redaction, cancellation, or rollback.",
]

[[aw.ec.generated.cases]]
id = "vat-docker-build-json-real-host"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "headless-docker-command-shim"
contract_id = "vat-docker-build-json-real-host"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-headless-docker-command-shim.md#vat-docker-build-json-real-host"
test_path = "apps/vat/tests/behavior_vat_docker_build_json_real_host.rs"
command = "RUST_TEST_THREADS=1 VAT_DOCKER_BUILD_JSON_E2E_REQUIRED=1 cargo test -p vat --test vat_docker_shim apple_container_docker_build_json_receipt_contract -- --ignored --nocapture"
required_for_production = true
assertions = [
    "Passed 1/1 (67 filtered) in 2.53 seconds. The opt-in probe proves one strict Docker-build receipt and records public Apple argv with JSON/deadline selectors stripped.",
    "Its high-entropy test tag and exact io.cclab.vat.e2e-owner label require exact native absence before build, exact label recheck before delete, and exact native absence after. This is test-only cleanup safety, not product behavior: Apple has no conditional build/delete, races are best effort and ambiguity leaks rather than authorizing cleanup.",
    "The receipt remains retained_no_auto_cleanup; this bounded host proof does not establish generic build correctness, Docker Engine/API, provenance, ownership, security, redaction, cancellation, or rollback.",
]

[[aw.ec.generated.cases]]
id = "vat-docker-compose-bounded-wait-fake-lifecycle"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "headless-docker-command-shim"
contract_id = "vat-docker-compose-bounded-wait-fake-lifecycle"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-headless-docker-command-shim.md#vat-docker-compose-bounded-wait-fake-lifecycle"
test_path = "apps/vat/tests/behavior_vat_docker_compose_bounded_wait_fake_lifecycle.rs"
command = "cargo test -p vat --test vat_docker_shim -- --nocapture"
required_for_production = true
assertions = [
    "The passed deterministic fake suite covers ready, timeout, later recovery/down cleanup, and down/re-import/relaunch replacement races for docker compose up -d --wait.",
    "It proves one final ready up JSON with topology, timeout runtime/registry retention, target-pinned safe ps handoff only after current observation, and no unsafe next for terminal/replaced/bare deadlines; degraded exposes no endpoint.",
    "The corresponding opt-in real Apple Container dual-service E2E is passed on this host; the fake suite remains the deterministic coverage for timeout/recovery/replacement races.",
]

[[aw.ec.generated.cases]]
id = "vat-docker-compose-host-facing-independent-fake-lifecycle"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "headless-docker-command-shim"
contract_id = "vat-docker-compose-host-facing-independent-fake-lifecycle"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-headless-docker-command-shim.md#vat-docker-compose-host-facing-independent-fake-lifecycle"
test_path = "apps/vat/tests/behavior_vat_docker_compose_host_facing_independent_fake_lifecycle.rs"
command = "cargo test -p vat --test vat_docker_shim compose_host_facing_independent_profile_runs_two_services_through_the_shim -- --nocapture"
required_for_production = true
assertions = [
    "A deterministic fake runtime starts two literal-image services selected by the exact host-facing-independent-v1 marker, with two distinct loopback host ports.",
    "The successful up JSON exposes profile=host-facing-independent-v1, service_name_dns=false, and host_loopback_only=true; exact no-argument ps preserves that known profile and adds ready topology in registered docs/inspector order with canonical loopback endpoints.",
    "A typed degraded ps omits every endpoint rather than null-filling or leaking a partial topology; ps --format is unsupported and topology is not an app-healthcheck.",
    "This deterministic fixture complements the opt-in real Apple Container dual-service E2E; it does not widen that passed host evidence to service-name DNS, general Compose, Docker Engine API, or Kubernetes.",
]

[[aw.ec.generated.cases]]
id = "vat-docker-compose-host-facing-independent-real-host"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "headless-docker-command-shim"
contract_id = "vat-docker-compose-host-facing-independent-real-host"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-headless-docker-command-shim.md#vat-docker-compose-host-facing-independent-real-host"
test_path = "apps/vat/tests/behavior_vat_docker_compose_host_facing_independent_real_host.rs"
command = "RUST_TEST_THREADS=1 VAT_DOCKER_COMPOSE_INDEPENDENT_SHIM_E2E_REQUIRED=1 cargo test -p vat --test vat_docker_shim apple_container_docker_compose_host_facing_independent_profile_contract -- --ignored --nocapture"
required_for_production = true
assertions = [
    "Passed 1/1 (50 filtered) on this host in 4.54 seconds.",
    "The opt-in gated Apple Container test proves host-facing-independent-v1 up -d --wait, both loopback endpoints, one-document JSON ps/logs/exec, text logs, text exec including a no-final-newline handoff, and down cleanup of exact containers, ports, and registry.",
    "It remains evidence for this bounded profile only, not service-name DNS, general Compose, a Docker Engine API, or Kubernetes.",
]

[[aw.ec.generated.cases]]
id = "vat-docker-compose-strict-build-real-host"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "headless-docker-command-shim"
contract_id = "vat-docker-compose-strict-build-real-host"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-headless-docker-command-shim.md#vat-docker-compose-strict-build-real-host"
test_path = "apps/vat/tests/behavior_vat_docker_compose_strict_build_real_host.rs"
command = "VAT_DOCKER_COMPOSE_BUILD_SHIM_E2E_REQUIRED=1 cargo test -p vat --test vat_docker_shim apple_container_docker_compose_strict_build_profile_contract -- --ignored --nocapture"
required_for_production = true
assertions = [
    "A one-service short-build Compose file builds an exact project-scoped image in Apple Container, starts it through the installed docker shim, and exposes host HTTP.",
    "ps, service-name exec -T, and logs operate over the built MicroVM service without exposing its generated Apple Container name.",
    "The public source-build up result exposes its exact image and cleanup_next; executing cleanup_next removes the exact service, host port, and image without VAT_HOME inspection or a shared-store prune.",
]

[[aw.ec.generated.cases]]
id = "vat-docker-compose-strict-real-host"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "headless-docker-command-shim"
contract_id = "vat-docker-compose-strict-real-host"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-headless-docker-command-shim.md#vat-docker-compose-strict-real-host"
test_path = "apps/vat/tests/behavior_vat_docker_compose_strict_real_host.rs"
command = "VAT_DOCKER_COMPOSE_SHIM_E2E_REQUIRED=1 cargo test -p vat --test vat_docker_shim apple_container_docker_compose_strict_profile_contract -- --ignored --nocapture"
required_for_production = true
assertions = [
    "A strict-single-image-v1 one-service literal nginx Compose file starts via the installed docker shim and emits the agent-facing Compose result.",
    "ps, published host HTTP, logs, and strict service-name exec -T succeed through VAT's MicroVM lifecycle without exposing the generated Apple Container name.",
    "A failing exec preserves its exact child exit code and emits a structured failure result with a runnable ps next.",
    "down emits terminal cleaned_up, removes the exact Apple Container service, and releases the published host port.",
]

[[aw.ec.generated.cases]]
id = "vat-docker-direct-json-real-host"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "headless-docker-command-shim"
contract_id = "vat-docker-direct-json-real-host"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-headless-docker-command-shim.md#vat-docker-direct-json-real-host"
test_path = "apps/vat/tests/behavior_vat_docker_direct_json_real_host.rs"
command = "VAT_DOCKER_SHIM_E2E_REQUIRED=1 cargo test -p vat --test vat_docker_shim apple_container_docker_run_published_port_contract -- --ignored --nocapture"
required_for_production = true
assertions = [
    "Passed 1/1 (50 filtered) on Apple Container 1.1.0. The gate uses a high-entropy nonce+PID temporary nginx container and verifies io.cclab.vat.e2e-owner=<token> by inspect before cleanup. Exact-label rechecks are conservative best-effort precautions, and the emergency guard retains the container on uncertainty. Apple Container has no atomic conditional delete, so this does not claim race-free cleanup or that a misdelete is impossible; the shared/cacheable nginx image is not cleaned up.",
    "Ps/images are global read-only inventory smoke observations; inspect/stats/VAT logs and direct exec target the temporary owner-labeled nginx container. The host smoke proves one valid native JSON document or VAT wrapper only, including an exec wrapper with both stdout and stderr markers.",
    "Fake/unit tests prove byte-preservation and fail-closed details. This remains bounded direct-command evidence only: it does not establish guest-timeout termination, Docker Engine parity, generic runtime behavior, Docker multiplex/demux, broader Compose semantics, immutable image identity, or image cleanup.",
]

[[aw.ec.generated.cases]]
id = "vat-docker-exec-json-fake"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "headless-docker-command-shim"
contract_id = "vat-docker-exec-json-fake"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-headless-docker-command-shim.md#vat-docker-exec-json-fake"
test_path = "apps/vat/tests/behavior_vat_docker_exec_json_fake.rs"
command = "RUST_TEST_THREADS=1 cargo test -p vat --test vat_docker_shim docker_exec_json -- --nocapture"
required_for_production = true
assertions = [
    "The deterministic fake contract accepts only direct/container exec JSON with one format and one 1..=1200 timeout before a safe id, a literal Docker-facing delimiter, and a nonempty raw command; raw/unformatted exec remains generic.",
    "VAT removes the Docker-only delimiter and normalizes to Apple `container exec CONTAINER COMMAND [ARG...]`, then emits one `vat.docker.exec.v1` / `vat_json` wrapper with separate serialized-64-KiB-capped suffixes, `timeout_scope=host-container-client-observation`, and a safe inspect next.",
    "Ordinary child failure preserves wrapper+exit; timeout or setup/capture failure emits no partial wrapper. Deterministic validation passed: docker_shim library 54/54 and focused docker_exec_json 4/4. The host timeout does not claim guest command termination; no Docker Engine parity, generic runtime, Compose, or Kubernetes claim follows.",
]

[[aw.ec.generated.cases]]
id = "vat-docker-image-inspect-json-fake"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "headless-docker-command-shim"
contract_id = "vat-docker-image-inspect-json-fake"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-headless-docker-command-shim.md#vat-docker-image-inspect-json-fake"
test_path = "apps/vat/tests/behavior_vat_docker_image_inspect_json_fake.rs"
command = "RUST_TEST_THREADS=1 cargo test -p vat --test vat_docker_shim docker_image_inspect_json -- --nocapture"
required_for_production = true
assertions = [
    "The deterministic fake contract accepts only direct docker image inspect JSON with one selector before one safe opaque IMAGE, strips the selector, invokes only container image inspect IMAGE, and byte-for-byte replays one validated Apple-native JSON document.",
    "Templates, --, extra references, and every other option fail before Apple Container; a valid native document with a nonzero child exit preserves that status, while malformed, oversized, or escaped-pipe capture suppresses raw stdout under five-second bounded isolated cleanup.",
    "Recorded validation: cargo check passed; canonical cargo test -p vat --lib docker_shim -- --nocapture passed 58/58; this focused integration passed 4/4 with 1 ignored. It does not claim Docker image-inspect schema/templates/Engine API, provenance, security, registry, build-completion, readiness, or secret redaction.",
]

[[aw.ec.generated.cases]]
id = "vat-docker-image-inspect-json-real-host"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "headless-docker-command-shim"
contract_id = "vat-docker-image-inspect-json-real-host"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-headless-docker-command-shim.md#vat-docker-image-inspect-json-real-host"
test_path = "apps/vat/tests/behavior_vat_docker_image_inspect_json_real_host.rs"
command = "RUST_TEST_THREADS=1 VAT_DOCKER_IMAGE_INSPECT_JSON_E2E_REQUIRED=1 cargo test -p vat --test vat_docker_shim apple_container_docker_image_inspect_json_contract -- --ignored --nocapture"
required_for_production = true
assertions = [
    "Passed 1/1 (61 filtered) in 1.21 seconds. The host probe validates one Apple-native JSON document for alpine:3.20 and records that VAT stripped the Docker selector before invoking only container image inspect alpine:3.20.",
    "It is bounded direct-image observation only: it does not establish Docker image-inspect schema/template/Engine API behavior, provenance, security, registry, build-completion, readiness, or secret-redaction properties.",
]

[[aw.ec.generated.cases]]
id = "vat-docker-images-json-fake"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "headless-docker-command-shim"
contract_id = "vat-docker-images-json-fake"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-headless-docker-command-shim.md#vat-docker-images-json-fake"
test_path = "apps/vat/tests/behavior_vat_docker_images_json_fake.rs"
command = "RUST_TEST_THREADS=1 cargo test -p vat --test vat_docker_shim docker_images_json -- --nocapture"
required_for_production = true
assertions = [
    "The deterministic fake contract accepts direct docker images JSON and only the documented image ls/list aliases, normalizes to canonical Apple Container image-list argv, and byte-for-byte replays one validated opaque native JSON value.",
    "Template/table/YAML/TOML output, filters, quiet, verbose, all, digests, no-trunc, positionals, duplicates, unknown flags, and -- fail before runtime; malformed, oversized, or escaped-pipe stdout fails closed under the five-second bounded isolated cleanup while text/quiet image listing remains inherited.",
    "Recorded validation: cargo check without default features passed; shared docker_shim library passed 54/54; focused docker_images_json integration passed 4/4. The full serial fake-shim aggregate is intentionally not recorded because an independent serial run exposed a nondeterministic pre-existing Compose JSON logs timing race. Direct real-host observation passed 1/1 (50 filtered) on Apple Container 1.1.0; images is a global read-only inventory smoke observation, not a targeted ownership result. It proves one valid native JSON document only; fake/unit tests prove byte-preservation and fail-closed details.",
]

[[aw.ec.generated.cases]]
id = "vat-docker-inspect-json-fake"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "headless-docker-command-shim"
contract_id = "vat-docker-inspect-json-fake"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-headless-docker-command-shim.md#vat-docker-inspect-json-fake"
test_path = "apps/vat/tests/behavior_vat_docker_inspect_json_fake.rs"
command = "RUST_TEST_THREADS=1 cargo test -p vat --test vat_docker_shim docker_inspect -- --nocapture"
required_for_production = true
assertions = [
    "The deterministic fake contract accepts direct docker inspect JSON and only the documented container inspect alias, strips the VAT-only selector, invokes canonical Apple Container inspect argv, and byte-for-byte replays one validated opaque native JSON value.",
    "--type, --size, templates/table/YAML/TOML, filters, a second id, --, and unknown flags fail before runtime; unformatted inspect remains inherited, valid JSON plus a nonzero child exit preserves status, and malformed, oversized, or flood output suppresses raw stdout under five-second bounded isolated cleanup.",
    "It is not Docker Engine inspect schema, ownership/provenance/security/image/registry/build-status, health/readiness/liveness/port-reachability evidence, or a secret-redaction guarantee. Recorded validation: cargo check without default features passed; shared docker_shim library passed 54/54; focused docker_inspect integration passed 5/5. The full serial fake-shim aggregate is intentionally not recorded because an independent serial run exposed a nondeterministic pre-existing Compose JSON logs timing race. Direct real-host observation passed 1/1 (50 filtered) on Apple Container 1.1.0; inspect targets the temporary owner-labeled nginx container and proves one valid native JSON document only. Fake/unit tests prove byte-preservation and fail-closed details.",
]

[[aw.ec.generated.cases]]
id = "vat-docker-logs-json-fake"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "headless-docker-command-shim"
contract_id = "vat-docker-logs-json-fake"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-headless-docker-command-shim.md#vat-docker-logs-json-fake"
test_path = "apps/vat/tests/behavior_vat_docker_logs_json_fake.rs"
command = "RUST_TEST_THREADS=1 cargo test -p vat --test vat_docker_shim docker_logs_json -- --nocapture"
required_for_production = true
assertions = [
    "The deterministic fake contract accepts direct/container logs only with exact format plus bounded tail selectors before one final safe id, strips the selector, invokes canonical Apple logs argv, and emits one VAT vat.docker.logs.v1/vat_json wrapper rather than a sixth native JSON or Docker multiplex/demux schema.",
    "The wrapper carries untrusted Apple stdio, bounded diagnostic stderr, truncation/lossy flags, backend/container/requested tail/runtime/child outcome, and safe inspect next. Ordinary child failure retains wrapper+exit; follow/boot/timestamps/since/until/templates and all other modifiers reject before runtime; timeout/setup/escaped-pipe paths emit no partial wrapper after five-second plus one-second bounded cleanup with dual-stream suffix/serialized caps.",
    "Recorded validation: cargo check without default features passed; canonical cargo test -p vat --lib docker_shim -- --nocapture passed 54/54; focused docker_logs_json integration passed 6/6. The full serial fake-shim aggregate is intentionally not recorded because an independent serial run exposed a nondeterministic pre-existing Compose JSON logs timing race. Direct real-host observation passed 1/1 (50 filtered) on Apple Container 1.1.0; VAT logs targets the high-entropy nonce+PID owner-labeled temporary nginx container and proves one VAT wrapper only. Exact-label rechecks are conservative best-effort precautions, the emergency guard retains on uncertainty, and Apple Container has no atomic conditional delete; this is not a race-free or impossible-to-misdelete cleanup guarantee. No shared nginx image cleanup is claimed. Fake/unit tests prove byte-preservation and fail-closed details.",
]

[[aw.ec.generated.cases]]
id = "vat-docker-ps-json-fake"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "headless-docker-command-shim"
contract_id = "vat-docker-ps-json-fake"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-headless-docker-command-shim.md#vat-docker-ps-json-fake"
test_path = "apps/vat/tests/behavior_vat_docker_ps_json_fake.rs"
command = "RUST_TEST_THREADS=1 cargo test -p vat --test vat_docker_shim docker_ps_json -- --nocapture"
required_for_production = true
assertions = [
    "The deterministic fake contract accepts direct docker ps JSON and only the documented container ls/list aliases, normalizes to canonical Apple Container list argv, and byte-for-byte replays one validated opaque native JSON value.",
    "Templates/table output, filters, quiet plus JSON, duplicate/unknown flags, positionals, and docker container ps JSON fail before runtime; malformed, oversized, or escaped-pipe stdout fails closed under the five-second bounded isolated cleanup.",
    "Recorded validation: cargo check without default features passed; shared docker_shim library passed 54/54; focused direct ps integration passed 4/4. The full serial fake-shim aggregate is intentionally not recorded because an independent serial run exposed a nondeterministic pre-existing Compose JSON logs timing race. Direct real-host observation passed 1/1 (50 filtered) on Apple Container 1.1.0; ps is a global read-only inventory smoke observation, not a targeted ownership result. It proves one valid native JSON document only; fake/unit tests prove byte-preservation and fail-closed details.",
]

[[aw.ec.generated.cases]]
id = "vat-docker-pull-json-fake"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "headless-docker-command-shim"
contract_id = "vat-docker-pull-json-fake"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-headless-docker-command-shim.md#vat-docker-pull-json-fake"
test_path = "apps/vat/tests/behavior_vat_docker_pull_json_fake.rs"
command = "cargo test -p vat --test vat_docker_shim docker_pull_json -- --nocapture"
required_for_production = true
assertions = [
    "The deterministic contract accepts only direct pull with exact format/1..=1200-timeout selectors before one opaque image reference; it rejects empty/leading-dash/whitespace-control/URL-style `://`/leading Git-style `git@` remote forms while keeping ordinary OCI `@digest` opaque, retains raw unselected pull and docker image pull behavior, and maps only a selector-stripped request to public container image pull argv.",
    "It emits one bounded vat.docker.pull.v1/vat_json receipt only after normal client completion or child nonzero, marks the image not_owned_no_auto_cleanup with no registry management, safely inspects only a success, and emits no receipt on timeout/setup/capture/pipe failure.",
    "Current validation: cargo check passed; docker_shim lib 65/65; focused pull suite 5 plus 1 ignored (68 filtered). It does not claim Engine/API, registry auth lifecycle, provenance, digest, platform, freshness, image state, ownership, security, secret redaction, cancellation, download completion, or rollback.",
]

[[aw.ec.generated.cases]]
id = "vat-docker-pull-json-real-host"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "headless-docker-command-shim"
contract_id = "vat-docker-pull-json-real-host"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-headless-docker-command-shim.md#vat-docker-pull-json-real-host"
test_path = "apps/vat/tests/behavior_vat_docker_pull_json_real_host.rs"
command = "RUST_TEST_THREADS=1 VAT_DOCKER_PULL_JSON_E2E_REQUIRED=1 cargo test -p vat --test vat_docker_shim apple_container_docker_pull_json_receipt_contract -- --ignored --nocapture"
required_for_production = true
assertions = [
    "Passed 1/1 (73 filtered) in 27.14 seconds. The opt-in probe proves one strict Docker-pull receipt and records public Apple `container image pull alpine:3.20` argv with JSON/deadline selectors stripped.",
    "The E2E deliberately uses a shared/cacheable alpine image but still runs the real pull client: it neither deletes that image nor asserts ownership on success or failure. It can contact a registry or alter shared image state, so it is bounded receipt evidence rather than transfer, image-state, registry-auth, or cleanup proof.",
    "The receipt remains not_owned_no_auto_cleanup; this host proof does not establish Docker Engine/API, registry management/auth lifecycle, provenance, digest, platform, freshness, security, secret redaction, cancellation, download completion, or rollback.",
]

[[aw.ec.generated.cases]]
id = "vat-docker-run-json-fake"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "headless-docker-command-shim"
contract_id = "vat-docker-run-json-fake"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-headless-docker-command-shim.md#vat-docker-run-json-fake"
test_path = "apps/vat/tests/behavior_vat_docker_run_json_fake.rs"
command = "RUST_TEST_THREADS=1 cargo test -p vat --test vat_docker_shim docker_run_json -- --nocapture"
required_for_production = true
assertions = [
    "The deterministic contract accepts only direct foreground docker run JSON with flexible-order format/timeout selectors before IMAGE, rejects every caller lifecycle/network/mount/env option before Apple Container, and creates a generated high-entropy name plus independent owner label.",
    "It emits one vat.docker.run.v1/vat_json document with bounded stdout/stderr only after exact owner-label cleanup confirms absence; ordinary child nonzero retains wrapper+exit, while timeout/setup/cleanup uncertainty emits no partial wrapper and only Apple's explicit not-found diagnostic counts as absence.",
    "Passed 5 plus 1 ignored in 1.80s. The host timeout is not guest-wide termination, and this makes no crash-recovery, Docker Engine parity, or secret-redaction claim.",
]

[[aw.ec.generated.cases]]
id = "vat-docker-run-json-real-host"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "headless-docker-command-shim"
contract_id = "vat-docker-run-json-real-host"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-headless-docker-command-shim.md#vat-docker-run-json-real-host"
test_path = "apps/vat/tests/behavior_vat_docker_run_json_real_host.rs"
command = "RUST_TEST_THREADS=1 VAT_DOCKER_RUN_JSON_E2E_REQUIRED=1 cargo test -p vat --test vat_docker_shim apple_container_docker_run_json_ephemeral_contract -- --ignored --nocapture"
required_for_production = true
assertions = [
    "Passed 1/1 (56 filtered) in 2.30s using local alpine:3.20: one foreground JSON document carries stdout/stderr markers and exact generated-container cleanup confirms absent after the run.",
    "The evidence is bounded to that owner-cleaned one-shot and does not establish guest-wide timeout termination, crash recovery, Docker Engine parity, or secret redaction.",
]

[[aw.ec.generated.cases]]
id = "vat-docker-shim-real-host"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "headless-docker-command-shim"
contract_id = "vat-docker-shim-real-host"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-headless-docker-command-shim.md#vat-docker-shim-real-host"
test_path = "apps/vat/tests/behavior_vat_docker_shim_real_host.rs"
command = "VAT_DOCKER_SHIM_E2E_REQUIRED=1 cargo test -p vat --test vat_docker_shim -- --ignored --nocapture"
required_for_production = true
assertions = [
    "A temporary shim builds a test-scoped Dockerfile image and removes it through docker image rm.",
    "A high-entropy nonce+PID owner-labeled temporary nginx docker run -d -p host:80 returns host HTTP 2xx and supports inspect/logs. Exact-label rechecks are conservative best-effort precautions and the emergency guard retains on uncertainty; Apple Container has no atomic conditional delete, so no race-free or impossible-to-misdelete cleanup claim is made.",
    "The shared/cacheable nginx image may remain; no image cleanup is claimed.",
]

[[aw.ec.generated.cases]]
id = "vat-docker-stats-fake"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "headless-docker-command-shim"
contract_id = "vat-docker-stats-fake"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-headless-docker-command-shim.md#vat-docker-stats-fake"
test_path = "apps/vat/tests/behavior_vat_docker_stats_fake.rs"
command = "cargo test -p vat --test vat_docker_shim docker_stats -- --nocapture"
required_for_production = true
assertions = [
    "The deterministic fake contract accepts only strict non-streaming native-JSON stats, invokes canonical Apple Container argv, preserves valid opaque native JSON and child nonzero exits, and suppresses malformed/oversized stdout.",
    "A five-second bounded observation plus isolated process-group cleanup replays stdout only after complete validated capture; an escaped pipe holder fails closed. It does not prove ownership, health, liveness, or a Docker Engine schema.",
    "Recorded validation: shared docker_shim library coverage passed 54/54. The full serial fake-shim aggregate is intentionally not recorded because an independent serial run exposed a nondeterministic pre-existing Compose JSON logs timing race. Direct real-host observation passed 1/1 (50 filtered) on Apple Container 1.1.0; stats targets the temporary owner-labeled nginx container and proves one valid native JSON document only. Fake/unit tests prove byte-preservation and fail-closed details.",
]

[[aw.ec.generated.cases]]
id = "vat-doctor-selected-plan-docker-probe"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "microvm-sandbox-backend-for-vat-run"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-microvm-phase-1-isolation-microvm-sandbox-backend-for-vat-ru.md#vat-doctor-selected-plan-docker-probe"
test_path = "apps/vat/tests/behavior_vat_doctor_selected_plan_docker_probe.rs"
command = "cargo test -p vat --test vat_toml_runner -- --nocapture"
required_for_production = true
assertions = [
    "A fake explicit MicroVm image/preset plan with an unselected Docker service invokes exactly one read-only container system status probe per invocation plus bounded read-only builder advisory probes, never Docker even when Docker is on PATH, maps services.docker_services to not_probed, and emits docker.daemon_probe.state=skipped with the selected-plan reason as provenance rather than unavailable evidence; daemon=false has no unavailable meaning because no Docker command ran.",
    "The builder advisory records shared_unknown ownership, automatic_cleanup=false, separate configuration/observed stats/global disk, and nonfatal unknown/timeout/probe errors without lifecycle mutation or a change to doctor runtime success; a real state is reported only when supported by the installed CLI.",
    "An unsupported selected MicroVm preset without a declared OCI route fails closed without Docker fallback. A selected Docker runtime, Auto image, eligible Auto preset fallback, or cluster retains normal Docker probing; a selected cluster requires Docker. Doctor never autostarts Apple Container or changes runtime through fallback.",
    "Recorded implementation validation: `cargo test -p vat --test vat_toml_runner -- --nocapture` passed 26/26; `cargo test -p vat --lib sandbox::microvm::tests -- --nocapture` passed 7/7.",
]

[[aw.ec.generated.cases]]
id = "vat-emulator-auth-rest-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "built-in-rust-emulators-pub-sub-grpc-firebase-auth-rest"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/built-in-rust-emulators-pub-sub-firebase-auth.md#vat-emulator-auth-rest-smoke"
test_path = "apps/vat/tests/behavior_vat_emulator_auth_rest_smoke.rs"
command = "cargo test -p vat --test vat_emulator_auth -- --nocapture"
required_for_production = true
assertions = [
    "spawning `vat emulator firebase-auth` and driving signUp -> signInWithPassword -> lookup over HTTP returns a JWT idToken and the created user.",
    "no Java / firebase-tools required; the emulator starts in well under a second.",
]

[[aw.ec.generated.cases]]
id = "vat-emulator-lean-build"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "built-in-rust-emulators-pub-sub-grpc-firebase-auth-rest"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/built-in-rust-emulators-pub-sub-firebase-auth.md#vat-emulator-lean-build"
test_path = "apps/vat/tests/behavior_vat_emulator_lean_build.rs"
command = "cargo build -p vat --no-default-features"
required_for_production = true
assertions = ["vat compiles without the emulator feature; `vat emulator ...` then exits non-zero with a clear 'built without emulator feature' message, never a panic."]

[[aw.ec.generated.cases]]
id = "vat-emulator-pubsub-grpc-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "built-in-rust-emulators-pub-sub-grpc-firebase-auth-rest"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/built-in-rust-emulators-pub-sub-firebase-auth.md#vat-emulator-pubsub-grpc-smoke"
test_path = "apps/vat/tests/behavior_vat_emulator_pubsub_grpc_smoke.rs"
command = "cargo test -p vat --test vat_emulator_pubsub -- --nocapture"
required_for_production = true
assertions = [
    "a tonic client generated from the same proto can CreateTopic -> CreateSubscription -> Publish -> Pull -> Acknowledge against `vat emulator pubsub`.",
    "no gcloud / Java required.",
]

[[aw.ec.generated.cases]]
id = "vat-emulator-unavailable-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "gcp-firebase-emulator-service-presets"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/gcp-firebase-emulator-service-presets.md#vat-emulator-unavailable-smoke"
test_path = "apps/vat/tests/behavior_vat_emulator_unavailable_smoke.rs"
command = "cargo test -p vat --test vat_emulators -- --nocapture"
required_for_production = true
assertions = [
    "a firestore preset with an empty PATH (no gcloud, no docker) emits a structured service_runtime_unavailable JSONL error and a non-zero exit.",
    "vat never panics on the unavailable path.",
]

[[aw.ec.generated.cases]]
id = "vat-firebase-bundle-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "gcp-firebase-emulator-service-presets"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/gcp-firebase-emulator-service-presets.md#vat-firebase-bundle-smoke"
test_path = "apps/vat/tests/behavior_vat_firebase_bundle_smoke.rs"
command = "cargo test -p vat --test vat_emulators -- --nocapture --include-ignored"
required_for_production = true
assertions = [
    "a firebase preset with a firebase.json starts the suite and exports the configured *_EMULATOR_HOST vars.",
    "the test skips gracefully when firebase-tools and docker are both unavailable.",
]

[[aw.ec.generated.cases]]
id = "vat-firestore-native-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "gcp-firebase-emulator-service-presets"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/gcp-firebase-emulator-service-presets.md#vat-firestore-native-smoke"
test_path = "apps/vat/tests/behavior_vat_firestore_native_smoke.rs"
command = "cargo test -p vat --test vat_emulators -- --nocapture --include-ignored"
required_for_production = true
assertions = [
    "with gcloud + Java + the firestore component, vat starts the emulator, the runner sees FIRESTORE_EMULATOR_HOST, and vat state shows the service ready with the var in exported_env.",
    "the test skips gracefully when the native firestore emulator is unavailable.",
]

[[aw.ec.generated.cases]]
id = "vat-grpc-mitm-build"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "network-sandbox-v2-transparent-grpc-routing-h2-mitm"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/interfaces/grpc/vat-network-sandbox-v2-transparent-grpc-routing-via-h2-mitm-reve.md#vat-grpc-mitm-build"
test_path = "apps/vat/tests/behavior_vat_grpc_mitm_build.rs"
command = "cargo build -p vat --no-default-features"
required_for_production = true
assertions = ["vat compiles with and without default features; hyper has the http2 feature explicitly."]

[[aw.ec.generated.cases]]
id = "vat-grpc-mitm-routing-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "network-sandbox-v2-transparent-grpc-routing-h2-mitm"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/interfaces/grpc/vat-network-sandbox-v2-transparent-grpc-routing-via-h2-mitm-reve.md#vat-grpc-mitm-routing-smoke"
test_path = "apps/vat/tests/behavior_vat_grpc_mitm_routing_smoke.rs"
command = "cargo test -p vat --test vat_emulator_grpc_mitm_routing -- --nocapture"
required_for_production = true
assertions = [
    "a generated google.cloud.tasks.v2 gRPC client over TLS to cloudtasks.googleapis.com (trusting vat's CA), routed through the proxy, CreateQueue + CreateTask succeeds and the task is dispatched to a local sink.",
    "HTTP routing (#503) and unmatched-host forwarding still work (no regression).",
]

[[aw.ec.generated.cases]]
id = "vat-grpc-pool-build"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "grpc-reverse-proxy-h2c-connection-pool"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/interfaces/grpc/pool-the-grpc-mitm-reverse-proxy-h2c-upstream-connections.md#vat-grpc-pool-build"
test_path = "apps/vat/tests/behavior_vat_grpc_pool_build.rs"
command = "cargo build -p vat --no-default-features"
required_for_production = true
assertions = ["vat compiles with and without default features."]

[[aw.ec.generated.cases]]
id = "vat-grpc-pool-reuse-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "grpc-reverse-proxy-h2c-connection-pool"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/interfaces/grpc/pool-the-grpc-mitm-reverse-proxy-h2c-upstream-connections.md#vat-grpc-pool-reuse-smoke"
test_path = "apps/vat/tests/behavior_vat_grpc_pool_reuse_smoke.rs"
command = "cargo test -p vat --test vat_emulator_grpc_mitm_routing -- --nocapture"
required_for_production = true
assertions = ["two sequential gRPC calls through the MITM to the same emulator both succeed (reuse path), and the #509 single-call e2e still passes."]

[[aw.ec.generated.cases]]
id = "vat-headless-ephemeral-k8s-bootstrap-diagnostics"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "headless-ephemeral-kubernetes-session"
contract_id = "vat-headless-ephemeral-k8s-bootstrap-diagnostics"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-headless-ephemeral-local-k8s.md#vat-headless-ephemeral-k8s-bootstrap-diagnostics"
test_path = "apps/vat/tests/behavior_vat_headless_ephemeral_k8s_bootstrap_diagnostics.rs"
command = "cargo test -p vat --test vat_k8s_ephemeral -- --nocapture"
required_for_production = true
assertions = [
    "The passed deterministic fake regression keeps the bootstrap root error primary, then emits staged non-sensitive installer/guest/machine diagnostics with exactly guest_install_log, guest_k3s_system, backing_container_logs, machine_boot_log, machine_inspect, and container_system_status.",
    "The diagnostics are fixed read-only probes under a six-second total and one-second-per-probe budget; they exclude private kubeconfig/cache and host credentials, do not change the existing 300-second bootstrap behavior, do not rerun k3s --version or add a wrapper/recovery command, and exact cleanup still runs.",
]

[[aw.ec.generated.cases]]
id = "vat-headless-ephemeral-k8s-real-host"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "headless-ephemeral-kubernetes-session"
contract_id = "vat-headless-ephemeral-k8s-real-host"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-headless-ephemeral-local-k8s.md#vat-headless-ephemeral-k8s-real-host"
test_path = "apps/vat/tests/behavior_vat_headless_ephemeral_k8s_real_host.rs"
command = "VAT_K8S_EPHEMERAL_E2E_REQUIRED=1 cargo test -p vat --test vat_k8s_ephemeral apple_container_k3s_session_exposes_host_api_then_cleans_up -- --ignored --nocapture"
required_for_production = true
assertions = [
    "An independently installed non-OrbStack kubectl is first on PATH; an OrbStack-provided binary is rejected before the K3s command runs.",
    "The public command's systemd image is present in Apple Container.",
    "Host kubectl reads exactly one Ready K3s node through the temporary 0600 kubeconfig.",
    "The terminal result's exact machine name returns Apple Container's not-found result after return.",
    "A pass is only a one-boot agent-session proof, never a durable microvm-k3s claim.",
]

[[aw.ec.generated.cases]]
id = "vat-headless-leased-k8s-json-exec-fake"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "headless-ephemeral-kubernetes-session"
contract_id = "vat-headless-leased-k8s-json-exec-fake"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-headless-ephemeral-local-k8s.md#vat-headless-leased-k8s-json-exec-fake"
test_path = "apps/vat/tests/behavior_vat_headless_leased_k8s_json_exec_fake.rs"
command = "cargo test -p vat --test vat_k8s_ephemeral leased_session_exec -- --nocapture"
required_for_production = true
assertions = [
    "The fake runtime proves the JSON leased exec result has one VAT-owned document, separate bounded streams, a preserved child exit code, no raw replay, and no session marker mutation; credential-validation and API-probe failures mask private paths.",
    "A fake owned-API probe that crosses the lease deadline does not spawn the credentialed child. The independent-kubectl leased real-host E2E passed 1/1 (36 filtered) in 29.97s and includes strict JSON exec with --timeout 30.",
]

[[aw.ec.generated.cases]]
id = "vat-headless-leased-k8s-local-image-real-host"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "headless-ephemeral-kubernetes-session"
contract_id = "vat-headless-leased-k8s-local-image-real-host"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-headless-ephemeral-local-k8s.md#vat-headless-leased-k8s-local-image-real-host"
test_path = "apps/vat/tests/behavior_vat_headless_leased_k8s_local_image_real_host.rs"
command = "RUST_TEST_THREADS=1 VAT_K8S_LOCAL_IMAGE_E2E_REQUIRED=1 cargo test -p vat --test vat_k8s_ephemeral apple_container_k3s_lease_imports_local_image_without_registry_pull -- --ignored --nocapture"
required_for_production = true
assertions = [
    "Passed 1/1 (36 filtered) in 49.73s: an already-local Apple alpine:3.20 image is inspected, privately delivered into one active K3s lease, and reported with an OCI descriptor digest.",
    "The one fixture pod uses imagePullPolicy Never, completes, and emits its marker log; this proves the imported local image for that pod only, not registry-pull generality.",
    "Explicit delete confirms exact Apple machine and private session storage cleanup. This is not persistence, GUI, or Docker Engine/API evidence.",
]

[[aw.ec.generated.cases]]
id = "vat-headless-leased-k8s-port-forward-json-fake"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "headless-ephemeral-kubernetes-session"
contract_id = "vat-headless-leased-k8s-port-forward-json-fake"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-headless-ephemeral-local-k8s.md#vat-headless-leased-k8s-port-forward-json-fake"
test_path = "apps/vat/tests/behavior_vat_headless_leased_k8s_port_forward_json_fake.rs"
command = "cargo test -p vat --test vat_k8s_ephemeral leased_session_port_forward_json -- --nocapture"
required_for_production = true
assertions = [
    "The fake runtime proves text stays separate while JSON returns one vat.k8s.session.port-forward.v1 only after exact tunnel/group cleanup, preserving the host child exit and separately bounded stream snapshots without raw replay.",
    "VAT masks its own setup/API/tunnel/cleanup errors, preserves opaque credential-free child output in a successful result, refuses to open a tunnel when the lease crosses expiry after API proof, and cleans direct/outer children before readers join after partial setup failure.",
    "Focused deterministic filter passed 7/7. The independent-kubectl Service-forward E2E passed 1/1 (36 filtered) in 49.57s and includes the strict JSON tunnel only for one Service-only loopback session.",
]

[[aw.ec.generated.cases]]
id = "vat-headless-leased-k8s-port-forward-real-host"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "headless-ephemeral-kubernetes-session"
contract_id = "vat-headless-leased-k8s-port-forward-real-host"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-headless-ephemeral-local-k8s.md#vat-headless-leased-k8s-port-forward-real-host"
test_path = "apps/vat/tests/behavior_vat_headless_leased_k8s_port_forward_real_host.rs"
command = "VAT_K8S_PORT_FORWARD_E2E_REQUIRED=1 cargo test -p vat --test vat_k8s_ephemeral apple_container_k3s_lease_port_forwards_local_service_to_one_credential_free_host_child -- --ignored --nocapture"
required_for_production = true
assertions = [
    "Independent-kubectl real-host E2E passed 1/1 (36 filtered) in 49.57s. A local alpine fixture was loaded into the active K3s lease; because BusyBox lacks `httpd`, an in-pod HTTP probe verified the fixture before its Service endpoint responded through one VAT-owned 127.0.0.1 text forward and the strict one-document JSON tunnel.",
    "One credential-free host curl child proves VAT supplied endpoint metadata but did not inject kubeconfig/cache/API variables or VAT_HOME. Its terminal record begins on a new line after child output.",
    "The terminal result confirms forward cleanup, the selected loopback port is closed afterward, and the lease remains active only until explicit delete confirms exact machine cleanup. This bounded result is not a same-UID OS-isolation claim, daemonized-child claim, persistence, ingress, public bind, background-proxy, or general Kubernetes claim.",
]

[[aw.ec.generated.cases]]
id = "vat-headless-leased-k8s-real-host"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "headless-ephemeral-kubernetes-session"
contract_id = "vat-headless-leased-k8s-real-host"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-headless-ephemeral-local-k8s.md#vat-headless-leased-k8s-real-host"
test_path = "apps/vat/tests/behavior_vat_headless_leased_k8s_real_host.rs"
command = "VAT_K8S_SESSION_E2E_REQUIRED=1 cargo test -p vat --test vat_k8s_ephemeral apple_container_k3s_leased_session_supports_multiple_host_commands_then_deletes -- --ignored --nocapture"
required_for_production = true
assertions = [
    "Two independent host kubectl commands use one active private lease.",
    "Explicit delete confirms exact Apple machine absence and removes the credential directory.",
    "The proof does not claim Apple-machine restart, reboot persistence, or a durable cluster backend.",
]

[[aw.ec.generated.cases]]
id = "vat-hermetic-build"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "full-hermetic-http-mock-no-forward-mode"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-network-sandbox-full-hermetic-http-mock-no-forward-mode-bloc.md#vat-hermetic-build"
test_path = "apps/vat/tests/behavior_vat_hermetic_build.rs"
command = "cargo build -p vat --no-default-features"
required_for_production = true
assertions = ["vat compiles with and without default features."]

[[aw.ec.generated.cases]]
id = "vat-hermetic-no-forward-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "full-hermetic-http-mock-no-forward-mode"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-network-sandbox-full-hermetic-http-mock-no-forward-mode-bloc.md#vat-hermetic-no-forward-smoke"
test_path = "apps/vat/tests/behavior_vat_hermetic_no_forward_smoke.rs"
command = "cargo test -p vat --test vat_emulator_httpmock_hermetic -- --nocapture"
required_for_production = true
assertions = ["a proxy started with --no-forward returns 502 hermetic for an unmatched host (no upstream reached) while a registered stub on the same proxy returns 200; an unmatched request on a default (forwarding) proxy still forwards."]

[[aw.ec.generated.cases]]
id = "vat-host-process-gpu-visibility"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "host-process-execution-and-gpu-visibility"
contract_id = "host-process-execution-and-gpu-visibility"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/external-contracts.md#vat-host-process-gpu-visibility"
test_path = "apps/vat/tests/behavior_vat_host_process_gpu_visibility.rs"
command = "rg -n -e 'Apple GPU' -e Metal -e MPS -e MLX -e tensorflow-metal apps/vat/README.md apps/vat/src/gpu.rs"
required_for_production = true
assertions = [
    "README/source names Apple GPU access as a host-process property",
    "Metal, MPS, MLX, and tensorflow-metal are present in the GPU contract",
]

[[aw.ec.generated.cases]]
id = "vat-http-mock-host-routing-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "network-sandbox-v1-transparent-http-host-routing"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-network-sandbox-v1-transparent-http-service-routing-to-local.md#vat-http-mock-host-routing-smoke"
test_path = "apps/vat/tests/behavior_vat_http_mock_host_routing_smoke.rs"
command = "cargo test -p vat --test vat_emulator_httpmock_routing -- --nocapture"
required_for_production = true
assertions = [
    "with a route example.test -> http://127.0.0.1:<sink>, a request through the proxy to http://example.test/p (and https://example.test/p via CONNECT MITM) is answered by the local sink, not forwarded upstream.",
    "an unmatched host still forwards/records exactly as before (no regression).",
]

[[aw.ec.generated.cases]]
id = "vat-http-mock-lean-build"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "built-in-http-mock-record-replay-proxy-https-mitm"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/built-in-http-mock-record-replay-proxy.md#vat-http-mock-lean-build"
test_path = "apps/vat/tests/behavior_vat_http_mock_lean_build.rs"
command = "cargo build -p vat --no-default-features"
required_for_production = true
assertions = ["vat compiles without the emulator feature; the http-mock emulator verb then errors cleanly, never a panic."]

[[aw.ec.generated.cases]]
id = "vat-http-mock-preset-run-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "built-in-http-mock-record-replay-proxy-https-mitm"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/built-in-http-mock-record-replay-proxy.md#vat-http-mock-preset-run-smoke"
test_path = "apps/vat/tests/behavior_vat_http_mock_preset_run_smoke.rs"
command = "cargo test -p vat --test vat_emulator_httpmock -- --nocapture --include-ignored"
required_for_production = true
assertions = ["a preset = http-mock vat.toml run exports HTTP(S)_PROXY + NO_PROXY + the CA-trust vars; the runner curls a stubbed https URL with no code change and gets the stub."]

[[aw.ec.generated.cases]]
id = "vat-http-mock-routing-build"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "network-sandbox-v1-transparent-http-host-routing"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-network-sandbox-v1-transparent-http-service-routing-to-local.md#vat-http-mock-routing-build"
test_path = "apps/vat/tests/behavior_vat_http_mock_routing_build.rs"
command = "cargo build -p vat --no-default-features"
required_for_production = true
assertions = ["vat compiles with and without default features."]

[[aw.ec.generated.cases]]
id = "vat-http-mock-stub-and-mitm-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "built-in-http-mock-record-replay-proxy-https-mitm"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/built-in-http-mock-record-replay-proxy.md#vat-http-mock-stub-and-mitm-smoke"
test_path = "apps/vat/tests/behavior_vat_http_mock_stub_and_mitm_smoke.rs"
command = "cargo test -p vat --test vat_emulator_httpmock -- --nocapture"
required_for_production = true
assertions = [
    "a stubbed GET http://api.test/v1/x through the proxy returns the stub body with no network.",
    "a client trusting the minted CA and using the proxy GETs a stubbed https://api.test/v1/y via CONNECT+MITM and receives the stub (no real upstream).",
    "with no stub, a first GET to a local upstream records+forwards and a second GET replays from the cassette with the upstream down.",
]

[[aw.ec.generated.cases]]
id = "vat-llm-agent-usage-guide"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "agent-legible-state-and-diff-surface"
contract_id = "agent-legible-state-and-diff-surface"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/llm-agent-usage-guide.md#vat-llm-agent-usage-guide"
test_path = "apps/vat/tests/behavior_vat_llm_agent_usage_guide.rs"
command = "cargo test -p vat --test vat_toml_runner -- --nocapture"
required_for_production = true
assertions = [
    "`vat llm --topic guide` exits successfully.",
    "The guide mentions vat.toml runner mode and direct command mode.",
    "The guide mentions state, diff, and logs evidence commands.",
    "The guide describes exact no-format and VAT-native Compose `ps --format json` / `--format=json` forms: JSON mode is one `vat.docker-compose.ps.v1` / `vat_json` document with no human table and the same claim-held profile/topology proof, not Docker Compose JSON/template/table compatibility; every other Compose ps format and invalid provenance fail closed. The full serial vat_docker_shim aggregate is intentionally not recorded because an independent serial run exposed a nondeterministic pre-existing Compose JSON logs timing race; no aggregate result is claimed for this sandbox.",
    "The guide describes strict `docker compose --dry-run -f FILE -p PROJECT up -d [--build]`: it parses only existing strict profiles and emits one vat.docker-compose.preflight.v1 document with validated=true, runtime_started=false, registry_written=false, image_built=false, launch_revalidates=true, launch_argv, and executable next using the parser's canonical source path; it invokes no Apple Container command, does not build/import/start/write registry, rejects wait and all other global/Compose flags, and real launch revalidates that same file after a cwd change.",
    "The guide describes text `logs SERVICE` preserving observed log bytes then starting its additive VAT handoff JSON on a new line after them, and `logs --format json [--tail LINES] SERVICE` capture-only VAT JSON: separate stdout/stderr, default 200/range 1..=1000 tail, truncated/utf8_lossy indicators, capture_only=true/runtime_invoked=false/compose_record_mutated=false, no topology/endpoints, and JSON-ps next. It holds existing claim/provenance without an Apple Container call or project.json mutation; VAT first caps each read and line tail, then after lossy UTF-8 plus JSON escaping retains a valid UTF-8 suffix whose serialized JSON string value remains within the same 64 KiB per-stream cap and marks it truncated. It rejects follow/timestamps/other flags and does not claim Docker Compose merged/follow/timestamp/template behavior. The full serial vat_docker_shim aggregate is intentionally not recorded because an independent serial run exposed a nondeterministic pre-existing Compose JSON logs timing race; the focused serialized-cap unit passed 1/1 for 0xff-heavy and NUL/control-heavy streams after actual JSON serialization. The recorded real dual-service E2E includes JSON logs for the bounded host-facing profile.",
    "The guide describes Compose text exec preserving observed child bytes then starting its additive VAT handoff JSON on a new line after them, and JSON only as `docker compose -p PROJECT exec -T --format json SERVICE -- COMMAND` or `--format=json`: one same-read claim-held snapshot requires known shim provenance plus exact unique ready VAT-owned MicroVM evidence through child spawn, VAT parses and validates the Docker-facing -- but does not forward it, invoking `container exec CONTAINER COMMAND [ARG...]`, then releases the claim, drains child stdout/stderr concurrently, and emits one VAT-native `vat.docker-compose.exec.v1` document with profile, child_exit_code, separate streams, per-stream truncated/utf8_lossy, runtime_invoked=true, compose_record_mutated=false, no raw child output, and no topology/endpoints. Each serialized stream string is capped at 64 KiB. Text ordering covers observed managed-child bytes only, not escaped descendants. Misordered format, missing delimiter, TTY, and all other exec flags fail closed; it is not Docker Compose exec-output compatibility. The full serial shim aggregate is intentionally not recorded because an independent serial run exposed a nondeterministic pre-existing Compose JSON logs timing race; its precise serialized-cap unit passed 1/1, and the bounded real-host JSON exec is covered by the dual-service E2E.",
    "The guide describes unchanged no-flag session status and the bounded `vat k8s session status --verify-api <id>` proof: only an active unexpired lease without a port-forward or exec marker may enter the private operation lock; it rechecks expiry after lock and immediately before the bounded private-credential API probe, verifies exact backing identity/endpoint, reports api_checked=true/api_state=reachable on success, returns non-probing api_checked=false/api_state=not_checked for expired/recovery state, and fails closed without lease/credential mutation for busy, unavailable, or identity-mismatched state. It remains one-boot/nonpersistent with no GUI or Docker Engine/API; focused fake coverage passed 4/4, a precise unit passed 1/1, and the independent-kubectl leased E2E passed 1/1 (36 filtered) in 29.97s with status --verify-api after text and strict JSON exec. That is bounded active-lease evidence, not a general API-status guarantee.",
    "The guide describes bounded text `vat k8s session exec [--timeout SECONDS] <id> -- COMMAND` and JSON `vat k8s session exec --format json [--timeout SECONDS] <id> -- COMMAND`: omission uses remaining TTL; explicit 1..=14400 seconds cannot exceed it. Both forms validate the active lease, exact backing/API identity, private credentials, and owned host API under the private lock, recheck expiry before spawn, own/reap the process group on normal exit, deadline, or interruption, and retain the lock through cleanup. A starting/live crash marker blocks later exec/delete/cleanup fail-closed rather than claiming termination. JSON returns one `vat.k8s.session.exec.v1`/`vat_json` document with child_exit_code, separate stdout/stderr with 64 KiB serialized-value caps and truncated/utf8-lossy indicators, api_verified=true, runtime_invoked=true, session_record_mutated=false, no raw replay, and no private paths. The leased real-host E2E passed 1/1 (36 filtered) in 29.97s, including JSON exec with `--timeout 30`, status verification, and exact delete.",
    "The guide describes unchanged text Service-forward and only `vat k8s session port-forward run --format json <id> service/<name> <port> -- COMMAND` for JSON: its loopback Service tunnel gives a credential-free child endpoint metadata, keeps the lock through shared-PGID cleanup, silently rechecks TTL after API proof and before exact kubectl/child spawn, then emits one post-cleanup `vat.k8s.session.port-forward.v1` document with child exit, separate 64 KiB serialized streams, no raw replay, masked VAT-owned failures, opaque-child preservation, and status-verify next. Partial reader setup cleans direct/outer children before reader joining. The independent-kubectl real-host E2E passed 1/1 (36 filtered) in 49.57s, covering one loopback Service text and strict JSON tunnel with confirmed cleanup and closed local ports; it is not a general tunnel guarantee.",
    "The guide describes strict direct inventory only as `docker ps --format json` / equals with optional exactly-once `--all` or `-a`, shared only by `docker container ls` and `docker container list`; `docker container ps` JSON remains rejected. It invokes `container list --format json [--all]`, validates one opaque Apple-native JSON value, then byte-for-byte replays stdout without a VAT wrapper or Docker Engine ps schema. Templates/table output, filter, quiet plus JSON, duplicate/unknown flags, and positionals fail before Apple Container. It is read-only inventory, not ownership/health/readiness/liveness proof; one five-second bounded isolated cleanup path covers root exit and both pipe EOFs, and malformed, oversized, or escaped-pipe output fails closed without stdout replay. Cargo check passed; shared docker_shim lib passed 54/54; focused ps fake integration passed 4/4; the full serial fake-shim aggregate is intentionally not recorded because an independent serial run exposed a nondeterministic pre-existing Compose JSON logs timing race; real-host observation passed 1/1 (50 filtered) on Apple Container 1.1.0; the temporary owner-labeled container uses a high-entropy name plus exact owner-label rechecks as conservative best-effort; the emergency guard retains on uncertainty; Apple Container has no atomic conditional delete, so there is no race-free/no-impossible-misdelete claim; ps is a global read-only inventory smoke observation, not a targeted ownership result, and proves one valid native JSON document only; fake/unit tests prove byte-preservation and fail-closed details",
    "The guide describes strict image inventory only as `docker images --format json` / equals, shared only by `docker image ls` and `docker image list`. It invokes `container image list --format json`, bounded-captures and validates one opaque Apple-native JSON value, then byte-for-byte replays stdout without a VAT wrapper or Docker Engine image schema. Template/table/YAML/TOML output, filters, quiet, verbose, all, digests, no-trunc, positionals, duplicates, unknown flags, and `--` fail before Apple Container; text/quiet image-list behavior remains inherited. It makes no ownership/provenance/security/executability/registry/build-readiness/health/readiness/liveness claim. A five-second bounded isolated cleanup path covers root exit and both pipe EOFs, and malformed, oversized, or escaped-pipe output fails closed without stdout replay. Cargo check passed; shared docker_shim lib passed 54/54; focused docker_images_json integration passed 4/4; the full serial fake-shim aggregate is intentionally not recorded because an independent serial run exposed a nondeterministic pre-existing Compose JSON logs timing race; real-host observation passed 1/1 (50 filtered) on Apple Container 1.1.0; the temporary owner-labeled container uses a high-entropy name plus exact owner-label rechecks as conservative best-effort; the emergency guard retains on uncertainty; Apple Container has no atomic conditional delete, so there is no race-free/no-impossible-misdelete claim; images is a global read-only inventory smoke observation, not a targeted ownership result, and proves one valid native JSON document only; fake/unit tests prove byte-preservation and fail-closed details",
    "The guide describes strict direct image inspect only as `docker image inspect --format json IMAGE` / equals: one JSON selector precedes exactly one opaque safe image reference, then VAT strips that selector and invokes only `container image inspect IMAGE`. Templates, `--`, extra references, and every other option reject before Apple Container; bounded capture validates one opaque Apple-native JSON document before byte-for-byte replay, preserves valid JSON plus a nonzero child exit, and suppresses malformed, oversized, or escaped-pipe output. It provides no Docker image-inspect schema/template/Engine API, provenance, security, registry, build-completion, readiness, or secret-redaction claim. Cargo check passed; docker_shim lib passed 58/58; focused docker_image_inspect_json passed 4/4 with 1 ignored; the opt-in host E2E passed 1/1 (61 filtered) in 1.21s and proves only one direct native image document.",
    "The guide describes strict `docker stats --no-stream --format json CONTAINER [CONTAINER...]` / equals form as read-only native Apple JSON, not a VAT/Docker Engine wrapper/schema or ownership/health/liveness proof. It uses a five-second bounded isolated cleanup path, replays only complete 256 KiB-capped validated capture, and fails closed without stdout replay for malformed, oversized, or escaped-pipe output. Shared strict-native-observation library coverage passed 54/54; the full serial fake-shim aggregate is intentionally not recorded because an independent serial run exposed a nondeterministic pre-existing Compose JSON logs timing race; real-host observation passed 1/1 (50 filtered) on Apple Container 1.1.0; stats targets the temporary owner-labeled container, whose high-entropy name plus exact owner-label rechecks are conservative best-effort; the emergency guard retains on uncertainty; Apple Container has no atomic conditional delete, so there is no race-free/no-impossible-misdelete claim; it proves one valid native JSON document only; fake/unit tests prove byte-preservation and fail-closed details",
    "The guide preserves permanent headless, non-Docker, non-daemon, full capabilities versus selected-plan doctor semantics: a full Docker probe maps `services.docker_services` to available or unavailable; `vat capabilities` exposes only a bounded read-only shared-builder advisory (shared_unknown ownership, automatic_cleanup=false, separate configuration/observed stats/global disk, nonfatal timeout/unknown/probe errors, no lifecycle mutation). An explicit Apple-Container-only plan uses one read-only container status probe and executes no Docker command; it maps `services.docker_services` to not_probed, retains `docker.daemon_probe.state=skipped` as provenance, and makes daemon=false non-unavailable evidence. Unsupported no-OCI-route presets and named volumes fail closed; builder advisory never decides doctor runtime success; Docker/Auto/cluster retains probe. K3s remains one-boot/local-lease only and requires an independently installed kubectl on PATH before K3s use. Independent-kubectl one-shot, leased, local-image, and Service-forward E2Es passed 1/1 (36 filtered) in 28.38s, 29.97s, 49.73s, and 49.57s. The local-image result is one already-local Apple alpine:3.20 pod with imagePullPolicy=Never, a marker log, and exact session cleanup; it does not establish registry-pull generality, persistence, GUI, or Docker Engine/API. The leased result covers strict JSON exec with `--timeout 30` and bounded `status --verify-api`; the Service-forward result covers one Service-only loopback strict JSON tunnel with a credential-free child, confirmed cleanup, and closed local ports. It adds no durable status backend: `status --verify-api` remains a private-lock, identity/endpoint, expiry-rechecked bounded active-lease proof, while `session exec --format json` is a credentialed agent-result form with no raw replay or session mutation. Those results do not establish persistence, a general cluster/tunnel, OS isolation, or crash-safe termination. It also preserves the opt-in shim, three-profile Compose, detached-only bounded wait (one wait, positive 300-default/1200-max timeout after import/build, durable VAT-only proof, target pinning, lock-free polls, safe timeout/next behavior, and verified-ready source-build cleanup), host-facing no-DNS/loopback-only JSON, exact ps topology with endpoint-proof gating and no --format/app-healthcheck claim, provenance isolation, text logs/exec with new-line additive handoffs plus strict VAT-native JSON exec, and no-Docker-Engine boundaries. The recorded opt-in gated dual-service E2E passed 1/1 (50 filtered) in 4.54s only for host-facing-independent-v1 up-wait/endpoints, one-document JSON ps/logs/exec, text logs, no-final-newline text-exec handoff, and down cleanup, never as service DNS, general Compose, Docker Engine API, or Kubernetes evidence.",
    "The guide describes strict direct container inspect only as `docker inspect --format json CONTAINER` / `--format=json`, shared only by `docker container inspect`: one safe id follows one VAT-only selector before runtime, unformatted inspect remains inherited, and canonical `container inspect CONTAINER` returns one complete validated Apple-native JSON document byte-for-byte without a VAT or Docker Engine inspect schema. It rejects `--type`, `--size`, templates/table/YAML/TOML, filters, a second id, `--`, and unknown flags; five-second bounded isolated cleanup covers root exit and both pipe EOFs, each stream is 256 KiB, valid JSON with a nonzero child exit preserves status, and malformed/oversized/flood output suppresses raw stdout. It supplies no ownership/provenance/security/image/registry/build-status/health/readiness/liveness/port-reachability proof or secret-redaction guarantee. Cargo check passed; shared docker_shim lib passed 54/54; focused docker_inspect integration passed 5/5; the full serial fake-shim aggregate is intentionally not recorded because an independent serial run exposed a nondeterministic pre-existing Compose JSON logs timing race; real-host observation passed 1/1 (50 filtered) on Apple Container 1.1.0; inspect targets the temporary owner-labeled container, whose high-entropy name plus exact owner-label rechecks are conservative best-effort; the emergency guard retains on uncertainty; Apple Container has no atomic conditional delete, so there is no race-free/no-impossible-misdelete claim; it proves one valid native JSON document only; fake/unit tests prove byte-preservation and fail-closed details",
    "The guide describes direct logs JSON as a separate finite VAT wrapper, not a sixth Apple-native form: only direct `docker logs --format json --tail LINES CONTAINER` / equals forms and the same form through `docker container logs` pass; one format and one tail may mix spelling but must precede one safe final id, and LINES is 1..=1000. Unformatted logs stay inherited, the selector is never forwarded, and canonical backend argv is `container logs -n LINES CONTAINER`. Apple text stdout becomes exactly one `vat.docker.logs.v1` / `vat_json` wrapper with untrusted stdio, bounded diagnostic stderr, truncation/lossy flags, backend/container/requested_tail/runtime/child outcome, and safe inspect next—not Docker schema/multiplex/demux. Ordinary child nonzero retains wrapper+exit; follow/boot/timestamps/since/until/templates and all other modifiers reject before runtime, while timeout/setup/escaped-pipe failure emits no partial wrapper after five-second plus one-second bounded dual-stream suffix/serialized caps. Cargo check passed; docker_shim lib passed 54/54; focused docker_logs_json integration passed 6/6; the full serial aggregate is intentionally not recorded because of the nondeterministic pre-existing Compose JSON logs timing race. `VAT_DOCKER_SHIM_E2E_REQUIRED=1 cargo test -p vat --test vat_docker_shim apple_container_docker_run_published_port_contract -- --ignored --nocapture` passed 1/1 (50 filtered) on Apple Container 1.1.0: logs targets a temporary owner-labeled container; its high-entropy name plus exact owner-label rechecks are conservative best-effort; the emergency guard retains on uncertainty; Apple Container has no atomic conditional delete, so there is no race-free/no-impossible-misdelete claim; the shared/cacheable nginx image is not cleaned up. The host smoke proves one wrapper only; fake/unit tests prove byte-preservation and fail-closed details.",
    "The guide documents direct exec JSON only as `docker exec --format json --timeout SECONDS CONTAINER -- COMMAND [ARG...]` / equals forms and `docker container exec`: exact one format plus one 1..=1200 timeout precede one safe id and a mandatory Docker-facing delimiter/raw command; raw exec remains inherited. VAT strips its selectors and delimiter before `container exec CONTAINER COMMAND [ARG...]`, emits one `vat.docker.exec.v1` / `vat_json` wrapper with separate serialized-64-KiB-capped untrusted streams and `timeout_scope=host-container-client-observation`, preserves ordinary child failure wrapper+exit, and emits no partial wrapper on timeout/setup/capture failure. The timeout does not claim guest termination. Deterministic docker_shim lib validation passed 54/54 and focused docker_exec_json passed 4/4; the direct real-host E2E passed 1/1 (50 filtered) on Apple Container 1.1.0 with an exec wrapper containing both stdout/stderr markers. The temporary owner-labeled container uses a high-entropy name plus exact owner-label rechecks as conservative best-effort; the emergency guard retains on uncertainty; Apple Container has no atomic conditional delete, so there is no race-free/no-impossible-misdelete claim. It is not Docker Engine parity, generic runtime, Compose, or Kubernetes evidence.",
    "The emitted guide documents the strict direct run JSON one-shot: only direct docker run with flexible-order format/timeout selectors before IMAGE, 1..=1200 timeout, raw command argv directly after IMAGE, a Docker `--` rejected before IMAGE or immediately after it but opaque child argv after the first non-`--` command token, caller lifecycle/network/mount/env options rejected before runtime, one owner-cleaned vat.docker.run.v1 result after exact absence, and no partial wrapper on timeout/setup/cleanup uncertainty. It preserves the narrow evidence: deterministic docker_run_json 5 plus 1 ignored in 1.80s and local alpine E2E 1/1 (56 filtered) in 2.30s, without guest-wide termination, crash-recovery, Docker Engine, or redaction claims.",
    "The emitted guide documents the strict direct build JSON receipt: only direct docker build with exact json/1..=1200-timeout/tag selectors, documented optional file/build-arg/target/platform/label fields before one canonical existing local-directory context, and no `--`, second context, or unsupported/misordered form. It strips only JSON/deadline selectors for public container build, returns one bounded vat.docker.build.v1 receipt with retained_no_auto_cleanup, safe success image-inspect next, and normal nonzero build_failed/docker-help without stale inspect; timeout/setup/capture emits no receipt. The deadline is host observation only, never cancellation/rollback/removal, and no Engine/provenance/ownership/readiness/security/redaction claim follows. It records 62/62 library, focused 4 plus 1 ignored (63 filtered), owner guard 1/1 (67 filtered), and opt-in host receipt E2E 1/1 (67 filtered) in 2.53s. The host test's high-entropy tag/exact label plus absence checks are test-only best effort; a race leaks rather than turning product builds into auto-cleanup.",
    "The emitted guide documents the strict direct pull JSON receipt: only direct docker pull with one json and one 1..=1200 timeout selector before one safe opaque image reference (nonempty, no leading dash, whitespace/control, URL-style `://`, or leading Git-style `git@` remote; ordinary OCI `@digest` remains opaque); `--`, second reference, missing/duplicate/misordered selectors, and unsupported flags reject before the client, while raw unselected direct pull and docker image pull remain inherited. It strips only JSON/deadline selectors for public container image pull and returns one bounded vat.docker.pull.v1 receipt with not_owned_no_auto_cleanup and absent registry-management/auth lifecycle. Success has strict image-inspect next without image-state/download-completion proof; ordinary nonzero is pull_failed/docker-help without stale inspect; timeout/setup/capture/pipe failure has no receipt. Host deadline is client/pipes observation only, not transfer cancellation/download completion/rollback/local-or-backend image state. It records 65/65 library, focused 5 plus 1 ignored (68 filtered), and opt-in host E2E 1/1 (73 filtered) in 27.14s; the real shared alpine cache probe deletes nothing and asserts no image ownership.",
]

[[aw.ec.generated.cases]]
id = "vat-llm-guide-cloud-tasks-scheduler-client-wiring-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "built-in-rust-emulators-cloud-tasks-cloud-scheduler"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-llm-guide-cloud-tasks-cloud-scheduler-emulator-usage.md#vat-llm-guide-cloud-tasks-scheduler-client-wiring-smoke"
test_path = "apps/vat/tests/behavior_vat_llm_guide_cloud_tasks_scheduler_client_wiring_smoke.rs"
command = "cargo test -p vat --test vat_toml_runner -- --nocapture"
required_for_production = true
assertions = [
    "`vat llm` exits successfully and still mentions vat.toml runner mode, direct command mode, and state/diff/logs evidence commands.",
    "the guide mentions the cloud-tasks / cloud-scheduler client-wiring factory (REST transport + http endpoint + anonymous credentials) and the direct-REST alternative.",
]

[[aw.ec.generated.cases]]
id = "vat-local-k8s-phase0-disposable-host-api"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "local-kubernetes-cluster-service-and-vat-cluster"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-local-k8s-phase-0-prove-k3s-on-apple-container-machine.md#vat-local-k8s-phase0-disposable-host-api"
test_path = "apps/vat/tests/behavior_vat_local_k8s_phase0_disposable_host_api.rs"
command = "VAT_LOCAL_K8S_DISPOSABLE_E2E=1 VAT_LOCAL_K8S_HOST_API_E2E=1 cargo test -p vat --test vat_local_k8s_phase0 apple_machine_bootstraps_disposable_k3s_via_backing_container_exec -- --ignored --nocapture"
required_for_production = true
assertions = [
    "The second explicit opt-in re-inspects the exact owned running machine and requires its backing container ID and IP address to be unchanged before it exports any credential. It installs k3s with that inspected IP as a TLS SAN.",
    "It copies the guest admin kubeconfig only into a private 0700 temporary directory, rewrites exactly one loopback server endpoint to the inspected IP, restricts the copied file to 0600, removes ambient KUBECONFIG and proxy environment variables, and makes kubectl use a private discovery cache inside the same owned directory. No credential contents are emitted in JSON evidence.",
    "The host command is `kubectl --kubeconfig <owned-path> --cache-dir <owned-path>/kubectl-cache --request-timeout=20s get nodes -o json`; the observed result is `ephemeral-host-api-go`, then the credential directory and exact owned machine are both confirmed absent.",
    "This is a one-boot reachability diagnostic only. It does not export a durable user kubeconfig or unlock a persistent microvm-k3s backend, port publication, local image delivery, storage, multi-node networking, or stop/run durability.",
]

[[aw.ec.generated.cases]]
id = "vat-local-k8s-phase0-disposable-k3s"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "local-kubernetes-cluster-service-and-vat-cluster"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-local-k8s-phase-0-prove-k3s-on-apple-container-machine.md#vat-local-k8s-phase0-disposable-k3s"
test_path = "apps/vat/tests/behavior_vat_local_k8s_phase0_disposable_k3s.rs"
command = "VAT_LOCAL_K8S_DISPOSABLE_E2E=1 cargo test -p vat --test vat_local_k8s_phase0 apple_machine_bootstraps_disposable_k3s_via_backing_container_exec -- --ignored --nocapture"
required_for_production = true
assertions = [
    "The explicit opt-in probe creates one auto-booted source-fixture machine, parses only that machine's inspect-returned running `containerId`, and never searches for or touches an ambient container.",
    "It requires PID 1 to be systemd and root command execution, installs pinned k3s v1.36.2+k3s1 with a guest-only 0600 admin kubeconfig, waits for `Node Ready`, then creates, waits for, logs, and deletes a BusyBox Job whose marker is `vat-k8s-phase0-workload-ok`.",
    "The probe captures node/pod state and k3s journal output in a JSON report, explicitly deletes the Job, then reconciles and proves bounded absence of only its owned machine; Drop cleanup is panic-only fallback. The observed host result is `ephemeral-go`.",
    "Without the separate host-API opt-in, this evidence proves a one-machine, one-boot guest substrate only. It does not prove default-add-on readiness, host API access, port exposure, local OCI image delivery, persistent volumes, multi-node networking, or stop/run durability.",
]

[[aw.ec.generated.cases]]
id = "vat-local-k8s-phase0-real-host"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "local-kubernetes-cluster-service-and-vat-cluster"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-local-k8s-phase-0-prove-k3s-on-apple-container-machine.md#vat-local-k8s-phase0-real-host"
test_path = "apps/vat/tests/behavior_vat_local_k8s_phase0_real_host.rs"
command = "VAT_LOCAL_K8S_E2E=1 cargo test -p vat --test vat_local_k8s_phase0 apple_machine_exec_control_is_usable_before_k3s -- --ignored --nocapture"
required_for_production = true
assertions = [
    "The ignored, explicit opt-in test preflights `local/vat-k8s-systemd:phase0` (or `VAT_LOCAL_K8S_MACHINE_IMAGE`) and skips cleanly when the Apple Container CLI is absent. It never builds or publishes an image itself.",
    "Using the source-controlled systemd fixture, the test creates one unique no-boot machine with no home mount, 2 CPUs, and 4G memory. It requires PID 1 to be `systemd`, waits for systemd, stops the machine, and requires `container machine run` to restart it and return the control marker.",
    "The test records exact argv, output, inspect/logs, bounded exact-name cleanup attempts, and a `go` or `no-go` verdict in a JSON report under `VAT_LOCAL_K8S_EVIDENCE_DIR` (default `/private/tmp`). It never deletes an ambient machine: after a failed or timed-out create it continues exact-name reconciliation through a stabilization window and requires a structured exact-name absence result; Drop is panic-only fallback.",
    "Recorded durability evidence: Apple Container 1.1.0 on macOS 26.5.1 boots the systemd fixture, but `container machine run` returns `Operation not supported by device` after a restart retry. A separate host-API probe also saw `machine create` report a bootMachine XPC timeout while its uniquely named machine was running, so failed create is treated as delayed allocation rather than proof of absence. `container exec` can diagnose an already-running backing container only; it cannot restart a stopped machine and is not a substitute for this control. The verdict remains NO-GO.",
    "This test does not treat a disposable `container exec` session as a durable pass. The durable failure blocks host kubeconfig, local image loading, networking, storage, stop/start reconciliation, and Phase 1 backend implementation.",
]

[[aw.ec.generated.cases]]
id = "vat-microvm-build"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "microvm-sandbox-backend-for-vat-run"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-microvm-phase-1-isolation-microvm-sandbox-backend-for-vat-ru.md#vat-microvm-build"
test_path = "apps/vat/tests/behavior_vat_microvm_build.rs"
command = "cargo build -p vat"
required_for_production = true
assertions = ["AC1: vat compiles cleanly with the new Isolation::MicroVm variant, EnvSpec.microvm_image field, and sandbox/microvm.rs module."]

[[aw.ec.generated.cases]]
id = "vat-microvm-fail-closed"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "microvm-sandbox-backend-for-vat-run"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-microvm-phase-1-isolation-microvm-sandbox-backend-for-vat-ru.md#vat-microvm-fail-closed"
test_path = "apps/vat/tests/behavior_vat_microvm_fail_closed.rs"
command = "cargo test -p vat --test vat_sandbox_microvm_fail_closed -- --nocapture"
required_for_production = true
assertions = [
    "AC3: `sandbox::pick(spec)` returns a hard Err (never a silently-degraded backend) for isolation=MicroVm when gpu=GpuRequest::Required, when spec.microvm_image is None, when egress=EgressPolicy::LocalhostOnly, and when microvm::available() is false; the LocalhostOnly error text carries the Phase-0-confirmed gateway-IP reasoning, not a generic 'no bridge exists' message.",
    "AC4: a dedicated case exercises the run.rs `gpu_satisfied()` preflight helper directly (not just pick()) rejecting `--isolation micro_vm --gpu required` before any workspace clone begins — proving the dual fail-closed layers are both wired, independently.",
]

[[aw.ec.generated.cases]]
id = "vat-microvm-published-port-real-host"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "microvm-sandbox-backend-for-vat-run"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-microvm-fail-closed-when-published-host-ports-are-unusable.md#vat-microvm-published-port-real-host"
test_path = "apps/vat/tests/behavior_vat_microvm_published_port_real_host.rs"
command = "VAT_MICROVM_E2E_REQUIRED=1 cargo test -p vat --test vat_microvm_published_port -- --ignored --nocapture"
required_for_production = true
assertions = [
    "On an explicit opt-in host with Apple's container CLI, a VAT-owned nginx MicroVM has its guest and published host endpoint checked separately.",
    "A host endpoint that resets or cannot complete the configured HTTP contract fails nonzero with service, endpoint, runtime, inspect, and logs remediation rather than Ready.",
    "The test removes only its uniquely named VAT-owned MicroVM and records the observed Apple container evidence for tracker review. A nonzero rm -f is accepted only when the successful bounded JSON list has no matching id; query failure, timeout, malformed JSON, or a matching id leaves durable cleanup_error, keeps the VAT and compose binding unavailable for reuse until retry, and returns nonzero.",
]

[[aw.ec.generated.cases]]
id = "vat-microvm-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "microvm-sandbox-backend-for-vat-run"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-microvm-phase-1-isolation-microvm-sandbox-backend-for-vat-ru.md#vat-microvm-smoke"
test_path = "apps/vat/tests/behavior_vat_microvm_smoke.rs"
command = "cargo test -p vat --test vat_sandbox_microvm -- --nocapture"
required_for_production = true
assertions = [
    "AC1/R2: `vat run --isolation micro_vm --microvm-image <ref> -- <cmd>` resolves and executes a real `container run` invocation, rootfs bind-mounted at /workspace, workdir honored, env vars visible inside the guest, and `--network none` enforced under EgressPolicy::Deny. Skips cleanly (does not fail) when the `container` CLI is not installed — mirrors the existing Docker-gated test pattern.",
    "Registered in the generated `apps/vat/aw.toml` EC inventory alongside the fail-closed integration test so `aw ec gen --verify` / `aw health --verify-tests` pick both up as configured EC-gated test commands for this capability.",
]

[[aw.ec.generated.cases]]
id = "vat-openapi-lean-build"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "openapi-driven-mock-http-service-spec-responses"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/interfaces/rest/openapi-driven-mock-http-service.md#vat-openapi-lean-build"
test_path = "apps/vat/tests/behavior_vat_openapi_lean_build.rs"
command = "cargo build -p vat --no-default-features"
required_for_production = true
assertions = ["vat compiles without the emulator feature; the openapi emulator verb then errors cleanly, never a panic."]

[[aw.ec.generated.cases]]
id = "vat-openapi-preset-run-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "openapi-driven-mock-http-service-spec-responses"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/interfaces/rest/openapi-driven-mock-http-service.md#vat-openapi-preset-run-smoke"
test_path = "apps/vat/tests/behavior_vat_openapi_preset_run_smoke.rs"
command = "cargo test -p vat --test vat_emulator_openapi -- --nocapture --include-ignored"
required_for_production = true
assertions = ["a preset = openapi vat.toml run exports OPENAPI_MOCK_HOST and the runner curls a documented operation, getting the spec-derived response with no app code change."]

[[aw.ec.generated.cases]]
id = "vat-openapi-standalone-and-proxy-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "openapi-driven-mock-http-service-spec-responses"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/interfaces/rest/openapi-driven-mock-http-service.md#vat-openapi-standalone-and-proxy-smoke"
test_path = "apps/vat/tests/behavior_vat_openapi_standalone_and_proxy_smoke.rs"
command = "cargo test -p vat --test vat_emulator_openapi -- --nocapture"
required_for_production = true
assertions = [
    "spawning vat emulator openapi --spec <tmp spec> and GETting a documented path returns the spec's example; an undocumented path returns 404.",
    "registering a spec for a host on the http-mock proxy answers a proxied HTTPS-MITM GET to that host from the spec (no stub, no upstream).",
]

[[aw.ec.generated.cases]]
id = "vat-pubsub-docker-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "gcp-firebase-emulator-service-presets"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/gcp-firebase-emulator-service-presets.md#vat-pubsub-docker-smoke"
test_path = "apps/vat/tests/behavior_vat_pubsub_docker_smoke.rs"
command = "cargo test -p vat --test vat_emulators -- --nocapture --include-ignored"
required_for_production = true
assertions = [
    "without the pubsub gcloud component, runtime=auto resolves to docker, exports PUBSUB_EMULATOR_HOST, and removes the container at teardown.",
    "the test skips gracefully when docker is unavailable.",
]

[[aw.ec.generated.cases]]
id = "vat-resource-isolation-boundary"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "resource-isolation-boundary"
contract_id = "resource-isolation-boundary"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/external-contracts.md#vat-resource-isolation-boundary"
test_path = "apps/vat/tests/behavior_vat_resource_isolation_boundary.rs"
command = "rg -n -e sandbox -e isolation -e seatbelt apps/vat/README.md apps/vat/src/sandbox"
required_for_production = true
assertions = [
    "vat documents resource isolation as its responsibility",
    "sandbox and seatbelt isolation remain visible implementation surfaces",
]

[[aw.ec.generated.cases]]
id = "vat-runner-sandbox-build"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "sandbox-applied-to-runner-mode-commands"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-sandbox-extend-seatbelt-isolation-egress-coverage-to-runner.md#vat-runner-sandbox-build"
test_path = "apps/vat/tests/behavior_vat_runner_sandbox_build.rs"
command = "cargo build -p vat --no-default-features"
required_for_production = true
assertions = ["vat compiles with and without default features."]

[[aw.ec.generated.cases]]
id = "vat-runner-sandbox-deny-egress"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "sandbox-applied-to-runner-mode-commands"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-sandbox-extend-seatbelt-isolation-egress-coverage-to-runner.md#vat-runner-sandbox-deny-egress"
test_path = "apps/vat/tests/behavior_vat_runner_sandbox_deny_egress.rs"
command = "cargo test -p vat --test vat_runner_sandbox -- --nocapture"
required_for_production = true
assertions = ["AC4: a `vat run <runner>` with `--isolation seatbelt` + `[network].egress = deny` denies a runner command's outbound connection (loopback and non-loopback alike), exit code non-zero, matching the existing direct-mode `vat_sandbox_egress` proof for EgressPolicy::Deny. Skips cleanly off-macOS / no sandbox-exec / no bash."]

[[aw.ec.generated.cases]]
id = "vat-runner-sandbox-egress-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "sandbox-applied-to-runner-mode-commands"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/apply-the-sandbox-seatbelt-isolation-egress-to-runner-mode-comma.md#vat-runner-sandbox-egress-smoke"
test_path = "apps/vat/tests/behavior_vat_runner_sandbox_egress_smoke.rs"
command = "cargo test -p vat --test vat_runner_sandbox -- --nocapture"
required_for_production = true
assertions = ["a `vat run <runner>` with isolation=seatbelt + egress=localhost-only runs the runner under sandbox-exec; the runner can reach a localhost listener but a connect to a non-loopback host is denied; an emulator service still reaches the network. Skips off-macOS / no sandbox-exec."]

[[aw.ec.generated.cases]]
id = "vat-runner-sandbox-service-exemption"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "sandbox-applied-to-runner-mode-commands"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-sandbox-extend-seatbelt-isolation-egress-coverage-to-runner.md#vat-runner-sandbox-service-exemption"
test_path = "apps/vat/tests/behavior_vat_runner_sandbox_service_exemption.rs"
command = "cargo test -p vat --test vat_runner_sandbox -- --nocapture"
required_for_production = true
assertions = ["AC2/R2: in the same `vat run` invoked with `[network].egress = deny`, a declared service (started via start_service, never sandbox_wrap'd) still binds/serves and remains reachable on its local port, while a sibling runner attempting outbound network under the same run is denied — proving the services exemption is enforced by construction, not a byproduct of a permissive default."]

[[aw.ec.generated.cases]]
id = "vat-seatbelt-egress-build"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "network-sandbox-v3-seatbelt-egress-policy"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-network-sandbox-v3-seatbelt-egress-policy-deny-outbound-exce.md#vat-seatbelt-egress-build"
test_path = "apps/vat/tests/behavior_vat_seatbelt_egress_build.rs"
command = "cargo build -p vat --no-default-features"
required_for_production = true
assertions = ["vat compiles with and without default features; egress policy is independent of the emulator feature."]

[[aw.ec.generated.cases]]
id = "vat-seatbelt-egress-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "network-sandbox-v3-seatbelt-egress-policy"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/vat-network-sandbox-v3-seatbelt-egress-policy-deny-outbound-exce.md#vat-seatbelt-egress-smoke"
test_path = "apps/vat/tests/behavior_vat_seatbelt_egress_smoke.rs"
command = "cargo test -p vat --test vat_sandbox_egress -- --nocapture"
required_for_production = true
assertions = ["under sandbox-exec with the localhost-only profile, a process connecting to a localhost listener succeeds while a connect to a non-loopback address is denied. Skips cleanly if sandbox-exec is absent or not macOS."]

[[aw.ec.generated.cases]]
id = "vat-toml-runner-local-service-smoke"
capability_id = "agent-native-gpu-native-dev-containers"
claim_id = "local-agent-test-runner-protocol"
contract_id = "local-agent-test-runner-protocol"
category = "behavior"
td_ref = "apps/vat/tech-design/logic/local-agent-test-runner-protocol.md#vat-toml-runner-local-service-smoke"
test_path = "apps/vat/tests/behavior_vat_toml_runner_local_service_smoke.rs"
command = "cargo test -p vat --test vat_toml_runner -- --nocapture"
required_for_production = true
assertions = [
    "vat run <runner-id> starts a local readiness service, runs the runner, captures logs, records artifacts, and returns JSON evidence.",
    "failed runner evidence remains inspectable.",
    "An unconfirmed Docker or MicroVM cleanup forces a nonzero result and keeps the VAT evidence regardless of the normal keep policy.",
    "direct vat run -- <cmd> compatibility is preserved.",
]
# AW-EC-END
