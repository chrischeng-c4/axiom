---
id: vat-llm-agent-usage-guide
summary: Add `vat llm [--topic <t>] [--format md|json]` as the offline agent-facing usage contract.
fill_sections: [scenarios, cli, e2e-test, changes]
capability_refs:
  - id: agent-native-gpu-native-dev-containers
    role: primary
    gap: agent-legible-state-and-diff-surface
    claim: agent-legible-state-and-diff-surface
    coverage: partial
    rationale: "The LLM guide makes vat's agent-facing command, evidence, and non-Docker contract directly discoverable from the CLI."
---

# Vat LLM Agent Usage Guide

## Scenarios
<!-- type: scenarios lang: yaml -->

```yaml
id: vat-llm-agent-usage-guide-scenarios
scenarios:
  - id: llm_reads_compact_usage_contract
    given:
      - "an agent needs to learn how to use vat from the CLI"
    when:
      - "the agent runs `vat llm --topic guide`"
    then:
      - "vat prints a compact markdown guide"
      - "the guide explains `vat run <runner-id> --json` for vat.toml runner mode"
      - "the guide explains `vat run -- <command>` for direct command mode"
      - "the guide names `vat state`, `vat diff`, and `vat logs` as evidence commands"
      - "the guide distinguishes full `vat capabilities --json` Docker discovery plus a bounded read-only Apple Container shared-builder advisory from selected-plan `vat doctor --json`: a full Docker probe maps `services.docker_services` to the explicit availability string available or unavailable; builder status has ownership=shared_unknown and automatic_cleanup=false, separates configuration from optional observed stats and global disk, treats unsupported/malformed/timed-out probes as nonfatal advisory unknown/probe_errors, and never starts/stops/deletes/prunes shared state"
      - "the guide says an explicit MicroVm/Apple-Container-only doctor plan uses exactly one read-only container system status probe per invocation, runs no Docker command, maps `services.docker_services` to not_probed, and retains `docker.daemon_probe.state=skipped` as provenance; daemon=false is not unavailable evidence in that state. It ignores unselected Docker services, never autostarts/falls back, and does not let a shared-builder advisory timeout/unknown/error change runtime success"
      - "the guide says unsupported MicroVm presets with no declared OCI route and MicroVm preset named volumes fail closed without Docker fallback, and a real builder state is reported only when the installed Apple Container CLI supports it"
      - "the guide says Docker runtime, Auto image, eligible Auto preset fallback, and selected cluster plans retain Docker probing, and a cluster requires Docker"
      - "the guide states vat is permanently headless with no GUI/Desktop surface, is not Docker, OCI, Compose, a Linux runtime, a VM, a daemon, or a long-lived process manager, and offers only an opt-in fail-closed Docker command shim without a Docker Engine socket/API"
      - "the guide keeps Apple Container K3s to one boot or a bounded local lease, never a persistent Kubernetes promise; a failed bootstrap renders its root error first, then staged non-sensitive installer evidence plus exactly guest_install_log, guest_k3s_system, backing_container_logs, machine_boot_log, machine_inspect, and container_system_status under a six-second total and one-second-per-probe read-only budget before exact cleanup"
      - "the guide says K3s diagnostics exclude private kubeconfig/cache and host credentials, do not change the existing 300-second bootstrap behavior, do not retry or rerun k3s --version, and add no wrapper/recovery command; deterministic fake regression passed. Independent-kubectl one-shot, leased, local-image, and Service-forward E2Es each passed 1/1 (36 filtered) in 28.38s, 29.97s, 49.73s, and 49.57s; the local-image run loads one already-local Apple alpine:3.20 into one lease, runs a pod with imagePullPolicy=Never, observes its marker log, and completes exact session cleanup—not registry-pull generality, persistence, GUI, Docker Engine/API, or OS-sandbox behavior. The Service-forward run covers one Service-only loopback text and strict JSON tunnel with a credential-free child, confirmed cleanup, and closed local ports"
      - "the guide distinguishes unchanged no-flag `vat k8s session status <id>` from `status --verify-api <id>`: the latter probes only an active unexpired lease without a retained port-forward or exec marker, takes the private operation lock, rechecks expiry after lock and immediately before one bounded private-credential API probe, proves exact backing identity/endpoint, reports api_checked=true/api_state=reachable only on success, returns non-probing api_checked=false/api_state=not_checked for expired/recovery state, and fails closed without lease/credential mutation for busy, unavailable, or identity-mismatched state; focused fake coverage is 4/4, the precise unit is 1/1, and the independent-kubectl leased E2E passed 1/1 (36 filtered) in 29.97s with status --verify-api after text and strict JSON exec. That is bounded active-lease evidence, not a general API-status or persistent-Kubernetes claim"
      - "the guide distinguishes bounded text `vat k8s session exec [--timeout SECONDS] <id> -- COMMAND` from `vat k8s session exec --format json [--timeout SECONDS] <id> -- COMMAND`: omitted timeout means remaining lease TTL; explicit timeout is 1..=14400 seconds and cannot exceed it. Both forms validate the exact active lease/backing/API/private credentials under the private lock, recheck expiry before spawn, own and reap the process group on exit/deadline/SIGINT/SIGTERM, and hold the lock through cleanup. A starting/live crash marker blocks later exec/delete/cleanup fail-closed rather than claiming recovery termination. JSON emits one vat.k8s.session.exec.v1 vat_json document with child exit, separate 64 KiB serialized-bounded streams, no raw replay, api_verified=true, runtime_invoked=true, session_record_mutated=false, and masked private paths; the child receives credentials, so it is not a credential-free or untrusted-child boundary. The leased real-host E2E passed 1/1 (36 filtered) in 29.97s, including JSON exec with `--timeout 30`, status verification, and exact delete"
      - "the guide distinguishes unchanged text Service-forward from `vat k8s session port-forward run --format json <id> service/<name> <port> -- COMMAND`: JSON stays Service-only and loopback-only with a credential-free cooperative host child, shared authenticated PGID, and private lock through cleanup; it silently rechecks the lease after API proof and before exact kubectl/child spawn, emits one post-cleanup vat.k8s.session.port-forward.v1 document with child exit, separately 64 KiB serialized-capped streams, no raw replay, and status-verify next, masks VAT-owned setup/API/tunnel/cleanup failures but preserves opaque child output, and completes direct/outer cleanup before joining partial-reader capture. The independent-kubectl real-host E2E passed 1/1 (36 filtered) in 49.57s and covers one loopback Service text and strict JSON tunnel, confirmed cleanup, and closed local ports—not a general tunnel or persistent Kubernetes claim"
      - "the guide distinguishes strict direct inventory `docker ps --format json` / equals with optional exactly-once `--all` or `-a`: only `docker container ls` and `docker container list` share it, while `docker container ps --format json` remains rejected. Canonical `container list --format json [--all]` output is one opaque validated Apple-native JSON value replayed byte-for-byte without a VAT wrapper or Docker Engine ps schema. Templates/table, filter, quiet plus JSON, duplicate/unknown flags, and positionals fail before Apple Container. It is a read-only snapshot, not ownership/health/readiness/liveness proof. A five-second deadline plus bounded isolated cleanup covers root exit and both pipe EOFs; complete 256 KiB-capped capture only, malformed/oversized/escaped-pipe stdout fails closed. Cargo check without default features passed; shared docker_shim library passed 54/54; direct ps fake integration passed 4/4; the full serial fake-shim aggregate is intentionally not recorded because an independent serial run exposed a nondeterministic pre-existing Compose JSON logs timing race; real-host observation passed 1/1 (50 filtered) on Apple Container 1.1.0; the temporary owner-labeled container uses a high-entropy name plus exact owner-label rechecks as conservative best-effort; the emergency guard retains on uncertainty; Apple Container has no atomic conditional delete, so there is no race-free/no-impossible-misdelete claim; ps is a global read-only inventory smoke observation, not a targeted ownership result, and proves one valid native JSON document only; fake/unit tests prove byte-preservation and fail-closed details"
      - "the guide distinguishes strict image inventory `docker images --format json` / equals: only `docker image ls` and `docker image list` share it. Canonical `container image list --format json` output is one opaque bounded-captured validated Apple-native JSON value replayed byte-for-byte without a VAT wrapper or Docker Engine image schema. Template/table/YAML/TOML output, filter, quiet, verbose, all, digests, no-trunc, positionals, duplicates, unknown flags, and `--` fail before Apple Container; inherited text/quiet image-list behavior is unchanged. It makes no ownership/provenance/security/executability/registry/build-readiness/health/readiness/liveness claim. A five-second deadline plus bounded isolated cleanup covers root exit and both pipe EOFs; complete 256 KiB-capped capture only, malformed/oversized/escaped-pipe stdout fails closed. Cargo check without default features passed; shared docker_shim library passed 54/54; focused docker_images_json integration passed 4/4; the full serial fake-shim aggregate is intentionally not recorded because an independent serial run exposed a nondeterministic pre-existing Compose JSON logs timing race; real-host observation passed 1/1 (50 filtered) on Apple Container 1.1.0; the temporary owner-labeled container uses a high-entropy name plus exact owner-label rechecks as conservative best-effort; the emergency guard retains on uncertainty; Apple Container has no atomic conditional delete, so there is no race-free/no-impossible-misdelete claim; images is a global read-only inventory smoke observation, not a targeted ownership result, and proves one valid native JSON document only; fake/unit tests prove byte-preservation and fail-closed details"
      - "the guide distinguishes strict direct image inspect only as `docker image inspect --format json IMAGE` / equals: exactly one JSON selector precedes exactly one opaque safe image reference (nonempty, no leading `-`, whitespace, or control characters); templates, `--`, extra references, and every other option fail before Apple Container. VAT strips the selector, invokes only `container image inspect IMAGE`, bounded-captures and validates one opaque Apple-native JSON document, then byte-for-byte replays complete native stdout. A five-second bounded isolated observer covers root exit and both pipe EOFs; each stream is 256 KiB, valid JSON plus a nonzero child exit preserves status, and malformed, oversized, or escaped-pipe capture suppresses raw stdout. It makes no Docker image-inspect schema/template/Engine API, provenance, security, registry, build-completion, readiness, or secret-redaction claim. Cargo check passed; canonical docker_shim lib passed 58/58; focused docker_image_inspect_json passed 4/4 with 1 ignored; the opt-in `VAT_DOCKER_IMAGE_INSPECT_JSON_E2E_REQUIRED=1` host command passed 1/1 (61 filtered) in 1.21s, proving only one direct `container image inspect alpine:3.20` call and one native document"
      - "the guide defines strict `docker stats --no-stream --format json CONTAINER [CONTAINER...]` or equals form: only explicit ids and the non-streaming native JSON flags are accepted, canonical Apple `container stats --format json --no-stream` output is validated and replayed without a VAT/Docker Engine wrapper/schema, and the read-only result is not ownership/health/readiness/liveness proof. A five-second deadline plus bounded isolated cleanup governs root exit and both pipe EOFs; only complete 256 KiB-capped capture is replayed and escaped-pipe capture fails closed. Shared strict-native-observation library coverage passed 54/54; the full serial fake-shim aggregate is intentionally not recorded because an independent serial run exposed a nondeterministic pre-existing Compose JSON logs timing race; real-host observation passed 1/1 (50 filtered) on Apple Container 1.1.0; stats targets the temporary owner-labeled container, whose high-entropy name plus exact owner-label rechecks are conservative best-effort; the emergency guard retains on uncertainty; Apple Container has no atomic conditional delete, so there is no race-free/no-impossible-misdelete claim; it proves one valid native JSON document only; fake/unit tests prove byte-preservation and fail-closed details"
      - "the guide teaches all three shim Compose profiles: strict-single-image-v1, strict-single-build-v1, and the exact `x-vat-compose-profile: host-facing-independent-v1` marker for two through four literal-image services with unique loopback ports and no service-name DNS"
      - "the guide makes the host-facing JSON negative contract explicit (`profile=host-facing-independent-v1`, `service_name_dns=false`, `host_loopback_only=true`), rejects dependencies/networks/volumes/build/interpolation/--env-file, and records that the opt-in gated real dual-service Apple Container E2E passed 1/1 (50 filtered) on this host in 4.54 seconds for up -d --wait, both loopback endpoints, one-document JSON ps/logs/exec, text logs, text exec including a no-final-newline handoff, and down cleanup of exact containers/ports/registry"
      - "the guide keeps that real evidence bounded: it does not establish service-name DNS, general Compose, a Docker Engine API, or Kubernetes"
      - "the guide defines `docker compose --dry-run -f FILE -p PROJECT up -d [--build]` as strict file/profile-only preflight for existing image/build/host-facing profiles: one `vat.docker-compose.preflight.v1` VAT JSON document has validated=true, runtime_started=false, registry_written=false, image_built=false, launch_revalidates=true, structured launch_argv, and an executable next using the parser's canonical source path so the same file is revalidated after a cwd change"
      - "the guide says Docker-shaped preflight invokes no Apple Container command, does not build/import/start or write a registry, rejects wait and every other global/Compose flag, and the returned real launch revalidates the file rather than claiming generic Compose dry-run parity"
      - "the guide defines two exact `docker compose -p PROJECT ps` shapes: no format preserves text and final additive profile/topology JSON; `--format json` or `--format=json` emits exactly one VAT-owned `schema=vat.docker-compose.ps.v1`, `format=vat_json` document with the same claim-held profile/topology proof and no human table"
      - "the guide keeps the Compose ps JSON exception narrow: it is not Docker Compose JSON/template/table compatibility, every other Compose ps format fails closed, all-or-none canonical endpoints and generic/missing/unknown provenance failure remain unchanged, and the full serial fake-shim aggregate is intentionally not recorded because an independent serial run exposed a nondeterministic pre-existing Compose JSON logs timing race; no aggregate result is claimed for this sandbox"
      - "the guide defines Docker-shaped text logs as exact `logs SERVICE`: observed log bytes are preserved, then the additive VAT handoff JSON starts on a new line after them; JSON is `logs --format json [--tail LINES] SERVICE` or equals forms with service final and emits one capture-only vat.docker-compose.logs.v1 document with separate stdout/stderr snapshots, tail_lines default 200 and range 1..=1000, per-stream truncated/utf8_lossy, capture_only=true, runtime_invoked=false, and compose_record_mutated=false"
      - "the guide says logs JSON holds existing claim/provenance then reads VAT-captured logs only, calls no Apple Container runtime, never mutates project.json, first caps each read and line tail, then after lossy UTF-8 plus JSON escaping retains a valid UTF-8 suffix whose serialized JSON string value remains within the same 64 KiB per-stream cap and marks it truncated, has no topology/endpoints, and next is VAT-native JSON ps; it rejects follow/timestamps/other flags and is not Docker Compose merged/follow/timestamp/template-schema compatibility"
      - "the guide records the full serial fake-shim aggregate is intentionally not recorded because an independent serial run exposed a nondeterministic pre-existing Compose JSON logs timing race, plus a focused serialized-cap unit passed 1/1 for 0xff-heavy and NUL/control-heavy streams after actual JSON serialization; the recorded real dual-service E2E includes JSON logs for the bounded host-facing profile"
      - "the guide says incomplete ready evidence becomes `phase=degraded, ready=false` with no endpoints, inactive/starting/stopping also have no endpoints, `--format` is rejected, topology is not an app-healthcheck, and generic/missing/unknown provenance fails closed before output"
      - "the guide defines bounded `docker compose ... up -d --wait [--wait-timeout SECONDS]`: detach is explicit, wait appears once, timeout requires wait and is positive seconds (300 default, 1200 max), begins after validated import/build before launch, and covers handoff plus durable VAT runner/topology observations only—not Docker healthchecks, app HTTP, service DNS, or generic Compose"
      - "the guide states profile/generation/ticket pinning and lock release between polls prevent stale waiters after down/re-import/relaunch; ready returns one final up JSON with ready topology, timeout retains runtime/registry and supplies ps only after a current-target observation, terminal/replaced/bare-deadline cases have no unsafe next, degraded has no endpoint, and source-build cleanup_next requires verified ready"
      - "the guide teaches text `docker compose -p PROJECT exec -T SERVICE -- COMMAND` preserves observed child bytes then starts its additive VAT handoff JSON on a new line after them, plus exact agent JSON `exec -T --format json SERVICE -- COMMAND` or `--format=json`: JSON emits one vat.docker-compose.exec.v1 VAT document with profile, child exit code, separate stdout/stderr, truncated/utf8_lossy, runtime_invoked=true, compose_record_mutated=false, no raw child output or topology/endpoints; one same-read claim-held known-provenance/exact-unique-ready-MicroVM snapshot applies through child spawn, VAT parses and validates the Docker-facing delimiter but does not forward it, invoking `container exec CONTAINER COMMAND [ARG...]`, then releases the claim; child streams drain concurrently and each serialized JSON string is capped at 64 KiB; text ordering covers observed managed-child bytes only, not escaped descendants; format misordering, missing delimiter, TTY, and other flags fail closed; this is not Docker Compose exec output compatibility; the full serial fake-shim aggregate is intentionally not recorded because an independent serial run exposed a nondeterministic pre-existing Compose JSON logs timing race; the precise serialized-cap unit is 1/1, and the real-host JSON exec is covered by the bounded dual-service E2E"
      - "the guide distinguishes strict direct container inspect `docker inspect --format json CONTAINER` / equals, shared only by `docker container inspect`. Exactly one safe explicit id follows exactly one JSON selector; it must precede the id, is VAT-only, and is never forwarded, while unformatted inspect remains inherited behavior. Canonical `container inspect CONTAINER` output is one opaque bounded-captured validated Apple-native JSON value replayed byte-for-byte without a VAT wrapper or Docker Engine inspect schema. `--type`, `--size`, templates/table/YAML/TOML, filter, a second id, `--`, and unknown flags fail before Apple Container. A five-second bounded isolated observer covers root exit and both pipe EOFs; each stream is capped at 256 KiB; valid JSON plus a nonzero child exit preserves status, while malformed, oversized, or flood output suppresses raw stdout. It makes no ownership/provenance/security/image/registry/build-status/health/readiness/liveness/port-reachability claim and gives no secret-redaction guarantee. Cargo check without default features passed; shared docker_shim library passed 54/54; focused docker_inspect integration passed 5/5; the full serial fake-shim aggregate is intentionally not recorded because an independent serial run exposed a nondeterministic pre-existing Compose JSON logs timing race; real-host observation passed 1/1 (50 filtered) on Apple Container 1.1.0; inspect targets the temporary owner-labeled container, whose high-entropy name plus exact owner-label rechecks are conservative best-effort; the emergency guard retains on uncertainty; Apple Container has no atomic conditional delete, so there is no race-free/no-impossible-misdelete claim; it proves one valid native JSON document only; fake/unit tests prove byte-preservation and fail-closed details"
      - "the guide distinguishes direct logs JSON as a separate finite VAT wrapper, not a sixth Apple-native form: only `docker logs --format json --tail LINES CONTAINER` / equals forms and the same form through `docker container logs` are accepted. Exactly one format and one tail may mix spellings, must precede one safe final id, and require LINES 1..=1000; unformatted logs retain inherited text translation. VAT invokes only `container logs -n LINES CONTAINER`, never forwards the selector, and returns exactly one `vat.docker.logs.v1` / `vat_json` wrapper with untrusted `apple_container_stdio`, bounded diagnostic stderr, truncation/lossy flags, backend/container/requested_tail/runtime/child outcome, and a safe inspect next—not Docker schema or multiplex/demux. Ordinary child nonzero retains wrapper plus exit; follow, boot, timestamps, since/until, templates, duplicate/misordered selectors, unsafe/second ids, and every other modifier fail before runtime; timeout, setup, or escaped-pipe capture yields no partial wrapper after a five-second observer plus one-second cleanup with dual-stream suffix and actual serialized-string caps of 64 KiB. Cargo check passed; canonical docker_shim lib passed 54/54; focused docker_logs_json integration passed 6/6; the full serial aggregate is intentionally not recorded because an independent serial run exposed a nondeterministic pre-existing Compose JSON logs timing race. `VAT_DOCKER_SHIM_E2E_REQUIRED=1 cargo test -p vat --test vat_docker_shim apple_container_docker_run_published_port_contract -- --ignored --nocapture` passed 1/1 (50 filtered) on Apple Container 1.1.0: logs targets a temporary owner-labeled container; its high-entropy name plus exact owner-label rechecks are conservative best-effort; the emergency guard retains on uncertainty; Apple Container has no atomic conditional delete, so there is no race-free/no-impossible-misdelete claim; the shared/cacheable nginx image is not cleaned up. The host smoke proves one VAT wrapper only; fake/unit tests prove byte-preservation and fail-closed details."

      - "the guide describes direct exec JSON as a finite VAT wrapper only for `docker exec --format json --timeout SECONDS CONTAINER -- COMMAND [ARG...]` / equals forms and the same form through `docker container exec`: one format and one 1..=1200 timeout occur in either order before one safe id, the Docker-facing delimiter plus a raw command are mandatory, and raw/unformatted exec remains inherited. VAT removes selectors and that delimiter before canonical `container exec CONTAINER COMMAND [ARG...]`, then emits one `vat.docker.exec.v1` / `vat_json` wrapper with bounded untrusted stdout/stderr suffixes, `timeout_scope=host-container-client-observation`, child outcome, no redaction guarantee, and safe inspect next. TTY/interactive/detach/env/user/workdir/templates and malformed or nonexact argv reject before runtime; timeout bounds only the host client observation and does not claim guest termination. Cargo check passed; docker_shim lib passed 54/54; focused docker_exec_json passed 4/4; the direct real-host E2E passed 1/1 (50 filtered) on Apple Container 1.1.0 and observed the wrapper with both stdout/stderr markers. The temporary owner-labeled container uses a high-entropy name plus exact owner-label rechecks as conservative best-effort; the emergency guard retains on uncertainty; Apple Container has no atomic conditional delete, so there is no race-free/no-impossible-misdelete claim. It is not Docker Engine parity, generic runtime, Compose, or Kubernetes evidence."

      - "the guide documents strict direct `docker run --format json --timeout SECONDS IMAGE [COMMAND...]` / equals forms only: exactly one flexible-order format and 1..=1200 timeout appear before IMAGE, command argv follows the image directly, and a Docker `--` before IMAGE or immediately after it is rejected while `--` after the first non-`--` command token remains opaque child argv. Detach, TTY/interactive, caller lifecycle/name/label/port/network/mount/env options, and every other run flag fail before Apple Container. VAT generates a high-entropy name and independent owner label, emits one vat.docker.run.v1/vat_json wrapper only after exact owner-label cleanup confirms absence, preserves a normal nonzero child exit only then, and emits no partial wrapper for timeout/setup/cleanup uncertainty. Only Apple's explicit container-not-found diagnostic proves absence; it makes no guest-wide timeout termination, crash-recovery cleanup, Docker Engine parity, or secret-redaction claim. Deterministic coverage passed 5 plus 1 ignored in 1.80s; local alpine:3.20 E2E passed 1/1 (56 filtered) in 2.30s with exact cleanup"
      - "the guide documents strict direct `docker build --format json --timeout SECONDS --tag TAG [--file DOCKERFILE] [--build-arg K=V ...] [--target STAGE] [--platform PLATFORM] [--label K=V ...] CONTEXT` / documented equals forms as a separate bounded VAT receipt, not a sixth native JSON form: exactly one json format, one positive 1..=1200 timeout, and one tag; file/target/platform at most once; repeated build args/labels; and one canonical existing local-directory context with every option before it. `--`, missing/duplicate/misordered selectors, a second context, and unsupported flags fail before a builder; raw builds with neither selector remain inherited. VAT strips only JSON/deadline selectors and invokes public `container build`; one vat.docker.build.v1/vat_json receipt contains bounded untrusted streams, outcome, and retained_no_auto_cleanup. Success points to strict image inspect; normal nonzero retains receipt/exit but terminal=build_failed with docker --help, never stale inspect; timeout/setup/capture emits no receipt. The host deadline is observation only, not cancellation/rollback/removal. No Docker Engine/API, provenance, ownership, readiness, security, redaction, cancellation, or rollback claim follows. Current evidence is cargo check, docker_shim 62/62, focused build 4 plus 1 ignored (63 filtered), owner guard 1/1 (67 filtered), and host E2E 1/1 (67 filtered) in 2.53s. Its high-entropy tag/exact io.cclab.vat.e2e-owner label plus pre/post native absence and pre-delete recheck are test-only safety; no conditional Apple build/delete means best-effort races may leak and never add product auto-cleanup"
      - "the guide documents strict direct `docker pull --format json --timeout SECONDS IMAGE` / documented equals forms as a separate bounded VAT receipt: exactly one json format and one positive 1..=1200 timeout may reorder before one safe opaque image reference (nonempty, no leading dash, whitespace/control, URL-style `://`, or leading Git-style `git@` remote; ordinary OCI `@digest` remains opaque). `--`, second reference, missing/duplicate/misordered selectors, and unsupported flags fail before the client; raw direct pull without either selector and every `docker image pull` form remain inherited. VAT strips only JSON/deadline selectors and invokes public `container image pull IMAGE`; one bounded vat.docker.pull.v1/vat_json receipt has untrusted streams, outcome, and not_owned_no_auto_cleanup. Success points to strict image inspect without proving image state/download completion; normal nonzero retains receipt/exit but terminal=pull_failed with docker --help, never stale inspect; timeout/setup/capture/pipe failure emits no receipt. The host deadline observes client/pipes only, never registry-transfer cancellation, download completion, rollback, or local/backend image state. Keep Docker Engine/API, registry management/auth lifecycle, provenance, digest, platform, freshness, image-state, ownership, security, redaction, cancellation, download-completion, and rollback claims out of scope. Current evidence is cargo check, docker_shim 65/65, focused pull 5 plus 1 ignored (68 filtered), and host E2E 1/1 (73 filtered) in 27.14s; the real alpine:3.20 shared-cache probe neither deletes the image nor asserts ownership"
  - id: help_points_to_llm_guide
    given:
      - "an agent starts with ordinary command help"
    when:
      - "the agent runs `vat --help`"
    then:
      - "the help output lists `llm` as a command"
      - "the help output points agents to `vat llm` for the usage contract"
```

## CLI
<!-- type: cli lang: yaml -->

```yaml
commands:
  - name: vat llm
    behavior:
      - "Follow the repo-wide convention: default `--topic outline`, `--format md`, with JSON available via `--format json`."
      - "`--topic guide` prints the stable detailed guide for LLM/tool agents."
      - "Exit successfully without requiring vat.toml or VAT_HOME."
      - "Mention runner mode, direct command mode, evidence commands, retention, full capabilities versus selected-plan doctor probe boundaries (including `services.docker_services=available|unavailable|not_probed`, skipped-probe provenance, no-Docker-command and daemon=false semantics, the bounded shared-builder advisory, one MicroVm system-status probe, and nonblocking builder result), permanent headless/non-Docker boundaries, bounded one-boot K3s bootstrap diagnostics plus fail-closed `session status --verify-api`, VAT-native `session exec --format json`, and post-cleanup `session port-forward run --format json`; the independent-kubectl leased E2E covers JSON exec and bounded status verification, the local-image E2E covers one already-local Apple alpine:3.20 pod with imagePullPolicy=Never, marker log, and exact cleanup only, and the Service-forward E2E covers the strict JSON tunnel. Keep registry-pull generality, persistence, GUI, Docker Engine/API, a general tunnel, and OS-sandbox claims out of scope. Also mention five strict native-JSON forms: direct `docker ps` inventory, `docker images` inventory, direct `docker image inspect --format json IMAGE`, direct `docker inspect --format json CONTAINER` / `docker container inspect` with exactly one VAT-only selector before exactly one safe id, and bounded `docker stats`; plus a separate finite direct `docker logs --format json --tail LINES CONTAINER` / `docker container logs` VAT wrapper (not a sixth native form), with exact selectors, one safe id, canonical Apple logs argv, no multiplex/demux claim, and fail-closed bounded capture; strict Docker-shaped Compose preflight, capture-only Docker-shaped logs JSON, exact non-interactive text and VAT-native JSON Compose exec, the opt-in fail-closed Docker command shim's three Compose profiles, bounded detached-only wait with timeout/pinning/retention boundaries, exact Compose ps topology/provenance boundary, and the continuing absence of a Docker Engine socket/API."
      - "Also document strict direct `docker run --format json --timeout SECONDS IMAGE [COMMAND...]` / equals forms as a foreground owner-cleaned VAT JSON one-shot: flexible selector order before IMAGE, timeout 1..=1200, command directly after IMAGE, a Docker `--` rejected before IMAGE or immediately after it but opaque child argv after the first non-`--` command token, caller lifecycle/network/mount/env options rejected before runtime, one bounded `vat.docker.run.v1` result only after exact owner-label cleanup, and no wrapper on timeout/setup/cleanup uncertainty. Keep guest-wide timeout termination, crash recovery, Docker Engine parity, and secret-redaction claims out of scope; cite deterministic 5+1-ignored/1.80s and local alpine E2E 1/1 (56 filtered)/2.30s evidence."
      - "Also document strict direct `docker build --format json --timeout SECONDS --tag TAG [--file DOCKERFILE] [--build-arg K=V ...] [--target STAGE] [--platform PLATFORM] [--label K=V ...] CONTEXT` / documented equals forms as a separate bounded VAT receipt: format json, positive 1..=1200 timeout, and tag each occur once; file/target/platform are optional once; build args/labels repeat; and all options precede one canonical existing local-directory context. Reject `--`, missing/duplicate/misordered selectors, a second context, and unsupported flags before the builder, but leave raw unselected builds inherited. Strip only format/deadline selectors and map the supported fields to public `container build`. Document retained_no_auto_cleanup, safe success inspect next, normal nonzero build_failed/docker-help handoff without stale inspect, and no receipt on timeout/setup/capture failure. Keep host-observation-only deadline, no cancellation/rollback/removal, and no Engine/provenance/ownership/readiness/security/redaction claim explicit. Cite 62/62 lib, focused 4+1-ignored (63 filtered), owner guard 1/1 (67 filtered), and host E2E 1/1 (67 filtered)/2.53s; call high-entropy tag/exact owner-label absence checks test-only best effort that can leak on race, never product cleanup."
      - "Also document strict direct `docker pull --format json --timeout SECONDS IMAGE` / documented equals forms as a separate bounded VAT receipt: exactly one json format and positive 1..=1200 timeout may reorder before one safe opaque image reference (nonempty, no leading dash, whitespace/control, URL-style `://`, or leading Git-style `git@` remote; ordinary OCI `@digest` remains opaque). Reject `--`, second reference, missing/duplicate/misordered selectors, and unsupported flags before the client; keep raw direct pull without either selector and all `docker image pull` forms inherited. Strip only format/deadline selectors and map only to public `container image pull IMAGE`. Document not_owned_no_auto_cleanup, absent registry login/auth/credential lifecycle, safe success inspect next without image-state/download-completion proof, normal nonzero pull_failed/docker-help without stale inspect, and no receipt on timeout/setup/capture/pipe failure. Keep host-client/pipes-only deadline plus no transfer cancellation/download completion/rollback/local-or-backend image-state and no Engine/registry-management/provenance/digest/platform/freshness/ownership/security/redaction claim explicit. Cite 65/65 lib, focused 5+1-ignored (68 filtered), and host E2E 1/1 (73 filtered)/27.14s; the real shared-cache alpine probe never deletes or owns its image."
  - name: vat --help
    behavior:
      - "Remain the clap-generated flag and command reference."
      - "Point agents to `vat llm` for the compact usage contract."
```

## E2E Test
<!-- type: e2e-test lang: yaml -->

```yaml
e2e_tests:
  - id: vat-llm-agent-usage-guide
    name: "vat llm agent usage guide"
    capability_id: agent-native-gpu-native-dev-containers
    claim_id: agent-legible-state-and-diff-surface
    contract_id: agent-legible-state-and-diff-surface
    category: behavior
    command: "cargo test -p vat --test vat_toml_runner -- --nocapture"
    assertions:
      - "`vat llm --topic guide` exits successfully."
      - "The guide mentions vat.toml runner mode and direct command mode."
      - "The guide mentions state, diff, and logs evidence commands."
      - "The guide describes exact no-format and VAT-native Compose `ps --format json` / `--format=json` forms: JSON mode is one `vat.docker-compose.ps.v1` / `vat_json` document with no human table and the same claim-held profile/topology proof, not Docker Compose JSON/template/table compatibility; every other Compose ps format and invalid provenance fail closed. The full serial vat_docker_shim aggregate is intentionally not recorded because an independent serial run exposed a nondeterministic pre-existing Compose JSON logs timing race; no aggregate result is claimed for this sandbox."
      - "The guide describes strict `docker compose --dry-run -f FILE -p PROJECT up -d [--build]`: it parses only existing strict profiles and emits one vat.docker-compose.preflight.v1 document with validated=true, runtime_started=false, registry_written=false, image_built=false, launch_revalidates=true, launch_argv, and executable next using the parser's canonical source path; it invokes no Apple Container command, does not build/import/start/write registry, rejects wait and all other global/Compose flags, and real launch revalidates that same file after a cwd change."
      - "The guide describes text `logs SERVICE` preserving observed log bytes then starting its additive VAT handoff JSON on a new line after them, and `logs --format json [--tail LINES] SERVICE` capture-only VAT JSON: separate stdout/stderr, default 200/range 1..=1000 tail, truncated/utf8_lossy indicators, capture_only=true/runtime_invoked=false/compose_record_mutated=false, no topology/endpoints, and JSON-ps next. It holds existing claim/provenance without an Apple Container call or project.json mutation; VAT first caps each read and line tail, then after lossy UTF-8 plus JSON escaping retains a valid UTF-8 suffix whose serialized JSON string value remains within the same 64 KiB per-stream cap and marks it truncated. It rejects follow/timestamps/other flags and does not claim Docker Compose merged/follow/timestamp/template behavior. The full serial vat_docker_shim aggregate is intentionally not recorded because an independent serial run exposed a nondeterministic pre-existing Compose JSON logs timing race; the focused serialized-cap unit passed 1/1 for 0xff-heavy and NUL/control-heavy streams after actual JSON serialization. The recorded real dual-service E2E includes JSON logs for the bounded host-facing profile."
      - "The guide describes Compose text exec preserving observed child bytes then starting its additive VAT handoff JSON on a new line after them, and JSON only as `docker compose -p PROJECT exec -T --format json SERVICE -- COMMAND` or `--format=json`: one same-read claim-held snapshot requires known shim provenance plus exact unique ready VAT-owned MicroVM evidence through child spawn, VAT parses and validates the Docker-facing -- but does not forward it, invoking `container exec CONTAINER COMMAND [ARG...]`, then releases the claim, drains child stdout/stderr concurrently, and emits one VAT-native `vat.docker-compose.exec.v1` document with profile, child_exit_code, separate streams, per-stream truncated/utf8_lossy, runtime_invoked=true, compose_record_mutated=false, no raw child output, and no topology/endpoints. Each serialized stream string is capped at 64 KiB. Text ordering covers observed managed-child bytes only, not escaped descendants. Misordered format, missing delimiter, TTY, and all other exec flags fail closed; it is not Docker Compose exec-output compatibility. The full serial shim aggregate is intentionally not recorded because an independent serial run exposed a nondeterministic pre-existing Compose JSON logs timing race; its precise serialized-cap unit passed 1/1, and the bounded real-host JSON exec is covered by the dual-service E2E."
      - "The guide describes unchanged no-flag session status and the bounded `vat k8s session status --verify-api <id>` proof: only an active unexpired lease without a port-forward or exec marker may enter the private operation lock; it rechecks expiry after lock and immediately before the bounded private-credential API probe, verifies exact backing identity/endpoint, reports api_checked=true/api_state=reachable on success, returns non-probing api_checked=false/api_state=not_checked for expired/recovery state, and fails closed without lease/credential mutation for busy, unavailable, or identity-mismatched state. It remains one-boot/nonpersistent with no GUI or Docker Engine/API; focused fake coverage passed 4/4, a precise unit passed 1/1, and the independent-kubectl leased E2E passed 1/1 (36 filtered) in 29.97s with status --verify-api after text and strict JSON exec. That is bounded active-lease evidence, not a general API-status guarantee."
      - "The guide describes bounded text `vat k8s session exec [--timeout SECONDS] <id> -- COMMAND` and JSON `vat k8s session exec --format json [--timeout SECONDS] <id> -- COMMAND`: omission uses remaining TTL; explicit 1..=14400 seconds cannot exceed it. Both forms validate the active lease, exact backing/API identity, private credentials, and owned host API under the private lock, recheck expiry before spawn, own/reap the process group on normal exit, deadline, or interruption, and retain the lock through cleanup. A starting/live crash marker blocks later exec/delete/cleanup fail-closed rather than claiming termination. JSON returns one `vat.k8s.session.exec.v1`/`vat_json` document with child_exit_code, separate stdout/stderr with 64 KiB serialized-value caps and truncated/utf8-lossy indicators, api_verified=true, runtime_invoked=true, session_record_mutated=false, no raw replay, and no private paths. The leased real-host E2E passed 1/1 (36 filtered) in 29.97s, including JSON exec with `--timeout 30`, status verification, and exact delete."
      - "The guide describes unchanged text Service-forward and only `vat k8s session port-forward run --format json <id> service/<name> <port> -- COMMAND` for JSON: its loopback Service tunnel gives a credential-free child endpoint metadata, keeps the lock through shared-PGID cleanup, silently rechecks TTL after API proof and before exact kubectl/child spawn, then emits one post-cleanup `vat.k8s.session.port-forward.v1` document with child exit, separate 64 KiB serialized streams, no raw replay, masked VAT-owned failures, opaque-child preservation, and status-verify next. Partial reader setup cleans direct/outer children before reader joining. The independent-kubectl real-host E2E passed 1/1 (36 filtered) in 49.57s, covering one loopback Service text and strict JSON tunnel with confirmed cleanup and closed local ports; it is not a general tunnel guarantee."
      - "The guide describes strict direct inventory only as `docker ps --format json` / equals with optional exactly-once `--all` or `-a`, shared only by `docker container ls` and `docker container list`; `docker container ps` JSON remains rejected. It invokes `container list --format json [--all]`, validates one opaque Apple-native JSON value, then byte-for-byte replays stdout without a VAT wrapper or Docker Engine ps schema. Templates/table output, filter, quiet plus JSON, duplicate/unknown flags, and positionals fail before Apple Container. It is read-only inventory, not ownership/health/readiness/liveness proof; one five-second bounded isolated cleanup path covers root exit and both pipe EOFs, and malformed, oversized, or escaped-pipe output fails closed without stdout replay. Cargo check passed; shared docker_shim lib passed 54/54; focused ps fake integration passed 4/4; the full serial fake-shim aggregate is intentionally not recorded because an independent serial run exposed a nondeterministic pre-existing Compose JSON logs timing race; real-host observation passed 1/1 (50 filtered) on Apple Container 1.1.0; the temporary owner-labeled container uses a high-entropy name plus exact owner-label rechecks as conservative best-effort; the emergency guard retains on uncertainty; Apple Container has no atomic conditional delete, so there is no race-free/no-impossible-misdelete claim; ps is a global read-only inventory smoke observation, not a targeted ownership result, and proves one valid native JSON document only; fake/unit tests prove byte-preservation and fail-closed details"
      - "The guide describes strict image inventory only as `docker images --format json` / equals, shared only by `docker image ls` and `docker image list`. It invokes `container image list --format json`, bounded-captures and validates one opaque Apple-native JSON value, then byte-for-byte replays stdout without a VAT wrapper or Docker Engine image schema. Template/table/YAML/TOML output, filters, quiet, verbose, all, digests, no-trunc, positionals, duplicates, unknown flags, and `--` fail before Apple Container; text/quiet image-list behavior remains inherited. It makes no ownership/provenance/security/executability/registry/build-readiness/health/readiness/liveness claim. A five-second bounded isolated cleanup path covers root exit and both pipe EOFs, and malformed, oversized, or escaped-pipe output fails closed without stdout replay. Cargo check passed; shared docker_shim lib passed 54/54; focused docker_images_json integration passed 4/4; the full serial fake-shim aggregate is intentionally not recorded because an independent serial run exposed a nondeterministic pre-existing Compose JSON logs timing race; real-host observation passed 1/1 (50 filtered) on Apple Container 1.1.0; the temporary owner-labeled container uses a high-entropy name plus exact owner-label rechecks as conservative best-effort; the emergency guard retains on uncertainty; Apple Container has no atomic conditional delete, so there is no race-free/no-impossible-misdelete claim; images is a global read-only inventory smoke observation, not a targeted ownership result, and proves one valid native JSON document only; fake/unit tests prove byte-preservation and fail-closed details"
      - "The guide describes strict direct image inspect only as `docker image inspect --format json IMAGE` / equals: one JSON selector precedes exactly one opaque safe image reference, then VAT strips that selector and invokes only `container image inspect IMAGE`. Templates, `--`, extra references, and every other option reject before Apple Container; bounded capture validates one opaque Apple-native JSON document before byte-for-byte replay, preserves valid JSON plus a nonzero child exit, and suppresses malformed, oversized, or escaped-pipe output. It provides no Docker image-inspect schema/template/Engine API, provenance, security, registry, build-completion, readiness, or secret-redaction claim. Cargo check passed; docker_shim lib passed 58/58; focused docker_image_inspect_json passed 4/4 with 1 ignored; the opt-in host E2E passed 1/1 (61 filtered) in 1.21s and proves only one direct native image document."
      - "The guide describes strict `docker stats --no-stream --format json CONTAINER [CONTAINER...]` / equals form as read-only native Apple JSON, not a VAT/Docker Engine wrapper/schema or ownership/health/liveness proof. It uses a five-second bounded isolated cleanup path, replays only complete 256 KiB-capped validated capture, and fails closed without stdout replay for malformed, oversized, or escaped-pipe output. Shared strict-native-observation library coverage passed 54/54; the full serial fake-shim aggregate is intentionally not recorded because an independent serial run exposed a nondeterministic pre-existing Compose JSON logs timing race; real-host observation passed 1/1 (50 filtered) on Apple Container 1.1.0; stats targets the temporary owner-labeled container, whose high-entropy name plus exact owner-label rechecks are conservative best-effort; the emergency guard retains on uncertainty; Apple Container has no atomic conditional delete, so there is no race-free/no-impossible-misdelete claim; it proves one valid native JSON document only; fake/unit tests prove byte-preservation and fail-closed details"
      - "The guide preserves permanent headless, non-Docker, non-daemon, full capabilities versus selected-plan doctor semantics: a full Docker probe maps `services.docker_services` to available or unavailable; `vat capabilities` exposes only a bounded read-only shared-builder advisory (shared_unknown ownership, automatic_cleanup=false, separate configuration/observed stats/global disk, nonfatal timeout/unknown/probe errors, no lifecycle mutation). An explicit Apple-Container-only plan uses one read-only container status probe and executes no Docker command; it maps `services.docker_services` to not_probed, retains `docker.daemon_probe.state=skipped` as provenance, and makes daemon=false non-unavailable evidence. Unsupported no-OCI-route presets and named volumes fail closed; builder advisory never decides doctor runtime success; Docker/Auto/cluster retains probe. K3s remains one-boot/local-lease only and requires an independently installed kubectl on PATH before K3s use. Independent-kubectl one-shot, leased, local-image, and Service-forward E2Es passed 1/1 (36 filtered) in 28.38s, 29.97s, 49.73s, and 49.57s. The local-image result is one already-local Apple alpine:3.20 pod with imagePullPolicy=Never, a marker log, and exact session cleanup; it does not establish registry-pull generality, persistence, GUI, or Docker Engine/API. The leased result covers strict JSON exec with `--timeout 30` and bounded `status --verify-api`; the Service-forward result covers one Service-only loopback strict JSON tunnel with a credential-free child, confirmed cleanup, and closed local ports. It adds no durable status backend: `status --verify-api` remains a private-lock, identity/endpoint, expiry-rechecked bounded active-lease proof, while `session exec --format json` is a credentialed agent-result form with no raw replay or session mutation. Those results do not establish persistence, a general cluster/tunnel, OS isolation, or crash-safe termination. It also preserves the opt-in shim, three-profile Compose, detached-only bounded wait (one wait, positive 300-default/1200-max timeout after import/build, durable VAT-only proof, target pinning, lock-free polls, safe timeout/next behavior, and verified-ready source-build cleanup), host-facing no-DNS/loopback-only JSON, exact ps topology with endpoint-proof gating and no --format/app-healthcheck claim, provenance isolation, text logs/exec with new-line additive handoffs plus strict VAT-native JSON exec, and no-Docker-Engine boundaries. The recorded opt-in gated dual-service E2E passed 1/1 (50 filtered) in 4.54s only for host-facing-independent-v1 up-wait/endpoints, one-document JSON ps/logs/exec, text logs, no-final-newline text-exec handoff, and down cleanup, never as service DNS, general Compose, Docker Engine API, or Kubernetes evidence."
      - "The guide describes strict direct container inspect only as `docker inspect --format json CONTAINER` / `--format=json`, shared only by `docker container inspect`: one safe id follows one VAT-only selector before runtime, unformatted inspect remains inherited, and canonical `container inspect CONTAINER` returns one complete validated Apple-native JSON document byte-for-byte without a VAT or Docker Engine inspect schema. It rejects `--type`, `--size`, templates/table/YAML/TOML, filters, a second id, `--`, and unknown flags; five-second bounded isolated cleanup covers root exit and both pipe EOFs, each stream is 256 KiB, valid JSON with a nonzero child exit preserves status, and malformed/oversized/flood output suppresses raw stdout. It supplies no ownership/provenance/security/image/registry/build-status/health/readiness/liveness/port-reachability proof or secret-redaction guarantee. Cargo check passed; shared docker_shim lib passed 54/54; focused docker_inspect integration passed 5/5; the full serial fake-shim aggregate is intentionally not recorded because an independent serial run exposed a nondeterministic pre-existing Compose JSON logs timing race; real-host observation passed 1/1 (50 filtered) on Apple Container 1.1.0; inspect targets the temporary owner-labeled container, whose high-entropy name plus exact owner-label rechecks are conservative best-effort; the emergency guard retains on uncertainty; Apple Container has no atomic conditional delete, so there is no race-free/no-impossible-misdelete claim; it proves one valid native JSON document only; fake/unit tests prove byte-preservation and fail-closed details"
      - "The guide describes direct logs JSON as a separate finite VAT wrapper, not a sixth Apple-native form: only direct `docker logs --format json --tail LINES CONTAINER` / equals forms and the same form through `docker container logs` pass; one format and one tail may mix spelling but must precede one safe final id, and LINES is 1..=1000. Unformatted logs stay inherited, the selector is never forwarded, and canonical backend argv is `container logs -n LINES CONTAINER`. Apple text stdout becomes exactly one `vat.docker.logs.v1` / `vat_json` wrapper with untrusted stdio, bounded diagnostic stderr, truncation/lossy flags, backend/container/requested_tail/runtime/child outcome, and safe inspect next—not Docker schema/multiplex/demux. Ordinary child nonzero retains wrapper+exit; follow/boot/timestamps/since/until/templates and all other modifiers reject before runtime, while timeout/setup/escaped-pipe failure emits no partial wrapper after five-second plus one-second bounded dual-stream suffix/serialized caps. Cargo check passed; docker_shim lib passed 54/54; focused docker_logs_json integration passed 6/6; the full serial aggregate is intentionally not recorded because of the nondeterministic pre-existing Compose JSON logs timing race. `VAT_DOCKER_SHIM_E2E_REQUIRED=1 cargo test -p vat --test vat_docker_shim apple_container_docker_run_published_port_contract -- --ignored --nocapture` passed 1/1 (50 filtered) on Apple Container 1.1.0: logs targets a temporary owner-labeled container; its high-entropy name plus exact owner-label rechecks are conservative best-effort; the emergency guard retains on uncertainty; Apple Container has no atomic conditional delete, so there is no race-free/no-impossible-misdelete claim; the shared/cacheable nginx image is not cleaned up. The host smoke proves one wrapper only; fake/unit tests prove byte-preservation and fail-closed details."
      - "The guide documents direct exec JSON only as `docker exec --format json --timeout SECONDS CONTAINER -- COMMAND [ARG...]` / equals forms and `docker container exec`: exact one format plus one 1..=1200 timeout precede one safe id and a mandatory Docker-facing delimiter/raw command; raw exec remains inherited. VAT strips its selectors and delimiter before `container exec CONTAINER COMMAND [ARG...]`, emits one `vat.docker.exec.v1` / `vat_json` wrapper with separate serialized-64-KiB-capped untrusted streams and `timeout_scope=host-container-client-observation`, preserves ordinary child failure wrapper+exit, and emits no partial wrapper on timeout/setup/capture failure. The timeout does not claim guest termination. Deterministic docker_shim lib validation passed 54/54 and focused docker_exec_json passed 4/4; the direct real-host E2E passed 1/1 (50 filtered) on Apple Container 1.1.0 with an exec wrapper containing both stdout/stderr markers. The temporary owner-labeled container uses a high-entropy name plus exact owner-label rechecks as conservative best-effort; the emergency guard retains on uncertainty; Apple Container has no atomic conditional delete, so there is no race-free/no-impossible-misdelete claim. It is not Docker Engine parity, generic runtime, Compose, or Kubernetes evidence."
      - "The emitted guide documents the strict direct run JSON one-shot: only direct docker run with flexible-order format/timeout selectors before IMAGE, 1..=1200 timeout, raw command argv directly after IMAGE, a Docker `--` rejected before IMAGE or immediately after it but opaque child argv after the first non-`--` command token, caller lifecycle/network/mount/env options rejected before runtime, one owner-cleaned vat.docker.run.v1 result after exact absence, and no partial wrapper on timeout/setup/cleanup uncertainty. It preserves the narrow evidence: deterministic docker_run_json 5 plus 1 ignored in 1.80s and local alpine E2E 1/1 (56 filtered) in 2.30s, without guest-wide termination, crash-recovery, Docker Engine, or redaction claims."
      - "The emitted guide documents the strict direct build JSON receipt: only direct docker build with exact json/1..=1200-timeout/tag selectors, documented optional file/build-arg/target/platform/label fields before one canonical existing local-directory context, and no `--`, second context, or unsupported/misordered form. It strips only JSON/deadline selectors for public container build, returns one bounded vat.docker.build.v1 receipt with retained_no_auto_cleanup, safe success image-inspect next, and normal nonzero build_failed/docker-help without stale inspect; timeout/setup/capture emits no receipt. The deadline is host observation only, never cancellation/rollback/removal, and no Engine/provenance/ownership/readiness/security/redaction claim follows. It records 62/62 library, focused 4 plus 1 ignored (63 filtered), owner guard 1/1 (67 filtered), and opt-in host receipt E2E 1/1 (67 filtered) in 2.53s. The host test's high-entropy tag/exact label plus absence checks are test-only best effort; a race leaks rather than turning product builds into auto-cleanup."
      - "The emitted guide documents the strict direct pull JSON receipt: only direct docker pull with one json and one 1..=1200 timeout selector before one safe opaque image reference (nonempty, no leading dash, whitespace/control, URL-style `://`, or leading Git-style `git@` remote; ordinary OCI `@digest` remains opaque); `--`, second reference, missing/duplicate/misordered selectors, and unsupported flags reject before the client, while raw unselected direct pull and docker image pull remain inherited. It strips only JSON/deadline selectors for public container image pull and returns one bounded vat.docker.pull.v1 receipt with not_owned_no_auto_cleanup and absent registry-management/auth lifecycle. Success has strict image-inspect next without image-state/download-completion proof; ordinary nonzero is pull_failed/docker-help without stale inspect; timeout/setup/capture/pipe failure has no receipt. Host deadline is client/pipes observation only, not transfer cancellation/download completion/rollback/local-or-backend image state. It records 65/65 library, focused 5 plus 1 ignored (68 filtered), and opt-in host E2E 1/1 (73 filtered) in 27.14s; the real shared alpine cache probe deletes nothing and asserts no image ownership."
```

## Changes
<!-- type: changes lang: yaml -->

```yaml
changes:
  - path: apps/vat/src/commands/llm.rs
    action: create
    section: scenarios
    impl_mode: hand-written
    reason: "Implements the `llm_reads_compact_usage_contract` scenario with Docker-service tri-state/doctor P1 provenance, shared-builder advisory boundaries, bounded K3s bootstrap diagnostics plus credentialed JSON session exec without a real-host claim, strict Compose preflight, capture-only logs JSON, and the stable three-profile Compose, bounded detached wait, ps topology/endpoint-proof, negative-contract, and provenance guidance."
  - path: apps/vat/src/cli.rs
    action: modify
    section: source
    impl_mode: hand-written
    reason: "Register `vat llm` and make root help point agents to it."
  - path: apps/vat/src/commands/mod.rs
    action: modify
    section: source
    impl_mode: hand-written
    reason: "Expose the new llm command module."
  - path: apps/vat/src/commands/llm.rs
    action: create
    section: source
    impl_mode: hand-written
    reason: "Print the stable LLM usage guide."
  - path: apps/vat/tests/vat_toml_runner.rs
    action: modify
    section: source
    impl_mode: hand-written
    reason: "Add a binary smoke test for the LLM guide contract."
  - path: apps/vat/tests/vat_toml_runner.rs
    action: validate
    section: e2e-test
    impl_mode: hand-written
    reason: "Verifies the `vat llm --topic guide` guide mentions the core agent commands and non-Docker boundaries."
  - path: apps/vat/README.md
    action: modify
    section: cli
    impl_mode: hand-written
    reason: "Document `vat llm` for operators and agents."
```
