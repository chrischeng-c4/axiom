---
id: github
fill_sections: [overview, schema, changes]
capability_refs:
  - id: github-issues-proxy
    role: primary
    claim: github-issues-proxy-service
    coverage: full
---

## Overview
<!-- type: overview lang: markdown -->

Module `github` (Rust) — 15 symbol(s) (10 public). Spec auto-generated by `score fillback`; `impl_mode: hand-written` until extended with proper Schema / Logic sections.

### Symbols

| Name | Kind | Visibility | Line |
|------|------|------------|------|
| `GITHUB_TOKEN_ENV` | constant | pub | 24 |
| `ALLOWED_REPOS_ENV` | constant | pub | 27 |
| `DEFAULT_ALLOWED_REPO` | constant | priv | 28 |
| `GithubError` | enum | pub | 34 |
| `GithubClient` | struct | pub | 41 |
| `from_env` | function | pub | 51 |
| `is_allowed` | function | pub | 76 |
| `search_issues` | function | pub | 84 |
| `view_issue` | function | pub | 108 |
| `create_issue` | function | pub | 116 |
| `comment_issue` | function | pub | 129 |
| `get` | function | priv | 146 |
| `send` | function | priv | 150 |
| `percent_encode_query` | function | priv | 177 |
| `tests` | module | priv | 191 |

### Public Signatures

```rust
/// Resolve the credential + allow-list from env. Fails fast when [`GITHUB_TOKEN_ENV`] is unset — matches [`crate::http::auth::AuthConfig::resolve`]'s fail-fast-at-startup discipline.
from_env() -> Result<Self>

/// Whether `owner/name` is in [`ALLOWED_REPOS_ENV`] (or its default).
is_allowed(&self, owner: &str, name: &str) -> bool

/// `GET /search/issues` scoped to one repo — mirrors `issue.rs::search`'s query shape (`repo:` + `is:issue` + optional `state:`/free text).
search_issues(
        &self,
        owner: &str,
        name: &str,
        state: &str,
        q: Option<&str>,
        limit: u32,
    ) -> Result<Value, GithubError>

/// `GET /repos/{owner}/{name}/issues/{number}`.
view_issue(&self, owner: &str, name: &str, number: u64) -> Result<Value, GithubError>

/// `POST /repos/{owner}/{name}/issues` — `payload` is forwarded verbatim (the caller shapes `title`/`body`/`labels`, same as `issue.rs::issue_payload`).
create_issue(
        &self,
        owner: &str,
        name: &str,
        payload: &Value,
    ) -> Result<Value, GithubError>

/// Reopen (`PATCH state=open`, idempotent when already open), then `POST` the comment — matches `issue.rs::comment`'s reopen-then-comment semantics.
comment_issue(
        &self,
        owner: &str,
        name: &str,
        number: u64,
        payload: &Value,
    ) -> Result<Value, GithubError>

```

### Imports

- `anyhow` (external)
- `reqwest::StatusCode` (external)
- `serde_json::Value` (external)

## Schema
<!-- type: schema lang: yaml -->

```yaml
schemas:
  - title: GithubError
    type: enum
    rust_type: GithubError
    enum:
      - Upstream
      - Github
  - title: GithubClient
    type: object
    rust_type: GithubClient
    required: [http, token, allowed_repos]
    properties:
      http:
        rust_type: "reqwest::Client"
      token:
        rust_type: "String"
      allowed_repos:
        rust_type: "Vec<String>"
```

## Changes
<!-- type: changes lang: yaml -->

```yaml
changes:
- action: modify
  anchor: GithubClient
  description: 'Pre-existing module captured by `score fillback`.

    Governance: hand-written until extended with

    Schema / Logic / Interface sections.'
  impl_mode: hand-written
  path: apps/courier/src/http/github.rs
  section: schema
coverage_kind: semantic
```
