---
id: mod
fill_sections: [overview, schema, changes]
capability_refs:
  - id: github-issues-proxy
    role: primary
    claim: github-issues-proxy-service
    coverage: full
---

## Overview
<!-- type: overview lang: markdown -->

Module `mod` (Rust) — 11 symbol(s) (9 public). Spec auto-generated by `score fillback`; `impl_mode: hand-written` until extended with proper Schema / Logic sections.

### Symbols

| Name | Kind | Visibility | Line |
|------|------|------------|------|
| `auth` | module | pub | 21 |
| `github` | module | pub | 22 |
| `openapi` | module | pub | 23 |
| `routes` | module | pub | 24 |
| `AppState` | struct | pub | 41 |
| `new` | function | pub | 52 |
| `github` | function | pub | 60 |
| `verifier` | function | priv | 64 |
| `start_drain` | function | pub | 70 |
| `is_draining` | function | priv | 78 |
| `router` | function | pub | 86 |

### Public Signatures

```rust
/// Build state from a resolved [`github::GithubClient`] and [`auth::AuthConfig`]: the data-plane auth layer runs the registry verifier when auth is required, the open verifier when off (the `COURIER_AUTH=off` default).
new(github: GithubClient, auth: auth::AuthConfig) -> Self

github(&self) -> &GithubClient

/// Flip readiness to draining so `/readyz` returns 503. Called on SIGTERM via `service_http::shutdown_with_drain`.
start_drain(&self)

/// Build the HTTP/2 router for the GitHub-issues-proxy data plane.  @spec apps/courier/tech-design/interfaces/rest/github-issues-proxy.md#logic
router(state: AppState) -> Router

```

### Imports

- `std::sync::atomic` (external)
- `std::sync::Arc` (external)
- `axum::middleware::from_fn_with_state` (external)
- `axum::routing` (external)
- `axum::Router` (external)
- `service_auth::StaticRoleMapVerifier` (external)
- `service_http::MetricsProvider` (external)
- `crate::http::github::GithubClient` (internal)

## Schema
<!-- type: schema lang: yaml -->

```yaml
schemas:
  - title: AppState
    type: object
    rust_type: AppState
    required: [github, draining, verifier]
    properties:
      github:
        rust_type: "Arc<GithubClient>"
      draining:
        rust_type: "Arc<AtomicBool>"
      verifier:
        rust_type: "Arc<StaticRoleMapVerifier>"
```

## Changes
<!-- type: changes lang: yaml -->

```yaml
changes:
- action: modify
  anchor: AppState
  description: 'Pre-existing module captured by `score fillback`.

    Governance: hand-written until extended with

    Schema / Logic / Interface sections.'
  impl_mode: hand-written
  path: apps/courier/src/http/mod.rs
  section: schema
coverage_kind: semantic
```
