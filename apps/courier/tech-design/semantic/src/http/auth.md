---
id: auth
fill_sections: [overview, schema, changes]
---

## Overview
<!-- type: overview lang: markdown -->

Module `auth` (Rust) — 8 symbol(s) (8 public). Spec auto-generated by `score fillback`; `impl_mode: hand-written` until extended with proper Schema / Logic sections.

### Symbols

| Name | Kind | Visibility | Line |
|------|------|------------|------|
| `AUTH_MODE_ENV` | constant | pub | 50 |
| `TOKEN_REGISTRY_FILE_ENV` | constant | pub | 52 |
| `LEGACY_TOKENS_ENV` | constant | pub | 54 |
| `AuthConfig` | struct | pub | 58 |
| `open` | function | pub | 65 |
| `resolve` | function | pub | 78 |
| `verifier` | function | pub | 102 |
| `authorize` | function | pub | 113 |

### Public Signatures

```rust
/// Open/dev config: auth off, no tokens.
open() -> Self

/// Resolve from the serve flags (each with env fallback): parse the mode, then load the registry through the shared loader. Fails fast — naming `COURIER_TOKEN_REGISTRY_FILE` — when auth is required but the registry file is missing, unparseable, or resolves empty: a server that can never authenticate anyone is a startup misconfiguration, not a per-request 401.
resolve(
        mode: &str,
        registry_file: Option<&str>,
        legacy_tokens_json: Option<&str>,
    ) -> Result<Self>

/// The verifier the data-plane `auth_middleware` runs: the shared static role-map over this registry (the `open()` shape when auth is off).
verifier(&self) -> StaticRoleMapVerifier

/// Per-repo authorization on the `{owner}/{name}` resource — the service-auth split: the middleware authenticates, handlers authorize. `create`/`comment` pass [`Role::Write`]; `search`/`view` pass [`Role::Read`]. A denial maps the structured [`service_auth::RoleMapDenied`] into the shared 403 `{"error": "forbidden", "message": ...}` shape.
authorize(
    principal: &RoleMapPrincipal,
    resource: &str,
    needed: Role,
) -> Result<(), AuthError>

```

### Imports

- `std::collections::HashMap` (external)
- `anyhow` (external)
- `service_auth` (external)

## Schema
<!-- type: schema lang: yaml -->

```yaml
schemas:
  - title: AuthConfig
    type: object
    rust_type: AuthConfig
    required: [required, tokens]
    properties:
      required:
        rust_type: "bool"
      tokens:
        rust_type: "HashMap<String, TokenClaims>"
```

## Changes
<!-- type: changes lang: yaml -->

```yaml
changes:
  - path: apps/courier/src/http/auth.rs
    action: modify
    impl_mode: hand-written
    description: |
      Pre-existing module captured by `score fillback`.
      Governance: hand-written until extended with
      Schema / Logic / Interface sections.
```
