[project]
name = "courier"
cap_path = "CAPABILITIES.md"
label = "app:courier"

[capability.profile]
traits = ["network_exposed", "long_running"]

[[workspaces]]
name = "courier"
paths = ["app/**", "pyproject.toml", "uv.lock"]
target = "python"
test_cmd = "cd apps/courier && uv run pytest"
