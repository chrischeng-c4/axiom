[project]
name = "courier"
cap_path = "README.md"
label = "app:courier"

[capability.profile]
traits = ["network_exposed", "long_running"]

[[workspaces]]
name = "courier"
paths = ["**"]
target = "python"
test_cmd = "uv run pytest"
