[project]
name = "courier"
cap_path = "README.md"
label = "app:courier"

[capability.profile]
traits = ["service", "network_exposed", "long_running", "cli_facing"]

[[workspaces]]
name = "courier"
paths = ["**"]
target = "rust"
test_cmd = "cargo test -p courier"
codegen.profile = "rust/score-crate"
