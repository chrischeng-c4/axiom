ec_review_backing = "agent"

[project]
name = "workbench"
cap_path = "CAPABILITIES.md"
label = "app:workbench"

[[workspaces]]
name = "workbench"
paths = ["**"]
target = "rust"
test_cmd = "cargo test -p workbench --tests -- --nocapture"
