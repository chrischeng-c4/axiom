---
id: 2219
summary: External contract for 2219.
fill_sections: [e2e-test]
---

# EC: 2219

## External Contract
<!-- type: e2e-test lang: yaml -->

```yaml
e2e_tests:
  - id: defer-http2-live-one-port-route-contract
    capability_id: http2-api-list
    claim_id: h2c-openapi-route-list
    contract_id: one-port-http1-h2c-standard-and-domain-routes
    category: behavior
    test_path: apps/defer/tests/behavior_defer_http2_live_one_port_route_contract.rs
    command: "cargo test -p defer --test http_api -- --nocapture"
    assertions:
      - "Independent HTTP/1.1 and prior-knowledge h2c clients use the same bound service URL and each receive 200 from /healthz, /readyz, /docs, /openapi.json, and /metrics, while an HTTP/1.1 queue GET succeeds after an h2c queue PUT."
      - "The served OpenAPI JSON is parsed and must equal the canonical Defer OpenAPI IR; its exact sorted inventory is nine method/path operations covering queue GET/PUT/control, task create/batch-create/status/cancel, dispatch, and admin backup, so an added, omitted, or wrong-method operation fails."
      - "Real live requests exercise every advertised domain operation: queue configure/read/control, individual and batch task creation, task status and cancellation, dispatch to a real target, and backup whose bytes recover the committed terminal task into a fresh Raft store."
  - id: defer-http2-offline-spec-and-three-client-codegen
    capability_id: http2-api-list
    claim_id: h2c-openapi-route-list
    contract_id: offline-openapi-route-twin-and-typed-client-generation
    category: behavior
    test_path: apps/defer/tests/behavior_defer_http2_offline_spec_and_three_client_codegen.rs
    command: "cargo test -p defer --test cli_contract offline_spec_and_typed_client_generation_use_one_contract -- --nocapture"
    assertions:
      - "defer spec --format openapi exits successfully, emits parseable chainable JSON followed by exactly next: done, equals the canonical OpenAPI IR semantically, and contains the exact same nine domain operations as the live contract."
      - "defer spec --format routes emits exactly all nine method/path strings including task batch creation and admin backup plus a terminal marker; a stale hand-maintained subset or extra route fails equality."
      - "TypeScript, Python, and Rust generation each exits successfully with an exact language-specific file inventory and terminal marker; every generated client contains all nine typed operations and the batch path, while Python includes sync and async h2c clients."
```
