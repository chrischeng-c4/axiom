# HANDWRITE-BEGIN gap="missing-generator:deployment:defer-direct-install-base" tracker="#766" reason="Defer's direct-install StatefulSet composition remains hand-authored until service-k8s emits the application base."
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: defer
resources:
  - namespace.yaml
  - serviceaccount.yaml
  - configmap.yaml
  - statefulset.yaml
  - service.yaml
  - pdb.yaml
labels:
  - pairs:
      app.kubernetes.io/name: defer
      app.kubernetes.io/part-of: defer
    includeSelectors: false
# HANDWRITE-END
