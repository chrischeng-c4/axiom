apiVersion: v1
kind: ConfigMap
metadata:
  name: defer-config
  namespace: defer
data:
  DEFER_BIND: "0.0.0.0:7141"
  DEFER_GRACE_SECS: "30"
  DEFER_LOG_FORMAT: "json"
  DEFER_AUTH: "off"
  RUST_LOG: "info"
