apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: defers.defer.dev
spec:
  group: defer.dev
  names:
    categories: []
    kind: Defer
    plural: defers
    shortNames:
    - dfr
    singular: defer
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.readyReplicas
      name: Ready
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for DeferSpec via `CustomResource`
        properties:
          spec:
            description: 'The generic cluster shape every sharded-HA service embeds in its CRD spec via `#[serde(flatten)] pub cluster: service_k8s::ClusterSpec`. @spec libs/service-k8s/tech-design/semantic/source/libs-service-k8s-src-service-rs.md#source'
            properties:
              auth:
                default: "off"
                type: string
              backup:
                description: |-
                  Kubernetes structural-schema-safe scheduled backup projection.

                  The runtime [`BackupPolicy`] uses a tagged [`BackupDestination`] enum whose variant schemas cannot be embedded directly in a CRD. This flat shape keeps the public `schedule`, `destination`, and `retentionSecs` fields shared by every service operator and validates them through one conversion path.
                nullable: true
                properties:
                  adminTokenSecret:
                    nullable: true
                    type: string
                  destination:
                    description: Destination URI accepted by [`BackupDestination::from_uri`].
                    type: string
                  retentionSecs:
                    description: Drop objects older than this many seconds after a successful put. `None` keeps every object.
                    type: integer
                    minimum: 0.0
                    nullable: true
                  schedule:
                    description: Cron expression rendered into `CronJob.spec.schedule`.
                    type: string
                required:
                - destination
                - schedule
                type: object
              bootstrapSeedUri:
                nullable: true
                type: string
              graceSecs:
                default: 10
                type: integer
                minimum: 0.0
              image:
                type: string
              imagePullPolicy:
                nullable: true
                type: string
              logLevel:
                nullable: true
                type: string
              peerTlsSecret:
                nullable: true
                type: string
              replicasPerShard:
                default: 1
                description: Starting/minimum members per shard. With startup-static raft-runtime membership this is also the fixed desired value; a future membership controller may plan whole replica layers above this floor.
                type: integer
                minimum: 0.0
              resources:
                default:
                  cpu: ''
                  memory: ''
                description: Per-pod CPU/memory requests. Empty values resolve to the shared data-plane defaults (`1` CPU / `4Gi`) at render time. Limits are intentionally omitted so a dedicated-node pod can use otherwise-idle node capacity. @spec libs/service-k8s/tech-design/semantic/source/libs-service-k8s-src-service-rs.md#source
                properties:
                  cpu:
                    default: ''
                    type: string
                  memory:
                    default: ''
                    type: string
                type: object
              shardCount:
                default: 1
                type: integer
                minimum: 0.0
              storage:
                default: 10Gi
                type: string
              storageClass:
                nullable: true
                type: string
              targetSigningKeyId:
                nullable: true
                type: string
              targetSigningSecret:
                nullable: true
                type: string
              tokensSecret:
                nullable: true
                type: string
              tokensSecretProviderClass:
                nullable: true
                type: string
              voterCount:
                default: 1
                type: integer
                minimum: 0.0
            required:
            - image
            type: object
          status:
            nullable: true
            properties:
              desiredReplicas:
                default: 0
                format: int32
                type: integer
              message:
                default: ''
                type: string
              observedGeneration:
                default: 0
                format: int64
                type: integer
              phase:
                default: ''
                type: string
              readyReplicas:
                default: 0
                format: int32
                type: integer
            type: object
        required:
        - spec
        title: Defer
        type: object
    served: true
    storage: true
    subresources:
      status: {}
