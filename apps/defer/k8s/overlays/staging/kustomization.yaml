# HANDWRITE-BEGIN gap="missing-generator:deployment:defer-staging-overlay" tracker="#766" reason="Defer staging storage and optional component composition remains hand-authored until service-k8s emits profile overlays."
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: [../../base]
components:
  - ../../components/network-policy
  - ../../components/observability
patches:
  - target: {kind: StatefulSet, name: defer}
    patch: |-
      - {op: replace, path: /spec/volumeClaimTemplates/0/spec/resources/requests/storage, value: 20Gi}
# HANDWRITE-END
