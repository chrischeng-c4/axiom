# HANDWRITE-BEGIN gap="missing-generator:deployment:defer-dev-overlay" tracker="#766" reason="Defer development logging and storage patches remain hand-authored until service-k8s emits profile overlays."
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: [../../base]
patches:
  - target: {kind: ConfigMap, name: defer-config}
    patch: |-
      - {op: replace, path: /data/RUST_LOG, value: debug}
  - target: {kind: StatefulSet, name: defer}
    patch: |-
      - {op: replace, path: /spec/volumeClaimTemplates/0/spec/resources/requests/storage, value: 1Gi}
# HANDWRITE-END
