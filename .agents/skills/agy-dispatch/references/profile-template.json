{
  "root": "/absolute/path/to/persistent-project-worktree/.agy-worktrees/issue-1234",
  "agy_project_root": "/absolute/path/to/persistent-project-worktree",
  "repo": "owner/repository",
  "agy_project_id": "registered-agy-project-id-for-the-persistent-root",
  "state_dir": "/tmp/agy-dispatch/registered-agy-project-id-for-the-persistent-root/1234",
  "mode": "measure-only",
  "external_payload_consent": {
    "mode": "standing",
    "consent_id": "all-bounded-work-items-v1"
  },
  "task_contract": {
    "kind": "measurement",
    "session_policy": "ticketed",
    "issue": "1234",
    "design_inputs": []
  },
  "model": "gemini-3.7-flash-high",
  "worktree_layout": "in-project",
  "launch_cwd": "task-worktree",
  "timeout": "30m",
  "sandbox": false,
  "inject_prompt_file": "/absolute/path/to/optional-round-prompt.md",
  "project_settings": {
    "outside_of_folder_file_access": "always_deny"
  },
  "project_policy_observation": {
    "source": "official_project_ui_or_permissions",
    "observed_at": "REPLACE_WITH_FRESH_OFFICIAL_UI_OBSERVATION_TIME",
    "project_id": "registered-agy-project-id-for-the-persistent-root",
    "matching_project_ids": ["registered-agy-project-id-for-the-persistent-root"],
    "project_root": "/absolute/path/to/persistent-project-worktree",
    "permissions": {
      "allow": [],
      "deny": [],
      "ask": []
    },
    "outside_of_folder_file_access": "always_deny"
  },
  "global_permissions": {
    "allow": [
      "command(pwd)",
      "command(rg)",
      "command(sed)",
      "command(shasum)",
      "command(git log)",
      "command(git status)",
      "command(git diff)",
      "command(git show)",
      "command(git rev-parse)",
      "command(git ls-files)",
      "command(git merge-base)",
      "command(uv)",
      "command(python3)"
    ],
    "deny": [
      "command(git add)",
      "command(git commit)",
      "command(git push)",
      "command(git checkout)",
      "command(git switch)",
      "command(git reset)",
      "command(git restore)",
      "command(git stash)",
      "command(git worktree)",
      "command(git merge)",
      "command(git rebase)",
      "command(git cherry-pick)",
      "command(git revert)",
      "command(git clean)",
      "command(git tag)",
      "command(git update-ref)",
      "command(git apply)",
      "command(git am)",
      "command(git rm)",
      "command(git mv)",
      "command(gh issue close)",
      "command(gh issue edit)",
      "command(gh issue comment)",
      "command(gh pr create)",
      "command(gh pr merge)",
      "command(gh release create)",
      "command(gh api)",
      "command(rm -rf)"
    ],
    "ask": []
  },
  "project_permissions": {
    "allow": [],
    "deny": [],
    "ask": []
  },
  "task_commands": {
    "allow": [
      "pwd",
      "git rev-parse HEAD",
      "git status --short",
      "rg -n 'EXACT_SELECTOR' src/allowed-file.rs",
      "sed -n '1,220p' src/allowed-file.rs",
      "shasum -a 256 /absolute/path/to/pinned-artifact"
    ],
    "deny": [
      "git push origin main",
      "git commit -am forbidden"
    ]
  },
  "protected_artifacts": [
    {"path": "/absolute/path/to/pinned-artifact", "sha256": "replace-with-current-sha256"}
  ],
  "snapshot_paths": ["src", "tests"],
  "allowed_repo_writes": [],
  "path_change_budgets": {}
}
