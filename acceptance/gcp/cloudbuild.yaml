steps:
  - id: lumen
    name: gcr.io/cloud-builders/docker
    args:
      - build
      - --file=acceptance/gcp/images/Dockerfile.services
      - --build-arg=SOURCE_REVISION=${_GIT_SHA}
      - --target=lumen
      - --tag=${_REGISTRY}/lumen:${_TAG}
      - .

  - id: sift
    name: gcr.io/cloud-builders/docker
    waitFor: ["lumen"]
    args:
      - build
      - --file=acceptance/gcp/images/Dockerfile.services
      - --build-arg=SOURCE_REVISION=${_GIT_SHA}
      - --target=sift
      - --tag=${_REGISTRY}/sift:${_TAG}
      - .

images:
  - ${_REGISTRY}/lumen:${_TAG}
  - ${_REGISTRY}/sift:${_TAG}

tags:
  - axiom-gcp-operator-acceptance
  - axiom-run-${_RUN_ID}

timeout: 2700s

options:
  machineType: E2_HIGHCPU_8
  logging: CLOUD_LOGGING_ONLY
