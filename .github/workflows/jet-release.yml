# Build `jet` binaries for all supported targets.
#
# Triggers:
#   - push of a tag matching `jet@*`    → build + attach to GitHub release
#                                         (e.g. jet@0.1.0, jet@0.4.0)
#   - workflow_dispatch                 → build artifacts only (manual smoke)
#
# Targets (macOS Intel intentionally dropped — Apple Silicon only):
#   - aarch64-apple-darwin      (Apple Silicon,      macos-14 native)
#   - x86_64-unknown-linux-gnu  (most Linux servers, ubuntu-latest native)
#   - aarch64-unknown-linux-gnu (Linux on ARM,       ubuntu-latest cross)
#
# jet is a Rust-native build tool / package manager for JS/TS. Its release
# binary links no native HTTP client into the build graph that needs openssl,
# so the aarch64 Linux cross leg needs only the gcc cross-linker — same shape
# as lumen/cap/vat/meter.
name: jet-release

on:
  push:
    tags:
      - "jet@*"
  workflow_dispatch:

permissions:
  contents: write  # needed to create / upload release assets

jobs:
  build:
    name: build (${{ matrix.target }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: aarch64-apple-darwin
            os: macos-14
            cross: false
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            cross: false
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
            cross: true

    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install cross-compilation toolchain (Linux aarch64)
        if: matrix.cross
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu
          mkdir -p .cargo
          cat >> .cargo/config.toml <<'EOF'
          [target.aarch64-unknown-linux-gnu]
          linker = "aarch64-linux-gnu-gcc"
          EOF

      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: jet-${{ matrix.target }}

      - name: Build jet binary
        # --bin jet: only the CLI binary (skip the lib's integration-test bins).
        run: cargo build --release --locked -p jet --bin jet --target ${{ matrix.target }}

      - name: Package artifact
        id: package
        shell: bash
        run: |
          set -euo pipefail
          name="jet-${{ matrix.target }}"
          # Build inside a `pkg/` staging dir, then tar with -C so the
          # archive layout is `jet-<target>/{jet,README.md}` — matches
          # what install.sh expects on extract.
          stage="pkg/${name}"
          mkdir -p "${stage}"
          cp "target/${{ matrix.target }}/release/jet" "${stage}/jet"
          cp projects/jet/README.md "${stage}/README.md"
          # Emit the tarball + sha256 into `out/` (workspace root) so
          # upload-artifact's preserved `out/` prefix is shallow and
          # the release glob below is predictable.
          mkdir -p out
          tarball="out/${name}.tar.gz"
          tar -C pkg -czf "${tarball}" "${name}"
          if command -v shasum >/dev/null; then
            shasum -a 256 "${tarball}" | awk '{print $1}' > "${tarball}.sha256"
          else
            sha256sum "${tarball}" | awk '{print $1}' > "${tarball}.sha256"
          fi
          echo "tarball=${tarball}" >> "${GITHUB_OUTPUT}"
          echo "sha=${tarball}.sha256" >> "${GITHUB_OUTPUT}"

      - uses: actions/upload-artifact@v4
        with:
          name: jet-${{ matrix.target }}
          path: |
            ${{ steps.package.outputs.tarball }}
            ${{ steps.package.outputs.sha }}
          if-no-files-found: error
          overwrite: true

  release:
    name: publish release
    needs: build
    if: startsWith(github.ref, 'refs/tags/jet@')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      # upload-artifact@v4 preserves the source path (`out/...`) inside
      # the artifact, so after merge-multiple the layout is
      # `dist/out/jet-*.tar.gz`. Glob with `**` so the release picks
      # them up regardless of nesting depth.
      - name: List artifacts (debug)
        run: find dist -type f -maxdepth 4 -print

      - name: Create / update release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/**/jet-*.tar.gz
            dist/**/jet-*.tar.gz.sha256
          generate_release_notes: true
          body: |
            `jet` release built from tag ${{ github.ref_name }}.

            ## Install

            ```sh
            curl -fsSL https://raw.githubusercontent.com/${{ github.repository }}/${{ github.ref_name }}/projects/jet/install.sh | sh
            ```

            Or pick a target tarball below and extract it manually.
            macOS is Apple Silicon only; for Linux choose x86_64 or aarch64.
