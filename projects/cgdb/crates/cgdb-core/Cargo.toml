# HANDWRITE-BEGIN gap="missing-generator:hand-written:b46a7789" tracker="2087" reason="Shared types (Catalog, GraphRecord, RpcEnvelope) library crate."
[package]
name = "cgdb-core"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
anyhow = "1"
# HANDWRITE-END
