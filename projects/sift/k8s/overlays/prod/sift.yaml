# HANDWRITE-BEGIN gap="sift-kustomize-prod-instance" tracker="1606" reason="Keep production templates on the supported 1x1 topology until Sift owns safe membership changes."
apiVersion: sift.axiom.dev/v1alpha1
kind: Sift
metadata:
  name: sift
  namespace: production
spec:
  image: ghcr.io/chrischeng-c4/axiom/sift:0.1.1
  replicasPerShard: 1
  voterCount: 1
  dataSize: 500Gi
  auth: required
  tokensSecret: REPLACE_ME__SIFT_TOKENS_SECRET
  backup:
    schedule: "0 * * * *"
    destination: REPLACE_ME__OFF_NODE_BACKUP_URI
    retentionSecs: 604800
    adminTokenSecret: REPLACE_ME__SIFT_BACKUP_ADMIN_TOKEN_SECRET
# HANDWRITE-END
