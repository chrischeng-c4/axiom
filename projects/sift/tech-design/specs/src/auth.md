---
id: auth
fill_sections: [overview, schema, changes]
---

## Overview
<!-- type: overview lang: markdown -->

Module `auth` (Rust) — 12 symbol(s) (7 public). Spec auto-generated by `score fillback`; `impl_mode: hand-written` until extended with proper Schema / Logic sections.

### Symbols

| Name | Kind | Visibility | Line |
|------|------|------------|------|
| `TOKEN_REGISTRY_FILE_ENV` | constant | priv | 20 |
| `LEGACY_TOKENS_ENV` | constant | priv | 21 |
| `SiftAuthConfig` | struct | pub | 24 |
| `open` | function | pub | 30 |
| `from_env` | function | pub | 37 |
| `SiftVerifier` | struct | pub | 59 |
| `new` | function | pub | 62 |
| `from_env` | function | pub | 66 |
| `authenticate` | function | priv | 74 |
| `required` | function | priv | 78 |
| `auth_middleware` | function | pub | 83 |
| `tests` | module | priv | 92 |

### Public Signatures

```rust
open() -> Self

from_env() -> Result<Self>

new(config: SiftAuthConfig) -> Self

from_env() -> Result<Self>

auth_middleware(
    State(verifier): State<Arc<SiftVerifier>>,
    request: Request,
    next: Next,
) -> Response

```

### Imports

- `std` (external)
- `anyhow` (external)
- `axum` (external)
- `service_auth` (external)

## Schema
<!-- type: schema lang: yaml -->

```yaml
schemas:
  - title: SiftAuthConfig
    type: object
    rust_type: SiftAuthConfig
    required: [required, tokens]
    properties:
      required:
        rust_type: "bool"
      tokens:
        rust_type: "HashMap<String, TokenClaims>"
  - title: SiftVerifier
    type: object
    rust_type: SiftVerifier
```

## Changes
<!-- type: changes lang: yaml -->

```yaml
changes:
  - path: projects/sift/src/auth.rs
    action: modify
    impl_mode: hand-written
    description: |
      Pre-existing module captured by `score fillback`.
      Governance: hand-written until extended with
      Schema / Logic / Interface sections.
```
