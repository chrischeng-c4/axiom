---
id: deploy
fill_sections: [overview, schema, logic, changes]
---

## Overview
<!-- type: overview lang: markdown -->

Module `deploy` (Rust) — 7 symbol(s) (6 public). Spec auto-generated by `score fillback`; `impl_mode: hand-written` until extended with proper Schema / Logic sections.

### Symbols

| Name | Kind | Visibility | Line |
|------|------|------------|------|
| `DockerfileVariant` | enum | pub | 7 |
| `InstanceProfile` | enum | pub | 13 |
| `dockerfile` | function | pub | 20 |
| `crd_yaml` | function | pub | 40 |
| `operator_yaml` | function | pub | 44 |
| `instance_yaml` | function | pub | 54 |
| `strip_ownership_markers` | function | priv | 97 |

### Public Signatures

```rust
dockerfile(variant: DockerfileVariant, version: Option<&str>) -> Result<String>

crd_yaml() -> String

operator_yaml(namespace: &str) -> Result<String>

instance_yaml(profile: InstanceProfile) -> String

```

### Imports

- `anyhow` (external)

## Schema
<!-- type: schema lang: yaml -->

```yaml
schemas:
  - title: DockerfileVariant
    type: enum
    rust_type: DockerfileVariant
    enum:
      - Source
      - Release
  - title: InstanceProfile
    type: enum
    rust_type: InstanceProfile
    enum:
      - Dev
      - Staging
      - Prod
      - Template
```

## Logic: dockerfile
<!-- type: logic lang: mermaid -->

```yaml
id: dockerfile
entry: start
title: dockerfile
nodes:
  decision_0: { kind: decision, label: "match" }
  else_0: { kind: process, label: "continue" }
  start: { kind: start }
  then_0: { kind: process, label: "continue" }
edges:
  - { from: start, to: decision_0 }
  - { from: decision_0, to: then_0, label: "else" }
  - { from: decision_0, to: else_0, label: "else" }
```

## Logic: operator_yaml
<!-- type: logic lang: mermaid -->

```yaml
id: operator_yaml
entry: start
title: operator_yaml
nodes:
  decision_0: { kind: decision, label: "namespace.trim().is_empty()" }
  else_0: { kind: process, label: "continue" }
  start: { kind: start }
  then_0: { kind: process, label: "continue" }
edges:
  - { from: start, to: decision_0 }
  - { from: decision_0, to: then_0, label: "branch" }
  - { from: decision_0, to: else_0, label: "else" }
```

## Logic: instance_yaml
<!-- type: logic lang: mermaid -->

```yaml
id: instance_yaml
entry: start
title: instance_yaml
nodes:
  decision_0: { kind: decision, label: "match" }
  else_0: { kind: process, label: "continue" }
  start: { kind: start }
  then_0: { kind: process, label: "continue" }
edges:
  - { from: start, to: decision_0 }
  - { from: decision_0, to: then_0, label: "else" }
  - { from: decision_0, to: else_0, label: "else" }
```

## Changes
<!-- type: changes lang: yaml -->

```yaml
changes:
  - path: projects/sift/src/deploy.rs
    action: modify
    impl_mode: hand-written
    description: |
      Pre-existing module captured by `score fillback`.
      Governance: hand-written until extended with
      Schema / Logic / Interface sections.
```
