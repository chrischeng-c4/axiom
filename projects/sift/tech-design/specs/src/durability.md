---
id: durability
fill_sections: [overview, schema, changes]
---

## Overview
<!-- type: overview lang: markdown -->

Module `durability` (Rust) — 8 symbol(s) (4 public). Spec auto-generated by `score fillback`; `impl_mode: hand-written` until extended with proper Schema / Logic sections.

### Symbols

| Name | Kind | Visibility | Line |
|------|------|------------|------|
| `JournalSnapshot` | struct | pub | 16 |
| `from_events` | function | pub | 22 |
| `SiftStateMachine` | struct | pub | 34 |
| `new` | function | pub | 40 |
| `apply` | function | priv | 50 |
| `snapshot` | function | priv | 57 |
| `restore` | function | priv | 64 |
| `applied_index` | function | priv | 72 |

### Public Signatures

```rust
from_events(events: Vec<StoredEvent>) -> Self

new(journal: Arc<DurableJournal>) -> Self

```

### Imports

- `std::sync` (external)
- `anyhow::Result` (external)
- `raft_host` (external)
- `serde` (external)
- `crate` (internal)

## Schema
<!-- type: schema lang: yaml -->

```yaml
schemas:
  - title: JournalSnapshot
    type: object
    rust_type: JournalSnapshot
    required: [applied_index, events]
    properties:
      applied_index:
        rust_type: "u64"
      events:
        rust_type: "Vec<StoredEvent>"
  - title: SiftStateMachine
    type: object
    rust_type: SiftStateMachine
    required: [journal, applied_index]
    properties:
      journal:
        rust_type: "Arc<DurableJournal>"
      applied_index:
        rust_type: "AtomicU64"
```

## Changes
<!-- type: changes lang: yaml -->

```yaml
changes:
  - path: projects/sift/src/durability.rs
    action: modify
    impl_mode: hand-written
    description: |
      Pre-existing module captured by `score fillback`.
      Governance: hand-written until extended with
      Schema / Logic / Interface sections.
```
