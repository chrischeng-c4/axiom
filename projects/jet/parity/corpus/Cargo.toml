# <HANDWRITE gap="missing-generator:hand-written:jet-parity-corpus" tracker="standardize-gap-projects-jet-parity-corpus-cargo-toml" reason="codegen for the parity corpus crate is not yet wired; see #2140">
[package]
name = "jet-parity-corpus"
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Manifest parser, content hasher, drift verifier, and CLI for the jet parity fixture corpus (issue #2140)."

[lib]
name = "jet_parity_corpus"
path = "src/lib.rs"

[[bin]]
name = "jet-parity-corpus"
path = "src/main.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
toml = "0.8"
sha2 = "0.10"
hex = "0.4"
thiserror = { workspace = true }
clap = { workspace = true }
regex = { workspace = true }

[dev-dependencies]
tempfile = "3"
# </HANDWRITE>
