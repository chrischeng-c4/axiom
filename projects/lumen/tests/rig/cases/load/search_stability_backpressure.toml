[record]
suite = "lumen"
dimension = "load"
case = "search_stability_backpressure"
subject = "overload backpressure keeps sharded search bounded and available"
kind = "e2e"
expected = "pass"
required = true

[[steps]]
type = "exec"
name = "health_and_ready"
cmd = ["cargo", "test", "-p", "lumen", "--test", "api_e2e", "health_and_ready", "--", "--nocapture"]
timeout_secs = 120
