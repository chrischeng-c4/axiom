# SPEC-MANAGED: generated by `aw ec gen --project lumen` from ec/ markdown or legacy TD EC sections.
# CODEGEN-BEGIN
version = 1
project = "lumen"
generated_from_td_digest = "sha256:af1fe6b09eec0fc68875fad51c8abff99b3c7fed6a8a9d79207a9cf73c5b7c12"

[[cases]]
id = "lumen-agentic-integration-llm-playbook"
capability_id = "agentic-integration"
claim_id = "lumen-llm-agent-topics-outline-workflow-integration-quickstart-recipes"
contract_id = "lumen-llm-agent-topics-outline-workflow-integration-quickstart-recipes"
category = "behavior"
td_ref = "projects/lumen/ec/behavior/agentic-integration.md#lumen-agentic-integration-llm-playbook"
test_path = "projects/lumen/tests/behavior_lumen_agentic_integration_llm_playbook.rs"
command = "cargo test -p lumen --test spec_cli -- --nocapture"
required_for_production = true
assertions = [
    "lumen llm outline, workflow, integration, quickstart, and recipes preserve the agent-facing topic set.",
    "lumen llm integration preserves the provider-neutral Postgres/AlloyDB adapter guidance and keeps Pub/Sub-specific ownership outside lumen core.",
    "agent-facing playbook output remains deterministic and offline.",
]

[[cases]]
id = "lumen-agentic-integration-offline-cli"
capability_id = "agentic-integration"
claim_id = "lumen-spec-schema-openapi-json-yaml-json-schema-offline"
contract_id = "offline-cli-agent-onboarding"
category = "behavior"
td_ref = "projects/lumen/ec/behavior/agentic-integration.md#lumen-agentic-integration-offline-cli"
test_path = "projects/lumen/tests/behavior_lumen_agentic_integration_offline_cli.rs"
command = "cargo test -p lumen --test spec_cli -- --nocapture"
required_for_production = true
assertions = [
    "lumen spec emits valid OpenAPI JSON, OpenAPI YAML, and JSON-schema output offline.",
    "lumen spec exposes query-shape, field, analyzer, and vector-metric catalogs.",
    "lumen llm outline, workflow, integration, quickstart, and recipes preserve the ingest-search-hydrate agent workflow and non-goals.",
    "lumen llm integration recommends the Postgres/AlloyDB boundary: database commit/outbox or CDC, external adapter-owned Pub/Sub retry/DLQ, HTTP writes into lumen, and no direct external publishing to lumen's NATS WAL.",
]

[[cases]]
id = "lumen-agentic-integration-query-catalog"
capability_id = "agentic-integration"
claim_id = "query-shape-cookbook-field-analyzer-catalog"
contract_id = "query-shape-cookbook-field-analyzer-catalog"
category = "behavior"
td_ref = "projects/lumen/ec/behavior/agentic-integration.md#lumen-agentic-integration-query-catalog"
test_path = "projects/lumen/tests/behavior_lumen_agentic_integration_query_catalog.rs"
command = "cargo test -p lumen --test spec_cli -- --nocapture"
required_for_production = true
assertions = [
    "lumen spec exposes query-shape, field, analyzer, and vector-metric catalogs.",
    "agent-facing query catalog output remains deterministic and offline.",
]

[[cases]]
id = "lumen-devops-operator-render"
capability_id = "k8s-deployment"
claim_id = "operator-renders-managed-child-set-incl-nats"
contract_id = "devops-operator-render-golden"
category = "behavior"
td_ref = "projects/lumen/ec/behavior/devops-render.md#lumen-devops-operator-render"
test_path = "projects/lumen/tests/behavior_lumen_devops_operator_render.rs"
command = "cargo test -p lumen --features operator --test operator_render -- --nocapture"
required_for_production = true
assertions = [
    "render(Lumen) emits the managed serving Deployment/Service/HPA/PDB plus the NATS JetStream ConfigMap/StatefulSet/Service when NATS is managed.",
    "BYO-NATS (nats.externalUrl) omits the managed NATS objects and wires the external URL into the serving env.",
]

[[cases]]
id = "lumen-ops-speed-benchmark-vs-db"
capability_id = "ops-operability"
claim_id = "competitive-regression-gate-beat-pg-os-per-cell-ratcheting"
contract_id = "competitive-regression-gate-beat-pg-os-per-cell-ratcheting"
category = "efficiency"
td_ref = "projects/lumen/ec/efficiency/competitive-search-benchmark-vs-db.md#lumen-ops-speed-benchmark-vs-db"
test_path = "projects/lumen/tests/benchmark_lumen_ops_speed_benchmark_vs_db.rs"
command = "cargo test -p lumen --release --test perf_gate_vs_db -- --ignored --test-threads=1"
required_for_production = false
assertions = [
    "lumen wins the contracted search-latency cells against Postgres (text_bm25 WIN; ratcheted floor holds).",
    "floor-dominated cells (pg btree point-lookup) stay EXEMPT, not gated.",
]

[[cases]]
id = "lumen-security-auth-bearer-rbac"
capability_id = "security-auth"
claim_id = "bearer-token-auth-lumen-auth"
contract_id = "bearer-token-auth-lumen-auth"
category = "security"
td_ref = "projects/lumen/ec/security/auth-bearer-rbac.md#lumen-security-auth-bearer-rbac"
test_path = "projects/lumen/tests/security_lumen_security_auth_bearer_rbac.rs"
command = "cargo test -p lumen --test auth_e2e --test authz_matrix_e2e -- --nocapture"
required_for_production = false
assertions = [
    "Bearer-token auth rejects missing and invalid tokens when LUMEN_AUTH=required; accepts valid tokens.",
    "Per-route RBAC authz matrix enforces each token's role permissions on every API route (read vs write vs admin).",
]

[[cases]]
id = "lumen-serve-functional-api-and-search"
capability_id = "search"
claim_id = "query-planner-boolean-eval-roaring-postings"
contract_id = "serve-functional-api-and-search-correctness"
category = "behavior"
td_ref = "projects/lumen/ec/behavior/serve-functional.md#lumen-serve-functional-api-and-search"
test_path = "projects/lumen/tests/behavior_lumen_serve_functional_api_and_search.rs"
command = "cargo test -p lumen --test api_e2e --test vector_e2e --test planner_diff -- --nocapture"
required_for_production = true
assertions = [
    "The HTTP API end-to-end (create -> index -> search -> hydrate ids) returns correct ranked external_ids and never documents.",
    "Vector kNN and filtered kNN return the nearest within the filter without recall collapse.",
    "The query planner produces byte-identical plans (planner_diff) across the search-flavor sub-capabilities.",
]

[[cases]]
id = "lumen-stability-resilience-survival"
capability_id = "resilience"
claim_id = "broker-kill-pod-kill-survival"
contract_id = "broker-kill-pod-kill-survival"
category = "stability"
td_ref = "projects/lumen/ec/stability/resilience-survival.md#lumen-stability-resilience-survival"
test_path = "projects/lumen/tests/stability_lumen_stability_resilience_survival.rs"
command = "cargo test -p lumen --test drop_drain_e2e --test reindex_stream_e2e -- --nocapture"
required_for_production = false
assertions = [
    "Search p99 stays within 2x baseline under 5% packet loss (toxiproxy timeout toxic; rig resilience scenario).",
    "Search survives a full network partition and recovers within budget; post-recovery p99 stays within 2x baseline.",
]

[[tool_manifests]]
id = "lumen-arena-competitive-search"
tool = "arena"
path = "projects/lumen/arena.toml"
td_ref = "projects/lumen/ec/efficiency/competitive-search-benchmark-vs-db.md#lumen-arena-competitive-search"
content_digest = "sha256:06afbe2a3fde6d3b91371c8972d3fc8e85a6105ddfa9ffcfef4b6474b84993c0"
command = "target/debug/arena run --spec projects/arena/examples/lumen-vs-pg-and-opensearch.toml"
category = "efficiency"

[[tool_manifests]]
id = "lumen-guard-auth-surface"
tool = "guard"
path = "projects/lumen/guard.toml"
td_ref = "projects/lumen/ec/security/auth-bearer-rbac.md#lumen-guard-auth-surface"
content_digest = "sha256:ad779b2a7c83d2fda32c5f37dacf85f9d6bfee93926799d61cfeedcb1037f5ca"
command = "guard scan projects/lumen --compact --no-persist"
category = "security"

[[tool_manifests]]
id = "lumen-rig-resilience"
tool = "rig"
path = "projects/lumen/rig.toml"
td_ref = "projects/lumen/ec/stability/resilience-survival.md#lumen-rig-resilience"
content_digest = "sha256:8d25197f040d071d51404da9fbbffb1e81c173503d221a481cc285315be49160"
command = "target/debug/rig run --dir projects/lumen/tests/rig/scenarios/resilience"
category = "stability"
# CODEGEN-END
