# SPEC-MANAGED: projects/lumen/tech-design/semantic/lumen-k8s-optional-consumer.md#deployment
# CODEGEN-BEGIN
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Optional sample bundle. NOT included from base/ or any overlay; opt in
# explicitly by listing this directory in your own overlay.
namespace: lumen

labels:
  - pairs:
      app.kubernetes.io/name: lumen
      app.kubernetes.io/component: consumer
    includeSelectors: false

resources:
  - deployment.yaml
  - hpa.yaml
# CODEGEN-END
