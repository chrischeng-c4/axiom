// HANDWRITE-BEGIN gap="missing-generator:unit-test:d757d3fb" tracker="pending-tracker" reason="Drive the compiled keep binary: keep spec emits the same OpenAPI inventory as http::routes::openapi (the /openapi.json source), keep spec gen --lang ts writes client files to a temp dir, and keep dockerfile render --variant source|release reproduces the committed Dockerfiles (with keep@version substitution)."
// TODO: hand-write content for `projects/keep/tests/deploy_cli.rs`.
// HANDWRITE-END
