[package]
name = "agentkit-llm"
version = "0.1.0"
edition = "2021"
description = "LLM primitives — prompts, parsers, retrievers, embeddings, vectorstores (Epic 4 — LangChain core)"
license = "MIT"
authors = ["cclab"]

[lib]
name = "agentkit_llm"
crate-type = ["rlib"]

[dependencies]
