[package]
name = "plotkit"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Data visualization (Matplotlib-equivalent). Mamba mambalibs kit."

[lib]
name = "plotkit"
crate-type = ["rlib"]

[dependencies]
thiserror.workspace = true
serde = { workspace = true, optional = true }

[dev-dependencies]
tempfile = "3"

[features]
default = []
serde = ["dep:serde"]
