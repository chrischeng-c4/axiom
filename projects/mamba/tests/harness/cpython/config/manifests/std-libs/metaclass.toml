# Manifest for the `metaclass` conformance corpus (language-level metaclass
# semantics; kept under bucket "std-libs" alongside the existing
# tests/cpython/behavior/std-libs/metaclass/ fixture for one discoverable
# lib directory).
#
# One [[case]] per fixture. `fixture_gen.py` turns this into the files under
# tests/cpython/<dimension>/std-libs/metaclass/<case>.py:
#   - mechanical surface/errors come out complete (status="filled")
#   - semantic cases come out as fillable skeletons (status="generated")

bucket = "std-libs"
lib = "metaclass"

[[case]]
dimension = "behavior"
case      = "super_call_forwards_args_kwargs_to_type_call"
subject   = "type.__call__"
kind      = "semantic"
intent    = "a metaclass __call__ override that forwards via super().__call__(*args, **kwargs) binds kwargs by name into __init__ (type.__call__'s default instance-creation bypass), for both an all-positional-plus-kwarg call and a call relying on defaults (#1951)"
source    = "Lib/test/test_metaclass.py"
