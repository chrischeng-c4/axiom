# Perf-pin regression gate (#1503).
# Source: tests/cpython/perf_pin.rs (per-pin: tests/cpython/perf/pins/s3transfer_1503.toml)
issue = 1503
lib = "s3transfer"
fixture = "tests/cpython/_regression/3rd-libs/s3transfer/bench/s3transfer_type_read_hot.py"
floor = 1.0
mem_floor = 1.0
samples = 9
prereq_imports = ["s3transfer"]

# Sentinel-shim quarantine (#1514): this pin measures
# src/runtime/stdlib/s3transfer_mod.rs, a hand-written module-attribute-identity
# shim (no pyo3/tonic/prost-backed implementation behind it). It is a real,
# enforced perf/RSS regression gate for that shim's own code path, but it must
# NOT be read as third-party-conformance readiness evidence for `s3transfer`. Any
# readiness rollup must exclude sentinel = true pins from its evidence count.
# Full quarantine list + marking rule: #1119.
sentinel = true
