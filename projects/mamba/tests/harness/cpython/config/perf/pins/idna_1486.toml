# Perf-pin regression gate (#1486).
# Source: tests/cpython/perf_pin.rs (per-pin: tests/cpython/perf/pins/idna_1486.toml)
issue = 1486
lib = "idna"
fixture = "tests/cpython/_regression/3rd-libs/idna/bench/idna_type_read_hot.py"
floor = 1.0
mem_floor = 1.0
samples = 9
prereq_imports = ["idna"]

# Sentinel-shim quarantine (#1514): this pin measures
# src/runtime/stdlib/idna_mod.rs, a hand-written module-attribute-identity
# shim (no pyo3/tonic/prost-backed implementation behind it). It is a real,
# enforced perf/RSS regression gate for that shim's own code path, but it must
# NOT be read as third-party-conformance readiness evidence for `idna`. Any
# readiness rollup must exclude sentinel = true pins from its evidence count.
# Full quarantine list + marking rule: #1119.
sentinel = true
