# Perf-pin regression gate (#1523).
# Source: tests/cpython/perf_pin.rs (per-pin: tests/cpython/perf/pins/sqlalchemy_1523.toml)
issue = 1523
lib = "sqlalchemy"
fixture = "tests/cpython/_regression/3rd-libs/sqlalchemy/bench/sqlalchemy_type_read_hot.py"
floor = 1.0
mem_floor = 1.0
samples = 9
prereq_imports = ["sqlalchemy"]

# Sentinel-shim quarantine (#1514): this pin measures
# src/runtime/stdlib/sqlalchemy_mod.rs, a hand-written module-attribute-identity
# shim (no pyo3/tonic/prost-backed implementation behind it). It is a real,
# enforced perf/RSS regression gate for that shim's own code path, but it must
# NOT be read as third-party-conformance readiness evidence for `sqlalchemy`. Any
# readiness rollup must exclude sentinel = true pins from its evidence count.
# Full quarantine list + marking rule: #1119.
sentinel = true
