# Perf-pin regression gate (#1507).
# Source: tests/cpython/perf_pin.rs (per-pin: tests/cpython/perf/pins/azure_storage_blob_1507.toml)
issue = 1507
lib = "azure_storage_blob"
fixture = "tests/cpython/_regression/3rd-libs/azure_storage_blob/bench/azure_storage_blob_type_read_hot.py"
floor = 1.0
mem_floor = 1.0
samples = 9
prereq_imports = ["azure.storage.blob"]

# Sentinel-shim quarantine (#1514): this pin measures
# src/runtime/stdlib/azure_storage_blob_mod.rs, a hand-written module-attribute-identity
# shim (no pyo3/tonic/prost-backed implementation behind it). It is a real,
# enforced perf/RSS regression gate for that shim's own code path, but it must
# NOT be read as third-party-conformance readiness evidence for `azure_storage_blob`. Any
# readiness rollup must exclude sentinel = true pins from its evidence count.
# Full quarantine list + marking rule: #1119.
sentinel = true
