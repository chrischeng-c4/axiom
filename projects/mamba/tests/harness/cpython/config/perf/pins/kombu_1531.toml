# Perf-pin regression gate (#1531).
# Source: tests/cpython/perf_pin.rs (per-pin: tests/cpython/perf/pins/kombu_1531.toml)
issue = 1531
lib = "kombu"
fixture = "tests/cpython/_regression/3rd-libs/kombu/bench/kombu_type_read_hot.py"
floor = 1.0
mem_floor = 1.0
samples = 9
prereq_imports = ["kombu"]

# Sentinel-shim quarantine (#1514): this pin measures
# src/runtime/stdlib/kombu_mod.rs, a hand-written module-attribute-identity
# shim (no pyo3/tonic/prost-backed implementation behind it). It is a real,
# enforced perf/RSS regression gate for that shim's own code path, but it must
# NOT be read as third-party-conformance readiness evidence for `kombu`. Any
# readiness rollup must exclude sentinel = true pins from its evidence count.
# Full quarantine list + marking rule: #1119.
sentinel = true
