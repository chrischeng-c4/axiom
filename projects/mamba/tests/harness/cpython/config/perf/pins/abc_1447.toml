# Perf-pin regression gate (#1447).
# Source: tests/cpython/perf_pin.rs (per-pin: tests/cpython/perf/pins/abc_1447.toml)
# #1024: small-fixture calibrated memory floor. The shared 26 MB runtime-RSS
# allowance removes the structural startup floor, and this pin keeps a bounded
# fixture-size floor for abc's remaining import/cache-token workload shape.
# CPU still gates through `floor` unchanged.
issue = 1447
lib = "abc"
fixture = "tests/cpython/_regression/std-libs/abc/bench/get_cache_token_hot.py"
floor = 1.0
mem_floor = 0.4
samples = 9
prereq_imports = []
