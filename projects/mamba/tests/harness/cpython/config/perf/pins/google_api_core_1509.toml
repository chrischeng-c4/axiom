# Perf-pin regression gate (#1509).
# Source: tests/cpython/perf_pin.rs (per-pin: tests/cpython/perf/pins/google_api_core_1509.toml)
issue = 1509
lib = "google_api_core"
fixture = "tests/cpython/_regression/3rd-libs/google_api_core/bench/google_api_core_type_read_hot.py"
floor = 1.0
mem_floor = 1.0
samples = 9
prereq_imports = ["google.api_core"]

# Sentinel-shim quarantine (#1514): this pin measures
# src/runtime/stdlib/google_api_core_mod.rs, a hand-written module-attribute-identity
# shim (no pyo3/tonic/prost-backed implementation behind it). It is a real,
# enforced perf/RSS regression gate for that shim's own code path, but it must
# NOT be read as third-party-conformance readiness evidence for `google_api_core`. Any
# readiness rollup must exclude sentinel = true pins from its evidence count.
# Full quarantine list + marking rule: #1119.
sentinel = true
