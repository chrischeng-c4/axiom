# Third-party packages the CPython 3.12 oracle needs so that every
# tests/cpython fixture that imports a 3p library can satisfy the
# "fixture must exit 0 under CPython" contract (runner.rs INVALID check).
#
# Union of top-level non-stdlib imports across tests/cpython/**/3rd-libs/**.
# Unpinned here; the committed requirements.txt (uv pip compile) is the
# content-addressed lock that defines the oracle environment's identity.
#
# Regenerate the lock:   uv pip compile requirements.in -o requirements.txt
# Materialize the env:   uv venv <repo>/projects/mamba/tests/cpython/.cache/oracle-env --python 3.12
#                        VIRTUAL_ENV=<that path> uv pip sync requirements.txt
# The harness auto-detects .cache/oracle-env/bin/python3 (harness_common.rs
# python3_bin); MAMBA_ORACLE_PYTHON still overrides everything.

aiofiles
aiohttp
alembic
anyio
attrs
azure-core
azure-identity
azure-keyvault-secrets
azure-storage-blob
boto3
botocore
celery
certifi
charset-normalizer
click
cryptography
fastapi
flask
google-api-core
grpcio
grpclib
gunicorn
httpx
hypothesis
idna
jinja2
jmespath
jsonschema
kombu
markupsafe
marshmallow
mock
msgpack
numpy
orjson
packaging
pandas
protobuf
psycopg
pydantic
pyopenssl
pytest
pytest-asyncio
redis
requests
rich
s3transfer
sqlalchemy
starlette
typing-extensions
urllib3
uvicorn
werkzeug
