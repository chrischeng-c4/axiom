[project]
name = "mamba"
cap_path = "projects/mamba/README.md"

# AW-EC-BEGIN
[aw.ec.generated]
version = 1
project = "mamba"
generated_from_td_digest = "sha256:e352a7d9e1af31759fc546991df09009b22489bced78c0e3dc9f23cde8ec3f17"

[[aw.ec.generated.cases]]
id = "build-globals-dict-key-leak-free"
capability_id = "mamba-core-semantics"
claim_id = "build-globals-dict-leaks-no-key-references"
contract_id = "MAMBA-T1-BUILD-GLOBALS-DICT-KEY-LEAK-FREE"
category = "stability"
td_ref = "projects/mamba/external-contracts/stability/build-globals-dict-key-leak-free.md#build-globals-dict-key-leak-free"
test_path = "projects/mamba/tests/external_contracts/ec_build_globals_dict_key_leak_free.rs"
command = "cargo test -p mamba --release --test mamba_core_semantics_ec -- build_globals_dict_key_leak_free --exact"
required_for_production = true
assertions = [
    "Comparing two mamba runs that call globals() 100 times versus 50,000 times (42 exposed names per call: 20 id_ns values, 20 func_info functions, plus the __name__ and total module globals), peak process RSS for the 50,000-call run is no greater than the 100-call run's peak RSS plus 24 MiB; unreleased per-call key allocations would instead grow roughly linearly with call count.",
    "Equal id_ns and func_info name counts (20 each) ensure a leak isolated to either loop alone still produces per-call growth far in excess of the 24 MiB slack, so the gate cannot pass by exercising only one of the two loops build_globals_dict populates.",
    "Each run's script accumulates and prints the sum of len(globals()) across every call; the gate asserts this sum exactly equals iterations times the expected 42-key count, so a degenerate or empty build_globals_dict cannot pass vacuously by having nothing left to leak.",
    "Peak RSS is sampled via the OS process resource-usage counter (wait4/getrusage-equivalent) on the real compiled mamba binary, not a runtime-internal self-reported counter.",
]

[[aw.ec.generated.cases]]
id = "mamba-t1-to-thread-gather-efficiency"
capability_id = "mamba-core-semantics"
claim_id = "parallel-to-thread-gather-preserves-every-result"
contract_id = "MAMBA-T1-FT-GATHER-EFFICIENCY"
category = "efficiency"
td_ref = "projects/mamba/external-contracts/efficiency/mamba-t1-to-thread-gather-efficiency.md#mamba-t1-to-thread-gather-efficiency"
test_path = "projects/mamba/tests/external_contracts/ec_mamba_t1_to_thread_gather_efficiency.rs"
command = "cargo test -p mamba --release --test mamba_core_semantics_ec -- to_thread_gather_efficiency --exact"
required_for_production = true
assertions = [
    "On a host exposing at least four logical CPUs, four equal CPU-bound asyncio.to_thread jobs complete with wall-clock speedup at least 1.50x versus the same jobs run serially, while returning the exact same ordered results.",
    "During the parallel phase, measured process CPU time divided by wall time is at least 1.50, proving concurrent work used more than one core rather than cooperative single-loop scheduling.",
    "Parallel peak RSS is no greater than 1.25 times serial peak RSS plus 16 MiB for the same workload; CPU scaling cannot be purchased with unbounded retained worker/future state.",
    "The gate records logical CPU count, serial and parallel wall time, process CPU time, peak RSS, result digest, and speedup; unsupported hosts are explicit evidence, never a silent pass.",
]

[[aw.ec.generated.cases]]
id = "mamba-t1-to-thread-gather-results"
capability_id = "mamba-core-semantics"
claim_id = "parallel-to-thread-gather-preserves-every-result"
contract_id = "MAMBA-T1-FT-GATHER-RESULTS"
category = "behavior"
td_ref = "projects/mamba/external-contracts/behavior/mamba-t1-to-thread-gather-results.md#mamba-t1-to-thread-gather-results"
test_path = "projects/mamba/tests/external_contracts/ec_mamba_t1_to_thread_gather_results.rs"
command = "cargo test -p mamba --release --test mamba_core_semantics_ec -- to_thread_gather_results --exact"
required_for_production = true
assertions = [
    "Two or more concurrently completing asyncio.to_thread calls with distinct inputs are gathered into one result list containing every expected value exactly once and in asyncio.gather input order.",
    "No gathered slot may be None, missing, duplicated, stale, or borrowed from another worker, regardless of whether that worker finishes before or after the gather await begins.",
    "The contract exercises the public Mamba asyncio surface from a compiled Python program; a Rust-only registry or helper test cannot satisfy this behavior gate by itself.",
    "The CPython control program must produce the same ordered values; Mamba's intentional divergence is multicore execution, not gather result semantics.",
]

[[aw.ec.generated.cases]]
id = "mamba-t1-to-thread-gather-stability"
capability_id = "mamba-core-semantics"
claim_id = "parallel-to-thread-gather-preserves-every-result"
contract_id = "MAMBA-T1-FT-GATHER-STABILITY"
category = "stability"
td_ref = "projects/mamba/external-contracts/stability/mamba-t1-to-thread-gather-stability.md#mamba-t1-to-thread-gather-stability"
test_path = "projects/mamba/tests/external_contracts/ec_mamba_t1_to_thread_gather_stability.rs"
command = "cargo test -p mamba --release --test mamba_core_semantics_ec -- to_thread_gather_stability --exact"
required_for_production = true
assertions = [
    "Across 100 rounds of eight concurrently gathered CPU-bound asyncio.to_thread calls, every round returns all eight distinct expected values exactly once with zero crash, panic, timeout, or deadlock.",
    "The stability gate varies worker completion order and must fail on any None, missing, duplicate, stale, cross-worker, or wrong result; aggregate pass counts are insufficient evidence.",
    "Using an OS-visible process-thread count, the worker/thread count after a 250 ms quiescence period following the final round returns to the pre-soak baseline plus at most one runtime service thread; private runtime registries are not an EC oracle.",
    "Peak RSS is sampled in two equal soak windows; window two must be no greater than 1.10 times window one plus 8 MiB, so monotonic retained-result leaks fail the required gate.",
]

[[aw.ec.generated.cases]]
id = "mamba-t1-type-wall-conformance-determinism"
capability_id = "mamba-core-semantics"
claim_id = "deterministic-type-wall-outcomes"
contract_id = "MAMBA-T1-TYPE-WALL-CONFORMANCE-DETERMINISM"
category = "stability"
td_ref = "projects/mamba/external-contracts/behavior/1942.md#mamba-t1-type-wall-conformance-determinism"
test_path = "projects/mamba/tests/external_contracts/ec_mamba_t1_type_wall_conformance_determinism.rs"
command = "cargo test -p mamba --release --test mamba_core_semantics_ec -- type_wall_conformance_determinism --exact"
required_for_production = true
assertions = [
    "The external contract verifier verifies fixture inventory against manifest projects/mamba/tests/governance/gates/t1_type_wall_denominator/manifest.toml, asserting exact expected row_count = 7407 and exact expected denominator_sha256 = eb45a673ca92c766c1df6596592aa226fae56ab81f8f27fea47e1168743eae28.",
    "Before repeated evaluation, the verifier proves the denominator-scoped executable surface by invoking the current release cpython_ported_integration test binary twice with --list: first without a libtest filter, which must yield exactly 13767 unique terminal test names for the whole target, and then with the exact libtest filter cpython_ported::gen::_type, which must yield exactly 7407 unique terminal test names that are set-equal to projects/mamba/tests/governance/gates/t1_type_wall_denominator/denominator.txt.",
    "The evaluation consists of three fresh isolated subprocess executions of exactly cargo test -p mamba --release --test cpython_ported_integration cpython_ported::gen::_type -- --nocapture executed from the same source revision, exact rustup toolchain surface (rustc 1.96.1 (31fca3adb 2026-06-26) and cargo 1.96.1 (356927216 2026-06-26)), environment fingerprint, and manifest digest, where each subprocess has its own run-local output/temp capture and shares no prior-run in-memory state.",
    "Evaluation requires a named baseline evidence artifact at projects/mamba/external-contracts/evidence/mamba-t1-type-wall-conformance-determinism-baseline.json whose required fields include source revision, manifest path (projects/mamba/tests/governance/gates/t1_type_wall_denominator/manifest.toml), manifest digest (denominator_sha256), manifest row count (row_count), exact rustup_toolchain string, exact cargo_version string, full normalized allowed failing-path set, failure count, command (cargo test -p mamba --release --test cpython_ported_integration cpython_ported::gen::_type -- --nocapture), and capture timestamp. Missing baseline artifact or missing required fields constitutes a hard failure.",
    "Every one of the three post-fix runs must produce an executed terminal-name set that is exactly set-equal to the denominator's 7407 cpython_ported::gen::_type names, with no ignored or skipped executed rows, duplicates, missing rows, cross-namespace rows, or unparseable rows.",
    "For every one of the three post-fix runs, passed plus failed executed rows must equal exactly 7407.",
    "For every one of the three post-fix runs, the verifier must reconcile whole-target libtest filtering by proving exactly 6360 filtered-out rows, equal to the pinned whole-target preflight count 13767 minus the executed denominator count 7407.",
    "The three post-fix normalized failing sets and total failure counts across all three repetitions must be exactly equal.",
    "Every post-fix failing path must be a member of the pinned baseline allowed set recorded in projects/mamba/external-contracts/evidence/mamba-t1-type-wall-conformance-determinism-baseline.json, and the post-fix total failure count must be less than or equal to the baseline failure count, closing both subset and equal-count replacement.",
    "The verifier includes fail-closed self-tests/canaries that remove one fixture result and flip one outcome in captured test data, asserting that both simulated mutations are rejected before real evidence can pass.",
    "Timeouts, crash or signal termination, nonzero exit without complete terminal accounting, output truncation, revision/manifest/toolchain/environment drift, missing baseline, or parse errors are hard failures.",
    "Internal implementation causes, runtime micro-optimizations, CPython parity comparisons, and any inferred bijection to unrelated run_conformance::<path> cases do not constitute proof of external contract compliance and remain explicitly non-oracles.",
]
# AW-EC-END
