# Minimal known-good scenario: the lint fixture and the smallest runnable
# e2e case (no services needed).
[record]
suite = "rig"
dimension = "demo"
case = "sleep_settles"
subject = "rig runs a one-step scenario end to end"
kind = "e2e"
expected = "pass"
required = true

[[steps]]
type = "sleep"
name = "settle"
secs = 0
