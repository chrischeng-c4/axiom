# Retired AGY dispatcher v1

This folder is an inert archive for manual migration review.
Do not execute its scripts.

It contains the retired Claude-specific dispatcher and its old references.
That code used Gemini 3.6, private `~/.gemini` Project cache files, Project-root
repointing, and mutable `grant` operations.

Use `.agents/skills/agy-dispatch/` for every active dispatch.
