<!-- generated by aw meta from .agents/rules; do not edit -->
<!-- source: .agents/rules/operations/persistent-branches.md; id: operations.persistent-branches -->

# Persistent work-area branches

## Intent

Preserve the one-project, one-worktree allocation and converge persistent branches without data loss.

## Rules

- Use `app/<name>` for application work areas, `lib/<name>` for library work areas, and the retained `project-mamba` or `project-lumen` branches for those legacy roots.
- Treat `main`, `app/*`, `lib/*`, `project-mamba`, and `project-lumen` as persistent refs.
- Do not delete or force-overwrite a persistent ref without explicit user confirmation.
- Preserve dirty worktree changes and prefer rebase-based non-destructive convergence.
- Let WI manage tracker state without creating or switching git branches.

## Verification

- Inspect `git status --short --branch` before and after branch operations.
- Confirm the current worktree path maps to the intended persistent branch.

## References

- `AGENTS.md` section “Project and Branch Allocation”.
