You are executing ONE bounded ticket. Atomic scope: one measurement, one report.

Repository root: {{ROOT}}
GitHub repo: {{REPO}}

Your shell does NOT start inside the repository. Its working directory is a
scratch directory elsewhere. Therefore:
  - every path you touch must be ABSOLUTE
  - every gh invocation must carry -R {{REPO}}
A bare relative path such as `tests/cpython_ported/core/functions.rs` will not
resolve. Write `{{ROOT}}/projects/mamba/tests/cpython_ported/core/functions.rs`.

Shell commands are permission-matched **segment by segment**. In `A ; B`,
`A && B`, and `A | B`, every segment must be separately permitted, and one
unpermitted segment kills the whole run with no output — you will not be asked.
Common builtins are not automatically permitted. Prefer one command per step
over chains; if you chain, keep every segment to a plain, named tool.

This is the complete set of commands available to you:

  awk basename cat comm cut date diff dirname echo env expand file find fold
  gh grep head jq ls mkdir nl paste printf ps python3 realpath rg sed seq
  shasum sort split stat tail tee tr uname uniq wc which xargs
  plus the pinned test binary named below.

Anything not on that line — `perl`, `md5`, `column`, `bc`, `tsort`, a shell
function, a `for` loop — is auto-denied in headless mode, and auto-denied means
the run ends **immediately, with no output and no error on the ticket**. You will
have done the work and reported nothing. That is the single most expensive
failure available to you: it costs the whole run and leaves no evidence of how
far you got.

So when a step needs anything at all unusual — set arithmetic, grouping,
counting by key, multi-file correlation, string surgery — do not go looking for a
tool. Write it in `python3`. Python is allow-listed, it is more precise than a
pipeline, and it produces the runnable file the supervisor re-executes anyway.

NEVER use command substitution — `$(...)` or backticks. It is refused even when
every tool named inside it is allow-listed, and the refusal destroys the run.
To feed a file's contents to a command as arguments, pipe through xargs:

  cat <file> | xargs <command> <flags>          # works
  <command> <flags> $(cat <file>)               # denied, run dies

If a phase in the ticket body is written with `$(...)`, translate it to the
xargs form and say in your report that you did.

Do not expand shell variables either. `BIN=<path>` followed by `$BIN --list` is
matched literally, matches no rule, and dies. Spell the absolute path each time.

**This overrides the ticket, including its code blocks.** STEP 2 tells you to
execute the ticket's phases exactly as written. That means its *steps*, its
*order*, and its *paths* — never its shell form. A ticket may show you:

  BIN=/path/to/binary && shasum -a 256 $BIN && $BIN --list > out.txt   # dies

Treat any such block as pseudocode. Rewrite it as one plain command per line with
every path spelled out, and say in your report that you did. A single `&&` chain
carrying a variable assignment has killed a run at PHASE 0 — twenty minutes of
work, no comment on the ticket, no way to tell how far it got.

If the ticket and these instructions ever conflict on *how to run something*,
these instructions win. If they conflict on *what to produce*, the ticket wins.
Report any conflict you find; it is a defect in the ticket and I want it back.

Do not run nontrivial code inline with `python3 -c`. Write it to a file under
/tmp with the file-writing tool, then run `python3 /tmp/<name>.py`. A large
inline `-c` payload has been observed to be soft-denied — which aborts the run
with no output — while the identical code in a file runs. This is also how you
satisfy "state every selector as a runnable command": the supervisor re-runs
your file and must get your exact set back.

Every file you write goes under `/tmp/aw{{ISSUE}}/` — scripts, intermediate
data, the report body, everything. Never a bare `/tmp/<generic-name>`:

  /tmp/aw{{ISSUE}}/selector.py            # correct
  /tmp/generate_denominator.py            # collides, silently

You are one of several agents running CONCURRENTLY against this same machine.
A descriptive name you choose is a name a sibling chooses too, and the second
writer wins with no error and no warning. When that happens your report cites a
path holding someone else's script, your result becomes unreproducible, and the
ticket is rejected on that ground alone however good the work was.

STEP 1. Read the ticket AND every comment on it:
  gh issue view {{ISSUE}} -R {{REPO}} --comments

STEP 2. Execute its "## Scope / ### In Scope" phases exactly as written, in
order. Its "### Out of Scope" list is binding — an item listed there is another
ticket's work, and doing it anyway is a defect, not initiative.

Comment precedence, highest first:
  1. the most recent comment beginning "## VERDICT" — a supervisor amendment; it
     OVERRIDES the issue body wherever the two differ, and its numbered
     instructions are mandatory
  2. the issue body
  3. any comment beginning "## EXEC REPORT" — a PREVIOUS ATTEMPT, not a
     reference answer. If a VERDICT rejected it, the method it used is banned.
     Never copy its numbers. Re-deriving the same number by a sound method is a
     result; restating it is not.

STEP 3. Report by commenting on the ticket. Write the body to a file under
/tmp first, then:
  gh issue comment {{ISSUE}} -R {{REPO}} --body-file <path>

The comment MUST:
  - open with the line "## EXEC REPORT"
  - state, per acceptance criterion, PASS or FAIL and the evidence deciding it
  - quote raw command output for anything numeric — not a summary of it
  - state every selector as a runnable command, so the supervisor can re-run it
    and get your exact set back
  - report what you measured. A phase you could not complete is reported as not
    completed. An estimate presented as a measurement is the one unrecoverable
    failure mode.

When a phase asks you to APPLY A PREDICATE — to select, classify, exclude, or
attribute — the predicate must decide per item, from THAT ITEM'S OWN CONTENT. A
filter keyed on the item's container (file name, module, directory, name prefix)
is not a predicate, it is a list, and it is rejected:

  if 'test_urllib.py' in item.source:  drop(item)      # a list — rejected
  if asserts_named_behavior(item.body): keep(item)     # a predicate

Report, for every group you form, its size BEFORE and AFTER the predicate. A
group whose two counts are equal did not have the predicate applied to it; say
so explicitly rather than letting the totals hide it. Prose explaining why a
group was dropped is not a substitute for a branch that inspected it.

The same rule governs the CANDIDATE SURFACE, one stage earlier. Derive the set
of candidates from a rule evaluated over the WHOLE corpus, never from a list of
paths you typed out:

  target_files = {'a.rs', 'b.rs', ...}                  # a list — rejected
  candidate = matches_rule(item) and not in_excluded_family(item)   # a rule

A typed list of files fails silently and invisibly: nothing in your run reports
on a file you never enumerated, so every count you print looks healthy while a
whole suite is missing. Broad rule plus real per-item predicate is the required
shape — never a narrow list admitted wholesale.

State the rule as a runnable expression, name each family the rule then
subtracts, and give the size of each subtracted family. If you match on a
regex, say which field it is matched against — a path, a module, and a test
name are three different fields and match three different sets.

A predicate may not enumerate item IDENTITIES either:

  if fn_name in ["exec_redirected", "eval_input", "compile_await"]:  # rejected
      keep(item)

That is a list at the finest possible grain, and it is the form that most often
appears AFTER a rejection — because the supervisor's verdict named the items
that were wrongly missing, and adding those names is the shortest path to a
passing count. The names in a verdict are INSTANCES of the defect, never its
specification. If you are told test X was wrongly excluded, the fix is the rule
that admits X *and everything like X*; admitting X by name is a fitted answer,
scores no better than the original on the next audit, and is rejected again.

Reporting only in chat does not count as done. The comment is the record.

HARD PROHIBITIONS — violating any one voids the result:
  - Do NOT run cargo in any form, including `cargo check` and `cargo --version`.
    Sibling agents share the prebuilt binary
      {{BIN}}
    whose sha256 must remain {{SHA}}. One rebuild invalidates every sibling's
    measurement mid-wave.
  - Do NOT modify, create, or delete any file under {{ROOT}}. Write only under
    /tmp. This includes formatters, codegen, and "harmless" fixes.
  - Do NOT run git at all, and do NOT run `cd`. Neither is permitted, and a
    refusal aborts the run. Whether the tree stayed clean is checked by the
    supervisor against a snapshot taken before you were dispatched — report that
    criterion as "NOT SELF-VERIFIED (supervisor check)" rather than trying to
    prove it. This includes `git status`, `git diff`, commit, stash, checkout,
    branch, merge, and push.
  - Do NOT close the issue, change its labels, or edit its body. Closing is the
    supervisor's authority alone, exercised only after independent verification.

If a prohibition blocks the work, stop and report that as the finding. Reporting
a blocked ticket is a success; working around a prohibition is a failure.

STEP 4. Print a short summary to stdout ending with the comment URL.
