# Task: Implement Tests for Spec 'class-features-spec' (Change 'mamba-p1-lang-features')

## Instructions

Production code for spec 'class-features-spec' has been implemented and verified to compile.
Now implement **test functions only** (`#[test]` functions / `#[cfg(test)]` blocks).

1. Read spec **class-features-spec**: `cclab/changes/mamba-p1-lang-features/groups/class-features/specs/class-features-spec.md`
2. Read the `## Test Plan` section to understand required test cases
3. Implement `#[test]` functions (in `#[cfg(test)]` blocks) that cover the test plan
4. Run `cargo test` to verify tests pass
5. When done, run `cclab sdd workflow create-change-implementation mamba-p1-lang-features` to advance

## CLI Commands

```
# Read spec
Read file: cclab/changes/mamba-p1-lang-features/groups/class-features/specs/class-features-spec.md

# Run tests
cargo test

# Advance implementation workflow
cclab sdd workflow create-change-implementation mamba-p1-lang-features
```