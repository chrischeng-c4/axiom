---
id: mambalibs-http-uvicorn-style-run-surface
summary: Add a mambalibs.http run/App.run surface that returns a native Server handle over HostConfig and App metadata.
fill_sections: [scenarios, dependency, schema, manifest, test-plan, changes, tests]
---

## Scenarios
<!-- type: scenarios lang: yaml -->

```yaml
scenarios:
  - id: app-run-source-dx
    given:
      - a mambalibs.http FastAPI app has registered routes.
    when:
      - source calls app.run("127.0.0.1", 0).
    then:
      - the call returns a typed Server handle.
      - Server exposes url, host, port, running, and endpoint_count.

  - id: module-run-source-dx
    given:
      - source imports run from mambalibs.http.
    when:
      - source calls run(app, host, port).
    then:
      - the same native Server handle surface is returned.

  - id: openapi-on-server
    given:
      - the app has route metadata and schemas.
    when:
      - server.openapi is read.
    then:
      - it returns the app OpenAPI JSON string captured at run time.

  - id: compatibility-boundary
    given:
      - CPython stdlib modules and third-party uvicorn shim remain importable.
    when:
      - mambalibs.http adds methods, functions, or classes for framework ergonomics.
    then:
      - those additions are treated as extension.
      - stdlib syntax/behavior and uvicorn shim behavior remain compatibility constraints and are not mutated.
```

## Dependency Graph
<!-- type: dependency lang: mermaid -->

```mermaid
---
id: mambalibs_http_uvicorn_style_run_surface_graph
types:
  App: { kind: function, label: "mambalibs.http App" }
  Run: { kind: function, label: "mambalibs.http run/App.run" }
  HostConfig: { kind: schema, label: "HostConfig" }
  Server: { kind: schema, label: "Server handle" }
  OpenAPI: { kind: schema, label: "OpenAPI JSON" }
edges:
  - { from: App, to: Run, kind: invokes, label: "app.run" }
  - { from: Run, to: HostConfig, kind: builds, label: "host/port/protocol defaults" }
  - { from: Run, to: Server, kind: returns, label: "native handle" }
  - { from: App, to: OpenAPI, kind: exports, label: "captured metadata" }
  - { from: OpenAPI, to: Server, kind: stores, label: "server.openapi" }
---
flowchart TD
  App --> Run
  Run --> HostConfig
  Run --> Server
  App --> OpenAPI
  OpenAPI --> Server
```

## Schema
<!-- type: schema lang: yaml -->

```yaml
definitions:
  Server:
    type: object
    required: [host, port, url, running, endpoint_count, openapi]
    properties:
      host:
        type: string
      port:
        type: integer
      url:
        type: string
      running:
        type: boolean
      endpoint_count:
        type: integer
      openapi:
        type: string
        description: "OpenAPI JSON text captured when run was called."
```

## Manifest
<!-- type: manifest lang: yaml -->

```yaml
packages:
  - name: mambalibs-http-binding
    path: projects/mamba/mambalibs/httpkit/binding
    kind: rust-library
    dependencies:
      - { name: mambalibs-http, spec: path, path: ".." }
  - name: mamba
    path: projects/mamba
    kind: rust-binary
    features: [native-modules]
```

## Verification
<!-- type: test-plan lang: mermaid -->

```mermaid
---
id: mambalibs_http_uvicorn_style_run_surface_verification
requirements:
  app_run:
    id: R1
    text: "App.run returns Server handle from Mamba source"
    kind: integration
    risk: high
    verify: test
  module_run:
    id: R2
    text: "mambalibs.http.run returns equivalent Server handle"
    kind: regression
    risk: medium
    verify: test
  server_accessors:
    id: R3
    text: "Server exposes url/host/port/running/endpoint_count/openapi"
    kind: integration
    risk: high
    verify: test
  compatibility:
    id: R4
    text: "run surface is isolated to mambalibs.http"
    kind: compatibility
    risk: medium
    verify: test
elements:
  binding_tests:
    kind: test
    type: "cargo test --locked -p mambalibs-http-binding run_surface_returns_server_handle -- --nocapture"
  source_test:
    kind: test
    type: "cargo test --locked -p mamba --features native-modules native_modules_http_run_surface_source -- --nocapture"
relations:
  - { from: binding_tests, verifies: module_run }
  - { from: binding_tests, verifies: server_accessors }
  - { from: source_test, verifies: app_run }
  - { from: source_test, verifies: compatibility }
---
requirementDiagram
  requirement R1 {
    id: R1
    text: "app run"
    risk: high
    verifymethod: test
  }
  requirement R2 {
    id: R2
    text: "module run"
    risk: medium
    verifymethod: test
  }
  requirement R3 {
    id: R3
    text: "server accessors"
    risk: high
    verifymethod: test
  }
  requirement R4 {
    id: R4
    text: "compatibility"
    risk: medium
    verifymethod: test
  }
  element binding_tests {
    type: "cargo test --locked -p mambalibs-http-binding run_surface_returns_server_handle -- --nocapture"
  }
  element source_test {
    type: "cargo test --locked -p mamba --features native-modules native_modules_http_run_surface_source -- --nocapture"
  }
  binding_tests - verifies -> R2
  binding_tests - verifies -> R3
  source_test - verifies -> R1
  source_test - verifies -> R4
```

## Changes
<!-- type: changes lang: yaml -->

```yaml
files:
  - path: .aw/tech-design/projects/mamba/specs/4017.md
    action: create
    section: changes
    note: "Source of truth for #4017."
  - path: projects/mamba/mambalibs/httpkit/binding/src/server.rs
    action: create
    section: changes
    note: "Register Server/run and App.run binding accessors."
  - path: projects/mamba/mambalibs/httpkit/binding/src/lib.rs
    action: update
    section: changes
    note: "Register server surface after app surface."
  - path: projects/mamba/mambalibs/httpkit/binding/tests/mamba_registry_test.rs
    action: update
    section: tests
    note: "Cover binding-level run surface and server accessors."
  - path: projects/mamba/src/driver/mod.rs
    action: update
    section: tests
    note: "Cover source-level app.run/run DX."
  - path: projects/mamba/mambalibs/httpkit/README.md
    action: update
    section: changes
    note: "Document mambalibs.http run surface and compatibility boundary."
```

## Tests
<!-- type: tests lang: yaml -->

```yaml
tests:
  - name: run_surface_returns_server_handle
    assertions:
      - "module run returns Server"
      - "Server accessors expose host/port/url/running/endpoint_count/openapi"
      - "Server.stop flips running to false"
  - name: native_modules_http_run_surface_source
    assertions:
      - "app.run source syntax works"
      - "mambalibs.http.run source syntax works"
      - "Server accessors print expected values"
```
