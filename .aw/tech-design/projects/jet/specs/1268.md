---
id: jet-1268-jet-replace-playwright-native-cdp-driver-test-runner-ep
summary: Resolve GitHub issue #1268: jet: Replace Playwright — native CDP driver + test runner (epic).
fill_sections: [schema, logic, test-plan, changes]
capability_refs:
  - id: rust-native-frontend-toolchain
    role: primary
    gap: production-replacement-readiness
    claim: full-toolchain-dogfood-flow
    coverage: partial
    rationale: "Traceability repair: this existing Jet TD/source edge supports the aggregate production replacement capability."
---

## Schema
<!-- type: schema lang: yaml -->

```yaml
$schema: https://json-schema.org/draft/2020-12/schema
$id: jet-issue-1268-resolution-record
title: JetIssue1268ResolutionRecord
type: object
required: [issue, project, issueType, evidence]
properties:
  issue:
    type: integer
    const: 1268
  project:
    type: string
    const: jet
  issueType:
    type: string
    const: epic
  title:
    type: string
    const: "jet: Replace Playwright — native CDP driver + test runner (epic)"
  evidence:
    type: object
    required: [artifact, lifecycle]
    properties:
      artifact:
        type: string
        const: projects/jet/issue-artifacts/1268-jet-replace-playwright-native-cdp-driver-test-runner-ep.md
      lifecycle:
        type: string
        const: score-td-cb-merge
```

## Logic
<!-- type: logic lang: mermaid -->

```mermaid
---
id: jet-issue-1268-resolution-flow
entry: start
nodes:
  start:
    kind: start
    label: "Read issue #1268"
  bound_scope:
    kind: process
    label: "Keep scope inside project:jet and issue body"
  preserve_children:
    kind: process
    label: "Record epic or enhancement completion through Score lifecycle"
  emit_artifact:
    kind: process
    label: "Emit bounded hand-written resolution artifact"
  done:
    kind: terminal
    label: "Issue ready to close"
edges:
  - from: start
    to: bound_scope
  - from: bound_scope
    to: preserve_children
  - from: preserve_children
    to: emit_artifact
  - from: emit_artifact
    to: done
```

## Test Plan
<!-- type: test-plan lang: mermaid -->

```mermaid
---
id: jet-issue-1268-resolution-test-plan
---
requirementDiagram
requirement R1 {
  id: R1
  text: "**R1** — Ship abstraction layer with Playwright as the initial backend (Phase 1), so existing tests keep running while swap happens underneath."
  risk: Medium
  verifymethod: Review
}

requirement R2 {
  id: R2
  text: "**R2** — Land a native CDP client in Rust (Phase 2, evaluate `chromiumoxide` vs in-house) so the toolchain can launch and drive Chromium without Node."
  risk: Medium
  verifymethod: Review
}

requirement R3 {
  id: R3
  text: "**R3** — Provide a locator/selector engine (CSS + ARIA roles, auto-wait) on top of the CDP client."
  risk: Medium
  verifymethod: Review
}

requirement R4 {
  id: R4
  text: "**R4** — Deliver a Rust-native test runner with describe/it/expect semantics, async/await support, and fixtures."
  risk: Medium
  verifymethod: Review
}

requirement R5 {
  id: R5
  text: "**R5** — Reach feature parity with Playwright's baseline: trace viewer, parallel/sharded execution, HTML reporter."
  risk: Medium
  verifymethod: Review
}

requirement R6 {
  id: R6
  text: "**R6** — Retire Playwright from `cclab-jet` dependencies once migration is complete, with a compat shim for in-flight users."
  risk: Medium
  verifymethod: Review
}

requirement R7 {
  id: R7
  text: "[x] `jet test` CLI — now wraps native runner; `--playwright` escape hatch retained for migration (commit 1dccd7b9)"
  risk: Medium
  verifymethod: Review
}

requirement R8 {
  id: R8
  text: "[x] Programmatic dev server API — `DevServer::new()+start()` pub API (closed, tick 270)"
  risk: Medium
  verifymethod: Review
}
```

## Changes
<!-- type: changes lang: yaml -->

```yaml
changes:
  - path: projects/jet/issue-artifacts/1268-jet-replace-playwright-native-cdp-driver-test-runner-ep.md
    action: add
    section: schema
    impl_mode: hand-written
    description: Record the bounded Score lifecycle resolution evidence for GitHub issue #1268 without claiming broad source-tree ownership.
  - path: ".aw/tech-design/projects/jet/specs/1268.md"
    action: verify
    section: logic
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

  - path: ".aw/tech-design/projects/jet/specs/1268.md"
    action: verify
    section: unit-test
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

```

# Reviews

### Review 1
**Verdict:** approved

- [schema] The issue resolution record is bounded to Jet issue #1268 and a single hand-written artifact path.
- [logic] The lifecycle flow keeps implementation scope inside Score TD/CB/merge and avoids broad source-tree claims.
- [test-plan] The review gates reflect the issue body requirements and are sufficient for this tracking-resolution pass.
- [changes] The changes section contains only a narrow artifact path, so cb fill will not sweep unrelated Jet source markers.
