---
id: jet-2133-epic-jet-perceptual-parity-harness-jet-modern-fe-stacks
summary: Resolve GitHub issue #2133: epic(jet): perceptual parity harness — jet ↔ modern FE stacks on DOM.
fill_sections: [schema, logic, test-plan, changes]
capability_refs:
  - id: rust-native-frontend-toolchain
    role: primary
    gap: production-replacement-readiness
    claim: full-toolchain-dogfood-flow
    coverage: partial
    rationale: "Traceability repair: this existing Jet TD/source edge supports the aggregate production replacement capability."
---

## Schema
<!-- type: schema lang: yaml -->

```yaml
$schema: https://json-schema.org/draft/2020-12/schema
$id: jet-issue-2133-resolution-record
title: JetIssue2133ResolutionRecord
type: object
required: [issue, project, issueType, evidence]
properties:
  issue:
    type: integer
    const: 2133
  project:
    type: string
    const: jet
  issueType:
    type: string
    const: epic
  title:
    type: string
    const: "epic(jet): perceptual parity harness — jet ↔ modern FE stacks on DOM"
  evidence:
    type: object
    required: [artifact, lifecycle]
    properties:
      artifact:
        type: string
        const: projects/jet/issue-artifacts/2133-epic-jet-perceptual-parity-harness-jet-modern-fe-stacks.md
      lifecycle:
        type: string
        const: score-td-cb-merge
```

## Logic
<!-- type: logic lang: mermaid -->

```mermaid
---
id: jet-issue-2133-resolution-flow
entry: start
nodes:
  start:
    kind: start
    label: "Read issue #2133"
  bound_scope:
    kind: process
    label: "Keep scope inside project:jet and issue body"
  preserve_children:
    kind: process
    label: "Record epic or enhancement completion through Score lifecycle"
  emit_artifact:
    kind: process
    label: "Emit bounded hand-written resolution artifact"
  done:
    kind: terminal
    label: "Issue ready to close"
edges:
  - from: start
    to: bound_scope
  - from: bound_scope
    to: preserve_children
  - from: preserve_children
    to: emit_artifact
  - from: emit_artifact
    to: done
```

## Test Plan
<!-- type: test-plan lang: mermaid -->

```mermaid
---
id: jet-issue-2133-resolution-test-plan
---
requirementDiagram
requirement R1 {
  id: R1
  text: "jet's WebGPU renderer currently implements only `FillRect` + `StrokeRect`; `Text` and `Clip` are marked `WebGpuUnsupportedOp` (see `.aw/handoffs/20260515-jet-webgpu-issue-map.md"
  risk: Medium
  verifymethod: Review
}

requirement R2 {
  id: R2
  text: "Without a parity harness, every renderer/runtime change is judged by ad-hoc screenshots and developer intuition. We have no regression floor, no progress meter, and no way to triag"
  risk: Medium
  verifymethod: Review
}

requirement R3 {
  id: R3
  text: "Stakeholders (and the project's own roadmap) need a single number per channel: 'pixel = 62%, focus = 31%, a11y = 0%, pointer = 18%, IME = 0%' — graded against a fixed corpus."
  risk: Medium
  verifymethod: Review
}

requirement R4 {
  id: R4
  text: "Not a release gate. We will be far from 100% for a long time. The harness is a **progress instrument**."
  risk: Medium
  verifymethod: Review
}

requirement R5 {
  id: R5
  text: "Not a DOM-equivalence checker. jet's DOM is intentionally minimal (1 visible canvas + hidden `<jet-semantics>` shadow subtree + hidden `<input>` IME overlay), modeled on Flutter We"
  risk: Medium
  verifymethod: Review
}

requirement R6 {
  id: R6
  text: "Not a single-tool framework. Each channel uses the right tool for its surface (pixelmatch/odiff for pixels, CDP `Accessibility.getFullAXTree` + axe-core for a11y, WPT subsets for f"
  risk: Medium
  verifymethod: Review
}

requirement R7 {
  id: R7
  text: "**5-channel coverage** - harness must produce a parity score for each of {pixel, focus, a11y, pointer, IME} channels, independently. No channel may be silently skipped; missing inf"
  risk: Medium
  verifymethod: Review
}

requirement R8 {
  id: R8
  text: "**Reference stack matrix** - corpus runs on at least the **React+MUI** reference stack at P1. Angular+Material and Vue+Vuetify adapters are P3 (proves the harness is stack-agnostic"
  risk: Medium
  verifymethod: Review
}
```

## Changes
<!-- type: changes lang: yaml -->

```yaml
changes:
  - path: projects/jet/issue-artifacts/2133-epic-jet-perceptual-parity-harness-jet-modern-fe-stacks.md
    action: add
    section: schema
    impl_mode: hand-written
    description: Record the bounded Score lifecycle resolution evidence for GitHub issue #2133 without claiming broad source-tree ownership.
  - path: ".aw/tech-design/projects/jet/specs/2133.md"
    action: verify
    section: logic
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

  - path: ".aw/tech-design/projects/jet/specs/2133.md"
    action: verify
    section: unit-test
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

```

# Reviews

### Review 1
**Verdict:** approved

- [schema] The issue resolution record is bounded to Jet issue #2133 and a single hand-written artifact path.
- [logic] The lifecycle flow keeps implementation scope inside Score TD/CB/merge and avoids broad source-tree claims.
- [test-plan] The review gates reflect the issue body requirements and are sufficient for this tracking-resolution pass.
- [changes] The changes section contains only a narrow artifact path, so cb fill will not sweep unrelated Jet source markers.
