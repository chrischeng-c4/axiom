---
id: jet-native-test-runner-readiness-audit
summary: "Audit Jet native test-runner readiness against the L5 production replacement capability bar."
fill_sections:
  - scenarios
  - mindmap
  - state-machine
  - interaction
  - logic
  - dependency
  - db-model
  - schema
  - rest-api
  - rpc-api
  - async-api
  - cli
  - wireframe
  - component
  - design-token
  - config
  - manifest
  - runtime-image
  - deployment
  - test-plan
  - changes
  - tests
capability_refs:
  - id: native-test-product-flow-e2e
    role: primary
    gap: native-test-runner-readiness
    claim: native-test-runner-core
    coverage: partial
    rationale: "This TD defines the audit path for closing the native-test-runner readiness gap."
  - id: native-test-product-flow-e2e
    role: primary
    gap: native-test-runner-readiness
    claim: built-in-ts-test-runtime
    coverage: partial
    rationale: "This TD defines the audit path for closing the built-in TS test runtime claim."
  - id: native-test-product-flow-e2e
    role: primary
    gap: native-test-runner-readiness
    claim: reporter-artifacts
    coverage: partial
    rationale: "This TD defines the audit path for closing the reporter artifacts claim."
  - id: rust-native-frontend-toolchain
    role: contributes
    gap: production-replacement-readiness
    claim: full-toolchain-dogfood-flow
    coverage: partial
    rationale: "Native test execution is one child readiness area of the broader Jet toolchain replacement promise."
---

# Jet Native Test Runner Readiness Audit

## Scenarios
<!-- type: scenarios lang: yaml -->

```yaml
scenarios:
  - id: native_test_runner_baseline
    given: "Jet native test-runner source and tests exist under projects/jet/src/test_runner and adjacent reporter/trace surfaces."
    when: "The audit inspects discovery, reporters, snapshots, traces, worker pool, sharding, watch/debug behavior, web-server bootstrap, shim compatibility, and CI exit codes."
    then: "The TD records the current readiness level with source and test evidence."
  - id: incumbent_parity_matrix
    given: "Vitest, Jest, and Playwright test-runner expectations are the replacement targets."
    when: "The audit compares Jet behavior against frontend unit, component, and integration-style test expectations."
    then: "Every unsupported or divergent behavior is recorded as a concrete L5 blocker or accepted out of scope."
  - id: verification_gate_inventory
    given: "README capability verification commands are required gates."
    when: "The audit evaluates current runnable commands and missing commands."
    then: "The capability map can list exact verification gates instead of transient pass/fail timestamps."
  - id: followup_candidate_filter
    given: "The audit finds an implementation gap."
    when: "The gap lacks a fixture, gate, diagnostic expectation, or close criteria."
    then: "No implementation WI is opened until those fields are explicit."
```
## Mindmap
<!-- type: mindmap lang: mermaid -->

```mermaid
---
id: jet-native-test-runner-readiness-map
---
mindmap
  root((Jet native test-runner readiness))
    "Vitest and Jest parity"
      "test discovery"
      "watch and debug"
      "CI exit codes"
    "Execution quality"
      "worker pool"
      "sharding"
      "web-server bootstrap"
    "Artifacts"
      "reporters"
      "snapshots and screenshots"
      "trace output"
    "Evidence"
      "source"
      "tests"
      "commands"
      "follow-up WI candidates"
```
## State Machine
<!-- type: state-machine lang: mermaid -->

```mermaid
---
id: jet-native-test-runner-readiness-state
initial: start
nodes:
  start: { kind: initial, label: "Start native test-runner audit" }
  collect_surface: { kind: normal, label: "Collect source, tests, CLI, and README evidence" }
  compare_incumbents: { kind: normal, label: "Compare Vitest/Jest/Playwright test-runner parity areas" }
  classify_gaps: { kind: normal, label: "Classify L5 blockers and accepted divergences" }
  define_gates: { kind: normal, label: "Define exact verification gates" }
  complete: { kind: terminal, label: "Audit evidence ready for capability map" }
edges:
  - { from: start, to: collect_surface, event: "audit begins" }
  - { from: collect_surface, to: compare_incumbents, event: "surface inventory complete" }
  - { from: compare_incumbents, to: classify_gaps, event: "parity matrix complete" }
  - { from: classify_gaps, to: define_gates, event: "blockers named" }
  - { from: define_gates, to: complete, event: "gates recorded" }
---
stateDiagram-v2
    [*] --> collect_surface
    collect_surface --> compare_incumbents
    compare_incumbents --> classify_gaps
    classify_gaps --> define_gates
    define_gates --> complete
```
## Interaction
<!-- type: interaction lang: mermaid -->

```mermaid
---
id: jet-native-test-runner-readiness-interaction
actors:
  - { id: auditor, kind: actor }
  - { id: jet_source, kind: system }
  - { id: incumbent_tools, kind: system }
  - { id: capability_map, kind: system }
messages:
  - { from: auditor, to: jet_source, name: "Inspect native test-runner source, tests, and commands", returns: "current evidence" }
  - { from: auditor, to: incumbent_tools, name: "Compare Vitest/Jest/Playwright test-runner behavior", returns: "parity expectations" }
  - { from: auditor, to: capability_map, name: "Record gates and blockers", returns: "capability gap update inputs" }
---
sequenceDiagram
    actor auditor
    participant jet_source
    participant incumbent_tools
    participant capability_map
    auditor->>jet_source: Inspect source, tests, commands
    jet_source-->>auditor: Current evidence
    auditor->>incumbent_tools: Compare Vitest/Jest/Playwright behavior
    incumbent_tools-->>auditor: Parity expectations
    auditor->>capability_map: Record gates and blockers
    capability_map-->>auditor: Gap update inputs
```
## Logic
<!-- type: logic lang: mermaid -->

```mermaid
---
id: jet-native-test-runner-readiness-logic
entry: start
nodes:
  start: { kind: start, label: "Start audit" }
  inventory: { kind: process, label: "Inventory native test-runner source, tests, and CLI surfaces" }
  parity: { kind: process, label: "Map Vitest/Jest/Playwright test-runner parity areas" }
  gate_check: { kind: decision, label: "Does each required area have a runnable gate?" }
  record_gate: { kind: process, label: "Record exact command and proof target" }
  record_gap: { kind: process, label: "Record concrete blocker with fixture, artifact, and close criteria" }
  done: { kind: terminal, label: "Audit output ready" }
edges:
  - { from: start, to: inventory }
  - { from: inventory, to: parity }
  - { from: parity, to: gate_check }
  - { from: gate_check, to: record_gate, label: "yes" }
  - { from: gate_check, to: record_gap, label: "no" }
  - { from: record_gate, to: done }
  - { from: record_gap, to: done }
---
flowchart TD
    start([Start audit]) --> inventory[Inventory native test-runner source, tests, and CLI surfaces]
    inventory --> parity[Map Vitest/Jest/Playwright test-runner parity areas]
    parity --> gate_check{Does each required area have a runnable gate?}
    gate_check -->|yes| record_gate[Record exact command and proof target]
    gate_check -->|no| record_gap[Record concrete blocker with fixture, artifact, and close criteria]
    record_gate --> done([Audit output ready])
    record_gap --> done
```
## Dependency
<!-- type: dependency lang: mermaid -->

```mermaid
---
id: jet-native-test-runner-readiness-dependency
---
classDiagram
    class NativeTestRunnerReadinessAudit
    class JetTestRunnerSource
    class JetReporterSource
    class JetTraceSource
    class IncumbentTestRunnerMatrix
    class CapabilityMap
    class FollowupWorkItems
    NativeTestRunnerReadinessAudit --> JetTestRunnerSource
    NativeTestRunnerReadinessAudit --> JetReporterSource
    NativeTestRunnerReadinessAudit --> JetTraceSource
    NativeTestRunnerReadinessAudit --> IncumbentTestRunnerMatrix
    NativeTestRunnerReadinessAudit --> CapabilityMap
    NativeTestRunnerReadinessAudit --> FollowupWorkItems
```
## Data Model
<!-- type: db-model lang: mermaid -->

```mermaid
---
id: jet-native-test-runner-readiness-data-model
---
erDiagram
    READINESS_AUDIT {
      string capability_id
      string gap_id
      string readiness_level
    }
    PARITY_AREA {
      string id
      string incumbent_behavior
      string jet_status
    }
    VERIFICATION_GATE {
      string id
      string command
      string proves
    }
    READINESS_AUDIT ||--o{ PARITY_AREA : covers
    READINESS_AUDIT ||--o{ VERIFICATION_GATE : requires
```
## Schema
<!-- type: schema lang: yaml -->

```yaml
readiness_audit:
  capability_id: native-test-product-flow-e2e
  gap_id: native-test-runner-readiness
  fields:
    readiness_level: "L0|L1|L2|L3|L4|L5"
    evidence:
      source: "list of source paths"
      tests: "list of test files or commands"
      commands: "list of verification commands"
    parity_areas:
      - id: "native-test-runner-core"
        incumbent: "Vitest/Jest/Playwright runner behavior"
        jet_status: "supported|partial|missing|out_of_scope"
    blockers:
      - id: "stable blocker id"
        fixture_or_project: "fixture or real project"
        required_gate: "exact command or missing command"
        artifact_expectation: "diagnostic or artifact"
        close_criteria: "bounded done condition"
```
## REST API
<!-- type: rest-api lang: yaml -->

```yaml
not_applicable:
  reason: "The native test-runner readiness audit does not introduce an HTTP REST API."
```
## RPC API
<!-- type: rpc-api lang: yaml -->

```yaml
not_applicable:
  reason: "The native test-runner readiness audit does not introduce an RPC API."
```
## Async API
<!-- type: async-api lang: yaml -->

```yaml
not_applicable:
  reason: "The native test-runner readiness audit does not introduce pub-sub or WebSocket contracts."
```
## CLI
<!-- type: cli lang: yaml -->

```yaml
commands_to_audit:
  - "jet test"
  - "jet test --reporter json"
  - "jet test --shard"
  - "jet test --watch"
  - "jet test --trace"
verification_candidates:
  - id: native-test-runner-tests
    command: "cargo test -p jet test_runner -- --nocapture"
    proves: "native test discovery, scheduling, reporting, sharding, and worker behavior"
  - id: native-test-reporter-tests
    command: "cargo test -p jet reporter -- --nocapture"
    proves: "JSON/HTML reporter and artifact behavior"
  - id: native-test-trace-tests
    command: "cargo test -p jet trace -- --nocapture"
    proves: "trace artifact behavior used by test failures"
```
## Wireframe
<!-- type: wireframe lang: yaml -->

```yaml
not_applicable:
  reason: "The native test-runner readiness audit is CLI and evidence oriented; it does not introduce a UI layout."
```
## Component
<!-- type: component lang: yaml -->

```yaml
not_applicable:
  reason: "The native test-runner readiness audit does not introduce UI components."
```
## Design Token
<!-- type: design-token lang: yaml -->

```yaml
not_applicable:
  reason: "The native test-runner readiness audit does not introduce design tokens."
```
## Config
<!-- type: config lang: yaml -->

```yaml
config_surfaces_to_audit:
  - "test file globs"
  - "reporter configuration"
  - "worker and shard settings"
  - "watch/debug settings"
  - "web-server bootstrap configuration"
```
## Manifest
<!-- type: manifest lang: yaml -->

```yaml
manifest_surfaces_to_audit:
  - "package.json test scripts"
  - "test config files"
  - "snapshot and trace output paths"
  - "browser/component test dependencies"
```
## Runtime Image
<!-- type: runtime-image lang: yaml -->

```yaml
not_applicable:
  reason: "The native test-runner readiness audit does not introduce a container runtime image."
```
## Deployment
<!-- type: deployment lang: yaml -->

```yaml
not_applicable:
  reason: "The native test-runner readiness audit does not introduce deployment manifests."
```
## Test Plan
<!-- type: test-plan lang: mermaid -->

```mermaid
---
id: jet-native-test-runner-readiness-test-plan
requirements:
  R1:
    text: "The audit records a current L0-L5 native test-runner readiness level with source and test evidence."
    risk: high
    verify: inspection
  R2:
    text: "The audit compares Jet against Vitest, Jest, and Playwright test-runner expectations named in the WI."
    risk: high
    verify: inspection
  R3:
    text: "Every L5 blocker names a concrete fixture or project, required gate, artifact expectation, and close criteria."
    risk: high
    verify: inspection
  R4:
    text: "README capability verification gates are exact commands and do not store transient runtime results."
    risk: medium
    verify: command
---
requirementDiagram
requirement R1 {
  id: R1
  text: "Readiness level has evidence"
  risk: High
  verifymethod: Inspection
}
requirement R2 {
  id: R2
  text: "Incumbent parity areas are compared"
  risk: High
  verifymethod: Inspection
}
requirement R3 {
  id: R3
  text: "L5 blockers are concrete"
  risk: High
  verifymethod: Inspection
}
requirement R4 {
  id: R4
  text: "Verification gates are exact commands"
  risk: Medium
  verifymethod: Test
}
```
## Changes
<!-- type: changes lang: yaml -->

```yaml
changes:
  - path: .aw/tech-design/projects/jet/specs/3785.md
    action: create
    section: scenarios
    impl_mode: hand-written
    description: "Add the native test-runner readiness audit TD with capability refs for native-test-product-flow-e2e and the broader Jet toolchain promise."
  - path: projects/jet/README.md
    action: modify
    section: scenarios
    impl_mode: hand-written
    description: "Update the native test-runner capability evidence and gap status after the audit produces gates and blockers."
  - path: ".aw/tech-design/projects/jet/specs/3785.md"
    action: verify
    section: async-api
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

  - path: ".aw/tech-design/projects/jet/specs/3785.md"
    action: verify
    section: cli
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

  - path: ".aw/tech-design/projects/jet/specs/3785.md"
    action: verify
    section: component
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

  - path: ".aw/tech-design/projects/jet/specs/3785.md"
    action: verify
    section: config
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

  - path: ".aw/tech-design/projects/jet/specs/3785.md"
    action: verify
    section: db-model
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

  - path: ".aw/tech-design/projects/jet/specs/3785.md"
    action: verify
    section: dependency
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

  - path: ".aw/tech-design/projects/jet/specs/3785.md"
    action: verify
    section: deployment
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

  - path: ".aw/tech-design/projects/jet/specs/3785.md"
    action: verify
    section: design-token
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

  - path: ".aw/tech-design/projects/jet/specs/3785.md"
    action: verify
    section: interaction
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

  - path: ".aw/tech-design/projects/jet/specs/3785.md"
    action: verify
    section: logic
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

  - path: ".aw/tech-design/projects/jet/specs/3785.md"
    action: verify
    section: manifest
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

  - path: ".aw/tech-design/projects/jet/specs/3785.md"
    action: verify
    section: mindmap
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

  - path: ".aw/tech-design/projects/jet/specs/3785.md"
    action: verify
    section: rest-api
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

  - path: ".aw/tech-design/projects/jet/specs/3785.md"
    action: verify
    section: rpc-api
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

  - path: ".aw/tech-design/projects/jet/specs/3785.md"
    action: verify
    section: runtime-image
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

  - path: ".aw/tech-design/projects/jet/specs/3785.md"
    action: verify
    section: schema
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

  - path: ".aw/tech-design/projects/jet/specs/3785.md"
    action: verify
    section: state-machine
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

  - path: ".aw/tech-design/projects/jet/specs/3785.md"
    action: verify
    section: unit-test
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

  - path: ".aw/tech-design/projects/jet/specs/3785.md"
    action: verify
    section: wireframe
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

```
## Tests
<!-- type: tests lang: yaml -->

```yaml
tests:
  - id: capability-check
    command: "aw capability check jet --json"
    proves: "README capability refs and TD capability refs resolve."
  - id: native-test-runner-tests
    command: "cargo test -p jet test_runner -- --nocapture"
    proves: "Native test discovery, reporting, sharding, and worker behavior have a focused verification gate."
  - id: native-test-reporter-tests
    command: "cargo test -p jet reporter -- --nocapture"
    proves: "Reporter artifact behavior has a focused verification gate."
  - id: native-test-trace-tests
    command: "cargo test -p jet trace -- --nocapture"
    proves: "Trace artifact behavior has a focused verification gate."
```

# Reviews

### Review 1
**Verdict:** approved

- [scenarios] The scenarios align with the WI requirements and keep audit output bounded to evidence, parity, gates, and follow-up candidate criteria.
- [schema] The audit data model captures readiness level, evidence, parity areas, blockers, and exact gates with stable IDs.
- [cli] The command inventory covers `jet test`, reporters, sharding, watch, and trace-oriented candidate gates.
- [test-plan] Requirements map cleanly to inspection and command verification without storing transient runtime results in README.
- [changes] The implementation scope stays hand-written and limited to TD evidence plus README capability linkage.
