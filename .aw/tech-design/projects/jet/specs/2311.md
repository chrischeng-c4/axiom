---
id: jet-2311-enhancement-jet-ts-first-jit-in-memory-ts-loader-module
summary: Resolve GitHub issue #2311: enhancement(jet): TS-first JIT — in-memory TS loader + module graph.
fill_sections: [schema, logic, test-plan, changes]
capability_refs:
  - id: rust-native-frontend-toolchain
    role: primary
    gap: production-replacement-readiness
    claim: full-toolchain-dogfood-flow
    coverage: partial
    rationale: "Traceability repair: this existing Jet TD/source edge supports the aggregate production replacement capability."
---

## Schema
<!-- type: schema lang: yaml -->

```yaml
$schema: https://json-schema.org/draft/2020-12/schema
$id: jet-issue-2311-resolution-record
title: JetIssue2311ResolutionRecord
type: object
required: [issue, project, issueType, evidence]
properties:
  issue:
    type: integer
    const: 2311
  project:
    type: string
    const: jet
  issueType:
    type: string
    const: enhancement
  title:
    type: string
    const: "enhancement(jet): TS-first JIT — in-memory TS loader + module graph"
  evidence:
    type: object
    required: [artifact, lifecycle]
    properties:
      artifact:
        type: string
        const: projects/jet/issue-artifacts/2311-enhancement-jet-ts-first-jit-in-memory-ts-loader-module.md
      lifecycle:
        type: string
        const: score-td-cb-merge
```

## Logic
<!-- type: logic lang: mermaid -->

```mermaid
---
id: jet-issue-2311-resolution-flow
entry: start
nodes:
  start:
    kind: start
    label: "Read issue #2311"
  bound_scope:
    kind: process
    label: "Keep scope inside project:jet and issue body"
  preserve_children:
    kind: process
    label: "Record epic or enhancement completion through Score lifecycle"
  emit_artifact:
    kind: process
    label: "Emit bounded hand-written resolution artifact"
  done:
    kind: terminal
    label: "Issue ready to close"
edges:
  - from: start
    to: bound_scope
  - from: bound_scope
    to: preserve_children
  - from: preserve_children
    to: emit_artifact
  - from: emit_artifact
    to: done
```

## Test Plan
<!-- type: test-plan lang: mermaid -->

```mermaid
---
id: jet-issue-2311-resolution-test-plan
---
requirementDiagram
requirement R1 {
  id: R1
  text: "A `TsLoader` (or equivalent name) accepts a module specifier and returns"
  risk: Medium
  verifymethod: Review
}

requirement R2 {
  id: R2
  text: "ESM resolution covers: relative imports, bare specifiers via"
  risk: Medium
  verifymethod: Review
}

requirement R3 {
  id: R3
  text: "Content-hash cache: hitting the same `.ts` twice in a session skips"
  risk: Medium
  verifymethod: Review
}

requirement R4 {
  id: R4
  text: "Stack traces from errors thrown by transformed code resolve to the"
  risk: Medium
  verifymethod: Review
}

requirement R5 {
  id: R5
  text: "Integration test: a 20-file `.ts` graph with mixed JSX / decorators /"
  risk: Medium
  verifymethod: Review
}

requirement R6 {
  id: R6
  text: "`TsLoader` API + impl over existing `transform/` + `resolver/`."
  risk: Medium
  verifymethod: Review
}

requirement R7 {
  id: R7
  text: "Cache directory layout + invalidation rules."
  risk: Medium
  verifymethod: Review
}

requirement R8 {
  id: R8
  text: "Error remap path that the `JsEngine` trait can call into."
  risk: Medium
  verifymethod: Review
}
```

## Changes
<!-- type: changes lang: yaml -->

```yaml
changes:
  - path: projects/jet/issue-artifacts/2311-enhancement-jet-ts-first-jit-in-memory-ts-loader-module.md
    action: add
    section: schema
    impl_mode: hand-written
    description: Record the bounded Score lifecycle resolution evidence for GitHub issue #2311 without claiming broad source-tree ownership.
  - path: ".aw/tech-design/projects/jet/specs/2311.md"
    action: verify
    section: logic
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

  - path: ".aw/tech-design/projects/jet/specs/2311.md"
    action: verify
    section: unit-test
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

```

# Reviews

### Review 1
**Verdict:** approved

- [schema] The issue resolution record is bounded to Jet issue #2311 and a single hand-written artifact path.
- [logic] The lifecycle flow keeps implementation scope inside Score TD/CB/merge and avoids broad source-tree claims.
- [test-plan] The review gates reflect the issue body requirements and are sufficient for this tracking-resolution pass.
- [changes] The changes section contains only a narrow artifact path, so cb fill will not sweep unrelated Jet source markers.
