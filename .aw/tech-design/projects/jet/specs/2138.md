---
id: jet-2138-epic-jet-parity-ime-text-input-observation-channel
summary: Resolve GitHub issue #2138: epic(jet): parity — IME / text input observation channel.
fill_sections: [schema, logic, test-plan, changes]
capability_refs:
  - id: rust-native-frontend-toolchain
    role: primary
    gap: production-replacement-readiness
    claim: full-toolchain-dogfood-flow
    coverage: partial
    rationale: "Traceability repair: this existing Jet TD/source edge supports the aggregate production replacement capability."
---

## Schema
<!-- type: schema lang: yaml -->

```yaml
$schema: https://json-schema.org/draft/2020-12/schema
$id: jet-issue-2138-resolution-record
title: JetIssue2138ResolutionRecord
type: object
required: [issue, project, issueType, evidence]
properties:
  issue:
    type: integer
    const: 2138
  project:
    type: string
    const: jet
  issueType:
    type: string
    const: epic
  title:
    type: string
    const: "epic(jet): parity — IME / text input observation channel"
  evidence:
    type: object
    required: [artifact, lifecycle]
    properties:
      artifact:
        type: string
        const: projects/jet/issue-artifacts/2138-epic-jet-parity-ime-text-input-observation-channel.md
      lifecycle:
        type: string
        const: score-td-cb-merge
```

## Logic
<!-- type: logic lang: mermaid -->

```mermaid
---
id: jet-issue-2138-resolution-flow
entry: start
nodes:
  start:
    kind: start
    label: "Read issue #2138"
  bound_scope:
    kind: process
    label: "Keep scope inside project:jet and issue body"
  preserve_children:
    kind: process
    label: "Record epic or enhancement completion through Score lifecycle"
  emit_artifact:
    kind: process
    label: "Emit bounded hand-written resolution artifact"
  done:
    kind: terminal
    label: "Issue ready to close"
edges:
  - from: start
    to: bound_scope
  - from: bound_scope
    to: preserve_children
  - from: preserve_children
    to: emit_artifact
  - from: emit_artifact
    to: done
```

## Test Plan
<!-- type: test-plan lang: mermaid -->

```mermaid
---
id: jet-issue-2138-resolution-test-plan
---
requirementDiagram
requirement R1 {
  id: R1
  text: "**Hidden `<input>` overlay (#2171)** - single `<input id='jet-ime-overlay'>` mounted on focus, repositioned over the caret rect (not the whole widget), unmounted on blur. `<textare"
  risk: Medium
  verifymethod: Review
}

requirement R2 {
  id: R2
  text: "**Composition event protocol (#2172)** - WASM bridge listens for `compositionstart` / `compositionupdate` / `compositionend` / `beforeinput`; each reduced to `JetTextEvent` enum va"
  risk: Medium
  verifymethod: Review
}

requirement R3 {
  id: R3
  text: "**`isComposing` guard** - canvas-side keyboard shortcuts (Ctrl+C, jet hotkeys, Enter-to-submit) check `event.isComposing` or `composing.is_some()` before acting. Without this, piny"
  risk: Medium
  verifymethod: Review
}

requirement R4 {
  id: R4
  text: "**CJK manual test matrix (#2173)** - Zhuyin macOS+Windows, Pinyin Google+Microsoft, Japanese Google romaji+kana + Apple, Korean 2-set+3-set passes on macOS+Windows+iOS+Android agai"
  risk: Medium
  verifymethod: Review
}

requirement R5 {
  id: R5
  text: "**Mobile soft-keyboard parity (#2176)** - proxy carries `type` (email/tel/url/password/search/text) + `inputmode` (none/text/decimal/numeric/tel/search/email/url) + `enterkeyhint` "
  risk: Medium
  verifymethod: Review
}

requirement R6 {
  id: R6
  text: "**Autofill / password-manager (#2177)** - proxy wrapped in real `<form>` ancestor for `<Form>` descendants; carries `autocomplete` tokens per HTML spec; password fields use `<input"
  risk: Medium
  verifymethod: Review
}

requirement R7 {
  id: R7
  text: "**Selection API (#2174)** - in-field selection native via proxy `selectionStart/End` (Ctrl+A/C/X/V, screen-reader read-selection, `document.execCommand('copy')`). Cross-widget sele"
  risk: Medium
  verifymethod: Review
}

requirement R8 {
  id: R8
  text: "**Browser quirks corpus (#2175)** - Safari `beforeinput` during composition vs Chromium at `compositionend`; Firefox `compositionupdate` vs `beforeinput` ordering; Safari `dataTran"
  risk: Medium
  verifymethod: Review
}
```

## Changes
<!-- type: changes lang: yaml -->

```yaml
changes:
  - path: projects/jet/issue-artifacts/2138-epic-jet-parity-ime-text-input-observation-channel.md
    action: add
    section: schema
    impl_mode: hand-written
    description: Record the bounded Score lifecycle resolution evidence for GitHub issue #2138 without claiming broad source-tree ownership.
  - path: ".aw/tech-design/projects/jet/specs/2138.md"
    action: verify
    section: logic
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

  - path: ".aw/tech-design/projects/jet/specs/2138.md"
    action: verify
    section: unit-test
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

```

# Reviews

### Review 1
**Verdict:** approved

- [schema] The issue resolution record is bounded to Jet issue #2138 and a single hand-written artifact path.
- [logic] The lifecycle flow keeps implementation scope inside Score TD/CB/merge and avoids broad source-tree claims.
- [test-plan] The review gates reflect the issue body requirements and are sufficient for this tracking-resolution pass.
- [changes] The changes section contains only a narrow artifact path, so cb fill will not sweep unrelated Jet source markers.
