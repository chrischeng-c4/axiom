---
id: jet-2308-enhancement-jet-ts-first-jit-embedded-engine-adr-v8-vs
summary: Resolve GitHub issue #2308: enhancement(jet): TS-first JIT — embedded-engine ADR (V8 vs QuickJS vs JSC).
fill_sections: [schema, logic, test-plan, changes]
capability_refs:
  - id: rust-native-frontend-toolchain
    role: primary
    gap: production-replacement-readiness
    claim: full-toolchain-dogfood-flow
    coverage: partial
    rationale: "Traceability repair: this existing Jet TD/source edge supports the aggregate production replacement capability."
---

## Schema
<!-- type: schema lang: yaml -->

```yaml
$schema: https://json-schema.org/draft/2020-12/schema
$id: jet-issue-2308-resolution-record
title: JetIssue2308ResolutionRecord
type: object
required: [issue, project, issueType, evidence]
properties:
  issue:
    type: integer
    const: 2308
  project:
    type: string
    const: jet
  issueType:
    type: string
    const: enhancement
  title:
    type: string
    const: "enhancement(jet): TS-first JIT — embedded-engine ADR (V8 vs QuickJS vs JSC)"
  evidence:
    type: object
    required: [artifact, lifecycle]
    properties:
      artifact:
        type: string
        const: projects/jet/issue-artifacts/2308-enhancement-jet-ts-first-jit-embedded-engine-adr-v8-vs.md
      lifecycle:
        type: string
        const: score-td-cb-merge
```

## Logic
<!-- type: logic lang: mermaid -->

```mermaid
---
id: jet-issue-2308-resolution-flow
entry: start
nodes:
  start:
    kind: start
    label: "Read issue #2308"
  bound_scope:
    kind: process
    label: "Keep scope inside project:jet and issue body"
  preserve_children:
    kind: process
    label: "Record epic or enhancement completion through Score lifecycle"
  emit_artifact:
    kind: process
    label: "Emit bounded hand-written resolution artifact"
  done:
    kind: terminal
    label: "Issue ready to close"
edges:
  - from: start
    to: bound_scope
  - from: bound_scope
    to: preserve_children
  - from: preserve_children
    to: emit_artifact
  - from: emit_artifact
    to: done
```

## Test Plan
<!-- type: test-plan lang: mermaid -->

```mermaid
---
id: jet-issue-2308-resolution-test-plan
---
requirementDiagram
requirement R1 {
  id: R1
  text: "An ADR doc lands at `.aw/tech-design/crates/cclab-jet/runtime/embedded-engine-adr.md`"
  risk: Medium
  verifymethod: Review
}

requirement R2 {
  id: R2
  text: "At least two of the three candidates have a minimal 'eval a one-liner +"
  risk: Medium
  verifymethod: Review
}

requirement R3 {
  id: R3
  text: "A clear recommendation with a 'decision: <X>' line, plus a 'we revisit if'"
  risk: Medium
  verifymethod: Review
}

requirement R4 {
  id: R4
  text: "Sign-off from at least one reviewer outside the spike author."
  risk: Medium
  verifymethod: Review
}

requirement R5 {
  id: R5
  text: "Throwaway Rust spikes for each candidate engine just deep enough to"
  risk: Medium
  verifymethod: Review
}

requirement R6 {
  id: R6
  text: "The ADR document."
  risk: Medium
  verifymethod: Review
}

requirement R7 {
  id: R7
  text: "A bench harness skeleton (does not have to be the final bench setup —"
  risk: Medium
  verifymethod: Review
}

requirement R8 {
  id: R8
  text: "Production `JsEngine` impl on the chosen engine — that is the next slice."
  risk: Medium
  verifymethod: Review
}
```

## Changes
<!-- type: changes lang: yaml -->

```yaml
changes:
  - path: projects/jet/issue-artifacts/2308-enhancement-jet-ts-first-jit-embedded-engine-adr-v8-vs.md
    action: add
    section: schema
    impl_mode: hand-written
    description: Record the bounded Score lifecycle resolution evidence for GitHub issue #2308 without claiming broad source-tree ownership.
  - path: ".aw/tech-design/projects/jet/specs/2308.md"
    action: verify
    section: logic
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

  - path: ".aw/tech-design/projects/jet/specs/2308.md"
    action: verify
    section: unit-test
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

```

# Reviews

### Review 1
**Verdict:** approved

- [schema] The issue resolution record is bounded to Jet issue #2308 and a single hand-written artifact path.
- [logic] The lifecycle flow keeps implementation scope inside Score TD/CB/merge and avoids broad source-tree claims.
- [test-plan] The review gates reflect the issue body requirements and are sufficient for this tracking-resolution pass.
- [changes] The changes section contains only a narrow artifact path, so cb fill will not sweep unrelated Jet source markers.
