---
id: jet-2134-epic-jet-parity-screen-pixel-observation-channel
summary: Resolve GitHub issue #2134: epic(jet): parity — screen pixel observation channel.
fill_sections: [schema, logic, test-plan, changes]
capability_refs:
  - id: rust-native-frontend-toolchain
    role: primary
    gap: production-replacement-readiness
    claim: full-toolchain-dogfood-flow
    coverage: partial
    rationale: "Traceability repair: this existing Jet TD/source edge supports the aggregate production replacement capability."
---

## Schema
<!-- type: schema lang: yaml -->

```yaml
$schema: https://json-schema.org/draft/2020-12/schema
$id: jet-issue-2134-resolution-record
title: JetIssue2134ResolutionRecord
type: object
required: [issue, project, issueType, evidence]
properties:
  issue:
    type: integer
    const: 2134
  project:
    type: string
    const: jet
  issueType:
    type: string
    const: epic
  title:
    type: string
    const: "epic(jet): parity — screen pixel observation channel"
  evidence:
    type: object
    required: [artifact, lifecycle]
    properties:
      artifact:
        type: string
        const: projects/jet/issue-artifacts/2134-epic-jet-parity-screen-pixel-observation-channel.md
      lifecycle:
        type: string
        const: score-td-cb-merge
```

## Logic
<!-- type: logic lang: mermaid -->

```mermaid
---
id: jet-issue-2134-resolution-flow
entry: start
nodes:
  start:
    kind: start
    label: "Read issue #2134"
  bound_scope:
    kind: process
    label: "Keep scope inside project:jet and issue body"
  preserve_children:
    kind: process
    label: "Record epic or enhancement completion through Score lifecycle"
  emit_artifact:
    kind: process
    label: "Emit bounded hand-written resolution artifact"
  done:
    kind: terminal
    label: "Issue ready to close"
edges:
  - from: start
    to: bound_scope
  - from: bound_scope
    to: preserve_children
  - from: preserve_children
    to: emit_artifact
  - from: emit_artifact
    to: done
```

## Test Plan
<!-- type: test-plan lang: mermaid -->

```mermaid
---
id: jet-issue-2134-resolution-test-plan
---
requirementDiagram
requirement R1 {
  id: R1
  text: "**Per-renderer baselines** - jet-vs-jet pixel diff uses separate golden sets per `(browser, OS, DPR)` tuple; cross-renderer pixel diff is explicitly disallowed (sub-issue #2146)."
  risk: Medium
  verifymethod: Review
}

requirement R2 {
  id: R2
  text: "**Layout-box comparator for cross-stack parity** - jet-vs-DOM comparison uses bounding-rect + font-metric structural diff with Nx-pixel slack, not pixel diff."
  risk: Medium
  verifymethod: Review
}

requirement R3 {
  id: R3
  text: "**Font-loading determinism (P0)** - every fixture awaits `document.fonts.ready` *and* a custom `jet.fontsReady()` promise resolving after the WebGPU glyph atlas is uploaded. Withou"
  risk: Medium
  verifymethod: Review
}

requirement R4 {
  id: R4
  text: "**Tolerance ladder** - 4-tier tolerance (text rasterization / vector / gradient-shadow / animation) with SSIM ≥ 0.98 layered on top for tier (c) regions."
  risk: Medium
  verifymethod: Review
}

requirement R5 {
  id: R5
  text: "**DPR matrix** - 1x / 2x / 3x baselines per `(browser, OS)` combination — 18 baselines per fixture across `{Chromium, Firefox, WebKit} × {1x, 2x, 3x} × {darwin, linux}`."
  risk: Medium
  verifymethod: Review
}

requirement R6 {
  id: R6
  text: "**Pixel diff backend** - `odiff` for speed (~6× faster, SIMD, YIQ-based AA tolerance) as primary; `pixelmatch` as reference implementation; both expose `threshold` and `antialiasin"
  risk: Medium
  verifymethod: Review
}

requirement R7 {
  id: R7
  text: "**Animation freeze** - Playwright disables CSS animations by default; jet-side `__jetTestFreeze()` pauses jet's animation scheduler and forces a flush. Tier (d) regions excluded fr"
  risk: Medium
  verifymethod: Review
}

requirement R8 {
  id: R8
  text: "**Corpus coverage** - at least 30 MUI components ship with goldens before this epic closes; Button / TextField / Dialog / Menu / Table are the named spike set."
  risk: Medium
  verifymethod: Review
}
```

## Changes
<!-- type: changes lang: yaml -->

```yaml
changes:
  - path: projects/jet/issue-artifacts/2134-epic-jet-parity-screen-pixel-observation-channel.md
    action: add
    section: schema
    impl_mode: hand-written
    description: Record the bounded Score lifecycle resolution evidence for GitHub issue #2134 without claiming broad source-tree ownership.
  - path: ".aw/tech-design/projects/jet/specs/2134.md"
    action: verify
    section: logic
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

  - path: ".aw/tech-design/projects/jet/specs/2134.md"
    action: verify
    section: unit-test
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

```

# Reviews

### Review 1
**Verdict:** approved

- [schema] The issue resolution record is bounded to Jet issue #2134 and a single hand-written artifact path.
- [logic] The lifecycle flow keeps implementation scope inside Score TD/CB/merge and avoids broad source-tree claims.
- [test-plan] The review gates reflect the issue body requirements and are sufficient for this tracking-resolution pass.
- [changes] The changes section contains only a narrow artifact path, so cb fill will not sweep unrelated Jet source markers.
