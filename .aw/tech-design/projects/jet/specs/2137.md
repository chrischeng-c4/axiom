---
id: jet-2137-epic-jet-parity-pointer-event-observation-channel
summary: Resolve GitHub issue #2137: epic(jet): parity — pointer event observation channel.
fill_sections: [schema, logic, test-plan, changes]
capability_refs:
  - id: rust-native-frontend-toolchain
    role: primary
    gap: production-replacement-readiness
    claim: full-toolchain-dogfood-flow
    coverage: partial
    rationale: "Traceability repair: this existing Jet TD/source edge supports the aggregate production replacement capability."
---

## Schema
<!-- type: schema lang: yaml -->

```yaml
$schema: https://json-schema.org/draft/2020-12/schema
$id: jet-issue-2137-resolution-record
title: JetIssue2137ResolutionRecord
type: object
required: [issue, project, issueType, evidence]
properties:
  issue:
    type: integer
    const: 2137
  project:
    type: string
    const: jet
  issueType:
    type: string
    const: epic
  title:
    type: string
    const: "epic(jet): parity — pointer event observation channel"
  evidence:
    type: object
    required: [artifact, lifecycle]
    properties:
      artifact:
        type: string
        const: projects/jet/issue-artifacts/2137-epic-jet-parity-pointer-event-observation-channel.md
      lifecycle:
        type: string
        const: score-td-cb-merge
```

## Logic
<!-- type: logic lang: mermaid -->

```mermaid
---
id: jet-issue-2137-resolution-flow
entry: start
nodes:
  start:
    kind: start
    label: "Read issue #2137"
  bound_scope:
    kind: process
    label: "Keep scope inside project:jet and issue body"
  preserve_children:
    kind: process
    label: "Record epic or enhancement completion through Score lifecycle"
  emit_artifact:
    kind: process
    label: "Emit bounded hand-written resolution artifact"
  done:
    kind: terminal
    label: "Issue ready to close"
edges:
  - from: start
    to: bound_scope
  - from: bound_scope
    to: preserve_children
  - from: preserve_children
    to: emit_artifact
  - from: emit_artifact
    to: done
```

## Test Plan
<!-- type: test-plan lang: mermaid -->

```mermaid
---
id: jet-issue-2137-resolution-test-plan
---
requirementDiagram
requirement R1 {
  id: R1
  text: "**Single glass-pane router (#2164)** - exactly one `<canvas id='jet-root'>` in the DOM; no per-widget listeners; all routing in WASM. Mirrors Flutter `_PointerAdapter`. Listeners o"
  risk: Medium
  verifymethod: Review
}

requirement R2 {
  id: R2
  text: "**Hit-test agrees with `document.elementFromPoint`** - 1000-sample parity grid (overlapping z-index, transforms, scrolled containers, modal overlays) must show 100% target-id match"
  risk: Medium
  verifymethod: Review
}

requirement R3 {
  id: R3
  text: "**WPT `pointerevents/` ≥ 95% pass rate** - capture, boundary events, coalesced events, predicted events, primary-pointer rules, button sanitization, `pointerType` normalisation. Po"
  risk: Medium
  verifymethod: Review
}

requirement R4 {
  id: R4
  text: "**Nested scroll & momentum (#2167)** - scroll chain rules defined; deepest scrollable consumes first, remainder bubbles. Fixtures: MUI `DataGrid` (nested vertical), Drawer-in-Dialo"
  risk: Medium
  verifymethod: Review
}

requirement R5 {
  id: R5
  text: "**`touch-action` consumed by browser pre-JS (#2168)** - layout pass emits `touch-action` on canvas root reflecting gesture surface under pointer at `pointerdown`. `pointercancel` f"
  risk: Medium
  verifymethod: Review
}

requirement R6 {
  id: R6
  text: "**Drag-and-drop bridge (#2169)** - real DOM proxy element with `draggable='true'` spawned under cursor on drag-eligible `pointerdown`; drop-zone overlays sized to widget bounds; `D"
  risk: Medium
  verifymethod: Review
}

requirement R7 {
  id: R7
  text: "**Hover & cursor (#2170)** - `canvas.style.cursor` updated on every `pointermove` per widget under pointer (`pointer`, `text`, `grab`, `not-allowed`, …). Browser only honors canvas"
  risk: Medium
  verifymethod: Review
}

requirement R8 {
  id: R8
  text: "**Click → focus coupling** - primary-pointer compatibility-click synthesizes focus on target (matches Chromium/Safari `mousedown` focus behaviour; Firefox UA-native)."
  risk: Medium
  verifymethod: Review
}
```

## Changes
<!-- type: changes lang: yaml -->

```yaml
changes:
  - path: projects/jet/issue-artifacts/2137-epic-jet-parity-pointer-event-observation-channel.md
    action: add
    section: schema
    impl_mode: hand-written
    description: Record the bounded Score lifecycle resolution evidence for GitHub issue #2137 without claiming broad source-tree ownership.
  - path: ".aw/tech-design/projects/jet/specs/2137.md"
    action: verify
    section: logic
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

  - path: ".aw/tech-design/projects/jet/specs/2137.md"
    action: verify
    section: unit-test
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

```

# Reviews

### Review 1
**Verdict:** approved

- [schema] The issue resolution record is bounded to Jet issue #2137 and a single hand-written artifact path.
- [logic] The lifecycle flow keeps implementation scope inside Score TD/CB/merge and avoids broad source-tree claims.
- [test-plan] The review gates reflect the issue body requirements and are sufficient for this tracking-resolution pass.
- [changes] The changes section contains only a narrow artifact path, so cb fill will not sweep unrelated Jet source markers.
