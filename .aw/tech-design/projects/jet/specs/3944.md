---
id: live-dom-wasm-layout-parity
summary: Compare live React DOM oracle layout boxes with Jet WASM debug layout boxes.
fill_sections:
  - scenarios
  - mindmap
  - state-machine
  - interaction
  - logic
  - dependency
  - db-model
  - schema
  - rest-api
  - rpc-api
  - async-api
  - cli
  - wireframe
  - component
  - design-token
  - config
  - manifest
  - runtime-image
  - deployment
  - unit-test
  - e2e-test
  - changes
capability_refs:
  - id: browser-trace-parity
    role: primary
    gap: parity-corpus-gates
    claim: parity-corpus-gates
    coverage: partial
    rationale: "This TD broadens live DOM/WASM parity evidence from host-tree shape to layout geometry."
---

# Live DOM/WASM Layout Parity

## Scenarios
<!-- type: scenarios lang: yaml -->

```yaml
scenarios:
  - id: compare_static_layout
    given: "A React DOM oracle page and a Jet WASM app render the same static fixture."
    when: "The harness captures DOM getBoundingClientRect data and Jet WASM debug layoutTree data."
    then: "The normalized layout-box sequences match within the configured CSS-pixel tolerance."
  - id: compare_stateful_layout_after_click
    given: "A stateful fixture mutates visible children after a click."
    when: "The harness clicks the DOM element and the matching WASM canvas point."
    then: "Both initial and post-click layout-box sequences match."
  - id: emit_structured_layout_mismatch
    given: "A DOM/WASM layout difference is detected."
    when: "The parity assertion fails."
    then: "The failure message includes fixture id, phase, expected boxes, and actual boxes as JSON."
  - id: require_live_e2e_prerequisites
    given: "Node, Chromium, wasm-pack, or local React dependencies are missing."
    when: "The layout parity test starts."
    then: "The test fails with an explicit prerequisite error instead of skipping."
  - id: reject_optional_env_gate
    given: "A maintainer searches Jet and AW metadata after the change."
    when: "They search for the legacy optional WASM E2E env gate."
    then: "No test, spec, issue, or health gate path references that optional gate."
```
## Mindmap
<!-- type: mindmap lang: mermaid -->

```mermaid
---
id: live-dom-wasm-layout-parity-map
---
mindmap
  root((layout parity))
    "DOM oracle"
      "React fixture page"
      "getBoundingClientRect"
      "normalized boxes"
    "WASM observation"
      "Jet browser"
      "__jet_debug.layoutTree"
      "normalized boxes"
    "Fixtures"
      "static"
      "className state"
      "list state"
    "Evidence"
      "fixture id"
      "phase"
      "expected boxes"
      "actual boxes"
    "Policy"
      "required prerequisites"
      "no optional env gate"
```
## State Machine
<!-- type: state-machine lang: mermaid -->

```mermaid
---
id: live-dom-wasm-layout-parity-state
initial: start
nodes:
  start: { kind: initial, label: "Start layout parity run" }
  enumerate: { kind: normal, label: "Enumerate fixture cases" }
  launch_dom: { kind: normal, label: "Launch DOM oracle page" }
  launch_wasm: { kind: normal, label: "Launch Jet WASM app" }
  capture_initial: { kind: normal, label: "Capture initial host tree and layout boxes" }
  interact: { kind: normal, label: "Apply optional interaction" }
  capture_after: { kind: normal, label: "Capture after-click host tree and layout boxes" }
  compare: { kind: choice, label: "Layout boxes equal within tolerance?" }
  passed: { kind: terminal, label: "Parity evidence accepted" }
  failed: { kind: terminal, label: "Structured mismatch emitted" }
edges:
  - { from: start, to: enumerate, event: "test starts" }
  - { from: enumerate, to: launch_dom, event: "fixture selected" }
  - { from: launch_dom, to: launch_wasm, event: "DOM ready" }
  - { from: launch_wasm, to: capture_initial, event: "WASM ready" }
  - { from: capture_initial, to: interact, event: "initial compared" }
  - { from: interact, to: capture_after, event: "interaction delivered" }
  - { from: capture_after, to: compare, event: "after compared" }
  - { from: compare, to: passed, event: "equal" }
  - { from: compare, to: failed, event: "mismatch" }
---
stateDiagram-v2
    [*] --> enumerate
    enumerate --> launch_dom: fixture selected
    launch_dom --> launch_wasm: DOM ready
    launch_wasm --> capture_initial: WASM ready
    capture_initial --> interact: initial compared
    interact --> capture_after: interaction delivered
    capture_after --> compare: after compared
    compare --> passed: equal
    compare --> failed: mismatch
```
## Interaction
<!-- type: interaction lang: mermaid -->

```mermaid
---
id: live-dom-wasm-layout-parity-interaction
actors:
  - { id: harness, kind: actor }
  - { id: dom_page, kind: participant }
  - { id: wasm_page, kind: participant }
  - { id: jet_browser, kind: system }
messages:
  - { from: harness, to: dom_page, name: "Render React DOM fixture", returns: "DOM root" }
  - { from: harness, to: wasm_page, name: "Build and render Jet WASM fixture", returns: "debug bridge" }
  - { from: harness, to: jet_browser, name: "Capture DOM layout boxes", returns: "expected boxes" }
  - { from: harness, to: jet_browser, name: "Capture WASM layoutTree", returns: "actual boxes" }
  - { from: harness, to: dom_page, name: "Run optional DOM click", returns: "updated DOM" }
  - { from: harness, to: wasm_page, name: "Run optional WASM canvas click", returns: "updated WASM" }
  - { from: harness, to: jet_browser, name: "Compare layout boxes", returns: "pass or JSON mismatch" }
---
sequenceDiagram
    actor harness
    participant dom_page
    participant wasm_page
    participant jet_browser
    harness->>dom_page: Render React DOM fixture
    dom_page-->>harness: DOM root
    harness->>wasm_page: Build and render Jet WASM fixture
    wasm_page-->>harness: debug bridge
    harness->>jet_browser: Capture DOM layout boxes
    jet_browser-->>harness: expected boxes
    harness->>jet_browser: Capture WASM layoutTree
    jet_browser-->>harness: actual boxes
    harness->>dom_page: Run optional DOM click
    dom_page-->>harness: updated DOM
    harness->>wasm_page: Run optional WASM canvas click
    wasm_page-->>harness: updated WASM
    harness->>jet_browser: Compare layout boxes
    jet_browser-->>harness: pass or JSON mismatch
```
## Logic
<!-- type: logic lang: mermaid -->

```mermaid
---
id: live-dom-wasm-layout-parity-logic
entry: start
nodes:
  start: { kind: start, label: "test invoked" }
  require: { kind: process, label: "require Node, Chromium, wasm-pack, React" }
  prepare: { kind: process, label: "prepare DOM page and WASM app" }
  capture_initial: { kind: process, label: "capture host tree and layout boxes" }
  initial_equal: { kind: decision, label: "initial boxes match?" }
  interact: { kind: process, label: "run optional click" }
  capture_after: { kind: process, label: "capture after-click boxes" }
  after_equal: { kind: decision, label: "after boxes match?" }
  pass: { kind: terminal, label: "pass" }
  fail: { kind: terminal, label: "emit expected/actual JSON" }
edges:
  - { from: start, to: require }
  - { from: require, to: prepare }
  - { from: prepare, to: capture_initial }
  - { from: capture_initial, to: initial_equal }
  - { from: initial_equal, to: interact, label: "yes" }
  - { from: initial_equal, to: fail, label: "no" }
  - { from: interact, to: capture_after }
  - { from: capture_after, to: after_equal }
  - { from: after_equal, to: pass, label: "yes" }
  - { from: after_equal, to: fail, label: "no" }
---
flowchart TD
    start([test invoked]) --> require[require Node, Chromium, wasm-pack, React]
    require --> prepare[prepare DOM page and WASM app]
    prepare --> capture_initial[capture host tree and layout boxes]
    capture_initial --> initial_equal{initial boxes match?}
    initial_equal -->|yes| interact[run optional click]
    initial_equal -->|no| fail([emit expected/actual JSON])
    interact --> capture_after[capture after-click boxes]
    capture_after --> after_equal{after boxes match?}
    after_equal -->|yes| pass([pass])
    after_equal -->|no| fail
```
## Dependency
<!-- type: dependency lang: mermaid -->

```mermaid
---
id: live-dom-wasm-layout-parity-dependency
---
classDiagram
    class FixtureParityCase {
      id
      example_name
      dom_render_script
      interaction
    }
    class ReactDomOracle {
      fixture_html()
      dom_layout_boxes_expr()
      layout_diff_message()
    }
    class JetTestApp {
      launch_project()
      layout_tree()
      click_canvas()
    }
    class LayoutBox {
      key
      tag
      text
      x
      y
      w
      h
    }
    class LayoutParityTest {
      assert_tree_phase()
      assert_layout_phase()
    }
    LayoutParityTest --> FixtureParityCase
    LayoutParityTest --> ReactDomOracle
    LayoutParityTest --> JetTestApp
    LayoutParityTest --> LayoutBox
```
## DB Model
<!-- type: db-model lang: mermaid -->

```mermaid
---
id: live-dom-wasm-layout-parity-db
---
erDiagram
    FIXTURE_CASE ||--o{ LAYOUT_PHASE : has
    LAYOUT_PHASE ||--|| DOM_LAYOUT_BOXES : expects
    LAYOUT_PHASE ||--|| WASM_LAYOUT_BOXES : observes
    DOM_LAYOUT_BOXES ||--|| PARITY_ASSERTION : compares
    WASM_LAYOUT_BOXES ||--|| PARITY_ASSERTION : compares
```
## Schema
<!-- type: schema lang: yaml -->

```yaml
schemas:
  LayoutBox:
    type: object
    required: [key, kind, x, y, w, h]
    properties:
      key: { type: string }
      kind: { enum: [element, text] }
      tag: { type: string }
      id: { type: string }
      text: { type: string }
      x: { type: number }
      y: { type: number }
      w: { type: number }
      h: { type: number }
  LayoutMismatch:
    type: object
    required: [fixture_id, phase, expected, actual]
    properties:
      fixture_id: { type: string }
      phase: { type: string }
      expected:
        type: array
        items: { $ref: "#/schemas/LayoutBox" }
      actual:
        type: array
        items: { $ref: "#/schemas/LayoutBox" }
```
## REST API
<!-- type: rest-api lang: yaml -->

```yaml
not_applicable:
  reason: "No REST API is added or changed by this test contract."
```
## RPC API
<!-- type: rpc-api lang: yaml -->

```yaml
not_applicable:
  reason: "No JSON-RPC API is added or changed by this test contract."
```
## Async API
<!-- type: async-api lang: yaml -->

```yaml
not_applicable:
  reason: "No pub-sub or WebSocket API is added or changed by this test contract."
```
## CLI
<!-- type: cli lang: yaml -->

```yaml
commands:
  - name: cargo test -p jet --test react_dom_oracle_conformance multi_fixture_dom_wasm_layout_parity
    purpose: "Run the targeted live DOM/WASM layout-box parity proof."
  - name: repository search for the legacy optional WASM E2E env gate
    purpose: "Prove the optional E2E env gate was not reintroduced."
```
## Wireframe
<!-- type: wireframe lang: yaml -->

```yaml
not_applicable:
  reason: "No user-facing UI layout is added; the browser surface is test evidence only."
```
## Component
<!-- type: component lang: yaml -->

```yaml
components:
  - name: LayoutParityHarness
    kind: test-helper
    inputs: [FixtureParityCase]
    outputs: [LayoutMismatch]
    responsibility: "Render matching DOM/WASM surfaces and compare normalized layout boxes."
```
## Design Token
<!-- type: design-token lang: yaml -->

```yaml
not_applicable:
  reason: "No visual design tokens are added or changed."
```
## Config
<!-- type: config lang: yaml -->

```yaml
config:
  required_env:
    - CHROME_PATH
  forbidden_env:
    - legacy optional WASM E2E gate
  layout_tolerance_css_px: 1.0
  behavior:
    missing_prerequisite: fail
```
## Manifest
<!-- type: manifest lang: yaml -->

```yaml
manifests:
  - path: projects/jet/Cargo.toml
    action: unchanged
    reason: "The test uses existing Jet test dependencies."
```
## Runtime Image
<!-- type: runtime-image lang: yaml -->

```yaml
not_applicable:
  reason: "No container or runtime image is added."
```
## Deployment
<!-- type: deployment lang: yaml -->

```yaml
not_applicable:
  reason: "No deployment manifest is added or changed."
```
## Unit Test
<!-- type: unit-test lang: mermaid -->

```mermaid
---
id: live-dom-wasm-layout-parity-unit
---
requirementDiagram
    requirement layout_message {
        id: UT1
        text: "Diff evidence includes fixture id, phase, expected layout boxes, and actual layout boxes."
        risk: medium
        verifymethod: test
    }
    requirement box_normalization {
        id: UT2
        text: "DOM and WASM layout observations normalize to comparable keys and CSS-pixel rectangles."
        risk: high
        verifymethod: test
    }
```
## E2E Test
<!-- type: e2e-test lang: yaml -->

```yaml
e2e_tests:
  - id: multi_fixture_dom_wasm_layout_parity
    name: multi fixture DOM/WASM layout parity
    command: "cargo test -p jet --test react_dom_oracle_conformance multi_fixture_dom_wasm_layout_parity -- --nocapture"
    prerequisites:
      missing_behavior: "fail"
      requires: [node, chromium, wasm-pack, react-dom-node-modules]
    assertions:
      - "static fixture initial DOM/WASM layout boxes match"
      - "class/state fixture initial and after-click DOM/WASM layout boxes match"
      - "list/state fixture initial and after-click DOM/WASM layout boxes match"
      - "layout mismatches include fixture id, phase, expected, and actual JSON"
```
## Changes
<!-- type: changes lang: yaml -->

```yaml
changes:
  - path: projects/jet/tests/common/react_oracle.rs
    action: update
    section: unit-test
    impl_mode: hand-written
    reason: "Add DOM layout-box capture, WASM layout normalization, and mismatch evidence helpers."
  - path: projects/jet/tests/react_dom_oracle_conformance.rs
    action: update
    section: e2e-test
    impl_mode: hand-written
    reason: "Add multi-fixture DOM/WASM layout parity coverage."
  - path: projects/jet/wasm/src/renderer/mod.rs
    action: update
    section: logic
    impl_mode: hand-written
    reason: "Make simple span/button/block layout boxes match the live DOM oracle for the parity fixtures."
  - path: .aw/tech-design/projects/jet/specs/3944.md
    action: add
    section: scenarios
    impl_mode: hand-written
    reason: "Record the TD contract for WI 3944."
  - path: .aw/tech-design/projects/jet/specs/3944.md
    action: add
    section: mindmap
    impl_mode: hand-written
    reason: "Record the TD contract for WI 3944."
  - path: .aw/tech-design/projects/jet/specs/3944.md
    action: add
    section: state-machine
    impl_mode: hand-written
    reason: "Record the TD contract for WI 3944."
  - path: .aw/tech-design/projects/jet/specs/3944.md
    action: add
    section: interaction
    impl_mode: hand-written
    reason: "Record the TD contract for WI 3944."
  - path: .aw/tech-design/projects/jet/specs/3944.md
    action: add
    section: logic
    impl_mode: hand-written
    reason: "Record the TD contract for WI 3944."
  - path: .aw/tech-design/projects/jet/specs/3944.md
    action: add
    section: dependency
    impl_mode: hand-written
    reason: "Record the TD contract for WI 3944."
  - path: .aw/tech-design/projects/jet/specs/3944.md
    action: add
    section: db-model
    impl_mode: hand-written
    reason: "Record the TD contract for WI 3944."
  - path: .aw/tech-design/projects/jet/specs/3944.md
    action: add
    section: schema
    impl_mode: hand-written
    reason: "Record the TD contract for WI 3944."
  - path: .aw/tech-design/projects/jet/specs/3944.md
    action: add
    section: rest-api
    impl_mode: hand-written
    reason: "Record the TD contract for WI 3944."
  - path: .aw/tech-design/projects/jet/specs/3944.md
    action: add
    section: rpc-api
    impl_mode: hand-written
    reason: "Record the TD contract for WI 3944."
  - path: .aw/tech-design/projects/jet/specs/3944.md
    action: add
    section: async-api
    impl_mode: hand-written
    reason: "Record the TD contract for WI 3944."
  - path: .aw/tech-design/projects/jet/specs/3944.md
    action: add
    section: cli
    impl_mode: hand-written
    reason: "Record the TD contract for WI 3944."
  - path: .aw/tech-design/projects/jet/specs/3944.md
    action: add
    section: wireframe
    impl_mode: hand-written
    reason: "Record the TD contract for WI 3944."
  - path: .aw/tech-design/projects/jet/specs/3944.md
    action: add
    section: component
    impl_mode: hand-written
    reason: "Record the TD contract for WI 3944."
  - path: .aw/tech-design/projects/jet/specs/3944.md
    action: add
    section: design-token
    impl_mode: hand-written
    reason: "Record the TD contract for WI 3944."
  - path: .aw/tech-design/projects/jet/specs/3944.md
    action: add
    section: config
    impl_mode: hand-written
    reason: "Record the TD contract for WI 3944."
  - path: .aw/tech-design/projects/jet/specs/3944.md
    action: add
    section: manifest
    impl_mode: hand-written
    reason: "Record the TD contract for WI 3944."
  - path: .aw/tech-design/projects/jet/specs/3944.md
    action: add
    section: runtime-image
    impl_mode: hand-written
    reason: "Record the TD contract for WI 3944."
  - path: .aw/tech-design/projects/jet/specs/3944.md
    action: add
    section: deployment
    impl_mode: hand-written
    reason: "Record the TD contract for WI 3944."
```

# Reviews

### Review 1
**Verdict:** approved

- [schema] The LayoutBox and LayoutMismatch shapes provide a clear machine-readable contract for implementation and failure output.
- [logic] The control flow preserves the existing fixture parity loop while adding layout-box assertions before and after stateful interactions.
- [e2e-test] The live browser command and assertions are specific enough to verify DOM/WASM external geometry without broadening into full pixel/a11y/focus parity.
- [changes] The implementation surface is narrowly scoped to the React oracle helper, conformance test, and TD artifact.
