---
id: jet-2136-epic-jet-parity-accessibility-tree-observation-channel
summary: Resolve GitHub issue #2136: epic(jet): parity — accessibility tree observation channel.
fill_sections: [schema, logic, test-plan, changes]
capability_refs:
  - id: rust-native-frontend-toolchain
    role: primary
    gap: production-replacement-readiness
    claim: full-toolchain-dogfood-flow
    coverage: partial
    rationale: "Traceability repair: this existing Jet TD/source edge supports the aggregate production replacement capability."
---

## Schema
<!-- type: schema lang: yaml -->

```yaml
$schema: https://json-schema.org/draft/2020-12/schema
$id: jet-issue-2136-resolution-record
title: JetIssue2136ResolutionRecord
type: object
required: [issue, project, issueType, evidence]
properties:
  issue:
    type: integer
    const: 2136
  project:
    type: string
    const: jet
  issueType:
    type: string
    const: epic
  title:
    type: string
    const: "epic(jet): parity — accessibility tree observation channel"
  evidence:
    type: object
    required: [artifact, lifecycle]
    properties:
      artifact:
        type: string
        const: projects/jet/issue-artifacts/2136-epic-jet-parity-accessibility-tree-observation-channel.md
      lifecycle:
        type: string
        const: score-td-cb-merge
```

## Logic
<!-- type: logic lang: mermaid -->

```mermaid
---
id: jet-issue-2136-resolution-flow
entry: start
nodes:
  start:
    kind: start
    label: "Read issue #2136"
  bound_scope:
    kind: process
    label: "Keep scope inside project:jet and issue body"
  preserve_children:
    kind: process
    label: "Record epic or enhancement completion through Score lifecycle"
  emit_artifact:
    kind: process
    label: "Emit bounded hand-written resolution artifact"
  done:
    kind: terminal
    label: "Issue ready to close"
edges:
  - from: start
    to: bound_scope
  - from: bound_scope
    to: preserve_children
  - from: preserve_children
    to: emit_artifact
  - from: emit_artifact
    to: done
```

## Test Plan
<!-- type: test-plan lang: mermaid -->

```mermaid
---
id: jet-issue-2136-resolution-test-plan
---
requirementDiagram
requirement R1 {
  id: R1
  text: "**Semantics emitter walks React fiber tree at commit time** - one ARIA-bearing DOM element (or small fragment for composites) per mounted widget, retained across frames keyed by st"
  risk: Medium
  verifymethod: Review
}

requirement R2 {
  id: R2
  text: "**Emitter contract is the keystone (#2158)** - one table per jet widget with columns `(ARIA role, required state attrs, ARIA APG pattern URL)`. Every other sub-issue asserts agains"
  risk: Medium
  verifymethod: Review
}

requirement R3 {
  id: R3
  text: "**Computed AX tree diff is the strongest signal (#2160, P0)** - CDP `Accessibility.getFullAXTree` snapshot normalised to `{role, name, value, properties_subset, ignored}` (no `node"
  risk: Medium
  verifymethod: Review
}

requirement R4 {
  id: R4
  text: "**axe-core CI gate (#2159)** - zero WCAG 2.1 AA violations on every parity fixture; same ruleset MUI's own CI uses (`jest-axe`). Pa11y intentionally not adopted (axe-core is the de"
  risk: Medium
  verifymethod: Review
}

requirement R5 {
  id: R5
  text: "**Live-region timing (#2161)** - `aria-live` mutations occur within one commit boundary of the React state transition, with the correct role (`status` / `alert`) and politeness."
  risk: Medium
  verifymethod: Review
}

requirement R6 {
  id: R6
  text: "**Accessible-name conformance (#2163)** - WPT `wpt/accname/` vendored subset is 100% passing; starts with `name/basic.html` + `name/comp_label.html` + `name/comp_labelledby.html` a"
  risk: Medium
  verifymethod: Review
}

requirement R7 {
  id: R7
  text: "**Screen-reader smoke matrix (#2162)** - per-release manual pass on NVDA+Firefox, VoiceOver+Safari, JAWS+Chrome, TalkBack+Chrome-on-Android using W3C ARIA-AT test plans."
  risk: Medium
  verifymethod: Review
}

requirement R8 {
  id: R8
  text: "**Shared `<jet-semantics>` subtree** - same shadow DOM the focus channel uses; emitter contract co-designed with focus's `tabindex`/focus-order contract (#2152 jointly owned)."
  risk: Medium
  verifymethod: Review
}
```

## Changes
<!-- type: changes lang: yaml -->

```yaml
changes:
  - path: projects/jet/issue-artifacts/2136-epic-jet-parity-accessibility-tree-observation-channel.md
    action: add
    section: schema
    impl_mode: hand-written
    description: Record the bounded Score lifecycle resolution evidence for GitHub issue #2136 without claiming broad source-tree ownership.
  - path: ".aw/tech-design/projects/jet/specs/2136.md"
    action: verify
    section: logic
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

  - path: ".aw/tech-design/projects/jet/specs/2136.md"
    action: verify
    section: unit-test
    impl_mode: hand-written
    description: |
      Traceability repair: hand-written TD section retained as the implementation edge during AW standardization.

```

# Reviews

### Review 1
**Verdict:** approved

- [schema] The issue resolution record is bounded to Jet issue #2136 and a single hand-written artifact path.
- [logic] The lifecycle flow keeps implementation scope inside Score TD/CB/merge and avoids broad source-tree claims.
- [test-plan] The review gates reflect the issue body requirements and are sufficient for this tracking-resolution pass.
- [changes] The changes section contains only a narrow artifact path, so cb fill will not sweep unrelated Jet source markers.
