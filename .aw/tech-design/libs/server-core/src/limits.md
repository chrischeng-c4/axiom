---
id: limits
fill_sections: [overview, schema, changes]
---

## Overview
<!-- type: overview lang: markdown -->

Module `limits` (Rust) — 11 symbol(s) (8 public). Spec auto-generated by `score fillback`; `impl_mode: hand-written` until extended with proper Schema / Logic sections.

### Symbols

| Name | Kind | Visibility | Line |
|------|------|------------|------|
| `ConnectionBudget` | struct | pub | 7 |
| `ConnectionBudgetInner` | struct | priv | 12 |
| `new` | function | pub | 18 |
| `max` | function | pub | 27 |
| `active` | function | pub | 31 |
| `available` | function | pub | 35 |
| `try_acquire` | function | pub | 39 |
| `ConnectionPermit` | struct | pub | 64 |
| `drop` | function | priv | 69 |
| `ConnectionLimitExceeded` | struct | pub | 76 |
| `tests` | module | priv | 82 |

### Public Signatures

```rust
new(max: usize) -> Self

max(&self) -> usize

active(&self) -> usize

available(&self) -> usize

try_acquire(&self) -> Result<ConnectionPermit, ConnectionLimitExceeded>

```

### Imports

- `std::sync::atomic` (external)
- `std::sync::Arc` (external)

## Schema
<!-- type: schema lang: yaml -->

```yaml
schemas:
  - title: ConnectionBudget
    type: object
    rust_type: ConnectionBudget
    required: [inner]
    properties:
      inner:
        rust_type: "Arc<ConnectionBudgetInner>"
  - title: ConnectionPermit
    type: object
    rust_type: ConnectionPermit
    required: [budget]
    properties:
      budget:
        rust_type: "ConnectionBudget"
  - title: ConnectionLimitExceeded
    type: object
    rust_type: ConnectionLimitExceeded
    required: [max, active]
    properties:
      max:
        rust_type: "usize"
      active:
        rust_type: "usize"
```

## Changes
<!-- type: changes lang: yaml -->

```yaml
changes:
  - path: libs/server-core/src/limits.rs
    action: modify
    impl_mode: hand-written
    description: |
      Pre-existing module captured by `score fillback`.
      Governance: hand-written until extended with
      Schema / Logic / Interface sections.
```
