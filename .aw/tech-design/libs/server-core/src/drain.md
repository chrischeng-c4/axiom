---
id: drain
fill_sections: [overview, schema, changes]
---

## Overview
<!-- type: overview lang: markdown -->

Module `drain` (Rust) — 13 symbol(s) (12 public). Spec auto-generated by `score fillback`; `impl_mode: hand-written` until extended with proper Schema / Logic sections.

### Symbols

| Name | Kind | Visibility | Line |
|------|------|------------|------|
| `DrainState` | enum | pub | 6 |
| `is_draining` | function | pub | 12 |
| `DrainController` | struct | pub | 19 |
| `new` | function | pub | 24 |
| `signal` | function | pub | 29 |
| `state` | function | pub | 35 |
| `is_draining` | function | pub | 39 |
| `start_drain` | function | pub | 43 |
| `default` | function | priv | 49 |
| `DrainSignal` | struct | pub | 56 |
| `state` | function | pub | 61 |
| `is_draining` | function | pub | 65 |
| `changed` | function | pub | 69 |

### Public Signatures

```rust
is_draining(self) -> bool

new() -> Self

signal(&self) -> DrainSignal

state(&self) -> DrainState

is_draining(&self) -> bool

start_drain(&self)

state(&self) -> DrainState

is_draining(&self) -> bool

changed(&mut self) -> DrainState

```

### Imports

- `tokio::sync::watch` (external)

## Schema
<!-- type: schema lang: yaml -->

```yaml
schemas:
  - title: DrainState
    type: enum
    rust_type: DrainState
    enum:
      - Ready
      - Draining
  - title: DrainController
    type: object
    rust_type: DrainController
    required: [tx]
    properties:
      tx:
        rust_type: "watch::Sender<DrainState>"
  - title: DrainSignal
    type: object
    rust_type: DrainSignal
    required: [rx]
    properties:
      rx:
        rust_type: "watch::Receiver<DrainState>"
```

## Changes
<!-- type: changes lang: yaml -->

```yaml
changes:
  - path: libs/server-core/src/drain.rs
    action: modify
    impl_mode: hand-written
    description: |
      Pre-existing module captured by `score fillback`.
      Governance: hand-written until extended with
      Schema / Logic / Interface sections.
```
