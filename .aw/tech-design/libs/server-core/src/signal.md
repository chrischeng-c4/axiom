---
id: signal
fill_sections: [overview, changes]
---

## Overview
<!-- type: overview lang: markdown -->

Module `signal` (Rust) — 2 symbol(s) (2 public). Spec auto-generated by `score fillback`; `impl_mode: hand-written` until extended with proper Schema / Logic sections.

### Symbols

| Name | Kind | Visibility | Line |
|------|------|------------|------|
| `wait_shutdown_signal` | function | pub | 5 |
| `shutdown_with_drain` | function | pub | 27 |

### Public Signatures

```rust
/// Resolve when SIGINT or, on Unix, SIGTERM arrives. @spec projects/agentic-workflow/tech-design/logic/shared-server-substrate-performance-layers.md#logic
wait_shutdown_signal()

/// Await shutdown, start draining, hold the grace window, then return.
shutdown_with_drain(start_drain: impl FnOnce() + Send, grace: Duration)

```

### Imports

- `std::time::Duration` (external)

## Changes
<!-- type: changes lang: yaml -->

```yaml
changes:
  - path: libs/server-core/src/signal.rs
    action: modify
    impl_mode: hand-written
    description: |
      Pre-existing module captured by `score fillback`.
      Governance: hand-written until extended with
      Schema / Logic / Interface sections.
```
