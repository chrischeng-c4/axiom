---
id: config
fill_sections: [overview, schema, changes]
---

## Overview
<!-- type: overview lang: markdown -->

Module `config` (Rust) — 5 symbol(s) (4 public). Spec auto-generated by `score fillback`; `impl_mode: hand-written` until extended with proper Schema / Logic sections.

### Symbols

| Name | Kind | Visibility | Line |
|------|------|------------|------|
| `BindConfig` | struct | pub | 6 |
| `any` | function | pub | 13 |
| `localhost` | function | pub | 21 |
| `socket_addr` | function | pub | 28 |
| `default` | function | priv | 34 |

### Public Signatures

```rust
/// Bind on all IPv4 interfaces.
any(port: u16) -> Self

/// Bind on localhost.
localhost(port: u16) -> Self

socket_addr(&self) -> SocketAddr

```

### Imports

- `std::net` (external)

## Schema
<!-- type: schema lang: yaml -->

```yaml
schemas:
  - title: BindConfig
    type: object
    rust_type: BindConfig
    required: [host, port]
    properties:
      host:
        rust_type: "IpAddr"
      port:
        rust_type: "u16"
```

## Changes
<!-- type: changes lang: yaml -->

```yaml
changes:
  - path: libs/server-core/src/config.rs
    action: modify
    impl_mode: hand-written
    description: |
      Pre-existing module captured by `score fillback`.
      Governance: hand-written until extended with
      Schema / Logic / Interface sections.
```
