#932 SpecIR codegen last mile: need actual .tera template files loaded by Tera engine, generators must produce compilable code from SpecIR (not inline fallback). Axum + FastAPI targets. #933 RequirementPlus test scaffold: need BDD test file generator (given/when/then → #[test] functions), coverage validation report (requirements without Verifies relationships). Rust target first. #934 Deploy section type completion: need deploy section_rules keyword registration, section type matcher, k8s validation rules. #937 Frontend codegen completion: need design-token DTCG → CSS custom properties + Tailwind config, CEM → TypeScript prop interface + component skeleton, cross-ref to rest-api for data fetching hooks.