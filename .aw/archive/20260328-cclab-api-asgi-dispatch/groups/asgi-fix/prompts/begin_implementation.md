# Task: Begin Implementation for Change 'cclab-api-asgi-dispatch'

## Instructions

1. List all change specs in `cclab/changes/cclab-api-asgi-dispatch/`
2. Read spec **asgi-dispatch-spec** to understand requirements: `cclab/changes/cclab-api-asgi-dispatch/groups/asgi-fix/specs/asgi-dispatch-spec.md`
3. Implement **production code only** (no `#[test]` functions) for each change spec in order, starting with **asgi-dispatch-spec**
4. When done with asgi-dispatch-spec, run `cclab sdd workflow create-change-implementation cclab-api-asgi-dispatch` to advance

## CLI Commands

```
# Read spec
Read file: cclab/changes/cclab-api-asgi-dispatch/groups/asgi-fix/specs/asgi-dispatch-spec.md

# Advance implementation workflow
cclab sdd workflow create-change-implementation cclab-api-asgi-dispatch
```