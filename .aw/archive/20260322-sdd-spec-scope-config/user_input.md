Configurable spec scope resolution via prefix matching in cclab/config.toml. (1) Add [specs.scopes] config with prefix→subdir mapping, (2) Replace hardcoded crates/projects/ fallback in scope.rs and file_service.rs with config-driven resolve_spec_dir(), (3) cclab sdd init auto-detects from Cargo.toml workspace, pyproject.toml, package.json, (4) cclab-agent FileSystemSpecStore using same config. Affects: crates/cclab-sdd (models, scope, file_service, knowledge_service, init) + crates/cclab-agent (new spec_store.rs)