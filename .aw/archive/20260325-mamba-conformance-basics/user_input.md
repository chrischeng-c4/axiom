#1037 — Fix basic Mamba conformance bugs found during benchmarking: (1) recursive function returns wrong value (fib(30) = 0 instead of 832040), (2) string concatenation with + operator raises type error instead of concatenating, (3) print() returns 0 which gets printed as extra output. These are critical correctness bugs in the runtime/codegen that must be fixed before any meaningful testing.