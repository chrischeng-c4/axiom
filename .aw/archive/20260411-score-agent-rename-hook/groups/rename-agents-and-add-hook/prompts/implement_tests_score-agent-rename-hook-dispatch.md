# Task: Implement Tests for Spec 'score-agent-rename-hook-dispatch' (Change 'score-agent-rename-hook')

## Instructions

Production code for spec 'score-agent-rename-hook-dispatch' has been implemented and verified to compile.
Now implement **test functions only** (`#[test]` functions / `#[cfg(test)]` blocks).

1. Read spec **score-agent-rename-hook-dispatch**: `.score/changes/score-agent-rename-hook/groups/rename-agents-and-add-hook/specs/score-agent-rename-hook-dispatch.md`
2. Read the `## Test Plan` section to understand required test cases
3. Implement `#[test]` functions (in `#[cfg(test)]` blocks) that cover the test plan
4. Run `cargo test` to verify tests pass
5. When done, run `score workflow create-change-implementation score-agent-rename-hook` to advance

## CLI Commands

```
# Read spec
Read file: .score/changes/score-agent-rename-hook/groups/rename-agents-and-add-hook/specs/score-agent-rename-hook-dispatch.md

# Run tests
cargo test

# Advance implementation workflow
score workflow create-change-implementation score-agent-rename-hook
```