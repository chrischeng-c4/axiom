AOT production build — tree shaking, code splitting, minification, source maps (#765). Phase 1: verify jet build correctness by building a real React TodoMVC app with both jet and Vite, comparing output via Playwright DOM snapshot tests. Phase 2: implement tree shaking (ESM unused export removal), env replacement (process.env.NODE_ENV), whitespace minification, and external source maps.