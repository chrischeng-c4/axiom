Consolidate 6 cclab-grid-* crates (core, formula, history, db, server, wasm) into a single cclab-grid crate with modules. Currently 23k LOC across 6 separate crates — merge into one crate with sub-modules: core/, formula/, history/, db/, server/. Keep wasm as separate crate (needs different build target). Crates: cclab-grid-core, cclab-grid-formula, cclab-grid-history, cclab-grid-db, cclab-grid-server