# Task: Begin Implementation for Change 'mamba-refcount-jit'

## Instructions

1. List all change specs in `cclab/changes/mamba-refcount-jit/`
2. Read spec **mamba-refcount-jit-spec** to understand requirements: `cclab/changes/mamba-refcount-jit/groups/refcount-jit/specs/mamba-refcount-jit-spec.md`
3. Implement **production code only** (no `#[test]` functions) for each change spec in order, starting with **mamba-refcount-jit-spec**
4. When done with mamba-refcount-jit-spec, run `cclab sdd workflow create-change-implementation mamba-refcount-jit` to advance

## CLI Commands

```
# Read spec
Read file: cclab/changes/mamba-refcount-jit/groups/refcount-jit/specs/mamba-refcount-jit-spec.md

# Advance implementation workflow
cclab sdd workflow create-change-implementation mamba-refcount-jit
```