---
change: gen-thread-pool
group: gen-thread-pool
date: 2026-03-26
written_by: artifact_cli
---

# Reference Context

| Spec | Group | Relevance | Key Requirements |
|------|-------|-----------|------------------|
| generator | runtime | high | Current architecture: thread_local! GENERATORS/PENDING_CHANNELS per-thread HashMaps — generator state invisible across threads; nested generators (e.g., [[j for j in range(3)] for i in range(3)]) stored in body thread's thread-local, never visible to test thread's cleanup_all_generators(), R1: GeneratorState machine (Created/Suspended/Running/Completed) must remain correct through pool dispatch, R2/R3: yield/send/throw protocol uses SyncSender/Receiver channels — channel endpoints must be stored in global registry so any thread can route messages, ensure_started: currently spawns a new thread::spawn per generator; replace with pool-dispatch: send body closure to a pool worker via crossbeam or std::sync::mpsc job queue, mb_generator_create: lazy-init pool on first call (OnceLock<GenPool>); alloc_gen_id must use AtomicU64 (not thread_local! Cell) for global uniqueness, cleanup_all_generators: must drain the GLOBAL registry (not just calling-thread's thread-local); join all pool workers before returning so JIT memory is safe to free |
| mamba-thread-safe-spec | runtime | high | R3: No-GIL — all runtime state accessible from any thread without GIL; the GenPool global registry follows this rule using Arc<Mutex<...>> or DashMap, R5: Global Shared State pattern — MambaRuntime uses DashMap for coroutines/tasks; GenPool follows the same pattern for generators, R7: Atomic Global Counters — alloc_gen_id in generator.rs must use AtomicU64 matching the pattern established for alloc_coro_id/alloc_task_id, Class diagram shows MambaRuntime as the global shared struct; GenPool registry should follow the same OnceLock<Arc<...>> initialization pattern, Safepoint model: workers in the pool must reach a safe termination point when cleanup is requested — drain pending jobs, close channels, then join |
| cclab-mamba-fix-xfail-spec | testing | high | conformance_tests.rs runs each test in thread::spawn — JIT backend is created, executed, and dropped per test; cleanup_all_generators() called before end_capture to drain lingering generator threads, list_comprehension.py fixture: [[j for j in range(3)] for i in range(3)] — outer comprehension generator spawns inner comprehension generators from within body thread; inner generators stored in body thread's thread-local are invisible to cleanup_all_generators() on test thread → SIGBUS when JIT drops, Fix must ensure cleanup_all_generators() drains ALL generators regardless of which thread created them — requires global (non-thread-local) generator registry, Existing xfail for list_comprehension nested case should be removed once thread pool fix lands, Generator conformance fixtures: generators/basic_yield.py, generators/send_throw.py, generators/yield_from.py — must remain passing after pool refactor |
| cranelift-jit | codegen | medium | R4: Executable Memory Management — JIT backend is dropped after test completes; generator body threads executing JIT-compiled code MUST be joined before drop or they will access freed executable memory → SIGBUS, R2: Symbol Table Population — mb_generator_create, mb_generator_next, mb_generator_send, mb_generator_yield_value are wired as mb_* symbols; pool refactor must keep same extern C signatures, JIT module lifecycle: CraneliftJitBackend dropped when test function completes; conformance_tests.rs calls cleanup_all_generators() before drop — this dependency must remain in the pool design, aarch64 SIGBUS context: bus error occurs when non-owner thread executes a page that has been mprotect-removed or freed by JIT module drop |
| value-and-rc | runtime | medium | MbValue is a 64-bit NaN-boxed value — it is Copy/Clone by value, safe to send across threads by copying the 64-bit word, MbObject heap objects use ref_count: usize (not AtomicUsize in current spec, but thread-safe-runtime.md R1 mandates migration to AtomicU32/AtomicUsize) — pool workers may hold MbValue pointers; reference counting must be thread-safe, Generator args (Vec<MbValue>) cloned when body thread starts — each pool worker gets its own cloned args; no shared mutable MbValue state between caller and worker, ObjData::Function variant holds MbFunction (body function address) — fn_addr stored as raw u64 in ThreadedGen; no aliasing issue since pool workers only read it |
| gc | runtime | medium | GC_TEST_LOCK: LazyLock<Mutex<()>> serialises GC tests — established pattern for safe global state in tests; GenPool initialization can use the same OnceLock pattern, Mark phase roots: pool worker threads hold MbValue references to generator args and local vars — GC root scanning must be aware of pool worker thread stacks when STW GC runs, R4: STW safepoint flag — pool workers must check gc_safepoint_flag at yield/resume checkpoints to allow GC to pause mutators (future work, but pool design must not block this), Container tracking: generator state structs (ThreadedGen) hold Vec<MbValue> args — these are heap allocations that GC must track if they contain heap object refs |
| stdlib-concurrency | stdlib | low | threading.Thread/Lock primitives provide the user-facing API; GenPool is an internal implementation detail not exposed as stdlib threading, Pre-clarification: pool size 2-4 threads — parallel list comprehensions within a single test need concurrent generators, not full parallelism across tests, Lazy init pattern matches threading.Thread start() semantics: pool created on first use, not at import time |

## Spec Plan

| Spec ID | Action | Main Spec Ref | Sections |
|---------|--------|---------------|----------|
| generator-thread-pool-design | modify | crates/mamba/runtime/generator.md | overview, state-machine, changes, test-plan |

