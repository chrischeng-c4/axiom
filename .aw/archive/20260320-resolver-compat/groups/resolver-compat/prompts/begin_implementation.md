# Task: Begin Implementation for Change 'resolver-compat'

## Instructions

1. List all change specs in `cclab/changes/resolver-compat/specs/`
2. Read spec **resolver-compat-spec** to understand requirements
3. Implement code for each change spec in order, starting with **resolver-compat-spec**
4. Run tests to verify
5. When done with resolver-compat-spec, run `cclab sdd workflow create-change-implementation resolver-compat` to advance

## CLI Commands

```
# Read spec
Read file: cclab/changes/resolver-compat/specs/resolver-compat-spec.md

# Advance implementation workflow
cclab sdd workflow create-change-implementation resolver-compat
```