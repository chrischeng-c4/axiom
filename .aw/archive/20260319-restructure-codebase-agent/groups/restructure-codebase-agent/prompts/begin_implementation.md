# Task: Begin Implementation for Change 'restructure-codebase-agent'

## Instructions

1. List all change specs in `cclab/changes/restructure-codebase-agent/specs/`
2. Read spec **restructure-codebase-agent-spec** to understand requirements
3. Implement code for each change spec in order, starting with **restructure-codebase-agent-spec**
4. Run tests to verify
5. When done with restructure-codebase-agent-spec, run `cclab sdd workflow create-change-implementation restructure-codebase-agent` to advance

## CLI Commands

```
# Read spec
Read file: cclab/changes/restructure-codebase-agent/specs/restructure-codebase-agent-spec.md

# Advance implementation workflow
cclab sdd workflow create-change-implementation restructure-codebase-agent
```