# Task: Implement Tests for Spec 'jet-hmr-validation-spec' (Change 'jet-hmr-validation')

## Instructions

Production code for spec 'jet-hmr-validation-spec' has been implemented and verified to compile.
Now implement **test functions only** (`#[test]` functions / `#[cfg(test)]` blocks).

1. Read spec **jet-hmr-validation-spec**: `cclab/changes/jet-hmr-validation/groups/jet-hmr-validation/specs/jet-hmr-validation-spec.md`
2. Read the `## Test Plan` section to understand required test cases
3. Implement `#[test]` functions (in `#[cfg(test)]` blocks) that cover the test plan
4. Run `cargo test` to verify tests pass
5. When done, run `cclab sdd workflow create-change-implementation jet-hmr-validation` to advance

## CLI Commands

```
# Read spec
Read file: cclab/changes/jet-hmr-validation/groups/jet-hmr-validation/specs/jet-hmr-validation-spec.md

# Run tests
cargo test

# Advance implementation workflow
cclab sdd workflow create-change-implementation jet-hmr-validation
```