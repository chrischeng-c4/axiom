# Task: Begin Implementation for Change 'change-impl-agent'

## Instructions

1. List all change specs in `cclab/changes/change-impl-agent/specs/`
2. Read spec **change-impl-agent-spec** to understand requirements
3. Implement code for each change spec in order, starting with **change-impl-agent-spec**
4. Run tests to verify
5. When done with change-impl-agent-spec, run `cclab sdd workflow create-change-implementation change-impl-agent` to advance

## CLI Commands

```
# Read spec
Read file: cclab/changes/change-impl-agent/specs/change-impl-agent-spec.md

# Advance implementation workflow
cclab sdd workflow create-change-implementation change-impl-agent
```