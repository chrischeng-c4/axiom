# Task: Fill Section 'scenarios' for Spec 'jet-remaining-spec' (Change 'jet-remaining')

1. Read spec: `cclab/changes/jet-remaining/specs/jet-remaining-spec.md`
2. Write content for **scenarios**: Write scenarios in Given/When/Then format.
3. Call artifact tool with section="scenarios" and your content.