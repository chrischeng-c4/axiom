# Task: Fill Section 'changes' for Spec 'restructure-agent-spec' (Change 'restructure-agent')

**DO NOT use Write or Edit tools to modify spec files directly. Use the artifact CLI only.**

1. Read spec: `cclab/changes/restructure-agent/specs/restructure-agent-spec.md`
2. Write content for **changes**: List files that will be changed.
3. Write payload JSON then run: `cclab sdd artifact create-change-spec restructure-agent cclab/changes/restructure-agent/payloads/create-change-spec.json`