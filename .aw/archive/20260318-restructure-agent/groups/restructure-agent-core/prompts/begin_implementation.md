# Task: Begin Implementation for Change 'restructure-agent'

## Instructions

1. List all change specs in `cclab/changes/restructure-agent/specs/`
2. Read spec **restructure-agent-spec** to understand requirements
3. Implement code for each change spec in order, starting with **restructure-agent-spec**
4. Run tests to verify
5. When done with restructure-agent-spec, run `cclab sdd workflow create-change-implementation restructure-agent` to advance

## CLI Commands

```
# Read spec
Read file: cclab/changes/restructure-agent/specs/restructure-agent-spec.md

# Advance implementation workflow
cclab sdd workflow create-change-implementation restructure-agent
```