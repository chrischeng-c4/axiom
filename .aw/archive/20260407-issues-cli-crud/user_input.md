Implement full CRUD + search for score issues CLI (#1179). Agent-first design: every verb outputs --json, no interactive prompts, structured errors. Verbs: create (local draft + GitHub push), update (labels/state/title/body), close (with reason), find (full-text search across local + GitHub). Cross-artifact references: frontmatter gains related:/implements: fields linking issues to changes/specs/BRDs. GitLab backend stub becomes real (glab shell-out). All backends implement the full IssueBackend trait uniformly.