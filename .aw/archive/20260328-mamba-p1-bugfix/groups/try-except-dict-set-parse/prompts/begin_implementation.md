# Task: Begin Implementation for Change 'mamba-p1-bugfix'

## Instructions

1. List all change specs in `cclab/changes/mamba-p1-bugfix/`
2. Read spec **statements** to understand requirements: `cclab/changes/mamba-p1-bugfix/groups/try-except-dict-set-parse/specs/statements.md`
3. Implement **production code only** (no `#[test]` functions) for each change spec in order, starting with **statements**
4. When done with statements, run `cclab sdd workflow create-change-implementation mamba-p1-bugfix` to advance

## CLI Commands

```
# Read spec
Read file: cclab/changes/mamba-p1-bugfix/groups/try-except-dict-set-parse/specs/statements.md

# Advance implementation workflow
cclab sdd workflow create-change-implementation mamba-p1-bugfix
```