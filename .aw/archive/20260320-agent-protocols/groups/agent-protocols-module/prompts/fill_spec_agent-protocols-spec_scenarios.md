# Task: Fill Section 'scenarios' for Spec 'agent-protocols-spec' (Change 'agent-protocols')

1. Read spec: `cclab/changes/agent-protocols/specs/agent-protocols-spec.md`
2. Write content for **scenarios**: Write scenarios in Given/When/Then format.
3. Call artifact tool with section="scenarios" and your content.