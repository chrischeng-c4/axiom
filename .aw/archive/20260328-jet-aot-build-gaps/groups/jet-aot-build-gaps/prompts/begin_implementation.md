# Task: Begin Implementation for Change 'jet-aot-build-gaps'

## Instructions

1. List all change specs in `cclab/changes/jet-aot-build-gaps/`
2. Read spec **jet-aot-build-gaps-spec** to understand requirements: `cclab/changes/jet-aot-build-gaps/groups/jet-aot-build-gaps/specs/jet-aot-build-gaps-spec.md`
3. Implement **production code only** (no `#[test]` functions) for each change spec in order, starting with **jet-aot-build-gaps-spec**
4. When done with jet-aot-build-gaps-spec, run `cclab sdd workflow create-change-implementation jet-aot-build-gaps` to advance

## CLI Commands

```
# Read spec
Read file: cclab/changes/jet-aot-build-gaps/groups/jet-aot-build-gaps/specs/jet-aot-build-gaps-spec.md

# Advance implementation workflow
cclab sdd workflow create-change-implementation jet-aot-build-gaps
```