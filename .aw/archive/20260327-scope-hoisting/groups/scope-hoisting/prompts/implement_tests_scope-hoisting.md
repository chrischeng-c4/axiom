# Task: Implement Tests for Spec 'scope-hoisting' (Change 'scope-hoisting')

## Instructions

Production code for spec 'scope-hoisting' has been implemented and verified to compile.
Now implement **test functions only** (`#[test]` functions / `#[cfg(test)]` blocks).

1. Read spec **scope-hoisting**: `cclab/changes/scope-hoisting/groups/scope-hoisting/specs/scope-hoisting.md`
2. Read the `## Test Plan` section to understand required test cases
3. Implement `#[test]` functions (in `#[cfg(test)]` blocks) that cover the test plan
4. Run `cargo test` to verify tests pass
5. When done, run `cclab sdd workflow create-change-implementation scope-hoisting` to advance

## CLI Commands

```
# Read spec
Read file: cclab/changes/scope-hoisting/groups/scope-hoisting/specs/scope-hoisting.md

# Run tests
cargo test

# Advance implementation workflow
cclab sdd workflow create-change-implementation scope-hoisting
```