Enhanced changes section with function/type-level targets (function, struct, enum, trait, impl, method) + lens-assisted implementation prompt for existing file modifications. Level 1: changes YAML schema adds targets array with {type, name, change, anchor, position} and do_not_touch list. Level 2: implementation prompt builder uses cclab-lens AST to extract target code, mark insertion points, and list protected functions.