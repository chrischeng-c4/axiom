{
  "groups": [
    {
      "id": "test-config",
      "issues": [],
      "requirements": "PR1 — Add [sdd.test] config parsing to SddConfig (data only, no workflow logic):\n\n1. Add TestConfig struct with optional `setup`, `teardown` commands and a `scope` vec of TestScope entries to `crates/sdd/src/models/change.rs`.\n2. Add TestScope struct with fields: `name` (string), `changes` (Vec<String> — GitLab CI-style glob patterns matching file paths), `test_cmd` (string — command to run tests), optional `setup` and `teardown` overrides.\n3. Add `test: Option<TestConfig>` field to SddConfig, mapped from `[sdd.test]` TOML section.\n4. Update SddConfig::load() to deserialize `[sdd.test]` and `[[sdd.test.scope]]` entries.\n5. Add test scope entries in `.score/config.toml` for conductor (changes: [\"projects/conductor/**\"]) and cclab-queue (changes: [\"crates/cclab-queue/**\"]).\n6. Acceptance: config round-trips through TOML serialize/deserialize, glob patterns in `changes` are stored as strings (matching deferred to PR2).",
      "questions": [
        "Should TestConfig support a global `test_cmd` default that scopes inherit if they don't specify their own?",
        "Should the `changes` patterns use gitignore-style globs (like GitLab CI) or standard Unix globs?"
      ]
    },
    {
      "id": "tdd-workflow-gate",
      "issues": [],
      "requirements": "PR2 — TDD workflow gate enforcement + test infrastructure (depends on test-config from PR1):\n\n1. Gate 1 — Requirement coverage check: Parse Mermaid requirementDiagram blocks from spec files, extract requirement IDs, scan test files for matching requirement markers (e.g. comments or test names referencing the requirement ID). Reject advancement to review if any requirement lacks a corresponding test marker.\n2. Gate 2 — Test execution gate: Match changed files (from git diff) against `changes` glob patterns in TestScope entries. For each matched scope, run `setup` (if any), then `test_cmd`, then `teardown` (if any). Reject advancement if any test command fails (non-zero exit).\n3. Integrate both gates into the SDD workflow state machine — insert a test phase between implementation-complete and review. Implementation without tests is rejected; failing tests are rejected; only passing tests with full requirement coverage advance to review.\n4. Update `sdd-change-implementation.md` agent prompt with TDD instructions: agent must write tests alongside implementation, tests must reference requirement IDs from the spec.\n5. Create `projects/conductor/scripts/test-env.sh` — setup/teardown script for conductor test environment (start mock server, seed data, etc.).\n6. Acceptance: implementation without tests is rejected at the gate, failing tests are rejected, passing tests with full requirement coverage advance to review.",
      "questions": [
        "What format should requirement markers in test files use — comments like `// REQ: REQ-001` or test function naming convention like `test_req_001_*`?",
        "Should the gate be a hard block (cannot advance at all) or a soft warning that can be overridden with a flag?",
        "For Gate 2 glob matching, should it use the same glob library as .gitignore or a simpler glob crate?"
      ]
    }
  ]
}
