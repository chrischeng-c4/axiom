---
number: 655
title: "Add native stdlib: importlib — import machinery"
state: open
labels: [enhancement, P1, crate:mamba]
group: "stdlib-system"
---

# #655 — Add native stdlib: importlib — import machinery

Implement `importlib` module: `import_module()`, `reload()`, `abc.Loader`, `resources` submodule.
