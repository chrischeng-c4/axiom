---
number: 662
title: "Add native stdlib: urllib — URL handling modules"
state: open
labels: [enhancement, P1, crate:mamba]
group: "stdlib-io-networking"
---

# #662 — Add native stdlib: urllib — URL handling modules

Implement `urllib.parse` (urlparse, urlencode, quote, unquote), `urllib.request` (urlopen, Request), `urllib.error` (URLError, HTTPError).
