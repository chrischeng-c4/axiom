---
number: 656
title: "Add native stdlib: codecs — codec registry and base classes"
state: open
labels: [enhancement, P1, crate:mamba]
group: "stdlib-system"
---

# #656 — Add native stdlib: codecs — codec registry and base classes

Implement `codecs` module: `encode()`, `decode()`, `lookup()`, `register()`, UTF-8/ASCII/Latin-1 codecs.
