---
number: 653
title: "Add native stdlib: gc — garbage collector interface"
state: open
labels: [enhancement, P1, crate:mamba]
group: "stdlib-system"
---

# #653 — Add native stdlib: gc — garbage collector interface

Implement `gc` module with `collect()`, `disable()`, `enable()`, `get_count()`, `get_threshold()` integrated with Mamba's runtime.
