# Task: Implement Tests for Spec 'mamba-conformance-p1-spec' (Change 'mamba-conformance-p1')

## Instructions

Production code for spec 'mamba-conformance-p1-spec' has been implemented and verified to compile.
Now implement **test functions only** (`#[test]` functions / `#[cfg(test)]` blocks).

1. Read spec **mamba-conformance-p1-spec**: `cclab/changes/mamba-conformance-p1/groups/mamba-p1-oop/specs/mamba-conformance-p1-spec.md`
2. Read the `## Test Plan` section to understand required test cases
3. Implement `#[test]` functions (in `#[cfg(test)]` blocks) that cover the test plan
4. Run `cargo test` to verify tests pass
5. When done, run `cclab sdd workflow create-change-implementation mamba-conformance-p1` to advance

## CLI Commands

```
# Read spec
Read file: cclab/changes/mamba-conformance-p1/groups/mamba-p1-oop/specs/mamba-conformance-p1-spec.md

# Run tests
cargo test

# Advance implementation workflow
cclab sdd workflow create-change-implementation mamba-conformance-p1
```