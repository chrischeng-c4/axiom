Complete the scheduler runtime: (1) Push receiver HTTP endpoint for Cloud Scheduler and K8s CronJob callbacks — receives scheduled trigger, validates OIDC/auth, calls broker.publish(). Shared endpoint for both external schedulers. (2) K8sCronJobBackend implementing SchedulerBackend trait — CRUD K8s CronJobs via K8s API, reuse existing executor/k8s.rs infrastructure. Feature-gated as k8s-scheduler. (3) Schedule monitor with expected_at vs actual_at tracking, configurable leeway duration, missed/late/on-time status detection, emits metrics (counter + histogram) via existing metrics infrastructure. (4) PeriodicScheduler::start() mode selection — self-hosted backends start tick loop, cloud/k8s backends start HTTP push receiver server. (5) Full upstream/downstream integration design: upstream = how each scheduler type registers tasks and receives triggers; downstream = how triggers flow through broker.publish() to workers, how monitoring integrates with existing metrics/tracing, how the push receiver authenticates requests from Cloud Scheduler (OIDC) vs K8s (ServiceAccount tokens).