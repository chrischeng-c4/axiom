pnpm feature parity for jet package manager: workspace support (jet-workspace.yaml, --filter, cross-package linking), frozen lockfile (--frozen-lockfile for CI), .npmrc support (registry auth, scoped registries), optional dependencies (platform-specific), overrides/resolutions, update command, content-addressable dedup, store GC (jet store prune), lockfile import (from package-lock.json/yarn.lock), alias deps (npm: protocol), patching (jet patch), audit (npm advisory API), workspace protocol (workspace:*), recursive commands (jet -r), hoisting strategies, catalog, deploy pruning, publishing (jet publish/pack)