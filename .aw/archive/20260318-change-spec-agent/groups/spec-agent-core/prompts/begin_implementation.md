# Task: Begin Implementation for Change 'change-spec-agent'

## Instructions

1. List all change specs in `cclab/changes/change-spec-agent/specs/`
2. Read spec **change-spec-agent-spec** to understand requirements
3. Implement code for each change spec in order, starting with **change-spec-agent-spec**
4. Run tests to verify
5. When done with change-spec-agent-spec, run `cclab sdd workflow create-change-implementation change-spec-agent` to advance

## CLI Commands

```
# Read spec
Read file: cclab/changes/change-spec-agent/specs/change-spec-agent-spec.md

# Advance implementation workflow
cclab sdd workflow create-change-implementation change-spec-agent
```