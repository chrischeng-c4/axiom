# Task: Implement Tests for Spec 'agent-context-builder' (Change 'lens-dissolution')

## Instructions

Production code for spec 'agent-context-builder' has been implemented and verified to compile.
Now implement **test functions only** (`#[test]` functions / `#[cfg(test)]` blocks).

1. Read spec **agent-context-builder**: `cclab/changes/lens-dissolution/groups/lens-dissolution/specs/agent-context-builder.md`
2. Read the `## Test Plan` section to understand required test cases
3. Implement `#[test]` functions (in `#[cfg(test)]` blocks) that cover the test plan
4. Run `cargo test` to verify tests pass
5. When done, run `cclab sdd workflow create-change-implementation lens-dissolution` to advance

## CLI Commands

```
# Read spec
Read file: cclab/changes/lens-dissolution/groups/lens-dissolution/specs/agent-context-builder.md

# Run tests
cargo test

# Advance implementation workflow
cclab sdd workflow create-change-implementation lens-dissolution
```