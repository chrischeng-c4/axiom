# Task: Begin Implementation for Change 'mangle-module-scope'

## Instructions

1. List all change specs in `cclab/changes/mangle-module-scope/specs/`
2. Read spec **mangle-module-scope-spec** to understand requirements
3. Implement code for each change spec in order, starting with **mangle-module-scope-spec**
4. Run tests to verify
5. When done with mangle-module-scope-spec, run `cclab sdd workflow create-change-implementation mangle-module-scope` to advance

## CLI Commands

```
# Read spec
Read file: cclab/changes/mangle-module-scope/specs/mangle-module-scope-spec.md

# Advance implementation workflow
cclab sdd workflow create-change-implementation mangle-module-scope
```