# Task: Begin Implementation for Change 'jet-workspace-protocol'

## Instructions

1. List all change specs in `cclab/changes/jet-workspace-protocol/`
2. Read spec **jet-workspace-protocol-spec** to understand requirements: `cclab/changes/jet-workspace-protocol/groups/jet-workspace-protocol/specs/jet-workspace-protocol-spec.md`
3. Implement **production code only** (no `#[test]` functions) for each change spec in order, starting with **jet-workspace-protocol-spec**
4. When done with jet-workspace-protocol-spec, run `cclab sdd workflow create-change-implementation jet-workspace-protocol` to advance

## CLI Commands

```
# Read spec
Read file: cclab/changes/jet-workspace-protocol/groups/jet-workspace-protocol/specs/jet-workspace-protocol-spec.md

# Advance implementation workflow
cclab sdd workflow create-change-implementation jet-workspace-protocol
```