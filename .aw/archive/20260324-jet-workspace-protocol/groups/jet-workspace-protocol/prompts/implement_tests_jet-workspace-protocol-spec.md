# Task: Implement Tests for Spec 'jet-workspace-protocol-spec' (Change 'jet-workspace-protocol')

## Instructions

Production code for spec 'jet-workspace-protocol-spec' has been implemented and verified to compile.
Now implement **test functions only** (`#[test]` functions / `#[cfg(test)]` blocks).

1. Read spec **jet-workspace-protocol-spec**: `cclab/changes/jet-workspace-protocol/groups/jet-workspace-protocol/specs/jet-workspace-protocol-spec.md`
2. Read the `## Test Plan` section to understand required test cases
3. Implement `#[test]` functions (in `#[cfg(test)]` blocks) that cover the test plan
4. Run `cargo test` to verify tests pass
5. When done, run `cclab sdd workflow create-change-implementation jet-workspace-protocol` to advance

## CLI Commands

```
# Read spec
Read file: cclab/changes/jet-workspace-protocol/groups/jet-workspace-protocol/specs/jet-workspace-protocol-spec.md

# Run tests
cargo test

# Advance implementation workflow
cclab sdd workflow create-change-implementation jet-workspace-protocol
```