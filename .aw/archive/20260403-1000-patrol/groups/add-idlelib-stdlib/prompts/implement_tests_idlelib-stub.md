# Task: Implement Tests for Spec 'idlelib-stub' (Change '1000-patrol')

## Instructions

Production code for spec 'idlelib-stub' has been implemented and verified to compile.
Now implement **test functions only** (`#[test]` functions / `#[cfg(test)]` blocks).

1. Read spec **idlelib-stub**: `cclab/changes/1000-patrol/groups/add-idlelib-stdlib/specs/idlelib-stub.md`
2. Read the `## Test Plan` section to understand required test cases
3. Implement `#[test]` functions (in `#[cfg(test)]` blocks) that cover the test plan
4. Run `cargo test` to verify tests pass
5. When done, run `cclab sdd workflow create-change-implementation 1000-patrol` to advance

## CLI Commands

```
# Read spec
Read file: cclab/changes/1000-patrol/groups/add-idlelib-stdlib/specs/idlelib-stub.md

# Run tests
cargo test

# Advance implementation workflow
cclab sdd workflow create-change-implementation 1000-patrol
```