[project]
name = "service-http"
cap_path = "README.md"
label = "lib:service-http"

[[workspaces]]
name = "service-http"
paths = ["**"]
target = "rust"
test_cmd = "cargo test -p service-http"
verify_cold = true
