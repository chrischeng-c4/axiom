# HANDWRITE-BEGIN gap="missing-generator:ui-runtime-manifest" tracker="pending-tracker" reason="Shared component runtime extracted from jet-wasm so desktop and WASM renderers reuse the same hooks/mount/flush layer."
[package]
name = "cclab-ui-runtime"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Renderer-neutral component runtime: hooks, fiber storage, mount, flush, and update scheduling over cclab-surface elements"

[features]
default = []
debug = ["serde_json"]

[dependencies]
cclab-surface = { path = "../surface" }
serde_json = { version = "1", optional = true }
# HANDWRITE-END
