# SPEC-MANAGED: libs/ui-runtime/tech-design/semantic/source/libs-ui-runtime-cargo-toml.md#text-source-unit
# CODEGEN-BEGIN
[package]
name = "cclab-ui-runtime"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Renderer-neutral component runtime: hooks, fiber storage, mount, flush, and update scheduling over cclab-surface elements"

[features]
default = []
debug = ["serde_json"]

[dependencies]
cclab-surface = { path = "../surface" }
serde_json = { version = "1", optional = true }
# CODEGEN-END
