[project]
name = "raft-runtime"
cap_path = "README.md"
label = "lib:raft-runtime"

[[workspaces]]
name = "raft-runtime"
paths = ["**"]
target = "rust"
test_cmd = "cargo test -p raft-runtime"
verify_cold = true
