[package]
name = "build-stamp"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Shared build.rs stamping for service CLIs — git short-sha, built-at epoch, and target triple as `cargo:rustc-env=<PREFIX>_*` directives. Consumed as a [build-dependencies] crate."

[lib]
name = "build_stamp"
path = "src/lib.rs"
