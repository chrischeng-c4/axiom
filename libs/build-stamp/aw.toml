[project]
name = "build-stamp"
cap_path = "README.md"
label = "lib:build-stamp"

[[workspaces]]
name = "build-stamp"
paths = ["**"]
target = "rust"
test_cmd = "cargo test -p build-stamp"
verify_cold = true
