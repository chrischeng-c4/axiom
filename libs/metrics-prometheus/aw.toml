[project]
name = "metrics-prometheus"
cap_path = "README.md"
label = "lib:metrics-prometheus"

[[workspaces]]
name = "metrics-prometheus"
paths = ["**"]
target = "rust"
test_cmd = "cargo test -p metrics-prometheus"
verify_cold = true
