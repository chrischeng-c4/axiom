[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "metrics-prometheus-tech-design"
version = "0.1.0"
description = "DDD-layered full-typed Python tech design for metrics-prometheus"
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/metrics_prometheus"]
