[package]
name = "server-tcp"
version = "0.1.0"
edition = "2021"
description = "Shared TCP protocol runtime built on server-lifecycle: accept loop, per-connection supervision, admission budgeting, drain-aware shutdown, and handler traits."

[lib]
name = "server_tcp"
path = "src/lib.rs"

[dependencies]
server-lifecycle = { path = "../server-lifecycle" }
tokio = { workspace = true }
tracing = { workspace = true }
anyhow = { workspace = true }
socket2 = "0.6"

<!-- marker: missing-generator:logic:fc16524e path: libs/server-tcp/Cargo.toml reason: scaffold for libs/server-tcp/Cargo.toml — fill in by hand and update tracker when codegen is ready -->
