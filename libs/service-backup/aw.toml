[project]
name = "service-backup"
cap_path = "README.md"
label = "lib:service-backup"

[[workspaces]]
name = "service-backup"
paths = ["**"]
target = "rust"
test_cmd = "cargo test -p service-backup"
verify_cold = true
