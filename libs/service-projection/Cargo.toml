[package]
name = "service-projection"
version = "0.1.0"
edition = "2021"
description = "Typed projection registry, checkpoints, catch-up, rebuild, and flush runtime."
autotests = false

[lib]
name = "service_projection"
path = "src/lib.rs"

[dependencies]
anyhow = { workspace = true }
base64 = "0.22"
chrono = { workspace = true }
hex = "0.4"
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = "0.10"
storage-durable = { path = "../storage-durable" }
tokio = { workspace = true }
utoipa = { workspace = true }

[dev-dependencies]
tempfile = "3"

[[test]]
name = "typed_projection_runtime"
path = "e2e/typed_projection_runtime.rs"
