[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "raft-core-tech-design"
version = "0.1.0"
description = "DDD-layered full-typed Python tech design for raft-core"
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/raft_core"]
