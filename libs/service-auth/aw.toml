[project]
name = "service-auth"
cap_path = "README.md"
label = "lib:service-auth"

[[workspaces]]
name = "service-auth"
paths = ["**"]
target = "rust"
test_cmd = "cargo test -p service-auth"
verify_cold = true

# There is no external-contract inventory. The TD/EC retirement deleted this
# crate's external-contracts/ tree, and with it the pyproject.toml that
# [tool.aw.python-ec] was read from. The AW-EC-BEGIN/END block that used to sit here
# generated one case from a markdown TD that no longer exists; the Rust suite it
# named is still run by test_cmd above.
