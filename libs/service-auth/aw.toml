[project]
name = "service-auth"
cap_path = "README.md"
label = "lib:service-auth"

[[workspaces]]
name = "service-auth"
paths = ["**"]
target = "rust"
test_cmd = "cargo test -p service-auth"
verify_cold = true

# The external-contract inventory lives in
# external-contracts/pyproject.toml under [tool.aw.python-ec], and is verified
# by scripts/td-ec-verify.py. The AW-EC-BEGIN/END block that used to sit here
# generated one case from a markdown TD that no longer exists; the Rust suite it
# named is still run by test_cmd above.
