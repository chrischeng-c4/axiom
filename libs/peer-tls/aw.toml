[project]
name = "peer-tls"
cap_path = "CAPABILITIES.md"
label = "lib:peer-tls"

[[workspaces]]
name = "peer-tls"
paths = ["**"]
target = "rust"
test_cmd = "cargo test -p peer-tls"
verify_cold = true
