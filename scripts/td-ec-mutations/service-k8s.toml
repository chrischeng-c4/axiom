# Seeded defects for `scripts/td-ec-verify.py --mutations`.
#
# Controller-owned. This file is deliberately kept OUTSIDE `libs/service-k8s/`
# so it is never part of the material handed to an implementation worker: a
# worker that can read the seeded defects can special-case them instead of
# writing a contract that catches them on principle.
#
# Each mutation rewrites one anchor in
# `libs/service-k8s/tech-design/src/service_k8s/` into a plausible wrong
# implementation. At least one EC case must exit nonzero for every one of them.
# A mutation that no case notices is a hole in the EC, never a licence to
# delete the mutation.
#
# `old` must be a unique substring of the named file; the harness restores the
# original bytes after each run.
#
# This suite grows one round at a time, in step with the case inventory. Every
# mutation listed here is covered by a case that has already landed.

# ---------------------------------------------------------------------------
# certificate identity — the tenancy boundary in the name channels
# ---------------------------------------------------------------------------

[[mutation]]
id = "namespace-check-is-a-prefix-match"
intent = "the namespace test degrades from the `.<ns>.svc` segment Kubernetes uses to a bare prefix, so `lumen.lumen-prod.svc.cluster.local` is waved through for namespace `lumen`"
file = "service_k8s/domain/profile.py"
old = "            if namespaced not in name:"
new = "            if not name.startswith(self.scope.namespace):"

[[mutation]]
id = "public-suffix-check-dropped"
intent = "a name outside the cluster-internal suffixes stops being refused as public, so the refusal an operator reads names the wrong channel"
file = "service_k8s/domain/profile.py"
old = """            if not any(name.endswith(suffix) for suffix in CLUSTER_SUFFIXES):
                raise PublicDnsName(name)"""
new = """            if False:
                raise PublicDnsName(name)"""

[[mutation]]
id = "empty-name-set-accepted"
intent = "a profile requesting no names at all becomes constructible, so a certificate is issued for nothing in particular"
file = "service_k8s/domain/profile.py"
old = """        if not self.identity.dns_names:
            raise NoNames()"""
new = """        if False:
            raise NoNames()"""

[[mutation]]
id = "peer-may-omit-its-spiffe-uri"
intent = "a peer profile becomes constructible without a SPIFFE URI, leaving two members of one headless Service indistinguishable from each other's point of view"
file = "service_k8s/domain/profile.py"
old = "        if self.purpose is Purpose.PEER and self.identity.spiffe_uri is None:"
new = "        if False:"

[[mutation]]
id = "foreign-spiffe-uri-accepted"
intent = "the SPIFFE prefix check is dropped, so a profile can name another tenant's identity"
file = "service_k8s/domain/profile.py"
old = "            if not self.identity.spiffe_uri.startswith(prefix):"
new = "            if False:"

[[mutation]]
id = "lifetime-bounds-dropped"
intent = "lifetime bounds stop being stated locally, so an out-of-range lifetime is learned from a rejected CSR instead"
file = "service_k8s/domain/profile.py"
old = "        if not (MIN_LIFETIME_SECS <= self.lifetime_secs <= MAX_LIFETIME_SECS):"
new = "        if False:"

[[mutation]]
id = "renew-floor-dropped"
intent = "the renewal window loses its floor, so a window with no room to retry is accepted"
file = "service_k8s/domain/profile.py"
old = "        if self.renew_before_secs < MIN_RENEW_BEFORE_SECS:"
new = "        if False:"

[[mutation]]
id = "renew-window-may-equal-the-lifetime"
intent = "the window bound relaxes from strict to non-strict, so a certificate is due for renewal the instant it is issued"
file = "service_k8s/domain/profile.py"
old = "        if self.renew_before_secs >= self.lifetime_secs:"
new = "        if self.renew_before_secs > self.lifetime_secs:"

[[mutation]]
id = "jitter-bound-dropped"
intent = "jitter stops being bounded by the window it spreads, so a jittered deadline can land after expiry"
file = "service_k8s/domain/profile.py"
old = "        if self.renew_jitter_secs > self.renew_before_secs:"
new = "        if False:"

# ---------------------------------------------------------------------------
# certificate identity — the digest and the derived names
# ---------------------------------------------------------------------------

[[mutation]]
id = "digest-drops-the-dns-names"
intent = "the digest stops covering the names, so renaming a service does not reissue its certificate"
file = "service_k8s/domain/profile.py"
old = '            "dns=" + ",".join(sorted(self.identity.dns_names)),'
new = '            "dns=",'

[[mutation]]
id = "digest-is-order-sensitive"
intent = "the digest stops sorting the names, so re-ordering an unchanged name list throws away a valid identity"
file = "service_k8s/domain/profile.py"
old = "sorted(self.identity.dns_names)"
new = "self.identity.dns_names"

[[mutation]]
id = "digest-includes-the-lifetime"
intent = "the digest covers the renewal cadence as well as the certified content, so changing the cadence reissues a perfectly valid identity"
file = "service_k8s/domain/profile.py"
old = '            "eku=" + ",".join(u.token for u in self.extended_key_usages()),'
new = '            "eku=" + ",".join(u.token for u in self.extended_key_usages()) + "|lifetime=" + str(self.lifetime_secs),'

[[mutation]]
id = "digest-covers-only-the-common-name"
intent = "the digest collapses to the common name, so every name, usage, and URI change becomes invisible to rotation"
file = "service_k8s/domain/profile.py"
old = '        return hex_sha256("|".join(parts).encode("utf-8"))'
new = "        return hex_sha256(self.common_name.encode('utf-8'))"

[[mutation]]
id = "serving-also-carries-client-auth"
intent = "a serving leaf gains clientAuth, becoming a credential its holder can replay to authenticate as the service to its own peers"
file = "service_k8s/domain/purpose.py"
old = "            return (ExtendedUsage.SERVER_AUTH,)"
new = "            return (ExtendedUsage.SERVER_AUTH, ExtendedUsage.CLIENT_AUTH)"

[[mutation]]
id = "peer-drops-client-auth"
intent = "a peer leaf loses the outward direction, so mutual authentication silently degrades to one-way"
file = "service_k8s/domain/purpose.py"
old = "        return (ExtendedUsage.SERVER_AUTH, ExtendedUsage.CLIENT_AUTH)"
new = "        return (ExtendedUsage.SERVER_AUTH,)"

[[mutation]]
id = "secret-name-ignores-the-purpose"
intent = "serving and peer material collide in one Secret, so issuing one purpose overwrites the other"
file = "service_k8s/domain/scope.py"
old = '        return f"{self.instance}-{purpose.token}-tls"'
new = '        return f"{self.instance}-tls"'

[[mutation]]
id = "spiffe-prefix-omits-the-namespace"
intent = "the SPIFFE prefix stops carrying the namespace, so every namespace in the trust domain shares one identity space"
file = "service_k8s/domain/scope.py"
old = '        return f"spiffe://{self.trust_domain}/ns/{self.namespace}/"'
new = '        return f"spiffe://{self.trust_domain}/ns/"'

[[mutation]]
id = "scope-covers-any-instance-in-its-namespace"
intent = "scope containment widens from identity to namespace, so one instance's scope covers its neighbour's"
file = "service_k8s/domain/scope.py"
old = "        return self == other"
new = "        return self.namespace == other.namespace"

# ---------------------------------------------------------------------------
# rotation — the ladder
# ---------------------------------------------------------------------------

[[mutation]]
id = "untrusted-issuer-may-still-be-issued-from"
intent = "trust stops being the first rung, so a leaf is minted from an issuer nobody trusts and fails at handshake time on the far side"
file = "service_k8s/application/rotation.py"
old = "    if desired.issuer not in observed.trust_bundle:"
new = "    if False:"

[[mutation]]
id = "trust-publish-narrows-instead-of-widening"
intent = "the publish step stops being a superset of what is already published, so widening trust retires the outgoing anchor in the same move"
file = "service_k8s/application/rotation.py"
old = "        issuers = sorted(set(observed.trust_bundle) | {desired.issuer})"
new = "        issuers = sorted({desired.issuer})"

[[mutation]]
id = "bootstrap-reported-as-a-renewal"
intent = "the first issuance is indistinguishable from a routine one, so an operator cannot tell a cold start from a cadence"
file = "service_k8s/application/rotation.py"
old = "        return Issue(desired.issuer, IssueReason.BOOTSTRAP)"
new = "        return Issue(desired.issuer, IssueReason.RENEWAL)"

[[mutation]]
id = "expiry-reported-as-a-routine-renewal"
intent = "`Expired` collapses into `Renewal`, so the reason that means something was already wrong reads as the reason that means nothing was"
file = "service_k8s/application/rotation.py"
old = "        return Issue(desired.issuer, IssueReason.EXPIRED)"
new = "        return Issue(desired.issuer, IssueReason.RENEWAL)"

[[mutation]]
id = "issuer-rotation-not-detected"
intent = "a leaf from the previous issuer is never reissued, so an issuer rotation stalls at the overlap forever"
file = "service_k8s/application/rotation.py"
old = "    if observed.leaf.issuer != desired.issuer:"
new = "    if False:"

[[mutation]]
id = "identity-change-not-detected"
intent = "a leaf whose certified identity no longer matches the profile is kept, so a renamed service keeps serving its old name"
file = "service_k8s/application/rotation.py"
old = "    if observed.leaf.identity_digest != desired.profile.identity_digest():"
new = "    if False:"

[[mutation]]
id = "expiry-not-detected"
intent = "an already-expired leaf takes the ordinary renewal rung, hiding the fact that the window was missed"
file = "service_k8s/application/rotation.py"
old = "    if now >= observed.leaf.not_after:"
new = "    if False:"

[[mutation]]
id = "retirement-not-gated-on-observed-activation"
intent = "anchors are retired without the runtime reporting it is presenting the new leaf, so an issuer still authenticating live connections is dropped"
file = "service_k8s/application/rotation.py"
old = "        if observed.activated_fingerprint == observed.leaf.fingerprint:"
new = "        if True:"

# ---------------------------------------------------------------------------
# rotation — the deterministic instants
# ---------------------------------------------------------------------------

[[mutation]]
id = "renewal-jitter-is-constant"
intent = "jitter collapses to zero, so every certificate in a fleet comes due at exactly the same instant"
file = "service_k8s/application/rotation.py"
old = "    offset = _fingerprint_offset(leaf.fingerprint, profile.renew_jitter_secs)"
new = "    offset = 0"

[[mutation]]
id = "renewal-window-not-subtracted"
intent = "renewal is due at expiry rather than a window before it, so there is no room left to retry"
file = "service_k8s/application/rotation.py"
old = "    base = leaf.not_after - timedelta(seconds=profile.renew_before_secs)"
new = "    base = leaf.not_after"

[[mutation]]
id = "activation-recheck-is-immediate"
intent = "the recheck interval collapses to zero, turning a bounded wait into a hot loop against the API server"
file = "service_k8s/application/rotation.py"
old = "ACTIVATION_RECHECK: Final[timedelta] = timedelta(seconds=15)"
new = "ACTIVATION_RECHECK: Final[timedelta] = timedelta(seconds=0)"

[[mutation]]
id = "backoff-has-no-ceiling"
intent = "retry backoff climbs past five minutes, so a controller racing an expiry backs off past the window it is racing"
file = "service_k8s/application/rotation.py"
old = "    secs = min(RETRY_BASE_SECS * (2**shift), RETRY_CEILING_SECS)"
new = "    secs = RETRY_BASE_SECS * (2**shift)"

[[mutation]]
id = "backoff-starts-at-zero"
intent = "the first retry is immediate, so a failing issuer is hammered instead of backed off from"
file = "service_k8s/application/rotation.py"
old = "        return timedelta(seconds=RETRY_BASE_SECS)"
new = "        return timedelta(seconds=0)"
# ---------------------------------------------------------------------------
# whole-layer capacity planning — the per-shard arithmetic and its two ladders
# ---------------------------------------------------------------------------

[[mutation]]
id = "the-ratio-truncates-instead-of-rounding-up"
intent = "the HPA ratio floors instead of ceiling, so a layer that is 4.29 replicas short of its target is planned at 4 and never catches up"
file = "service_k8s/application/capacity.py"
old = "    return (numerator + denominator - 1) // denominator"
new = "    return numerator // denominator"

[[mutation]]
id = "the-two-axes-take-the-smaller"
intent = "cpu and memory are combined with min instead of max, so an idle memory reading cancels out real cpu pressure"
file = "service_k8s/application/capacity.py"
old = "        desired = max(candidates)"
new = "        desired = min(candidates)"

[[mutation]]
id = "a-metrics-gap-reads-as-idleness"
intent = "an absent signal collapses the layer to its floor instead of holding, so a broken metrics pipeline scales the workload down"
file = "service_k8s/application/capacity.py"
old = """    if observed.cpu_percent is None and observed.memory_percent is None:
        desired = current_replicas_per_shard"""
new = """    if observed.cpu_percent is None and observed.memory_percent is None:
        desired = policy.min_replicas_per_shard"""

[[mutation]]
id = "the-upper-replica-bound-is-not-applied"
intent = "the per-shard ceiling stops clamping, so a runaway utilization reading requests an unbounded layer"
file = "service_k8s/application/capacity.py"
old = """    desired = min(
        max(desired, policy.min_replicas_per_shard),
        policy.max_replicas_per_shard,
    )"""
new = "    desired = max(desired, policy.min_replicas_per_shard)"

[[mutation]]
id = "the-lower-replica-bound-is-not-applied"
intent = "the per-shard floor stops clamping, so a quiet period can plan fewer replicas than the quorum policy allows"
file = "service_k8s/application/capacity.py"
old = """    desired = min(
        max(desired, policy.min_replicas_per_shard),
        policy.max_replicas_per_shard,
    )
"""
new = """    desired = min(desired, policy.max_replicas_per_shard)
"""

[[mutation]]
id = "the-desired-total-is-not-a-whole-layer"
intent = "the desired total drops the shard multiplier, so the plan describes a StatefulSet size that does not exist for a sharded topology"
file = "service_k8s/application/capacity.py"
old = "    desired_total_pods = shard_count * desired"
new = "    desired_total_pods = desired"

[[mutation]]
id = "the-current-total-is-computed-from-the-desired-layer"
intent = "the current total is reported from the desired replica count, so a caller diffing current against desired never sees a change"
file = "service_k8s/application/capacity.py"
old = "    current_total_pods = shard_count * current_replicas_per_shard"
new = "    current_total_pods = shard_count * desired"

[[mutation]]
id = "every-plan-claims-a-membership-change"
intent = "the Raft precondition always reports true, so a caller performs a membership transition for a plan that changes nothing"
file = "service_k8s/application/capacity.py"
old = "        return self.current_replicas_per_shard != self.desired_replicas_per_shard"
new = "        return True"

[[mutation]]
id = "the-split-threshold-becomes-inclusive"
intent = "the strict comparison relaxes to >=, so a shard sitting at exactly one GiB is split"
file = "service_k8s/application/capacity.py"
old = "        and busiest.durable_bytes > policy.split_threshold_bytes"
new = "        and busiest.durable_bytes >= policy.split_threshold_bytes"

[[mutation]]
id = "a-split-adds-more-than-one-shard"
intent = "a split doubles the topology instead of adding one shard, so a single observation triggers a data movement the caller never planned"
file = "service_k8s/application/capacity.py"
old = "        desired = current_shard_count + 1"
new = "        desired = current_shard_count * 2"

[[mutation]]
id = "ties-choose-the-highest-shard-index"
intent = "the tie-break flips, so two equally loaded shards produce a different plan depending on observation order"
file = "service_k8s/application/capacity.py"
old = "        busiest = max(observed, key=lambda u: (u.durable_bytes, -u.shard_index))"
new = "        busiest = max(observed, key=lambda u: (u.durable_bytes, u.shard_index))"

[[mutation]]
id = "the-shard-ceiling-is-ignored"
intent = "max_shards stops gating the split, so the topology grows past the bound the operator configured"
file = "service_k8s/application/capacity.py"
old = "    if threshold_crossed and not max_shards_reached:"
new = "    if threshold_crossed:"

[[mutation]]
id = "an-empty-observation-invents-a-busiest-shard"
intent = "no observations produce a synthetic zero-byte shard, so the plan reports evidence that was never measured"
file = "service_k8s/application/capacity.py"
old = """    if not observed:
        busiest = None"""
new = """    if not observed:
        busiest = ObservedShardUsage(0, 0)"""

[[mutation]]
id = "a-blank-resource-request-is-taken-literally"
intent = "an empty request string stops falling back, so a service that left the field blank gets no request at all"
file = "service_k8s/application/capacity.py"
old = "    if not value.strip():"
new = "    if value is None:"

[[mutation]]
id = "a-resource-request-is-silently-stripped"
intent = "the caller's own value is rewritten on the way out, so what the service declared is not what the pod spec carries"
file = "service_k8s/application/capacity.py"
old = """        return default
    return value"""
new = """        return default
    return value.strip()"""

[[mutation]]
id = "the-replica-ladder-checks-the-shard-count-second"
intent = "the validation order flips, so a caller with two invalid inputs is told about the wrong one"
file = "service_k8s/application/capacity.py"
old = """    if shard_count == 0:
        raise ZeroShards()
    if current_replicas_per_shard == 0:
        raise ZeroCurrentReplicas()"""
new = """    if current_replicas_per_shard == 0:
        raise ZeroCurrentReplicas()
    if shard_count == 0:
        raise ZeroShards()"""

[[mutation]]
id = "the-utilization-target-loses-its-upper-bound"
intent = "only a zero target is refused, so a target above 100 percent silently plans a layer that can never reach it"
file = "service_k8s/application/capacity.py"
old = """    if not (1 <= policy.target_cpu_utilization <= 100) or not (
        1 <= policy.target_memory_utilization <= 100
    ):"""
new = """    if policy.target_cpu_utilization == 0 or (
        policy.target_memory_utilization == 0
    ):"""

[[mutation]]
id = "the-replica-overflow-check-is-dropped"
intent = "a pod total past u32 is planned instead of refused, so the value the API server receives has wrapped"
file = "service_k8s/application/capacity.py"
old = "    if current_total_pods > U32_MAX or desired_total_pods > U32_MAX:"
new = "    if False:"

[[mutation]]
id = "a-zero-minimum-is-tolerated"
intent = "a floor of zero is accepted, so a policy that can plan an empty shard passes validation"
file = "service_k8s/application/capacity.py"
old = "    if policy.min_replicas_per_shard == 0:"
new = "    if False:"

[[mutation]]
id = "inverted-replica-bounds-are-tolerated"
intent = "a maximum below the minimum is accepted, so the clamp silently resolves to the maximum and the floor is lost"
file = "service_k8s/application/capacity.py"
old = "    if policy.max_replicas_per_shard < policy.min_replicas_per_shard:"
new = "    if False:"

[[mutation]]
id = "a-replica-refusal-loses-its-reason"
intent = "the zero-shard refusal degrades to a generic message, so an operator reading the error cannot tell which input was wrong"
file = "service_k8s/application/capacity.py"
old = """        super().__init__("shard_count must be greater than zero")"""
new = """        super().__init__("invalid replica-layer input")"""

[[mutation]]
id = "the-split-planner-tolerates-a-zero-shard-count"
intent = "an empty topology is planned against instead of refused, so a split is computed for a workload that does not exist"
file = "service_k8s/application/capacity.py"
old = """    if current_shard_count == 0:
        raise ZeroShardCount()"""
new = """    if False:
        raise ZeroShardCount()"""

[[mutation]]
id = "a-zero-split-threshold-is-tolerated"
intent = "a zero threshold is accepted, so every observation crosses it and the topology grows on every reconcile"
file = "service_k8s/application/capacity.py"
old = "    if policy.split_threshold_bytes == 0:"
new = "    if False:"

[[mutation]]
id = "a-maximum-below-the-current-shard-count-is-tolerated"
intent = "a ceiling under the live topology is accepted, so the policy silently describes a shrink this library cannot perform"
file = "service_k8s/application/capacity.py"
old = """    if (
        policy.max_shards is not None
        and policy.max_shards < current_shard_count
    ):
        raise InvalidMaximum()"""
new = """    if False:
        raise InvalidMaximum()"""

[[mutation]]
id = "an-observation-outside-the-topology-is-tolerated"
intent = "a shard index the topology does not contain is planned against, so a stale or forged metric drives a real split"
file = "service_k8s/application/capacity.py"
old = "    if any(u.shard_index >= current_shard_count for u in observed):"
new = "    if False:"

[[mutation]]
id = "the-shard-overflow-check-is-dropped"
intent = "a shard count past u32 is planned instead of refused"
file = "service_k8s/application/capacity.py"
old = """        if desired > U32_MAX:
            raise ShardOverflow()"""
new = """        if False:
            raise ShardOverflow()"""

[[mutation]]
id = "a-split-refusal-loses-its-reason"
intent = "the unknown-shard refusal degrades to a generic message, so the operator cannot tell that the observation, not the policy, was wrong"
file = "service_k8s/application/capacity.py"
old = """        super().__init__("observed shard index is outside the current topology")"""
new = """        super().__init__("invalid shard-split input")"""

# ---------------------------------------------------------------------------
# clock-free condition projection — the sticky instant and the metav1 shape
# ---------------------------------------------------------------------------

[[mutation]]
id = "the-transition-instant-always-refreshes"
intent = "every reconcile stamps the injected instant, so the 30-second requeue looks like a state change to everything watching"
file = "service_k8s/domain/condition.py"
old = """        last_transition_time = (
            carried.last_transition_time if carried is not None else now
        )"""
new = "        last_transition_time = now"

[[mutation]]
id = "the-transition-instant-survives-a-status-flip"
intent = "the carry-forward matches on type alone, so a condition that flips keeps the instant from the state it just left"
file = "service_k8s/domain/condition.py"
old = """        carried = next(
            (c for c in prior if c.type_ == fact.type_ and c.status == status_token),
            None,
        )"""
new = """        carried = next(
            (c for c in prior if c.type_ == fact.type_),
            None,
        )"""

[[mutation]]
id = "the-reason-and-message-are-carried-forward-too"
intent = "the whole prior condition is carried instead of only its instant, so a stale reason and message outlive the state that produced them"
file = "service_k8s/domain/condition.py"
old = """                reason=fact.reason,
                message=fact.message,"""
new = """                reason=carried.reason if carried is not None else fact.reason,
                message=carried.message if carried is not None else fact.message,"""

[[mutation]]
id = "the-observed-generation-is-carried-from-the-prior"
intent = "observedGeneration comes from the prior object instead of the caller, so status permanently claims to reflect an older spec"
file = "service_k8s/domain/condition.py"
old = "                observed_generation=observed_generation,"
new = "                observed_generation=(carried.observed_generation if carried is not None else observed_generation),"

[[mutation]]
id = "a-dropped-condition-survives-the-projection"
intent = "conditions the service stopped reporting are appended back, so a resolved condition never leaves status"
file = "service_k8s/domain/condition.py"
old = "    return tuple(result)"
new = "    return tuple(result) + tuple(c for c in prior if c.type_ not in {f.type_ for f in facts})"

[[mutation]]
id = "the-projection-reorders-the-facts"
intent = "the emitted order is sorted rather than preserved, so a service cannot control which condition a tool reads first"
file = "service_k8s/domain/condition.py"
old = "    for fact in facts:"
new = "    for fact in sorted(facts, key=lambda f: f.type_):"

[[mutation]]
id = "the-projection-returns-a-mutable-list"
intent = "the projection hands back the working list, so a caller can edit published status in place"
file = "service_k8s/domain/condition.py"
old = "    return tuple(result)"
new = "    return result"

[[mutation]]
id = "the-status-token-is-lowercased"
intent = "the wire token stops being the Kubernetes spelling, so kubectl wait --for=condition never matches"
file = "service_k8s/domain/condition.py"
old = "        return self.value"
new = "        return self.value.lower()"

[[mutation]]
id = "a-boolean-readiness-can-become-unknown"
intent = "a false observation is published as Unknown, so a definite not-ready reads as no information"
file = "service_k8s/domain/condition.py"
old = """        if value:
            return ConditionStatus.TRUE
        return ConditionStatus.FALSE"""
new = """        if value:
            return ConditionStatus.TRUE
        return ConditionStatus.UNKNOWN"""

[[mutation]]
id = "the-status-enumeration-is-not-closed"
intent = "a fourth status spelling appears, so a value no controller-aware tool understands can be published"
file = "service_k8s/domain/condition.py"
old = """    UNKNOWN = "Unknown\""""
new = """    UNKNOWN = "Unknown"
    DEGRADED = "Degraded\""""

[[mutation]]
id = "the-serialized-key-is-the-python-attribute-name"
intent = "the JSON key leaks the trailing underscore, so the object is not a metav1 condition at all"
file = "service_k8s/domain/condition.py"
old = """            "type": self.type_,"""
new = """            "type_": self.type_,"""

[[mutation]]
id = "an-absent-observed-generation-is-serialized-as-null"
intent = "observedGeneration is always emitted, so a condition with no generation publishes a null the API server stores"
file = "service_k8s/domain/condition.py"
old = "        if self.observed_generation is not None:"
new = "        if True:"

[[mutation]]
id = "an-empty-message-is-dropped-from-the-serialized-shape"
intent = "an empty message is pruned rather than published, so the serialized object stops being the fixed metav1 shape"
file = "service_k8s/domain/condition.py"
old = "        return res"
new = """        if not self.message:
            del res["message"]
        return res"""

[[mutation]]
id = "the-injected-instant-is-truncated-to-second-precision"
intent = "the projection rewrites the caller's instant instead of carrying it through, so the one place a clock is read is no longer the only place time is decided"
file = "service_k8s/domain/condition.py"
old = "                last_transition_time=last_transition_time,"
new = """                last_transition_time=last_transition_time[:19] + "Z","""

[[mutation]]
id = "a-projected-condition-is-mutable"
intent = "published status becomes editable in place, so a later reconcile step can rewrite a condition after it was projected"
file = "service_k8s/domain/condition.py"
old = """@dataclass(frozen=True)
class Condition:"""
new = """@dataclass
class Condition:"""

[[mutation]]
id = "a-condition-fact-is-mutable"
intent = "the service's own emitted fact becomes editable, so the input to a deterministic projection is no longer fixed"
file = "service_k8s/domain/condition.py"
old = """@dataclass(frozen=True)
class ConditionFact:"""
new = """@dataclass
class ConditionFact:"""
# service-k8s round K seeded defects — owner-scoped material projection.
#
# Controller-authored. Never part of a worker's readable material: these are
# the answer key for whether the round-K cases observe the design or merely
# transcribe a table.

# ---------------------------------------------------------- trust bundle

[[mutation]]
id = "a-pem-block-loses-its-line-structure-when-reassembled"
intent = "split_pem_blocks joins a block's lines with nothing, so a round-tripped bundle no longer reproduces its own PEM"
file = "service_k8s/application/trust_bundle.py"
old = 'blocks.append("\n".join(current))'
new = 'blocks.append("".join(current))'

[[mutation]]
id = "anchors-are-ordered-newest-issuer-first"
intent = "with_anchor sorts descending, so insertion order stops being irrelevant"
file = "service_k8s/application/trust_bundle.py"
old = 'pairs.sort(key=lambda pair: pair[0])'
new = 'pairs.sort(key=lambda pair: pair[0], reverse=True)'

[[mutation]]
id = "re-adding-an-issuer-appends-a-second-anchor"
intent = "with_anchor stops evicting the existing entry for that issuer, so a bundle accumulates duplicates"
file = "service_k8s/application/trust_bundle.py"
old = 'pairs = [(i, p) for (i, p) in self.entries if i != issuer]'
new = 'pairs = [(i, p) for (i, p) in self.entries]'

[[mutation]]
id = "retaining-keeps-exactly-the-issuers-it-was-told-to-drop"
intent = "retaining inverts its filter, so retirement removes the wrong anchors"
file = "service_k8s/application/trust_bundle.py"
old = 'pairs = [(i, p) for (i, p) in self.entries if i in issuers_set]'
new = 'pairs = [(i, p) for (i, p) in self.entries if i not in issuers_set]'

[[mutation]]
id = "a-bundle-with-more-blocks-than-ids-is-guessed-at"
intent = "parse only rejects a shortfall, so surplus PEM blocks are silently paired with the ids it does have"
file = "service_k8s/application/trust_bundle.py"
old = 'if len(blocks) != len(ids):'
new = 'if len(blocks) < len(ids):'

[[mutation]]
id = "each-anchor-is-separated-by-a-blank-line"
intent = "to_pem emits an extra newline per anchor, changing the bytes consumers read"
file = "service_k8s/application/trust_bundle.py"
old = '''
        out += "\n"
'''
new = '''
        out += "\n\n"
'''

[[mutation]]
id = "the-bundle-annotation-uses-a-separator-parse-cannot-read"
intent = "annotation joins on a semicolon while parse splits on a comma, so the bundle never round-trips"
file = "service_k8s/application/trust_bundle.py"
old = 'return ",".join(issuer.value for issuer in self.issuers())'
new = 'return ";".join(issuer.value for issuer in self.issuers())'

[[mutation]]
id = "emptiness-is-inverted"
intent = "is_empty reports a populated bundle as empty and an empty one as populated"
file = "service_k8s/application/trust_bundle.py"
old = 'return len(self.entries) == 0'
new = 'return len(self.entries) > 0'

[[mutation]]
id = "membership-answers-for-any-other-issuer"
intent = "contains tests inequality, so a bundle claims to trust every issuer it does not hold"
file = "service_k8s/application/trust_bundle.py"
old = 'return any(i == issuer for (i, _) in self.entries)'
new = 'return any(i != issuer for (i, _) in self.entries)'

[[mutation]]
id = "the-issuer-list-comes-back-reversed"
intent = "issuers() reverses the sorted entries, so the annotation and the bundle disagree on order"
file = "service_k8s/application/trust_bundle.py"
old = 'return tuple(i for (i, _) in self.entries)'
new = 'return tuple(i for (i, _) in reversed(self.entries))'

[[mutation]]
id = "parse-pairs-each-id-with-the-wrong-anchor"
intent = "parse zips the ids against the blocks in reverse, so every anchor is attributed to the wrong issuer"
file = "service_k8s/application/trust_bundle.py"
old = 'for id_, block in zip(ids, blocks):'
new = 'for id_, block in zip(ids, tuple(reversed(blocks))):'

[[mutation]]
id = "a-pem-block-is-never-closed"
intent = "split_pem_blocks stops recognizing the END line, so no block is ever emitted"
file = "service_k8s/application/trust_bundle.py"
old = 'elif line.startswith("-----END"):'
new = 'elif line.startswith("-----ENDOF"):'

# ------------------------------------------------------------ projection

[[mutation]]
id = "the-owner-reference-is-not-the-controller"
intent = "controller drops to false, so Kubernetes stops treating the instance as the managing owner"
file = "service_k8s/infrastructure/projection.py"
old = '"controller": True,'
new = '"controller": False,'

[[mutation]]
id = "the-owner-reference-stops-blocking-deletion"
intent = "blockOwnerDeletion drops to false, so material can outlive the instance it belongs to"
file = "service_k8s/infrastructure/projection.py"
old = '"blockOwnerDeletion": True,'
new = '"blockOwnerDeletion": False,'

[[mutation]]
id = "the-owner-reference-carries-no-uid"
intent = "the uid is blanked, so garbage collection cannot resolve the owner"
file = "service_k8s/infrastructure/projection.py"
old = '"uid": self.uid,'
new = '"uid": "",'

[[mutation]]
id = "the-secret-type-requires-a-leaf-to-exist"
intent = "the Secret becomes kubernetes.io/tls, which cannot represent the trust-only bootstrap write"
file = "service_k8s/infrastructure/projection.py"
old = '"type": "Opaque",'
new = '"type": "kubernetes.io/tls",'

[[mutation]]
id = "the-component-label-stops-naming-tls"
intent = "the component label loses its -tls suffix, so selectors that target certificate secrets miss them"
file = "service_k8s/infrastructure/projection.py"
old = 'f"{purpose.token}-tls"'
new = 'f"{purpose.token}-certs"'

[[mutation]]
id = "the-material-claims-a-different-manager"
intent = "managed-by names something other than this controller, so ownership auditing points elsewhere"
file = "service_k8s/infrastructure/projection.py"
old = 'MANAGED_BY: Final[str] = "service-k8s"'
new = 'MANAGED_BY: Final[str] = "kubectl"'

[[mutation]]
id = "the-trust-only-write-carries-the-private-key"
intent = "the trust-only write gains a key entry, which is exactly the leak the split write exists to prevent"
file = "service_k8s/infrastructure/projection.py"
old = 'secret["stringData"] = {TRUST_BUNDLE_KEY: bundle.to_pem()}'
new = 'secret["stringData"] = {TRUST_BUNDLE_KEY: bundle.to_pem(), PRIVATE_KEY_KEY: "KEYPEM"}'

[[mutation]]
id = "the-certificate-key-is-renamed"
intent = "tls.crt is renamed, breaking the deployed consumer contract renewal must never change"
file = "service_k8s/infrastructure/projection.py"
old = 'CERT_KEY: Final[str] = "tls.crt"'
new = 'CERT_KEY: Final[str] = "tls.certificate"'

[[mutation]]
id = "the-private-key-key-is-renamed"
intent = "tls.key is renamed, so a pod mounting the secret finds no key"
file = "service_k8s/infrastructure/projection.py"
old = 'PRIVATE_KEY_KEY: Final[str] = "tls.key"'
new = 'PRIVATE_KEY_KEY: Final[str] = "tls.privatekey"'

[[mutation]]
id = "the-trust-bundle-key-is-renamed"
intent = "ca.crt is renamed, so trust is published where nothing reads it"
file = "service_k8s/infrastructure/projection.py"
old = 'TRUST_BUNDLE_KEY: Final[str] = "ca.crt"'
new = 'TRUST_BUNDLE_KEY: Final[str] = "ca.bundle"'

[[mutation]]
id = "the-bundle-annotation-key-drifts"
intent = "the write and the readback disagree on the annotation name, so every bundle parses as empty"
file = "service_k8s/infrastructure/projection.py"
old = 'TRUST_BUNDLE_ANNOTATION: Final[str] = "service-k8s.axiom.dev/trust-bundle"'
new = 'TRUST_BUNDLE_ANNOTATION: Final[str] = "service-k8s.axiom.dev/bundle"'

[[mutation]]
id = "the-leaf-issuer-annotation-key-drifts"
intent = "the leaf issuer claim is written under a name readback does not look for"
file = "service_k8s/infrastructure/projection.py"
old = 'LEAF_ISSUER_ANNOTATION: Final[str] = "service-k8s.axiom.dev/leaf-issuer"'
new = 'LEAF_ISSUER_ANNOTATION: Final[str] = "service-k8s.axiom.dev/issuer"'

[[mutation]]
id = "the-identity-digest-annotation-key-drifts"
intent = "the identity digest is written under a name readback does not look for"
file = "service_k8s/infrastructure/projection.py"
old = 'IDENTITY_DIGEST_ANNOTATION: Final[str] = "service-k8s.axiom.dev/identity-digest"'
new = 'IDENTITY_DIGEST_ANNOTATION: Final[str] = "service-k8s.axiom.dev/digest"'

[[mutation]]
id = "material-lands-in-a-fixed-namespace"
intent = "the Secret is written to default rather than the instance's own namespace"
file = "service_k8s/infrastructure/projection.py"
old = '"namespace": scope.namespace,'
new = '"namespace": "default",'

[[mutation]]
id = "the-leaf-fingerprint-is-not-taken-over-the-certificate"
intent = "parse_leaf digests nothing, so every certificate reports the same fingerprint"
file = "service_k8s/infrastructure/projection.py"
old = 'return LeafFacts(not_before, not_after, hex_sha256(der))'
new = 'return LeafFacts(not_before, not_after, hex_sha256(b""))'

[[mutation]]
id = "a-leaf-is-reconstructed-only-when-its-digest-claim-is-missing"
intent = "readback inverts the identity-digest precondition, so a complete secret yields no leaf"
file = "service_k8s/infrastructure/projection.py"
old = 'and identity_digest is not None'
new = 'and identity_digest is None'

[[mutation]]
id = "a-leaf-is-reconstructed-only-when-its-issuer-claim-is-missing"
intent = "readback inverts the issuer precondition, so a complete secret yields no leaf"
file = "service_k8s/infrastructure/projection.py"
old = 'and issuer is not None'
new = 'and issuer is None'

[[mutation]]
id = "the-bundle-is-parsed-only-when-its-bytes-are-undecodable"
intent = "readback inverts the decode guard, so a readable bundle is dropped"
file = "service_k8s/infrastructure/projection.py"
old = '''
        if text is not None:
'''
new = '''
        if text is None:
'''

[[mutation]]
id = "an-unreadable-leaf-propagates-instead-of-yielding-none"
intent = "readback stops catching LeafParseError, so corrupt bytes crash the reconcile rather than reporting no leaf"
file = "service_k8s/infrastructure/projection.py"
old = 'except (UnicodeDecodeError, LeafParseError):'
new = 'except UnicodeDecodeError:'

# ------------------------------------------------------------ scope / purpose / digest

[[mutation]]
id = "the-secret-name-leads-with-the-purpose"
intent = "secret_name reorders its parts, so every projected Secret lands under a name consumers do not mount"
file = "service_k8s/domain/scope.py"
old = 'return f"{self.instance}-{purpose.token}-tls"'
new = 'return f"{purpose.token}-{self.instance}-tls"'

[[mutation]]
id = "the-peer-purpose-token-is-pluralized"
intent = "the peer token changes, moving both the Secret name and the component label"
file = "service_k8s/domain/purpose.py"
old = 'PEER = "peer"'
new = 'PEER = "peers"'

[[mutation]]
id = "the-serving-purpose-token-is-abbreviated"
intent = "the serving token changes, moving both the Secret name and the component label"
file = "service_k8s/domain/purpose.py"
old = 'SERVING = "serving"'
new = 'SERVING = "serve"'

[[mutation]]
id = "the-digest-is-truncated-to-half-width"
intent = "hex_sha256 returns 32 characters, so a fingerprint stops being a full sha256"
file = "service_k8s/domain/digest.py"
old = 'return hashlib.sha256(data).hexdigest()'
new = 'return hashlib.sha256(data).hexdigest()[:32]'

# ---------------------------------------------------------------- status

[[mutation]]
id = "the-published-fingerprint-is-twice-as-long"
intent = "short_fingerprint publishes 32 characters, enough to be mistaken for an artifact"
file = "service_k8s/application/status.py"
old = 'return fingerprint[:16]'
new = 'return fingerprint[:32]'

[[mutation]]
id = "peer-conditions-collide-with-serving-conditions"
intent = "condition_prefix returns Serving for both purposes, so the two lifecycles overwrite each other"
file = "service_k8s/application/status.py"
old = '''
    return "Peer"
'''
new = '''
    return "Serving"
'''

[[mutation]]
id = "the-ready-condition-type-is-shortened"
intent = "the ready condition is renamed, so existing status readers stop finding it"
file = "service_k8s/application/status.py"
old = 'READY_CONDITION: Final[str] = "CertificateReady"'
new = 'READY_CONDITION: Final[str] = "Ready"'

[[mutation]]
id = "the-rotating-condition-type-is-shortened"
intent = "the rotating condition is renamed, so existing status readers stop finding it"
file = "service_k8s/application/status.py"
old = 'ROTATING_CONDITION: Final[str] = "CertificateRotating"'
new = 'ROTATING_CONDITION: Final[str] = "Rotating"'

[[mutation]]
id = "a-failing-lifecycle-stays-pending-until-the-fifth-attempt"
intent = "the failure threshold rises, so repeated issuance failures keep reporting Pending"
file = "service_k8s/application/status.py"
old = 'elif self.consecutive_failures > 0:'
new = 'elif self.consecutive_failures > 4:'

[[mutation]]
id = "the-ready-reason-is-renamed"
intent = "the ready reason changes, so a reason-matching alert stops firing"
file = "service_k8s/application/status.py"
old = 'reason = "Issued"'
new = 'reason = "Ready"'

[[mutation]]
id = "the-quiet-rotating-reason-is-renamed"
intent = "the not-rotating reason changes, so the steady state is reported under a different word"
file = "service_k8s/application/status.py"
old = 'rot_reason = "Stable"'
new = 'rot_reason = "Idle"'

[[mutation]]
id = "an-active-rotation-reports-itself-as-not-rotating"
intent = "the rotating condition is published false while a rotation is under way"
file = "service_k8s/application/status.py"
old = 'rot_status = ConditionStatus.TRUE'
new = 'rot_status = ConditionStatus.FALSE'

[[mutation]]
id = "the-ready-message-prints-the-issuer-wrapper-rather-than-its-value"
intent = "the message interpolates the IssuerId dataclass, so the published text stops being the issuer name"
file = "service_k8s/application/status.py"
old = 'parts.append(f"issuer {self.issuer.value}")'
new = 'parts.append(f"issuer {self.issuer}")'

[[mutation]]
id = "the-ready-message-separator-changes"
intent = "message parts are joined on a comma, colliding with the comma that separates trusted issuers"
file = "service_k8s/application/status.py"
old = 'return "; ".join(parts)'
new = 'return ", ".join(parts)'

[[mutation]]
id = "the-pem-placeholder-changes"
intent = "the redaction marker changes, so a status consumer matching on it stops recognizing a redaction"
file = "service_k8s/application/status.py"
old = 'out.append("[redacted pem]")'
new = 'out.append("[pem]")'

[[mutation]]
id = "a-bearer-token-is-published-with-its-value"
intent = "the bearer scrubber emits the token it was supposed to replace"
file = "service_k8s/application/status.py"
old = 'out.append("Bearer [redacted]")'
new = 'out.append("Bearer " + segment[:16])'

[[mutation]]
id = "a-jwt-shaped-string-passes-through"
intent = "the JWT scrubber keeps the word it classified as a token"
file = "service_k8s/application/status.py"
old = 'words.append("[redacted token]" if looks_like_jwt else word)'
new = 'words.append(word)'

[[mutation]]
id = "only-very-long-jwt-segments-are-redacted"
intent = "the per-segment length floor rises, so an ordinary projected token is no longer recognized"
file = "service_k8s/application/status.py"
old = 'len(part) >= 8'
new = 'len(part) >= 32'

[[mutation]]
id = "redaction-is-a-no-op"
intent = "redact returns its input, removing the backstop entirely"
file = "service_k8s/application/status.py"
old = 'return _strip_jwt(_strip_bearer(_strip_pem(text)))'
new = 'return text'

[[mutation]]
id = "an-issuer-rotation-is-reported-under-a-different-reason"
intent = "the rotation reason mapping changes, so the condition names the wrong trigger"
file = "service_k8s/application/status.py"
old = 'IssueReason.ISSUER_ROTATION: "IssuerRotation",'
new = 'IssueReason.ISSUER_ROTATION: "Rotation",'

[[mutation]]
id = "an-issuer-rotation-explains-itself-differently"
intent = "the rotation detail text changes, so the published explanation stops matching the trigger"
file = "service_k8s/application/status.py"
old = 'IssueReason.ISSUER_ROTATION: "the configured issuer changed",'
new = 'IssueReason.ISSUER_ROTATION: "the issuer was rotated",'

[[mutation]]
id = "rotation-is-reported-for-every-action-except-an-issuance"
intent = "from_action inverts its action test, so waiting looks like rotating and rotating looks quiet"
file = "service_k8s/application/status.py"
old = 'rotating = action.reason if isinstance(action, Issue) else None'
new = 'rotating = action.reason if not isinstance(action, Issue) else None'

[[mutation]]
id = "the-failure-message-drops-the-word-consecutive"
intent = "the failing message changes, so a total is mistaken for a run"
file = "service_k8s/application/status.py"
old = '+ " consecutive attempts"'
new = '+ " attempts"'

# ---------------------------------------------------------------------------
# Cluster Compatibility Surfaces (round L): crd.py + resize.py
# ---------------------------------------------------------------------------
# ---------------------------------------------------------------------------
# crd.py — unsigned format normalization
# ---------------------------------------------------------------------------

[[mutation]]
id = "normalize-handles-only-uint32"
intent = "only the 32-bit unsigned format is recognized, so a uint64 field keeps a format the API server rejects"
file = "service_k8s/infrastructure/crd.py"
old = '        if value.get("format") in ("uint32", "uint64"):'
new = '        if value.get("format") in ("uint32",):'

[[mutation]]
id = "normalize-rewrites-format-instead-of-removing-it"
intent = "the unrecognized format is replaced rather than deleted, so structural OpenAPI still sees a format key"
file = "service_k8s/infrastructure/crd.py"
old = '            del value["format"]'
new = '            value["format"] = "integer"'

[[mutation]]
id = "normalize-loses-the-unsigned-contract"
intent = "the removed format is not replaced by a non-negative bound, so an unsigned field silently accepts negatives"
file = "service_k8s/infrastructure/crd.py"
old = '                value["minimum"] = 0'
new = '                value["minimum"] = -1'

[[mutation]]
id = "normalize-overwrites-a-tighter-minimum"
intent = "the guard against clobbering an existing bound is dropped, so `minimum: 1` is loosened to 0"
file = "service_k8s/infrastructure/crd.py"
old = '            if "minimum" not in value:'
new = "            if True:"

[[mutation]]
id = "normalize-does-not-recurse-into-mappings"
intent = "recursion stops at the top level, so a nested property keeps its unsigned format"
file = "service_k8s/infrastructure/crd.py"
old = "        for child in value.values():"
new = "        for child in ():"

[[mutation]]
id = "normalize-does-not-recurse-into-arrays"
intent = "list children are skipped, so a format inside `items` survives"
file = "service_k8s/infrastructure/crd.py"
old = "    elif isinstance(value, list):"
new = "    elif False:"

# ---------------------------------------------------------------------------
# crd.py — the CEL null guard
# ---------------------------------------------------------------------------

[[mutation]]
id = "cel-guard-strips-only-spaces"
intent = "whitespace normalization degrades to a space-only strip, so a tab-separated null comparison evades the guard and produces an uninstallable CRD"
file = "service_k8s/infrastructure/crd.py"
old = '    clean_rule = "".join(rule.split())'
new = '    clean_rule = rule.replace(" ", "")'

[[mutation]]
id = "cel-guard-misses-equality-against-null"
intent = "only the inequality spelling is rejected, so `self.x == null` reaches the API server"
file = "service_k8s/infrastructure/crd.py"
old = '    if "!=null" in clean_rule or "==null" in clean_rule:'
new = '    if "!=null" in clean_rule:'

[[mutation]]
id = "cel-guard-misses-inequality-against-null"
intent = "only the equality spelling is rejected, so the documented failure mode itself gets through"
file = "service_k8s/infrastructure/crd.py"
old = '    if "!=null" in clean_rule or "==null" in clean_rule:'
new = '    if "==null" in clean_rule:'

# ---------------------------------------------------------------------------
# crd.py — attaching rules
# ---------------------------------------------------------------------------

[[mutation]]
id = "attach-raises-on-a-crd-without-a-spec"
intent = "a CRD with no spec mapping raises instead of attaching nothing"
file = "service_k8s/infrastructure/crd.py"
old = '''
    if not isinstance(spec, dict):
        return 0'''
new = '''
    if not isinstance(spec, dict):
        raise CelRuleError("no spec")'''

[[mutation]]
id = "attach-raises-on-a-non-list-versions"
intent = "a malformed versions field raises instead of attaching nothing"
file = "service_k8s/infrastructure/crd.py"
old = '''
    if not isinstance(versions, list):
        return 0'''
new = '''
    if not isinstance(versions, list):
        raise CelRuleError("bad versions")'''

[[mutation]]
id = "attach-replaces-the-previous-rule"
intent = "a second rule overwrites the first instead of accumulating"
file = "service_k8s/infrastructure/crd.py"
old = '        rules.append({"rule": rule, "message": message})'
new = '        rules[:] = [{"rule": rule, "message": message}]'

[[mutation]]
id = "attach-counts-versions-only-once"
intent = "the returned count saturates at one, so a caller cannot notice a CRD that lost a version"
file = "service_k8s/infrastructure/crd.py"
old = "        attached += 1"
new = "        attached = 1"

[[mutation]]
id = "attach-always-reports-nothing-attached"
intent = "the count is discarded, so a caller asserting non-zero sees a silent no-op as success"
file = "service_k8s/infrastructure/crd.py"
old = "    return attached"
new = "    return 0"

# ---------------------------------------------------------------------------
# crd.py — the YAML 1.1 quoter
# ---------------------------------------------------------------------------

[[mutation]]
id = "quoter-drops-y-from-the-boolean-spellings"
intent = "the single-letter YAML 1.1 spellings are forgotten, so a `y` default arrives as a boolean"
file = "service_k8s/infrastructure/crd.py"
old = 'BOOLEAN_LIKE: Final[tuple[str, ...]] = ("y", "yes", "n", "no", "on", "off")'
new = 'BOOLEAN_LIKE: Final[tuple[str, ...]] = ("yes", "no", "on", "off")'

[[mutation]]
id = "quoter-drops-off-from-the-boolean-spellings"
intent = "the off spelling is forgotten, which is the exact value a disabled feature flag carries"
file = "service_k8s/infrastructure/crd.py"
old = 'BOOLEAN_LIKE: Final[tuple[str, ...]] = ("y", "yes", "n", "no", "on", "off")'
new = 'BOOLEAN_LIKE: Final[tuple[str, ...]] = ("y", "yes", "n", "no", "on")'

[[mutation]]
id = "quoter-ignores-sequence-entries"
intent = "only mapping values are quoted, so a boolean-like string inside a list is left bare"
file = "service_k8s/infrastructure/crd.py"
old = '        elif trimmed.startswith("- "):'
new = "        elif False:"

[[mutation]]
id = "quoter-ignores-mapping-values"
intent = "only sequence entries are quoted, so `enabled: on` is left bare"
file = "service_k8s/infrastructure/crd.py"
old = '        if ": " in trimmed:'
new = "        if False:"

[[mutation]]
id = "quoter-becomes-case-sensitive"
intent = "the case fold is dropped, so YAML 1.1's `Yes` and `On` spellings are missed"
file = "service_k8s/infrastructure/crd.py"
old = "        if scalar is not None and scalar.lower() in BOOLEAN_LIKE:"
new = "        if scalar is not None and scalar in BOOLEAN_LIKE:"

[[mutation]]
id = "quoter-splits-on-the-last-colon"
intent = "the scalar is taken after the last `: ` rather than the first, so prose ending in a boolean word is mangled"
file = "service_k8s/infrastructure/crd.py"
old = '            scalar = trimmed.split(": ", 1)[1]'
new = '            scalar = trimmed.split(": ")[-1]'

[[mutation]]
id = "quoter-miscomputes-the-prefix"
intent = "the retained prefix is one character short, so quoting eats the character before the scalar"
file = "service_k8s/infrastructure/crd.py"
old = "            prefix_len = len(line) - len(scalar)"
new = "            prefix_len = len(line) - len(scalar) - 1"

[[mutation]]
id = "quoter-drops-a-trailing-newline"
intent = "the document's trailing newline is not restored, so the rewritten manifest loses its final line ending"
file = "service_k8s/infrastructure/crd.py"
old = "    if trailing_newline:"
new = "    if False:"

[[mutation]]
id = "quoter-invents-a-trailing-newline"
intent = "every document gains a trailing newline, so a fragment is silently reshaped"
file = "service_k8s/infrastructure/crd.py"
old = '    trailing_newline = yaml_text.endswith("\n")'
new = "    trailing_newline = True"

# ---------------------------------------------------------------------------
# resize.py — the suffix table
# ---------------------------------------------------------------------------

[[mutation]]
id = "ki-is-a-decimal-thousand"
intent = "the binary kibi suffix is scaled by 1000, the classic Kubernetes quantity bug"
file = "service_k8s/infrastructure/resize.py"
old = '    ("Ki", 1024),'
new = '    ("Ki", 1000),'

[[mutation]]
id = "mi-is-off-by-one"
intent = "the mebi multiplier is wrong by a byte"
file = "service_k8s/infrastructure/resize.py"
old = '    ("Mi", 1048576),'
new = '    ("Mi", 1048577),'

[[mutation]]
id = "gi-is-off-by-one"
intent = "the gibi multiplier is wrong by a byte, which a grow/noop comparison would mis-classify"
file = "service_k8s/infrastructure/resize.py"
old = '    ("Gi", 1073741824),'
new = '    ("Gi", 1073741823),'

[[mutation]]
id = "ti-is-off-by-one"
intent = "the tebi multiplier is wrong by a byte"
file = "service_k8s/infrastructure/resize.py"
old = '    ("Ti", 1099511627776),'
new = '    ("Ti", 1099511627775),'

[[mutation]]
id = "pi-is-off-by-one"
intent = "the pebi multiplier is wrong by a byte"
file = "service_k8s/infrastructure/resize.py"
old = '    ("Pi", 1125899906842624),'
new = '    ("Pi", 1125899906842623),'

[[mutation]]
id = "ei-is-scaled-decimally"
intent = "the exbi suffix is given the decimal exa multiplier, collapsing the Ei/E distinction at the largest scale"
file = "service_k8s/infrastructure/resize.py"
old = '    ("Ei", 1152921504606846976),'
new = '    ("Ei", 1000000000000000000),'

[[mutation]]
id = "k-is-a-binary-kibi"
intent = "the decimal kilo suffix is scaled by 1024, inverting the binary/decimal distinction"
file = "service_k8s/infrastructure/resize.py"
old = '    ("k", 1000),'
new = '    ("k", 1024),'

[[mutation]]
id = "m-is-a-binary-mebi"
intent = "the decimal mega suffix is scaled by 1024^2"
file = "service_k8s/infrastructure/resize.py"
old = '    ("M", 1000000),'
new = '    ("M", 1048576),'

[[mutation]]
id = "g-is-a-binary-gibi"
intent = "the decimal giga suffix is scaled by 1024^3, so `1G` and `1Gi` become indistinguishable"
file = "service_k8s/infrastructure/resize.py"
old = '    ("G", 1000000000),'
new = '    ("G", 1073741824),'

[[mutation]]
id = "t-is-a-binary-tebi"
intent = "the decimal tera suffix is scaled by 1024^4"
file = "service_k8s/infrastructure/resize.py"
old = '    ("T", 1000000000000),'
new = '    ("T", 1099511627776),'

[[mutation]]
id = "p-multiplier-collapses"
intent = "the peta suffix loses its multiplier entirely"
file = "service_k8s/infrastructure/resize.py"
old = '    ("P", 1000000000000000),'
new = '    ("P", 1),'

[[mutation]]
id = "e-multiplier-collapses"
intent = "the exa suffix loses its multiplier entirely"
file = "service_k8s/infrastructure/resize.py"
old = '    ("E", 1000000000000000000),'
new = '    ("E", 1),'

# ---------------------------------------------------------------------------
# resize.py — quantity parsing
# ---------------------------------------------------------------------------

[[mutation]]
id = "bare-count-accepts-python-integer-spellings"
intent = "the ASCII-digit floor is widened, so Python's underscore and sign spellings parse as byte counts a cluster would reject"
file = "service_k8s/infrastructure/resize.py"
old = '    if not s or not all(c in "0123456789" for c in s):'
new = '    if not s or not all(c in "0123456789_+" for c in s):'

[[mutation]]
id = "empty-quantity-is-not-detected-early"
intent = "the empty check no longer matches, so an empty quantity falls through to a different, less specific refusal"
file = "service_k8s/infrastructure/resize.py"
old = '    if s == "":'
new = '    if s == " ":'

[[mutation]]
id = "quantity-is-not-trimmed"
intent = "surrounding whitespace is no longer stripped, so a manifest value with a stray space stops parsing"
file = "service_k8s/infrastructure/resize.py"
old = "    s = qty.strip()"
new = "    s = qty"

[[mutation]]
id = "mantissa-slice-is-off-by-one"
intent = "the numeric head loses its last character, so `1.5Gi` is read as `1.`"
file = "service_k8s/infrastructure/resize.py"
old = "            head = s[: len(s) - len(suffix)].strip()"
new = "            head = s[: len(s) - len(suffix) - 1].strip()"

[[mutation]]
id = "negative-quantities-are-accepted"
intent = "the negativity floor drops below zero, so a negative declaration becomes a negative byte count"
file = "service_k8s/infrastructure/resize.py"
old = "            if value < 0.0:"
new = "            if value < -1.0:"

[[mutation]]
id = "scaled-quantity-is-truncated-not-rounded"
intent = "the scaled value is truncated toward zero instead of rounded, losing a byte on any fraction above a half"
file = "service_k8s/infrastructure/resize.py"
old = "            return round(value * multiplier)"
new = "            return int(value * multiplier)"

[[mutation]]
id = "empty-quantity-message-changes"
intent = "the empty-quantity refusal no longer says what was wrong"
file = "service_k8s/infrastructure/resize.py"
old = '        raise QuantityError("empty storage quantity")'
new = '        raise QuantityError("bad storage quantity")'

[[mutation]]
id = "invalid-mantissa-message-changes"
intent = "the refusal stops naming the numeric part as the problem"
file = "service_k8s/infrastructure/resize.py"
old = '                    "invalid numeric part in storage quantity " + repr(qty)'
new = '                    "bad storage quantity " + repr(qty)'

[[mutation]]
id = "negative-message-changes"
intent = "the refusal stops naming negativity as the problem"
file = "service_k8s/infrastructure/resize.py"
old = '                raise QuantityError("negative storage quantity " + repr(qty))'
new = '                raise QuantityError("bad storage quantity " + repr(qty))'

[[mutation]]
id = "unrecognized-message-changes"
intent = "the bare-count refusal stops naming the offending string"
file = "service_k8s/infrastructure/resize.py"
old = '        raise QuantityError("unrecognized storage quantity " + repr(s))'
new = '        raise QuantityError("bad storage quantity")'

# ---------------------------------------------------------------------------
# resize.py — classification
# ---------------------------------------------------------------------------

[[mutation]]
id = "grow-swallows-the-equal-case"
intent = "a size that already matches is classified as a grow, so every reconcile issues a pointless patch"
file = "service_k8s/infrastructure/resize.py"
old = "    if desired_bytes > current_bytes:"
new = "    if desired_bytes >= current_bytes:"

[[mutation]]
id = "equal-sizes-are-classified-as-a-shrink"
intent = "the equality test is inverted, so an already-correct volume is reported as an unsupported shrink"
file = "service_k8s/infrastructure/resize.py"
old = "    if desired_bytes == current_bytes:"
new = "    if desired_bytes < current_bytes:"

[[mutation]]
id = "shrink-kind-token-changes"
intent = "the shrink kind's wire token changes, so a caller matching on it silently stops matching"
file = "service_k8s/infrastructure/resize.py"
old = '    SHRINK_UNSUPPORTED = "shrink-unsupported"'
new = '    SHRINK_UNSUPPORTED = "shrink_unsupported"'

[[mutation]]
id = "noop-kind-token-changes"
intent = "the no-op kind's wire token changes"
file = "service_k8s/infrastructure/resize.py"
old = '    NOOP = "noop"'
new = '    NOOP = "no-op"'

[[mutation]]
id = "shrink-detail-says-shrinking-is-possible"
intent = "the shrink explanation is inverted into an assertion that Kubernetes can shrink a bound PVC"
file = "service_k8s/infrastructure/resize.py"
old = '    "desired size is smaller than current; Kubernetes cannot shrink a bound "'
new = '    "desired size is smaller than current; Kubernetes can shrink a bound "'

[[mutation]]
id = "grow-loses-its-current-size"
intent = "a grow action no longer carries the size it is growing from"
file = "service_k8s/infrastructure/resize.py"
old = '''
            ResizeKind.GROW,
            current_bytes=current_bytes,'''
new = '''
            ResizeKind.GROW,
            current_bytes=None,'''

[[mutation]]
id = "decide-noop-loses-its-reason"
intent = "the no-op classification stops explaining itself"
file = "service_k8s/infrastructure/resize.py"
old = '''
            desired_bytes=desired_bytes,
            detail="already at desired size",'''
new = '''
            desired_bytes=desired_bytes,
            detail="",'''

[[mutation]]
id = "unparseable-current-is-labelled-desired"
intent = "the failing side is mislabelled, so an operator debugs the wrong value"
file = "service_k8s/infrastructure/resize.py"
old = '            detail="current quantity " + repr(current) + ": " + str(exc),'
new = '            detail="desired quantity " + repr(current) + ": " + str(exc),'

[[mutation]]
id = "unparseable-desired-is-labelled-current"
intent = "the failing side is mislabelled in the other direction"
file = "service_k8s/infrastructure/resize.py"
old = '            detail="desired quantity " + repr(desired) + ": " + str(exc),'
new = '            detail="current quantity " + repr(desired) + ": " + str(exc),'

# ---------------------------------------------------------------------------
# resize.py — the plan
# ---------------------------------------------------------------------------

[[mutation]]
id = "patch-targets-limits-not-requests"
intent = "the patch writes the wrong resource stanza, so nothing resizes and nothing errors"
file = "service_k8s/infrastructure/resize.py"
old = '    return {"spec": {"resources": {"requests": {"storage": desired}}}}'
new = '    return {"spec": {"resources": {"limits": {"storage": desired}}}}'

[[mutation]]
id = "patch-uses-the-wrong-field-name"
intent = "the patched field is renamed, so the one mutable field is never touched"
file = "service_k8s/infrastructure/resize.py"
old = '{"storage": desired}'
new = '{"size": desired}'

[[mutation]]
id = "plan-ignores-the-name-filter"
intent = "the name filter is bypassed, so PVCs the caller excluded are planned anyway"
file = "service_k8s/infrastructure/resize.py"
old = "        if not name_filter(pvc.name):"
new = "        if False:"

[[mutation]]
id = "plan-inverts-the-name-filter"
intent = "the filter selects exactly the PVCs the caller meant to exclude"
file = "service_k8s/infrastructure/resize.py"
old = "        if not name_filter(pvc.name):"
new = "        if name_filter(pvc.name):"

[[mutation]]
id = "plan-treats-unparseable-as-a-grow"
intent = "an unparseable current size falls through to the patch path, so a PVC of unknown size is patched"
file = "service_k8s/infrastructure/resize.py"
old = "        if action.kind == ResizeKind.UNPARSEABLE:"
new = "        if False:"

[[mutation]]
id = "plan-treats-a-shrink-as-a-grow"
intent = "a shrink falls through to the patch path, so Kubernetes is asked to do the one thing it cannot"
file = "service_k8s/infrastructure/resize.py"
old = "        elif action.kind == ResizeKind.SHRINK_UNSUPPORTED:"
new = "        elif False:"

[[mutation]]
id = "plan-treats-a-noop-as-a-grow"
intent = "an already-correct PVC falls through to the patch path and is patched on every reconcile"
file = "service_k8s/infrastructure/resize.py"
old = "        elif action.kind == ResizeKind.NOOP:"
new = "        elif False:"

[[mutation]]
id = "plan-patches-a-non-expandable-class"
intent = "the expansion gate is removed, so a PVC whose StorageClass forbids expansion is patched anyway"
file = "service_k8s/infrastructure/resize.py"
old = "            if not expandable:"
new = "            if False:"

[[mutation]]
id = "plan-assumes-a-classless-pvc-is-expandable"
intent = "a PVC with no bound StorageClass is assumed expandable"
file = "service_k8s/infrastructure/resize.py"
old = '''
            if pvc.storage_class is None:
                expandable = False'''
new = '''
            if pvc.storage_class is None:
                expandable = True'''

[[mutation]]
id = "plan-defaults-an-unknown-class-to-expandable"
intent = "a StorageClass absent from the expansion map defaults to allowed instead of refused"
file = "service_k8s/infrastructure/resize.py"
old = "                expandable = allow_expansion.get(class_name, False)"
new = "                expandable = allow_expansion.get(class_name, True)"

[[mutation]]
id = "plan-mislabels-a-classless-pvc"
intent = "the placeholder for a missing StorageClass names a real class instead"
file = "service_k8s/infrastructure/resize.py"
old = '                class_name = "<none>"'
new = '                class_name = "default"'

[[mutation]]
id = "plan-inverts-the-expansion-explanation"
intent = "the refusal claims the class does allow expansion"
file = "service_k8s/infrastructure/resize.py"
old = '                    + " does not allow volume expansion; recreate the PVC/StatefulSet manually"'
new = '                    + " allows volume expansion"'

[[mutation]]
id = "plan-patches-during-a-dry-run"
intent = "the dry-run gate is removed, so a rehearsal mutates the cluster"
file = "service_k8s/infrastructure/resize.py"
old = "            elif dry_run:"
new = "            elif False:"

[[mutation]]
id = "plan-reports-a-real-patch-as-not-patched"
intent = "a patch that happened is reported as skipped, so a caller re-patches forever"
file = "service_k8s/infrastructure/resize.py"
old = "                        patched=True,"
new = "                        patched=False,"

[[mutation]]
id = "plan-dry-run-claims-it-patched"
intent = "the dry-run detail claims the patch was applied"
file = "service_k8s/infrastructure/resize.py"
old = '                        detail="dry run: would patch spec.resources.requests.storage",'
new = '                        detail="patched spec.resources.requests.storage",'

[[mutation]]
id = "plan-noop-outcome-loses-its-reason"
intent = "the per-PVC no-op outcome stops explaining itself"
file = "service_k8s/infrastructure/resize.py"
old = '''
                    patched=False,
                    detail="already at desired size",'''
new = '''
                    patched=False,
                    detail="",'''

[[mutation]]
id = "plan-reverses-the-outcome-order"
intent = "outcomes no longer follow input order, so a caller zipping them against its own list mispairs every row"
file = "service_k8s/infrastructure/resize.py"
old = "    return tuple(outcomes)"
new = "    return tuple(reversed(outcomes))"
